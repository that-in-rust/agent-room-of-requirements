# Plan for 23 Mar - Mon


# Learning Rust via Apache Iggy

``` text

- What is asyc_trait::async_trait?
    - async_trait::async_trait is a macro that allows you to write async traits in Rust. It is a macro because it generates code at compile time.




```

# OSS Ideas for the day

``` text

1. So many open source repositories exist, but we don't have their dependency graphs. I think they should be standardized across the industry, and we should create a data set for open source repositories' dependency graphs. That would be really cool. 
    - Might need, though, some kind of a database which will be native to open source repositories. Is there a database which can play this role? I don't know

2. 


```