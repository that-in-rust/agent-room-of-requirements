# Threejs React Visualization Patterns

**Decision supported.** This section helps decide how to frame a three-generation 3D-frontend lineage in one reference. The seed no seed line conveys that this theme aggregates the archive's whole 3D-frontend lineage, a 2196-line January-2026 Three.js idiom anchor, a 709-line Parseltongue-specific coder agent from the 202602 sweep, and the 202604 distillation into a 66-line skill entrypoint with two small reference files, three generations of the same practice at three compression levels.

**Recommended default and causal basis.** Consult the skill entrypoint for ownership decisions and descend to the older generations only for concrete pattern code. The lineage exists because one product need, rendering Parseltongue's diff graphs in a browser, kept getting re-expressed as the authors' skill format matured, from encyclopedic idiom anchor to project agent to lane-model skill.

**Operating conditions and assumptions.** All five files remain at their archive paths, checksums recorded this session. This reference covers the React-plus-Three.js visualization practice, backend graph production belongs elsewhere in the corpus.

**Failure boundary and alternatives.** Reading the three generations as redundant copies misses that they answer different questions, the anchor answers how Three.js is written, the agent answers how this app is built, and the skill answers who owns which state, conflating them loses the layer each was designed for. Bounded alternatives and recoveries: the react and design themes for non-3D frontend questions.

**Counterexample, gotchas, and operational comparison.** The two entrypoint-role sources are not twins, unlike the retainer family the 202602 agent and 202604 skill are different documents at different abstractions, byte comparison across sweeps proves nothing here. Good: taking the lane model from the skill and the store code from the agent knowingly. Bad: quoting the anchor's class-based SceneManager as current guidance for an R3F app. Borderline: using the agent's version table as a stack recommendation today, dated pins, verify each.

**Verification, evidence, and uncertainty.** Confirm generation-specific claims against the specific file each cites. All five files read with line counts and checksums this session. Whether a newer generation exists outside the archive is unrecorded.

**Second-order consequence.** The 202604 distillation kept exactly one idea from each ancestor and discarded the rest, from the anchor it kept keep per-frame work out of React, from the agent it kept the stack and the adapter discipline, and everything else, naming tables, code samples, version pins, was pushed down into reference files or dropped, a live demonstration of what its authors judged essential.

**Revision decision.** Open with the three-generation inventory and the compression trajectory from 2196 lines to 66.

**Retained seed evidence.** The exact theme title and framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide which of five diverging sources anchors which claim class. The seed five rows list paths without recording the topology measured this session, one 2196-line legacy anchor, one 709-line project agent, and one 202604 skill package of entrypoint plus two references totaling 140 lines, three distinct documents plus two satellites, no byte twins anywhere, the corpus's first mapped family whose generations all diverge.

**Recommended default and causal basis.** Cite the 202604 skill for current doctrine and name the generation explicitly when quoting older code. The divergence follows from purpose, each sweep rewrote rather than recopied because the format itself was evolving, unlike the retainer family whose reference core crossed sweeps untouched.

**Operating conditions and assumptions.** The generation ordering holds, inferred from sweep dates and the skill's reference-use pointers. Provenance for this theme's claims, the Parseltongue product context is background, not a mapped source.

**Failure boundary and alternatives.** Applying the twin heuristic from other themes would search for equality that does not exist and misread its absence as decay, here divergence is the design, each generation superseding by rewriting. Bounded alternatives and recoveries: treating the anchor as canonical for its size, depth is not currency.

**Counterexample, gotchas, and operational comparison.** The anchor is dated 2026-01-30 inside its own header, the one source here that dates itself, its version-history section makes staleness checkable in a way the sweep-dated files are not. Good: lane-model claims cited to the skill, disposal code cited to the anchor with its date. Bad: presenting the agent's zustand-4 pin as current ecosystem fact. Borderline: citing the checklist file for interaction rules, current and terse, its parent skill is the entry route.

**Verification, evidence, and uncertainty.** Re-run the three checksums and confirm the chain ordering at each audit. Checksum and line-count runs plus full or structural reads this session. Exact authorship sequence between the anchor and the agent is inferred from dates.

**Second-order consequence.** The five sources form a supersession chain with satellites, anchor to agent to skill, and citation should follow recency for doctrine but specificity for code, the newest file carries the current ownership model while the oldest carries the only disposal and instancing implementations, freshness and depth living at opposite ends of the chain.

**Revision decision.** Annotate the rows with line counts, checksums, the generation ordering, and the no-twins topology.

**Retained seed evidence.** The five local rows and six external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/react-threejs-coder-01.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202604/react-threejs-coder-01/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202604/react-threejs-coder-01/references/force-graph-scene-checklist.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202604/react-threejs-coder-01/references/react-threejs-patterns.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/idiomatic-references-202602/Idiom-ThreeJS-Visualization-Patterns.md | local_corpus_source_material | local_corpus_sourced_fact | historical idiomatic pattern corpus |
| https://react.dev/learn | external_research_source_material | external_research_sourced_fact | primary React learning and component model documentation |
| https://www.typescriptlang.org/docs/handbook/intro.html | external_research_source_material | external_research_sourced_fact | TypeScript language model for React applications |
| https://threejs.org/docs/ | external_research_source_material | external_research_sourced_fact | primary Three.js API documentation |
| https://react.dev/learn | external_research_source_material | external_research_sourced_fact | component-driven frontend behavior reference |
| https://threejs.org/docs/ | external_research_source_material | external_research_sourced_fact | visual scene API documentation |
| https://docs.github.com/actions | external_research_source_material | external_research_sourced_fact | delivery verification and automation reference |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which lineage patterns deserve default-tier ranking on this evidence. The seed three corpus-process rows stand where the lineage's own priorities rank, the ownership split leads, React for structure, query layer for remote data, store for durable app state, scene layer for mutable objects and per-frame math, the one rule every generation restates, then the adapter discipline, typed transforms between transport payloads and scene entities, then disposal, the anchor's critical-for-memory pattern that the newer generations compress into event cleanup checks.

**Recommended default and causal basis.** Classify every piece of state into a lane before writing any component, and let the lane dictate the container. The ownership split ranks first because every listed anti-pattern across all three generations is one of its violations, the giant canvas component, store writes per hover, raw payloads into meshes, scene objects recreated per render, one rule whose negations fill three documents.

**Operating conditions and assumptions.** The app is React-hosted 3D, the split presumes both worlds are present. Ranking the lineage's internal patterns, corpus-process patterns rank in the seed rows.

**Failure boundary and alternatives.** Ranking by code volume would promote the anchor's shader and animation sections, but the skill's distillation already voted, what survived compression is what mattered, the scoreboard follows the survivors. Bounded alternatives and recoveries: the seed's process-tier scoreboard for corpus workflow ranking.

**Counterexample, gotchas, and operational comparison.** Transient hover is the boundary case every generation flags, it looks like app state and belongs to the scene lane, the skill says not every transient pointer movement into Zustand, the most common mislane in practice. Good: hover state in a ref inside the scene layer, selection in the store. Bad: a global store write on every animation frame. Borderline: camera position mirrored into the store on interaction end for persistence, a deliberate lane crossing, document it.

**Verification, evidence, and uncertainty.** Spot-check ranking claims against all three generations at audit. The skill workflow, patterns file, and anchor anti-pattern sections read this session. Real defect frequencies behind the anti-pattern lists are unrecorded.

**Second-order consequence.** The lane model is the skill generation's genuine addition, not a compression of anything older, the agent had stores and hooks but no doctrine of three state species, server, app, scene, with different ownership rules each, the lineage's one idea that moved forward instead of shrinking.

**Revision decision.** Present the three-pattern core, ownership split, typed adapters, and lifecycle disposal.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `threejs_react_visualization_patterns` | 95 | default_adoption_pattern_tier | Source Map First: Load local threejs react material before synthesizing visualization patterns recommendations. |
| `threejs_react_visualization_patterns` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `threejs_react_visualization_patterns` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what problem this whole lineage actually solves and by what mechanism. The seed generic corpus formulas stand where the lineage's thesis is stated in the skill's overview line, the hard part is keeping declarative UI, scene mutation, async data, and performance in the right lanes, naming the domain's core tension, React's declarative rerender model and Three.js's mutable per-frame world are opposed paradigms sharing one page, and the discipline is boundary management, not framework mastery.

**Recommended default and causal basis.** Resolve every design question by asking which paradigm owns the object and keep per-frame work out of React's render path. The tension is structural, React reconciles trees on state change while a 60fps scene mutates objects thousands of times between changes, letting either paradigm govern the other's territory produces the two canonical failures, rerender storms and stale scenes.

**Operating conditions and assumptions.** Performance matters, a static scene rendered once tolerates paradigm mixing that a live graph cannot. The thesis governs the React-Three.js boundary, pure Three.js technique belongs to the anchor's deeper sections.

**Failure boundary and alternatives.** The naive integration puts scene objects in React state, every frame becomes a state update, every update a reconciliation, the app shell rerendering at animation frequency, the failure the agent's guardrails and the anchor's section 8.2 both name. Bounded alternatives and recoveries: canvas-only or React-only architectures, which dissolve the tension by abandoning one paradigm's benefits.

**Counterexample, gotchas, and operational comparison.** The boundary is invisible in working code until scale arrives, small scenes run fine with scene objects in state, the failure activates with data volume, which is why the checklist file interrogates degradation explicitly. Good: a frame handler mutating mesh positions from a ref while React renders panels. Bad: setState per tick driving mesh transforms. Borderline: R3F's declarative scene graph, the boundary moved inside a library, the lanes still apply beneath it.

**Verification, evidence, and uncertainty.** Audit designs for paradigm-ownership answers before implementation. The skill overview, guardrails, and anchor anti-patterns read this session. The scale threshold where mixing fails varies by scene and device, unmeasured here.

**Second-order consequence.** The thesis explains the lineage's compression direction, as the authors internalized the boundary discipline the code samples became unnecessary, the 66-line skill is not a summary of the 2196-line anchor but a claim that once the lanes are right the rest is ordinary engineering, expertise shrinking documentation.

**Revision decision.** State the two-paradigm tension thesis with the lane model as its resolution.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `threejs_react_visualization_patterns` contains 5 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Threejs React Visualization Patterns comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide how five documents at three abstraction levels divide one theme's questions. The seed heading signals name section titles while the working map is the supersession chain with satellites, enter at the 202604 skill for workflow and lanes, descend to its patterns reference for the directory shape and layer split, to its checklist for graph-specific interrogations, drop to the 202602 agent for runnable store, hook, canvas, adapter, and test code, and reach the anchor only for deep Three.js mechanics, disposal, pooling, instancing, shaders.

**Recommended default and causal basis.** Descend the chain only as far as the question requires and cite the stop that answered. The chain shape follows the compression history, each generation delegating downward what it stopped restating, the skill's own reference-use section prescribes exactly this descent order for its two satellites.

**Operating conditions and assumptions.** The archive layout holds, the skill's relative links to its satellites resolve only in the 202604 tree. Navigation across this theme's five documents, their content doctrine lives in the later sections.

**Failure boundary and alternatives.** Entering at the anchor buries a reader in 2196 lines of class-based patterns predating the React integration question entirely, the newest sixty-six lines orient faster than the oldest two thousand. Bounded alternatives and recoveries: heading search within each file, workable, the chain explains which file to search.

**Counterexample, gotchas, and operational comparison.** The agent's code is Parseltongue-specific, workspace stores, diff events, snake_case API fields matching a Rust backend, lifting it into other apps means stripping product vocabulary the patterns are demonstrated in. Good: lanes from the skill, store shape from the agent, disposal from the anchor, one question each. Bad: implementing from the anchor's pooling class in an R3F app unquestioned. Borderline: using the agent's Playwright specs as e2e templates, sound shape, product-specific selectors.

**Verification, evidence, and uncertainty.** Trace answers to their chain stop at review. All five documents read or structurally mapped this session. None about structure, headings and line ranges were enumerated.

**Second-order consequence.** The map has a hidden seam, the anchor's section 11 already sketched R3F integration in January 2026 while the later generations never mention R3F, so the oldest document holds the newest-looking technique, supersession chains preserve orphaned foresight at their tails and a reader who never descends never finds it.

**Revision decision.** Annotate the map with the descent order, skill, satellites, agent, anchor, and each stop's question class.

**Retained seed evidence.** The five local source rows with titles, heading signals, and usage roles remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/react-threejs-coder-01.md | React + Three.js + TypeScript Coder Agent | React + Three.js + TypeScript Coder Agent; Tech Stack; Project Structure; 4-Word Naming Convention (TypeScript Adaptation); Functions (camelCase, 4 words); Components (PascalCase, 3-4 words) | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202604/react-threejs-coder-01/SKILL.md | react-threejs-coder-01 | React Threejs Coder 01; Overview; Workflow; Default Rules; Reference Use; Output Contract | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202604/react-threejs-coder-01/references/force-graph-scene-checklist.md | Force Graph And Scene Checklist | Force Graph And Scene Checklist; Interaction; Data Volume; Rendering; Reliability | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202604/react-threejs-coder-01/references/react-threejs-patterns.md | React Three.js Patterns | React Three.js Patterns; Core Separation; Recommended Shape; Good Defaults; Anti-Patterns | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/idiomatic-references-202602/Idiom-ThreeJS-Visualization-Patterns.md | Idiom-ThreeJS-Visualization-Patterns.md | Idiom-ThreeJS-Visualization-Patterns.md; Three.js Idiomatic Patterns Reference (LLM Context Anchor); Section 1: The Four-Word Naming Convention (Three.js Adaptation); 1.1 Scene Objects (PascalCase, 4 units); 1.2 Functions (camelCase, 4 words); 1.3 Animation/Update Functions | historical idiomatic pattern corpus |

## External Research Source Map

**Decision supported.** This section helps decide which external claims this theme owns and how each decays. The seed six inherited rows point at React, TypeScript, Three.js, and Actions docs while the lineage's real external surface is its dated pin table, React 18, TypeScript 5, react-force-graph-3d 1.24, zustand 4, tanstack-query 5, tailwind 3, vite 5, all frozen in the 202602 agent, plus the anchor's self-declared version 1.0.0 of 2026-01-30, the fastest-aging theme the corpus has evolved from this archive.

**Recommended default and causal basis.** Verify any version-specific claim against current ecosystem docs before reuse and reuse lane doctrine freely. Frontend stacks major-version on yearly clocks, so a pin table from early 2026 is a snapshot by design, the agent even labels its stack table with versions where the retainer family's sheets carried nothing datable at all.

**Operating conditions and assumptions.** The stack families persist, a wholesale ecosystem shift would age even the doctrine. External dependencies and their decay clocks, the boundary rule keeps all URLs unretrieved candidates.

**Failure boundary and alternatives.** Treating the pin table as a current recommendation imports whatever migrations React, zustand, and vite have shipped since, the anti-decay move is reading the pins as the tested-together set of their date, not as advice. Bounded alternatives and recoveries: the seed's inherited doc-site rows, retained as the verification destinations.

**Counterexample, gotchas, and operational comparison.** React-force-graph-3d is the pin most likely to matter, it is the niche dependency with the least stable API surface among the seven, and the agent's canvas component is written against its 1.24 ref methods. Good: checking tanstack-query's current major before reusing the query-layer advice. Bad: scaffolding a new app from the 2026 pin table unexamined. Borderline: reusing the tailwind class strings in the agent's components, low-risk surface, still versioned.

**Verification, evidence, and uncertainty.** Date-stamp any pin-dependent claim at each refresh. The pin table and anchor version header read this session, no URLs fetched. Which pins have since broken is exactly what was not retrieved.

**Second-order consequence.** Decay concentrates by generation and inversely with abstraction, the skill's lane doctrine is nearly timeless, the agent's code decays at library speed, and the anchor's core Three.js patterns sit between, scene-graph and disposal mechanics outliving any React cycle, so the chain's descent order is also its staleness gradient, every step down is a step back in time.

**Revision decision.** Keep the inherited rows and add the pin-table reading with the anchor's self-dating as the freshness anchor.

**Retained seed evidence.** All six external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://react.dev/learn | React documentation | primary React learning and component model documentation | external_research_sourced_fact |
| https://www.typescriptlang.org/docs/handbook/intro.html | TypeScript handbook | TypeScript language model for React applications | external_research_sourced_fact |
| https://threejs.org/docs/ | Three.js documentation | primary Three.js API documentation | external_research_sourced_fact |
| https://react.dev/learn | React documentation | component-driven frontend behavior reference | external_research_sourced_fact |
| https://threejs.org/docs/ | Three.js documentation | visual scene API documentation | external_research_sourced_fact |
| https://docs.github.com/actions | GitHub Actions documentation | delivery verification and automation reference | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which product failures this theme's sources reject with one voice. The seed three corpus-process rows stand where the lineage maintains the corpus's richest native registry, the giant GraphCanvas.tsx holding fetching, transforms, controls, camera, and panels, global store writes per hover or frame, raw API payloads flowing into mesh creation, scene objects recreated per render, plus the anchor's three dedicated sections, memory leaks from undisposed geometry and orphaned listeners, per-frame allocations and uncached derived metrics, and unbounded render work.

**Recommended default and causal basis.** Review 3D-frontend changes against the four patterns-file bullets first and the anchor's sections for depth. Every row is a lane violation or a lifecycle omission, the two failure families the thesis predicts, paradigm mixing and forgetting that Three.js resources are manually managed underneath a garbage-collected language.

**Operating conditions and assumptions.** The app has a live render loop, static-render apps dodge the per-frame rows. Product-code rejections from the sources, corpus-process rejections stay in the seed rows.

**Failure boundary and alternatives.** The compounding case is the giant canvas, once one component owns everything, each fix touches everything, hover churn rerenders the fetch logic, and the component becomes unrefactorable at exactly the scale that demands refactoring. Bounded alternatives and recoveries: the checklist file's question form of the same registry for review-time use.

**Counterexample, gotchas, and operational comparison.** Disposal failures are invisible in short sessions, memory grows across scene swaps and long exploration, the leak only observable in exactly the usage patterns developers test least. Good: a review catching a mesh construction inside a component body. Bad: shipping a canvas component that owns its own fetching. Borderline: a temporary giant component during a spike, acceptable scaffolding if the lane split lands before data volume does.

**Verification, evidence, and uncertainty.** Confirm registry rows trace to their source sections at audit. The patterns file bullets and anchor sections 8.1-8.3 read this session. Relative production frequency of these failures is unrecorded.

**Second-order consequence.** The registry is generationally stable in substance while everything else compressed, the anchor's 8.x sections and the patterns file's four bullets name the same failures five sweeps apart, evidence that in this domain the mistakes are structural constants even as the recommended stack churns, anti-patterns aging slower than patterns.

**Revision decision.** Add the native registry with the giant canvas as the gateway failure that breeds the rest.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide how 3D-frontend work gets verified when the canvas itself resists assertion. The seed document verifier text stands where the lineage's verification is behavioral, the skill's step five verifies loading, empty, degraded, and disconnected states, camera reset, selection persistence, event cleanup, and usability under data growth, the checklist file turns graph scenes into fourteen reviewable questions, the agent contributes Playwright specs asserting testids and canvas visibility, and the anchor adds a TDD-specification section with dependency constraints it calls Parseltongue-verifiable.

**Recommended default and causal basis.** Gate merges on the skill's step-five states plus the checklist for graph scenes, with e2e specs pinning the DOM shell. 3D UI defeats assertion-based testing, a wrong scene renders a right-looking canvas element, so the lineage verifies around the canvas, states, lifecycles, and behavior contracts rather than pixels.

**Operating conditions and assumptions.** Testids exist to assert against, the agent's specs presume instrumented components. The lineage's verification doctrine, this document's own structural verification keeps the seed's retained block.

**Failure boundary and alternatives.** Trusting canvas-visible assertions certifies only that WebGL mounted, the agent's own spec comments admit canvas rendering makes direct assertions difficult, the gate must interrogate what surrounds the scene. Bounded alternatives and recoveries: screenshot-diff pipelines, heavier and viable where pixel output is the contract.

**Counterexample, gotchas, and operational comparison.** Selection persistence across data refresh is the subtlest gate item, force-graph data swaps rebuild node objects and identity-based selection silently empties, present in the checklist precisely because it passes every naive test. Good: a merge blocked because disconnect left the canvas frozen with no indicator. Bad: shipping on canvas-is-visible green. Borderline: skipping the WebGL fallback for an internal tool with known hardware, scoped waiver, record it.

**Verification, evidence, and uncertainty.** Check merged changes for state-coverage evidence at review. The skill step five, checklist file, and agent specs read this session. How much of the anchor's TDD section was ever executed is unrecorded.

**Second-order consequence.** The checklist's questions are almost all about degradation and recovery, refresh-stable selection, orientation reset, label culling, teardown, WebGL fallback, verification aimed at the second hour of use rather than the first render, encoding that 3D visualization fails by erosion under load and time, not by failing to appear.

**Revision decision.** State the three-layer gate, state-coverage checks, checklist interrogation, and e2e specs on the DOM shell.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide how agents should consume and emit this lineage's doctrine. The seed four generic bullets stand where this lineage is agent-native in an unusual way, every generation is itself an agent prompt, and the 202604 skill carries a five-part output contract, Rendering Architecture, then State Ownership, then Performance Risks, then Implementation Plan, then Code Changes, ordering an agent's reasoning so that lanes and costs are settled before code exists.

**Recommended default and causal basis.** Hold substantial 3D-frontend agent outputs to the five-section order and reject code-first responses. The contract exists because agents produce code first by default, and in this domain premature code is the giant-canvas generator, so the contract makes the two analysis artifacts and the risk register precede implementation structurally.

**Operating conditions and assumptions.** The task is substantial, the skill itself scopes the contract to substantial work. Agent consumption of this lineage, general agent journaling belongs to the retainer themes.

**Failure boundary and alternatives.** An agent skipping to Code Changes ships the naive integration, the contract's whole value is the forced order, honoring the sections as headers while writing code-first prose defeats it invisibly. Bounded alternatives and recoveries: freeform agent output with post-hoc review, recovering by inspection what the contract gets by construction.

**Counterexample, gotchas, and operational comparison.** The guardrail about pairing with frontend-design is a routing instruction inside the skill, look-and-feel tasks leave this lineage's jurisdiction entirely, an agent applying scene-state doctrine to a styling request is out of scope, not thorough. Good: an agent response whose Performance Risks section changed its own Implementation Plan. Bad: five headers pasted above code written first. Borderline: collapsing sections for a one-line fix, the skill's substantial qualifier permits it, note the judgment.

**Verification, evidence, and uncertainty.** Sample agent outputs for genuine section order at review. The skill's output contract and guardrails read verbatim this session. Contract compliance rates in real agent use are unrecorded.

**Second-order consequence.** The contract is the lane model operationalized for agents, sections one and two force the paradigm-ownership decisions the thesis demands, section three prices them, and only then is code permitted, the same analysis-before-implementation shape the corpus's TDD themes enforce with failing tests, here enforced with document structure.

**Revision decision.** Recast the section around the output contract as the lineage's machine-facing interface.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide what a complete build through this lineage looks like end to end. The seed one generic engineer stands where the natural journey is the Parseltongue build the agent was written for, a developer receives a diff-visualization task, splits state into lanes, types the transport payloads and writes the adapter, builds the store with an actions object, mounts the force-graph canvas with color-coded change types, wires the WebSocket hook with reconnect refs, then walks the checklist, selection across refresh, camera reset, teardown, fallback, and pins the shell with Playwright specs.

**Recommended default and causal basis.** Build in lane order, types, adapter, store, hooks, scene, verification, and resist pixel-first sequencing. The journey works because each artifact isolates one lane, the adapter absorbs transport shape, the store absorbs app state, the canvas component stays a pure scene consumer, and the hooks own their lifecycles, so growth in any lane stays in its lane.

**Operating conditions and assumptions.** The backend contract is stable enough to type, churning APIs push the adapter to absorb even more. Journeys through this lineage's practice, corpus-process journeys stay in the seed lines.

**Failure boundary and alternatives.** The journey's classic derailment is skipping the adapter under schedule pressure, raw snake_case payloads flow into mesh code, and every backend field rename becomes a scene-layer hunt, the shortcut converting one file of mapping into permanent coupling. Bounded alternatives and recoveries: the anchor's journey for non-React scenes, the same discipline without the component layer.

**Counterexample, gotchas, and operational comparison.** The WebSocket hook's resubscribe-on-reconnect ref is the journey's easiest omission, without it a dropped connection silently unsubscribes the user from live updates, the exact degraded state the skill's step five interrogates. Good: a first PR containing only types, adapter, and store with tests. Bad: a demo canvas wired to raw fetch responses growing into the product. Borderline: prototyping visuals on mock data before the adapter exists, legitimate spike if the mock shape is the typed shape.

**Verification, evidence, and uncertainty.** Audit build sequences for lane order in sampled work. The agent's file tree, code samples, and the skill's workflow read this session. Whether the Parseltongue frontend shipped as designed is unrecorded here.

**Second-order consequence.** The journey's order is load-bearing, data model before visuals is the skill's step three, and the agent's file tree encodes it, types and adapters exist before components consume them, reversing the order to get pixels early is the path to the giant canvas, the lineage's journey is a dependency order wearing a narrative.

**Revision decision.** Recast the scenario as the diff-graph build with the lane split as its spine.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The frontend product engineer is starting from a user workflow that must become a typed, accessible, resilient interface and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `threejs_react_visualization_patterns` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing component boundaries, state ownership, loading/error/empty states, and visual verification.
Reference opening trigger: Open this file when the task mentions threejs react visualization patterns, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide which recurring trade-offs this practice prices and in which direction. The seed template rows skip the trade-offs the lineage adjudicates, declarative R3F versus imperative scene management, the anchor sketches both and the later generations stay library-agnostic, store granularity versus selector discipline, zustand with selector hooks versus rerender storms, eager versus lazy scene assets, the checklist's lazily-enabled labels and textures, and depth of typing versus adapter overhead, full backend-mirroring types versus lighter shapes.

**Recommended default and causal basis.** Resolve trade-offs toward frame budget under load and document any convenience-side choice with its scale ceiling. Each trade-off prices the same currency, rerender and frame cost against developer convenience, the lineage consistently spends convenience to protect the frame budget, its one revealed preference across all generations.

**Operating conditions and assumptions.** Interactive performance is a product requirement, throwaway visualizations may spend the other way. Trade-off structure the sources teach, corpus adopt-adapt-avoid rows stay in the seed.

**Failure boundary and alternatives.** Taking the convenient side of every trade-off individually recreates the anti-pattern registry collectively, each choice defensible alone, the sum a store-driven rerendering giant canvas with eager assets. Bounded alternatives and recoveries: the seed's adopt, adapt, avoid framework for corpus-level decisions.

**Counterexample, gotchas, and operational comparison.** Selector discipline decays socially, the first store consumer uses selectors, the fifth grabs the whole store, and zustand permits both silently, the trade-off re-litigated at every new consumer without anyone noticing. Good: choosing lazy labels after measuring hover latency at target node counts. Bad: full-store subscriptions because selectors felt verbose. Borderline: mirroring every backend field into frontend types, maximal safety, the adapter can slim what the UI ignores.

**Verification, evidence, and uncertainty.** Review trade-off choices against frame measurements at target scale. The trade-off surfaces read across all five sources this session. Quantified costs of each convenient choice are unmeasured here.

**Second-order consequence.** The lineage never resolves the R3F question, the anchor sketches R3F integration, the agent uses react-force-graph which wraps Three.js itself, and the skill names neither, leaving library choice deliberately open while making the lane doctrine library-invariant, the rare trade-off a three-generation lineage declined to settle.

**Revision decision.** Add the four trade-off rows with frame-budget-first as the lineage's resolver.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the threejs react visualization patterns pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong threejs react visualization patterns guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which file governs which question class when labels and dates disagree. The seed role labels call the 202602 agent canonical and the 202604 skill legacy, but the measured topology inverts that, the skill package is the newest generation and the agent is the middle one, the seed's roles ranking by size and self-containment while the supersession chain ranks by recency, two defensible hierarchies that disagree.

**Recommended default and causal basis.** Route questions by the per-question hierarchy and treat the seed's single-canon labels as historical. The seed's role assignment predates this session's dating of the generations, a 709-line self-contained agent looks more canonical than a 66-line entrypoint until the reference-use links reveal the entrypoint is the maintained facade of a package.

**Operating conditions and assumptions.** The generation dating holds, sweep folders and the anchor's self-date are the evidence. Precedence among this theme's five sources, the correction is recorded here rather than by rewriting the seed table.

**Failure boundary and alternatives.** Following the seed's roles literally would cite superseded stack pins as canonical doctrine while discarding the current lane model as legacy, the exact inversion this session's evidence corrects. Bounded alternatives and recoveries: rewriting the seed table's roles, rejected, preservation rules keep seed content intact.

**Counterexample, gotchas, and operational comparison.** The two satellite files inherit the skill's currency despite their supporting-detail labels, they are the current generation's own references, more current than the canonical-labeled agent. Good: citing the skill for ownership doctrine while quoting agent code as a dated exhibit. Bad: dismissing the lane model because its file is labeled legacy. Borderline: citing the anchor's R3F sketch as current, oldest file, unsuperseded content, label the age.

**Verification, evidence, and uncertainty.** Test citation targets against the per-question hierarchy at audit. Sweep dates, the anchor's header date, and the skill's link structure this session. The seed labeler's intent is inferred, not recorded.

**Second-order consequence.** The corpus's hierarchy vocabulary assumes one canonical source, but a supersession chain has a moving canon, each generation canonical for what it kept and legacy for what its successor rewrote, the honest hierarchy here is per-question, doctrine to the skill, code to the agent, mechanics to the anchor, no single file wearing the canonical label alone.

**Revision decision.** Keep the seed rows and state the corrected reading, skill package as current doctrine, agent as canonical code exhibit, anchor as deep-technique legacy.

**Retained seed evidence.** The hierarchy rows and the classification vocabulary remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/react-threejs-coder-01.md | canonical primary source | React + Three.js + TypeScript Coder Agent; Tech Stack; Project Structure | What guidance, warning, or example should this source contribute to Threejs React Visualization Patterns? |
| agents-used-monthly-archive/codex-skills-202604/react-threejs-coder-01/SKILL.md | legacy historical source | React Threejs Coder 01; Overview; Workflow | What guidance, warning, or example should this source contribute to Threejs React Visualization Patterns? |
| agents-used-monthly-archive/codex-skills-202604/react-threejs-coder-01/references/force-graph-scene-checklist.md | supporting detail source | Force Graph And Scene Checklist; Interaction; Data Volume | What guidance, warning, or example should this source contribute to Threejs React Visualization Patterns? |
| agents-used-monthly-archive/codex-skills-202604/react-threejs-coder-01/references/react-threejs-patterns.md | supporting detail source | React Three.js Patterns; Core Separation; Recommended Shape | What guidance, warning, or example should this source contribute to Threejs React Visualization Patterns? |
| agents-used-monthly-archive/idiomatic-references-202602/Idiom-ThreeJS-Visualization-Patterns.md | legacy historical source | Idiom-ThreeJS-Visualization-Patterns.md; Three.js Idiomatic Patterns Reference (LLM Context Anchor); Section 1: The Four-Word Naming Convention (Three.js Adaptation) | What guidance, warning, or example should this source contribute to Threejs React Visualization Patterns? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing structure makes lane discipline reviewable over time. The seed the seed names a component ownership map with state, data boundary, render budget, and tests, and the lineage supplies its exact columns, one row per component or hook naming its lane, its data source, adapter, store, or scene ref, its rerender triggers, its per-frame work if any, and its verification hook, the skill's output-contract sections one and two rendered as a standing table.

**Recommended default and causal basis.** Keep the ownership map beside the frontend source and require map updates in any PR that adds state or subscriptions. The artifact exists because lane violations are invisible in file trees, a directory can look like the patterns file's recommended shape while hover churn secretly rerenders the shell, only an ownership map makes the invisible coupling reviewable.

**Operating conditions and assumptions.** The app has enough components for lanes to blur, single-canvas toys need no ledger. One artifact format for any app under this lineage, composable with the seed's completion rules.

**Failure boundary and alternatives.** Without the map, lane review happens by reading every component's subscriptions, unscalable past a handful of files, drift accumulating precisely where nobody re-reads. Bounded alternatives and recoveries: deriving ownership from code on demand, accurate and unaffordable at review frequency.

**Counterexample, gotchas, and operational comparison.** The render-budget column needs numbers, not adjectives, per-frame work recorded as none, bounded, or measured milliseconds, a column of fine entries is the map failing. Good: a PR adding a hover ref updates the map showing scene-lane ownership. Bad: a map last touched three refactors ago. Borderline: generating the map from static analysis of store imports, promising automation, subscriptions hide in hooks.

**Verification, evidence, and uncertainty.** Diff the map against store subscriptions in sampled reviews. The seed artifact line and the skill's contract sections read this session. Map upkeep costs at real team scale are unmeasured.

**Second-order consequence.** The map doubles as the skill's output contract in maintenance form, a new agent task on the app starts by reading the map instead of re-deriving architecture, making it the transition artifact this domain lacks, the retainer family's journal discipline reappearing as an architecture ledger.

**Revision decision.** Concretize the seed's artifact with the lane, data-source, rerender-trigger, frame-work, and verification columns.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: component ownership map with state, data boundary, render budget, and tests.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Threejs React Visualization Patterns | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exhibits transmit the lane model's feel fastest. The seed abstract usage lines stand where the agent file is a complete worked example set, a typed Zustand store with an actions object and selector exports, a WebSocket hook demonstrating ref-based resubscription, a ForceGraph3D component with memoized accessors and camera focus on click, a pure adapter function, and Playwright specs, five runnable exhibits of the lane model in one product's vocabulary.

**Recommended default and causal basis.** Certify understanding by rebuilding one exhibit in a different product vocabulary with imports staying lane-pure. The lineage teaches by exhibit because lane discipline is a property of code shape, prose can name the rule but only the store's selector exports show what protecting rerenders looks like in TypeScript.

**Operating conditions and assumptions.** Learners read TypeScript, all exhibits assume it. Teaching this theme's material, generic React pedagogy belongs to the react theme.

**Failure boundary and alternatives.** Teaching from the exhibits without naming their product coupling produces cargo-culted workspace stores in unrelated apps, the pattern is the shape, the vocabulary is Parseltongue's. Bounded alternatives and recoveries: the anchor's class-based exhibits for the same discipline without React.

**Counterexample, gotchas, and operational comparison.** The WebSocket hook is the deceptive exhibit, its reconnect logic looks incidental but the subscribedWorkspaceRef pattern is the point, state that must survive reconnection lives in a ref, not in render state, easy to strip as boilerplate. Good: a learner's rebuilt store with clean imports and selector exports. Bad: copying workspaceStore.ts wholesale into a new product. Borderline: keeping the snake_case API fields when the new backend is also Rust, coincidentally right, verify the contract.

**Verification, evidence, and uncertainty.** Grade rebuilt exhibits by import purity and selector discipline. All five agent exhibits read in full this session. Exhibit transfer rates to unrelated products are unmeasured.

**Second-order consequence.** The exhibits demonstrate the lanes by their imports, the store file imports no Three.js, the canvas component imports no fetch, the adapter imports neither React nor Three.js, lane purity visible in each file's import block before reading a single function body, the cheapest lane audit available.

**Revision decision.** Anchor the section on the agent's five exhibits with a strip-the-vocabulary exercise.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Threejs React Visualization Patterns after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Threejs React Visualization Patterns as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Threejs React Visualization Patterns only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which numbers show lane discipline holding versus quietly failing. The seed generic indicator lines stand where the lineage implies concrete gauges, frame time under target at target node counts, rerender counts on hover and animation staying scoped to scene components, memory stable across scene swaps and long sessions, reconnect success restoring subscriptions, and state-coverage completeness, loading, empty, degraded, disconnected all rendering something intentional.

**Recommended default and causal basis.** Check rerender scope during development and the full five gauges at data-volume milestones. Each gauge watches one lane's health, frame time the scene lane, rerender scope the store boundary, memory the disposal discipline, reconnect the hook lifecycles, coverage the skill's step five, the metric set is the architecture instrumented.

**Operating conditions and assumptions.** Target scale is defined, gauges without node-count targets measure nothing. Measuring apps built under this lineage, corpus-document metrics stay in the seed lines.

**Failure boundary and alternatives.** Watching only frame rate hides the others until they compound, memory leaks and rerender storms both eventually surface as frame drops, by which time the cause is three lanes away from the symptom. Bounded alternatives and recoveries: user-reported sluggishness, the lagging signal the gauges exist to preempt.

**Counterexample, gotchas, and operational comparison.** Developer hardware masks scene-lane failures, the checklist's degradation questions exist because a scene fine at a thousand nodes on a workstation dies at ten thousand on a laptop, gauge at target scale, not desk scale. Good: a milestone run recording frame time and memory across a 10x data sweep. Bad: shipping on smooth-on-my-machine. Borderline: skipping reconnect gauges for a poll-based app, right scope, note the absent lane.

**Verification, evidence, and uncertainty.** Confirm gauge runs at each scale milestone in project records. The skill's step five and checklist's volume questions read this session. Numeric thresholds are product-specific, the sources give none beyond the seed's p95 line.

**Second-order consequence.** Rerender counting is the leading indicator among the five because lane violations appear there first, a hover wired into the store shows as shell rerenders long before frames drop on developer hardware, React devtools exposing at design time what profilers only catch at load, the cheapest early warning in the domain.

**Revision decision.** Adopt the five-gauge set with lane attribution as the reading discipline.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: primary workflow completion improves without introducing layout, accessibility, or state regressions.
Failure signal: the reference lists tools without describing the user workflow and failure states.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this synthesis is declared faithful. The seed shape items never audit this theme's distinctive claims, the three-generation chain dating with its three checksums, the no-twins topology, the corrected hierarchy reading against the seed's labels, the lane model's presence in the skill, the output contract's five sections, and the anchor's self-date grounding the freshness claims.

**Recommended default and causal basis.** Run the file-level checks mechanically and the supersession scan against sweep folders each cycle. The synthesis rests on generation dating and cross-file structure, so its audit must check the chain, a single-file check would pass while the supersession ordering silently broke under a new sweep.

**Operating conditions and assumptions.** Sweep folders remain the archive's organizing convention. Auditing this reference against its five mapped paths at chain grain.

**Failure boundary and alternatives.** A shape-only audit passes while a 202605 sweep adds a fourth generation, leaving the chain claims, the hierarchy correction, and the currency routing all quietly stale. Bounded alternatives and recoveries: sampled auditing, weak here, the chain claims are few and cheap to check whole.

**Counterexample, gotchas, and operational comparison.** The hierarchy-correction item cuts against the seed table, auditors must check the correction is still warranted rather than reconciling the document to the seed's labels, the one item where seed fidelity and claim fidelity point opposite ways. Good: an audit logging three checksums, live links, contract sections, and no newer react-threejs sweep. Bad: declaring fidelity from seed-shape checks alone. Borderline: skipping the supersession scan in a cycle with no new sweeps, defensible, log the skip.

**Verification, evidence, and uncertainty.** Execute the distinctive items and date each result in the audit record. The claim structure of every distinctive statement in this document. Future sweeps are the moving part all chain claims hedge against.

**Second-order consequence.** This theme's audit has one item no sibling needed, the supersession scan, because a chain's truth is falsified by addition rather than by edit, the existing files can all verify byte-identical while a newer generation elsewhere makes every currency claim wrong, completeness here means checking for what does not yet exist.

**Revision decision.** Add the distinctive items, three checksums, five paths resolving, the skill's two satellite links, the contract's section list, and a newer-generation scan.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Threejs React Visualization Patterns.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a question arriving at the React-Three.js boundary. The seed token-split rows point at generic threejs, react, and visualization neighbors while the operative routes are the ones the sources themselves name, look-and-feel tasks route to the frontend-design pairing the skill's guardrail prescribes, component-logic and state questions without a canvas route to the react and typescript themes, deep scene mechanics stay with the anchor inside this theme, and agent-workflow questions route to the corpus's skill-structure themes.

**Recommended default and causal basis.** Answer boundary and scene-system questions here, pair design references for mixed tasks, and forward pure-React questions explicitly. The skill draws its own jurisdiction line, 3D systems versus product look-and-feel, so routing follows the source's boundary rather than keyword overlap, the rare theme whose primary source contains an explicit routing instruction.

**Operating conditions and assumptions.** The adjacent themes exist in the corpus, the react and design surfaces are separately covered. Sending away what this theme holds only incidentally, destination doctrine is not restated.

**Failure boundary and alternatives.** Keyword routing sends every React question here because the title says react, but this theme owns only the React-Three.js boundary, ordinary component questions answered here arrive wrapped in scene doctrine they do not need. Bounded alternatives and recoveries: for WebGL-without-React questions, the anchor within this theme is already the destination.

**Counterexample, gotchas, and operational comparison.** Force-graph questions are this theme's even when they sound like data questions, layout physics, node identity across refresh, and label culling live in the checklist here, not in a data-visualization neighbor. Good: pairing this theme with design guidance for a polished graph explorer. Bad: answering a button-styling question with lane doctrine. Borderline: state management for a non-3D dashboard, the store patterns transfer, route to react and note the overlap.

**Verification, evidence, and uncertainty.** Sample recent answers for boundary-consistent routing. The skill's guardrail and the theme's jurisdiction read this session. The corpus's exact coverage of the design surface is the queue's fact, not this analysis's.

**Second-order consequence.** This theme is the corpus's only member whose source prescribes pairing rather than replacement, the guardrail says pair with frontend-design, not defer to it, encoding that real tasks straddle the boundary and the two references are meant to run together, routing as composition rather than dispatch.

**Revision decision.** Route by the skill's own boundary, canvas-adjacent systems here, look-and-feel to design, plain React elsewhere.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use React, Three.js, or design references when the task is component logic, canvas behavior, or UI quality.
Adjacent reference 1: consider the threejs adjacent reference when the current task pivots away from threejs react visualization patterns.
Adjacent reference 2: consider the react adjacent reference when the current task pivots away from threejs react visualization patterns.
Adjacent reference 3: consider the visualization adjacent reference when the current task pivots away from threejs react visualization patterns.

## Workload Model

**Decision supported.** This section helps decide how to budget a theme whose sources span two orders of magnitude in size. The seed generic boundary rows stand beside this node's measured units, a 3045-line source read across five files dominated by the 2196-line anchor, per-consultation cost stratified by chain depth, sixty-six lines for doctrine, seven hundred for code exhibits, two thousand for mechanics, and per-cycle upkeep of three checksums plus the supersession scan, cheap mechanics around one expensive optional descent.

**Recommended default and causal basis.** Consult the skill by default, descend by question class, and batch the audit mechanics with the family's other themes. The workload concentrates at the anchor because depth lives there, so the model's job is making the descent optional, most consultations should end at the skill or the satellites in under a hundred fifty lines.

**Operating conditions and assumptions.** The chain's delegation links stay live, a broken satellite link forces premature descent. Budgeting this node's upkeep and consultation, app-building budgets belong to the projects using it.

**Failure boundary and alternatives.** Budgeting every consultation for the full chain makes the theme feel expensive and discourages use, the mispricing that leaves lane doctrine unconsulted while giant canvases get written. Bounded alternatives and recoveries: the seed's bounded-capacity row, retained as the outer limit on any single flow's scope.

**Counterexample, gotchas, and operational comparison.** The anchor's token-budget header says ~4000 tokens when fully loaded, an underestimate against its measured 2196 lines, its own self-description understating the descent cost, budget from measurements, not from headers. Good: an ownership question answered in the skill's sixty-six lines. Bad: reading the full anchor to answer a store-shape question. Borderline: reading the agent end to end before a greenfield build, expensive and coherent, the exhibits reward it once.

**Verification, evidence, and uncertainty.** Check consultation records for depth-proportional reads at review. The five files' line counts measured this session. Real consultation depth distributions are unmeasured.

**Second-order consequence.** The compression history is itself a workload statement, the authors spent three sweeps reducing this domain's always-load cost from 2196 lines to 66, the archive's clearest example of progressive disclosure as deliberate budget engineering, and this reference should preserve that economy rather than flattening it back into one expensive read.

**Revision decision.** Restate the model as stratified consultation with the anchor descent priced separately.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Threejs React Visualization Patterns as a frontend experience operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | interface implementation, visual verification, accessibility review, and browser/runtime behavior checks where perceived reliability is user-visible | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one user flow, three viewport classes, one loading state, one error state, and one keyboard-only path | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include React + Three.js + TypeScript Coder Agent; Tech Stack; Project Structure; 4-Word Naming Convention (TypeScript Adaptation); Functions (camelCase, 4 wo | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is component ownership map with state, data boundary, render budget, and tests | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which invariants must demonstrably hold for this node's claims to stay citable. The seed generic thresholds stand where this node's invariants should, chain integrity, the three checksums and five paths staying resolvable with the skill's satellite links live, generation dating holding against new sweeps, lane-doctrine fidelity keeping the three-lane model and ownership split quoted exactly, contract fidelity keeping the five output sections in order, and pin honesty keeping every version claim labeled with its 2026 date.

**Recommended default and causal basis.** Hold all five invariants at audit cadence with the date-label grep as the standing tripwire. Each invariant guards a distinct corruption, path rot breaks the evidence, a new sweep silently outdates the currency routing, paraphrased lanes drift into generic state advice, reordered contract sections lose the analysis-before-code design, and unlabeled pins become false current claims.

**Operating conditions and assumptions.** Derivative quotations are discoverable, doctrine copied into private prompts evades the diffs. Invariants for this node's claims, app reliability targets live in the seed rows and metrics section.

**Failure boundary and alternatives.** The costliest breach is pin dishonesty, a consumer scaffolding a product from unlabeled 2026 versions inherits every migration since, the failure that turns a faithful archive into bad advice without a byte changing. Bounded alternatives and recoveries: byte-exact quotation only, safe and useless across product vocabularies.

**Counterexample, gotchas, and operational comparison.** Lane-doctrine fidelity permits vocabulary translation, server, app, scene may become query, ui, canvas in a consumer's dialect, the invariant follows the three-species distinction, not the words. Good: an audit finding all links live, dates labeled, and lanes quoted intact. Bad: a derivative citing zustand 4 as the recommendation without its date. Borderline: a two-lane simplification for a fetch-free app, scoped collapse, label it.

**Verification, evidence, and uncertainty.** Review the five invariant series at each corpus checkpoint with dated evidence. The claim structure established across this document. None about current truth, all five invariants were verified during this evolution.

**Second-order consequence.** Pin honesty is the one invariant that decays without any file changing, every other invariant fails by mutation while this one fails by the world moving, so its check is calendar-driven rather than diff-driven, the reliability table's only clock-based row.

**Revision decision.** Publish the five invariants with evidence methods, checksum and link runs, sweep scans, quotation diffs, section-order checks, and date-label greps.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which decay modes need standing probes for a supersession-chain theme. The seed the seed's product-level rows stand while this node's own decay modes go unnamed, chain break, a satellite file moved or a sweep folder reorganized severing the skill's links, silent supersession, a newer generation landing unnoticed, exhibit rot, the agent's code aging past its pins until quoted samples no longer compile, and doctrine flattening, the lane model paraphrased down to keep state organized until the three species blur.

**Recommended default and causal basis.** Run the four probes at audit cadence with the sweep scan first, it invalidates the most. The first two attack the chain topology this synthesis depends on, the third attacks the code layer at library speed, and the fourth attacks through friendly summarization, the four arriving on different clocks, filesystem events, sweep cadence, ecosystem years, and every retelling.

**Operating conditions and assumptions.** Quoted samples remain small enough to compile-check in isolation. Decay of this node's claims, the seed's four product failure rows remain the app-level table.

**Failure boundary and alternatives.** They compound worst through silent supersession, a consumer faithfully citing this reference routes to a superseded skill while a fourth generation sits unmapped, fidelity to the document becoming infidelity to the archive. Bounded alternatives and recoveries: quotation-only reuse, resistant to flattening, helpless against supersession.

**Counterexample, gotchas, and operational comparison.** Doctrine flattening is hardest to probe because summaries are legitimate, the audit question is whether the three-lane distinction survives the summary, not whether the summary is shorter, a flattened retelling can be longer than the original. Good: a probe catching a new 202605 react-threejs sweep and triggering re-evolution. Bad: citing chain currency from a year-old audit. Borderline: a compile check failing on a pin upgrade rather than a source error, expected aging, log it as pin drift.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe and trigger clock. The decay structure consolidated from this document's preceding sections. Sweep cadence going forward is the unknowable clock.

**Second-order consequence.** Exhibit rot has a unique property here, the samples can be mechanically tested, the agent's TypeScript compiles or it does not against a stated stack, making this the corpus's first theme whose staleness is partially decidable by a compiler, an audit no prose theme can run.

**Revision decision.** Add the four node-level rows with probes, link checks, sweep scans, compile checks on quoted samples, and lane-vocabulary audits.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| viewport state breaks flow | layout, loading, or error state was not rendered across target viewports | capture screenshots and check text overlap, focus order, and interaction latency |
| client error lacks recovery | network, auth, or hydration failure leaves user stuck | add retry affordance, empty state, and instrumentation event |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide what automatic responses are allowed and under what bounds. The seed corpus-process bullets stand beside what the sources say about failure response in the domain, the agent's error handling wraps fetches with status checks and error state for the UI, sketches WebSocket reconnection with a three-second delay, five-attempt cap, and exponential backoff note, and the checklist demands bounded camera animation so repeated events cannot thrash, retry discipline appearing at every layer that talks to the world.

**Recommended default and causal basis.** Bound every automatic response, network or visual, with caps and budgets at design time. A live visualization multiplies retry surfaces, HTTP, sockets, and even camera tweens can loop pathologically, so the lineage bounds every automatic response, capped attempts, delays, and animation budgets, backpressure as a design property rather than an incident response.

**Operating conditions and assumptions.** The app has live connections or event-driven animation, static views carry none of these surfaces. Failure-response doctrine for apps under this lineage, corpus-process retry rules stay in the seed bullets.

**Failure boundary and alternatives.** Unbounded reconnection is the canonical failure, a dead backend turns every open tab into a hammering client, the agent's cap and delay existing precisely because the naive loop is three lines and wrong. Bounded alternatives and recoveries: the seed's bounded-retry bullets for corpus-workflow failures, the same shape at document grain.

**Counterexample, gotchas, and operational comparison.** Resubscription after reconnect is the easy omission, the agent's ref pattern exists because a successful reconnect without resubscribe restores the socket but not the data flow, a connection that looks healthy and delivers nothing. Good: reconnect with capped backoff restoring subscriptions and surfacing a degraded banner meanwhile. Bad: a while-true reconnect loop with no cap. Borderline: silent background retry with no UI signal, bounded but opaque, the skill's degraded state asks for the banner.

**Verification, evidence, and uncertainty.** Audit connection and animation code for caps and budgets at review. The agent's error-handling section and checklist questions read this session. The sketched backoff was noted as a comment, its implementation status unrecorded.

**Second-order consequence.** The checklist's bounded-camera question extends retry thinking somewhere unusual, animation, repeated fit-to-view events can queue competing tweens into visual thrash, the same unbounded-response pathology as reconnect storms but rendered at 60fps, the lineage applying one discipline to sockets and cameras alike.

**Revision decision.** State the layer rules, status-checked fetches with UI error states, capped backoff reconnection, and bounded scene animation.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide which conditions become visible and through what rendered surface. The seed the seed's evidence bullets stand while the lineage's own observability surface goes unnamed, connection status as a first-class UI indicator with its own component and testid, console error counts as an e2e signal, rerender inspection via devtools as the lane auditor, and the skill's degraded states doubling as observability, every abnormal condition owning a visible rendering rather than a silent log line.

**Recommended default and causal basis.** Give every liveness and loading condition a rendered representation with a testid, and treat unrepresented states as observability gaps. A visualization's users cannot read logs, so the lineage pushes state visibility into the interface itself, the ConnectionStatusIndicator existing because a live graph without a liveness signal lies whenever the socket drops.

**Operating conditions and assumptions.** The app has states worth signaling, the doctrine scales down to a single spinner. Observability of apps under this lineage, corpus-audit evidence stays in the seed bullets.

**Failure boundary and alternatives.** The silent degraded state is the observability failure, a disconnected canvas showing stale data as if current, users making decisions on a frozen picture, worse than an error screen because nothing looks wrong. Bounded alternatives and recoveries: backend-side telemetry, complementary and invisible to the deciding user.

**Counterexample, gotchas, and operational comparison.** The stale-while-disconnected case needs explicit design, data freshness is a state the UI must represent, not just connection health, the indicator can show connected while the graph shows minutes-old data after a missed resubscribe. Good: a graph pane with connection badge, loading shimmer, and empty-state copy each owning a testid. Bad: a canvas that renders identically connected or dead. Borderline: developer-only debug overlays in production builds, useful telemetry, gate them behind flags.

**Verification, evidence, and uncertainty.** Check that sampled abnormal states have rendered representations in review. The agent's indicator component and testid discipline read this session. Real incident rates behind the indicator's design are unrecorded.

**Second-order consequence.** The lineage makes the interface its own telemetry, the same canvas that resists assertion-based testing becomes observable through the states around it, indicator components, banners, empty states, the untestable center instrumented by its testable frame, one design answering both the verification and observability problems.

**Revision decision.** State the visible-state doctrine, every connection and loading condition rendered, with devtools and console counts as the developer layer.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture viewport, browser, console error count, interaction latency, and screenshot evidence.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide how performance work gets diagnosed and where each remedy applies. The seed the seed's p95-and-screenshots packet stands while the lineage's performance doctrine goes unstated, the agent's five tips, memoized callbacks, store selectors, debounced socket events, list virtualization, and lazy-loading the 3D bundle, the checklist's volume questions, cached derived metrics, culled labels, degrading geometry, reused materials, and the anchor's whole sections on instancing, pooling, and delta-clamped render loops.

**Recommended default and causal basis.** Attribute frame cost to its lane with profiling before touching code, and activate scene optimizations at measured volume thresholds. Performance work here splits by lane, React-side costs answer to memoization and selectors, scene-side costs answer to instancing and culling, and the socket layer answers to debouncing, one budget spent in three currencies.

**Operating conditions and assumptions.** Profiling at target scale is feasible, the method needs representative data volumes. Performance doctrine from the sources, the seed's measurement packet remains the reporting format.

**Failure boundary and alternatives.** Optimizing the wrong lane wastes the effort, memoizing callbacks cannot save a scene drowning in per-frame allocations, and instancing cannot save a shell rerendering on every hover, diagnosis before optimization is the method. Bounded alternatives and recoveries: the anchor's instancing and pooling sections when scene-lane costs dominate.

**Counterexample, gotchas, and operational comparison.** Debouncing socket events trades latency for stability, a debounced diff stream shows changes late, acceptable for visualization, wrong for alerting, the tip carries an unstated product assumption. Good: a profile showing hover rerenders, fixed with selectors, then a second profile. Bad: adding InstancedMesh to fix a store-driven rerender storm. Borderline: pre-emptive virtualization of a list that may grow, cheap insurance, note the unmeasured trigger.

**Verification, evidence, and uncertainty.** Require before-and-after profiles on performance PRs. The agent's tips, checklist questions, and anchor sections read this session. No numeric thresholds exist in the sources beyond the seed's p95 line.

**Second-order consequence.** The lineage's performance advice is scale-conditional throughout, the checklist asks whether geometry can degrade and labels cull at high counts rather than prescribing them, encoding that premature scene optimization is real, the toolkit activates at measured thresholds, not at design time, measurement as the trigger rather than the afterthought.

**Revision decision.** Organize the method by lane, attribute the cost first, then apply the lane's own toolkit.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require interaction latency p95 under 100 ms for local UI actions and no layout overlap across mobile, tablet, and desktop screenshots.
Leading indicator to measure: primary workflow completion improves without introducing layout, accessibility, or state regressions.
Failure signal to watch: the reference lists tools without describing the user workflow and failure states.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what scale this doctrine applies and where its evidence ends. The seed the seed's distributed-execution paragraphs stand while the lineage's own scale limits go unnamed, the lane model governs one React app with one canvas boundary, multi-canvas dashboards multiply scene lanes without new doctrine, the checklist's volume questions bound the force-graph approach itself, beyond some node count no culling saves a naive force layout, and the doctrine says nothing about server-side rendering, native targets, or worker-offloaded physics.

**Recommended default and causal basis.** Replicate lanes per canvas, respect measured force-graph ceilings, and treat SSR and worker questions as outside this lineage's evidence. The sources were written for one product's single-canvas graph explorer, their boundaries are the product's, and the doctrine generalizes exactly as far as the lane structure holds, one declarative shell around one imperative scene.

**Operating conditions and assumptions.** The host remains a client-rendered React app, the sources assume nothing else. Scale limits of the lineage's doctrine, corpus-execution splitting stays in the seed paragraphs.

**Failure boundary and alternatives.** The characteristic overreach is treating the force graph as infinitely scalable because the checklist mentions degradation, degradation strategies extend the ceiling, they do not remove it, past the ceiling the answer is aggregation or a different visualization, not more culling. Bounded alternatives and recoveries: aggregation-first designs, clustering and level-of-detail summaries, when node counts pass interactive ceilings.

**Counterexample, gotchas, and operational comparison.** Worker-offloaded layout is the boundary most likely to matter at scale, force physics on the main thread competes with rendering for the same frame budget, and the sources never mention workers, a real technique beyond this evidence. Good: a dashboard with three canvases each owning lane instances. Bad: promising smooth interaction at any node count because culling exists. Borderline: moving layout to a worker while keeping the lanes, doctrine-compatible and evidence-exceeding, label the extension.

**Verification, evidence, and uncertainty.** At design reviews, check scale claims against the sources' single-canvas evidence. The checklist's volume framing and the sources' product scope read this session. Actual force-graph ceilings vary by hardware and layout, unmeasured here.

**Second-order consequence.** The lane model scales by replication rather than extension, each additional canvas gets its own scene lane with the same rules, so dashboards need lane instances, not new doctrine, the model's real boundary is not canvas count but the assumption that React owns the shell, invalidated only when the host paradigm changes.

**Revision decision.** State the three boundaries, single-canvas doctrine, force-graph ceilings, and the unaddressed rendering targets.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Threejs React Visualization Patterns stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal decay in this node's claims and on which clocks. The seed theme-name queries would surface tutorial noise while this node's staleness has exact homes, the three checksums, the five paths and two satellite links, the sweep folders where a fourth generation would land, the pin table whose seven versions are the fastest-decaying claims, and the anchor's version-history section, the one source that will announce its own revisions.

**Recommended default and causal basis.** Run archive probes at corpus cadence and pin rechecks on a calendar clock, flagging any claim older than the newest major release of its library. A supersession-chain theme decays by addition and by ecosystem motion, so its probes split into archive scans for new generations and dated rechecks of the pin-dependent claims, two clocks, sweep cadence and library release cycles.

**Operating conditions and assumptions.** The archive stays locally searchable and release calendars remain public knowledge. Refreshing this node's evidence and labels, app-level ecosystem tracking belongs to the projects.

**Failure boundary and alternatives.** Undirected refreshing would grep for the theme title and miss a renamed successor skill, the fingerprint that survives renames is the content, lane vocabulary, force-graph plus zustand plus query stack, the terms a rewrite would keep. Bounded alternatives and recoveries: declaring the doctrine layer stable and probing only pins, defensible, the sweep scan is cheap enough to keep.

**Counterexample, gotchas, and operational comparison.** The anchor's own version-history section is a probe target, a 1.1.0 entry appearing there would revise the deep-technique layer without any sweep folder changing, the one in-file supersession channel among the five sources. Good: a refresh logging checksums, live links, no new sweeps, and two pins flagged stale. Bad: logging that threejs react patterns searches returned nothing new. Borderline: auto-flagging every pin at every library release, noisy and safe, batch the review.

**Verification, evidence, and uncertainty.** Record each probe with its date, clock, and the claim it confirmed or updated. The chain topology and pin table established this session. Future sweep organization and release cadences are the hedged unknowns.

**Second-order consequence.** This theme is the corpus's first where refresh urgency is externally driven, the retainer themes could sleep for years, but the pin table's claims decay on the ecosystem's clock regardless of archive stillness, refresh cadence here should follow library release calendars, not corpus audit convenience.

**Revision decision.** Replace name queries with targeted probes, checksum and link runs, sweep scans keyed on the content fingerprint, and calendar-driven pin rechecks.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | threejs react visualization patterns official documentation best practices |
| `github_repository_query_phrase` | threejs react visualization patterns GitHub repository examples |
| `release_notes_query_phrase` | threejs react visualization patterns changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label, chain, and state each claim class here may be reused. The seed label definitions stand without this assignment's distinctive ledger, five mapped paths read fully or structurally mapped, three checksums recorded, the 2196-line anchor read at header and section grain with deep code sections sampled rather than exhaustively read, the generation chain dated from sweep folders and the anchor's self-date, zero external URLs fetched, and the pin table's currency deliberately unverified.

**Recommended default and causal basis.** Before reusing a claim, declare it file fact, archive quotation, synthesis, or unverified-currency quotation, and treat unlabeled claims as inference. A three-generation code lineage splits evidence by layer, doctrine claims rest on full reads of the small files, code claims on the agent's exhibits read whole, and deep-mechanics claims on the anchor's structure with sampled bodies, each layer's verification depth matching its citation weight in this document.

**Operating conditions and assumptions.** The five files stay unedited and the sweep dating holds. Reuse rules for this document's claims, each claim travels with class and verification state.

**Failure boundary and alternatives.** The costliest mislabel would present the pin table or the anchor's 2026 techniques as ecosystem-current, everything here is local fact about what the archive says, nothing is verified against the living ecosystem, current-ness claims would be fabrication. Bounded alternatives and recoveries: an exhaustive anchor read and live pin verification, the natural upgrades, deferred as beyond this assignment's evidence needs.

**Counterexample, gotchas, and operational comparison.** The anchor's sampled-read status matters for reuse, section-level claims about it are verified while any line-level quotation from its unsampled bodies would need a fresh read, the one source where this document's verification depth varies within the file. Good: reusing the lane model as archive-sourced doctrine with its file cited. Bad: citing zustand 4 as the current recommendation. Borderline: recommending the ownership split for new apps, synthesis over strong archive evidence, the inference label travels with it.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares its class cleanly. This assignment's ledger, five reads, three checksums, zero fetches, sampled anchor depth. The pin table's current validity is the deliberately open question.

**Second-order consequence.** This assignment adds a stratum the corpus had not needed before, the deliberately unverified claim, the pin table is faithfully quoted and knowingly unchecked, a label distinct from both fact and inference, the honest state of any archived version claim in a moving ecosystem, quotation with a staleness warning attached.

**Revision decision.** Publish the strata explicitly, file facts and checksums as same-session local evidence, quoted doctrine and code as archive-sourced, the chain reading and hierarchy correction as this evolution's synthesis, and all currency questions as unverified by design.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
