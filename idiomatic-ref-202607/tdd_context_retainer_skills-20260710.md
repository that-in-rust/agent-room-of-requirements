# Tdd Context Retainer Skills

**Decision supported.** This section helps decide how to frame a three-generation memory skill the session itself is using. The seed no seed line conveys that this theme holds three generations of one memory skill, a 112-line Claude persona agent from 202602 with a project-specific Pensieve section, a 93-line Codex orchestrator from the same sweep that extracted the persona's substance into a scripted workflow, and a 100-line 202604 retainer that renamed the family back and added one workflow step, resume from evidence, not memory.

**Recommended default and causal basis.** Treat the 202604 retainer as the living generation and the earlier forms as its documented ancestry. The three forms exist because the same memory doctrine was re-fitted twice, first from persona prose to executable script-plus-contract, then refined with the evidence-resume rule that experience with real resumptions evidently taught.

**Operating conditions and assumptions.** All three files remain at their archive paths, checksums recorded this session. This reference covers the three entrypoints, the shared references, cadence playbook, schema, prompts, belong to their own themes.

**Failure boundary and alternatives.** Reading any single generation as the skill misses the trajectory, the persona defines the responsibilities, the orchestrator mechanizes them, and the retainer hardens the weakest moment, the resume. Bounded alternatives and recoveries: the cadence playbook theme for the checkpoint schedule these entrypoints assume.

**Counterexample, gotchas, and operational comparison.** The family name oscillates, retainer to orchestrator-01 and back to retainer, so name-based identity tracking fails, content identity is what links the generations. Good: citing the evidence-resume step as the 202604 addition with the diff as witness. Bad: quoting the Pensieve crate architecture as skill doctrine. Borderline: treating the persona's responsibilities as current contract, their substance survived, cite the living generation's phrasing.

**Verification, evidence, and uncertainty.** Confirm the generation inventory against all three files before structural claims. All three files read in full with checksums this session. Whether intermediate revisions existed between the sweeps is unrecorded.

**Second-order consequence.** This corpus session is running the newest generation right now, the user asked for the Claude file by its exact archive path, and the work proceeded on the 202604 script, so this assignment documents both the requested ancestor and the operating descendant, the theme closing the loop on its own session's tooling.

**Revision decision.** Open with the three-generation trajectory, persona to script to evidence-hardened script, and the single added step as the visible lesson.

**Retained seed evidence.** The exact theme title and framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide what the effective evidence set is and which comparisons it licenses. The seed three local rows list paths without recording that this theme has no twins, all three documents are distinct, three checksums, three line counts, making it evidence-richer than the recent twin-heavy themes, a genuine three-witness base where differences between files are signal rather than copy noise.

**Recommended default and causal basis.** Run the two comparisons separately, dialect within 202602, generation within the Codex line. Distinctness follows from the generational structure, each file is a real revision event, so every diff between them records an authorial decision rather than a copy artifact.

**Operating conditions and assumptions.** All three paths stay unedited, any change converts a diff finding into a moving target. Provenance for this theme's claims, the shared reference files' provenance lives in their themes.

**Failure boundary and alternatives.** The habit formed on twin themes, collapsing same-named files, would here erase the 202602-to-202604 diff that carries the family's most important lesson, the added resume step. Bounded alternatives and recoveries: treating the Claude file as out-of-family because of its platform, wrong, its content is the family's seed.

**Counterexample, gotchas, and operational comparison.** The 202602 Codex skill is named orchestrator-01 while both same-named files are the Claude original and the 202604 retainer, matching by name pairs the wrong two files for the generation diff. Good: dialect conclusions from the 202602 pair and learning conclusions from the Codex pair. Bad: reading the persona-to-script gap as two years of evolution, both are 202602. Borderline: using the Claude file for current-contract questions, its substance mostly survives, cite the living file.

**Verification, evidence, and uncertainty.** Re-run the three checksums and re-confirm distinctness at each audit. Checksum runs and full reads of all three paths this session. Authoring order within the 202602 sweep is inferred, not recorded.

**Second-order consequence.** The mapped set spans both platform and time, Claude versus Codex at 202602 and Codex against Codex across sweeps, so the table supports two independent comparisons, a dialect diff and a generation diff, and conflating them would misattribute platform packaging choices to temporal learning or vice versa.

**Revision decision.** Annotate the rows with all three checksums, the no-twins finding, and the diff-as-signal reading.

**Retained seed evidence.** The three local rows and three external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/tdd-task-progress-context-retainer.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| agents-used-monthly-archive/codex-skills-202602/tdd-context-retainer-orchestrator-01/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202604/tdd-task-progress-context-retainer/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| https://developers.openai.com/codex/guides/agents-md | external_research_source_material | external_research_sourced_fact | primary agent instruction context model |
| https://docs.github.com/actions | external_research_source_material | external_research_sourced_fact | verification and automation model |
| https://agents.md/ | external_research_source_material | external_research_sourced_fact | general agent instruction format |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which family patterns deserve default-tier ranking on survival evidence. The seed three corpus-process rows stand where the family's own priorities rank, the seven-section journal contract leads as the invariant every generation preserves, from the persona's output format through both scripts unchanged, then the evidence-resume rule, the one hard-won addition, then the never-empty next-steps guardrail, the contract's single most load-bearing field made explicit law.

**Recommended default and causal basis.** Hold the seven sections as the checkpoint floor, resume only from snapshot evidence, and never emit an entry with empty next steps. The contract ranks first because it is the family's genome, phase, tests, implementation, focus, next steps, notes, metrics, surviving two repackagings byte-for-byte in section names while everything around it was rewritten.

**Operating conditions and assumptions.** The ranking reflects cross-generation survival, not usage measurements. Ranking the entrypoints' patterns, the cadence rules rank in the playbook's theme.

**Failure boundary and alternatives.** Ranking the script machinery first mistakes the enforcement for the doctrine, the script exists to make the contract cheap, the contract is what the family actually teaches. Bounded alternatives and recoveries: the seed's process-tier scoreboard for corpus workflow ranking.

**Counterexample, gotchas, and operational comparison.** The contract's seventh section, performance and metrics, is the easiest to leave hollow, the persona's template marks it for any relevant benchmarks, meaning empty is legal there and only there. Good: a checkpoint with all seven sections and a snapshot-based resume restating failing tests. Bad: resuming from memory of where things stood. Borderline: merging notes and metrics for a session with neither, the contract lists them separately, keep the headers even if brief.

**Verification, evidence, and uncertainty.** Spot-check survival claims against all three files at audit. The contract compared across all three generations this session. Real-world compliance rates with the guardrails are unrecorded.

**Second-order consequence.** The evidence-resume step is the only workflow change across the entire family history, one added step in one revision, and it targets resumption, confirming from the author's own revision behavior what the cadence playbook claims doctrinally, that the read-back moment is where memory systems actually fail.

**Revision decision.** Present the three-pattern core, invariant contract, evidence resume, and the next-steps law, with survival evidence.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `tdd_context_retainer_skills` | 95 | default_adoption_pattern_tier | Source Map First: Load local tdd context material before synthesizing retainer skills recommendations. |
| `tdd_context_retainer_skills` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `tdd_context_retainer_skills` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what the family fundamentally claims about session memory. The seed generic corpus formulas stand where the family states its thesis across generations, the persona's closing line, every detail preserved prevents wasted time re-discovering context, and the 202604 addition's sharper form, resume from evidence, not memory, together claiming that continuity is a stored artifact, not a mental state, and must be rebuilt from records each time.

**Recommended default and causal basis.** Treat the journal as the only authoritative session state and the mind's version as a cache requiring validation. Memory feels continuous from inside a session, so the thesis has to be stated as doctrine, the developer or agent who trusts the feeling of remembering resumes on a model that interruption already corrupted.

**Operating conditions and assumptions.** The journal is actually maintained, the thesis holds only where the records exist to resume from. The thesis governs session continuity, what to record and when belongs to the contract and cadence layers.

**Failure boundary and alternatives.** The memory-trusting failure is the naive continuation the 202604 step names dangerous, picking up where you think you left off and executing a plan the evidence no longer supports. Bounded alternatives and recoveries: flow-state reliance, the null practice whose failure modes populated the family's revisions.

**Counterexample, gotchas, and operational comparison.** Evidence-resume applies even when the pause was brief, the 202604 step says before continuing after a pause without a duration threshold, short gaps corrupt selectively and invisibly. Good: a resume that reruns the snapshot and restates the failing tests before any edit. Bad: continuing after lunch from remembered state. Borderline: continuing after a sixty-second interruption without snapshot, the letter says pause, proportionality is a judgment, record it.

**Verification, evidence, and uncertainty.** Audit resumes for evidence-first openings in sampled sessions. The closing line and step five read verbatim this session. The incident history behind the hardening is inferred from the revision, not documented.

**Second-order consequence.** The thesis evolved from positive to negative framing, the persona sells the benefit, preserved context saves time, while the retainer forbids the failure, do not trust memory, and the direction of that shift is itself evidence, doctrine hardens into prohibition where real incidents accumulated.

**Revision decision.** State the two-generation thesis, preserved detail as the persona's economy claim, evidence-over-memory as the retainer's hardened rule.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `tdd_context_retainer_skills` contains 3 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Tdd Context Retainer Skills comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide which document to open for which question about this family. The seed heading signals name sections while the working map is the generational anatomy, the persona carries responsibilities, guidelines, template, self-verification, and a project-specific section, both scripts carry workflow, contract, script usage, guardrails, and context strategy, and the 202604 file differs from its 202602 sibling by exactly one workflow step and the name.

**Recommended default and causal basis.** Consult the scripts for operation, the persona for the reasoning the laws compressed away, and the diff for what experience added. The anatomy shifted when the doctrine was mechanized, persona sections describe what a specialist does, script sections describe what an operator runs, the same knowledge in agent-shaped versus tool-shaped containers.

**Operating conditions and assumptions.** All three files remain at their archive paths unedited. Navigation across this theme's three documents, the three referenced depth files navigate through their own themes.

**Failure boundary and alternatives.** Looking for the self-verification questions in the scripts or the script usage in the persona finds nothing, each container holds sections the other's shape cannot express. Bounded alternatives and recoveries: reading only the living generation, losing the question-to-law mapping that explains why the guardrails exist.

**Counterexample, gotchas, and operational comparison.** The persona's Pensieve section is project contamination, an 8-crate architecture and TPS target from one codebase embedded in an otherwise general skill, the scripts correctly dropped it, quote around it. Good: quoting the guardrails from 202604 and their rationale from the persona's questions. Bad: citing the Pensieve dependency rules as family doctrine. Borderline: quoting workflow steps without naming which script, four of five match, the fifth is generation-specific.

**Verification, evidence, and uncertainty.** Trace section claims to the correct document and generation at review. The section-by-section comparison of all three files this session. None about structure, all documents were read wholesale.

**Second-order consequence.** The persona's five self-verification questions became the scripts' five guardrails, could another developer resume immediately became never leave next steps empty, the why captured became track why not only what, a near one-to-one mapping showing mechanization preserved the checklist by translating questions into laws.

**Revision decision.** Annotate the map with the two anatomies and the one-step generation diff as the family's entire recent changelog.

**Retained seed evidence.** The three local source rows with titles, heading signals, and usage roles remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/tdd-task-progress-context-retainer.md | tdd-task-progress-context-retainer | TDD Session State: [Date/Time]; Current Phase: [Red / Green / Refactor]; Tests Written:; Implementation Progress:; Current Focus:; Next Steps: | local agent-usable source material |
| agents-used-monthly-archive/codex-skills-202602/tdd-context-retainer-orchestrator-01/SKILL.md | tdd-context-retainer-orchestrator-01 | Tdd Context Retainer Orchestrator 01; Workflow; Journal Contract; Script Usage; Guardrails; Context Strategy | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202604/tdd-task-progress-context-retainer/SKILL.md | tdd-task-progress-context-retainer | Tdd Task Progress Context Retainer; Workflow; Journal Contract; Script Usage; Guardrails; Context Strategy | skill entrypoint or reusable agent prompt |

## External Research Source Map

**Decision supported.** This section helps decide which external claims this theme owns and how each decays. The seed three inherited agent-format anchors stand while this family's external surface is thin and dated, the sonnet model pin on the persona, the Pensieve project's crate names, TPS target, and MLX-versus-Candle vocabulary frozen from one 202602 codebase, and the python3 invocation convention the scripts assume, with no URLs or research citations anywhere in the family.

**Recommended default and causal basis.** Reuse the doctrine freely, quarantine the project section, and treat the model pin as dated configuration. Memory doctrine needs no external evidence, but the persona was authored inside a specific project and absorbed its details, so this family's decay surface is contamination rather than dependency.

**Operating conditions and assumptions.** The persona file remains the only contaminated generation, the scripts stayed clean. External dependencies for freshness tracking, all URLs in inherited rows stayed unretrieved.

**Failure boundary and alternatives.** The Pensieve details are the trap, an 8-crate architecture and a 25-TPS target read like worked examples but are one project's facts, reusing them as doctrine imports another codebase's constraints. Bounded alternatives and recoveries: the seed's inherited AGENTS.md and Actions anchors, retained for ecosystem context.

**Counterexample, gotchas, and operational comparison.** The contamination is inside an otherwise reusable file, a consumer loading the whole persona into an agent imports Pensieve rules silently, excerpt rather than include wholesale. Good: citing the persona's guidelines with the project section excised and noted. Bad: adopting the L1-to-L3 dependency rule as TDD doctrine. Borderline: using the Pensieve section as an example of project-integration sections, legitimate meta-use, label it.

**Verification, evidence, and uncertainty.** Check persona reuse for contamination quarantine at audit. The persona's integration section read verbatim this session. Whether the Pensieve project persists anywhere is unknown and irrelevant to the quarantine.

**Second-order consequence.** The contamination marks the persona's birthplace, the skill was evidently extracted from live work on that project, generalized in the scripts by deletion, so the family's own history demonstrates the extraction pattern, working practice first, portable skill second, that much of this archive follows.

**Revision decision.** Keep the inherited rows and add the family's own inventory, the model pin, the project contamination, and the python3 convention, each labeled.

**Retained seed evidence.** All three external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://developers.openai.com/codex/guides/agents-md | OpenAI Codex AGENTS.md guide | primary agent instruction context model | external_research_sourced_fact |
| https://docs.github.com/actions | GitHub Actions documentation | verification and automation model | external_research_sourced_fact |
| https://agents.md/ | AGENTS.md open format | general agent instruction format | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which journal behaviors the family rejects and for whose protection. The seed three corpus-process rows stand where the family names its rejections, vague descriptions that confuse resumption, recording what without why, leaving next steps empty, rewriting journal history instead of appending, resuming from memory after a pause, and big infrequent updates that let context drift between checkpoints.

**Recommended default and causal basis.** Screen journal practice against the registry with append-only violations treated as integrity incidents. Each rejection guards a different consumer of the journal, vagueness cheats the resumer, missing why cheats the refactorer, empty next steps cheat the successor, rewrites cheat the auditor, and memory-resumes cheat everyone including the resumer themselves.

**Operating conditions and assumptions.** The journal file is the single source, parallel unofficial notes recreate the vagueness problem elsewhere. Journal-practice rejections, cadence-timing rejections belong to the playbook theme.

**Failure boundary and alternatives.** The compounding failure is history rewriting, an edited checkpoint invalidates every downstream decision that trusted the original, which is why append-only is a guardrail rather than a preference. Bounded alternatives and recoveries: entry templates enforced by the script, which make several rejections structurally impossible.

**Counterexample, gotchas, and operational comparison.** The small-and-frequent rule fails silently in the other direction too, checkpoint spam with trivial diffs buries the meaningful entries, small means scoped, not empty. Good: correcting a wrong checkpoint with a new dated entry noting the correction. Bad: editing last week's entry to match what actually happened. Borderline: consolidating three trivial checkpoints into one at day's end, violates append rhythm, acceptable only before anything consumed them.

**Verification, evidence, and uncertainty.** Confirm registry rows cite their guardrail or persona guideline at audit. The guardrails and operational guidelines read across generations this session. Relative frequency of these violations in practice is unrecorded.

**Second-order consequence.** Append-only makes the journal evidence-grade, the same property that makes ledgers auditable, and it is what licenses the 202604 evidence-resume rule, you may only resume from records you can trust, and you can only trust records nobody edits, the two guardrails are one design.

**Revision decision.** Add the registry with its per-consumer reading and append-only as the integrity keystone.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide how journal quality gets verified at both ends of the storage. The seed document verifier text stands where the family verifies through its own tooling, snapshot as the resume-time gate restoring phase, failing tests, implementation status, and next action, the persona's five self-verification questions as the write-time gate, and the 202604 restatement discipline, exact failing tests, files in motion, why the next step is next, and dangerous-continuation constraints spoken aloud before work resumes.

**Recommended default and causal basis.** Close every checkpoint against the five questions and open every resume with snapshot plus full restatement. A memory system's verification is bidirectional, entries are checked at write time for resumability and snapshots are checked at read time against reality, the two gates bracketing the storage they protect.

**Operating conditions and assumptions.** The script's snapshot command remains available, the read gate leans on it. The family's verification doctrine, this document's own structural verification keeps the seed's retained block.

**Failure boundary and alternatives.** Gating only one end fails characteristically, write-only gating produces beautiful entries nobody reconciles, read-only gating reconciles against entries too vague to check. Bounded alternatives and recoveries: the corpus verifier command in the seed's retained block for this document's own checks.

**Counterexample, gotchas, and operational comparison.** The persona's question, could another developer resume immediately, is the write gate's whole standard compressed, entries passing it pass everything else, entries failing it fail invisibly until resume day. Good: a resume transcript restating two failing tests, three files, and the constraint blocking the obvious shortcut. Bad: a resume that runs snapshot and immediately edits code. Borderline: restating from a fresh checkpoint written minutes ago, the gate is cheap when context is warm, run it anyway.

**Verification, evidence, and uncertainty.** Check resume records for restatement completeness at sampled sessions. Step five and the self-verification section read verbatim this session. How often restatement catches real misunderstandings is unmeasured.

**Second-order consequence.** The restatement requirement is a comprehension proof, not a recital, restating why the next step is next cannot be done from a skimmed snapshot, it forces the resumer to reconstruct the reasoning chain, the difference between reading state and re-inhabiting it, which is what resumption actually requires.

**Revision decision.** State the two-gate bracket, self-verification questions at write, snapshot-plus-restatement at read.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide how agents should consume this family and through which generation. The seed four generic bullets stand where agent consumption has a live documented instance, this corpus's sessions initialize journals per lane, update on the batch trigger with phase, focus, test results, implementation, next step, and metric flags, and resume across context summaries by reading journal state, running the 202604 script exactly as its usage section prescribes.

**Recommended default and causal basis.** Consume through the script's init, update, and snapshot commands and let the contract ride the flags. The skill fits agents because its script makes the contract structural, an agent supplies flag values and the tool guarantees section completeness, converting quality rules from discipline into interface.

**Operating conditions and assumptions.** The agent platform can execute the script, pure-prompt platforms fall back to the persona form. Agent consumption of the entrypoints, cadence timing for agents lives in the playbook theme.

**Failure boundary and alternatives.** An agent journaling free-form instead of through the script regains every failure the flags prevent, missing sections, empty next steps, unparseable state. Bounded alternatives and recoveries: platform-native memory features, complementary and opaque, the journal stays portable and inspectable.

**Counterexample, gotchas, and operational comparison.** The script enforces sections but not quality within them, an agent can pass empty-looking strings through required flags, the guardrails still bind above the tooling. Good: an agent updating with exact counts and a command-shaped next step, as this assignment does. Bad: an agent narrating progress in chat and calling it journaled. Borderline: an agent on a scriptless platform using the persona's template manually, the contract survives, the enforcement does not, compensate with review.

**Verification, evidence, and uncertainty.** Sample agent journals for script mediation and flag quality. This corpus's journal practice and the script usage sections compared this session. Optimal flag granularity for agent entries is unmeasured.

**Second-order consequence.** The user of this session invoked the family by its Claude ancestor's path while the work runs on the Codex descendant's script, and the handoff between them was seamless because the contract is identical across generations, name and platform churn concealing a stable interface, which is what let the instruction survive its own outdated address.

**Revision decision.** Recast the section around script-mediated consumption with this corpus as the documented deployment.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide what a well-run adoption-and-succession journey looks like. The seed one generic engineer stands where the natural journey is this session's own origin, a user asks an agent to continue predecessors' work using the tdd context retainer skill, citing the Claude file's path, the agent finds the archive holds three generations, adopts the living script, initializes nothing because the corpus journals already exist, and continues sixty-plus assignments across summaries with every resume evidence-based.

**Recommended default and causal basis.** When handed a skill by path, resolve the family's living generation before operating, and inherit existing journals rather than reinitializing. The journey works because the family was built for exactly this, succession between workers who share no memory, the user handing to an agent being the same handoff shape as a developer handing to a colleague.

**Operating conditions and assumptions.** The archive's generations are discoverable, source maps like this one are the resolution aid. Journeys through this family's adoption, the per-checkpoint round trip was the cadence theme's journey.

**Failure boundary and alternatives.** The journey's failure branch was available at the first step, treating the cited ancestor as the only source and operating a persona without its script, losing the enforcement layer the later generations added. Bounded alternatives and recoveries: the cadence theme's interruption round trip for the within-session journey shape.

**Counterexample, gotchas, and operational comparison.** Reinitializing journals on takeover would have severed the chain this journey depends on, init is once per workstream, succession inherits, never restarts. Good: this session, resolving the path to the living script and continuing the existing journals. Bad: a successor starting fresh journals beside the predecessors'. Borderline: a successor switching lanes mid-corpus, workable if the lane journals record the transfer.

**Verification, evidence, and uncertainty.** Audit successions for generation resolution and journal inheritance. This session's own history and the family files read together. How the journey shape generalizes beyond agent successors is untested here.

**Second-order consequence.** The journey demonstrates the family's doctrine at a scale its author never wrote down, continuity held not within a session but across an entire lineage of sessions and summaries, each successor resuming from journals alone, the skill's premise stress-tested by its own deployment as the memory layer of a multi-week evolution.

**Revision decision.** Recast the scenario as the succession journey with the generation-resolution decision as its pivotal beat.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The agent-system designer is starting from a task that needs context selection, tool use, delegation, and verification and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `tdd_context_retainer_skills` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing what context to load, what to offload, when to delegate, and how to prove completion.
Reference opening trigger: Open this file when the task mentions tdd context retainer skills, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide which side wins each recurring memory-skill trade-off and why. The seed template rows skip the trade-offs the family adjudicates, persona richness versus script enforceability, resolved by generation, rich reasoning first, then structural enforcement, generality versus project fit, resolved by extraction, the Pensieve section deleted in mechanization, name stability versus descriptive naming, resolved carelessly, the oscillating names being the family's one clear mistake, and template flexibility versus contract fixity, resolved for fixity across every generation.

**Recommended default and causal basis.** Fix the contract, enforce through tooling, keep the reasoning-rich ancestor accessible, and name families for stability. Each ruling weighs write-side convenience against read-side reliability, and the family consistently pays at write time, fixed sections, mandatory flags, restatement rituals, because its entire value lands at read time.

**Operating conditions and assumptions.** The family's files stay co-archived, the both-in-order answer needs the ancestor kept. Trade-off structure the family teaches, cadence-timing trade-offs belong to the playbook theme.

**Failure boundary and alternatives.** Relaxing contract fixity for convenience, custom sections per session, breaks the property that makes journals machine-consumable and successor-readable, uniformity is the interface. Bounded alternatives and recoveries: single-file skills that compress reasoning into comments, workable and lossy.

**Counterexample, gotchas, and operational comparison.** Contract fixity binds section names, not section depth, a fixed skeleton with proportional flesh is the intent, uniform emptiness satisfies the letter and defeats the purpose. Good: a new memory skill shipping script-enforced with its design rationale archived beside it. Bad: forking the contract per team preference. Borderline: adding an eighth section for a domain need, extension not violation if appended consistently and journaled.

**Verification, evidence, and uncertainty.** Review skill-design decisions for read-side reasoning at retrospectives. The generational sequence and contract stability read this session. Whether the naming oscillation caused real confusion beyond this analysis is unrecorded.

**Second-order consequence.** The persona-to-script sequence resolves a trade-off most skill authors face wrongly, reasoning-rich prose and enforceable structure compete for the same file, and this family's answer was both, in that order, in separate generations, keeping the persona as the why-record while the script became the how-tool, ancestry as documentation strategy.

**Revision decision.** Add the four trade-off rows with the read-side-pays resolver and the naming oscillation flagged as unresolved.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the tdd context retainer skills pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong tdd context retainer skills guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which family file governs which question class across the tree. The seed role labels rank three paths while the operative hierarchy is a family tree with a shared library, the 202604 retainer is the living entrypoint, the 202602 orchestrator its direct predecessor, the Claude persona the common ancestor, and beneath the Codex generations sit three shared references, schema, cadence playbook, and resume prompts, carried identically by both.

**Recommended default and causal basis.** Cite the living entrypoint for current operation, the ancestor for rationale, and the references for their domains regardless of entrypoint generation. The tree shape follows authorship, one doctrine seeded by the persona, mechanized twice with the references extracted once and reused, so depth files outrank no one but serve both script generations equally.

**Operating conditions and assumptions.** The family's files remain co-archived at their sweep locations. Precedence within the retainer family, the references' own content ranks in their themes.

**Failure boundary and alternatives.** Ranking the orchestrator above the retainer by its grander name inverts the succession, names mislead in this family, dates and content decide. Bounded alternatives and recoveries: flat authority across the family, which the naming oscillation makes actively dangerous.

**Counterexample, gotchas, and operational comparison.** The corpus evolved the cadence playbook under its own theme already, so this family's depth layer is partially evolved elsewhere, hierarchy claims here must not double-own it. Good: operating from the 202604 workflow while citing the persona for why next steps must never be empty. Bad: treating orchestrator-01 as the current generation because its name sounds architectural. Borderline: citing the 202602 script's identical sections instead of 202604's, textually safe, cite living anyway.

**Verification, evidence, and uncertainty.** Test citation targets against the tree at audit. The family structure and shared references verified this session. None material, the tree is fully enumerated locally.

**Second-order consequence.** The references are older than the living entrypoint and newer than the ancestor, born in the 202602 mechanization and inherited unchanged by 202604, so the family's stable core is again its depth layer while entrypoints churn, the same shell-versus-core dynamic the cadence theme found, now visible across an entire skill family.

**Revision decision.** State the tree, living entrypoint, predecessor, ancestor, and the shared reference library beneath the Codex line.

**Retained seed evidence.** The hierarchy rows and the classification vocabulary remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/tdd-task-progress-context-retainer.md | canonical primary source | TDD Session State: [Date/Time]; Current Phase: [Red / Green / Refactor]; Tests Written: | What guidance, warning, or example should this source contribute to Tdd Context Retainer Skills? |
| agents-used-monthly-archive/codex-skills-202602/tdd-context-retainer-orchestrator-01/SKILL.md | legacy historical source | Tdd Context Retainer Orchestrator 01; Workflow; Journal Contract | What guidance, warning, or example should this source contribute to Tdd Context Retainer Skills? |
| agents-used-monthly-archive/codex-skills-202604/tdd-task-progress-context-retainer/SKILL.md | legacy historical source | Tdd Task Progress Context Retainer; Workflow; Journal Contract | What guidance, warning, or example should this source contribute to Tdd Context Retainer Skills? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing structure makes a renamed family safely addressable. The seed generic worked-example artifact misses this theme's natural object, a generation resolution guide, a small standing table mapping every known name and path of this family, tdd-task-progress-context-retainer at claude-202602, tdd-context-retainer-orchestrator-01 at codex-202602, tdd-task-progress-context-retainer at codex-202604, to its generation, status, and the living successor, so any future citation by any historical name resolves to current tooling in one lookup.

**Recommended default and causal basis.** Build the guide from the current tree, store it with the audit records, and extend it when sweeps add generations. The naming oscillation guarantees future misaddressing, this very session was pointed at the ancestor's path, and a resolution guide converts each such stale address from an archaeology task into a lookup.

**Operating conditions and assumptions.** The family's naming instability persists, a stabilized name would freeze the guide gratefully. One guide for this family, the pattern generalizes to any archive family with renamed generations.

**Failure boundary and alternatives.** Without the guide, every consumer repeats this session's resolution work, and less careful ones operate whatever generation their stale path lands on. Bounded alternatives and recoveries: resolving per incident from source maps like this one, exactly the repeated work the artifact removes.

**Counterexample, gotchas, and operational comparison.** The guide must record the shared script path too, the orchestrator script under 202604 is what stale addresses ultimately need to reach, resolution ends at tooling, not at documents. Good: a stale 202602 citation resolved to the living script in one lookup. Bad: a guide listing names without status, preserving the ambiguity it exists to kill. Borderline: including the family's reference files in the guide, useful expansion, keep entrypoint resolution primary.

**Verification, evidence, and uncertainty.** Test the guide against each known historical address at audit. The three paths and their relationships established this session. Future sweep naming behavior is what the guide hedges against.

**Second-order consequence.** The guide is this theme's executable summary, the whole generational analysis compressed into the only form future sessions will actually consult at speed, and its existence test is concrete, the next user who cites the Claude path should reach the 202604 script in one hop instead of three file reads.

**Revision decision.** Define the artifact as the name-to-generation resolution table with status and successor columns.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: RED/GREEN transcript with refactor rule and resume checkpoint.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Tdd Context Retainer Skills | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercises transmit the contract's intended grain fastest. The seed abstract usage lines stand where the family ships a complete worked example in both scripts, the feature-a command sequence, init with path and task, update with phase Red, a focus line, a pipe-delimited test triple, an implementation line with file path, a command-shaped next step, and a fixture note, then snapshot, one journal lifecycle in nine flag lines.

**Recommended default and causal basis.** Certify understanding by writing a real feature's init-update-snapshot sequence at the example's grain. The example survived both Codex generations unchanged because it is load-bearing teaching, every contract section appears as a concrete flag value, making it the contract demonstrated rather than described.

**Operating conditions and assumptions.** Learners run the commands rather than read them, the script's output is part of the lesson. Teaching this theme's material, cadence-timing drills belong to the playbook theme.

**Failure boundary and alternatives.** Learning the contract from section names alone produces entries with right headers and wrong grain, the example shows the intended grain, one test triple, one file-scoped implementation line, one executable next step. Bounded alternatives and recoveries: this corpus's own journals as production-grade examples at heavier density.

**Counterexample, gotchas, and operational comparison.** The example's next step, implement minimum handler logic to satisfy validation tests, is imperatively scoped, minimum and to-satisfy bound the work, imitations that write grand next steps miss the TDD-green discipline embedded in the phrasing. Good: an update whose test triple names a real test, its live status, and its expectation. Bad: an update with tests as a prose paragraph in the focus flag. Borderline: multiple test triples in one update after a batch, faithful if each keeps the three-field form.

**Verification, evidence, and uncertainty.** Grade practice sequences against the example's grain at review. The script usage sections read verbatim across both Codex files this session. Transfer from the drill to sustained practice is unmeasured.

**Second-order consequence.** The test flag's format, name pipe status pipe expectation, is a micro-contract inside the contract, forcing every recorded test to carry its identity, its state, and its meaning in one string, the same three fields a resumer needs to rerun and interpret it, format as pedagogy.

**Revision decision.** Anchor the section on the feature-a lifecycle with the pipe-delimited test triple as its finest teaching detail.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Tdd Context Retainer Skills after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Tdd Context Retainer Skills as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Tdd Context Retainer Skills only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which numbers show the family preserving continuity versus performing it. The seed compile-centric indicator misses the family's measurable surface, restatement fidelity, resumes whose restated tests and files match the snapshot, succession depth, how many summary boundaries a workstream has crossed on journals alone, contract completeness rates from script-mediated entries, and stale-address incidents, consumers arriving via outdated family names.

**Recommended default and causal basis.** Log the four measures per workstream and read succession depth as the integrated system score. The family's product is successful resumption, so its metrics live where resumptions succeed or fail, with this corpus's own multi-summary run as the deepest local succession chain to measure.

**Operating conditions and assumptions.** Workstreams are long enough to cross boundaries, short work never exercises the machinery. Measuring this family's operation, cadence-adherence metrics belong to the playbook theme.

**Failure boundary and alternatives.** Without the loop, the family's health is inferred from journal volume, which measures writing, while every failure mode of interest lives in reading. Bounded alternatives and recoveries: write-side volume metrics, measuring effort where the value is not.

**Counterexample, gotchas, and operational comparison.** Restatement fidelity can be theatrically perfect if the resumer copies the snapshot verbatim, the metric wants restatement in the resumer's own operational terms, comprehension, not transcription. Good: a workstream logging twelve boundary crossings with zero reconstruction incidents. Bad: celebrating journal length as continuity health. Borderline: a perfect run with one boundary, promising and underpowered, depth is the denominator.

**Verification, evidence, and uncertainty.** Confirm workstreams log the read-side measures at each cycle. This corpus's own succession record consulted this session. Cross-team baselines for these measures do not exist locally.

**Second-order consequence.** Succession depth is a metric no single session can game, each summary boundary crossed successfully is an independent trial of the whole system, journal quality, contract, resume protocol together, and this corpus's chain of sixty-plus assignments across many summaries is the family's strongest performance datum anywhere in the archive.

**Revision decision.** Adopt the four measures with restatement fidelity as the sharpest quality signal.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: the next run needs fewer clarifications and produces fewer unverifiable claims.
Failure signal: the reference tells agents what to do without defining context budget or escalation rules.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this synthesis is declared faithful. The seed shape items never audit this theme's distinctive claims, the three-distinct-documents finding, the one-step generation diff, the question-to-law mapping, the Pensieve contamination quarantine, the shared-references inheritance, and the live claim that this corpus operates the 202604 script.

**Recommended default and causal basis.** Run the file checks wholesale, the forward sweep scan, and the operating-script verification each cycle. The synthesis rests on cross-file comparisons and one reflexive operational claim, each verifiable cheaply and each breakable by different events, sweep edits, family revisions, or corpus practice drift.

**Operating conditions and assumptions.** The archive remains scannable for new family members by title and content. Auditing this reference against its three mapped paths, the shared references, and the corpus's operating practice.

**Failure boundary and alternatives.** A shape-only audit passes while a new sweep adds a fourth generation, silently obsoleting the living-generation designation this reference asserts throughout. Bounded alternatives and recoveries: sampled auditing, workable for the mapping claims, insufficient for the living designation.

**Counterexample, gotchas, and operational comparison.** Content-based forward scanning is required, not name-based, the family's naming history proves a fourth generation could arrive under any name, scan for the contract's seven sections as the family fingerprint. Good: an audit logging three checksums, the diff, and a no-new-generations scan result. Bad: declaring fidelity from seed-shape checks alone. Borderline: skipping the forward scan when no sweep directory was added, defensible if sweep inventory is itself checked.

**Verification, evidence, and uncertainty.** Execute the distinctive items and date each result in the audit record. The claim structure of every distinctive statement in this document. Future sweep contents are the audit's standing unknown.

**Second-order consequence.** The living-generation designation is this checklist's perishable core, every other claim describes frozen files, but living is a relational status a future sweep revokes without touching any current file, so the audit must scan forward in the archive, not just down at sources, the second forward-looking checklist in the corpus after the family-completeness sideways check.

**Revision decision.** Add the distinctive items, three checksums with distinctness, the diff check, the mapping spot-check, the contamination boundary, and the operating-script verification.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Tdd Context Retainer Skills.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a question arriving at the family's identity layer. The seed token-split rows point nowhere while this theme anchors a family whose depth is distributed, checkpoint timing goes to the cadence playbook theme, journal field schemas and resume prompt wording go to their reference files' themes where evolved, TDD phase semantics go to the TDD method themes, and this theme keeps the entrypoints, the generations, and the family's identity questions.

**Recommended default and causal basis.** Answer generation, contract, and workflow questions here and forward timing, schema, and prompt questions to their owners explicitly. The family's mechanization split one persona into an entrypoint plus three references, so its knowledge is constitutionally distributed and routing is the price of the extraction.

**Operating conditions and assumptions.** The sibling themes remain complete, the cadence theme was finished as assignment 64. Sending away what this theme holds only incidentally, destination doctrine is deliberately not restated.

**Failure boundary and alternatives.** Answering cadence questions here paraphrases a sibling theme's owned content, while routing away generation questions abandons the one theme that can resolve the family's names. Bounded alternatives and recoveries: for memory tooling beyond this family, nothing local, say so rather than stretching.

**Counterexample, gotchas, and operational comparison.** The journal contract appears in the entrypoints and the schema reference both, contract membership questions are this theme's, field formatting questions are the schema's, the same sections split by altitude. Good: answering which generation is current here and forwarding how often to checkpoint to the cadence theme. Bad: explaining trigger families from this theme. Borderline: a question about the test triple format, shown in the entrypoints' example, specified in the schema, answer from the example and cite the schema.

**Verification, evidence, and uncertainty.** Sample recent answers for extraction-consistent routing. The family structure and sibling theme map read across assignments. Whether the remaining reference files have their own pending themes is determined by the queue, not this analysis.

**Second-order consequence.** This theme and the cadence theme form the corpus's second linked pair after the tauri generations, entrypoint here, depth there, from the same family, and the routing between them mirrors the skill's own context strategy, load the entrypoint first, the playbook for long sessions, the corpus again converging on the archive's internal structure.

**Revision decision.** Route by extraction history, whatever the mechanization moved to a reference file routes to that file's theme, whatever stayed in the entrypoints stays here.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use debate, subagent, context, or verification references when the task narrows to a specific agent behavior.
Adjacent reference 1: consider the tdd adjacent reference when the current task pivots away from tdd context retainer skills.
Adjacent reference 2: consider the context adjacent reference when the current task pivots away from tdd context retainer skills.
Adjacent reference 3: consider the retainer adjacent reference when the current task pivots away from tdd context retainer skills.

## Workload Model

**Decision supported.** This section helps decide how to budget a tooled memory practice and its archive-facing audit. The seed symbol-count boundary misprices this node's work, whose units are the sunk 305-line three-file read in minutes, per-adoption generation resolution now reduced to a lookup by the resolution guide, per-workstream journal operation at minutes per checkpoint through the script, and per-audit checks including the forward sweep scan, the first audit in the corpus with an archive-scan component.

**Recommended default and causal basis.** Budget checkpoints at command cost, resolution at lookup cost, and audits with the scan surcharge. The family's operating cost is deliberately minimized by tooling, flags replace formatting, so the recurring human cost concentrates in resolution and audit, both one-time or cyclical rather than per-checkpoint.

**Operating conditions and assumptions.** The script stays functional, tool breakage converts command-priced work back to prose-priced. Budgeting this node's upkeep and the family's operation, cadence-frequency budgets live in the playbook theme.

**Failure boundary and alternatives.** Pricing journal operation at prose-writing rates overestimates it tenfold, the script's whole design is that a checkpoint costs a command, and overestimation becomes the excuse for skipping checkpoints. Bounded alternatives and recoveries: the seed's crate-slice and symbol boundaries, retained as outer limits on any single consultation's scope.

**Counterexample, gotchas, and operational comparison.** The restatement ritual at resume is deliberately not command-priced, it is thinking work the tooling must not absorb, budget resumes at comprehension cost and resist optimizing the one expensive step that exists to be expensive. Good: a workstream budgeting minutes per checkpoint and real thought per resume. Bad: skipping checkpoints because journaling feels heavyweight, against a command-priced reality. Borderline: scripting the restatement into a template, structure helps, prefilled answers defeat the comprehension gate.

**Verification, evidence, and uncertainty.** Check workstream records for cost-category-consistent budgeting at review. The script interface and audit structure from this session. Archive growth rate, which drives the scan surcharge, is outside this theme's control.

**Second-order consequence.** The forward sweep scan is a new audit cost category this theme introduces, proportional to archive growth rather than to source size, and as the corpus documents more families with living generations, that category recurs, audit budgets across the corpus will trend from file-sized toward archive-sized as living designations accumulate.

**Revision decision.** Restate the model in its four units, sunk read, lookup-priced resolution, command-priced checkpoints, and scan-extended audits.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Tdd Context Retainer Skills as a agent workflow operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | agent planning, tool use, context loading, delegation, or skill/plugin execution where bad guidance compounds across long-running sessions | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one primary task, up to 10 source files, up to 3 delegated subtasks, and a written completion audit for every run | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include TDD Session State: [Date/Time]; Current Phase: [Red / Green / Refactor]; Tests Written:; Implementation Progress:; Current Focus:; Next Steps:; Tdd Co | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is RED/GREEN transcript with refactor rule and resume checkpoint | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which invariants must demonstrably hold for this node's claims to stay citable. The seed document-property thresholds stand where this node's invariants should, three-checksum distinctness, generation-attribution fidelity keeping every quote tagged to its file and sweep, living-designation currency for the 202604 retainer, contamination quarantine keeping Pensieve details out of doctrine reuse, and self-instance accuracy for the claim that this corpus operates the family's script.

**Recommended default and causal basis.** Hold all five invariants at audit cadence with the forward scan gating the living designation. Each invariant guards a distinct corruption, checksum drift breaks the diff findings, misattribution scrambles the generational story, a stale living designation misroutes adopters, quarantine leaks import foreign constraints, and self-instance drift falsifies the theme's best evidence.

**Operating conditions and assumptions.** The archive and the corpus's journals both remain inspectable. Invariants for this node's claims, journal reliability in practice belongs to the metrics loop.

**Failure boundary and alternatives.** The costliest breach is living-designation staleness because it propagates, the resolution guide, the routing defaults, and every adopt-the-current-generation instruction all inherit the designation, one stale fact poisoning the theme's most-consulted outputs. Bounded alternatives and recoveries: fact-only invariants, blind to the relational and reflexive claims this theme runs on.

**Counterexample, gotchas, and operational comparison.** Quarantine violations can be constructive, someone may legitimately document the Pensieve section as an example of project-integration design, the invariant polices unlabeled doctrine reuse, not labeled meta-use. Good: an audit logging three checksums, a clean forward scan, and a verified operating script. Bad: a consumer routed to the orchestrator as current by an unrefreshed designation. Borderline: quoting the persona's closing line as family doctrine without a generation tag, its substance survived, tag it anyway.

**Verification, evidence, and uncertainty.** Review the five invariant series at each corpus checkpoint with dated evidence. The claim structure established across this document. None about current truth, all five invariants were verified during this evolution.

**Second-order consequence.** This theme concentrates the corpus's newest invariant types in one place, a forward-looking designation, a reflexive self-instance, and a contamination boundary, none of which classic document-fidelity auditing covers, evidence that as the corpus documents living tooling rather than frozen doctrine, its reliability apparatus is having to grow new organs.

**Revision decision.** Publish the five invariants with evidence methods, checksum runs, tag audits, forward scans, quarantine spot-checks, and operating-script verification.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which decay modes need standing probes for a renamed, living, tooled family. The seed generic rows miss how this node specifically dies, generation confusion, stale names routing consumers to superseded forms, designation rot, the living label surviving a fourth generation's arrival, contamination leakage, Pensieve specifics reused as doctrine through wholesale persona loading, and enforcement bypass, the contract practiced free-form until the script's guarantees quietly lapse.

**Recommended default and causal basis.** Run the four probes at audit cadence with adoption incidents triggering immediate guide updates. The first two attack addressing, the third attacks doctrine purity, and the fourth attacks the structural layer that makes the family's promises cheap to keep, each rooted in a real property, oscillating names, frozen files, embedded project detail, optional tooling.

**Operating conditions and assumptions.** Adoption events are observable, silent adoptions elsewhere in an organization evade every probe. Decay of this node's claims and the family's adoptability, journal-practice decay belongs to the cadence theme's probes.

**Failure boundary and alternatives.** They compound at adoption time, a consumer arriving by stale name, loading the contaminated ancestor wholesale, and practicing its template free-form gets a superseded, contaminated, unenforced version of a family whose living form would have cost one lookup. Bounded alternatives and recoveries: quotation-only reuse with generation tags, resistant to confusion and leakage, helpless against bypass, which lives in behavior.

**Counterexample, gotchas, and operational comparison.** Enforcement bypass often begins as platform necessity, a scriptless environment falls back to the template legitimately, the probe distinguishes compensated fallback, manual review standing in for flags, from plain lapse. Good: a probe catching a new consumer operating the 202602 orchestrator and redirecting in one message. Bad: an agent built on the persona wholesale, Pensieve rules and all. Borderline: a scriptless team using the template with peer review of entries, compensated bypass, record the compensation.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe and trigger point. The decay structure consolidated from this document's preceding sections. Adoption visibility beyond this corpus is the probes' blind spot.

**Second-order consequence.** Every one of this theme's failure modes strikes at adoption rather than operation, the family works well once correctly adopted, so the probes cluster at the front door, and the resolution guide plus this reference are themselves the mitigation layer, documentation as the family's missing front-door tooling.

**Revision decision.** Add the four rows with probes, resolution-guide hit checks for confusion, forward scans for rot, quarantine audits for leakage, and script-mediation sampling for bypass.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| context window loses plan | long-running session forgets early constraints or overwrites user intent | write checkpoint summaries and re-read plan before each destructive step |
| tool fanout outruns budget | parallel actions create conflicts, duplicate work, or unbounded external calls | cap fanout, assign ownership, and require merge/audit checkpoints |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide what a journal-practice failure is allowed to trigger. The seed corpus-process bullets stand beside what the family implies for practice failures, a journal found unresumable at resume time is repaired forward, a fresh evidence-based checkpoint reconstructed from repository state, never backfilled into history, a script failure downgrades practice to the manual template with review compensation until tooling returns, and repeated unresumable journals from one workstream signal grain problems, entries too coarse, addressed by cadence and grain, not by abandoning the contract.

**Recommended default and causal basis.** Repair unresumable journals with one honest reconstruction checkpoint and address root causes at grain, not at contract. The family's append-only law shapes all repair, corrections are new entries, so every failure response reduces to write forward honestly, the ledger discipline applied to its own breakdowns.

**Operating conditions and assumptions.** Repository state is intact, forward repair leans on the other record. Failure-response doctrine for the journal practice, work-level TDD failures belong to the method themes.

**Failure boundary and alternatives.** The erosion path is quiet abandonment, a workstream whose journal failed once at resume concluding the practice does not work and reverting to memory, the exact failure mode the family exists to prevent, now unrecorded. Bounded alternatives and recoveries: the cadence theme's late-marked catch-up entries for missed checkpoints, the sibling repair at smaller scale.

**Counterexample, gotchas, and operational comparison.** The reconstruction checkpoint must declare itself reconstructed, a repaired entry indistinguishable from a live one is a small history rewrite, the lateness-marking rule from the cadence theme applies with the same logic. Good: an unresumable journal repaired by one marked reconstruction entry from test output and diffs. Bad: quietly rewriting the last three entries to match reality. Borderline: abandoning a journal for a genuinely dead workstream, legitimate closure if a final entry records the abandonment.

**Verification, evidence, and uncertainty.** Audit repair records for forward-only, marked reconstructions. The append-only guardrail and repository-evidence relationship read this session. How often forward repair is needed in practice is unmeasured.

**Second-order consequence.** Forward repair from repository state is possible precisely because the journal is not the only record, code and tests carry ground truth, so a broken journal is re-derivable at reconstruction cost, the journal's value was never irreplaceability but cheapness, and repair economics are the practice's real justification restated.

**Revision decision.** State the response rules, forward repair from repository evidence, compensated manual fallback, and grain escalation for recurring unresumability.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide which properties of a session the contract makes queryable. The seed generic records stand beside what the family's contract makes observable, seven fixed sections turning each checkpoint into a structured telemetry frame, phase, test states with identities, implementation locations, current attention, ordered intentions, decision rationale, and performance readings, with explicit identifiers, test names, paths, signatures, error messages, as the frame's required precision.

**Recommended default and causal basis.** Emit checkpoints as full seven-section frames with identifier-grade precision and let the cadence govern frame timing. The contract is an observability schema in disguise, its sections are exactly the state variables of a development session, and fixing them across all checkpoints makes session state queryable over time like any well-schemaed telemetry.

**Operating conditions and assumptions.** Sections keep their canonical names, queryability rides on the fixed vocabulary. Observability through the contract, cadence-driven sampling density was the playbook theme's half.

**Failure boundary and alternatives.** Free-form journaling destroys queryability even when content is complete, the same information scattered across prose cannot answer which checkpoints were in Red or which tests stayed failing longest without a human rereading everything. Bounded alternatives and recoveries: the flight-recorder framing of the cadence theme, the same telemetry read from the sampling side.

**Counterexample, gotchas, and operational comparison.** The notes section is the schema's escape valve, everything unstructured lands there, and monitoring its share of total content is a health check, a growing notes fraction means the schema is being routed around. Good: querying a lane journal for all Red-phase checkpoints touching one file. Bad: a journal whose test states live in narrative sentences. Borderline: extending frames with an eighth section per domain, queryable if added uniformly, journal the extension.

**Verification, evidence, and uncertainty.** Run sample queries against journals at audit to confirm queryability. The contract sections and identifier rules read across generations this session. How often journals get queried rather than read linearly is unmeasured.

**Second-order consequence.** The family splits observability into schema and sampling, this theme's contract defines what each frame contains while the sibling playbook defines when frames are taken, two halves the archive keeps in separate files that only together make sessions observable, and the corpus's paired themes now mirror that split exactly.

**Revision decision.** State the schema framing with the seven sections as state variables and explicit identifiers as the precision floor.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture tool-call count, context files loaded, delegated tasks, retry count, and completion-audit outcome.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide how performance numbers flow through the family's frames. The seed latency packet stands beside the family's performance surface, the contract reserves a permanent performance-and-metrics section in every frame, the workflow lists performance regression discovery among checkpoint triggers, and the persona's guideline records benchmarks established during development, together treating performance numbers as session state to preserve, not gates to enforce.

**Recommended default and causal basis.** Record benchmarks in the metrics section when established and checkpoint immediately on regression discovery. Performance context evaporates between sessions faster than functional context, a benchmark run and not recorded is a benchmark re-run, so the family gives numbers a standing home in the frame schema.

**Operating conditions and assumptions.** The work has a performance dimension, the section legitimately reads not-applicable elsewhere. The family's performance-context rules, measurement methodology and thresholds belong to other themes.

**Failure boundary and alternatives.** Leaving the metrics section perpetually empty on performance-relevant work silently deletes the baseline history a future regression investigation will need. Bounded alternatives and recoveries: external benchmark tracking systems, complementary machine records beside the session-level narrative.

**Counterexample, gotchas, and operational comparison.** The persona's Pensieve TPS target sits adjacent to the general benchmark guideline, quoting the 25-TPS number as the family's example imports the contaminated section, use the guideline, not the project's figure. Good: a checkpoint recording a new baseline with its command and machine context. Bad: a regression discovered, fixed, and never journaled. Borderline: recording relative changes without absolute numbers, preserves the signal, weakens future comparison, note the trade.

**Verification, evidence, and uncertainty.** Sample performance-relevant workstreams for populated metrics sections. The contract, trigger list, and persona guideline read this session. How teams decide performance relevance is unrecorded.

**Second-order consequence.** The family and the cadence playbook state the same performance doctrine from their two halves, the playbook makes regressions a trigger and metrics a handoff item while the contract gives them a permanent field, schema and sampling agreeing independently, an internal consistency that suggests the doctrine was lived rather than theorized.

**Revision decision.** State the three touchpoints, standing section, regression trigger, and benchmark recording, under the memory-not-gate framing.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require tool-call budgets, timeout budgets, and a resumable journal when the workflow exceeds one focused session.
Leading indicator to measure: the next run needs fewer clarifications and produces fewer unverifiable claims.
Failure signal to watch: the reference tells agents what to do without defining context budget or escalation rules.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what scale the practice applies and how it composes upward. The seed task-splitting bounds stand beside the family's own scale limits, one journal per feature or ticket by the workflow's first rule, frames bounded by the seven-section contract, workstreams scaled by adding journals rather than growing them, and the family's authority ending at session-state management, it tracks TDD work and never directs what to build or how to test.

**Recommended default and causal basis.** Open a journal per workstream, compose tiers when workstreams themselves compose, and keep the practice out of build decisions. The one-journal-per-workstream rule keeps each file's state space coherent, mixing features in one journal makes phase and next-steps ambiguous, the sections assume a single workstream's answers.

**Operating conditions and assumptions.** Workstream boundaries are real, artificial splits multiply journals without separating state. Scale limits of the practice, this evolution's own task splitting keeps the seed paragraphs.

**Failure boundary and alternatives.** The monolithic journal is the characteristic scale failure, one file absorbing an entire project until its current phase means nothing and resumers must read all history to find their thread. Bounded alternatives and recoveries: monolithic project logs, readable early and unresumable at scale.

**Counterexample, gotchas, and operational comparison.** The authority boundary cuts both ways, the journal records design decisions without ratifying them, citing a journal entry as approval for a decision confuses memory with governance. Good: this corpus's lane journals plus master journal composition. Bad: one journal spanning every feature of a quarter. Borderline: a journal per developer rather than per workstream, workable for solo streams, breaks on shared features.

**Verification, evidence, and uncertainty.** At each audit, check journal-to-workstream mapping in active deployments. The workflow's first rule and this corpus's tier structure read together. The composition pattern's limits beyond two tiers are untested locally.

**Second-order consequence.** This corpus scaled the family's design to four parallel lanes plus a master journal, per-lane journals for workstream state and a master for corpus-level state, a two-tier extension the entrypoints never describe but their per-workstream rule implies, the local deployment discovering the composition rule the doctrine left implicit.

**Revision decision.** State the three boundaries, journal per workstream, frame per contract, and state-management-only authority.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Tdd Context Retainer Skills stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal decay in this node's claims and on what clocks. The seed theme-name queries would surface generic TDD chatter while this node's staleness has exact homes, the three checksums, the sweep inventory that could add a fourth generation under any name, the contract's seven section names as the content fingerprint for name-blind scanning, the corpus's continued operation of the 202604 script, and the resolution guide's continued accuracy.

**Recommended default and causal basis.** Run the fingerprint scan as the primary forward probe with checksums and script verification as the integrity layer. A renamed family defeats name-anchored monitoring by construction, so this theme's probes must scan by content fingerprint, the stable contract, rather than by any of the three names the family has already used.

**Operating conditions and assumptions.** The archive remains locally searchable, all probes here are offline. Refreshing this node's evidence and labels, journal practice needs no external refresh.

**Failure boundary and alternatives.** Name-anchored probes would have missed both historical renames, and will miss the next one, the family's own history is the argument against the obvious monitoring design. Bounded alternatives and recoveries: declaring the theme stable and unwatched, refuted by two renames in two sweeps.

**Counterexample, gotchas, and operational comparison.** The fingerprint can false-positive on documents quoting the contract, like this reference itself and the corpus journals, the scan needs an exclusion list of known non-generation carriers. Good: a refresh logging three checksums, a clean fingerprint scan minus exclusions, and a verified operating script. Bad: logging that tdd context retainer searches returned nothing new. Borderline: skipping the fingerprint scan when no new sweep directory exists, acceptable if sweep inventory is itself the checked fact.

**Verification, evidence, and uncertainty.** Record each probe with its date and the claim it confirmed or updated. The naming history and contract stability established this session. Future sweep naming and structuring behavior is what the fingerprint hedges against.

**Second-order consequence.** The seven-section contract doubles as the family's genome for detection purposes, any future generation will almost certainly carry it since every generation so far has, so the most stable content in the family is also the best search key, the same core-over-shell probe design the cadence theme reached by title and this theme reaches by structure.

**Revision decision.** Replace name queries with targeted probes, checksum runs, fingerprint scans for the seven-section contract across new sweeps, operating-script verification, and guide-accuracy checks each cycle.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | tdd context retainer skills official documentation best practices |
| `github_repository_query_phrase` | tdd context retainer skills GitHub repository examples |
| `release_notes_query_phrase` | tdd context retainer skills changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label, chain, and state each claim class here may be reused. The seed label definitions stand without this assignment's distinctive ledger, three mapped paths read in full, three distinct checksums recorded, the shared reference files verified present, the generation diff computed directly, the corpus's own journals and script operation consulted as live instance, zero external URLs fetched, and the family's effectiveness supported only by this corpus's uncontrolled deployment.

**Recommended default and causal basis.** Before reusing a claim, declare it per-file fact, tagged quotation, comparative finding, or entangled usage, and treat unlabeled claims as inference. A lineage-plus-self-instance theme carries every evidence class the corpus has developed, per-file facts, tagged quotations, comparative findings, and reflexive observations, making its boundary notes the fullest ledger yet.

**Operating conditions and assumptions.** All three files stay unedited and the corpus's journals remain inspectable. Reuse rules for this document's claims, each claim travels with class, generation tag, and verification state.

**Failure boundary and alternatives.** The costliest mislabel would blur the reflexive stratum, this corpus's successful use of the script is documented usage by an entangled user, the practice's evolver testifying to the practice, not independent validation. Bounded alternatives and recoveries: an independent team deploying the family and reporting, the natural validation upgrade, outside this corpus's reach.

**Counterexample, gotchas, and operational comparison.** Entangled usage still beats no usage, the corpus's succession record is real evidence of operability at depth, the label bounds its comparative force, not its existence. Good: reusing the one-step generation diff as a comparative finding with both checksums. Bad: citing this corpus's smooth operation as independent proof the family works. Borderline: recommending the family on the strength of the succession record, reasonable guidance if the entanglement label travels with it.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares its class cleanly. This assignment's ledger, full reads, three checksums, live-instance consultation, zero fetches. The family's comparative effectiveness against alternatives remains unmeasured anywhere.

**Second-order consequence.** This theme's evidence base is uniquely self-involving, the user cited one source file as the method for this work, the work runs on another, and this assignment evolves both, evaluator, operator, and subject collapsed into one session, which strengthens access, every claim was checkable directly, while demanding the entanglement stay visible on every reused claim.

**Revision decision.** Publish the strata explicitly, texts and checksums as per-file facts, doctrine as generation-tagged quotation, the diffs as this evolution's comparative findings, and the operating experience as entangled documented usage.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
