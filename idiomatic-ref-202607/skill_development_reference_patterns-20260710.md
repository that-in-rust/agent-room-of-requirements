# Skill Development Reference Patterns

**Decision supported.** This section helps decide how to treat a derivative guide that bundles its own ancestor. The seed no seed line states this theme's unusual shape, a 637-line plugin-adapted skill-authoring guide that carries its own 209-line ancestor inside itself, the original generic skill-creator embedded verbatim under references/skill-creator-original.md, so derivation and source travel together in one bundle, with both files' live-tree copies verified byte-identical to the archive.

**Recommended default and causal basis.** Answer plugin-skill questions from the outer guide, generic-workflow questions from the embedded original, and adaptation questions from their explicit deltas. The plugin-dev authors adapted the generic creator for Claude Code plugins and preserved the original as a bundled reference rather than a link, making the derivative self-documenting about its own lineage, every adaptation is checkable against the ancestor without leaving the directory.

**Operating conditions and assumptions.** The live copies stay identical to the archive, divergence reopens the currency caveat. This reference covers the plugin skill-development guide and its embedded ancestor, the sibling plugin-dev skills it points to belong to their own themes.

**Failure boundary and alternatives.** Reading only the outer guide misses which rules are plugin-specific adaptations versus inherited doctrine, and reading only the embedded original teaches workflows, init_skill.py, package_skill.py, that the plugin context explicitly replaces. Bounded alternatives and recoveries: the skill-creator-evaluation theme for the lineage's evaluation-harness descendants, this theme owns the plugin adaptation.

**Counterexample, gotchas, and operational comparison.** The embedded original is dated by embedding, the standalone skill-creator theme holds newer harness-bearing versions of the same lineage, so the original here documents the pre-evaluation-loop generation, not the current generic creator. Good: citing the outer guide for plugin structure and the embedded original for the replaced packaging step. Bad: telling a plugin author to run package_skill.py because the bundled reference says so. Borderline: citing the embedded original for the shared anatomy diagram, both carry it, cite the outer guide as the operative copy.

**Verification, evidence, and uncertainty.** Re-run the two live-versus-archive diffs before relying on the identity claim. Full reads of both documents and two identity diffs this session. Whether plugin-dev revised this skill after 202603 is outside the corpus.

**Second-order consequence.** This is the corpus's only theme where a document ships its own ancestor as a bundled resource, the lineage-index work other themes must assemble from cross-directory diffs arrives here pre-packaged by the source authors themselves, the skill applying progressive disclosure to its own history.

**Revision decision.** Open with the derivative-with-embedded-ancestor structure, the twin identity, and the adaptation inventory as the organizing frame.

**Retained seed evidence.** The exact theme title and reference framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide how much independent weight each of the four listed rows carries. The seed four local rows resolve to two unique documents totaling 846 lines, the archived pair and its live-tree twins verified byte-identical this session, so the effective evidence base is one guide plus its embedded ancestor, with the duplications carrying deployment-identity information rather than independent authority.

**Recommended default and causal basis.** Cite the archived outer guide for plugin doctrine, the embedded original only for ancestor content, and note the live identity when currency matters. The live claude-skills tree ships the same plugin-dev bundle the archive froze, so the twin rows prove the archived guidance is the deployed guidance, the same currency structure the skill-creator-evaluation theme established for its five pairs.

**Operating conditions and assumptions.** Both trees remain in the repository and the diffs stay re-runnable. Provenance for this document's statements, the three external URLs stayed unretrieved and confer no current-fact authority.

**Failure boundary and alternatives.** Counting four independent sources would double-weight every claim, while the subtler error is treating the embedded original as a third independent voice, it shares roughly two hundred lines verbatim with the outer guide's opening sections. Bounded alternatives and recoveries: a lineage-index entry recording both the twin relation and the embedded-ancestor relation in one place.

**Counterexample, gotchas, and operational comparison.** The shared anatomy and process sections appear in both files at different line numbers, quoting by line without naming the file invites citations that resolve into the wrong document. Good: a word-budget claim cited once to the outer guide with the live identity noted. Bad: the anatomy diagram cited to both files as corroboration. Borderline: citing the embedded original for doctrine the outer guide repeats verbatim, harmless, the outer guide is the operative home.

**Verification, evidence, and uncertainty.** Re-run the two identity diffs at each audit cycle. The diff runs and both full reads from this session. Whether the live tree tracks upstream automatically or by manual copy is unrecorded.

**Second-order consequence.** The evidence here has three layers of duplication to untangle, archive-versus-live twins, outer-guide-versus-embedded-ancestor overlap, and this lineage's overlap with the standalone skill-creator theme's sources, each layer answering a different question, deployment currency, adaptation deltas, and generational position respectively.

**Revision decision.** Annotate the mapping with the twin structure, the dated identity diffs, the two-document effective base, and the internal overlap between guide and embedded ancestor.

**Retained seed evidence.** All four local rows and three external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/plugins/plugin-dev/skill-development/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/claude-skills-202603/plugins/plugin-dev/skill-development/references/skill-creator-original.md | local_corpus_source_material | local_corpus_sourced_fact | reference detail file for deeper pattern evidence |
| claude-skills/plugins/plugin-dev/skill-development/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| claude-skills/plugins/plugin-dev/skill-development/references/skill-creator-original.md | local_corpus_source_material | local_corpus_sourced_fact | reference detail file for deeper pattern evidence |
| https://developers.openai.com/codex/guides/agents-md | external_research_source_material | external_research_sourced_fact | primary agent instruction context model |
| https://docs.github.com/actions | external_research_source_material | external_research_sourced_fact | verification and automation model |
| https://agents.md/ | external_research_source_material | external_research_sourced_fact | general agent instruction format |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which authoring patterns deserve default adoption for plugin skills. The seed three corpus-process rows stand where the guide's own patterns rank by leverage, trigger-description discipline ranks first, third-person phrasing with quoted user phrases is the only surface that controls whether the skill loads at all, progressive disclosure second, the 1500-2000 word body budget with detail pushed to references, imperative writing style third, and the study-working-examples pattern, pointing authors at six sibling skills as living templates, ranks as the guide's distinctive teaching move.

**Recommended default and causal basis.** Spend authoring effort in ranking order, description first, disclosure second, style third, with sibling skills as the worked reference. Patterns rank by failure cost, a weak description means the skill never fires regardless of content quality, a bloated body taxes every trigger, style errors merely degrade clarity, so the ranking follows the loading pipeline from trigger to context to comprehension.

**Operating conditions and assumptions.** The skill targets Claude Code plugin auto-discovery, standalone skills weight packaging concerns this guide explicitly drops. Ranking the authoring patterns this guide teaches, evaluation-harness patterns rank under the skill-creator-evaluation theme.

**Failure boundary and alternatives.** Ranking by section prominence would elevate the anatomy diagram, which is shared inheritance, above the trigger-phrase doctrine, which is where the plugin guide concentrates its own added value, three good-versus-bad description examples and a dedicated mistake entry. Bounded alternatives and recoveries: the corpus's own verifier-gate ranking for document artifacts, structural rather than trigger-driven.

**Counterexample, gotchas, and operational comparison.** The third-person description rule is plugin-house style, the embedded original states the same third-person preference but the Codex-side creator in the sibling theme does not, cross-platform authors should treat person as platform convention, not universal law. Good: a description rewritten with quoted trigger phrases before any body editing. Bad: polishing imperative style in an 8000-word body no trigger will ever load efficiently. Borderline: skipping the sibling-skill study for a trivial one-file skill, defensible, the minimal-skill layout is sanctioned.

**Verification, evidence, and uncertainty.** Spot-check ranking claims against the mistake catalog and checklist ordering. Both documents' structure compared this session. How often live plugin authors follow the ranking is unrecorded.

**Second-order consequence.** The guide encodes its priorities structurally, the two failure modes it ranks highest each get a named mistake, paired good-bad examples, a checklist section, and a best-practices bullet, four redundant enforcement surfaces, while style errors get two, redundancy of enforcement inside the document is a readable map of what its authors feared most.

**Revision decision.** Present the failure-cost ranking with the guide's own evidence, its mistake catalog dedicates its first and largest entries to descriptions and body bloat in exactly this order.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `skill_development_reference_patterns` | 95 | default_adoption_pattern_tier | Source Map First: Load local skill development material before synthesizing reference patterns recommendations. |
| `skill_development_reference_patterns` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `skill_development_reference_patterns` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what standard admits content into a skill. The seed generic corpus formulas stand where the theme's thesis is that a skill is an onboarding guide written for another instance of Claude, both documents state it in the same words, skills transform a general-purpose agent into a specialized one by carrying procedural knowledge no model can fully possess, and every rule in the guide serves that reader, trigger phrases so the right instance finds it, lean bodies so loading stays cheap, imperative form so instructions parse as procedure.

**Recommended default and causal basis.** Filter every candidate sentence through the beneficial-and-non-obvious test before it earns body space. The audience-is-Claude framing explains rules that look arbitrary otherwise, third-person descriptions exist because the description is read by a triggering system, not a human, and the beneficial-and-non-obvious content test exists because the reader already knows everything obvious.

**Operating conditions and assumptions.** The consuming agent is comparably capable, skills for weaker executors may need the obvious stated. The thesis governs skill authoring doctrine, evaluation of authored skills belongs to the sibling theme.

**Failure boundary and alternatives.** Authors who write for human readers produce skills with second-person tutorials, vague descriptions, and restated common knowledge, exactly the guide's three mistake categories, all one root error about audience. Bounded alternatives and recoveries: documentation-for-humans conventions, legitimate for references meant for dual audiences, the guide's rules assume Claude-only consumption.

**Counterexample, gotchas, and operational comparison.** Non-obvious decays, what the model cannot know shifts with model generations, a skill's content doctrine is implicitly versioned against model capability, the guide never says so but the thesis implies it. Good: a BigQuery skill carrying table schemas Claude cannot infer. Bad: a skill paragraph explaining what a PDF is. Borderline: rationale for a workflow choice, the sibling Codex guide endorses explaining why, include it when it changes execution.

**Verification, evidence, and uncertainty.** Sample skill content against the non-obvious test at review time. The shared About Skills sections and content guidance read this session. How the non-obvious threshold moves across model versions is unmeasured.

**Second-order consequence.** The onboarding-guide metaphor does real work, onboarding assumes a capable new hire who lacks local knowledge, so the guide's content rules, schemas yes, general programming advice no, company policies yes, language tutorials no, all fall out of asking what a competent newcomer genuinely cannot know.

**Revision decision.** State the write-for-Claude thesis with the audience test, include what is beneficial and non-obvious to another Claude instance, as the filter every content decision passes through.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `skill_development_reference_patterns` contains 4 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Skill Development Reference Patterns comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide which document or exemplar answers a given authoring question. The seed heading signals list section names without the two-document read protocol, the outer guide is the operative text whose first 86 lines are inherited verbatim from the ancestor, its middle is plugin adaptation, and its tail is enforcement machinery, checklists, mistakes, quick reference, while the embedded original is consulted only to see what the adaptation changed.

**Recommended default and causal basis.** Read the outer guide as operative, the ancestor as baseline, and follow the sibling pointers when a worked example beats a rule. The outer guide interleaves inherited and adapted content without marking which is which, so the read protocol needs the ancestor open beside it, the diff between them is the plugin doctrine.

**Operating conditions and assumptions.** The sibling skills remain in the plugin at their relative paths. Navigation of this theme's two documents, the six sibling skills the guide points to navigate under their own themes.

**Failure boundary and alternatives.** Reading the outer guide alone makes the mkdir-based structure step look like original doctrine when it is a deliberate replacement of init_skill.py, the guide flags this one replacement explicitly but leaves others, packaging, validation, unmarked for the reader to discover. Bounded alternatives and recoveries: ripgrep across the plugin for locating which sibling demonstrates a given practice.

**Counterexample, gotchas, and operational comparison.** The ancestor's Step 3 and Step 5 describe scripts, init_skill.py and package_skill.py, that do not exist in this bundle, they lived in the generic creator's own scripts directory, so the embedded reference describes tooling the plugin context cannot run. Good: checking the ancestor to confirm packaging was dropped, then citing the plugin distribution rule. Bad: quoting the ancestor's init_skill.py usage as current procedure. Borderline: reading a sibling skill instead of the guide for reference-layout doctrine, sanctioned, the guide itself routes there.

**Verification, evidence, and uncertainty.** Confirm the zone boundaries against the two documents' shared and divergent sections. Both documents' full reads and their overlap measured this session. None about structure, both mapped documents were read in full.

**Second-order consequence.** The guide's study-these-skills section makes the source map recursive, it routes authors to six sibling skills as worked examples with named virtues, lean word counts, reference layouts, working scripts, so the full navigation surface is not two documents but a directory of exemplars the guide curates with one-line reviews.

**Revision decision.** Annotate the map with the three-zone structure of the outer guide, inherited opening, adapted middle, enforcement tail, and the ancestor's role as the diff baseline.

**Retained seed evidence.** All four local source rows with titles, heading signals, and usage roles remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/plugins/plugin-dev/skill-development/SKILL.md | skill-development | Skill Development for Claude Code Plugins; About Skills; What Skills Provide; Anatomy of a Skill; Progressive Disclosure Design Principle; Skill Creation Process | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/claude-skills-202603/plugins/plugin-dev/skill-development/references/skill-creator-original.md | skill-creator | Skill Creator; About Skills; What Skills Provide; Anatomy of a Skill; Progressive Disclosure Design Principle; Skill Creation Process | reference detail file for deeper pattern evidence |
| claude-skills/plugins/plugin-dev/skill-development/SKILL.md | skill-development | Skill Development for Claude Code Plugins; About Skills; What Skills Provide; Anatomy of a Skill; Progressive Disclosure Design Principle; Skill Creation Process | skill entrypoint or reusable agent prompt |
| claude-skills/plugins/plugin-dev/skill-development/references/skill-creator-original.md | skill-creator | Skill Creator; About Skills; What Skills Provide; Anatomy of a Skill; Progressive Disclosure Design Principle; Skill Creation Process | reference detail file for deeper pattern evidence |

## External Research Source Map

**Decision supported.** This section helps decide which external claims this theme owns, and the answer is the plugin runtime seams. The seed three inherited agent-format anchors stand while the theme's real external surface is Claude Code's plugin runtime, the auto-discovery scan that finds SKILL.md files under skills directories, the cc --plugin-dir local-testing flag, the three-level loading system's word budgets, and the skill-reviewer agent the guide tells authors to invoke.

**Recommended default and causal basis.** Verify discovery, testing-flag, and budget seams against current Claude Code documentation before applying the guide on a live platform. The guide's doctrine is calibrated to runtime behavior it does not control, trigger matching, metadata always-loading, body loading on trigger, so its external dependencies are platform loading semantics that can shift under any Claude Code release.

**Operating conditions and assumptions.** Claude Code retains directory-scan discovery in recognizable form. External rows serve future freshness verification, all three URLs stayed unretrieved throughout this evolution.

**Failure boundary and alternatives.** Trusting archive-dated loading semantics on a current platform risks miscalibrated budgets, the roughly-100-word metadata estimate and the sub-5k body ceiling are runtime facts frozen at 202603. Bounded alternatives and recoveries: the seed's inherited AGENTS.md and Actions anchors for ecosystem context, unmotivated for this theme's platform seams.

**Counterexample, gotchas, and operational comparison.** The word budgets appear three times with different numbers, 1500-2000 ideal, under 3000 acceptable, under 5k hard ceiling, these are one budget with soft and hard bounds, not contradictions, cite the full ladder. Good: a refresh testing cc --plugin-dir against a current release. Bad: assuming the 100-word metadata estimate holds because the archive states it. Borderline: reusing the budget ladder unverified for a rough draft, workable, verify before enforcement.

**Verification, evidence, and uncertainty.** Record dated seam checks at each external refresh. Zero fetches this assignment and the seam inventory read across both documents. Current Claude Code loading semantics are unverified from local evidence.

**Second-order consequence.** The guide's most fragile external claim is its most reassuring one, no packaging needed, distribution semantics are pure platform policy, if plugin installation ever changes shape the guide's cleanest simplification, skills ship with the plugin, is the first sentence to rot.

**Revision decision.** Keep the inherited rows, add the load-bearing seams, plugin auto-discovery behavior, the --plugin-dir flag, loading-level budgets, and the skill-reviewer agent's availability, all marked archive-dated.

**Retained seed evidence.** All three external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://developers.openai.com/codex/guides/agents-md | OpenAI Codex AGENTS.md guide | primary agent instruction context model | external_research_sourced_fact |
| https://docs.github.com/actions | GitHub Actions documentation | verification and automation model | external_research_sourced_fact |
| https://agents.md/ | AGENTS.md open format | general agent instruction format | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which authoring failures need standing detection and what detects them. The seed three corpus-process rows stand where the guide maintains its own registry, four named mistakes with paired bad-good exhibits, weak trigger descriptions that never fire, monolithic 8000-word bodies that tax every load, second-person prose that reads as tutorial rather than procedure, and unreferenced resources Claude cannot know exist, plus the don't-list's quieter entries, broken examples and skipped validation.

**Recommended default and causal basis.** Check drafts against the four mistakes in loading-pipeline order and confirm no embedded-ancestor workflow leaked into plugin practice. Each mistake breaks a different loading level, weak descriptions break triggering, bloat breaks body loading economics, style breaks instruction parsing, and missing references break resource discovery, so the registry mirrors the three-level disclosure system plus its resource tail.

**Operating conditions and assumptions.** The draft targets plugin distribution, standalone skills legitimately need the packaging step this guide's registry would flag. Authoring failures from the mapped guide, evaluation-process failures belong to the sibling theme.

**Failure boundary and alternatives.** The mistakes compound directionally, an unreferenced references directory is invisible, so authors compensate by inflating the body, which worsens the bloat the references existed to solve, the guide's avoid-duplication rule is the cycle breaker. Bounded alternatives and recoveries: the skill-reviewer agent as an automated registry check, the guide routes there explicitly.

**Counterexample, gotchas, and operational comparison.** The second-person prohibition has a boundary the guide leaves implicit, quoted user trigger phrases inside descriptions are user speech, not instruction prose, they escape the style rule. Good: a review catching a vague description before any body work. Bad: shipping a skill whose references directory is never mentioned in SKILL.md. Borderline: 2500 words of body, past ideal, inside the acceptable band, the budget ladder governs.

**Verification, evidence, and uncertainty.** Confirm each registry row cites its exhibit in the guide. The mistake catalog and don't-list read this session. How often live authors invoke the skill-reviewer is unrecorded.

**Second-order consequence.** Every registry entry comes with a paired counter-exhibit and a why-bad why-good gloss, the guide teaches anti-patterns by contrast rather than prohibition, the same good-bad-borderline pedagogy this corpus uses, independently converged on by the plugin authors.

**Revision decision.** Adopt the guide's four named mistakes with their exhibits as the registry core, adding the ancestor-confusion failure this theme's structure creates, running replaced workflows from the embedded original.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide which validation gates apply to which skill generation. The seed document verifier text stands where this theme's gates are two generations of tooling, the ancestor's script gates, init_skill.py for generation and package_skill.py for validate-then-zip, and the plugin era's replacement gates, an eight-point manual validation checklist, the skill-reviewer agent invocation, and cc --plugin-dir as the live trigger test.

**Recommended default and causal basis.** Run the checklist, invoke the reviewer, and trigger-test with --plugin-dir before calling a plugin skill done. The plugin adaptation traded executable gates for procedural ones because plugin skills skip packaging, so validation moved from a script that refuses to zip into a checklist plus an agent review plus a runtime test.

**Operating conditions and assumptions.** The skill-reviewer agent exists in the consuming environment, its availability is a platform fact. The gate progression these documents define, this document's own structural verification keeps the seed's retained block.

**Failure boundary and alternatives.** Authors migrating from the generic creator look for the validation script and find none, the risk is skipping validation entirely rather than running its procedural replacement. Bounded alternatives and recoveries: the corpus verifier command in the seed's retained block for this document's own checks.

**Counterexample, gotchas, and operational comparison.** The ancestor's package_skill.py validated as a side effect of packaging, dropping packaging silently dropped its validation unless the checklist consciously replaces it, the guide's checklist exists precisely to catch that gap. Good: a plugin skill checked against all eight checklist points then trigger-tested. Bad: declaring a skill valid because package_skill.py passed it, wrong generation. Borderline: skipping the reviewer agent when unavailable, acceptable, the checklist still binds.

**Verification, evidence, and uncertainty.** Check finished skills for checklist, reviewer, and trigger-test evidence. Both generations' gate descriptions read this session. The checklist's coverage relative to the dropped script validation is undocumented.

**Second-order consequence.** The gate story runs opposite to the corpus's usual direction, elsewhere doctrine hardens from prose into scripts, here scripts dissolved back into prose because the plugin runtime absorbed their jobs, discovery replaced initialization scaffolding and installation replaced packaging, gates migrate to wherever the platform leaves gaps.

**Revision decision.** Inventory both generations with the replacement mapping, script validation became checklist plus reviewer, packaging became plugin distribution, and testing became --plugin-dir trigger checks.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide which role owns each stage of a plugin skill's lifecycle. The seed four generic bullets stand where the guide defines a lean agent economy, the authoring agent who follows the six-step process, the skill-reviewer agent invoked by a plain-language request to check best practices, and the future consuming Claude instance the whole artifact is written for, three roles connected by the write-for-the-reader thesis.

**Recommended default and causal basis.** Author against the future-consumer test, invoke the reviewer before finalizing, and route behavioral validation to the sibling theme's harness when stakes warrant it. The roles partition by time, the author works now, the reviewer checks now, and the consumer executes later with no channel back to the author except skill revision, so authoring quality is the only message that reaches the future reader.

**Operating conditions and assumptions.** The reviewer agent ships with the plugin-dev tooling, its absence degrades to checklist-only review. Agent roles in plugin skill authoring, the five-role evaluation cast belongs to the sibling theme.

**Failure boundary and alternatives.** Collapsing author and reviewer loses the fresh-eyes check the reviewer exists for, and forgetting the future consumer produces the human-audience prose the mistake catalog documents. Bounded alternatives and recoveries: the sibling theme's full evaluation cast for skills whose quality claims need behavioral evidence.

**Counterexample, gotchas, and operational comparison.** The guide's interview questions make the user a fourth informal role, the source of concrete examples in Step 1, an author who skips the interview substitutes imagination for the one input no agent can generate. Good: a draft interviewed into shape, reviewer-checked, then written for the future instance. Bad: an author self-certifying without reviewer or checklist. Borderline: skipping the user interview for a domain the author knows deeply, the guide permits skipping with clear reason.

**Verification, evidence, and uncertainty.** Audit finished skills for interview, reviewer, and consumer-framing evidence. The process steps and reviewer invocation read this session. The reviewer agent's checking depth is described only by its coverage list.

**Second-order consequence.** Compared to the sibling theme's five-role harness this guide runs almost roleless, one reviewer instead of executors, graders, comparators, and analyzers, because plugin skills validate structurally rather than behaviorally here, the role count of a workflow is a direct read of how empirical its quality standard is.

**Revision decision.** Recast the section as the three-role economy with the temporal partition and the reviewer's invocation phrase.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide where this author is in the skill lifecycle and which entry serves them. The seed one generic engineer stands where the guide's opening steps map the arrivals, the plugin author starting from concrete user examples, the migrator arriving from the generic creator with script habits the plugin context replaces, and the improver returning right after using a skill, whom both documents single out as arriving with fresh context of how the skill performed.

**Recommended default and causal basis.** Identify the arrival first, enter at the matching step, and protect the improver's fresh context by acting on it before it cools. Step 1's interview protocol, its skip clause, and the iteration section each address a different arrival, so journey identification is built into the process order rather than overlaid on it.

**Operating conditions and assumptions.** The migrator's origin is the generic creator, migrants from other ecosystems need the platform-convention deltas too. Journeys through plugin skill authoring, journeys through created skills belong to their subjects.

**Failure boundary and alternatives.** Treating every arrival as a fresh creator walks the migrator through anatomy they know while missing the unlearning they need, and makes the improver re-interview when their fresh usage context is the more valuable input. Bounded alternatives and recoveries: the sibling theme's four-journey map for evaluation-loop arrivals, a different process with different doors.

**Counterexample, gotchas, and operational comparison.** The interview protocol warns against batching questions, the journey's first step has its own pacing rule, most-important question first, follow-ups as needed, front-loading the whole questionnaire is itself a documented mistake. Good: an improver routed straight to the iteration loop with their usage notes. Bad: a migrator told to run init_skill.py from muscle memory. Borderline: skipping the interview when usage patterns are already clearly understood, the guide's own sanctioned skip.

**Verification, evidence, and uncertainty.** Check authoring engagements for arrival identification before process entry. The process steps, skip clauses, and iteration sections read this session. The actual mix of arrivals among live plugin authors is unrecorded.

**Second-order consequence.** The fresh-context observation is the guide's subtlest journey insight, improvement requests cluster immediately after usage because the pain is still specific, so the iteration workflow is designed for hot-path entry, notice struggles, identify updates, implement, retest, a loop shaped for someone who just felt the friction.

**Revision decision.** Recast the scenario as the three arrivals with their distinct entry points, interview for creators, delta-study for migrators, iteration workflow for improvers.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The new contributor or agent is starting from an unfamiliar theme and deciding whether this reference is the right tool and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `skill_development_reference_patterns` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing what to load, what to trust, what to avoid, and what evidence proves success.
Reference opening trigger: Open this file when the task mentions skill development reference patterns, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide where each fact lives, which directories exist, and which inherited rules still bind. The seed template rows skip the guide's live tensions, body versus references, what is truly core to the skill against the avoid-duplication rule that forbids keeping it in both, minimal versus complete structure, the guide blesses layouts from a lone SKILL.md to a four-directory bundle, and inherited versus adapted doctrine, when the embedded original's generic advice still applies against when the plugin context overrides it.

**Recommended default and causal basis.** Place by the truly-core test, build only needed directories, and inherit generic doctrine only where the plugin runtime has not absorbed it. Each axis is decided per-skill, content placement follows the truly-core test, structure follows resource needs, and inheritance follows whether the plugin runtime absorbed the concern, packaging yes, writing style no.

**Operating conditions and assumptions.** The author can tell core from detail, new domains may need a usage pass before the ranking stabilizes. Tradeoffs in authoring plugin skills, evaluation-rigor tradeoffs belong to the sibling theme.

**Failure boundary and alternatives.** Wrong poles fail characteristically, core content buried in references adds a load step to every use, boilerplate directories signal resources that do not exist, and misapplied inheritance revives packaging steps the distribution model deleted. Bounded alternatives and recoveries: the sibling Codex guide's degrees-of-freedom ladder for the instruction-specificity axis this guide leaves implicit.

**Counterexample, gotchas, and operational comparison.** The guide's own structure quietly demonstrates the placement axis, it keeps its ancestor in references rather than inlining the shared history, the document eats its own dog food on progressive disclosure. Good: schemas moved to references with a grep hint in the body. Bad: the same workflow documented in body and reference drifting apart. Borderline: a minimal one-file skill for simple knowledge, sanctioned explicitly by the quick reference.

**Verification, evidence, and uncertainty.** Review skills for duplication, empty directories, and revived ancestor workflows. The duplication rule, layout ladder, and adaptation notes read this session. How authors resolve the truly-core judgment in practice is undocumented.

**Second-order consequence.** The avoid-duplication rule is the placement axis's sharp edge, information lives in the body or a reference, never both, which forces a genuine ranking decision per fact, the rule converts the fuzzy what-is-core question into a forced choice that keeps the body lean by construction.

**Revision decision.** Add the three axes with their deciding tests, truly-core for placement, actual-resource-need for structure, runtime-absorption for inheritance.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the skill development reference patterns pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong skill development reference patterns guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which of the four rows settles a given question. The seed role labels in this table call the archived pair canonical-plus-legacy and the live pair supporting, but the measured structure is a two-axis hierarchy, archive and live trees are byte-identical peers on the currency axis, while inside the bundle the outer guide is operative and the embedded original is deliberately historical, the one source in this corpus whose historical status is declared by its own filename, skill-creator-original.

**Recommended default and causal basis.** Answer plugin questions from the outer guide, lineage questions from the embedded original, and prefer archive paths in citations for stability. The plugin authors froze their ancestor as a named reference, so the operative-versus-historical ranking here is authorial intent rather than archaeological inference, and the twin diffs settle the currency axis mechanically.

**Operating conditions and assumptions.** The twin identity holds, divergence would create a genuine version hierarchy. Precedence among this theme's four rows, cross-theme precedence belongs to the corpus hierarchy.

**Failure boundary and alternatives.** Reading the embedded original as operative revives init_skill.py and packaging in a context that replaced them, the exact confusion its -original suffix exists to prevent. Bounded alternatives and recoveries: escalating outer-versus-embedded conflicts to nothing, the outer guide wins by declared design.

**Counterexample, gotchas, and operational comparison.** The embedded original is not this lineage's newest generic version, the standalone skill-creator theme holds the 479-line harness-bearing descendant, so the bundle's internal ancestor is a snapshot of an older generation than the sibling theme's current text. Good: citing the outer guide for validation doctrine. Bad: citing the embedded original's packaging step as current plugin procedure. Borderline: citing the embedded original for anatomy shared verbatim with the outer guide, harmless, prefer the operative copy.

**Verification, evidence, and uncertainty.** Test rulings against the two-axis structure and re-run the twin diffs. The identity diffs and the declared naming read this session. None about current structure, all comparisons were run on both pairs.

**Second-order consequence.** This theme inverts the corpus's usual labeling problem, elsewhere seeds mislabel current files as legacy, here the source itself labels correctly and the only error available is ignoring a label the authors already wrote, hierarchy work reduces to trusting a self-documenting structure.

**Revision decision.** Restate the hierarchy as two axes, twin equality between trees, and declared operative-versus-ancestor ranking inside the bundle.

**Retained seed evidence.** All four hierarchy rows and the classification vocabulary remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/plugins/plugin-dev/skill-development/SKILL.md | canonical primary source | Skill Development for Claude Code Plugins; About Skills; What Skills Provide | What guidance, warning, or example should this source contribute to Skill Development Reference Patterns? |
| agents-used-monthly-archive/claude-skills-202603/plugins/plugin-dev/skill-development/references/skill-creator-original.md | supporting detail source | Skill Creator; About Skills; What Skills Provide | What guidance, warning, or example should this source contribute to Skill Development Reference Patterns? |
| claude-skills/plugins/plugin-dev/skill-development/SKILL.md | supporting context source | Skill Development for Claude Code Plugins; About Skills; What Skills Provide | What guidance, warning, or example should this source contribute to Skill Development Reference Patterns? |
| claude-skills/plugins/plugin-dev/skill-development/references/skill-creator-original.md | supporting detail source | Skill Creator; About Skills; What Skills Provide | What guidance, warning, or example should this source contribute to Skill Development Reference Patterns? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing evidence makes the ancestor-to-plugin adaptation consultable. The seed generic worked-example artifact misses this theme's natural object, an adaptation delta table, one standing table mapping each generic-creator element to its plugin fate, init_skill.py to mkdir scaffolding, package_skill.py to no-packaging distribution, script validation to checklist plus reviewer, generic triggering to third-person quoted-phrase descriptions, with each row citing its line evidence in both documents.

**Recommended default and causal basis.** Consult the table for any migrator or lineage question and mine its invariant rows for the doctrine safest to reuse across contexts. The bundle ships ancestor and derivative together but never tabulates their diff, the deltas are scattered through notes and replacements, so the table converts a read-both-and-compare task into a lookup.

**Operating conditions and assumptions.** The table's citations stay synchronized with the twin identity. One table for this bundle's two documents, extensible if later sweeps add newer generations of either.

**Failure boundary and alternatives.** Without the table, migrator questions get answered by re-deriving the diff per question, and the subtle unmarked deltas, validation's migration into the checklist, get missed while the marked one, init_skill.py, gets over-cited. Bounded alternatives and recoveries: leaving the comparison in this packet's prose, recoverable but unconsultable per question.

**Counterexample, gotchas, and operational comparison.** One delta is easy to tabulate wrongly, the examples directory exists in the plugin layout but not the ancestor's anatomy, it is an addition, not a replacement, the table needs an added row class alongside replaced and carried. Good: a migrator question answered from the replaced-row for packaging in one lookup. Bad: re-reading 846 lines to confirm the style rules match. Borderline: extending the table toward the sibling theme's 479-line descendant, valuable, a second generation axis rather than this table's scope.

**Verification, evidence, and uncertainty.** Check the table exists with replaced, added, and carried rows all dually cited. The element inventory assembled from both full reads this session. Whether later sweeps hold newer generations of either document is a queue question.

**Second-order consequence.** The table's carried-unchanged rows matter as much as its changed ones, anatomy, disclosure levels, writing style, and the iteration loop crossed the adaptation untouched, mapping what a fork does not change is the cheapest way to identify a lineage's invariant core, the doctrine both generations agree is load-bearing.

**Revision decision.** Define the artifact as the element-by-fate delta table with dual citations and a carried-unchanged row class for inherited doctrine.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: skill activation contract with progressive disclosure, misuse case, and verification gate.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Skill Development Reference Patterns | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercises build description and placement judgment fastest. The seed abstract usage lines stand where this theme wants a description-writing drill, give a learner a skill concept and require a frontmatter description graded against the guide's own exhibits, third person, quoted trigger phrases, concrete scenarios, no vagueness, then a placement exercise, sort ten candidate facts into body, references, examples, or scripts under the avoid-duplication rule.

**Recommended default and causal basis.** Run the description drill before body writing and the placement sort before structure creation on every new skill. The guide's two highest-ranked failure modes are description weakness and placement bloat, so its exercises should drill exactly those two judgments before any full skill gets written.

**Operating conditions and assumptions.** The learner has real trigger phrases to work from, invented ones make the drill circular. Teaching plugin authoring judgment, evaluation-design rehearsal belongs to the sibling theme.

**Failure boundary and alternatives.** Undrilled authors produce the exhibits' bad columns, provides-guidance descriptions and 8000-word monoliths, discovered only after the skill fails to trigger or taxes every load. Bounded alternatives and recoveries: apprenticeship by studying the six sibling skills, the guide's own recommended route, slower but richer.

**Counterexample, gotchas, and operational comparison.** Drilled descriptions can overfit the quoted-phrase pattern into keyword stuffing, the guide's good exhibits stay readable sentences that happen to quote phrases, the drill's rubric should penalize phrase lists without sentence structure. Good: a drilled description with three quoted triggers and a concrete scenario list. Bad: certifying on a description the guide's own bad column would match. Borderline: a placement sort putting rationale prose in the body, defensible when it changes execution, the truly-core test decides.

**Verification, evidence, and uncertainty.** Grade drill outputs against the guide's exhibit annotations. The exhibit pairs and budget ladder read this session. Transfer from drills to full authoring quality is unmeasured.

**Second-order consequence.** The guide already contains its own answer key, three bad descriptions each annotated with why, wrong person, vague, no trigger phrases, so the drill can be self-graded against author-written rubrics rather than instructor judgment, the rare case where the source doubles as the exercise's marking scheme.

**Revision decision.** Anchor the section on the description drill and placement sort, graded against the guide's good-bad pairs and the budget ladder.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Skill Development Reference Patterns after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Skill Development Reference Patterns as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Skill Development Reference Patterns only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which measurements prove a plugin skill stays lean, findable, and current. The seed compile-centric indicator misses what the guide itself measures, body word count against the 1500-2000 ideal and its soft and hard ceilings, trigger coverage, whether the description quotes the phrases users actually say, checklist pass rate across the eight validation points, and iteration cadence, improvements landing while usage context is fresh, plus this theme's own twin-identity series.

**Recommended default and causal basis.** Track word count at every edit, trigger coverage at every description change, and run the checklist before each release. The guide quantifies where loading economics bite, word counts are its only hard numbers because context cost is the one currency every skill spends, everything else is checklist-graded.

**Operating conditions and assumptions.** Usage phrasing is observable enough to score trigger coverage, otherwise the interview's examples stand in. Measuring plugin skill health and this node's evidence, behavioral pass-rate metrics belong to the sibling harness.

**Failure boundary and alternatives.** Unmeasured bodies drift past the ceiling one paragraph at a time, and unmeasured trigger coverage is discovered only when users phrase requests the description never anticipated. Bounded alternatives and recoveries: the sibling theme's benchmark deltas for skills whose value claims need behavioral evidence.

**Counterexample, gotchas, and operational comparison.** Word count is a proxy with a failure mode, a body can hit 1800 words while still duplicating references content, the avoid-duplication rule needs its own check, count and placement are separate measurements. Good: a skill tracked at 1750 words with all eight checklist points green. Bad: a description never re-scored after users' phrasing shifted. Borderline: a 2900-word body inside the soft ceiling, acceptable, the ladder says references should absorb the next growth.

**Verification, evidence, and uncertainty.** Confirm the five series exist with dated entries at the next refresh. The budget ladder, checklist, and exemplar word counts read this session. Real trigger-coverage measurement methods are undocumented by the guide.

**Second-order consequence.** The guide's exemplar reviews model the measurement culture it wants, it praises sibling skills by their numbers, a lean 1651-word SKILL.md, a focused 1438 words, three references, three scripts, quality stated as measured facts rather than adjectives, the study-these-skills section is a metrics dashboard wearing prose.

**Revision decision.** Adopt the four native series, word count, trigger coverage, checklist pass, iteration latency, and add the twin-diff series at audit cadence.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: the next task uses the reference to make a better decision with less ambiguity.
Failure signal: the reference remains a source map and never becomes an operating guide.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this derivation reference is declared faithful. The seed shape items never audit this theme's distinctive claims, the two twin identities with dates, the operative-versus-ancestor reading, the adaptation delta table's dual citations, the replaced-workflow warnings, and the budget ladder's three-tier reconciliation.

**Recommended default and causal basis.** Run the twin diffs every audit cycle and the delta spot-checks on the table's schedule. The theme's substance is a derivation story resting on verified identity and a declared internal hierarchy, so completeness auditing means re-running diffs and spot-checking delta rows, not counting headings.

**Operating conditions and assumptions.** Both trees remain present, removal of the live pair converts currency claims to archive-dated ones. Auditing this reference against its derivation claims, both source documents were read in full once.

**Failure boundary and alternatives.** A shape-only audit passes while the live tree updates, leaving the deployment-currency claim stale, or while a delta row miscites, leaving migrators trusting a wrong fate for a workflow. Bounded alternatives and recoveries: freezing the identity claim as dated fact without re-checking, cheap and blind to the divergence that matters.

**Counterexample, gotchas, and operational comparison.** A live-tree update here has a special reading, plugin-dev revising its own authoring guide would itself be an adaptation event worth synthesizing, the audit should treat divergence as new evidence about how the plugin ecosystem's doctrine moves. Good: an audit logging two clean diffs and sampled delta citations. Bad: fidelity declared from heading counts while a delta row cites the wrong line. Borderline: skipping the ladder reconciliation after it was verified once, defensible, the three numbers are stable text.

**Verification, evidence, and uncertainty.** Execute the distinctive items and date each result in the audit record. The claim structure of every distinctive statement in this document. Live-tree update frequency is unknown, which is why the diff cadence matters.

**Second-order consequence.** This theme's audit is the sibling theme's at half scale, two diffs instead of five, one internal hierarchy instead of a five-document hub, the derivative bundle's self-documentation does the audit's hardest work in advance, self-labeling sources cut verification cost roughly in proportion to how much they declare.

**Revision decision.** Add the distinctive items, two twin diffs, one delta-row citation check per row class, the ancestor-label reading, and the ladder reconciliation.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Skill Development Reference Patterns.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a question arriving at the plugin skill-development theme. The seed token-split rows point nowhere while this theme's neighbors are precise, the skill-creator-evaluation theme for the same lineage's harness-bearing generation and all behavioral testing, the platform-entrypoint themes for ecosystem-wide skill conventions, the sibling plugin-dev skills, hooks, agents, commands, for their own domains this guide only points at, and the corpus's own document machinery for non-skill artifacts.

**Recommended default and causal basis.** Classify the question's generation and artifact first, plugin authoring routes here, behavioral evaluation and the current generic creator route to the sibling theme. This theme owns plugin skill authoring doctrine, questions about evaluating authored skills, about other plugin components, or about the generic creator's current form all have closer homes.

**Operating conditions and assumptions.** The sibling theme remains evolved, it completed as assignment 55 in this same lane. Sending away what this theme holds only by pointer, destination doctrine is deliberately not restated.

**Failure boundary and alternatives.** Answering evaluation questions here reproduces the guide's own gap, it validates structurally and never measures behavior, while answering hook or agent questions here substitutes pointers for the actual sibling doctrine. Bounded alternatives and recoveries: for sibling-skill internals, hook patterns, agent frontmatter, the plugin-dev skills themselves if later sweeps map them as themes.

**Counterexample, gotchas, and operational comparison.** Trigger-description questions split subtly, writing one routes here, the guide owns the style rules, optimizing one empirically routes to the sibling, which holds the 20-query train-test machinery. Good: forwarding a benchmark-delta question to the evaluation theme. Bad: answering a hooks.json question from this guide's one-line pointer. Borderline: a description question about both style and measured optimization, answer jointly with both themes cited.

**Verification, evidence, and uncertainty.** Sample recent answers for generation-and-artifact routing consistency. The theme boundaries assembled across this cluster's evolutions. Whether sibling plugin-dev skills become mapped themes depends on future queue scope.

**Second-order consequence.** The two skill-creator themes split one lineage cleanly along its own history, this theme holds the pre-harness generation and its plugin fork, the sibling holds the harness generation, so between them the corpus carries the lineage's full arc and routing between the two is really a question about which generation's doctrine the asker needs.

**Revision decision.** Route by lineage generation and artifact, authoring here, evaluation to the sibling theme, component doctrine to the siblings, ecosystem conventions to the platform themes.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use the nearest language, workflow, agent, or documentation reference when the theme becomes concrete.
Adjacent reference 1: consider the skill adjacent reference when the current task pivots away from skill development reference patterns.
Adjacent reference 2: consider the development adjacent reference when the current task pivots away from skill development reference patterns.
Adjacent reference 3: consider the reference adjacent reference when the current task pivots away from skill development reference patterns.

## Workload Model

**Decision supported.** This section helps decide how to budget a node with cheap steady-state and event-priced revisions. The seed symbol-count boundary misprices this node's work, whose units are per-audit twin verification, two diffs, per-cycle delta-table upkeep against a stable two-document base, and per-question routing along the generation split, with the one-time read cost already sunk, 846 lines across two documents, roughly two-fifths of the sibling theme's read.

**Recommended default and causal basis.** Run the cheap regime on audit cadence and provision re-synthesis the moment either twin diff breaks. The reading concentrated at evolution time and was cheapened by overlap, the embedded ancestor shares its opening third with the outer guide verbatim, so effective novel reading was well under the line total.

**Operating conditions and assumptions.** Version events surface through the trees this corpus holds, out-of-repo plugin-dev updates stay invisible until a sweep imports them. Budgeting this node's upkeep and revision triggers, plugin authors' own workload belongs to them.

**Failure boundary and alternatives.** Budgeting future work at evolution rates overprices maintenance, while budgeting a new-generation sweep arrival at maintenance rates underprices the re-synthesis a real plugin-dev revision demands. Bounded alternatives and recoveries: the seed's crate-slice and symbol boundaries, retained as outer limits on any single consultation's scope.

**Counterexample, gotchas, and operational comparison.** Both documents here have twins, unlike the sibling theme's Codex asymmetry, so this theme's currency is fully diff-covered and its only blind spot is upstream revisions no tree has imported yet. Good: provisioning a delta-table rebuild when a live diff breaks. Bad: quarterly full re-reads of 846 unchanged lines. Borderline: partial re-read of only the changed file after divergence, efficient, check the change against every delta row it touches.

**Verification, evidence, and uncertainty.** Check regime-switch decisions against diff events in the audit record. The read-cost ledger and twin structure from this session. Plugin-dev's revision tempo is the model's hidden demand variable.

**Second-order consequence.** The delta table converts maintenance from re-reading to spot-checking, once the adaptation is tabulated with dual citations, most future questions resolve at table granularity and full re-reads are needed only when a diff breaks, standing artifacts are workload prepayment, this theme's clearest case.

**Revision decision.** Restate the model in its three units with the two-regime warning, cheap steady-state, expensive on version events, and note the overlap discount on any re-read.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Skill Development Reference Patterns as a agent workflow operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | agent planning, tool use, context loading, delegation, or skill/plugin execution where bad guidance compounds across long-running sessions | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one primary task, up to 10 source files, up to 3 delegated subtasks, and a written completion audit for every run | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Skill Development for Claude Code Plugins; About Skills; What Skills Provide; Anatomy of a Skill; Progressive Disclosure Design Principle; Skill Creat | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is skill activation contract with progressive disclosure, misuse case, and verification gate | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which invariants must demonstrably hold for this node's claims to stay citable. The seed document-property thresholds stand where this node's invariants should, the two twin identities holding or breaks being processed as version events, the delta table's dual citations resolving into both documents, the operative-versus-ancestor reading staying aligned with the bundle's own labels, and the adaptation characterizations, what was replaced, added, and carried, staying faithful to the measured diff.

**Recommended default and causal basis.** Hold all four invariants on diff and citation schedules, with the light interpretive residue re-checked on heavy reuse. Each invariant guards a load-bearing claim, identity guards currency, citations guard the table's consultability, the label reading guards against reviving replaced workflows, and diff fidelity guards the derivation story itself.

**Operating conditions and assumptions.** The source documents stay reachable at their cited paths. Invariants for this node's claims, plugin skills' own reliability belongs to their authors.

**Failure boundary and alternatives.** The invariants fail toward different victims, silent divergence strands currency claims, dead citations strand migrators mid-lookup, label misreading revives packaging in plugin contexts, and drifted characterizations turn a measured diff into folklore. Bounded alternatives and recoveries: dropping the citation invariant and letting the table float, cheaper and exactly how lookup artifacts decay into rumor.

**Counterexample, gotchas, and operational comparison.** A twin break does not falsify past claims, statements dated to the identity period stay true of it, the invariant governs forward reuse while re-synthesis processes the new generation. Good: a reuse pause re-resolving a delta row's citations before quoting its fate. Bad: the packaging-was-dropped claim repeated for a year without a diff run. Borderline: citing the identity claim days after a clean diff, fresh enough, date it anyway.

**Verification, evidence, and uncertainty.** Review the four invariant series at each corpus checkpoint with dated evidence. The claim structure established across this document. None about current truth, all four invariants were verified this session.

**Second-order consequence.** This theme's interpretive surface is thinner than the sibling's, its central claims are diffable facts, replaced, added, carried, rather than philosophical characterizations, so its reliability rests more on mechanical probes and less on re-reading judgment, themes built on diffs age better than themes built on readings.

**Revision decision.** Publish the four invariants with evidence methods, diff batches, citation resolution, label spot-checks, and periodic re-verification of the delta rows against the sources.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which decay modes need standing probes for a derivation-centered theme. The seed generic rows miss how this node specifically dies, twin divergence unprocessed while identity claims persist, delta-table rot as citations dangle after any revision, generation confusion, this theme's pre-harness doctrine quoted as the lineage's current generic form when the sibling theme holds the newer text, and ancestor revival, the embedded original's replaced workflows leaking back into plugin guidance.

**Recommended default and causal basis.** Run the four probes at their trigger points, tagging every reused claim with its generation. All four attack the theme's differentiators, currency, consultability, generational position, and the adaptation boundary, the claims that make this node more than a paraphrase of two files.

**Operating conditions and assumptions.** The sibling theme stays evolved so the generation check has a stable counterpart. Decay of this node's claims and reuse, plugin skills' operational failures belong to their authors.

**Failure boundary and alternatives.** They compound, an unprocessed divergence dates the table, a dated table stops being consulted, unconsulted derivation claims get paraphrased from memory, and memory is where generation confusion and ancestor revival both breed. Bounded alternatives and recoveries: merging both generations into one theme to eliminate the split, rejected by the queue's structure and the documents' genuinely different subjects.

**Counterexample, gotchas, and operational comparison.** Ancestor revival can be correct outside plugins, package_skill.py remains real doctrine for standalone skills of its generation, the failure is context leakage, not the workflow's existence, the probe checks placement rather than truth. Good: a generation check catching pre-harness doctrine cited as current generic guidance. Bad: a migrator sent to run packaging from a revived ancestor quote. Borderline: quoting shared invariant doctrine without a generation tag, low risk, the carried rows are generation-stable by definition.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe and trigger point. The decay structure consolidated from this document's preceding sections. How often reuse crosses the generation boundary in practice is unobserved.

**Second-order consequence.** Generation confusion is the two-theme split's native hazard, holding one lineage across two references means every reuse must carry a which-generation tag, and the cheapest defense is built already, the delta table names its baseline and the routing section names the split, decay prevention here is mostly refusing to drop those tags.

**Revision decision.** Add the four rows with probes, the diff batch for divergence, citation resolution for rot, a generation check against the sibling theme for confusion, and replaced-row consultation for revival.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| context window loses plan | long-running session forgets early constraints or overwrites user intent | write checkpoint summaries and re-read plan before each destructive step |
| tool fanout outruns budget | parallel actions create conflicts, duplicate work, or unbounded external calls | cap fanout, assign ownership, and require merge/audit checkpoints |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide when to iterate, how fast, and how to pace demands on the user. The seed corpus-process bullets stand beside the guide's own iteration doctrine, skill improvement retries at usage granularity, use on real tasks, notice struggles, update, test again, with the fresh-context window as its scheduling pressure, and the interview protocol's pacing rule as conversational backpressure, most-important question first, follow-ups only as needed, never a batched questionnaire.

**Recommended default and causal basis.** Act on usage friction while fresh and pace interviews to the user's answering bandwidth. Skill iteration has no harness here, its retry signal is felt friction during real usage, so the loop is designed to fire while the friction is specific, and the interview paces itself to avoid exhausting the one non-renewable input, user attention.

**Operating conditions and assumptions.** Usage and authorship stay close enough that friction reaches the author, distributed teams need a channel to carry it. Retry semantics in plugin skill authoring, evaluation-iteration contamination rules belong to the sibling theme.

**Failure boundary and alternatives.** Deferred iteration cools the context until improvements become guesses, and batched interviews overwhelm users into thin answers, both losing signal the process cannot regenerate. Bounded alternatives and recoveries: the sibling theme's iteration-directory regeneration rules for skills under formal evaluation.

**Counterexample, gotchas, and operational comparison.** The fresh-context rule cuts both ways, hot-path improvements risk overfitting to the single task just performed, the sibling theme's million-invocation test is the counterweight this guide leaves implicit. Good: a struggle noted mid-usage becoming a same-day reference update. Bad: a six-week-old friction memory driving a speculative rewrite. Borderline: batching two closely-related interview questions, acceptable, the rule targets overwhelming, not efficiency.

**Verification, evidence, and uncertainty.** Audit improvement commits for proximity to the usage that motivated them. The iteration sections of both documents and the interview pacing rule read this session. How long usage context stays actionable is undocumented.

**Second-order consequence.** Both rules manage the same scarce resource from different sides, human attention, the iteration loop harvests it when it is naturally concentrated, right after usage, and the interview rations it deliberately, one question at a time, a skill process tuned around attention economics before any token is spent.

**Revision decision.** State the usage-granularity loop with its fresh-context scheduling and the interview's question-pacing rule as this theme's two backpressure surfaces.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide which records exist for skill health and who consumes each. The seed generic records stand beside what this theme actually needs observed, the skill's own health surfaces, word count against the ladder, checklist results, reviewer findings, trigger-test outcomes under --plugin-dir, and this node's evidence surfaces, dated twin diffs and delta-table citation checks, with the guide's structural records standing in for the behavioral telemetry it never collects.

**Recommended default and causal basis.** Persist the six record classes and route behavioral questions to the sibling machinery when stakes warrant. Plugin skills validate structurally, so their observability is document-shaped, counts, checklists, findings, unlike the sibling harness's run-shaped records, transcripts, timings, grades.

**Operating conditions and assumptions.** Authors actually run the checklist and reviewer, records only exist where process is followed. Records for plugin skill health and this node's evidence, harness run records belong to the sibling theme.

**Failure boundary and alternatives.** Each missing record hides a specific decay, untracked word counts hide creeping bloat, unrecorded reviewer findings repeat across drafts, and undated diffs leave the currency claim unfalsifiable. Bounded alternatives and recoveries: platform-level skill-usage telemetry if Claude Code ever exposes it, unavailable at the archive date.

**Counterexample, gotchas, and operational comparison.** Reviewer findings are prose, not schema, so trend analysis across drafts needs the findings kept, not just addressed, discarding them after fixing loses the pattern signal across a plugin's skills. Good: a skill directory with dated checklist runs, kept reviewer findings, and word-count history. Bad: a currency claim with no dated diff behind it. Borderline: skipping trigger tests for a skill with one unambiguous phrase, cheap omission, the test costs one command.

**Verification, evidence, and uncertainty.** Reconstruct a skill's health story from its records alone at the next refresh. The validation, reviewer, and testing surfaces read across both documents. Live trigger behavior is unobservable from local evidence.

**Second-order consequence.** The guide's observability gap is behavioral, nothing records whether skills actually trigger on real user phrasings in production, the trigger-test flag covers development only, so the record most predictive of skill value, live trigger hit rate, is precisely the one neither generation of this lineage collects, the sibling theme's optimization loop is the nearest substitute.

**Revision decision.** Adopt the four skill surfaces and two evidence surfaces as the record inventory, each with its consumer named, authors, reviewers, and this node's auditors.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture tool-call count, context files loaded, delegated tasks, retry count, and completion-audit outcome.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide what evidence shows a skill's context cost is justified at each level. The seed latency packet stands beside this lineage's actual performance doctrine, skill cost is context cost, measured in words at three loading levels, roughly 100 always-loaded description words, a body budget with 1500-2000 ideal and 5k hard ceiling paid on every trigger, and unlimited reference capacity paid only on demand, with scripts costing nothing to execute unread.

**Recommended default and causal basis.** Verify skill performance by auditing words per loading level against the budget ladder and migrating weight downward at each breach. Every triggered skill taxes the context window before any work happens, so the guide prices content by loading level and pushes weight down-level, the whole progressive-disclosure design is a performance optimization stated as document structure.

**Operating conditions and assumptions.** The level budgets reflect current platform behavior, they are archive-dated runtime facts. Performance of skills as context investments, runtime performance of skill-invoked scripts belongs to their domains.

**Failure boundary and alternatives.** Unpriced content accumulates at the always-loaded and per-trigger levels, the 8000-word monolith exhibit is a performance bug wearing a documentation costume. Bounded alternatives and recoveries: the sibling theme's token-delta benchmarks for measuring a skill's actual loaded cost empirically.

**Counterexample, gotchas, and operational comparison.** Word counts undercount structural tokens, tables, code fences, and frontmatter cost more than their word tally suggests, budget compliance measured by words alone flatters heavily-formatted bodies. Good: a schema table migrated to references with a grep hint left in the body. Bad: a per-trigger body carrying migration guides read once a quarter. Borderline: a 4800-word body under the hard ceiling, compliant, the ladder says references should have absorbed it long before.

**Verification, evidence, and uncertainty.** Audit level word counts at each release against the ladder. The disclosure budgets and script-execution note read this session. Actual token multipliers for formatted content are platform-volatile.

**Second-order consequence.** The asterisk on unlimited bundled resources is the model's sharpest line, scripts can be executed without being read, making execution the only zero-context-cost consumption mode, which quietly explains the ancestor's script preference for repeated code, moving work from tokens to processes is the lineage's deepest performance move.

**Revision decision.** State the three-level cost model with its budgets and the down-level migration rule as this theme's verification method.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require tool-call budgets, timeout budgets, and a resumable journal when the workflow exceeds one focused session.
Leading indicator to measure: the next task uses the reference to make a better decision with less ambiguity.
Failure signal to watch: the reference remains a source map and never becomes an operating guide.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what size each container splits and along which route. The seed task-splitting bounds stand beside the documents' own scale limits, a skill scales until its body hits the disclosure ceiling, then splits into references, references scale until they exceed 10k words, then need grep patterns in the body, and the plugin scales skills by directory, auto-discovery finds any subdirectory with a SKILL.md, making the skills directory the unit that absorbs growth.

**Recommended default and causal basis.** Split at each threshold along the designed route, body to references, references to grep-indexed references, domains to separate skills. Each container has a designed overflow route, body to references, references to searchable references, plugin to more skills, so scaling doctrine here is a chain of graceful spillovers rather than a single ceiling.

**Operating conditions and assumptions.** Domains actually partition, entangled domains resist the skill-per-directory split and argue for shared references. Bounding skill and plugin growth, the corpus's own document scale limits belong to its process themes.

**Failure boundary and alternatives.** Ignoring an overflow route breaks the next level up, an overgrown body defeats triggering economics, an unsearchable 15k-word reference defeats on-demand loading, and one mega-skill defeats the plugin's per-domain triggering. Bounded alternatives and recoveries: monolithic skills with aggressive grep indexing, workable below the trigger-economics ceiling, against the guide's grain.

**Counterexample, gotchas, and operational comparison.** Splitting a skill in two splits its trigger surface too, each half needs its own quoted-phrase description, a split that leaves one half undiscoverable trades bloat for invisibility. Good: a 3200-word body shedding its patterns section into references. Bad: a 15k-word reference loaded whole for every consultation. Borderline: two related skills sharing a reference file by relative path, economical, discovery treats them independently.

**Verification, evidence, and uncertainty.** At each release, check every container against its threshold and route. The budget ladder, grep rule, and auto-discovery description read this session. Where trigger economics actually collapse per plugin is a platform behavior.

**Second-order consequence.** The 10k grep-pattern rule is progressive disclosure applied recursively, when a reference outgrows loading whole, the skill discloses search patterns instead of content, pointing the reader at retrieval rather than text, the same move the corpus makes with its own oversized sources, spilled scale becomes an indexing problem at every level.

**Revision decision.** Name the three-level spillover chain with its thresholds, the word ladder, the 10k grep rule, and directory-per-domain splitting.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Skill Development Reference Patterns stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal decay in this node's claims and on what clocks. The seed theme-name queries would surface generic authoring chatter while this node's staleness has four homes, the two twin identities, upstream plugin-dev revisions no tree has imported, the platform seams, discovery behavior, loading budgets, the --plugin-dir flag, the reviewer agent, and newer generations of either document arriving in later monthly sweeps.

**Recommended default and causal basis.** Run twin diffs on audit cadence, sweep scans on each monthly import, and seam checks at external refresh windows. The twin diffs cover in-repo currency mechanically, upstream and platform movement age invisibly until a sweep or a seam check surfaces them, so refresh probes must look outward as well as inward.

**Operating conditions and assumptions.** Monthly sweeps continue arriving, a frozen archive decays this theme toward the purely historical pattern. Refreshing this node's evidence, seams, and generational awareness, plugin skills' own freshness belongs to their authors.

**Failure boundary and alternatives.** Internal-only refreshing leaves seam rot and upstream staleness undetected, the two surfaces where this theme's doctrine touches live systems. Bounded alternatives and recoveries: declaring the theme historical and dropping currency claims, defensible if maintenance lapses, record the decision.

**Counterexample, gotchas, and operational comparison.** A newer fork generation would not obsolete this theme, it extends the delta table with a second generation column, plan the table's schema for that now rather than rebuilding it then. Good: a refresh logging clean diffs, a sweep scan of both branches, and a dated seam check. Bad: logging that skill development searches returned nothing new. Borderline: skipping seam checks while no live plugin work consumes the theme, defensible deprioritization, record it.

**Verification, evidence, and uncertainty.** Record each probe with its date and the claim it confirmed or updated. The currency structure and seam inventory established this session. Future sweep contents are exactly what the sweep scan exists to learn.

**Second-order consequence.** This theme's refresh must watch two lineage branches at once, plugin-dev revising the fork and the generic creator evolving its trunk, the sibling theme already proves the trunk moved, 209 lines to 479, so the most likely refresh event is not divergence here but a newer fork generation appearing in a sweep and adding a version axis to the delta table.

**Revision decision.** Replace the name queries with the four probes, twin diffs, sweep scans for newer plugin-dev or skill-creator generations, seam checks against current Claude Code documentation, and delta-table re-synthesis on any version event.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | skill development reference patterns official documentation best practices |
| `github_repository_query_phrase` | skill development reference patterns GitHub repository examples |
| `release_notes_query_phrase` | skill development reference patterns changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label, chain, and state each claim class here may be reused. The seed label definitions stand without this assignment's distinctive ledger, two unique documents read in full totaling 846 lines with a substantial verbatim overlap between them, two identity diffs run, zero external URLs fetched, zero platform commands executed, no cc invocation, no reviewer agent, no trigger test, so every workflow claim here is documentation-derived.

**Recommended default and causal basis.** Before reusing a claim, declare class, chain, and verification state, and never let diff-grade certainty about text bleed into confidence about runtime. The theme's sources are one self-contained bundle, so evidence chains are short and same-session, the only cross-theme dependency is the generational comparison against the sibling skill-creator theme, itself grounded in that assignment's own full reads.

**Operating conditions and assumptions.** The twin identity holds and the documents stay unedited, both watched by the established probes. Reuse rules for this document's claims, each claim travels with class, chain, and verification state.

**Failure boundary and alternatives.** The costliest mislabel would present platform behavior as observed, auto-discovery, loading budgets, and reviewer output are all described, never demonstrated, prompt-described behavior is not verified runtime behavior. Bounded alternatives and recoveries: running one skill through cc --plugin-dir to convert a platform claim to demonstrated behavior, declined to keep scope on synthesis, queued as the natural next verification step.

**Counterexample, gotchas, and operational comparison.** The generational claims lean on the sibling assignment's reads, reusing them outside this lane should cite both assignments' evidence, a two-link chain, short but no longer same-session. Good: reusing the packaging-was-dropped delta as diff-grounded fact and the loading budgets as archive-dated description. Bad: citing auto-discovery behavior as tested on this document's authority. Borderline: reusing the trunk-moved generational claim, well-grounded across two assignments, carry both citations.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares class, chain, and state cleanly. This assignment's ledger, two full reads, two diffs, zero fetches, zero executions. Behavioral verification awaits an actual plugin load on a live platform.

**Second-order consequence.** This theme's strongest claims are its diffable ones and its weakest are its platform ones, the same currency-versus-verification split the sibling theme named, but here even sharper because the delta table is pure text comparison, fully verifiable locally, while every loading-semantics number depends on a runtime nobody in this corpus has touched.

**Revision decision.** Publish the strata explicitly, document contents and twin identity as same-session facts with dated diffs, the adaptation deltas as same-session diff-grounded synthesis, the generational comparison as cross-assignment synthesis over full reads, and all platform behavior as read-but-unrun.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
