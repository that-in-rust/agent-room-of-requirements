# Rust Executable Reliability Reference

**Decision supported.** This section helps decide how to treat a byte-identical doctrine transplant when both copies sit in the corpus. The seed no seed line states this file's defining fact, it is a verbatim re-shipment, the 202604 executable-specs bundle carries the rust-coder-02 reliability reference byte for byte, sixty scored patterns, rubric, ladder, anti-patterns, and stack, unchanged from its 202603 home, embedded as the deep-doctrine leg of a different skill.

**Recommended default and causal basis.** Consume this copy through its host bundle's router and resolve doctrine questions against the rust-coder reliability theme's evolved coverage. The executable-specs bundle needed reliability depth and its authors chose transplantation over rewriting, importing a proven 1541-line doctrine wholesale rather than compressing or forking it, so this theme's subject is reliability doctrine in a second habitat.

**Operating conditions and assumptions.** The byte identity holds, any future edit to either copy forks the doctrine and changes every answer here. This reference covers the transplanted copy's role inside the executable-specs bundle, the doctrine's content is canonically covered by the rust-coder reliability theme.

**Failure boundary and alternatives.** Readers who treat the two habitats as independent doctrines double-maintain one text, and readers who assume bundle-specific tailoring look for executable-specs adaptations that do not exist, the file still opens with rust-coder-02's own premise check. Bounded alternatives and recoveries: the compression strategy the bundle's standalone merge uses, which distills this file's lists into portable form at the cost of repair depth.

**Counterexample, gotchas, and operational comparison.** The host bundle's router dispatches into this file by exact section names, so the transplant is load-bearing for navigation, editing this copy without updating the router breaks dispatch even if the edit improves the doctrine. Good: citing the rust-coder theme for pattern depth and this theme for host-bundle routing. Bad: evolving divergent guidance from the two identical copies. Borderline: quoting this copy's path in an executable-specs context, correct locally, the canonical doctrine citation still names the origin.

**Verification, evidence, and uncertainty.** Re-run the diff between the two copies before relying on their equivalence. The identity diff run this session and the assignment 47 full read of the same bytes. Whether the 202604 authors intended eventual divergence or permanent mirroring is unrecorded.

**Second-order consequence.** The transplant carries its old identity visibly, the file's title heading still reads as rust-coder-02 material and its self-referential sections critique rust-coder-01, so inside the executable-specs bundle it is a guest speaking its home dialect, evidence that the 202604 authors valued fidelity over seamlessness.

**Revision decision.** Open with the re-shipment fact, dated diff evidence, and the habitat difference, here the file is routed material under a spec-first workflow rather than a skill's own core.

**Retained seed evidence.** The exact theme title and reference framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide which source and which provenance chain justifies each claim class reused from this theme. The seed one local row names the 202604 path without recording the provenance chain that actually establishes its content, the full read happened against the 202603 rust-coder-02 copy during assignment 47, and this path's authority rides on a dated byte-identity diff between the two.

**Recommended default and causal basis.** Cite the doctrine to the rust-coder theme and cite this path for host-bundle claims, carrying the diff date wherever equivalence matters. Reading 1541 lines twice buys nothing when a diff proves identity, so the evidence economy here is legitimate two-hop provenance, full read of one copy plus measured equivalence to the other.

**Operating conditions and assumptions.** Both archive paths remain stable so the chain stays mechanically checkable. Provenance for this document's statements, the four external URLs stayed unretrieved throughout this evolution and confer no current-fact authority.

**Failure boundary and alternatives.** Recording this as a direct full read would overstate the ledger, and skipping the diff date would let the equivalence silently expire, the same shelf-life hazard the reference-maps theme documented for its duplicate. Bounded alternatives and recoveries: a future mapping revision that records cross-references between duplicate corpus files explicitly, replacing repeated diff runs with a maintained identity index.

**Counterexample, gotchas, and operational comparison.** The four external rows are the rust-cluster template set again, and here they are doubly inherited, the same candidates already sit unretrieved on the origin theme, retrieval effort should be spent once and shared, not queued twice. Good: citing pattern RC2 doctrine to the origin theme with this path noted as mirror. Bad: presenting this row as an independently read source. Borderline: citing this path alone inside host-bundle discussions, acceptable, the chain should still be discoverable.

**Verification, evidence, and uncertainty.** Trace disputed claims through both hops, the origin read and the dated diff. The diff run this session and the assignment 47 read ledger. Whether other unnoticed duplicates of this file exist elsewhere in the corpus is unchecked.

**Second-order consequence.** The corpus now holds three byte-identical instances of executable-specs material across sweeps, this reliability copy, and the merge with its unclassified twin, suggesting the 202604 sweep systematically duplicated rather than referenced, a filing pattern future classification work should expect elsewhere.

**Revision decision.** Annotate the row with the two-hop chain, origin read in assignment 47, identity verified by diff this session, and date both hops.

**Retained seed evidence.** The single local row and all four external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202604/rust-executable-specs-01/references/rust-reliability-reference.md | local_corpus_source_material | local_corpus_sourced_fact | reference detail file for deeper pattern evidence |
| https://github.com/rust-lang/api-guidelines | external_research_source_material | external_research_sourced_fact | Rust library-team API design recommendations |
| https://github.com/tokio-rs/tokio | external_research_source_material | external_research_sourced_fact | async runtime implementation and operational model |
| https://github.com/tokio-rs/axum | external_research_source_material | external_research_sourced_fact | Rust web framework implementation and design claims |
| https://docs.rs/axum/latest/axum/ | external_research_source_material | external_research_sourced_fact | published crate documentation for routing and extractors |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which host-bundle reference leads for a given risk class and where this transplant sits. The seed three corpus-process rows stand where this habitat's real ranking question is precedence among the host bundle's three references, the router sends Reliability and Review modes here first and to the gates file second, making this transplant the bundle's designated deep end, outranking the playbook and gates file whenever correctness under stress is the risk.

**Recommended default and causal basis.** Let mode classification decide when this file leads and defer its internal prioritization to the origin theme's rubric and floor. The router's mode table encodes the bundle's own priority judgment, spec risk routes to the playbook, closing risk to the gates file, and correctness risk here, so within-bundle ranking is a dispatch fact, not an interpretive claim.

**Operating conditions and assumptions.** Consumers arrive through the router, direct consumers inherit the origin theme's own use-order guidance instead. Ranking within the host bundle's reference set, the sixty-pattern internal scoreboard is the origin theme's subject.

**Failure boundary and alternatives.** Flattening the three references into equal peers loses the dispatch information, readers averaging the playbook's process rules with this file's boundary clauses produce blended guidance neither document supports. Bounded alternatives and recoveries: the standalone merge's flattened ordering for consumers holding one file, which trades this dispatch structure for portability.

**Counterexample, gotchas, and operational comparison.** The router names this file's internal sections in its reading orders, so the ranking couples to section-name stability, the origin copy could rename a section without breaking anything while the same edit here would orphan router rows. Good: a cancellation review entering here first per Review Mode. Bad: starting an async correctness task in the gates file because it is shortest. Borderline: a vague async feature, Spec Mode leads to the playbook first, this file joins at the delivery step.

**Verification, evidence, and uncertainty.** Check routed consultations entered the mode-designated reference first. The host router's mode table read in full during assignment 49. How often direct consumption bypasses the router in practice is unrecorded.

**Second-order consequence.** This file is the only bundle reference that two modes route to first, Reliability and Review both lead here, which quantifies its weight, half the bundle's dispatch surface terminates in the transplant, the imported guest carries the heaviest routing load.

**Revision decision.** Present the router's mode-based precedence as the operative ranking and note the fixed terminal position of the gates file behind this one in both relevant modes.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `rust_executable_reliability_reference` | 95 | default_adoption_pattern_tier | Source Map First: Load local rust executable material before synthesizing reliability reference recommendations. |
| `rust_executable_reliability_reference` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `rust_executable_reliability_reference` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide whether reliability depth should travel as verbatim doctrine, compression, or reference. The seed generic corpus formulas stand where this habitat's thesis is doctrine reuse without dilution, the bundle imported its reliability depth verbatim rather than summarizing it, betting that an unmodified expert document plus a router beats a tailored abridgment, fidelity chosen over fit.

**Recommended default and causal basis.** Route depth questions to the verbatim copy and treat every compression of it as a derived artifact needing periodic comparison. Abridgments decay independently of their sources and tailoring invites silent doctrine drift, importing the whole file keeps one text authoritative in two habitats and concentrates all adaptation work in the thin routing layer.

**Operating conditions and assumptions.** The routing layer stays cheap, if consumers cannot navigate 1541 lines even with the router, pressure for abridgment returns. The thesis governs how the bundle packages reliability doctrine, the doctrine's own selection thesis belongs to the origin theme.

**Failure boundary and alternatives.** The strategy fails where the merge's list divergence already shows it failing, the moment any derived artifact paraphrases this file, the single-text guarantee breaks and the bundle carries both a faithful copy and an unfaithful compression simultaneously. Bounded alternatives and recoveries: reference-by-path instead of copying, one physical file cited from both bundles, which prevents forks structurally but breaks the bundle's self-containment.

**Counterexample, gotchas, and operational comparison.** Verbatim reuse also transplants the origin's self-critique sections, bundle consumers reading the rust-coder-01 comparison encounter context about a skill their bundle does not contain, fidelity's cost is carrying another lineage's scar tissue. Good: a bundle consumer reading the transplant's full section three for a cancellation design. Bad: settling a boundary dispute from the merge's eleven-line list. Borderline: quoting the transplant's anti-patterns without the surrounding clauses, faithful text, partial fidelity.

**Verification, evidence, and uncertainty.** Compare derived artifacts against the transplant on the audit cadence and check consultations reach the verbatim text for depth questions. The observed list divergence between the merge and this file's doctrine. Whether fidelity or portability serves the bundle's real consumers better is unmeasured.

**Second-order consequence.** The bundle demonstrates both packaging strategies on the same doctrine simultaneously, this file is the fidelity pole and the standalone merge's compressed lists are the portability pole, making the executable-specs bundle a natural experiment whose divergence the corpus has already observed once.

**Revision decision.** State the reuse-without-dilution thesis and its boundary, verbatim transplant preserves fidelity only for consumers who actually read the transplant rather than its compressions.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `rust_executable_reliability_reference` contains 1 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Rust Executable Reliability Reference comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide which navigation layer applies when consulting this copy in its host habitat. The seed map row carries heading signals without the habitat map a transplanted file needs, where the copy sits in its host bundle, references beside a playbook, a gates file, and the router that dispatches into it, and how its 1541 lines are entered in this habitat, by routed section lists rather than by its own use-order guidance.

**Recommended default and causal basis.** Enter this copy through the router's task-type section lists when working in the bundle and fall back to the file's own pattern map when arriving directly. The same bytes are navigated differently per habitat, the origin bundle enters through its own reference map and pattern map, while the host bundle enters through mode dispatch and per-task-type section lists, so navigation guidance is habitat property, not file property.

**Operating conditions and assumptions.** The router's section references stay aligned with the file's actual headings, the coupling the routing theme's rot probe watches. Navigation of this copy within the executable-specs bundle, the file's internal structure is the origin theme's subject.

**Failure boundary and alternatives.** Applying origin navigation in the host habitat skips the router's task-type tailoring, and applying host navigation to the origin copy references a router that does not exist there, both misfits waste the navigation layer each bundle built. Bounded alternatives and recoveries: ripgrep heading search across the host bundle, endorsed by both bundles' routers as the known-item shortcut.

**Counterexample, gotchas, and operational comparison.** The file's own internal navigation aids, its pattern map and reference-map sibling in the origin bundle, are absent or different here, a consumer following the text's internal pointers can be led toward origin-bundle structure the host does not replicate. Good: an async-service task reading exactly the router-listed sections here. Bad: hunting for the origin's reference-map file inside the executable-specs bundle. Borderline: using the file's internal pattern map in-bundle, workable, it ignores the task-type tailoring the host built.

**Verification, evidence, and uncertainty.** Check that host-bundle consultations cite router-listed sections rather than origin navigation. The host router's section-level dispatch lists read in full during assignment 49. Whether host consumers actually use the router or navigate the file raw is unrecorded.

**Second-order consequence.** The router routes into this file at section granularity, listing specific numbered sections per task type, which means the host bundle effectively re-indexed a guest document without touching it, navigation metadata layered entirely outside the navigated file, the cleanest possible separation of text and index.

**Revision decision.** Annotate the row with the host-habitat entry points, the router's six task-type lists naming this file's numbered sections, and the two modes that lead here.

**Retained seed evidence.** The single local source row with title, heading signals, and usage role remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202604/rust-executable-specs-01/references/rust-reliability-reference.md | rust-coder-02 | Rust Coder 02; Premise Check; What rust-coder-01 Gets Right; Where rust-coder-01 Is Thin; Source Pool; Selection Thesis | reference detail file for deeper pattern evidence |

## External Research Source Map

**Decision supported.** This section helps decide which external targets deserve retrieval effort for a theme whose text is a verified mirror. The seed four inherited framework anchors stand where this theme's external surface is wholly inherited twice over, the doctrine's named crates and tools, tokio, loom, proptest, thiserror, anyhow, secrecy, Miri, age with their ecosystems exactly as they do on the origin theme, and no host-bundle-specific external claim exists to freshen.

**Recommended default and causal basis.** Run the identity diff first at each refresh, then inherit the origin theme's dated external findings wholesale. A byte-identical transplant introduces zero new external claims, so its external freshness rides entirely on the origin theme's probes, and any independent freshening here would duplicate work and risk divergent conclusions about identical text.

**Operating conditions and assumptions.** The identity holds, a diverged copy immediately regains its own independent freshness obligations. External rows serve future freshness verification only, all four remained unretrieved throughout this evolution.

**Failure boundary and alternatives.** Queueing separate external verification for both copies wastes retrieval effort and can produce the absurdity of one copy's claims marked current while the identical other copy's claims stay flagged stale. Bounded alternatives and recoveries: independent probes per copy, defensible only if the copies are expected to diverge, which nothing in the corpus currently indicates.

**Counterexample, gotchas, and operational comparison.** Delegation must be recorded, a future maintainer seeing no external checks on this theme could reasonably conclude it was neglected rather than delegated, the ledger note is what distinguishes economy from omission. Good: a refresh running the diff then citing the origin theme's dated crate findings. Bad: separately fetching tokio release notes for each identical copy. Borderline: a host-bundle consumer asking about axum currency, neither copy's content answers it, the candidate rows stay candidates.

**Verification, evidence, and uncertainty.** Confirm the delegation note exists and the last identity diff is dated within the audit window. Zero fetches this assignment and the dated identity diff. The origin theme's external probes have themselves not yet run, delegation inherits their pending status.

**Second-order consequence.** Identity plus delegation forms a freshness economy for duplicated corpora, verify equivalence cheaply by diff, then let one copy carry all expensive external verification, the pattern generalizes to every duplicate the 202604 sweep created.

**Revision decision.** Keep the rows as inherited candidates, declare freshness delegation to the origin theme's probe set, and add the one host-specific check, that the transplant still matches the origin before delegation applies.

**Retained seed evidence.** All four external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://github.com/rust-lang/api-guidelines | Rust API Guidelines | Rust library-team API design recommendations | external_research_sourced_fact |
| https://github.com/tokio-rs/tokio | Tokio repository | async runtime implementation and operational model | external_research_sourced_fact |
| https://github.com/tokio-rs/axum | Axum repository | Rust web framework implementation and design claims | external_research_sourced_fact |
| https://docs.rs/axum/latest/axum/ | Axum docs.rs | published crate documentation for routing and extractors | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which failure modes this theme must watch beyond the doctrine's own rejection list. The seed three corpus-process rows stand where this habitat adds transplant-specific failure modes to the doctrine's own twelve rejections, divergent evolution, editing one copy of a mirrored doctrine, compression substitution, letting the merge's abridged lists stand in for this file's clauses, and orphaned dispatch, changing section names that the host router lists.

**Recommended default and causal basis.** Run the three mechanical detections on the audit cadence and route code rejection sweeps through the origin doctrine. Each added row exists because duplication created relations the origin never had, a single-copy doctrine cannot fork, cannot be shadowed by its own abridgment inside the same bundle, and cannot orphan a router it predates.

**Operating conditions and assumptions.** Audits actually run, mechanical detectability without execution is the same as no detection. Registry rows for this habitat's failure surface, the twelve code-level rejections are enforced through the origin theme's coverage.

**Failure boundary and alternatives.** The doctrine-level twelve rejections still apply to code reviewed under this theme, but the transplant rows fail differently, they corrupt the reference system itself, producing wrong guidance served confidently rather than wrong code caught late. Bounded alternatives and recoveries: structural prevention by symlinking or reference-by-path, which eliminates the fork row at the cost of bundle self-containment.

**Counterexample, gotchas, and operational comparison.** Divergent evolution is most likely to arrive as a well-intentioned fix, someone correcting a typo or updating a crate name in whichever copy they found first, the registry row exists precisely because the harmful edit will look like maintenance. Good: an audit catching a one-line edit that forked the copies. Bad: a bundle consumer citing the merge's list as this file's full registry. Borderline: a deliberate coordinated edit to both copies, legitimate maintenance, the audit record should show both hashes moving together.

**Verification, evidence, and uncertainty.** Check each transplant row has its probe wired into the audit cadence with an owner. The relation structure created by the verified duplication. Which copy future maintainers will treat as primary when fixing real defects is unknown.

**Second-order consequence.** The transplant rows are all relation failures detectable by the same mechanical probes the routing theme prescribed, so this registry's new surface is fully automatable, unlike the judgment-heavy code rejections it supplements, duplication added risk but only cheap-to-watch risk.

**Revision decision.** Carry the twelve rejections by reference and register the three transplant rows with detections, the identity diff for forks, block comparison for compression substitution, and the heading grep for orphaned dispatch.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide which gate set a change that consulted this transplant owes at its close. The seed document verifier stands where this habitat's gate story is layered inheritance, code under this theme owes the doctrine's boundary-matched gates, clippy with denied warnings, doctests, compile-fail, property and fuzz coverage, loom, Miri, and above them the host bundle's own four-command floor from its gates file, two gate layers from two lineages meeting on one task.

**Recommended default and causal basis.** Run the bundle floor on every change and add doctrine gates per the boundaries the change touches. The host bundle routes every task-type reading order through the gates file last, so work consulting this transplant always terminates in the bundle's command floor, while the transplant itself prescribes the risk-matched heavier gates per boundary, floor and escalation arriving from different documents.

**Operating conditions and assumptions.** Boundary classification happens, unclassified changes get the floor only and silently under-verify exactly when they touch hard boundaries. How gate obligations compose in this habitat, each layer's own content belongs to its origin theme.

**Failure boundary and alternatives.** Consumers taking only one layer under-verify, the four-command floor alone misses the interleaving and unsafe scrutiny this doctrine demands for concurrent and FFI work, while doctrine gates alone skip the fmt and build uniformity the bundle's floor enforces. Bounded alternatives and recoveries: the origin bundle's own workflow gates, equivalent doctrine-side, lacking the executable-specs floor that this habitat adds.

**Counterexample, gotchas, and operational comparison.** The doctrine's gates are named inside prose clauses rather than as a command block, so extracting the owed gate set requires reading the relevant sections, a consumer grepping for a runnable block here finds nothing and may wrongly conclude the floor suffices. Good: a concurrency change closing with the four commands plus loom evidence. Bad: an unsafe-touching change closed on the floor alone. Borderline: doctrine gates without the floor on a prototype branch, the doctrine tolerates it, the bundle's unconditional wording does not.

**Verification, evidence, and uncertainty.** Audit closes of transplant-consulting work for both layers' evidence. The router's terminal gates-file position and the doctrine's per-boundary gate clauses. How consistently teams compose both layers without tooling support is unmeasured.

**Second-order consequence.** The two layers never conflict because they partition cleanly by trigger, the floor is change-invariant and the doctrine gates are boundary-variant, which means composition is additive with no precedence question, a rare case of two independent gate lineages composing without arbitration.

**Revision decision.** State the composition rule, the bundle floor is unconditional, the doctrine's heavy gates trigger by boundary risk, and both appear in the close for work that consulted this file.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide what composed contract an agent consulting this transplant in the host bundle must satisfy. The seed four generic bullets stand where an agent in this habitat needs a composed contract, classify the task by the host router's modes, read this transplant's routed sections for the touched boundaries, walk the doctrine's seven-rung ladder for design decisions, and return through the bundle's output contract with gate results from both layers.

**Recommended default and causal basis.** Require agents in this habitat to show ladder-derived reasoning inside the contract's design section rather than treating the sections as free-form. The habitat wires the origin doctrine's decision ladder into the host bundle's response format, so an agent serving executable-specs consumers must speak both lineages, ladder-shaped reasoning inside contract-shaped output.

**Operating conditions and assumptions.** The task is substantial enough for the full contract, small fixes take the ladder's relevant rungs without the ceremony. Agent behavior when this transplant is the consulted depth source, generic ladder guidance belongs to the origin theme.

**Failure boundary and alternatives.** Agents using only the host contract produce well-formatted responses with shallow boundary reasoning, and agents using only the ladder produce deep designs that skip the requirement and traceability sections the bundle's consumers expect. Bounded alternatives and recoveries: treating the merge as the agent's whole input, faster to load, and the exact compression substitution the registry rejects for boundary-heavy work.

**Counterexample, gotchas, and operational comparison.** The transplant's own how-an-LLM-should-use-this-file section speaks as rust-coder-02, an agent reading it literally may adopt origin-bundle workflow steps, reference-map reading, that do not exist in the host habitat, the guest's self-instructions need habitat translation. Good: a response whose design section walks ownership and cancellation rungs explicitly. Bad: a contract-formatted response with no boundary reasoning. Borderline: ladder reasoning without the traceability table on a spike, acceptable when labeled a spike.

**Verification, evidence, and uncertainty.** Sample agent responses for ladder content inside contract structure with dual-layer gates. The doctrine's ladder section and the bundle's output contract, both read in full. Whether agents reliably translate the guest's self-instructions to the host habitat is untested.

**Second-order consequence.** The ladder and the output contract interlock naturally, the ladder's rungs, ownership, errors, concurrency, API stability, verification mix, produce exactly the content the contract's design and verification sections demand, the two artifacts were authored separately yet compose like they were designed together.

**Revision decision.** Compose the contract explicitly, router modes for dispatch, ladder rungs for boundary choices, bundle output order for the response, and dual-layer gate evidence for the close.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide which arrival pattern fits the current consumer of this transplanted copy. The seed one generic engineer stands where this habitat's travelers arrive pre-classified, a router-dispatched implementer landing on specific numbered sections for their task type, a Review Mode auditor entering at the anti-patterns and stack, and a corpus maintainer arriving not to read at all but to verify the transplant still mirrors its origin.

**Recommended default and causal basis.** Arrive with a dispatch list or an audit purpose, and treat top-to-bottom reading as an origin-theme activity. The host bundle does the journey design upstream, its router hands each traveler a section list before they open this file, so journeys here begin mid-document by construction, and whole-file reading is reserved for the maintainer's diff, which needs no reading at all.

**Operating conditions and assumptions.** The router's section lists remain accurate, stale dispatch converts the implementer journey into blind search. Journeys through this copy in its host habitat, origin-bundle journeys belong to the origin theme.

**Failure boundary and alternatives.** A traveler ignoring their dispatch list and reading from the top spends their budget on the file's self-history, premise checks and rust-coder-01 critique, before reaching the boundary doctrine their task actually routed them toward. Bounded alternatives and recoveries: the origin theme's own three journeys for consumers working the doctrine outside this bundle.

**Counterexample, gotchas, and operational comparison.** Review Mode's entry sections, the anti-patterns and default stack, sit near the file's end, so auditors paging from the top traverse fifty patterns before their content, entry-point discipline matters most for the journey whose sections are deepest. Good: a parser implementer opening directly at the router-listed sections. Bad: an auditor starting at the premise check and running out of review time mid-file. Borderline: a curious implementer reading adjacent unrouted sections, harmless enrichment, not chargeable to the task.

**Verification, evidence, and uncertainty.** Ask recent consumers whether they entered via dispatch list, audit purpose, or drift. The router's section-level lists and the file's structure, both established this session and in assignment 47. The traveler mix in this habitat is unrecorded.

**Second-order consequence.** The maintainer's journey is the only one unique to this theme and it touches zero doctrine, its entire itinerary is one diff and one dated ledger entry, the cheapest journey in the corpus guarding one of the largest files in it, effort inversely proportional to the text protected.

**Revision decision.** Recast the scenario as the three arrivals, dispatched implementer at routed sections, auditor at the rejection and stack sections, maintainer at the diff command.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The Rust reliability engineer is starting from a requirement that needs a safe API, explicit error model, and testable boundary and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `rust_executable_reliability_reference` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing the idiomatic ownership, async, error, and crate-boundary shape before coding.
Reference opening trigger: Open this file when the task mentions rust executable reliability reference, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide how the transplant's packaging tensions get resolved and when the resolutions deserve reopening. The seed template rows skip the transplant's live tensions, fidelity versus habitat fit, where verbatim import preserved doctrine but carries another bundle's self-history, duplication versus reference, where copying bought self-containment at the price of fork risk, and depth versus dispatch, where 1541 lines of clauses need a router to stay consumable.

**Recommended default and causal basis.** Keep the frozen-text bias, spend maintenance on the metadata layer, and revisit the poles only on their named triggers. Each tension was resolved by the 202604 authors in one direction, verbatim, copy, and route, and each resolution created the maintenance obligations this theme's registry and probes now carry, tradeoffs converted into standing costs.

**Operating conditions and assumptions.** The metadata layer is actually maintained, frozen text over rotted metadata is worse than either strategy done fully. Tuning this habitat's packaging resolutions, the doctrine's own design tradeoffs belong to the origin theme.

**Failure boundary and alternatives.** Each resolution has a failure edge, fidelity fails when guests' self-instructions mislead host consumers, duplication fails at the first uncoordinated edit, and routing fails when dispatch rots, the chosen poles are cheap to adopt and priced in upkeep. Bounded alternatives and recoveries: forking deliberately with a divergence changelog, honest when habitats genuinely need different doctrine, unjustified while their needs remain identical.

**Counterexample, gotchas, and operational comparison.** The fidelity resolution quietly transfers editorial authority, improvements to this doctrine can only happen at the origin, so host-bundle maintainers who spot defects must fix upstream and re-mirror, a workflow nothing in either bundle documents. Good: a defect fixed in the origin copy and re-mirrored with a fresh diff. Bad: a quick fix applied to this copy alone. Borderline: annotating this copy with host-specific notes in a separate sidecar file, preserves byte identity, adds a new artifact to maintain.

**Verification, evidence, and uncertainty.** Audit recent maintenance events for upstream-first flow and coordinated mirroring. The resolutions visible in the bundle's structure and the obligations they created. Whether upstream-first maintenance will be followed without documentation is doubtful.

**Second-order consequence.** The three resolutions share a bias toward frozen text and living metadata, every adaptation lives in the router, the ledger, and the probes while the doctrine stays untouched, which concentrates all maintenance in small cheap artifacts and leaves the expensive text maintenance-free, a defensible inversion of where effort usually lands.

**Revision decision.** Add the three axes with their chosen poles, the created obligations, and the reversal triggers, fork detection reopens duplication versus reference, dispatch rot reopens depth versus routing.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the rust executable reliability reference pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong rust executable reliability reference guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which theme settles a question about text this theme shares byte-identically with another. The seed lone canonical row cannot express this theme's real structure, a mirrored text with a canonical origin, the rust-coder-02 copy holds editorial authority because its bundle authored the doctrine, this copy holds habitat authority for how the executable-specs bundle consumes it, and neither copy outranks the other on content because content is provably identical.

**Recommended default and causal basis.** Classify the question first, content and edits go upstream, consumption and composition resolve here. Authority follows authorship for text and follows habitat for usage, so the hierarchy splits by question type rather than by file, an arrangement single-copy themes never need.

**Operating conditions and assumptions.** The identity holds, divergence would force a genuine content hierarchy where none currently exists. Precedence between the two mirrored copies and their themes, doctrine-internal hierarchy belongs to the origin theme's coverage.

**Failure boundary and alternatives.** Content disputes settled against this copy invite fork pressure, each local ruling is one temptation to annotate the local text, while usage disputes settled at the origin import workflow assumptions from a bundle the asker is not in. Bounded alternatives and recoveries: declaring one copy authoritative for everything, simpler, and it makes half the questions travel to a bundle that cannot answer them.

**Counterexample, gotchas, and operational comparison.** The seed assigns this file supporting rank from file-position logic, but within its host bundle this transplant is the doctrine of record for two of four modes, supporting rank at corpus level coexists with primary rank in habitat, rank is scoped like authority. Good: routing a clause-wording dispute to the origin theme. Bad: settling host-router composition questions from the origin bundle's workflow. Borderline: a defect report against the doctrine filed here, acceptable intake, resolution still flows upstream.

**Verification, evidence, and uncertainty.** Test hierarchy claims by question type against the authorship and habitat split. The identity diff and both bundles' structures read across assignments 47 and 49. How disputes spanning both question types should sequence their resolution is undesigned.

**Second-order consequence.** Identical bytes carrying different authority is the corpus's clearest demonstration that authority is relational, nothing in either file differs, yet the correct answer to who rules depends entirely on whether the question is about the text or about a habitat, hierarchy lives in the edges, not the nodes.

**Revision decision.** Record the split hierarchy, origin canonical for doctrine content and edits, this theme canonical for host-bundle routing, gate composition, and consumption patterns.

**Retained seed evidence.** The single hierarchy row and the confidence warning remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.
Confidence warning: only one local corpus source is mapped, so this reference should invite adjacent-source discovery before becoming canonical policy.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202604/rust-executable-specs-01/references/rust-reliability-reference.md | canonical primary source | Rust Coder 02; Premise Check; What rust-coder-01 Gets Right | What guidance, warning, or example should this source contribute to Rust Executable Reliability Reference? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing evidence makes the mirror relationship visible to maintainers and tooling. The seed generic worked-example artifact misses this theme's natural object, a mirror ledger, one standing record holding the pair of paths, the latest diff result with its date, the routing dependencies that break on divergence, the host router's section references into this file, and the upstream-first maintenance rule for edits.

**Recommended default and causal basis.** Create the ledger at the first audit touching either copy and gate edits to either path on consulting it. Every obligation this theme created, fork detection, dispatch integrity, delegation of freshness, reads from the same small fact set, so one artifact holding those facts serves all the probes instead of each rediscovering them.

**Operating conditions and assumptions.** A predictable ledger location exists, an unfindable ledger protects nothing. One ledger per mirrored pair, gate records belong to the gates theme and routing manifests to the reference-maps theme.

**Failure boundary and alternatives.** Without the ledger, each audit re-derives the pair relationship from scratch, and the first maintainer who does not know the second copy exists edits confidently, the fork arriving through ignorance rather than intent. Bounded alternatives and recoveries: encoding the relationship in a corpus-wide duplicate index covering all mirrored files at once, better at scale, heavier to bootstrap for one pair.

**Counterexample, gotchas, and operational comparison.** The ledger itself can rot, dependent-router entries go stale as bundles evolve, so the ledger needs its own refresh trigger, folding it into the existing audit cadence keeps it from becoming a second maintenance orphan. Good: an editor consulting the ledger and routing their fix upstream. Bad: an audit re-running discovery because the last audit's findings lived in a chat log. Borderline: a ledger with a stale diff date, still names the relationship, the expired measurement must rerun before edits proceed.

**Verification, evidence, and uncertainty.** Check the ledger exists at its location with all five fields current. The fact set this theme's obligations repeatedly consume. Who owns ledger upkeep between two themes' audit cadences is unassigned.

**Second-order consequence.** The ledger converts tribal knowledge into corpus structure, today the pair relationship exists only in this evolution's prose, discoverable by whoever reads this theme, while a ledger at a predictable location makes the relationship findable by tooling and by maintainers who never read reference documents.

**Revision decision.** Define the artifact as the mirror ledger with five fields, paths, dated diff, dependent routers, section references, maintenance rule, refreshed each audit cycle.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: reference freshness workflow with canonical source policy and update checklist.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Rust Executable Reliability Reference | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercises build the habits this habitat's counterintuitive maintenance rule requires. The seed abstract usage lines stand where this theme wants a maintenance rehearsal, present a maintainer with a realistic defect in this copy, a stale crate reference, and require the full correct flow, consult the mirror ledger, fix at the origin, re-mirror, re-diff, update the ledger date, and verify the host router's sections still resolve.

**Recommended default and causal basis.** Run the rehearsal with anyone who will maintain archive material and the consumption drill with new bundle consumers. The transplant's failure modes all begin at a maintenance event handled wrong, so the training moment is the edit impulse, and rehearsing the upstream-first flow once builds the habit the undocumented workflow otherwise leaves to chance.

**Operating conditions and assumptions.** A realistic seeded defect exists, drilling on invented typos teaches the flow without the judgment of what warrants an upstream fix. Teaching this theme's maintenance and consumption flows, doctrine-content exercises belong to the origin theme.

**Failure boundary and alternatives.** Unrehearsed maintainers follow ordinary instinct, fix the file in front of you, which is correct everywhere else in the corpus and wrong exactly here, the habitat's one counterintuitive rule is the one that needs drilling. Bounded alternatives and recoveries: documentation alone at the ledger location, cheaper, and the failure statistics of unrehearsed counterintuitive rules argue against relying on it.

**Counterexample, gotchas, and operational comparison.** The rehearsal must include the tempting wrong path explicitly, learners should see the local edit compile, pass, and look done before learning why it forked the corpus, prevention lands harder after the shortcut looks safe. Good: a trainee routing the seeded fix upstream and producing a fresh dated diff. Bad: certifying maintenance readiness from doctrine knowledge alone. Borderline: a trainee proposing deliberate divergence with a changelog, wrong for the seeded case, evidence they understand the alternatives.

**Verification, evidence, and uncertainty.** Audit the trainee's rehearsal record for ledger consultation before any edit. The failure-at-maintenance-event structure of every transplant risk this theme names. Reflex persistence between rare maintenance events is unmeasured.

**Second-order consequence.** The maintenance rehearsal's hardest step is the first one, remembering the ledger exists, every later step is mechanical, so the drill's real payload is a reflex, check for mirrors before editing archive files, a two-second habit that neutralizes the theme's costliest failure.

**Revision decision.** Anchor the section on the maintenance rehearsal plus a consumption drill, dispatch a learner through the router to their task-type sections and have them state which gate layers their close owes.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Rust Executable Reliability Reference after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Rust Executable Reliability Reference as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Rust Executable Reliability Reference only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which measurements prove the transplant's obligations are being met. The seed compile-centric indicator misses what this habitat should measure, mirror integrity over time, consecutive audits with the identity diff passing, dispatch resolution rate, router section references that still land, and upstream-flow compliance, maintenance events that consulted the ledger and routed edits to the origin.

**Recommended default and causal basis.** Fold all three collections into the existing audit cadence and review the series at corpus refresh. Each metric watches one created obligation, integrity watches the fork risk, resolution watches the rot risk, and flow compliance watches the counterintuitive rule most likely to be violated in good faith.

**Operating conditions and assumptions.** Maintenance events are visible, edits that bypass any review defeat flow measurement entirely. Measuring this habitat's transplant obligations, doctrine-adoption outcomes belong to the origin theme's metrics.

**Failure boundary and alternatives.** Unmeasured, the obligations decay silently in their characteristic ways, the fork arrives as maintenance, the rot arrives as bundle evolution, and the first symptom of either is a consumer served wrong structure with full confidence. Bounded alternatives and recoveries: measuring nothing until the first incident, the default path, which converts the first fork into the discovery mechanism.

**Counterexample, gotchas, and operational comparison.** Flow compliance has a denominator problem, maintenance events on this pair are rare, so a perfect score over one event proves little, the metric matures only across years or across all mirrored pairs pooled. Good: six consecutive audits with identity passing and all router references landing. Bad: discovering divergence from a consumer's contradiction report. Borderline: a failed resolution check caused by deliberate origin-side restructuring, the metric fired correctly, the repair is router-side.

**Verification, evidence, and uncertainty.** Confirm the three series exist in audit records and were reviewed at the last refresh. The obligation structure this theme established across its sections. Pooling strategy across the corpus's other mirrored pairs is undesigned.

**Second-order consequence.** Integrity and resolution are boolean per audit, making their healthy state a flat unbroken series, so the metric consumers should learn to read absence of change as success, a reporting inversion, most metrics reward movement while these reward a straight line.

**Revision decision.** Add the three metrics with collection points, the audit record for integrity, the heading grep output for resolution, and maintenance-event review for flow compliance.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: compile attempts and review comments decrease because the API shape is explicit before implementation.
Failure signal: the reference hides ownership or error tradeoffs behind generic guidance.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this transplant reference is declared faithful. The seed shape items never audit this theme's distinctive claims, the identity assertion with its date, the delegation of doctrine coverage to the origin theme, the host router's section references into this file, and the two-layer gate composition, none of which any heading count can confirm.

**Recommended default and causal basis.** Run the relational audit alongside the standard count audit at every reread cycle. This theme's substance is relational, its claims describe how two copies, two bundles, and two gate lineages connect, so completeness auditing must check relations, diff results, resolving references, standing delegations, rather than local document properties.

**Operating conditions and assumptions.** All referenced paths remain stable, moved files convert relation checks into provenance investigations first. Auditing this reference document against its relational claims, the mirrored text's own content audit belongs to the origin theme.

**Failure boundary and alternatives.** A shape-only audit could pass this document perfectly while the identity has broken, the router has moved on, and the delegation points at a restructured origin theme, every distinctive claim stale behind intact headings. Bounded alternatives and recoveries: checksum-based monitoring replacing scheduled diffs, continuous instead of periodic, at tooling cost the audit cadence currently avoids.

**Counterexample, gotchas, and operational comparison.** The delegation item is the easiest to skip because its failure is invisible locally, the origin theme could be restructured tomorrow and nothing in this document would look different, only the cross-check catches a delegation pointing at absent coverage. Good: an audit re-running the diff and re-gripping router references in one pass. Bad: declaring fidelity because all 26 headings exist. Borderline: accepting a one-cycle-old diff date under audit time pressure, tolerable once, the next cycle owes a fresh measurement.

**Verification, evidence, and uncertainty.** Execute the relational items and date each result in the audit record. The relational claim structure of every distinctive statement this document makes. Whether shared relational audit tooling will be built is an open corpus question.

**Second-order consequence.** This checklist and the reference-maps checklist share the dated-relation problem and could share tooling, one relational audit script serving every theme whose claims span files, the corpus's duplicated-material themes are converging on a common audit shape.

**Revision decision.** Add the relational items, re-run the identity diff, grep the router's section references, confirm the origin theme still covers what is delegated to it, and re-check the gate composition against both source documents.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Rust Executable Reliability Reference.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a question arriving at a theme that owns only its delta. The seed token-split rows point nowhere while this theme's neighbors are precisely determined by its structure, the origin rust-coder reliability theme for all doctrine content, the reference-maps theme for the host router's own patterns, the gates theme for the bundle's command floor, and the backend and testing themes for the domains the doctrine's sections compress.

**Recommended default and causal basis.** Answer mirror and composition questions here and forward everything else with the owning theme named. A mirror theme owns almost nothing, its content authority delegates upstream and its habitat authority is thin by design, so nearly every substantive question that arrives here is a forwarding decision, and the routing table is the theme's main working surface.

**Operating conditions and assumptions.** The origin theme remains the doctrine's evolved home, its restructuring would require re-pointing every forward. Sending away what this theme cannot own, destination summaries are deliberately absent.

**Failure boundary and alternatives.** Answering doctrine questions locally rebuilds the origin theme's coverage in parallel, the documentation equivalent of the fork this theme's registry rejects, divergence through independent answering rather than independent editing. Bounded alternatives and recoveries: for questions about the unread playbook sibling the router references, the honest answer remains unread-source, flagged rather than improvised.

**Counterexample, gotchas, and operational comparison.** Questions about differences between the copies are this theme's to answer even though they sound like content questions, difference claims are relation claims, and the mirror ledger, not the origin theme, holds the evidence. Good: forwarding a cancel-safety clause question upstream while answering the gate-composition question locally. Bad: paraphrasing doctrine sections here to save the asker a hop. Borderline: a question about the doctrine's fitness for the host bundle's consumers, genuinely spans both, answer the habitat half and forward the content half.

**Verification, evidence, and uncertainty.** Sample recent answers for delta-versus-inheritance classification. The delegation structure established across this document's sections. Forward-hop friction for askers is the cost this routing rule accepts unmeasured.

**Second-order consequence.** This theme's keep-set is exactly its additions to the corpus, everything it did not add, it forwards, which is the cleanest possible routing rule and a template for any future mirror theme, own your delta, forward your inheritance.

**Revision decision.** Route by the established split, content and clauses to the origin theme, router mechanics to the reference-maps theme, floor commands to the gates theme, and keep only mirror maintenance, habitat composition, and consumption-pattern questions.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use backend, executable, or quality-gate Rust references when the question shifts to HTTP delivery, specs, or review gates.
Adjacent reference 1: consider the rust adjacent reference when the current task pivots away from rust executable reliability reference.
Adjacent reference 2: consider the executable adjacent reference when the current task pivots away from rust executable reliability reference.
Adjacent reference 3: consider the reliability adjacent reference when the current task pivots away from rust executable reliability reference.

## Workload Model

**Decision supported.** This section helps decide how to budget work whose danger is being too small to remember rather than too large to afford. The seed symbol-count boundary misprices mirror work, whose units are per-audit relational cost, the diff, the grep, and the ledger refresh, per-maintenance-event cost, the upstream-first flow with re-mirroring, and per-consultation cost, which this theme adds nothing to because consumption effort belongs to the doctrine and the router.

**Recommended default and causal basis.** Attach all mirror upkeep to existing audit and maintenance events and never let it own an independent calendar. The theme's own work is pure metadata upkeep, minutes per audit cycle and a bounded flow per rare edit, so its workload rounds to near-zero between events, the cheapest theme in the cluster to keep healthy.

**Operating conditions and assumptions.** The host cadences keep running, mirror upkeep inherits any lapse in the cadence it rides. Budgeting this theme's mirror obligations, consumption and implementation workload belong to the doctrine and routing themes.

**Failure boundary and alternatives.** The mispricing risk runs opposite to most themes, this work is so cheap it gets skipped as trivial, and skipped relational audits are how forks and rot accumulate their silent quarters. Bounded alternatives and recoveries: the seed's crate-slice and symbol boundaries, retained as outer limits on any single consultation's scope.

**Counterexample, gotchas, and operational comparison.** The maintenance-event unit has high variance hiding in the re-mirror step, an origin-side fix that touches sections the host router lists triggers router updates too, occasionally converting a minutes-long flow into a cross-bundle change. Good: audit records showing the diff and grep appended to every reread cycle. Bad: a standalone mirror-audit calendar entry skipped four quarters running. Borderline: batching ledger updates across two audit cycles, tolerable for the ledger, never for the diff itself.

**Verification, evidence, and uncertainty.** Check that mirror upkeep appears inside host cadence records rather than on its own lapsed schedule. The magnitude analysis of every obligation this theme established. Actual edit-event frequency for this pair over the corpus's life is unknown.

**Second-order consequence.** Cheap standing obligations survive best when attached to expensive ones, the relational audit should ride the corpus's existing reread cadence rather than owning a schedule, because a task too small to schedule is a task that silently stops, piggybacking is the survival strategy for near-zero workloads.

**Revision decision.** Restate the model in its three units and their expected magnitudes, minutes per audit, one bounded flow per edit event, zero marginal consultation cost.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Rust Executable Reliability Reference as a rust system operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | systems implementation, CLI or service hardening, async/runtime review, and safety-sensitive refactoring where compile success is necessary but not sufficient | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one crate or bounded workspace slice, up to 100 changed symbols, and one integration path that exercises error handling | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Rust Coder 02; Premise Check; What rust-coder-01 Gets Right; Where rust-coder-01 Is Thin; Source Pool; Selection Thesis | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is reference freshness workflow with canonical source policy and update checklist | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which mirror invariants must demonstrably hold for this theme's guidance to count as adopted. The seed document-property thresholds stand where mirror invariants should, the identity diff passing at every audit with no gap longer than one cycle, every router section reference resolving continuously, every edit event showing ledger consultation and upstream-first flow, and the delegation to the origin theme remaining live and accurate.

**Recommended default and causal basis.** Hold all four invariants through the audit cadence with the delegation cross-check at corpus refresh. Each invariant guards one relation this theme created or depends on, and together they define what a healthy mirror means operationally, identical, navigable, maintained upstream-first, and honestly delegated.

**Operating conditions and assumptions.** Audit cycles run on schedule, every invariant's guarantee window is exactly the audit gap. Mirror invariants for this theme, code-level reliability targets belong to the doctrine's own coverage.

**Failure boundary and alternatives.** The invariants fail toward different victims, broken identity misleads future evolvers, broken resolution misleads routed consumers, broken flow forks the corpus, and broken delegation strands doctrine questions between two themes each assuming the other answers them. Bounded alternatives and recoveries: structural enforcement by replacing the copy with a reference, which would convert three invariants into impossibilities at the cost of the bundle's self-containment.

**Counterexample, gotchas, and operational comparison.** The flow invariant cannot be verified preventively, only retrospectively per event, so its health between events is an assumption, which argues for the rehearsal drills that make the assumption safer rather than for more measurement. Good: an unbroken dated-diff series across the audit history. Bad: declaring mirror health from the absence of complaints. Borderline: one audit cycle missed during corpus-wide work, the window doubled honestly, the next cycle's diff carries double weight.

**Verification, evidence, and uncertainty.** Review the four invariant series at each corpus checkpoint with dated evidence attached. The relation structure this theme's sections established and depend upon. The audit-gap length that keeps fork risk acceptably low is a judgment call.

**Second-order consequence.** The delegation invariant is the novel one, delegating coverage creates a standing dependency most reliability thinking never models, theme A's correctness now depends on theme B continuing to exist in a compatible shape, inter-document reliability rather than intra-document, and only an explicit cross-check keeps it real.

**Revision decision.** Publish the four invariants with evidence methods, dated diffs, grep outputs, maintenance-event records, and periodic delegation cross-checks against the origin theme's actual coverage.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which mirror-decay modes need standing probes and where each probe must live. The seed generic rows miss how a mirror specifically dies, silent fork, one copy edited without the ledger flow, dispatch orphaning, origin-side or local restructuring breaking the router's section references, delegation drift, the origin theme evolving until the coverage this theme points at no longer exists in the pointed shape, and ledger death, the mirror ledger itself rotting until the relationship becomes tribal knowledge again.

**Recommended default and causal basis.** Run the four probes on the audit cadence with the ledger-freshness check anchored in the corpus-level record. All four are relation failures with independent drivers, editors cause forks, restructuring causes orphaning, corpus evolution causes drift, and neglect causes ledger death, so no single practice prevents them all, each needs its own probe.

**Operating conditions and assumptions.** Probes have owners, four owned probes beat ten aspirational ones. Decay of this theme's mirror relations, doctrine-content decay belongs to the origin theme's failure table.

**Failure boundary and alternatives.** The modes chain, ledger death enables silent fork, silent fork invalidates delegation, and the chain's end state is two diverged doctrines each claiming the other's authority, the exact condition the transplant strategy existed to prevent. Bounded alternatives and recoveries: periodic full re-mirroring from the origin regardless of detected drift, brute-force but self-healing, at the cost of masking whether the flow discipline actually works.

**Counterexample, gotchas, and operational comparison.** Silent fork's most likely vector is not carelessness but tooling, a corpus-wide search-and-replace or formatting pass touching both archive folders unevenly, the probe must therefore diff full bytes, not just the sections humans edit. Good: an audit catching a formatting pass that touched one copy only. Bad: trusting the ledger's diff date after two skipped cycles. Borderline: origin-side restructuring that improves the doctrine while orphaning two router rows, correct upstream move, the repair bill lands in the host bundle.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe, owner, cadence, and probe location. The chained relation structure of the four modes as established across this document. Which mode would manifest first in this corpus's actual practice is unknown.

**Second-order consequence.** Ledger death is the meta-failure that disables detection of the others, its probe is therefore the one that cannot itself live in the ledger, freshness of the detection layer must be checked from outside it, in the corpus audit record, a two-level watchfulness this theme's structure makes explicit.

**Revision decision.** Add the four rows with probes, the audit diff for forks, the heading grep for orphaning, the delegation cross-check for drift, and a ledger-freshness check for ledger death.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| runtime contention masks failure | async tasks, locks, or shared state degrade under concurrent load | add contention benchmark, timeout budget, and structured cancellation path |
| panic path reaches production | unwrap or unchecked invariant survives compile and unit tests | require panic audit, Result propagation, and integration failure fixture |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide which artifact answers retry and backpressure questions for host-bundle consumers. The seed corpus-process bullets stand beside what the transplanted doctrine itself teaches about retry, bounded channels making overload visible, cancellation tokens making shutdown cooperative, cancel-safe select preventing retried futures from corrupting state, and backpressure as a first-class design surface, all served in this habitat through the router's async-service reading order.

**Recommended default and causal basis.** Hand retry and backpressure questions the routed section, not the compressed list, and require repair-level answers to cite clause text. In this habitat the retry doctrine arrives pre-routed, an async-service task's dispatch list includes the concurrency section where these clauses live, so the habitat's contribution is delivery, ensuring the consumer who needs retry doctrine is handed the section that contains it.

**Operating conditions and assumptions.** The router's async reading order stays aligned with the section's actual heading, the standing rot dependency. How retry doctrine reaches consumers in this habitat, the clauses' full content belongs to the origin theme.

**Failure boundary and alternatives.** The habitat betrays that delivery when consumers take the merge shortcut, whose compression names unbounded channels as rejected but drops the cancel-safety clauses, retry guidance reduced to a prohibition without its repair patterns. Bounded alternatives and recoveries: the backend themes' delivery-focused retry guidance for HTTP-specific concerns layered above these primitives.

**Counterexample, gotchas, and operational comparison.** The doctrine's retry clauses assume a tokio-shaped runtime, consumers in this bundle working other runtimes inherit the principles but not the named primitives, a translation burden the habitat does not document. Good: an overload design citing the bounded-channel and cancellation clauses from the routed section. Bad: a retry policy justified solely by the merge's one-line rejection. Borderline: infrastructure-level retries below the application, legitimate layering the clauses do not cover, the design should state the assumption.

**Verification, evidence, and uncertainty.** Audit retry-bearing designs in this habitat for clause-level citations. The doctrine's concurrency clauses established in assignment 47 and the router's async dispatch from assignment 49. How often consumers take the compression shortcut in practice is unrecorded.

**Second-order consequence.** Retry doctrine is the sharpest case for the fidelity strategy, prohibitions compress well but repairs do not, a list can say never unbounded channels in one line while bounded-channel sizing, cancellation wiring, and select discipline take pages, so the content that most needs the verbatim transplant is exactly the content compressions lose first.

**Revision decision.** State the delivery guarantee, async and retry questions in this bundle route to this file's concurrency section, with the merge's compressed rows treated as review vocabulary only.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide which records each layer owes and where they must live. The seed generic records stand beside two observability layers this habitat composes, the doctrine's own emission rules, structured tracing with spans and correlation IDs, secrecy wrappers for credentials, actionable diagnostics with exact locations, and the mirror layer's records, the dated diff series, grep outputs, and ledger state that make the transplant itself observable.

**Recommended default and causal basis.** Emit mirror records at their trigger events and route code-emission questions to the routed doctrine sections. Code observability travels with the doctrine into any habitat unchanged, while transplant observability exists only here, created by the duplication, so the checklist splits by layer with no overlap.

**Operating conditions and assumptions.** Records live in findable locations, the audit trail and the ledger, scattered evidence observes nothing. What this habitat must record at both layers, full telemetry design belongs to the doctrine and ops themes.

**Failure boundary and alternatives.** The layers fail independently and mislead differently, unobservable code hides runtime failures from operators, an unobservable mirror hides corpus failures from maintainers, and only the second is this theme's to fix. Bounded alternatives and recoveries: continuous checksum monitoring replacing periodic records, more coverage, less human-readable history.

**Counterexample, gotchas, and operational comparison.** The doctrine's secrecy rule has a mirror-layer echo, audit records quoting file content should quote structure rather than secrets-adjacent examples, an audit trail is itself an emission surface subject to the discipline it documents. Good: an audit trail from which the mirror's full history reconstructs. Bad: a maintenance event whose only trace is a commit message. Borderline: recording grep output only on failure, compact, it forfeits the healthy-series evidence the straight-line metric needs.

**Verification, evidence, and uncertainty.** Reconstruct the mirror's state history from the records alone at the next refresh. The doctrine's observability clauses and the record set this theme's sections established. Record retention across corpus reorganizations is unguaranteed.

**Second-order consequence.** The mirror layer's records are observability in its purest form, they exist solely so that a future reader can reconstruct what was true when, no runtime, no spans, just dated evidence against silent change, the same epistemics as tracing applied to a filesystem relationship.

**Revision decision.** Keep the doctrine's emission rules by reference and specify the mirror records concretely, each audit appends a dated diff result and grep outcome, each maintenance event appends a flow record.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture error count, timeout count, retry count, saturation signal, and rollback trigger.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide which composed rules govern performance claims and optimizations for work consulting this transplant. The seed latency packet stands beside what the transplanted doctrine actually teaches about performance, correctness-preserving optimization only, panic-free arithmetic ranked above ergonomic speedups, prepare-once precomputation justified by local reasoning, and measurement before belief, delivered in this habitat through the parser and performance-relevant reading orders.

**Recommended default and causal basis.** Require stated thresholds for any performance claim and doctrine-cited correctness preservation for any optimization. The doctrine's performance stance is a subordination rule, speed claims enter only after correctness boundaries hold, and the habitat inherits that rule wholesale along with the host bundle's own threshold discipline from its requirement template.

**Operating conditions and assumptions.** Both rules are actually applied together, each alone leaves half the failure surface open. How performance verification doctrine reaches this habitat's consumers, benchmark mechanics belong to the origin and testing themes.

**Failure boundary and alternatives.** Performance work that skips the subordination rule optimizes into the exact panic edges and boundary violations the doctrine's other sections exist to prevent, fast code failing the twelve rejections. Bounded alternatives and recoveries: the backend themes' service-level latency budgets layered above these rules for HTTP-facing work.

**Counterexample, gotchas, and operational comparison.** This theme's mirror layer has a performance story of its own worth one line, the identity diff on a 1541-line file completes in milliseconds, so no efficiency argument ever justifies skipping it, the cheapest verification in the corpus guards one of the largest files. Good: an optimization citing preserved panic-freedom with a before-after benchmark against a stated threshold. Bad: a speedup that replaced checked arithmetic with raw indexing. Borderline: a precomputation with local-reasoning justification but no benchmark, the doctrine tolerates it, the host bundle's claim rule bars advertising it.

**Verification, evidence, and uncertainty.** Sample performance changes for both the threshold citation and the correctness-preservation citation. The doctrine's performance sections and the host template's units rule, both previously read in full. How often the two rules are applied jointly in practice is unmeasured.

**Second-order consequence.** The two lineages meet frictionlessly here as with gates, the host bundle says no performance claim without units and method, the doctrine says no optimization without preserved correctness, one governs claims and the other governs changes, composition without conflict for the same structural reason, disjoint triggers.

**Revision decision.** Compose the two inheritances explicitly, thresholds and methods specified per the host bundle's units rule, optimizations bounded by the doctrine's correctness-first subordination.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require memory, panic, and concurrency evidence; hot-path operations need p95 under 10 ms locally or an explicit benchmark exception.
Leading indicator to measure: compile attempts and review comments decrease because the API shape is explicit before implementation.
Failure signal to watch: the reference hides ownership or error tradeoffs behind generic guidance.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what copy count and at which consultation domains this theme's strategy and coverage stop applying. The seed task-splitting bounds stand beside this theme's real scale limits, the mirror strategy scales poorly with copy count, every additional duplicate multiplies diff pairs and ledger entries, the doctrine's own coverage claim excludes compiler internals, kernel-grade lock-free work, deeply embedded no_std, and advanced proc-macro internals, and the habitat inherits those exclusions verbatim.

**Recommended default and causal basis.** Hold the pair-level ledger while copies number two, adopt the index at three, and surface the coverage exclusions wherever the file is newly embedded. Mirror obligations grow quadratically in the worst case, pairwise identity among n copies, while the doctrine's exclusions are authored limits that transplantation cannot widen, copying a document never extends its competence.

**Operating conditions and assumptions.** New duplications are noticed at all, the index trigger assumes the corpus knows its own copy count. Bounding this theme's mirror strategy and inherited coverage, the doctrine's internal scale seams belong to the origin theme.

**Failure boundary and alternatives.** Past either limit the failure is quiet overconfidence, a corpus with five copies and no identity index serves diverged doctrine somewhere, and a consumer applying this file to kernel lock-free design exits its coverage with full confidence and no warning from the text. Bounded alternatives and recoveries: canonical-plus-symlink architecture at higher copy counts, which collapses the quadratic obligation back to one authoritative node.

**Counterexample, gotchas, and operational comparison.** The exclusions sit inside the file's own coverage-claim section, which routed consumers may never reach, their dispatch lists target boundary sections, so the habitat should carry the exclusions in its routing metadata rather than trusting deep-file discovery. Good: a third-copy event triggering the index migration. Bad: five copies each pairwise-ledgered by hand. Borderline: consulting the file for moderately low-level systems code, inside the claim's stated coverage, near enough its edge to note the judgment.

**Verification, evidence, and uncertainty.** At each duplication or out-of-domain event, re-test both limits and record which trigger fired. The doctrine's coverage-claim section and the combinatorics of mirror obligations. Whether the corpus will ever hold a third copy is unknown, the trigger exists regardless.

**Second-order consequence.** The two limits interact through consumer trust, duplication increases the doctrine's availability while coverage exclusions bound its applicability, so wider mirroring spreads a document further beyond audiences who know its limits, scale in copies quietly becomes scale in misapplication risk.

**Revision decision.** Name both limits with triggers, a third copy of any bundle file triggers the corpus-wide duplicate index over per-pair ledgers, and out-of-coverage consultations route to specialist sources the corpus does not currently hold.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Rust Executable Reliability Reference stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal that this theme's relations have gone stale. The seed theme-name queries would surface generic reliability content while this theme's staleness has exactly three homes, the identity relation between the two copies, the host router's section references into this file, and the origin theme's coverage that this theme delegates to, every other claim here ages only as those relations age.

**Recommended default and causal basis.** Run the three probes at each corpus refresh, inherit the origin theme's external findings, and date everything. A mirror theme makes no independent doctrine claims, so it has no independent doctrine staleness, its refresh surface is purely relational and its probes are the same diff, grep, and cross-check its audit already runs, refresh and audit collapse into one activity here.

**Operating conditions and assumptions.** The origin theme's probes actually run, delegated freshness is only as fresh as the delegate. Refreshing this document's relational claims, doctrine freshness delegates to the origin theme's own probe set.

**Failure boundary and alternatives.** Name-based searching returns rust reliability material that neither confirms nor denies whether these two files still match, the only operational question this theme's refresh must answer. Bounded alternatives and recoveries: duplicating the origin theme's external probes locally, safe and wasteful, the delegation exists to prevent exactly that.

**Counterexample, gotchas, and operational comparison.** The delegation cross-check needs more than existence, the origin theme must still cover the specific delegated subjects, doctrine content, external freshness, and internal hierarchy, a restructured origin theme could exist happily while covering less than the delegation assumes. Good: a dated three-probe run appended to the refresh record. Bad: logging that rust reliability searches returned nothing new. Borderline: probing whether newer reliability references supersede the doctrine, real research, it refreshes the origin theme's claims, not this theme's relations.

**Verification, evidence, and uncertainty.** Record each probe with its date and the specific relation it confirmed or flagged. The purely relational claim structure established across this document. Actual relational drift since this baseline is unknowable until the probes run.

**Second-order consequence.** This is the corpus's first theme whose refresh queries contain no external target at all, even the toolchain-sensitive content inside the file refreshes upstream through the origin theme, a fully internal refresh profile that demonstrates delegation working as designed, one theme's probes covering two themes' text.

**Revision decision.** Replace the name queries with the three relational probes, the identity diff, the router-reference grep, and the delegation cross-check, each dated against this assignment's baseline.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | rust executable reliability reference official documentation best practices |
| `github_repository_query_phrase` | rust executable reliability reference GitHub repository examples |
| `release_notes_query_phrase` | rust executable reliability reference changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label and with what chain declaration each claim here may be reused. The seed label definitions stand without this assignment's distinctive ledger, zero new lines read from the mapped file itself, the content established by assignment 47's full read of byte-identical text, the identity established by a dated diff this session, the host-habitat claims established by assignment 49's full reads, and zero external URLs fetched.

**Recommended default and causal basis.** Before reusing a claim, identify which assignment's evidence grounds it and whether a dated measurement sits in the chain. This evolution is built almost entirely on previously established evidence plus one new measurement, so its boundary notes must document evidence reuse across assignments, a provenance pattern the label set was not designed around.

**Operating conditions and assumptions.** The referenced assignments' packets and journals remain in the corpus as the chain's anchors. Reuse rules for this document's claims, each claim travels with its actual provenance hop count.

**Failure boundary and alternatives.** The costliest mislabel would flatten the reuse chain, presenting assignment 47's read as this assignment's read, or the diff-dated identity as a permanent fact, both flattenings would survive casual review because the text they support is genuinely accurate today. Bounded alternatives and recoveries: re-reading the mapped file in full here to flatten the chain to one hop, honest and redundant, the diff makes it unnecessary.

**Counterexample, gotchas, and operational comparison.** The two-hop structure means a future correction to assignment 47's reading would silently invalidate claims here, cross-theme errata have no propagation mechanism in this corpus, a gap worth naming even without solving. Good: reusing a clause claim citing the origin theme with the identity date attached. Bad: citing this theme as an independent second witness to the doctrine. Borderline: reusing the habitat claims without naming assignment 49, acceptable informally, the chain should survive in any formal reuse.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares its chain cleanly. This assignment's ledger, one diff run, zero new reads of the mapped file, zero retrievals. Readers can check the chain's anchors but not the readings behind them.

**Second-order consequence.** Cross-assignment evidence reuse is how a corpus becomes more than its files, assignment 47's reading labor now grounds two themes, and the only added cost was one diff, but the ledger discipline is what makes the reuse honest rather than circular, provenance chains must be declared to be legitimate.

**Revision decision.** Publish the strata explicitly, doctrine content as local corpus fact via the origin read plus dated identity, habitat structure as local corpus fact via the assignment 49 reads, mirror machinery as this evolution's inference, and the playbook sibling as unread-source description.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
