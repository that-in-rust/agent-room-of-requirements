# Skill Creator Evaluation Patterns

**Decision supported.** This section helps decide how to treat two same-month creator systems with opposed evaluation philosophies. The seed no seed line states what this theme actually spans, two whole skill-creation systems from the same month, the Claude plugin bundle whose 479-line entrypoint plus three agent prompts and a schema sheet build a quantitative evaluation harness, and the Codex system skill whose 416 lines teach principles-first authoring with forward-testing, plus a live-tree copy of the Claude bundle verified byte-identical to its archive.

**Recommended default and causal basis.** Route measurement questions to the Claude harness material and authoring-discipline questions to the Codex guide, citing the live-identity fact when currency matters. Both platforms shipped a skill for making skills in 202603, and each encoded its platform's evaluation philosophy, Claude measuring skills with baselines, assertions, and benchmark deltas, Codex stress-testing them through uncontaminated subagent runs, so the theme's real subject is how skill quality gets proven, not just how skills get written.

**Operating conditions and assumptions.** The live copies stay identical to the archive, any divergence reopens the currency caveat. This reference covers the two creator systems and their evaluation methods, the skills they create are governed by their own themes.

**Failure boundary and alternatives.** Reading the bundle as one doctrine merges two incompatible evaluation designs, a benchmark harness that needs baseline runs and exact JSON schemas, and a forward-testing discipline that forbids leaking context to the tester, advice from one misapplied under the other breaks both. Bounded alternatives and recoveries: the platform-entrypoint themes for how these two ecosystems structure skills generally, this theme owns the evaluation machinery specifically.

**Counterexample, gotchas, and operational comparison.** The Claude entrypoint is conversational and first-person in places, cool-cool asides and all, quoting it verbatim into formal guidance imports a register the Codex guide deliberately avoids. Good: citing the benchmark schema for a Claude eval run and forward-testing rules for a Codex validation. Bad: demanding baseline benchmark runs inside a Codex forward-test. Borderline: borrowing the grader's evidence discipline for Codex subagent reviews, cross-platform but philosophically compatible.

**Verification, evidence, and uncertainty.** Re-run the live-versus-archive diffs before relying on the identity claim. Full reads of all six unique documents and five identity diffs this session. Whether either platform revised its creator after 202603 is outside the corpus.

**Second-order consequence.** The live claude-skills tree carrying a byte-identical copy of the archived plugin means the archive is not merely historical here, it is a verified snapshot of the working tool, the only theme so far whose ancestor evidence and current deployment are provably the same bytes, collapsing the archive-versus-live caveat that every other theme must carry.

**Revision decision.** Open with the two-system inventory, the identical live copy, and the evaluation-philosophy split as the organizing contrast.

**Retained seed evidence.** The exact theme title and reference framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide how much independent weight each of the eleven listed rows carries. The seed eleven local rows overstate the evidence base, five archived Claude files, their five live-tree twins verified byte-identical this session, and one Codex system skill, so the honest count is six unique documents totaling 2024 lines, with the five duplications carrying their own information, proof that the archived plugin is the deployed plugin.

**Recommended default and causal basis.** Cite the archived paths for stable references and note the live-tree identity when a consumer needs deployment currency. The live tree ships the same plugin the archive froze, so the duplicate rows are deployment evidence rather than redundancy, a different duplicate species from the frozen resweeps, identity across trees rather than across months.

**Operating conditions and assumptions.** Both trees remain in the repository and the diffs stay re-runnable. Provenance for this document's statements, the three external URLs stayed unretrieved and confer no current-fact authority.

**Failure boundary and alternatives.** Counting eleven independent sources would double-weight every Claude claim, while dismissing the twins as mere copies would discard the deployment-identity fact that makes this theme's evidence unusually current. Bounded alternatives and recoveries: a lineage-index entry recording the archive-live twin relation for all five pairs at once.

**Counterexample, gotchas, and operational comparison.** The Codex skill lives under a .system directory, a hidden path that casual tree listings skip, its row is the easiest of the eleven to overlook and the only non-Claude evidence in the theme. Good: a harness claim cited once to the archived SKILL.md with the live identity noted. Bad: archive and live rows cited as corroborating sources. Borderline: citing the live path for a currently-deployed-behavior claim, correct emphasis, same bytes either way.

**Verification, evidence, and uncertainty.** Re-run the five identity diffs at each audit cycle. The diff runs and six full reads from this session. Whether the live tree tracks upstream updates automatically or by manual copy is unrecorded.

**Second-order consequence.** This theme has the corpus's richest verified evidence, six documents spanning entrypoint, three agent prompts, a schema sheet, and a rival platform's counterpart, all read in full, plus cross-tree identity, so its synthesis can compare systems at the prompt-role level, entrypoint to entrypoint, grader to validation rules, a granularity no single-file theme can reach.

**Revision decision.** Annotate the mapping with the archive-live twin structure, the five dated identity diffs, and the six-document effective evidence base.

**Retained seed evidence.** All eleven local rows and three external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/agents/analyzer.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/agents/comparator.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/agents/grader.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/references/schemas.md | local_corpus_source_material | local_corpus_sourced_fact | reference detail file for deeper pattern evidence |
| agents-used-monthly-archive/codex-skills-202603/.system/skill-creator/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| claude-skills/plugins/skill-creator/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| claude-skills/plugins/skill-creator/agents/analyzer.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| claude-skills/plugins/skill-creator/agents/comparator.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| claude-skills/plugins/skill-creator/agents/grader.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| claude-skills/plugins/skill-creator/references/schemas.md | local_corpus_source_material | local_corpus_sourced_fact | reference detail file for deeper pattern evidence |
| https://developers.openai.com/codex/guides/agents-md | external_research_source_material | external_research_sourced_fact | primary agent instruction context model |
| https://docs.github.com/actions | external_research_source_material | external_research_sourced_fact | verification and automation model |
| https://agents.md/ | external_research_source_material | external_research_sourced_fact | general agent instruction format |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which evaluation patterns deserve default adoption for skill quality work. The seed three corpus-process rows stand where the two systems' own patterns rank by evaluative power, blind baseline comparison ranks first, the same prompt run with and without the skill in the same turn is the only pattern that isolates the skill's causal contribution, assertion grading with evidence citations second, discriminating-assertion design third, the grader's demand that assertions fail for wrong outputs, while description trigger optimization ranks lowest, valuable but dependent on platform-specific CLI machinery.

**Recommended default and causal basis.** Adopt baseline comparison and evidence-cited grading wherever skills get evaluated, treating blinding as the non-negotiable shared core. Evaluation patterns rank by how directly they answer does-the-skill-help, baseline deltas answer it causally, evidence-cited grades answer it verifiably, discriminating assertions keep both honest, and trigger tuning optimizes reach rather than quality.

**Operating conditions and assumptions.** Baselines stay affordable, each eval doubles its runs, the harness assumes subagent parallelism absorbs the cost. Ranking the evaluation patterns these systems teach, skill-authoring patterns rank under the platform themes.

**Failure boundary and alternatives.** Ranking by prominence in the entrypoint would elevate the viewer workflow, which is presentation machinery, above the baseline design, which is the epistemic core the viewer merely displays. Bounded alternatives and recoveries: the corpus's own verifier-based evaluation for document artifacts, structural rather than behavioral measurement.

**Counterexample, gotchas, and operational comparison.** The grader's eval-critique duty means assertion quality is itself graded output, a passing benchmark built on non-discriminating assertions ranks worse than a failing one built on sharp assertions, score interpretation needs the analyst notes, not just the pass rate. Good: a skill judged by baseline delta with evidence-cited grades. Bad: a skill declared improved because its own author eyeballed three outputs. Borderline: skipping baselines on Claude.ai per the adaptation section, sanctioned degradation, the entrypoint itself calls the human review compensatory.

**Verification, evidence, and uncertainty.** Spot-check ranking claims against the grader, comparator, and forward-testing texts. The four Claude documents and the Codex guide compared this session. How often live users run the full harness versus the vibe path is unrecorded.

**Second-order consequence.** Both systems independently converge on the same core insight, that the evaluator must not know the answer, Claude blinds its comparator to which skill produced which output, Codex forbids telling subagents they are testing a skill at all, two platforms arriving at blinding from opposite directions, one to remove judge bias, the other to remove executor contamination.

**Revision decision.** Present the evaluative-power ranking and note the Codex counterpart, forward-testing with contamination controls, as the same causal-isolation instinct expressed without a measurement harness.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `skill_creator_evaluation_patterns` | 95 | default_adoption_pattern_tier | Source Map First: Load local skill creator material before synthesizing evaluation patterns recommendations. |
| `skill_creator_evaluation_patterns` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `skill_creator_evaluation_patterns` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what standard of evidence a skill improvement claim must meet. The seed generic corpus formulas stand where the theme's thesis is that skill quality is a measured delta, not an impression, both systems refuse to let the author's satisfaction stand as evidence, Claude by demanding with-versus-without benchmark runs graded on evidence-cited assertions, Codex by demanding uncontaminated forward-tests whose subagents do not know a test is happening.

**Recommended default and causal basis.** Evaluate skills through runs the author did not steer and grades the author did not write, on both platforms. Skill authors overfit to their own examples and their own reading of outputs, both entrypoints say so explicitly, the Claude guide warns the iterating pair knows the examples in and out, the Codex guide warns against leaked conclusions, so both architectures exist to manufacture independence from the author.

**Operating conditions and assumptions.** Subagent machinery exists, both systems degrade explicitly when it does not, Claude.ai inline runs, user-approval-gated forward-tests. The thesis governs skill evaluation, skill content doctrine belongs to the skills themselves.

**Failure boundary and alternatives.** Author-judged skills accumulate fiddly overfitty changes, the Claude guide's own phrase, that win the three known examples and lose the million future invocations both guides name as the real target population. Bounded alternatives and recoveries: human review alone, which the Claude guide accepts as the vibe path when the user explicitly declines rigor.

**Counterexample, gotchas, and operational comparison.** The held-out split in description optimization guards against a subtler overfit, descriptions tuned to the training queries, the 60/40 split with test-score selection is the harness quietly applying ML hygiene to prompt text. Good: an improvement claim backed by a benchmark delta on assertions the grader critiqued. Bad: the author rerunning their favorite prompt and declaring victory. Borderline: empty feedback treated as approval, the entrypoint endorses it, silence is weak evidence and the guide says focus improvements where complaints were specific.

**Verification, evidence, and uncertainty.** Sample improvement claims for independent-run and independent-grade provenance. Both entrypoints' explicit overfitting warnings read this session. How faithfully live usage honors the independence machinery is unrecorded.

**Second-order consequence.** The two systems place independence at different pipeline points, Claude makes the judgment independent, blind comparator, evidence-bound grader, held-out test split in trigger optimization, while Codex makes the execution independent, fresh threads, no leaked diagnosis, cleaned artifacts, and a complete evaluation discipline needs both, an uncontaminated run judged by an unbiased grader.

**Revision decision.** State the measured-delta thesis with both platforms' independence mechanisms and the shared million-invocation framing.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `skill_creator_evaluation_patterns` contains 11 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Skill Creator Evaluation Patterns comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide which document answers a given harness question and when it gets loaded. The seed heading signals list section names without the bundle's read-order architecture, the Claude entrypoint delegates by task, grader.md when spawning graders, comparator.md plus analyzer.md for blind comparison, schemas.md whenever JSON gets generated manually, while the Codex skill stands alone, its own references directory mapped but not part of this theme's sources.

**Recommended default and causal basis.** Enter through the entrypoint, load spokes at their workflow events, and treat schemas.md as binding whenever any JSON is written by hand. The Claude bundle is a hub-and-spoke system, the entrypoint holds the workflow and the spokes hold role prompts and schemas loaded at spawn time, so navigation is event-driven, read the spoke when the workflow reaches its event, not linearly.

**Operating conditions and assumptions.** The bundle's relative structure holds, spoke references are directory-relative like every skill bundle's. Navigation of this theme's six documents, the live twins navigate identically by construction.

**Failure boundary and alternatives.** Reading spokes without the hub strands them, the grader's exact-field output contract only matters because the viewer depends on those names, a fact the hub states and the spoke merely implements. Bounded alternatives and recoveries: ripgrep across the bundle for locating which document owns a JSON field or workflow step.

**Counterexample, gotchas, and operational comparison.** Analyzer.md is two documents in one file, a post-hoc comparison analyzer and a benchmark-notes analyzer separated by a horizontal rule, loading it for one role brings the other's instructions along, the hub disambiguates by naming the section to read. Good: loading grader.md at grader-spawn time with schemas.md open for the output contract. Bad: implementing grading.json from memory of the hub's summary. Borderline: reading all spokes upfront for a bundle overview, fine for study, the runtime pattern is event-driven loading.

**Verification, evidence, and uncertainty.** Confirm the delegation claims against the hub's reference-files section. All six documents' cross-references read this session. None about structure, every mapped document was read in full.

**Second-order consequence.** The schemas file is the bundle's integration backbone, the hub warns twice that the viewer reads exact field names and routes manual generation to the schema sheet, so the bundle's most failure-prone surface is field-name drift between producer prompts and consumer scripts, and the documentation's defense is a single canonical schema page every producer must consult.

**Revision decision.** Annotate the map with the hub-and-spoke structure, the spawn-time loading events, and the schema sheet's role as the cross-cutting contract both hub and spokes cite.

**Retained seed evidence.** All eleven local source rows with titles, heading signals, and usage roles remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/SKILL.md | skill-creator | Skill Creator; Communicating with the user; Creating a skill; Capture Intent; Interview and Research; Write the SKILL.md | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/agents/analyzer.md | Post-hoc Analyzer Agent | Post-hoc Analyzer Agent; Role; Inputs; Process; Step 1: Read Comparison Result; Step 2: Read Both Skills | local agent-usable source material |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/agents/comparator.md | Blind Comparator Agent | Blind Comparator Agent; Role; Inputs; Process; Step 1: Read Both Outputs; Step 2: Understand the Task | local agent-usable source material |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/agents/grader.md | Grader Agent | Grader Agent; Role; Inputs; Process; Step 1: Read the Transcript; Step 2: Examine Output Files | local agent-usable source material |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/references/schemas.md | JSON Schemas | JSON Schemas; evals.json; history.json; grading.json; metrics.json; timing.json | reference detail file for deeper pattern evidence |
| agents-used-monthly-archive/codex-skills-202603/.system/skill-creator/SKILL.md | skill-creator | Skill Creator; About Skills; What Skills Provide; Core Principles; Concise is Key; Set Appropriate Degrees of Freedom | skill entrypoint or reusable agent prompt |
| claude-skills/plugins/skill-creator/SKILL.md | skill-creator | Skill Creator; Communicating with the user; Creating a skill; Capture Intent; Interview and Research; Write the SKILL.md | skill entrypoint or reusable agent prompt |
| claude-skills/plugins/skill-creator/agents/analyzer.md | Post-hoc Analyzer Agent | Post-hoc Analyzer Agent; Role; Inputs; Process; Step 1: Read Comparison Result; Step 2: Read Both Skills | local agent-usable source material |
| claude-skills/plugins/skill-creator/agents/comparator.md | Blind Comparator Agent | Blind Comparator Agent; Role; Inputs; Process; Step 1: Read Both Outputs; Step 2: Understand the Task | local agent-usable source material |
| claude-skills/plugins/skill-creator/agents/grader.md | Grader Agent | Grader Agent; Role; Inputs; Process; Step 1: Read the Transcript; Step 2: Examine Output Files | local agent-usable source material |
| claude-skills/plugins/skill-creator/references/schemas.md | JSON Schemas | JSON Schemas; evals.json; history.json; grading.json; metrics.json; timing.json | reference detail file for deeper pattern evidence |

## External Research Source Map

**Decision supported.** This section helps decide which external claims this theme owns, and the answer is the platform seams above all. The seed three inherited agent-format anchors stand while the theme's real external surface is the platform machinery its workflows invoke, the claude CLI that trigger optimization shells out to, the subagent task notifications whose total_tokens and duration_ms fields the timing capture depends on, and the Codex-side init_skill.py and quick_validate.py scripts whose flags the guide freezes.

**Recommended default and causal basis.** Verify the notification and CLI seams against current platform behavior before running the harness on a live system. Both systems couple to their platforms at documented seams, and those seams are volatile, a notification payload change silently breaks timing capture, a CLI flag change breaks run_loop.py, so the external map should watch platform interfaces, not generic format sites.

**Operating conditions and assumptions.** The platforms retain subagent notifications and programmatic invocation in recognizable form. External rows serve future freshness verification, all three URLs stayed unretrieved throughout this evolution.

**Failure boundary and alternatives.** Trusting the frozen seam documentation on a current platform risks exactly the timing-capture failure the hub warns is unrecoverable, the data comes through the notification and isn't persisted elsewhere. Bounded alternatives and recoveries: the seed's inherited AGENTS.md and Actions anchors for ecosystem-level context, unmotivated for this theme's platform-specific seams.

**Counterexample, gotchas, and operational comparison.** The model-id instruction in trigger optimization, use the model powering the current session, makes eval results session-relative by design, two users optimizing the same skill on different models get different best descriptions, correctly. Good: a refresh testing timing capture on a current platform build. Bad: assuming notification fields persist because the archive says to save them. Borderline: reusing the harness with manual timing when notifications change shape, workable degradation, record the seam break.

**Verification, evidence, and uncertainty.** Record dated seam checks at each external refresh. Zero fetches this assignment and the seam inventory read across both systems. Current platform notification schemas are unverified from local evidence.

**Second-order consequence.** The timing-capture instruction documents a platform ephemerality, data that exists only in a notification moment, and builds workflow discipline around it, process each notification as it arrives, so the skill is compensating in prose for a platform persistence gap, the kind of coupling that silently heals or silently breaks when the platform changes either way.

**Revision decision.** Keep the inherited rows, add the load-bearing seams, platform subagent notification schemas and the claude CLI's programmatic interface, and mark all seam descriptions archive-dated.

**Retained seed evidence.** All three external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://developers.openai.com/codex/guides/agents-md | OpenAI Codex AGENTS.md guide | primary agent instruction context model | external_research_sourced_fact |
| https://docs.github.com/actions | GitHub Actions documentation | verification and automation model | external_research_sourced_fact |
| https://agents.md/ | AGENTS.md open format | general agent instruction format | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which evaluation failures need standing detection and where the detectors live. The seed three corpus-process rows stand where the two systems name richer failures, non-discriminating assertions that pass regardless of skill, the analyzer's first pattern check, false-confidence grades on weak assertions, the grader's worse-than-useless warning, context leakage that lets a forward-test subagent reconstruct the answer, the Codex guide's central hazard, overfitting to known examples, both entrypoints' shared enemy, and viewer-schema drift, hand-built JSON whose field names the viewer silently zeroes.

**Recommended default and causal basis.** Keep each detector attached to its producing role, and treat any all-green result as triggering the discrimination check rather than celebration. Each failure corrupts a different evaluation layer, assertions corrupt measurement, leakage corrupts execution, overfit corrupts improvement, and schema drift corrupts presentation, so the registry spans the full harness pipeline.

**Operating conditions and assumptions.** The roles actually execute their critique duties, a grader prompt trimmed of step six loses the registry's main assertion detector. Evaluation-process failures from the mapped systems, generic skill-writing failures belong to the platform themes.

**Failure boundary and alternatives.** The failures compound insidiously because most produce green results, leaked tests pass, weak assertions pass, overfit skills pass their three known evals, the registry's members are precisely the failures that look like success. Bounded alternatives and recoveries: periodic external eval review, the harness's built-in critique is faster and better contextualized.

**Counterexample, gotchas, and operational comparison.** Empty feedback and passing grades are ambiguous between genuine success and non-discriminating evaluation, the analyzer's notes are the only layer that distinguishes them, skipping the analyst pass silently converts ambiguity into false confidence. Good: a grader flagging an assertion a hallucinated output would pass. Bad: shipping a skill on a benchmark of always-pass assertions. Borderline: a forward-test that succeeded with mild context leakage, the Codex rule says tighten before trusting, not discard.

**Verification, evidence, and uncertainty.** Confirm each added row cites its documented detection mechanism. The grader, analyzer, comparator, and forward-testing texts read this session. How often live usage skips the analyst pass is unrecorded.

**Second-order consequence.** The systems build their detectors into the roles that produce the risk, the grader critiques the assertions it grades, the analyzer flags the non-discrimination in the benchmarks it summarizes, the forward-tester checks its own setup for leakage, self-auditing at the point of production rather than downstream review, the same instinct as the corpus's generation-time sanity gates.

**Revision decision.** Add the five rows with their documented detections, the analyzer's always-passes check, the grader's eval-critique duty, the Codex leaked-context test, tighten if success requires leakage, benchmark-versus-future-prompt divergence, and the viewer's empty-values symptom.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide which stages of skill evaluation run through frozen commands versus judgment. The seed document verifier text stands where the theme's own gates are executable, the aggregation gate, python -m scripts.aggregate_benchmark over an iteration directory, the viewer gate, generate_review.py with benchmark and previous-workspace flags, the trigger gate, run_loop.py with eval set, skill path, model id, and max iterations, and the Codex gates, quick_validate.py for structure and init_skill.py for generation.

**Recommended default and causal basis.** Run each stage through its script with documented flags, and get outputs in front of the human before forming conclusions. The harness expresses its quality doctrine as commands the way the Rust family expresses its as cargo gates, each command freezes a workflow stage, aggregation, presentation, optimization, validation, into a reproducible invocation.

**Operating conditions and assumptions.** The scripts exist alongside the prompts, this corpus holds the documents that describe them, not the scripts themselves. The gate commands these systems define, this document's own structural verification keeps the seed's retained block.

**Failure boundary and alternatives.** Hand-rolling any stage invites the documented failures, boutique HTML instead of the viewer, the hub explicitly forbids it, manual benchmark JSON with drifted fields, ad hoc trigger testing without train-test discipline. Bounded alternatives and recoveries: the corpus verifier command in the seed's retained block for this document's own checks.

**Counterexample, gotchas, and operational comparison.** Aggregate_benchmark must run from the skill-creator directory as a module, python -m scripts.aggregate_benchmark, running it as a file path breaks the package-relative import, a trap the invocation syntax quietly encodes. Good: an iteration aggregated, viewed, and human-reviewed through the three scripts in order. Bad: custom HTML review pages replacing generate_review.py. Borderline: inline grading without a grader subagent, the hub permits it, the evidence discipline still applies.

**Verification, evidence, and uncertainty.** Check harness runs for script provenance at each stage. The command inventory read across both entrypoints this session. Script contents are undescribed beyond their interfaces, the corpus maps prompts, not code.

**Second-order consequence.** The strongest gate language in the whole bundle guards presentation, not measurement, the Cowork section goes all-caps demanding the viewer be generated before self-evaluation, because the failure it fights is the agent judging outputs itself instead of getting them before the human, the harness's gates protect the human's place in the loop as fiercely as the data's integrity.

**Revision decision.** Inventory the four command families with their binding flags and the hub's use-the-script imperatives.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide which agent may know what during a skill evaluation. The seed four generic bullets stand where the systems define a five-role agent cast, the orchestrating creator who runs the loop, executor subagents who run test prompts with or without the skill, the grader who scores assertions with evidence, the blind comparator who judges unattributed outputs, and the analyzer who unblinds and explains, each role with its own prompt document and information boundary.

**Recommended default and causal basis.** Assign evaluation work to roles that lack the knowledge that would bias them, using the documented prompts as role contracts. The roles exist to partition knowledge, executors must not know they are being tested, the comparator must not know which skill made which output, the grader must not accept claims without evidence, so the cast is an information architecture, not a task decomposition.

**Operating conditions and assumptions.** Subagent spawning exists, both guides define explicit degraded modes when it does not. Agent roles inside the evaluation harness, the created skills' own agent usage belongs to their themes.

**Failure boundary and alternatives.** Collapsing roles collapses boundaries, an orchestrator who grades its own runs knows what it hoped to see, the exact bias the grader's burden-of-proof rule and the comparator's blindness exist to exclude. Bounded alternatives and recoveries: the descendant-generation contract roles in the Rust themes for gate-report agent duties, a different cast for a different artifact type.

**Counterexample, gotchas, and operational comparison.** The orchestrator legitimately holds all knowledge, its bias is managed not by blinding but by routing judgment outward, to the human via the viewer, to the grader via evidence rules, orchestrator neutrality is procedural, not epistemic. Good: a comparator receiving unlabeled outputs and an eval prompt only. Bad: an executor prompt mentioning this tests the new skill version. Borderline: inline grading by the orchestrator on Claude.ai, sanctioned degradation with the evidence rules still binding.

**Verification, evidence, and uncertainty.** Audit role prompts for knowledge-boundary violations before runs. The four role documents and forward-testing rules read this session. How strictly live orchestrators respect the boundaries is unrecorded.

**Second-order consequence.** The Codex guide runs the same partition with two roles instead of five, author and uncontaminated forward-tester, showing the knowledge-partition principle scales down, what cannot scale down is zero roles, the author alone, both systems treat single-agent self-evaluation as the failure state their whole architecture exists to prevent.

**Revision decision.** Recast the section as the five-role cast with each role's knowledge boundary and prompt document.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide where this user is in the creation process and what register serves them. The seed one generic engineer stands where the Claude entrypoint itself maps the journeys, the from-scratch creator whom the skill walks from intent capture through iteration, the improver who arrives with a draft and jumps straight to the eval loop, the vibe collaborator who declines rigor and gets flexibility by name, and the plumber, the entrypoint's own image of newly-empowered non-programmers who need jargon translated.

**Recommended default and causal basis.** Identify the arrival point first, adopt the matching entry into the loop, and calibrate vocabulary to the user's demonstrated fluency. The skill's opening section instructs the agent to locate the user in the process and join there, so journey identification is not analysis overlaid on the source, it is the source's own first instruction.

**Operating conditions and assumptions.** Context cues about fluency exist, the guide defaults to brief explanations when in doubt. Journeys through the creator systems, journeys through created skills belong elsewhere.

**Failure boundary and alternatives.** Ignoring the journey step produces the misfit the guide warns about, walking a draft-holding improver through intent-capture interviews, or drowning the plumber in assertion JSON the communication section says to translate. Bounded alternatives and recoveries: the Codex guide's interview pattern for its platform's equivalent journey, fewer roles, same locate-the-user instinct.

**Counterexample, gotchas, and operational comparison.** The vibe path is user-elected, not agent-elected, an agent skipping rigor because evaluation is effortful inverts the guide, flexibility is offered to the user, discipline is demanded of the agent. Good: a draft-holder routed straight to test-case writing in plain language. Bad: a grandmother asked to review assertion JSON schemas. Borderline: a fluent user given unexplained jargon on thin cues, the guide says clarify briefly when unsure.

**Verification, evidence, and uncertainty.** Check opening exchanges for journey identification and register calibration. The entrypoint's process-location and communication sections read this session. The plumber demographic's actual share of usage is the guide's own anecdote, not data.

**Second-order consequence.** The communication section makes vocabulary a graded interface, evaluation and benchmark are borderline-acceptable, JSON and assertion require demonstrated fluency, a two-tier jargon whitelist in a skill prompt, the only place in the corpus where a skill formally specifies its own register per audience, treating explanation itself as an adaptive surface.

**Revision decision.** Recast the scenario as the source's own four arrivals plus the register rule, gauge jargon fluency from context cues before speaking.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The new contributor or agent is starting from an unfamiliar theme and deciding whether this reference is the right tool and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `skill_creator_evaluation_patterns` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing what to load, what to trust, what to avoid, and what evidence proves success.
Reference opening trigger: Open this file when the task mentions skill creator evaluation patterns, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide how rigorous, how constrained, and how responsive a skill and its evaluation should be. The seed template rows skip the systems' live tensions, rigor versus velocity, the full benchmark loop versus the sanctioned vibe path, freedom versus constraint in the Codex degrees-of-freedom ladder, narrow bridges get scripts, open fields get prose, and generalization versus responsiveness, fixing the user's specific complaint versus the fiddly-overfitty warning against changes that only serve known examples.

**Recommended default and causal basis.** Let the user set the rigor level, the task set the freedom level, and the million-invocation test screen every improvement. Each axis is decided per-skill and per-user, both guides refuse global answers, the Claude guide bends rigor to user election, the Codex guide indexes freedom to task fragility, and both subordinate specific fixes to million-invocation generality.

**Operating conditions and assumptions.** The author can actually assess fragility, unfamiliar domains may need a forward-test to discover where the cliffs are. Tuning decisions inside the creator systems, created skills inherit the degrees-of-freedom analysis for their own content.

**Failure boundary and alternatives.** Wrong poles fail characteristically, forced rigor alienates the vibe user, low-freedom scripts strangle tasks needing judgment, high-freedom prose lets fragile operations wobble, and complaint-chasing builds skills that only pass their own evals. Bounded alternatives and recoveries: platform defaults when no signal exists, imperative prose with scripts only for proven repetition.

**Counterexample, gotchas, and operational comparison.** The repeated-work signal cuts across axes, three executors independently writing the same helper script argues for bundling it, a scripted low-freedom island inside an otherwise high-freedom skill, freedom is set per-operation, not per-skill. Good: a fragile PDF operation scripted while the surrounding workflow stays prose. Bad: ALWAYS-NEVER carpeting because one eval failed once. Borderline: honoring a user complaint with a narrowly-scoped fix, responsive, run the million-invocation test before shipping it.

**Verification, evidence, and uncertainty.** Review skill revisions against the three axes' deciding conditions. The degrees-of-freedom, vibe-path, and improvement-philosophy texts read this session. How authors assess fragility in practice is undocumented.

**Second-order consequence.** The degrees-of-freedom ladder is the Codex guide's deepest export, it reframes skill authoring as control-system tuning, match specificity to fragility, and the Claude guide's yellow-flag rule, all-caps MUSTs signal a reframe is needed, is the same principle from the other side, rigid language is a symptom that the freedom setting is wrong for the explanation the model actually needs.

**Revision decision.** Add the three axes with their documented deciding conditions, user election for rigor, task fragility for freedom, and the generalize-from-feedback rule for improvements.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the skill creator evaluation patterns pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong skill creator evaluation patterns guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which of the eleven rows settles a given question. The seed role labels in this table call the Claude agent files legacy and the live copies supporting, but the measured structure is different, archive and live Claude trees are byte-identical peers, neither is legacy to the other, the Claude bundle is internally hub-and-spoke with the entrypoint canonical for workflow and the spokes canonical for their roles, and the Codex skill is a parallel system, not a subordinate source.

**Recommended default and causal basis.** Answer workflow questions from the entrypoint, role questions from the spokes, schema questions from the schema sheet, platform-comparison questions from both systems side by side. Identity diffs collapse the archive-versus-live ranking, and role-prompt delegation makes the spokes authoritative within their events, so hierarchy here is functional, who owns which question, rather than temporal or positional.

**Operating conditions and assumptions.** The twin identity holds, divergence would create a genuine version hierarchy overnight. Precedence among this theme's eleven rows, cross-theme precedence belongs to the corpus hierarchy.

**Failure boundary and alternatives.** Treating the agent prompts as legacy history would discard the harness's operative role contracts, and ranking Codex under Claude would erase the theme's cross-platform comparison surface. Bounded alternatives and recoveries: escalating Claude-versus-Codex philosophy conflicts to nothing, they are parallel doctrines, choose by target platform.

**Counterexample, gotchas, and operational comparison.** The seed ranks the archived entrypoint canonical and the live entrypoint supporting, harmless while identical, but citations should prefer the archive path anyway for stability, live trees move with deployments. Good: citing grader.md as the operative grading contract. Bad: dismissing comparator rules as historical because the path says archive. Borderline: citing the live path in a deployment-currency argument, valid emphasis, note the identity.

**Verification, evidence, and uncertainty.** Test rulings against the three-structure classification and re-run the twin diffs. The identity diffs and delegation structure measured this session. None about current structure, all comparisons were run on all pairs.

**Second-order consequence.** This is the first theme where the seed's legacy label is measurably wrong rather than merely coarse, the live tree proves the so-called legacy files are the current deployment, a concrete instance of the corpus lesson that role labels assigned by path pattern need verification against content, archives can be current and live trees can be stale, only diffs decide.

**Revision decision.** Restate the hierarchy as three structures, twin equality between trees, hub-and-spoke functional ownership within the Claude bundle, and platform parallelism between Claude and Codex systems.

**Retained seed evidence.** All eleven hierarchy rows and the classification vocabulary remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/SKILL.md | canonical primary source | Skill Creator; Communicating with the user; Creating a skill | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/agents/analyzer.md | legacy historical source | Post-hoc Analyzer Agent; Role; Inputs | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/agents/comparator.md | legacy historical source | Blind Comparator Agent; Role; Inputs | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/agents/grader.md | legacy historical source | Grader Agent; Role; Inputs | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |
| agents-used-monthly-archive/claude-skills-202603/plugins/skill-creator/references/schemas.md | supporting detail source | JSON Schemas; evals.json; history.json | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |
| agents-used-monthly-archive/codex-skills-202603/.system/skill-creator/SKILL.md | legacy historical source | Skill Creator; About Skills; What Skills Provide | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |
| claude-skills/plugins/skill-creator/SKILL.md | supporting context source | Skill Creator; Communicating with the user; Creating a skill | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |
| claude-skills/plugins/skill-creator/agents/analyzer.md | supporting context source | Post-hoc Analyzer Agent; Role; Inputs | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |
| claude-skills/plugins/skill-creator/agents/comparator.md | supporting context source | Blind Comparator Agent; Role; Inputs | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |
| claude-skills/plugins/skill-creator/agents/grader.md | supporting context source | Grader Agent; Role; Inputs | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |
| claude-skills/plugins/skill-creator/references/schemas.md | supporting detail source | JSON Schemas; evals.json; history.json | What guidance, warning, or example should this source contribute to Skill Creator Evaluation Patterns? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing evidence makes the two-system comparison consultable. The seed generic worked-example artifact misses this theme's natural object, a cross-platform evaluation correspondence map, one standing table aligning each evaluation concern, causal isolation, judge independence, execution independence, assertion quality, overfit control, presentation integrity, with its Claude mechanism and its Codex mechanism side by side.

**Recommended default and causal basis.** Consult the map for any cross-platform evaluation question and mine its empty cells when improving either platform's practice. The two systems solve the same concerns with different machinery, baseline runs versus forward-tests, blind comparison versus contamination controls, and only an aligned map makes the correspondence consultable rather than rediscoverable.

**Operating conditions and assumptions.** The map's citations stay synchronized with the twin identity and any future sweeps. One map for the two mapped systems, extensible if later sweeps add other platforms' creators.

**Failure boundary and alternatives.** Without the map, cross-platform questions get answered from one system's vocabulary, prescribing benchmark JSON to a Codex author or forward-test purity rules to a Claude harness user, category errors the alignment would prevent. Bounded alternatives and recoveries: leaving the comparison in this theme's packet prose, recoverable but unconsultable, the status quo the artifact improves.

**Counterexample, gotchas, and operational comparison.** Correspondence is not equivalence, baseline deltas and forward-tests both isolate causation but measure different things, quantified assertion deltas versus qualitative struggle observation, the map must record the character difference or it flattens the philosophies it exists to compare. Good: an import decision citing the map's empty degrees-of-freedom cell on the Claude side. Bad: re-deriving the blinding correspondence from six documents per question. Borderline: adding a third column for the corpus's own document-evaluation machinery, expansive, arguably the map's natural home.

**Verification, evidence, and uncertainty.** Check the map exists with concern rows, both platform columns, citations, and a gaps row. The mechanism inventory assembled from all six documents this session. Whether later sweeps hold newer creator versions is a queue question.

**Second-order consequence.** Building the map exposes asymmetric coverage, Claude has no counterpart to the Codex degrees-of-freedom ladder, Codex has no counterpart to trigger-description optimization, so the map's empty cells are import opportunities, each platform's blind spot is the other's documented strength, the concrete payoff of holding two parallel systems in one theme.

**Revision decision.** Define the artifact as the concern-by-platform correspondence map with mechanism citations into the six documents and a gaps row for concerns only one platform addresses.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: skill activation contract with progressive disclosure, misuse case, and verification gate.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Skill Creator Evaluation Patterns | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercises build harness-design and trigger-query judgment. The seed abstract usage lines stand where this theme wants an evaluation-design rehearsal, give a learner a small draft skill and require a full harness plan, two or three realistic test prompts, discriminating assertions with descriptive names, a baseline design matching the create-versus-improve rule, new skills against no skill, improvements against the snapshot, and a grading plan naming evidence sources.

**Recommended default and causal basis.** Run the design rehearsal before any first harness use and the query drill before any description optimization. Harness skill is design skill, the documented failures, weak assertions, missing baselines, leaked context, are all design-time errors, so the rehearsal exercises the decisions before any run spends tokens.

**Operating conditions and assumptions.** The learner has a real or realistic draft skill, toy skills make baseline design trivially uninformative. Teaching evaluation design for skills, document-corpus evaluation exercises belong to the corpus themes.

**Failure boundary and alternatives.** Unrehearsed designers produce the entrypoint's named bads, abstract test prompts no real user would type, assertions that check filenames instead of content, baselines skipped because the skill obviously helps. Bounded alternatives and recoveries: apprenticeship on a live harness run, richer and costlier, the design rehearsal catches most errors for free.

**Counterexample, gotchas, and operational comparison.** The rehearsal must include the timing-capture plan, the notification-moment persistence rule is the harness's easiest-to-miss operational detail and losing it silently degrades every benchmark's resource columns. Good: a rehearsal plan with near-miss negatives and snapshot baselines. Bad: certifying on a plan whose assertions all check file existence. Borderline: a plan skipping blind comparison, it is optional machinery, the rehearsal should note why it was declined.

**Verification, evidence, and uncertainty.** Grade rehearsal plans against the documented failure inventory. The query examples, baseline rules, and assertion guidance read this session. Transfer from rehearsal to live harness discipline is unmeasured.

**Second-order consequence.** The entrypoint's own good-versus-bad query example is the theme's best teaching object, the bad is Format this data, the good is a paragraph of boss-sent-xlsx backstory with column guesses and a typo, realism operationalized as concrete detail, personal context, and imperfection, a checkable rubric hiding inside an example pair.

**Revision decision.** Anchor the section on the harness-design rehearsal plus a trigger-query drill, write ten should-and-should-not-trigger queries where the negatives are near-misses, graded against the entrypoint's too-easy warning.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Skill Creator Evaluation Patterns after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Skill Creator Evaluation Patterns as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Skill Creator Evaluation Patterns only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which measurements prove a skill helps and this theme's evidence holds. The seed compile-centric indicator misses what the systems themselves measure, benchmark deltas, pass rate, time, tokens with mean and stddev between with-skill and baseline configurations, feedback convergence, iterations until user comments empty out, trigger accuracy, held-out test scores from the optimization loop, and for this theme's own upkeep, twin-identity persistence across the five pairs.

**Recommended default and causal basis.** Read skill quality from deltas, iteration health from feedback convergence, reach from held-out trigger scores, and theme evidence from twin diffs. The harness is already a metrics system, so this section's work is adopting its native measures rather than inventing proxies, plus the one meta-metric the theme's evidence structure adds.

**Operating conditions and assumptions.** Runs-per-configuration stays high enough for stddev to mean anything, the schema shows three as the working default. Measuring skill evaluation and this node's evidence, created skills' domain metrics belong to their own themes.

**Failure boundary and alternatives.** Unmeasured skill iteration is the author-impression loop both guides architect against, and unmeasured twin identity would let the theme's strongest evidence claim silently rot. Bounded alternatives and recoveries: qualitative-only review for subjective skills, the entrypoint's sanctioned path where assertions would be forced.

**Counterexample, gotchas, and operational comparison.** Token and time deltas are costs, not qualities, the analyzer's example note prices a 13-second overhead against a 50-point pass-rate gain, the metrics need joint reading, a skill can win pass rate while losing its economic case. Good: an iteration judged on delta plus cost with converging feedback. Bad: pass rate celebrated while tokens doubled unexamined. Borderline: a subjective writing skill measured only by human review, sanctioned, note the assertion waiver.

**Verification, evidence, and uncertainty.** Confirm the four series exist with dated entries at the next refresh. The schema structures and analyzer pricing example read this session. Real-world feedback convergence rates are unrecorded.

**Second-order consequence.** The delta structure in run_summary is the harness's core epistemic move rendered as schema, with-skill and without-skill blocks plus a delta block, meaning every benchmark file carries its own causal comparison, no external analysis needed to see whether the skill earned its tokens, self-contained causal evidence as a file format.

**Revision decision.** Adopt the three native series with their documented collection points, benchmark JSON, feedback JSON, optimization reports, and add the twin-diff series at audit cadence.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: the next task uses the reference to make a better decision with less ambiguity.
Failure signal: the reference remains a source map and never becomes an operating guide.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this comparison reference is declared faithful. The seed shape items never audit this theme's distinctive claims, the five twin identities with dates, the hub-and-spoke delegation reading, the two-platform correspondence map's citation accuracy, the seam inventory's coverage, and the corrected hierarchy labels against the seed's legacy misclassification.

**Recommended default and causal basis.** Run the twin diffs every audit cycle and the semantic spot-checks on the correspondence map's schedule. The theme's substance is a two-system comparison resting on verified identity and full reads, so completeness auditing means re-running diffs and spot-checking correspondence citations, not counting headings.

**Operating conditions and assumptions.** Both trees remain present, removal of the live tree would convert currency claims back to archive-dated ones. Auditing this reference against its comparison claims, each source document's internal structure was read in full once.

**Failure boundary and alternatives.** A shape-only audit passes while a live-tree deployment update silently breaks twin identity, leaving every currency claim in this document stale at its foundation. Bounded alternatives and recoveries: freezing the identity claim as a dated fact and never re-checking, cheap and blind to exactly the divergence that matters most.

**Counterexample, gotchas, and operational comparison.** A live-tree update is not corruption, it is deployment progress, the audit must treat broken identity as a version event requiring re-synthesis, new deltas to analyze, not as an integrity incident to revert. Good: an audit logging five clean diffs and sampled correspondence citations. Bad: fidelity declared from heading counts while the live tree moved on. Borderline: skipping spoke spot-checks the cycle after full re-reads, defensible freshness reasoning, date the skip.

**Verification, evidence, and uncertainty.** Execute the distinctive items and date each result in the audit record. The claim structure of every distinctive statement in this document. Live-tree update frequency is unknown, which is precisely why the diff cadence matters.

**Second-order consequence.** Twin identity is this theme's single point of audit leverage, one diff batch either sustains or collapses the deployment-currency claim that differentiates the whole theme, making it the corpus's cheapest high-stakes audit, five commands guarding the largest claim any theme makes about being current.

**Revision decision.** Add the distinctive items, five twin diffs, one delegation spot-check per spoke, one correspondence citation per concern row, and the hierarchy-label correction's continued validity.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Skill Creator Evaluation Patterns.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a question arriving at the skill-evaluation theme. The seed token-split rows point nowhere while this theme's neighbors are functional, the platform-entrypoint themes for how Claude and Codex structure skills generally, the TDD and progress-journal themes for the iteration-discipline instincts the harness shares, the Rust gate themes for command-gate doctrine when created skills target Rust work, and the corpus's own verifier machinery for document-artifact evaluation.

**Recommended default and causal basis.** Classify the question's artifact first, skill behavior routes here, skill structure routes to platforms, document structure routes to the verifier themes. This theme owns skill evaluation machinery, questions about skill content, platform conventions, or non-skill artifact evaluation all have better-fitted homes, so routing follows the artifact type and the pipeline stage.

**Operating conditions and assumptions.** The platform themes remain evolved, routing to unevolved themes parks questions as pending per cluster practice. Sending away what this theme holds only incidentally, destination doctrine is deliberately not restated.

**Failure boundary and alternatives.** Answering platform-convention questions from the harness theme produces evaluation-flavored answers to structure questions, and keeping document-evaluation questions here misses that the corpus's verifier is a different species, structural rather than behavioral. Bounded alternatives and recoveries: for questions about the harness's script internals, unavailable-source description, the corpus maps the prompts and schemas, not the Python.

**Counterexample, gotchas, and operational comparison.** Trigger-description optimization looks like a structure question, it edits frontmatter, but it is evaluation machinery through and through, eval queries, train-test splits, score-selected output, route it here despite its structural surface. Good: forwarding a progressive-disclosure question to the platform theme. Bad: answering a benchmark-schema question from platform-convention memory. Borderline: an iteration-cadence question, shared territory with the TDD theme, answer jointly with both cited.

**Verification, evidence, and uncertainty.** Sample recent answers for artifact-and-stage routing consistency. The theme boundaries assembled across this cluster's evolutions. The platform themes' evolution status shifts with the queue.

**Second-order consequence.** The harness and the corpus's own TDD process are convergent designs, red-green-refactor maps onto run-review-improve, both journal progress, both gate acceptance on verification, so the routing between them is bidirectional enrichment rather than ownership dispute, each system is the other's best external example.

**Revision decision.** Route by artifact and stage, skill structure to platform themes, skill evaluation here, document evaluation to the corpus machinery, iteration philosophy shared with the TDD theme by explicit cross-reference.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use the nearest language, workflow, agent, or documentation reference when the theme becomes concrete.
Adjacent reference 1: consider the skill adjacent reference when the current task pivots away from skill creator evaluation patterns.
Adjacent reference 2: consider the creator adjacent reference when the current task pivots away from skill creator evaluation patterns.
Adjacent reference 3: consider the evaluation adjacent reference when the current task pivots away from skill creator evaluation patterns.

## Workload Model

**Decision supported.** This section helps decide how to budget a node with cheap steady-state and event-priced revisions. The seed symbol-count boundary misprices this node's work, whose units are per-audit twin verification, five diffs over identical trees, per-cycle correspondence upkeep, six concern rows re-checked against six documents, and per-question routing, cheap after the artifact-first classification, plus the one-time cost already sunk, 2024 lines read across six documents, the cluster's largest single-assignment read.

**Recommended default and causal basis.** Run the cheap regime on audit cadence and provision the expensive regime the moment any twin diff breaks. The reading cost concentrated at evolution time because the theme demanded full reads of a five-document bundle plus a rival system, while maintenance is light, diffs are mechanical and the correspondence map ages only when either platform's creator revises.

**Operating conditions and assumptions.** Version events surface through the trees this corpus holds, out-of-repo platform updates stay invisible until a sweep imports them. Budgeting this node's upkeep and revision triggers, harness runtime costs belong to harness users.

**Failure boundary and alternatives.** Budgeting future work at evolution-time rates would overprice maintenance fivefold, while budgeting the next creator-version sweep at maintenance rates would underprice the re-read a real revision demands. Bounded alternatives and recoveries: the seed's crate-slice and symbol boundaries, retained as outer limits on any single consultation's scope.

**Counterexample, gotchas, and operational comparison.** The Codex side has no twin, its single copy gives no in-repo divergence signal, so Codex-side currency has only the sweep cadence as its clock, an asymmetry the model must carry explicitly. Good: provisioning a full re-read when a live-tree diff breaks. Bad: quarterly full re-reads of 2024 unchanged lines. Borderline: partial re-read of just the changed file after a small divergence, efficient, verify the change does not ripple into the correspondence map.

**Verification, evidence, and uncertainty.** Check regime-switch decisions against diff events in the audit record. The read-cost ledger and twin structure from this session. Platform revision tempo is the model's hidden demand variable.

**Second-order consequence.** Twin identity doubles as the workload trigger, the same five diffs that audit the currency claim also detect the version event that switches the cost regime, one mechanical check serving as both evidence probe and work scheduler, the cluster's cleanest example of audit machinery earning its keep twice.

**Revision decision.** Restate the model in its three units with the two-regime warning, cheap steady-state, expensive on version events, and tie the expensive regime to twin-identity breaks and new-sweep arrivals.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Skill Creator Evaluation Patterns as a agent workflow operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | agent planning, tool use, context loading, delegation, or skill/plugin execution where bad guidance compounds across long-running sessions | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one primary task, up to 10 source files, up to 3 delegated subtasks, and a written completion audit for every run | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Skill Creator; Communicating with the user; Creating a skill; Capture Intent; Interview and Research; Write the SKILL.md; Post-hoc Analyzer Agent; Rol | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is skill activation contract with progressive disclosure, misuse case, and verification gate | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which invariants must demonstrably hold for this node's claims to stay citable. The seed document-property thresholds stand where this node's invariants should, the five twin identities holding or their breaks being processed as version events, the correspondence map's citations resolving into the six documents, the hierarchy correction staying valid, the seed's legacy labels remaining wrong for the reasons stated, and the evaluation-philosophy characterizations staying faithful to their sources.

**Recommended default and causal basis.** Hold the mechanical invariants on diff and citation schedules and the interpretive one on a re-read cadence tied to heavy reuse. Each invariant guards a load-bearing claim, identity guards currency, citations guard the map's consultability, the label correction guards against regression to the seed's misclassification, and philosophical fidelity guards the theme's core comparison.

**Operating conditions and assumptions.** The source passages stay reachable at their cited locations. Invariants for this node's claims, harness reliability belongs to harness operators.

**Failure boundary and alternatives.** The invariants fail toward different victims, silent divergence strands currency claims, dead citations strand the map, label regression re-buries the operative role contracts as legacy, and drifted characterizations turn the comparison into caricature. Bounded alternatives and recoveries: dropping the interpretive invariant and letting characterizations float, cheaper and exactly how comparison themes decay into folklore.

**Counterexample, gotchas, and operational comparison.** A twin break does not falsify past claims, statements dated to the identity period stay true of that period, the invariant governs forward reuse, date-scoping the claims keeps history honest while the present re-synthesizes. Good: a reuse pause to re-read the blinding passages before quoting the shared-core claim. Bad: the philosophy comparison repeated thirdhand for a year unchecked. Borderline: citing the identity claim the week after a clean diff, fresh enough, the date still belongs in the citation.

**Verification, evidence, and uncertainty.** Review the four invariant series at each corpus checkpoint with dated evidence. The claim structure established across this document. None about current truth, all four invariants were verified this session.

**Second-order consequence.** The philosophical-fidelity invariant is the cluster's first guarding an interpretation rather than a fact, blinding-as-shared-core and independence-at-different-pipeline-points are readings, not measurements, so its probe is necessarily a re-read against the source passages, interpretation claims age not by the world changing but by paraphrase drifting through reuse, and only source contact re-anchors them.

**Revision decision.** Publish the four invariants with evidence methods, diff batches, citation resolution checks, label spot-checks, and periodic re-reads of the characterized passages.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which decay modes need standing probes for a comparison-centered theme. The seed generic rows miss how this node specifically dies, twin divergence unprocessed, the live tree updating while this document keeps claiming identity, correspondence rot, map citations dangling after either system revises, philosophy flattening, the two evaluation designs blurring into generic test-your-skills advice through repeated paraphrase, and register bleed, the Claude entrypoint's conversational voice leaking into formal guidance that quotes it.

**Recommended default and causal basis.** Run the four probes at their established trigger points, weighting re-reads toward passages that get reused most. All four attack the theme's differentiators, currency, consultability, comparative sharpness, and professional reuse, the claims that make this node more than a summary of six files.

**Operating conditions and assumptions.** Reuse is observable enough to weight, unobserved reuse defaults the re-read cadence to fixed. Decay of this node's claims and reuse, harness operational failures belong to its operators.

**Failure boundary and alternatives.** They compound, an unprocessed divergence dates the map, the dated map stops being consulted, unconsulted comparisons get paraphrased from memory, and paraphrase is where flattening and register bleed both live. Bounded alternatives and recoveries: quarantining quotations entirely, paraphrase-only reuse, which trades register risk for flattening risk, the wrong trade here.

**Counterexample, gotchas, and operational comparison.** Register bleed runs both directions, formalizing the Claude guide's plumber empathy into stiff prose loses the audience-calibration insight the informality carries, the probe should check fidelity of spirit, not enforce uniform solemnity. Good: a re-read catching the shared-core claim drifting toward both-platforms-recommend-testing. Bad: a year of citations to a map whose Codex column predates a platform revision. Borderline: quoting cool-cool in an internal explainer, harmless color there, wrong in a compliance document.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe and trigger point. The decay structure consolidated from this document's preceding sections. Reuse observability determines whether weighting is possible.

**Second-order consequence.** Philosophy flattening is the comparison theme's native decay, the sharp version, blinding shared, independence placed differently, requires holding both systems in mind, while the flat version, both say test properly, survives on one, so entropy favors flatness and only source re-contact restores the gradient, the interpretive analogue of bit rot.

**Revision decision.** Add the four rows with probes, the diff batch for divergence, citation resolution for rot, periodic source re-reads for flattening, and quotation review for register, each probe already established by the preceding sections.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| context window loses plan | long-running session forgets early constraints or overwrites user intent | write checkpoint summaries and re-read plan before each destructive step |
| tool fanout outruns budget | parallel actions create conflicts, duplicate work, or unbounded external calls | cap fanout, assign ownership, and require merge/audit checkpoints |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide what gets regenerated, what stays frozen, and how execution degrades under pressure. The seed corpus-process bullets stand beside the systems' own retry doctrine, the harness retries at iteration granularity, rerun all test cases into a fresh iteration directory after each improvement, never reuse stale runs, the forward-tester retries with fresh threads and cleaned artifacts to prevent cross-iteration contamination, and the Cowork adaptation sanctions series execution as backpressure when parallel subagents hit timeouts.

**Recommended default and causal basis.** Regenerate everything per iteration, clean the workspace of findable answers, and drop to series execution when the platform pushes back. Evaluation retries differ from network retries, the hazard is not transient failure but state leakage, stale eval metadata, prior-iteration artifacts a subagent can find, so both systems' retry rules are contamination rules wearing retry clothes.

**Operating conditions and assumptions.** Iteration directories stay cheap, the harness's disk layout assumes storage is not the constraint. Retry semantics inside skill evaluation, network-level retry doctrine belongs to the reliability themes.

**Failure boundary and alternatives.** Reusing prior runs after a skill edit measures a mixture of versions, and leaving artifacts on disk lets later forward-tests succeed by finding earlier answers, both producing confident numbers about nothing. Bounded alternatives and recoveries: the descendants' network retry doctrine for skills whose content is retry logic, a routing case, not a contradiction.

**Counterexample, gotchas, and operational comparison.** The snapshot baseline for improve-mode is the one deliberate reuse, the old version frozen at loop start, reused across iterations by design, the no-reuse rule governs evidence, not the comparison anchor. Good: iteration three with fresh metadata, fresh runs, and the loop-start snapshot as baseline. Bad: grading iteration three against iteration one's cached outputs. Borderline: reusing unchanged eval prompts across iterations, sanctioned, their metadata files still need recreating per the hub.

**Verification, evidence, and uncertainty.** Audit iteration directories for regeneration completeness and workspace cleanliness. The iteration-loop and forward-testing hygiene texts read this session. How often live runs violate the fresh-metadata rule is unrecorded.

**Second-order consequence.** The entrypoint's warning that eval metadata does not carry over between iterations is a cache-invalidation rule for evidence, every iteration's evidence must be regenerated from the current skill version, evaluation state is never warm-startable, the one place where recomputing from scratch is the correctness requirement rather than the naive baseline.

**Revision decision.** State the iteration-granularity rule, fresh directories, fresh metadata, fresh baselines per the create-versus-improve rule, and the contamination hygiene, fresh threads, cleaned artifacts, series fallback under timeout pressure.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide which run records exist and which consumer each one feeds. The seed generic records stand beside the harness's observability design, which is unusually complete, transcripts persisted per run, timing captured at the notification moment or lost, metrics JSON counting tool calls and errors, grading evidence quoting its sources, analyst notes surfacing what aggregates hide, and user notes flagging executor uncertainties upward into grading.

**Recommended default and causal basis.** Persist every record class the harness names, and read user notes wherever grades look clean. The harness treats every run as an evidence-producing event whose observability is designed in advance, because its whole epistemology depends on being able to show why a number is what it is.

**Operating conditions and assumptions.** Executors actually write notes, the channel only carries what agents volunteer. The harness's run-level records plus this node's own evidence records, established in the completeness and metrics sections.

**Failure boundary and alternatives.** Each missing record breaks a documented consumer, no transcript starves the analyzer's execution-pattern reading, no timing empties the benchmark's resource columns irrecoverably, no evidence field turns grades into assertions about assertions. Bounded alternatives and recoveries: platform-level telemetry where it exists, the harness's file-based records work without any.

**Counterexample, gotchas, and operational comparison.** Timing is the one record with a hard capture window, the notification moment, all other records can be reconstructed late from disk, timing cannot, so pipeline designs should order timing capture first among post-run tasks. Good: a run directory with transcript, timing, metrics, evidenced grading, and a doubt-bearing user note. Bad: a benchmark whose resource columns are silently zero from missed notifications. Borderline: skipping user notes on a trivial eval, cheap omission, the channel costs nothing to leave open.

**Verification, evidence, and uncertainty.** Reconstruct a full run's story from its records alone at the next refresh. The record inventory and consumer mappings read across the schema sheet and role prompts. Note-writing diligence in live executors is unrecorded.

**Second-order consequence.** The user-notes channel is the harness's most humane record, executors flag their own uncertainties and workarounds, and the grader is instructed to read them because they may reveal problems even when expectations pass, a designed path for the executing agent's doubt to survive into the quality verdict, doubt as a first-class observable.

**Revision decision.** Adopt the harness's record inventory as the section's core, transcripts, timing, metrics, evidenced grades, analyst notes, user notes, each with its documented consumer named.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture tool-call count, context files loaded, delegated tasks, retry count, and completion-audit outcome.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide what evidence a skill's performance cost and benefit claim requires. The seed latency packet stands beside the harness's own performance doctrine, skills are performance-verified as a cost-benefit delta, time and token overhead against pass-rate gain, with mean and stddev over repeated runs, outlier awareness in the analyzer's checklist, and the explicit example trade, thirteen seconds bought fifty points.

**Recommended default and causal basis.** Verify skill performance by multi-run deltas with variance reported, and read cost and benefit together. A skill is a context investment, it spends tokens and time on every invocation, so the harness prices it like one, measuring both sides of the ledger per configuration and refusing single-run numbers.

**Operating conditions and assumptions.** Runs per configuration stay at three or more, below that stddev is decoration. Performance verification of skills as artifacts, performance doctrine inside created skills routes to their themes.

**Failure boundary and alternatives.** Single-run timing on nondeterministic agents is noise presented as measurement, and pass-rate-only evaluation hides skills whose gains cost more context than they return, the failure the delta schema structurally prevents. Bounded alternatives and recoveries: the Rust themes' benchmark-versus-contract-test doctrine for performance claims inside created skills, different artifact, same claims-need-evidence spine.

**Counterexample, gotchas, and operational comparison.** Token overhead partly measures the skill's own length being loaded, the analyzer's example attributes higher usage to script output parsing, so overhead decomposition matters before cutting content, the expensive part may be the work, not the words. Good: a skill adopted on a plus-fifty pass delta priced against plus-thirteen seconds across three runs. Bad: a skill judged fast from one lucky run. Borderline: adopting a skill whose delta is positive but within stddev, the analyzer's flakiness flag applies, run more before deciding.

**Verification, evidence, and uncertainty.** Sample adoption decisions for multi-run provenance and joint cost-benefit reading. The run-summary schema and analyzer pricing note read this session. How token costs scale with skill length in current runtimes is platform-volatile.

**Second-order consequence.** The harness quietly holds the corpus's most complete answer to the debug-mode problem in agent form, run variance is not an obstacle to measurement but a measured quantity, stddev in every summary block, high-variance evals flagged as possibly flaky rather than averaged into false precision, uncertainty reported alongside every mean as schema, not as caveat.

**Revision decision.** State the cost-benefit method, repeated runs per configuration, joint reading of pass, time, and token deltas, analyst screening for outliers and variance.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require tool-call budgets, timeout budgets, and a resumable journal when the workflow exceeds one focused session.
Leading indicator to measure: the next task uses the reference to make a better decision with less ambiguity.
Failure signal to watch: the reference remains a source map and never becomes an operating guide.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what eval count and skill length the documented designs stop applying. The seed task-splitting bounds stand beside the systems' own scale limits, the harness scales to a handful of evals iterated by one attentive user, its viewer, feedback loop, and same-turn spawn design all assume single-digit test cases, and the entrypoint says so, expand the test set and try again at larger scale is the exit, not the loop, while the Codex guide bounds SKILL.md at 500 lines and pushes overflow into referenced files.

**Recommended default and causal basis.** Keep iteration loops at reviewable eval counts, exit to larger scale only after convergence, and split skills at the line boundary with clear pointers. The human review loop is the harness's throughput ceiling, one user clicking through outputs caps eval count long before compute does, and context economics cap skill length, both systems name their own boundaries explicitly.

**Operating conditions and assumptions.** A single user reviews, team review might raise the eval ceiling at coordination cost. Bounding the creator systems' designed operating ranges, created skills' own scale limits belong to their content.

**Failure boundary and alternatives.** Past the eval ceiling, feedback quality collapses before run capacity does, users skim instead of review, and past the line ceiling, skills hog the context window the Codex guide calls a public good. Bounded alternatives and recoveries: automated large-scale eval sweeps beyond the loop, which the entrypoint gestures at but does not design, genuinely beyond the documented systems.

**Counterexample, gotchas, and operational comparison.** The 500-line rule appears in both platforms' guidance with different framing, ideal versus hard budget language, an author targeting both platforms should treat it as binding, the stricter reading costs nothing and satisfies both. Good: a five-eval loop converging then expanding the test set. Bad: a forty-eval iteration whose feedback came back all empty from fatigue. Borderline: a 520-line SKILL.md pending its split, briefly tolerable, both guides say the split is the fix.

**Verification, evidence, and uncertainty.** At each loop design, check eval count and line count against the documented ceilings. Both entrypoints' explicit boundary statements read this session. Where attention actually collapses per user varies, the documented numbers are design targets.

**Second-order consequence.** Both boundaries are attention budgets, the eval ceiling rations the user's reviewing attention, the line ceiling rations the model's context attention, and both systems solve them the same way, progressive disclosure, the harness discloses outputs one test case at a time in the viewer exactly as skills disclose content one reference file at a time, one design principle governing both the tool and its products.

**Revision decision.** Name both limits with their documented exits, larger-scale test expansion after loop convergence, and progressive-disclosure splitting at the line boundary.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Skill Creator Evaluation Patterns stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal decay in this node's claims and on what clocks. The seed theme-name queries would surface generic skill-authoring chatter while this node's staleness has four homes, the five twin identities, the Codex side's sweep-only currency, the platform seams, notifications, CLI interfaces, script flags, and the possibility of newer creator versions in later monthly sweeps this corpus has not mapped.

**Recommended default and causal basis.** Run twin diffs on audit cadence, sweep scans on each new monthly import, and seam checks at external refresh windows. The twin diffs cover Claude-side currency mechanically, the Codex side has no twin and ages invisibly until a sweep imports a newer version, and the seams age with platforms entirely outside the repository.

**Operating conditions and assumptions.** Monthly sweeps continue arriving, a frozen archive would decay this theme toward the historical pattern. Refreshing this node's evidence, seams, and version awareness, created-skill freshness belongs to skill owners.

**Failure boundary and alternatives.** Internal-only refreshing leaves seam rot and Codex staleness undetected, precisely the two surfaces where this theme's guidance touches live systems. Bounded alternatives and recoveries: declaring the theme historical and dropping currency claims, defensible if maintenance lapses, and it should be a recorded decision.

**Counterexample, gotchas, and operational comparison.** A newer creator version found in a sweep does not obsolete this theme, it extends it, the 202603 pair becomes the ancestor generation and the correspondence map gains a version dimension, plan the schema for that now rather than rebuilding then. Good: a refresh logging clean diffs, a sweep scan, and a dated seam check. Bad: logging that skill creator searches returned nothing new. Borderline: skipping seam checks because no one currently runs the harness, defensible deprioritization, record it.

**Verification, evidence, and uncertainty.** Record each probe with its date and the claim it confirmed or updated. The currency structure and seam inventory established this session. Future sweep contents are exactly what the sweep scan exists to learn.

**Second-order consequence.** This theme's refresh economics invert the frozen-pair themes, there the evidence was static and the obligation was interpretive, here the evidence claims currency, so refresh work is front-line evidence maintenance, the price of the deployment-identity advantage is that this node must actually keep earning it every cycle.

**Revision decision.** Replace the name queries with the four probes, twin diffs, sweep scans for newer creator versions on both platforms, seam checks against current platform documentation, and correspondence-map re-synthesis triggered by any version event.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | skill creator evaluation patterns official documentation best practices |
| `github_repository_query_phrase` | skill creator evaluation patterns GitHub repository examples |
| `release_notes_query_phrase` | skill creator evaluation patterns changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label, chain, and state each claim class here may be reused. The seed label definitions stand without this assignment's distinctive ledger, six unique documents read in full totaling 2024 lines, five identity diffs run, zero external URLs fetched, zero harness scripts executed, and a claim population that is unusually same-session, nearly everything here rests on this session's own reads rather than inherited chains.

**Recommended default and causal basis.** Before reusing a claim, declare class, chain, and verification state, and never let the currency advantage stand in for behavioral verification. The theme's sources are self-contained, one bundle plus one counterpart, so unlike the Rust lineage themes there are almost no cross-assignment composites, the evidence chains are short, fresh, and locally verifiable.

**Operating conditions and assumptions.** The twin identity holds and the documents stay unedited, both watched by the established probes. Reuse rules for this document's claims, each claim travels with class, chain, and verification state.

**Failure boundary and alternatives.** The costliest mislabel would present harness behavior as observed execution, every workflow claim here is documentation-derived, the scripts were never run, the subagents never spawned, prompt-described behavior is not demonstrated behavior. Bounded alternatives and recoveries: running the harness once to convert workflow claims to demonstrated behavior, declined to keep scope on synthesis, and queued as this theme's natural next verification step.

**Counterexample, gotchas, and operational comparison.** The philosophy comparison is interpretation over complete evidence, stronger than inference from partial reads but still not fact, reuse should carry the interpretation label even though every underlying line was read. Good: reusing the twin identity as dated fact and the workflow as documented-but-unrun design. Bad: citing benchmark aggregation as tested behavior on this document's authority. Borderline: reusing the blinding-convergence insight as established fact, complete reads support it, the interpretive label still applies.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares class, chain, and state cleanly. This assignment's ledger, six full reads, five diffs, zero fetches, zero executions. Behavioral verification awaits an actual harness run on a live platform.

**Second-order consequence.** This theme and the frozen gate pair sit at opposite ends of the same axis, there the evidence was old and the unrun commands stranded, here the evidence is provably current yet equally unrun, currency of text and verification of behavior are independent dimensions, a document can be perfectly up to date about machinery nobody has demonstrated, and the labels must track both dimensions separately.

**Revision decision.** Publish the strata explicitly, document contents and twin identity as same-session facts with dated diffs, cross-platform correspondence as same-session synthesis over full reads, philosophy characterizations as labeled interpretation, and all workflow behavior as read-but-unrun.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
