# Python Reference Routing Sources

**Decision supported.** This section helps decide how a reader or agent should enter the python-coder-01 bundle and how its claims can be traced back to sources. The seed title promises routing and sourcing but the seed never says what the two mapped files actually are, a 28-line router that tells readers which reliability section to open, and a research brief that discloses the sixteen primary sources and three repo-local precedents behind the whole python-coder-01 bundle.

**Recommended default and causal basis.** Enter the bundle through reference-map.md's three-step Use Order and consult sources-and-research-brief.md whenever a claim's provenance or freshness is questioned. Reference-map.md exists to prevent full-file reads, its Jump Points name eight destinations, its Section Search gives three rg commands, and its Use Order prescribes scoreboard first, matching sections only, anti-patterns last, while sources-and-research-brief.md exists to make the bundle's evidence auditable.

**Operating conditions and assumptions.** The reader has a bounded Python task touching one identifiable boundary, typing, parsing, async, testing, or packaging, so the map's section routing has something to route. This reference governs how to navigate into the bundle and how to audit where its claims came from, it does not restate the reliability patterns or quality gates those destinations contain.

**Failure boundary and alternatives.** Routing guidance decays into a table of contents if it drops the Use Order, the map's value is the reading discipline it imposes, not the list of headings it repeats. Bounded alternatives and recoveries: full sequential reads of the reliability reference when the task spans several boundaries at once, slower but immune to misrouting.

**Counterexample, gotchas, and operational comparison.** The map routes only within python-coder-01, questions outside its five numbered areas silently fall through to full-corpus search where the map gives no help. Good: an agent running the map's rg section-search line and opening only the async section for a cancellation bug. Bad: pasting the whole reliability reference into context for a one-line fixture question. Borderline: skipping the scoreboard step on a repeat visit, defensible once destinations are memorized.

**Verification, evidence, and uncertainty.** Confirm a routed reading session touched only the sections the map named and that any provenance question was answered from the brief, not from memory. Full reads this session of the 28-line map and the research brief with its sixteen listed primary sources. Whether the map's eight jump points stay accurate as the bundle's reference files are revised is not enforced by any tooling.

**Second-order consequence.** The map and the brief are the bundle's two ends of the same accountability loop, the map tells a reader where to spend attention and the brief lets a skeptic check whether the destination deserved it.

**Revision decision.** Open by naming the two instruments, a router that rations reading and a provenance ledger that discloses sources, so readers use each for its designed job.

**Retained seed evidence.** The exact theme title and routing framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide which of the two mapped files, or which unretrieved link, may justify each class of claim in this theme. The seed map lists both files under the same generic skill-entrypoint role although they do opposite jobs, the reference map is a navigation instrument consumed before reading and the research brief is a provenance disclosure consumed when checking claims.

**Recommended default and causal basis.** Cite the map for routing claims, cite the brief for what-was-consulted claims, and refuse to let either stand in for a live external retrieval. The map's own opening line says use this file to route quickly before loading the full Python references, while the brief's opening line dates the skill's preparation to 2026-06-23 and names its documentation basis, two self-declared and incompatible roles.

**Operating conditions and assumptions.** The archive path codex-skills-202606/python-coder-01/references keeps both files byte-stable at their recorded locations. The table records provenance for this document's claims, it does not certify that the brief's sixteen links still resolve or still say what they said in 2026.

**Failure boundary and alternatives.** The four external URL rows repeat the bundle-wide doc-site set with sourced-fact labels although this evolution retrieved none of them, and the brief itself names twelve further primary links the seed table omits entirely. Bounded alternatives and recoveries: replace the four generic URL rows with the brief's own more specific pages, PEP 8, PEP 484, pytest good-practices, and the Ruff formatter page, which are the bundle's declared basis.

**Counterexample, gotchas, and operational comparison.** The brief's link list looks like a bibliography of verified facts but records author intent at preparation time, links may have been skimmed, misread, or drifted since. Good: citing the map's Use Order for a reading-sequence claim. Bad: citing the brief's mypy link as evidence about current mypy behavior. Borderline: citing the brief to say the bundle used PEP 484, correct as a claim about the bundle, wrong as a claim about typing.

**Verification, evidence, and uncertainty.** Audit each claim in this document for either a named heading in one of the two mapped files or an explicit unretrieved-candidate label. The self-describing opening lines of both mapped files read in full. How many of the brief's sixteen links still match their 2026-06-23 content cannot be determined from the archive alone.

**Second-order consequence.** The brief is second-order evidence, a local file whose content is a list of external claims, citing it proves what the bundle authors consulted, never what the consulted pages said.

**Revision decision.** Split the two local rows into navigation-instrument and provenance-ledger roles and mark all external rows as unretrieved candidates pointed at by the brief.

**Retained seed evidence.** Both local rows and all four external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202606/python-coder-01/references/reference-map.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202606/python-coder-01/references/sources-and-research-brief.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| https://docs.python.org/3/library/typing.html | external_research_source_material | external_research_sourced_fact | primary typing module documentation |
| https://docs.pytest.org/en/stable/ | external_research_source_material | external_research_sourced_fact | primary Python testing framework documentation |
| https://mypy.readthedocs.io/ | external_research_source_material | external_research_sourced_fact | static type checking documentation and constraints |
| https://docs.astral.sh/ruff/ | external_research_source_material | external_research_sourced_fact | linting and formatting tool documentation |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which navigation device a reader reaches for first when entering the bundle under time pressure. The seed scoreboard scores three document-hygiene rules but ignores the ranking decision this theme actually owns, which of the map's three navigation devices, jump points, rg section search, or use order, should dominate a reader's habit.

**Recommended default and causal basis.** Internalize the three-step Use Order as the habit and treat the other two devices as its supporting infrastructure. The map itself ranks them by position and imperative force, the Use Order is numbered and prescriptive, the Jump Points are the destinations it orders, and the Section Search is the fallback for headings the jump list does not name.

**Operating conditions and assumptions.** Tasks arrive one boundary at a time so a scoreboard-then-sections-then-gates pass stays short enough to repeat every session. This ranking orders navigation devices for habitual use, it does not rank the reliability patterns at the destinations, which the bundle's own Pattern Scoreboard already covers.

**Failure boundary and alternatives.** Reading the seed's 95, 91, and 88 scores as measured routing efficiency would fabricate data, they encode the corpus template's authorial emphasis and nothing about this bundle. Bounded alternatives and recoveries: reorder devices by observed misroute frequency once enough routed sessions have been journaled to show where readers actually get lost.

**Counterexample, gotchas, and operational comparison.** Device priority inverts for agents, an agent greps cheaply but follows prose ordering unreliably, so the rg lines outrank the numbered steps for machine readers. Good: a five-minute session that reads the scoreboard, opens one section, and closes at the stack. Bad: browsing all eight jump points because no step was chosen. Borderline: jumping straight to a memorized section, efficient for veterans, it silently skips the closing gate step.

**Verification, evidence, and uncertainty.** Check each prioritized device against the map's literal headings and confirm the priority claim cites position or imperative form, not invented measurements. The numbered Use Order, bulleted Jump Points, and three rg lines of the map read in full. Actual misroute rates for readers who skip the Use Order have never been measured in this corpus.

**Second-order consequence.** The Use Order's third step, finish with High-Value Anti-Patterns and the Default Reliability Stack, quietly welds navigation to quality gating, every routed session ends at the gate no matter where it started.

**Revision decision.** Add a device-priority row set, use order first as the governing habit, jump points second as its address book, rg search third as the escape hatch for unlisted headings.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `python_reference_routing_sources` | 95 | default_adoption_pattern_tier | Source Map First: Load local python reference material before synthesizing routing sources recommendations. |
| `python_reference_routing_sources` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `python_reference_routing_sources` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what organizing principle new routing or sourcing guidance for this bundle should follow. The seed thesis recites the generic load-local-first formula while the mapped files argue something sharper, reading is a budget to be rationed by a router and every synthesized claim must carry a disclosed pedigree.

**Recommended default and causal basis.** Write any new bundle-level guidance so a reader can reach it through the map in one step and audit it through the brief in one lookup. The map spends all 28 of its lines preventing unnecessary reading, and the brief spends its entire length disclosing pedigree, sixteen primary links, three repo-local precedents, and three synthesis notes that even declare what the skill deliberately excludes.

**Operating conditions and assumptions.** The bundle keeps its two-file split, one router and one ledger, so each thesis half has a single instrument to bind to. The thesis governs how bundle knowledge is entered and audited, it does not govern the content quality of the destination references, which have their own themes.

**Failure boundary and alternatives.** A ration-and-disclose thesis assumes the router is current, a stale map rations reading toward sections that no longer exist, turning the discipline itself into the source of misdirection. Bounded alternatives and recoveries: inline citations inside each destination reference instead of a central ledger, heavier to maintain but immune to ledger-reference drift.

**Counterexample, gotchas, and operational comparison.** Disclosure invites false confidence, a sixteen-link bibliography reads as thoroughness even to readers who will never open a single link. Good: a new reference section registered in the map's jump points the same day it lands. Bad: a new pattern justified by it is well known with no brief entry. Borderline: citing the brief's repo-local rust-coder-02 precedent for a Python convention, disclosed but cross-language.

**Verification, evidence, and uncertainty.** Test each thesis clause against the two files, rationing clauses should trace to map lines and disclosure clauses to brief lines. The complementary single-purpose structure of the two mapped files read in full. Whether one central ledger scales past a handful of reference files or degrades into an unmaintained appendix is unresolved.

**Second-order consequence.** The brief's synthesis note that framework-specific doctrine should live in companion skills is a routing decision at authoring time, exclusion boundaries are themselves navigation devices that tell readers where this bundle deliberately ends.

**Revision decision.** Restate the thesis as ration reading through the router and disclose pedigree through the brief, with router freshness named as the standing precondition.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `python_reference_routing_sources` contains 2 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Python Reference Routing Sources comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide which of the two mapped files a given question should open, or whether it belongs in this theme at all. The seed map lists both files with heading signals but no guidance on which file answers which question class, leaving a reader to open both for every lookup even though their contents never overlap.

**Recommended default and causal basis.** Classify the incoming question as where-to-read or built-from-what before opening either file, then open exactly one. The split is clean by construction, where-do-I-read questions are answered only by the map's Jump Points, Section Search, and Use Order, and what-was-this-built-from questions only by the brief's Primary Sources, Repo-Local Sources, and Synthesis Notes.

**Operating conditions and assumptions.** Questions arrive separable into navigation and provenance classes, mixed questions must be split before routing. This map routes between the two mapped files, routing into the reliability and gates content is the job of the reference map itself, one level down.

**Failure boundary and alternatives.** The routing key breaks for content questions, neither file contains a single line of actual Python guidance, a reader routed here for a cancellation or fixture answer has been misrouted twice. Bounded alternatives and recoveries: merging both files into one indexed document, one lookup for everything at the cost of blurring the instrument-ledger distinction.

**Counterexample, gotchas, and operational comparison.** The brief's Repo-Local Sources section looks navigational because it contains paths, but those paths are authoring precedents from 202603 skills, not reading destinations. Good: opening only the brief to answer which PEPs informed the typing guidance. Bad: scanning the brief for the async section's location. Borderline: opening the map to check whether packaging is in scope, workable since jump point five names packaging, though the brief's exclusion note is the sharper answer.

**Verification, evidence, and uncertainty.** Spot-check the heading signals of both rows against the live files and confirm the question-class key still matches their actual sections. Section-by-section content audit of both mapped files performed this assignment. Whether future bundle revisions keep navigation and provenance in separate files is a maintainer choice this map cannot constrain.

**Second-order consequence.** Because the two files partition perfectly, any question that fits neither file is a signal the questioner is in the wrong theme entirely, the partition doubles as a misroute detector.

**Revision decision.** Attach a question-class key to each row, navigation and reading-order questions to the map, provenance and freshness questions to the brief.

**Retained seed evidence.** Both local source rows with title, heading signals, and usage role remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202606/python-coder-01/references/reference-map.md | Reference Map | Reference Map; Jump Points; Section Search; Use Order | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202606/python-coder-01/references/sources-and-research-brief.md | Sources And Research Brief | Sources And Research Brief; Primary Sources; Repo-Local Sources; Synthesis Notes | skill entrypoint or reusable agent prompt |

## External Research Source Map

**Decision supported.** This section helps decide what confidence external links may contribute to routing-theme claims and which links a refresh should fetch first. The seed map carries the bundle's four generic doc-site URLs although this theme's own mapped brief publishes a richer and more specific sixteen-link list that the seed never mentions.

**Recommended default and causal basis.** Treat the brief's link list as the authoritative retrieval queue for this bundle and quote it as archive fact about what the authors consulted. The brief names PEP 8, PEP 484, asyncio tasks, contextlib, dataclasses, built-in exceptions, unittest, the pyproject packaging guide, two pytest pages, mypy, Pyright, two Ruff pages, Black, and the typing dataclasses specification, each tied to a stated support role in its synthesis notes.

**Operating conditions and assumptions.** The linked standards and doc sites keep their URL structure, PEP links are effectively permanent, tool-doc links are the volatile minority. This map queues external verification targets for the routing theme, it grants no live-fact authority until a dated retrieval of a specific page exists.

**Failure boundary and alternatives.** None of the sixteen links nor the seed's four URLs was retrieved during this evolution, every one of them is candidate material and no current-behavior claim may rest on any of them. Bounded alternatives and recoveries: checking a target repository's pinned tool versions and changelogs directly, which answers current-behavior questions faster than any documentation page.

**Counterexample, gotchas, and operational comparison.** PEP links never drift but tool-doc links drift constantly, mixing both in one candidate list hides the fact that only a third of the list carries real staleness risk. Good: writing that the bundle's typing guidance drew on PEP 484 per the brief. Bad: asserting current Ruff formatter defaults from the unretrieved Ruff row. Borderline: inferring pytest primacy from two pytest links in the brief, plausible but the reliability reference's testing section is the checkable local evidence.

**Verification, evidence, and uncertainty.** Confirm every externally-flavored claim in this document names the brief as its local carrier and carries no retrieval date it cannot show. Zero external fetches recorded in this assignment's working notes. Which of the sixteen pages have changed materially since 2026-06-23 is unknowable without fetching them.

**Second-order consequence.** The brief pre-sorts future retrieval work, its synthesis notes bind link groups to claim groups, official docs to exception and asyncio guidance, tool docs to gate commands, so a refresh can fetch only the group backing a disputed claim.

**Revision decision.** Annotate the external rows as a subset of the brief's sixteen-link list and mark the whole list as unretrieved candidates inherited from the 2026-06-23 preparation.

**Retained seed evidence.** All four external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://docs.python.org/3/library/typing.html | Python typing docs | primary typing module documentation | external_research_sourced_fact |
| https://docs.pytest.org/en/stable/ | pytest documentation | primary Python testing framework documentation | external_research_sourced_fact |
| https://mypy.readthedocs.io/ | mypy documentation | static type checking documentation and constraints | external_research_sourced_fact |
| https://docs.astral.sh/ruff/ | Ruff documentation | linting and formatting tool documentation | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which navigation and sourcing behaviors this theme must reject on sight in session records. The seed registry lists three generic documentation failures but not the routing-specific hazards the mapped files are built to prevent, whole-corpus context dumping, unrouted reading, pedigree-free synthesis, and stale-router trust.

**Recommended default and causal basis.** Review agent and human sessions against the four routing rows whenever bundle material was consumed, using the journal record as the detection surface. Each mapped device is a countermeasure with a nameable failure behind it, the map's opening line exists because agents paste entire references into context, the Use Order exists because readers browse instead of route, and the brief exists because synthesized guidance otherwise carries no auditable basis.

**Operating conditions and assumptions.** Sessions leave enough trace, loaded files, sections opened, citations made, for the detection signals to have something to inspect. This registry names failures of bundle navigation and source discipline, code-level anti-patterns belong to the quality-gates theme next door.

**Failure boundary and alternatives.** Registry rows about reading behavior cannot be lint-caught, unlike code anti-patterns they are detectable only in journals and session records, so this registry needs a review-based detection column, not a tooling one. Bounded alternatives and recoveries: hard context budgets enforced by tooling instead of review-based detection, blunt but self-enforcing where journals are unreliable.

**Counterexample, gotchas, and operational comparison.** Context dumping often masquerades as thoroughness in review, a session that loaded everything looks diligent precisely when it was most wasteful. Good: bouncing a session record that cites no map trace for a three-file read. Bad: approving a synthesis because its prose sounds source-aware while the brief is never named. Borderline: a veteran skipping the scoreboard step from memory, tolerable until the map changes underneath them.

**Verification, evidence, and uncertainty.** Cross-check each registry row against the mapped device it inverts and confirm its detection signal is observable in an ordinary session journal. The countermeasure framing visible in the map's opening line and use order read in full. Base rates for these four failures among real sessions are unmeasured in this repository.

**Second-order consequence.** All four routing anti-patterns share one root, treating reading as free, the map's whole design is a bet that attention is the scarce resource and every row here is a way of spending it without accounting.

**Revision decision.** Add four routing rows, context dumping detected by context size versus task size, unrouted reading detected by absent Use Order traces, pedigree-free claims detected by missing brief citations, stale-router trust detected by map-to-file heading diffs.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide which commands constitute the verification gate for this theme's routing claims. The seed gate section supplies only the corpus document verifier while the mapped map publishes three runnable rg commands that are this theme's real operational tooling, plus an implicit freshness check no one has written down.

**Recommended default and causal basis.** Run the heading-enumeration rg lines from the bundle root and treat any jump point without a matching live heading as a router defect to fix before further routing. The map's Section Search block gives rg lines that enumerate live headings in both destination references and grep for the scoreboard and gate anchors, and comparing that live output against the map's own Jump Points is a mechanical staleness test for the router itself.

**Operating conditions and assumptions.** Rg is available and the bundle's relative layout is intact, both true in this archive and in any faithful checkout. These commands verify router accuracy and destination reachability, verifying the destination content's correctness belongs to the gates theme.

**Failure boundary and alternatives.** The rg lines assume the archive layout, they reference references/ relative paths that only resolve from the bundle root, run from anywhere else they silently return nothing and a naive reader concludes the sections are gone. Bounded alternatives and recoveries: a small script that parses the map's bullet list and asserts each named anchor exists, turning the manual diff into a repeatable check.

**Counterexample, gotchas, and operational comparison.** An empty rg result is ambiguous between wrong working directory and genuinely missing section, always confirm the path resolves before reading absence as drift. Good: running the heading scan before trusting a jump point in a months-old checkout. Bad: certifying the map fresh because its prose looks well maintained. Borderline: verifying only the two gate anchors and skipping the full heading scan, cheap and covers the mandatory closing step, misses drift elsewhere.

**Verification, evidence, and uncertainty.** Execute the three rg lines from the bundle root and record which jump points matched live headings. The Section Search command block of the map read in full. Whether future map revisions keep the rg lines synchronized with renamed sections depends on maintainer discipline.

**Second-order consequence.** The map contains its own audit tooling without saying so, the same commands it offers for finding sections double as the test of whether its jump points still tell the truth.

**Revision decision.** Import the three rg commands with their working-directory caveat and add the derived gate, diff rg-enumerated headings against the map's jump points to certify router freshness.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide what procedure an agent follows when a task requires the python-coder-01 bundle. The seed guide gives four generic bullets while the map's Use Order is already a three-step agent script, and an agent needs one addition the map leaves implicit, cite the brief when the synthesis is challenged.

**Recommended default and causal basis.** Have agents execute the Use Order literally, logging the boundary matched at step two and emitting a brief citation whenever asked what grounds the guidance. The map was written for exactly the agent failure its opening line names, loading full references, and its numbered steps translate directly to machine behavior, grep the scoreboard, open matched sections only, close with the anti-pattern and stack anchors.

**Operating conditions and assumptions.** The task names or implies one of the map's five boundary areas so step two has a match condition to evaluate. This guide scripts agent navigation and provenance behavior for the bundle, what the agent does with retrieved content is scripted by the destination themes.

**Failure boundary and alternatives.** Agents follow the letter of a script and miss its economy, an agent that opens every jump point technically routed but reproduced the context dump the map exists to prevent, step two's word only carries the entire value. Bounded alternatives and recoveries: embedding the routed spans directly in agent prompts for recurring task shapes, skipping live routing at the cost of freezing the routed content.

**Counterexample, gotchas, and operational comparison.** Step two fails open, a task matching no jump point tempts the agent to open everything, the correct behavior is falling through to the rg search and then escalating, never broadening the read. Good: an agent log showing scoreboard read, section three opened for a cancellation task, stack cited at close. Bad: eight jump points opened for a docstring question. Borderline: skipping step one on the fifth identical task of a session, efficient, should be logged as a deliberate skip.

**Verification, evidence, and uncertainty.** Reject any agent session over this bundle whose log cannot show the step-two match that justified each opened section. The numbered Use Order and its only qualifier read in full. Whether three steps stay sufficient as the bundle grows more reference files is untested.

**Second-order consequence.** The Use Order gives agents a natural session record for free, an agent that logs which step produced each retrieved span leaves exactly the trace the anti-pattern registry's detection signals need.

**Revision decision.** Restate the guide as the four-step agent script, scoreboard, matched sections only, closing gate anchors, brief citation on provenance challenge.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide whether the reader's current moment, pre-reading or provenance-challenge, is one this reference serves. The seed scenario describes a maintainer wanting general discipline, but the journey this theme serves starts at a sharper moment, a person or agent holding a bounded Python task and deciding what to read before touching code.

**Recommended default and causal basis.** Enter this reference when holding an unstarted Python task, exit with a section shortlist, and return only if someone challenges the basis of what was built. The map addresses exactly that pre-reading moment, its opening line says route quickly before loading the full references, and the brief serves the complementary later moment when a delivered synthesis gets challenged and must show its sources.

**Operating conditions and assumptions.** The task is scoped enough to name its boundary, exploratory greenfield work without a nameable boundary defeats section routing. The journey covered runs from task-in-hand to sections-chosen, plus the audit reprise when provenance is questioned, the reading and coding between those moments belong to destination themes.

**Failure boundary and alternatives.** Arriving mid-implementation misfires, someone already deep in code wanting a specific pattern needs the reliability reference directly, forcing them back through scoreboard routing adds ceremony precisely when context is hottest. Bounded alternatives and recoveries: for users arriving post-implementation with quality doubts, route to the gates theme, their moment has already passed this one.

**Counterexample, gotchas, and operational comparison.** The routing moment is skippable in a way the gate moment is not, nothing blocks a reader who dives straight into code, which is why routing discipline decays silently without journal checks. Good: a developer with a flaky-cancellation ticket spending ninety seconds here and leaving with one section name. Bad: a reviewer mid-code-review opening the map hoping for review prompts. Borderline: an agent re-routing on every subtask of one feature, correct per script, wasteful past the first.

**Verification, evidence, and uncertainty.** Ask whether a task exists and reading has not started, or a delivered synthesis is being audited, any other moment belongs elsewhere. The before-loading framing of the map and disclosure purpose of the brief. Median time from task receipt to correct section shortlist has never been measured for this bundle.

**Second-order consequence.** The journey has an asymmetry the seed misses, routing happens every session but the provenance reprise happens rarely and matters most, one unanswerable where-did-this-come-from erodes more trust than a dozen slow routings.

**Revision decision.** Recast the scenario around the two moments the mapped files serve, the pre-reading routing decision and the post-delivery provenance challenge.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The Python maintainer is starting from scripts or services that need typing, fixtures, packaging, and lint discipline and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `python_reference_routing_sources` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing how strict to make typing, tests, validation, and dependency hygiene for the current risk level.
Reference opening trigger: Open this file when the task mentions python reference routing sources, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide how much routing ceremony each session owes given the operator's familiarity with the boundary. The seed guide offers the generic adopt-adapt-avoid rows without this theme's live tension, routing overhead versus reading waste, every minute spent consulting the router is pure cost on tasks where the destination was already obvious.

**Recommended default and causal basis.** Route fully on first contact with a boundary, allow logged direct access thereafter, and reset everyone to full routing whenever the map file changes. The map prices its own overhead low, three steps and 28 lines, because it is betting against a much larger waste, full-reference context loads, and that bet only pays when the task's destination is genuinely uncertain.

**Operating conditions and assumptions.** Operators can honestly distinguish familiar from unfamiliar boundaries, and map changes are visible enough to trigger the reset. This guide tunes when routing ceremony applies, it does not license skipping the closing gate anchors, which the Use Order couples to every session regardless.

**Failure boundary and alternatives.** Mandatory routing on trivially-routed tasks teaches contributors to fake the trace, journals fill with pro-forma scoreboard citations for sessions that never opened it, corrupting the very records the anti-pattern detection needs. Bounded alternatives and recoveries: uniform mandatory routing regardless of familiarity, simpler to audit and defensible in high-turnover teams where familiarity claims cannot be trusted.

**Counterexample, gotchas, and operational comparison.** Self-assessed familiarity inflates, the honest test is whether the operator can name the destination section before opening the map, not whether the area feels known. Good: full routing for a first-ever packaging task, direct access with logged skip for the tenth async fix. Bad: a newcomer skipping routing because a veteran does. Borderline: direct access after a long gap away from a boundary, familiarity has a half-life the policy should acknowledge.

**Verification, evidence, and uncertainty.** Sample logged skips quarterly and check the operator could indeed name the destination before opening it. The low-ceremony three-step design of the map read in full. The familiarity threshold at which routing stops paying is judgment, no measurements exist.

**Second-order consequence.** Routing value concentrates in the first sessions with any boundary and decays with familiarity, but the map's freshness risk moves opposite, growing over time, so veterans who earned the right to skip routing are exactly the readers most exposed to silent map drift.

**Revision decision.** Add the overhead-waste axis with tiered defaults, full three-step routing for unfamiliar boundaries, direct section access with a logged skip for boundaries the operator has routed before.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the python reference routing sources pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong python reference routing sources guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which file's word governs when the router, the ledger, and the destination references disagree. The seed hierarchy names the map canonical and the brief supporting but leaves the precedence question unanswered for the one conflict that matters, what wins when the map's jump points and the live reference headings disagree.

**Recommended default and causal basis.** Resolve any map-versus-destination mismatch by trusting the destination and filing the mismatch as router drift to repair. The map is a derived index over the two destination references, its authority is entirely borrowed, so on any content disagreement the destination files win by construction and the map is simply stale.

**Operating conditions and assumptions.** All bundle files remain at their archived paths so the three-way comparison is always mechanically possible. This hierarchy settles precedence among the two mapped files and their destinations, precedence among the destination references themselves belongs to their own themes.

**Failure boundary and alternatives.** Calling the map canonical invites the inverted reading, trusting the index over the indexed, a reader who cannot find a jump-point section may conclude the content was removed when only the pointer rotted. Bounded alternatives and recoveries: treating this corpus's own python themes as a second-opinion layer above the bundle, useful when the bundle's internal hierarchy cannot settle a question.

**Counterexample, gotchas, and operational comparison.** The brief's date, 2026-06-23, silently scopes its authority, it is canonical about that day's preparation and says nothing about any later edit to the bundle. Good: fixing the map when rg shows a renamed section its jump point missed. Bad: deleting a reference section because the map no longer lists it. Borderline: trusting the brief's exclusion note over a packaging jump point to decide scope, both speak, the note is more specific.

**Verification, evidence, and uncertainty.** Diff the map's jump points against rg-enumerated destination headings and confirm every disagreement was resolved toward the destination. The derived-index relationship visible across all bundle files read in full. Whether maintainers will update the map atomically with destination edits is a process question this hierarchy cannot enforce.

**Second-order consequence.** Each file in this hierarchy is unfalsifiable on exactly one axis, the brief can never be wrong about what it lists, the map can never be wrong about what order it prescribes, conflicts are only possible where a file speaks beyond its axis.

**Revision decision.** Declare the destination references canonical for content, the map canonical only for reading order, and the brief canonical only for preparation-time provenance.

**Retained seed evidence.** Both hierarchy rows and the confidence warning remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202606/python-coder-01/references/reference-map.md | canonical primary source | Reference Map; Jump Points; Section Search | What guidance, warning, or example should this source contribute to Python Reference Routing Sources? |
| agents-used-monthly-archive/codex-skills-202606/python-coder-01/references/sources-and-research-brief.md | supporting detail source | Sources And Research Brief; Primary Sources; Repo-Local Sources | What guidance, warning, or example should this source contribute to Python Reference Routing Sources? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what concrete object a team maintains to make routed bundle entry repeatable and auditable. The seed artifact table asks for abstract routing rules while the mapped files compose directly into a concrete object, a routing card that pairs each jump point with its live heading check and each claim class with its brief citation line.

**Recommended default and causal basis.** Instantiate one routing card per active checkout, refresh it by rerunning the heading checks, and retire it the moment any bundle file changes. Every element of such a card is verbatim bundle content, eight jump points, three rg commands, three use-order steps, and the brief's link groups, only the last-verified date requires per-checkout instantiation.

**Operating conditions and assumptions.** Someone owns the card per checkout, ownerless cards decay into exactly the stale-router hazard they exist to prevent. The card standardizes routed entry into this bundle, it does not replace the map file, which stays the reference of record the card is derived from.

**Failure boundary and alternatives.** A routing card without its verification date is worse than none, it propagates the map's pointers with an added layer of authority while dropping the one field that flags staleness. Bounded alternatives and recoveries: a checked-in script that regenerates the card from live files on demand, eliminating manual refresh at the cost of maintaining the generator.

**Counterexample, gotchas, and operational comparison.** Laminating the use order into the card is safe, laminating the jump-point line numbers is not, positions drift faster than names and the card should carry anchors, never line numbers. Good: a card dated last week with all eight heading checks marked found. Bad: an undated laminated card from a fork three revisions old. Borderline: sharing one card across teammates on the same commit, sound until anyone's checkout diverges.

**Verification, evidence, and uncertainty.** Check the card's date against bundle file history and spot-rerun two of its heading checks. The direct composability of card fields from the two mapped files read in full. Card upkeep economics across many simultaneous checkouts is untested here.

**Second-order consequence.** The card's date field turns router freshness from a trust question into an arithmetic one, any reader can compare card date against bundle file modification dates and know instantly whether re-verification is owed.

**Revision decision.** Define the artifact as the dated routing card, jump points with heading-check results, the three-step order, and the brief's claim-class citation key.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: canonical source routing rules for stale, duplicate, and conflicting sources.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Python Reference Routing Sources | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercises most efficiently teach someone to operate this theme's routing and provenance discipline. The seed examples describe reference usage abstractly while the mapped files support a concrete end-to-end walkthrough, one task routed through all three use-order steps with a provenance challenge answered from the brief at the end.

**Recommended default and causal basis.** Train new operators on the hit walkthrough first, then deliberately hand them a miss-case task before certifying them on this bundle. The map's steps are demonstration-shaped, a walkthrough needs only a realistic task, a cancellation bug say, to show the scoreboard read, the single-section match on async and resource reliability, the closing anti-pattern check, and the brief lookup when asked why cancellation guidance is trustworthy.

**Operating conditions and assumptions.** Training tasks are realistic enough to have a genuine boundary, contrived tasks with obvious destinations teach ritual, not routing. Worked examples here teach routing and provenance mechanics, examples of applying the routed content belong to the destination themes.

**Failure boundary and alternatives.** Single-happy-path walkthroughs mislead about the fallback, learners also need the miss case, a task matching no jump point, resolved by the rg search rather than by opening everything, or the fallback never becomes habit. Bounded alternatives and recoveries: paired routing, a newcomer routes aloud while a veteran observes, slower than solo walkthroughs but surfaces misconceptions live.

**Counterexample, gotchas, and operational comparison.** Walkthroughs rehearsed only on the current map overfit to its present jump points, rotating one renamed-section drill keeps the staleness reflex alive. Good: a trainee routing a fixture-flakiness task to the testing section in under two minutes and citing the pytest links from the brief when challenged. Bad: memorizing the eight jump point names without ever routing a task. Borderline: practicing on the same boundary repeatedly, fluency in one route, fragility in seven.

**Verification, evidence, and uncertainty.** Have the trainee route a novel task solo and compare their step trace against a veteran's routing of the same task. The walkthrough-ready three-step structure of the map read in full. How many practice routings produce durable routing habit is unmeasured.

**Second-order consequence.** The miss case teaches more than the hit case, the moment a jump point fails is where readers choose between the rg fallback and the context dump, and one rehearsed miss inoculates against the dump better than any rule statement.

**Revision decision.** Anchor the section on two walkthroughs, a hit case routed in three steps with a brief-backed provenance answer, and a miss case resolved through the rg fallback.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Python Reference Routing Sources after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Python Reference Routing Sources as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Python Reference Routing Sources only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which measurements justify the router's existence and drive its revision. The seed metrics name a coverage indicator but no loop connects routing outcomes back to the router, nothing counts which jump points get used, which rg fallbacks fire, or how often provenance challenges find the brief sufficient.

**Recommended default and causal basis.** Record the jump-point outcome and any fallback in each session journal entry and review the quarterly distribution before touching the map. Routed sessions generate classifiable events for free, each session has a matched-or-missed jump point, a fallback-used flag, and occasionally a provenance-challenge outcome, and tallying them shows exactly where the router earns or fails.

**Operating conditions and assumptions.** Journals capture routing events consistently enough to aggregate, sporadic recording biases every count toward the diligent. This loop tunes the router and the ledger, it does not measure destination content quality, which the destination themes own.

**Failure boundary and alternatives.** Usage tallies mislead without task-mix context, a never-used jump point may mark dead weight or simply a boundary the team has not touched this quarter, raw counts need the task distribution beside them. Bounded alternatives and recoveries: instrumenting an agent wrapper to log routing automatically, complete capture for agent sessions, humans still need the journal habit.

**Counterexample, gotchas, and operational comparison.** Publishing per-person routing stats turns discipline into surveillance, aggregate by boundary area and keep individuals out of the tally. Good: a quarter showing three fallback firings on a subprocess topic, and the next map revision adds that jump point. Bad: declaring the router useless because veterans stopped consulting it. Borderline: timing routing overhead per session, useful for the tradeoff guide, easy to over-optimize into skipping.

**Verification, evidence, and uncertainty.** Confirm journal entries for recent bundle sessions carry routing outcomes and that the last quarterly review consumed them. The tallyable event structure implied by the map's fixed jump list read in full. Expected fallback rates for a well-tuned router have no baseline yet in this repository.

**Second-order consequence.** Fallback firings are the loop's sharpest signal, every rg-fallback event names a section readers needed that the jump list lacks, making the miss log a ready-made backlog for the map's next revision.

**Revision decision.** Add the routing tally loop, per-jump-point match counts, fallback firings, and provenance-challenge outcomes, reviewed quarterly against the quarter's task mix.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: the next agent can modify the package without weakening type, lint, or fixture coverage.
Failure signal: the reference says quality matters but gives no migration path from loose scripts to strict package code.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before declaring this reference complete and faithful to its two sources. The seed checklist confirms this document's sections exist but never audits its routing content against the mapped files, no item catches a dropped jump point, a paraphrased use-order step, or a silently shortened link list.

**Recommended default and causal basis.** Run the count audit plus the role spot-check at every reread and refresh cycle before accepting this document as complete. This theme's correctness is largely transcription correctness, eight jump points, three rg commands, three steps, sixteen primary links, three repo-local paths, three synthesis notes, all countable against two short files in minutes.

**Operating conditions and assumptions.** Both mapped files remain available at their archived paths for line-level comparison. This checklist audits this reference document's fidelity to its two sources, auditing the router against its destinations is the verification gate's job.

**Failure boundary and alternatives.** Count checks miss role drift, a brief link that kept its URL but got recategorized from supporting typing guidance to supporting gates would pass every count while corrupting the citation key. Bounded alternatives and recoveries: checksumming both source files and re-auditing only on checksum change, near-zero steady-state cost, trades away drift detection in this document's own text.

**Counterexample, gotchas, and operational comparison.** Auditing against memory of the files rather than the files themselves recreates the pedigree-free-synthesis failure inside the audit itself. Good: an audit catching that the transcribed link list dropped the Pyright entry. Bad: passing the audit because all 26 headings exist. Borderline: accepting paraphrased use-order steps, tolerable if the only-matched-sections restriction survives verbatim.

**Verification, evidence, and uncertainty.** Count devices, steps, links, paths, and notes in this document against both source files and spot-check three link roles. The fully countable structure of both mapped files read in full. Whether count-plus-role auditing catches all meaningful drift in routing guidance is unproven.

**Second-order consequence.** The two mapped files are so short that full re-transcription-audit costs less than selective checking, below some source size the economical audit is simply reading both files again beside this document.

**Revision decision.** Add fidelity items with the exact counts, and a role-spot-check item pairing three random brief links against the synthesis note that claims them.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Python Reference Routing Sources.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a question that arrived at this router theme but is not a reading-order or provenance question. The seed routing splits the theme name into python, reference, and routing tokens, producing self-referential rows, while the real neighbors are the entrypoint theme for activation, the gates theme for verification, and the reliability-patterns theme for the content this router points at.

**Recommended default and causal basis.** Classify arriving questions as reading-order, provenance, activation, verification, or behavior, and keep only the first two. This theme is the corpus's router-about-a-router, questions that reach it wrongly are usually one hop from home, activation questions belong to python_language_skill_entrypoint, quality questions to python_quality_antipattern_gates, and content questions to the reliability material itself.

**Operating conditions and assumptions.** The named destination themes exist in the corpus and cover their assigned question classes. Routing sends away questions this theme cannot close, it does not summarize what the destination themes contain.

**Failure boundary and alternatives.** The commonest misroute arriving here is the content question, how do I handle cancellation, which no amount of routing guidance answers, the tell is any question about Python behavior rather than about reading or provenance. Bounded alternatives and recoveries: for tool-behavior questions no local theme answers, external documentation explicitly labeled unretrieved, never presented as verified.

**Counterexample, gotchas, and operational comparison.** Provenance questions about destination content, which sources back the async guidance, look like they belong to the destination but the brief answers them here, provenance stays with this theme regardless of which content it grounds. Good: sending a when-does-the-skill-activate question to the entrypoint theme. Bad: routing to the reference adjacent reference, a token synonym for this same file. Borderline: keeping a which-section-covers-protocols question, answerable here via the map, though the reliability reference's own headings answer it more directly.

**Verification, evidence, and uncertainty.** Confirm each named destination file exists in the corpus and matches its assigned question class. The corpus python-theme listing and both mapped files' declared scopes. Actual misroute frequency into this theme is estimated, not measured.

**Second-order consequence.** This theme and the map it documents fail in the same characteristic way, both are one indirection above the content, so every misroute observed here is also a preview of how readers will misuse the map, the corpus theme doubles as a telemetry source for the bundle router.

**Revision decision.** Route behavior questions to the reliability content via the map itself, activation questions to the entrypoint theme, verification questions to the gates theme, and keep only reading-order and provenance questions here.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use reliability, routing, or quality references when the problem is governance rather than syntax.
Adjacent reference 1: consider the python adjacent reference when the current task pivots away from python reference routing sources.
Adjacent reference 2: consider the reference adjacent reference when the current task pivots away from python reference routing sources.
Adjacent reference 3: consider the routing adjacent reference when the current task pivots away from python reference routing sources.

## Workload Model

**Decision supported.** This section helps decide how much routing and refresh effort one operator or team can sustain without the discipline decaying. The seed model budgets one audience and twelve source documents, an authoring workload, while this theme's operational unit is the routing decision, seconds of overhead applied at the start of every bundle session, plus a rarer refresh cycle when the map is re-verified.

**Recommended default and causal basis.** Route each task at its own start rather than batch-routing a backlog, and budget the refresh cycle to whoever owns the routing card. The map is sized for that economics, 28 lines readable in under a minute, its design assumes it will be consulted hundreds of times and must cost almost nothing each time, while the brief is consulted rarely and can afford its greater length.

**Operating conditions and assumptions.** Tasks arrive with enough boundary information to route, unroutable tasks must be clarified upstream, not force-routed. This model budgets routing and refresh effort for bundle consumers, authoring effort for this corpus document is a separate, rarer workload.

**Failure boundary and alternatives.** The model breaks when routing decisions queue, a batch of twenty heterogeneous tasks routed in one sitting degrades to jump-list skimming by the tenth, routing quality is per-decision attention and cannot be amortized across a batch. Bounded alternatives and recoveries: scheduled weekly refresh regardless of bundle changes, wasteful on quiet weeks but removes the need to watch for change events.

**Counterexample, gotchas, and operational comparison.** The refresh workload hides inside the routing workload, sessions silently absorb staleness costs as confusion time until someone accounts for refresh as its own budgeted activity. Good: each ticket routed at pickup in under a minute with a one-line journal trace. Bad: Friday batch-routing of the week's tickets from memory. Borderline: routing once per epic rather than per task, defensible when all subtasks share one boundary, should be logged as one decision covering many.

**Verification, evidence, and uncertainty.** Track routing traces per task in the journal and watch for batch-routing signatures, many traces with one timestamp. The under-a-minute size of the map and rare-consultation design of the brief read in full. The batch size at which routing attention measurably decays is judgment, not measured.

**Second-order consequence.** Routing is the rare workload whose per-use cost rounds to zero while its skip cost does not, the asymmetry means the budget question is never can we afford to route but only how to keep the near-free habit from being dropped.

**Revision decision.** Restate capacity in routing terms, one routing decision per task at negligible marginal cost, one card refresh per bundle change at minutes of cost, batch routing capped before attention decays.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Python Reference Routing Sources as a documentation reference operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | reference selection, writing, roadmap, or explanation work where the output should improve the next human or agent decision | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one audience, one decision, up to 12 source documents, and one verification checklist per reference use | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Reference Map; Jump Points; Section Search; Use Order; Sources And Research Brief; Primary Sources; Repo-Local Sources; Synthesis Notes | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is canonical source routing rules for stale, duplicate, and conflicting sources | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide what reliability the router and its pointer set must demonstrate to keep their authority. The seed targets measure document properties like label preservation while the router's own reliability question goes unstated, how often does a routed session land on the wrong section, and how often does the map's jump list disagree with the live headings it points at.

**Recommended default and causal basis.** Audit pointer accuracy at every refresh and triage every reported misroute to map defect or routing error before the week closes. A router's worth is its misroute rate and its pointer accuracy, the eight jump points and three commands define the accuracy claim precisely, and every routed session's outcome is observable in the journal trace it leaves.

**Operating conditions and assumptions.** Misroutes get reported at all, which requires journaling norms that treat a logged miss as data rather than fault. Targets here cover router operation and pointer accuracy, content correctness at the destinations is measured by the destination themes.

**Failure boundary and alternatives.** Misroute measurement needs honest miss reporting, an operator who quietly recovers from a wrong section and logs the session as clean erases the signal, low-blame journaling is a precondition for any of these numbers meaning anything. Bounded alternatives and recoveries: sampled re-routing of past sessions by a second operator where live miss reporting is too sparse to trust.

**Counterexample, gotchas, and operational comparison.** Zero reported misroutes over a long window more often means unreported recoveries than perfect routing, corroborate quiet windows with an occasional seeded misroute drill. Good: a triaged miss showing the map lacked a subprocess jump point, fixed in the next revision. Bad: counting a behavior question misrouted into this theme as a router failure, that is corpus routing, not bundle routing. Borderline: counting a veteran's from-memory miss, the map was right and unconsulted, policy decides whether that counts.

**Verification, evidence, and uncertainty.** Review the misroute ledger and the latest pointer-audit results together at each quarterly review. The precisely enumerable pointer set formed by the map's jump points and commands. This repository's baseline misroute rate is unknown until tracking begins.

**Second-order consequence.** The two targets police each other, a clean pointer audit with a rising misroute rate isolates the fault in routing skill, a failing pointer audit excuses the operators, so tracking both turns any failure into an attributable one.

**Revision decision.** Add the operational targets, zero stale jump points at every card refresh, and misroutes triaged within the week to either a map defect or an operator error.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which ways this theme's instruments silently fail and how each failure is caught. The seed table covers drift and traceability generally but not the router-specific decay modes, silent map staleness after destination edits, ritual routing where traces are logged but sections are chosen from memory, and provenance rot as the brief's 2026-06-23 snapshot ages.

**Recommended default and causal basis.** Attach each failure row to its own detection cadence, heading diffs per refresh, route-aloud checks per quarter, citation dating always. Each mode follows from the instruments' construction, the map is a derived index that nothing forces into sync with its destinations, routing traces are self-reported and thus fakeable, and the brief is a point-in-time disclosure that only loses accuracy from its preparation date forward.

**Operating conditions and assumptions.** Someone actually runs the probes, a failure table without assigned probe owners is itself the mode it describes. This table catalogs failures of the routing and provenance instruments, failures in destination content are the destination themes' subject matter.

**Failure boundary and alternatives.** All three modes produce healthy-looking records, a stale map still reads confidently, ritual traces look identical to real ones, and the brief never changes at all, so detection must probe behind the records rather than read them. Bounded alternatives and recoveries: automating the heading diff into a repository check that fails when map and destinations diverge, converting the fastest-moving mode from probed to enforced.

**Counterexample, gotchas, and operational comparison.** Probing routing behavior reads as distrust if framed punitively, frame the route-aloud check as calibrating the map, the same reframe the gates theme uses for its drills. Good: a refresh diff catching a renamed testing heading the day after the destination edit. Bad: quarterly review praising unbroken routing traces no one has ever spot-checked. Borderline: a brief citation without a date in an internal scratch note, tolerable there, never in delivered guidance.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe, its owner, and its cadence, and that the journal shows the probes actually ran. The derived-index and point-in-time construction of the two mapped files. Actual onset times for ritual routing among real operators are unmeasured.

**Second-order consequence.** The three modes decay at different speeds, staleness tracks destination edit frequency, ritual tracks operator tenure, and rot tracks calendar time, so one annual review cannot cover them and each needs its own cadence.

**Revision decision.** Add rows for silent map staleness caught by the heading diff at refresh, ritual routing caught by occasional route-aloud spot checks, and provenance rot caught by dating every brief citation.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| decision path remains implicit | reader cannot tell when to adopt, adapt, or avoid the pattern | add decision table, cost of being wrong, and adjacent-reference route |
| large corpus becomes list | many sources are indexed but not synthesized into ranked guidance | classify canonical, supporting, legacy, duplicate, and conflicting sources |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide how many routing attempts a task gets and what each failed attempt must record. The seed guidance covers document-verification retries but not routing retry policy, what a reader does after landing on a wrong section, how many re-routes a task gets, and when repeated routing failures stop being noise and start indicting the map.

**Recommended default and causal basis.** On a wrong-section landing, take one rg fallback, and if that also misses, stop routing and re-diagnose the task's boundary before touching the bundle again. The map has a built-in retry ladder the seed never names, a failed jump point falls back to the rg section search, a failed search falls back to escalation, and each rung is cheaper than the context dump that tempts frustrated readers.

**Operating conditions and assumptions.** Fallback firings get logged, an unlogged retry ladder starves the metrics loop that would otherwise fix the map. This section governs the routing retry loop, retry policy for code and for corpus generation belongs to the gates and evolution themes respectively.

**Failure boundary and alternatives.** Unbounded re-routing converges on reading everything one section at a time, which is the context dump with extra steps, two failed routes on one task is the signal to stop routing and question whether the task's boundary was misidentified. Bounded alternatives and recoveries: pairing a stuck router with someone who knows the bundle, which collapses the retry loop entirely at the cost of an interruption.

**Counterexample, gotchas, and operational comparison.** Backpressure applies to the map's maintainers too, a burst of fallback firings across operators is a queue of unmet jump points, thinning routing discipline in response treats the symptom and deepens the cause. Good: one jump-point miss, one rg hit, both logged, task proceeds. Bad: five consecutive section opens hunting for subprocess guidance that is not in the bundle. Borderline: skipping straight to rg because the task's phrasing matches no jump point, a legitimate first rung when logged as such.

**Verification, evidence, and uncertainty.** Count routing attempts per task in the journal and confirm escalations fired at the two-re-route cap. The fallback structure connecting the map's jump points and section search read in full. The right escalation cap may differ between human and agent operators.

**Second-order consequence.** Each rung of the ladder failing carries diagnostic weight the retry itself does not, a jump-point miss suggests map staleness, an rg miss suggests the content genuinely is not there, and a boundary re-diagnosis suggests the task was never routable as stated.

**Revision decision.** Add the routing retry ladder, jump point, then rg search, then boundary re-diagnosis, capped at two re-routes before escalating, with every fallback firing logged for the metrics loop.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide what evidence each routed session must produce for the surrounding loops to function. The seed checklist logs document-usage evidence but not the routing record schema, what a routed session must leave behind for the misroute ledger, the fallback tally, and the ritual-routing spot checks to consume.

**Recommended default and causal basis.** Emit the full routing record for every bundle session, misses and fallbacks included, and store it where the quarterly tally can reach it. The metrics, reliability, and failure sections above all read from session records, so the schema is the load-bearing choice, the boundary diagnosed, the jump point or fallback used, the sections actually opened, and the closing gate-anchor confirmation.

**Operating conditions and assumptions.** Record emission stays cheap, one journal line per session, heavier schemas will be abandoned within weeks. This section specifies observability of routing operation, observability of the destination content's use belongs to the destination themes.

**Failure boundary and alternatives.** Records that omit the sections-actually-opened field cannot distinguish disciplined routing from ritual traces, the opened-set is the only field that cross-checks the claimed route against observable behavior. Bounded alternatives and recoveries: wrapper tooling that emits the record as a side effect of opening bundle files, complete capture at the cost of building and trusting the wrapper.

**Counterexample, gotchas, and operational comparison.** For agent sessions the record can be derived from the context log automatically, but deriving human records from memory at day's end produces fiction, humans must log at routing time. Good: a one-line record naming the async boundary, jump point three, two sections opened, stack confirmed. Bad: a week of sessions summarized as used the bundle as needed. Borderline: recording only non-obvious routings, compact but erases the base-rate data the misroute ledger needs.

**Verification, evidence, and uncertainty.** Sample recent records and check every field of the minimal schema is present and consistent with itself. The consumption needs established by this document's tally, misroute, and spot-check loops. The record weight at which operators start abbreviating is unknown.

**Second-order consequence.** With sections-opened captured, ritual routing becomes computable rather than interrogable, any record whose opened-set disagrees with its claimed route flags itself, no route-aloud session required.

**Revision decision.** Define the minimal routing record, task identifier, diagnosed boundary, device used with jump point or fallback named, sections opened, gate-anchor confirmation, operator, and date.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture reviewer decision, unresolved uncertainty, and next refresh trigger.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide how to prove the routing operation is fast and frugal without rewarding wrong-but-quick routing. The seed method demands a ten-minute next-action ceiling for the document while the routing operation's performance question is sharper, seconds from task to section shortlist, and whether routing overhead stays small enough that nobody is tempted to skip it.

**Recommended default and causal basis.** Log route time and footprint per session, review monthly, and treat footprint regressions as urgent even when route times look excellent. The map's entire justification is a time trade, under a minute of routing against the minutes or context-window cost of unrouted reading, so measuring both sides of that trade is measuring whether the instrument works at all.

**Operating conditions and assumptions.** Timestamps and opened-sets are captured in the routing record, which the observability schema already requires. This method verifies the routing operation's performance, performance of the destination guidance in running code belongs to the reliability theme's own verification methods.

**Failure boundary and alternatives.** Optimizing routing latency alone invites hollow speed, a three-second route that lands wrong costs more than a ninety-second route that lands right, latency numbers are only interpretable beside the misroute ledger. Bounded alternatives and recoveries: periodic timed routing drills on standard tasks instead of continuous measurement, coarser but immune to inconsistent live logging.

**Counterexample, gotchas, and operational comparison.** Averages hide the tail here as everywhere, one lost session that opened everything disappears into a monthly mean, track the worst footprint of the month as its own number. Good: monthly review showing median footprint of one section and route times under a minute. Bad: celebrating three-second routes while footprints creep toward four sections. Borderline: excluding first-contact routings from the stats, defensible since learning is slow, should be reported separately rather than dropped.

**Verification, evidence, and uncertainty.** Inspect the monthly footprint and route-time distributions beside the misroute ledger before drawing any conclusion. The time-trade justification visible in the map's route-quickly framing read in full. Acceptable route-time ceilings vary by task urgency and are a local policy choice.

**Second-order consequence.** Reading footprint is the more honest metric of the two, route time rewards haste but footprint directly prices the context dump, a footprint of one is the router working, a footprint of eight is the failure the map exists to prevent regardless of how fast it happened.

**Revision decision.** Measure route time from task pickup to section shortlist and reading footprint as sections opened versus sections the map named, reading both beside the misroute rate.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require the reader to find the correct next action in 10 minutes or less during review sampling.
Leading indicator to measure: the next agent can modify the package without weakening type, lint, or fixture coverage.
Failure signal to watch: the reference says quality matters but gives no migration path from loose scripts to strict package code.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what bundle size or revision cadence this theme's two instruments must change shape rather than merely grow. The seed statement bounds document scale but not the router's scaling seams, one map serving one bundle, a jump list that stays legible only while it stays short, and a single ledger whose preparation date ages for everyone at once.

**Recommended default and causal basis.** Within one bundle of a few references apply both instruments as written, and split or hierarchize the moment either the jump list or the revision cadence outgrows single-glance, single-date operation. The map works because eight destinations fit in one glance and 28 lines cost nothing to consult, both properties are functions of bundle size, and the brief works because one preparation event covered the whole bundle, a property that breaks the first time references are revised independently.

**Operating conditions and assumptions.** Bundle growth is observable, someone notices when the ninth reference file lands and asks whether the flat map still serves. This statement marks where this theme's guidance stops, corpus-wide routing across many bundles is the corpus's own indexing problem, not this file's.

**Failure boundary and alternatives.** Scaling by growing the jump list defeats the instrument, a forty-entry jump list is a table of contents that must itself be searched, past some size the flat list must become a routed hierarchy or split into per-area maps. Bounded alternatives and recoveries: generated maps built from destination headings on demand, which scale mechanically but lose the curation that makes eight hand-chosen jump points better than forty extracted ones.

**Counterexample, gotchas, and operational comparison.** The map scales worse for agents than humans in one respect, humans skim an oversized list tolerably while an agent given forty jump points may match several and open them all. Good: splitting into per-area maps when the bundle reaches its tenth reference. Bad: appending jump points until the map needs its own map. Borderline: one shared ledger with per-file date columns, a workable middle state between one date and per-file briefs.

**Verification, evidence, and uncertainty.** Check jump-list length and per-file revision dates at each refresh against the stated seams. The single-glance and single-date construction of the two mapped files. The exact jump-list length where glanceability fails is an estimate, not a measurement.

**Second-order consequence.** The brief's single preparation date is the sharpest scale ceiling of the three, one stale reference poisons the whole ledger's implied freshness, so per-file dating is not an enhancement but the precondition for the bundle ever being revised piecemeal.

**Revision decision.** Add the seams, jump lists beyond roughly a dozen entries need hierarchy, independently-revised references need per-file provenance dates, and multi-bundle estates need a router of routers this file does not provide.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Python Reference Routing Sources stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which searches and probes reveal that this theme's pointers or pedigree have gone stale. The seed queries recycle the theme name, but this theme's staleness lives in two specific places, the map's pointers against the live destination headings, and the brief's sixteen links against what those pages now say.

**Recommended default and causal basis.** Run the heading diff at every refresh and fetch the tool-doc subset of the brief's links on a slower cadence, dating every fetch. The theme's claims are pointer claims and pedigree claims, so its refresh probes are diffs and fetches, rerun the heading enumeration against the jump list, and fetch the volatile minority of the brief's links, the tool-doc pages, to see whether the bundle's declared basis has drifted.

**Operating conditions and assumptions.** Refresh effort stays bounded, probes are ordered by expected drift so the budget goes to the volatile links first. These probes refresh this file's routing and provenance claims, refreshing the destination content's technical claims belongs to the reliability and gates themes.

**Failure boundary and alternatives.** Theme-name searches return this corpus's own files and generic Python listicles, neither can reveal a renamed section heading or a rewritten Ruff formatter page, the two failures that actually age this theme. Bounded alternatives and recoveries: watching upstream python-coder-01 for a newer bundle revision, if the authors ship an updated brief, diffing it beats re-fetching sixteen pages independently.

**Counterexample, gotchas, and operational comparison.** A fetched page that moved to a new URL looks like deletion, chase redirects and site reorganizations before recording a brief link as dead. Good: a probe finding the Ruff formatter page restructured, and the brief citation gains a caveat dated to the fetch. Bad: logging that the theme-name query returned nothing new. Borderline: probing PEP 8 for changes, nearly pointless given PEP stability, harmless on a long cadence.

**Verification, evidence, and uncertainty.** Record each probe with its date and the specific pointer or link it confirmed or invalidated. The pointer-and-pedigree composition of this theme's claims across both mapped files. How often the destination references will be revised, and thus how often the heading diff fires, is unpredictable.

**Second-order consequence.** The brief's link list orders its own refresh queue by construction, PEP and stdlib links are near-permanent, packaging-guide links drift slowly, and tool-doc links drift fastest, so a refresh that fetches only the five tool pages covers most of the realistic staleness for a fraction of the fetch cost.

**Revision decision.** Replace the name-based trio with targeted probes, the map-versus-headings diff each refresh, tool-doc link fetches ordered by volatility, and a watch on the bundle itself for upstream revisions.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | python reference routing sources official documentation best practices |
| `github_repository_query_phrase` | python reference routing sources GitHub repository examples |
| `release_notes_query_phrase` | python reference routing sources changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide which of this document's statements may travel elsewhere and under what label. The seed notes define the three labels but omit this evolution's concrete ledger, two mapped files totaling under seventy lines read fully, three sibling bundle files read fully during this corpus effort and cited by name where used, zero external URLs retrieved, and every operational policy above marked as inference.

**Recommended default and causal basis.** When reusing a claim from this file, ask first whether the count audit could verify it, citing the source heading if yes and carrying the reasoning along if no. The routing card, retry ladder, tally loop, record schema, and scale seams in this document extend the mapped files rather than restate them, honest labeling requires separating transcribed instrument content from this evolution's constructed operating machinery.

**Operating conditions and assumptions.** Both mapped files stay at their archived paths so fact-class claims remain mechanically re-checkable. These notes govern reuse of this document's claims, the transcribed devices travel freely with citations, the constructed machinery travels only with its reasoning attached.

**Failure boundary and alternatives.** The highest-risk mislabel here is presenting the retry ladder or the refresh cadences as bundle facts, the map names a fallback command but prescribes no cap, no cadence, and no ledger, all of that is inference built on its structure. Bounded alternatives and recoveries: inline per-claim labels throughout the document if the section-level split proves too coarse for downstream disputes.

**Counterexample, gotchas, and operational comparison.** The brief's own content is disclosure, not verification, quoting it accurately is a local fact about a list of links, and no accuracy of quotation upgrades those links into retrieved evidence. Good: reusing the three-step use order with its map heading cited. Bad: citing the two-re-route escalation cap as a python-coder-01 rule. Borderline: reusing the volatility ordering of the brief's links, derived from link-type reasoning, the derivation should travel with it.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each falls cleanly on one side of the published split. This assignment's read ledger, both mapped files read in full, zero retrievals. Readers cannot re-verify the reading itself, only the citations it produced.

**Second-order consequence.** The fact-inference split follows the countability line exactly as it did in the gates theme, everything the completeness audit can count is fact, everything it cannot count is this evolution's machinery, one boundary again serves both the audit and the label.

**Revision decision.** Publish the split explicitly, jump points, rg commands, use-order steps, link lists, and synthesis notes are local corpus facts, ladders, caps, cadences, cards, and seams are combined-evidence inference.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
