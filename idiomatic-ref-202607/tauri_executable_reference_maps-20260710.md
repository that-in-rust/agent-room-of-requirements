# Tauri Executable Reference Maps

**Decision supported.** This section helps decide whether to spend the first minutes of a Tauri task on routing rather than diving into the nearest familiar reference. The seed title names a maps theme without stating that its subject is routing discipline, choosing which Tauri reference to read, in what order, for which work mode, before spending any reading budget.

**Recommended default and causal basis.** Consult the reference map before opening any heavier Tauri document, select a work mode, then follow its task-type read order. Reference corpora fail at the routing step, since an agent that opens the wrong document reads diligently and still plans the wrong boundary.

**Operating conditions and assumptions.** The task can be classified into one of the five work modes, spec, app architecture, desktop security, lifecycle, or review, and the routed files are reachable. This reference governs which Tauri document to read and when, not the content of those documents, and it presumes the routed files still exist at their mapped paths.

**Failure boundary and alternatives.** Routing advice cannot help when the corpus lacks a document for the task at hand, and pretending a route exists is worse than naming the gap. Bounded alternatives and recoveries: full linear reading for unfamiliar corpora, heading search via the rg commands the map itself provides, or asking the user when the task fits no mode.

**Counterexample, gotchas, and operational comparison.** Agents skip the map under time pressure and open the largest file by habit, and mode labels drift from the destination documents they promise. Good: classifying a permissions task as desktop security mode and reading doctrine then conventions. Bad: opening the playbook for a tray bug because it is the best-known file. Borderline: reading two modes' orders in parallel for a mixed spec-plus-security task while logging the mix.

**Verification, evidence, and uncertainty.** Walk one task per work mode through the map and confirm each routed file exists and its named sections answer the task. The local reference map literally defines the five work modes, eight task-type read orders, a default order for large tasks, and heading-search commands. How much routing time is worth paying on very small tasks is unquantified, so the map's own cost threshold remains judgment.

**Second-order consequence.** A routing map is the cheapest document in the corpus to maintain and the most expensive to get wrong, because every misroute multiplies into a misread of heavier files.

**Revision decision.** State up front that the theme is read-order routing across the Tauri specs playbook, doctrine, and conventions files keyed by work mode and task type.

**Retained seed evidence.** The exact theme title and its framing as reference maps for Tauri executables remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide which recorded source may back which category of routing claim in this reference. The seed map lists three local paths and three public URLs under uniform fact labels without noting that two of the local files are byte-identical duplicates or that no external retrieval is recorded.

**Recommended default and causal basis.** Treat the codex-skills reference map as the routing source, the merged specs reference as its single content companion, and the external URLs as unrefreshed candidates. Provenance rows steer trust, and a duplicate pair presented as two independent sources manufactures false corroboration for every claim they share.

**Operating conditions and assumptions.** Each row names a resolvable path or URL, an artifact role, and a confidence label a reader can act on. The map assigns provenance for this document's claims only and does not rank the sources or promise the archive contains no further Tauri routing material.

**Failure boundary and alternatives.** The table cannot certify freshness, since a recorded path proves existence at mapping time, not that content still matches what Tauri 2 currently ships. Bounded alternatives and recoveries: collapse the duplicate rows into one with a mirrors note, add a retrieval-date column for external rows, or hash local sources to detect drift.

**Counterexample, gotchas, and operational comparison.** Duplicate rows silently double-weight shared claims, and URL rows rot as documentation sites restructure. Good: citing the reference map for the five-mode table it literally contains. Bad: citing the duplicate pair as two agreeing sources. Borderline: citing v2.tauri.app for capability vocabulary while labeling the citation unrefreshed.

**Verification, evidence, and uncertainty.** Diff the two specs-reference paths to confirm identity, open the reference map to match claimed headings, and record which external row any statement actually depends on. A byte-level diff run during this evolution confirmed the idiomatic-references and unclassified-yet copies are identical at 214 lines each. Whether the three public URLs still agree with the local material is unknown here because none was retrieved during this pass.

**Second-order consequence.** Counting sources without deduplicating them inflates apparent evidence, so an honest map must state identity relations between its own rows.

**Revision decision.** Record that the legacy and unclassified references are identical duplicates and that the three external rows carry no retrieval date.

**Retained seed evidence.** All six source rows with their category, confidence, and synthesis-role columns remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202604/tauri-executable-specs-01/references/reference-map.md | local_corpus_source_material | local_corpus_sourced_fact | reference detail file for deeper pattern evidence |
| agents-used-monthly-archive/idiomatic-references-202604/tauri-executable-specs-01/tauri-executable-specs-reference.md | local_corpus_source_material | local_corpus_sourced_fact | historical idiomatic pattern corpus |
| unclassified-yet/Tauri Executable Specifications Reference.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| https://v2.tauri.app/ | external_research_source_material | external_research_sourced_fact | primary Tauri 2 application and security documentation |
| https://github.com/tauri-apps/tauri | external_research_source_material | external_research_sourced_fact | desktop runtime implementation and release surface |
| https://github.com/rust-lang/api-guidelines | external_research_source_material | external_research_sourced_fact | Rust-facing API and error design reference |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which routing practices deserve default adoption when a Tauri task arrives. The seed scoreboard scores three generic corpus-hygiene patterns at 95, 91, and 88 and never ranks the routing practices this theme actually teaches, mode selection, task-type read orders, and bounded reading budgets.

**Recommended default and causal basis.** Rank work-mode classification first, task-type read order second, and default-order fallback third, ahead of the generic hygiene rows. Rankings direct scarce attention, and a scoreboard silent about mode-first routing lets agents optimize citation hygiene while opening files in arbitrary order.

**Operating conditions and assumptions.** Each row names an observable failure the pattern prevents and a tier that separates default adoption from situational use. The scoreboard ranks planning patterns for this routing theme and does not grade Tauri code patterns, which the doctrine and playbook references own.

**Failure boundary and alternatives.** Numeric scores carry no measurement basis in this corpus, so treating them as evidence rather than editorial priority misleads reviewers. Bounded alternatives and recoveries: replace scores with a prevented-failure severity column, order rows by observed misroute frequency once history exists, or collapse tiers to a default flag.

**Counterexample, gotchas, and operational comparison.** Near-tied scores give no ordering signal, and unsourced numbers acquire authority through repetition. Good: adopting mode-first routing after misroutes wasted two sessions. Bad: quoting the 95 score as proof in review. Borderline: keeping numeric scores while documenting they encode editorial priority only.

**Verification, evidence, and uncertainty.** Trace each scoreboard row to the section that operationalizes it and check top rows match failures the failure-mode table lists. The local reference map's own structure, mode table first, task orders second, default order third, encodes the priority this revision makes explicit. No misroute frequency data exists in this corpus, so all rankings await calibration from real routing history.

**Second-order consequence.** A scoreboard that omits its own theme's core practice teaches readers the theme is about something else.

**Revision decision.** Add mode-selection, ordered-reading, and read-budget rows above the hygiene rows and label every score editorial.

**Retained seed evidence.** All three scored rows with their tier labels and failure-prevention targets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `tauri_executable_reference_maps` | 95 | default_adoption_pattern_tier | Source Map First: Load local tauri executable material before synthesizing reference maps recommendations. |
| `tauri_executable_reference_maps` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `tauri_executable_reference_maps` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what single orienting claim should govern an agent's first move on any Tauri task. The seed thesis counts three local source paths and repeats the generic load-local-first formula without stating the theme's actual claim, that reading order is a designed artifact keyed to work mode, not a habit.

**Recommended default and causal basis.** State the thesis as mode classification before reading, ordered reading before synthesis, and heading search when no order fits. The thesis is the sentence agents act on when they read nothing else, and a retrieval-order slogan orients them toward loading files rather than classifying the task first.

**Operating conditions and assumptions.** The three evidence-boundary labels stay attached so local fact, external fact, and inference remain distinguishable in reuse. The thesis governs how this reference is used and does not summarize the routed documents' content or replace opening them.

**Failure boundary and alternatives.** A routing thesis is wrong for corpora without mode structure, where linear reading or search is the only honest strategy. Bounded alternatives and recoveries: phrase the thesis as a falsifiable claim about misroute cost, split it per evidence label, or adopt the reference map's own opening sentence verbatim.

**Counterexample, gotchas, and operational comparison.** Synthesis statements get copied into prompts and outlive their sources, and unlabeled clauses read as fact downstream. Good: an agent quoting the thesis to justify spending five minutes on mode classification. Bad: quoting it as proof the ecosystem endorses this routing scheme. Borderline: paraphrasing the thesis in a prompt while keeping its labels.

**Verification, evidence, and uncertainty.** Check each thesis clause carries an evidence label and that the local clause matches the reference map's use-this-file-first instruction. The reference map opens with use this file first to route the request before loading heavier Tauri references, which is the thesis in source form. Whether the wider ecosystem endorses mode-keyed routing is unverified, so the combined clause stays inference.

**Second-order consequence.** Encoding read order as data makes reading auditable, since a journal can record which order was followed and where it broke down.

**Revision decision.** Restate the combined inference as classify the task into a work mode first, then read only the files and sections that mode's order names.

**Retained seed evidence.** All three labeled thesis lines, local fact, external fact, and combined inference, remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `tauri_executable_reference_maps` contains 3 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Tauri Executable Reference Maps comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide which local file, and which part of it, an agent must open when this reference's summaries are not detailed enough. The seed map lists three local rows with truncated heading signals and generic usage roles, without saying the two specs-reference rows are the same document or which of the map's four numbered parts answers which routing need.

**Recommended default and causal basis.** Route mode questions to the map's mode table, ordering questions to its task-type section, fallback questions to the default order, and discovery to the heading-search commands. A source map earns its keep by resolving questions to subsections, and the reference map's four parts, mode table, task orders, default order, heading search, serve distinct moments.

**Operating conditions and assumptions.** Each row records exact path, heading signals, and a usage role that says when summary reading must give way to full reading. The map covers this theme's local evidence only and does not claim the archive holds no other Tauri routing material.

**Failure boundary and alternatives.** Path-based trust breaks silently when archive folders are reorganized, and a truncated signal list hides sections the reader needed. Bounded alternatives and recoveries: expand rows into a per-section routing table, add content hashes to detect divergence between mirrors, or record sibling files in the same skill directory as secondary context.

**Counterexample, gotchas, and operational comparison.** Mirrored files drift independently once edited, and heading signals cut mid-word invite wrong guesses about coverage. Good: opening the map's task-type section for a sidecar read order. Bad: reading both mirrored specs references as if they might differ. Borderline: citing the merged reference's TDD loop from memory while flagging exact wording needs the file.

**Verification, evidence, and uncertainty.** Open each mapped path, confirm its table of contents matches the recorded signals, and walk one routing question to the map part that answers it. All three mapped paths were opened during this evolution and the two specs references were confirmed byte-identical. Whether adjacent archive directories hold further Tauri routing material is unexplored, so the map describes what was mapped, not the whole corpus.

**Second-order consequence.** An index that stops at file level pushes the lookup cost onto every future reader at their moment of least patience.

**Revision decision.** Add per-part routing guidance for the reference map and mark the specs-reference rows as one document mirrored at two paths.

**Retained seed evidence.** All three local source rows with their paths, titles, heading signals, and usage roles remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202604/tauri-executable-specs-01/references/reference-map.md | Reference Map | Reference Map; 1) Choose the Work Mode; 2) Choose by Task Type; Commands and Frontend IPC; Shared State and Rust Core; Capabilities, Permissions, and Filesystem or Plugin Access | reference detail file for deeper pattern evidence |
| agents-used-monthly-archive/idiomatic-references-202604/tauri-executable-specs-01/tauri-executable-specs-reference.md | Tauri Executable Specs Reference | Tauri Executable Specs Reference; 1) Work Modes; 2) Choose by Task Type; Commands and Frontend IPC; Shared State and Rust Core; Capabilities, Permissions, and Filesystem or Plugin Access | historical idiomatic pattern corpus |
| unclassified-yet/Tauri Executable Specifications Reference.md | Tauri Executable Specs Reference | Tauri Executable Specs Reference; 1) Work Modes; 2) Choose by Task Type; Commands and Frontend IPC; Shared State and Rust Core; Capabilities, Permissions, and Filesystem or Plugin Access | local agent-usable source material |

## External Research Source Map

**Decision supported.** This section helps decide which public source to consult, and for what, when local routing evidence is stale or silent. The seed map lists the same three URLs as every Tauri theme with usage roles but no retrieval record, no version anchor, and no statement of which routing claims each could confirm.

**Recommended default and causal basis.** Consult v2.tauri.app for current capability and IPC vocabulary, the tauri repository for release signals, and the Rust API guidelines for interface conventions, recording date and version each time. External sources date the local corpus, and routing advice keyed to Tauri 2 structure goes stale when the framework reorganizes its documentation or security model.

**Operating conditions and assumptions.** Each row keeps its evidence-boundary label so external facts stay distinguishable from local routing facts in reuse. The map directs freshness checks and cannot substitute for the local reference map, which encodes corpus-specific read orders no public page knows.

**Failure boundary and alternatives.** A URL cited for a claim it never contained poisons downstream trust faster than an admitted gap. Bounded alternatives and recoveries: pin rows to versioned documentation paths, add Tauri release notes as an explicit row, or drive refresh through the future-queries section instead of ad hoc search.

**Counterexample, gotchas, and operational comparison.** Documentation sites restructure and break deep links, and repository READMEs lag released behavior. Good: checking current capability terminology on v2.tauri.app before renaming a mode, with the date logged. Bad: citing this map as proof the ecosystem endorses these read orders. Borderline: borrowing a naming convention from the API guidelines with an unrefreshed label.

**Verification, evidence, and uncertainty.** Resolve each URL, record what was read and when, and note one local claim it confirmed, contradicted, or ignored. The three mapped URLs are the authoritative public surfaces for Tauri semantics, releases, and Rust interface conventions respectively. None of the three sources was retrieved during this evolution, so their current content remains unverified.

**Second-order consequence.** External references are clocks as much as sources, and an undated citation cannot even tell the time.

**Revision decision.** Add retrieval-status expectations and state that these sources can refresh Tauri vocabulary and structure but not this corpus's internal routing.

**Retained seed evidence.** All three external rows with their names, usage roles, and evidence-boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://v2.tauri.app/ | Tauri documentation | primary Tauri 2 application and security documentation | external_research_sourced_fact |
| https://github.com/tauri-apps/tauri | Tauri repository | desktop runtime implementation and release surface | external_research_sourced_fact |
| https://github.com/rust-lang/api-guidelines | Rust API Guidelines | Rust-facing API and error design reference | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which recurring routing failures deserve standing names, detection signals, and prescribed replacements. The seed registry lists three corpus-hygiene failures and omits the routing failures this theme exists to prevent, skipping the map, reading files in habit order, following a stale route, and mode misclassification.

**Recommended default and causal basis.** Extend the registry with the four routing failures and detect each from journal evidence, which files were opened, in what order, under which claimed mode. Anti-patterns pay off at triage, and a reviewer armed only with hygiene rows will approve a session that read the wrong documents thoroughly.

**Operating conditions and assumptions.** Each row names the failure, the risk that makes it expensive, the safer replacement, and a detection method executable from records alone. The registry covers routing and reading failures for this theme, not Tauri code anti-patterns, which the merged reference's own anti-pattern list owns.

**Failure boundary and alternatives.** Rows too abstract to match a live session log detect nothing, and a replacement pattern that merely negates the failure gives no action. Bounded alternatives and recoveries: order rows by observed frequency once session history accumulates, link rows to checklist items, or fold rare rows into families with shared detection.

**Counterexample, gotchas, and operational comparison.** Two routing failures co-occur and mask each other, and a named registry breeds false comfort that unlisted failures are rare. Good: rejecting a session log that opened doctrine before classifying the mode, citing the habit-order row. Bad: approving it because sources were all cited. Borderline: accepting an undocumented route for a task that genuinely fit no mode, with the gap logged.

**Verification, evidence, and uncertainty.** Apply each detection signal to one real session journal and confirm it fires on a seeded violation. The reference map's own instruction to read it first and follow mode orders names exactly the behaviors these rows enforce. Actual frequency ranking of routing failures in this team's work is unknown, so row order encodes expected rather than observed cost.

**Second-order consequence.** A registry is the file's immune memory, and unencoded failures get rediscovered at the most expensive moment.

**Revision decision.** Add map-skipping, habit-order reading, stale-route following, and mode-misclassification rows each with a detection signal readable from a session journal.

**Retained seed evidence.** All three seed rows, context-free summaries, unsourced claims, and unverified instructions, with their detection methods remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide which checks must pass before this reference and the routing decisions it produces may be called verified. The seed gate names one repository verifier for editing this file and stays silent about how to verify that a routing decision itself was correct or that the routed documents still exist.

**Recommended default and causal basis.** Run the repository verifier after edits, resolve every routed path before relying on the map, and record mode plus order in the session journal. A gate that checks only document form trains agents to treat well-formed markdown as correct routing, while routes silently dangle when files move.

**Operating conditions and assumptions.** Each gate names its command or check, what it protects, and where its result is recorded. Gates verify this document and its routes, not the Tauri work products, whose gates the merged reference's cargo command block defines.

**Failure boundary and alternatives.** No command can prove a mode classification was right, so the gate must pair mechanical checks with a recorded human judgment. Bounded alternatives and recoveries: wire path resolution into CI, generate the route list mechanically from the map, or accept documented manual review when automation is unavailable.

**Counterexample, gotchas, and operational comparison.** Gates run selectively with only passes recorded, and a copied gate command can silently no-op in a differently structured repo. Good: a journal entry naming the mode, the files read in order, and the verifier result. Bad: a green verifier run cited as proof the route was right. Borderline: proceeding past one unresolvable routed path with the gap escalated.

**Verification, evidence, and uncertainty.** Run the verifier, resolve each mapped path, and audit one session journal for recorded mode and order. The merged reference separately defines cargo fmt, clippy, test, and build gates for Tauri work, confirming this section's scope is document and routing verification. How strictly journal-recording should be enforced on small tasks is a cost judgment this corpus has not measured.

**Second-order consequence.** The cheapest high-value gate for a routing document is a link checker, because dangling routes are its dominant decay mode.

**Revision decision.** Add a route-resolution check that every mapped path exists and a journal check that mode, order followed, and deviations were recorded.

**Retained seed evidence.** The repository verifier command and its run-after-editing instruction remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide whether an agent facing a task should open this reference and what it should do inside. The seed guide gives four generic bullets about loading local sources without saying which task shapes need this routing file or that its whole use takes about a minute before heavier reading starts.

**Recommended default and causal basis.** Open this reference at the start of any Tauri task, classify the work mode, follow the matching read order, and leave. Usage guides succeed by matching task shape to action, and this file's action is unusually cheap, classify the mode, read the order, move on.

**Operating conditions and assumptions.** The agent has a concrete task, can name whether spec, architecture, security, lifecycle, or review risk dominates, and can reach the routed files. The guide routes agents into this reference and does not rank it against other corpus references, which the adjacent-routing section owns.

**Failure boundary and alternatives.** The guide misleads when a task fits no mode, where honest output is a recorded gap rather than a forced classification. Bounded alternatives and recoveries: encode the guide as if-then rules keyed to task properties, add a thirty-second triage path, or fold usage guidance into the journey scenario.

**Counterexample, gotchas, and operational comparison.** Generic bullets satisfy checklists without changing behavior, and overloaded agents skip routing exactly when misroutes cost most. Good: a permissions task classified as desktop security mode and routed in under a minute. Bad: an agent reading this file end to end during an incident. Borderline: an agent straddling two modes reading both orders with the straddle logged.

**Verification, evidence, and uncertainty.** Walk one task per mode through the guide and confirm it admits Tauri tasks, routes them correctly, and turns away non-Tauri work. The reference map's opening instruction and five-mode table directly support the classify-then-route usage shape. How often agents follow routing advice under time pressure is unmeasured, so practical compliance is unknown.

**Second-order consequence.** The correct dwell time in a routing document is minutes, and any agent still reading it after that is using it wrong.

**Revision decision.** Add mode-shaped admission criteria and a use-then-leave instruction, this file is a junction, not a destination.

**Retained seed evidence.** The usage trigger sentence and all four seed bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide which concrete situation should anchor a reader's mental model of this reference in use. The seed scenario names a team and a frontend-to-Rust crossing but stops before the routing acts this theme teaches, which mode was chosen, which files were read in which order, and what the ordered reading changed.

**Recommended default and causal basis.** Show the team classifying the task as app architecture mode, reading playbook then doctrine patterns then conventions, and emerging with a typed contract decision. Journeys teach by completeness, and a routing journey must show the junction working, classification, ordered reading, and a decision that differs from what habit-order reading would have produced.

**Operating conditions and assumptions.** The scenario keeps its seed elements, role, starting state, decision, and trigger, so the extension stays anchored to the same persona. The scenario illustrates one representative routing journey and does not claim every task needs the full read order.

**Failure boundary and alternatives.** A journey where routing changes nothing argues the map is decoration, so the scenario must show a consequence. Bounded alternatives and recoveries: add a second mini-journey for a security-mode task, include a mid-journey misclassification that gets corrected, or anchor to a journaled real session once one exists.

**Counterexample, gotchas, and operational comparison.** Scenarios written after templates justify rather than test them, and a frictionless story teaches less than one where a route dead-ends. Good: a journey where reclassifying from lifecycle to security mode redirects reading before code is written. Bad: a journey ending with the team feeling oriented. Borderline: a synthetic journey labeled illustrative pending a logged case.

**Verification, evidence, and uncertainty.** Walk the extended scenario against the reference map's orders step by step and confirm each stage names the file and section it reads. The seed's frontend-action-into-Rust framing matches the commands-and-frontend-IPC task type whose read order the map defines. No journaled real-world run of this journey exists in the corpus, so the scenario remains illustrative.

**Second-order consequence.** An unfinished journey teaches readers to start, and only a finished one shows what a correctly routed session looks like.

**Revision decision.** Extend the journey through mode classification, the followed read order, and the boundary decision the ordered reading produced.

**Retained seed evidence.** The role-based opening, starting state, decision line, and reference opening trigger remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The Tauri desktop app team is starting from a frontend action that must cross into Rust commands and platform permissions and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `tauri_executable_reference_maps` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing the safest command, permission, IPC, packaging, and verification boundary.
Reference opening trigger: Open this file when the task mentions tauri executable reference maps, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide whether to adopt, adapt, or avoid the mode-first routing discipline for a specific task. The seed table keys adopt, adapt, and avoid to generic evidence agreement instead of the properties that govern routing value, task classifiability, corpus familiarity, and reading budget.

**Recommended default and causal basis.** Adopt full mode-first routing for unfamiliar or multi-boundary tasks, adapt by jumping to a known task-type order for routine work, and avoid the map only for single-section lookups. The routing discipline pays when the task classifies cleanly and the corpus is unfamiliar, and it costs when the agent already knows exactly which section it needs.

**Operating conditions and assumptions.** Each row names its triggering task property, the cost accepted, the recovery if wrong, and a verification question answerable from the journal. The guide ranks routing-discipline choices for this theme and cannot authorize skipping the security or review reading the routed documents demand.

**Failure boundary and alternatives.** Avoid becomes the habitual route for unfamiliar corpora, which is precisely where skipping the map costs most. Bounded alternatives and recoveries: run a one-task pilot of full routing before trimming, defer only the conventions file rather than whole orders, or ask the user when mode ambiguity persists.

**Counterexample, gotchas, and operational comparison.** Familiarity is overestimated after a few tasks, and trimmed routes drop exactly the security reading that felt skippable. Good: full routing for a first-ever sidecar task. Bad: skipping the map on an unfamiliar updater task because one doctrine pattern is remembered. Borderline: jumping straight to a task-type order on the tenth similar task, with the shortcut journaled.

**Verification, evidence, and uncertainty.** Answer each row's verification question from session journals and trace one adopt and one avoid decision to its outcome. The seed's four-row adopt-adapt-avoid-cost skeleton with verification prompts provides reusable scaffolding for the rekeyed conditions. True familiarity thresholds where routing stops paying are unmeasured, so the trigger wording remains judgment.

**Second-order consequence.** Routing value scales with uncertainty about where answers live, so the guide's real input is the agent's honest familiarity estimate.

**Revision decision.** Rekey adopt to unfamiliar-corpus or ambiguous-mode tasks, adapt to partial orders for well-known tasks, and avoid to single-lookup errands with the target section already known.

**Retained seed evidence.** The Adopt when, Adapt when, Avoid when, and Cost of being wrong rows with their verification prompts remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the tauri executable reference maps pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong tauri executable reference maps guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide how much authority each local source carries and which copy is authoritative when mirrors exist. The seed hierarchy crowns the reference map canonical and labels the two specs-reference copies legacy and supporting without recording that they are the same document, making the three-row ranking partly illusory.

**Recommended default and causal basis.** Treat the reference map as canonical for routing, the merged specs reference as canonical for content, and note the unclassified copy as a mirror pending cleanup. Hierarchy semantics assume distinct competitors, and ranking a duplicate above or below itself gives readers a decision that does not exist.

**Operating conditions and assumptions.** Each row records path, role, heading signals, and a reviewer question, with the duplicate relation stated beside the roles. The hierarchy classifies retrieval priority within this corpus and does not measure the map's quality against external Tauri practice.

**Failure boundary and alternatives.** Treating the duplicate as corroboration lets one author's merged reference count as two agreeing sources. Bounded alternatives and recoveries: hash both copies to detect future divergence, delete or symlink the mirror in a separate cleanup task, or keep both rows with an explicit identity note.

**Counterexample, gotchas, and operational comparison.** Mirrors drift once either copy is edited, and a supporting label on a duplicate invites readers to look for differences that do not exist. Good: citing the merged reference once with both paths noted. Bad: describing the unclassified copy as independently confirming the legacy one. Borderline: keeping the mirror row for discoverability with a duplicate flag.

**Verification, evidence, and uncertainty.** Diff the mirrored paths, confirm the map and merged reference still match their recorded signals, and check downstream citations name one content source. The byte-identical diff between the legacy and unclassified copies was verified during this evolution. Which copy the repository owner intends to keep long-term is unknown, so mirror cleanup remains a user decision.

**Second-order consequence.** In a corpus with mirrored files the hierarchy's first job is identity resolution, because ranking presupposes knowing how many sources exist.

**Revision decision.** Collapse the duplicate rows into one content source with two recorded locations and rank the true pair, routing map versus merged reference.

**Retained seed evidence.** The classification vocabulary line and all three hierarchy rows with reviewer questions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202604/tauri-executable-specs-01/references/reference-map.md | canonical primary source | Reference Map; 1) Choose the Work Mode; 2) Choose by Task Type | What guidance, warning, or example should this source contribute to Tauri Executable Reference Maps? |
| agents-used-monthly-archive/idiomatic-references-202604/tauri-executable-specs-01/tauri-executable-specs-reference.md | legacy historical source | Tauri Executable Specs Reference; 1) Work Modes; 2) Choose by Task Type | What guidance, warning, or example should this source contribute to Tauri Executable Reference Maps? |
| unclassified-yet/Tauri Executable Specifications Reference.md | supporting context source | Tauri Executable Specs Reference; 1) Work Modes; 2) Choose by Task Type | What guidance, warning, or example should this source contribute to Tauri Executable Reference Maps? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what concrete worked artifact must exist before this reference counts as operational. The seed artifact names a reference freshness workflow with three abstract completion rules and never instantiates the routing artifact this theme implies, a recorded route trace from one real task.

**Recommended default and causal basis.** Complete one route trace for an actual Tauri task and keep it beside this reference as living proof the orders execute. An instantiated route trace forces every part of the map to survive contact with a real task, exposing dangling routes and unusable orders before other agents inherit them.

**Operating conditions and assumptions.** The trace records task identity, mode, ordered files with sections, any deviation with its reason, and the outcome a reviewer can audit. The artifact certifies that the routing map is usable, not that every future task needs a recorded trace, which the tradeoff guide governs.

**Failure boundary and alternatives.** Fields filled with restated definitions instead of task facts certify nothing, and a trace written after the fact decorates a decision already made. Bounded alternatives and recoveries: link to a journaled real session instead of a synthetic trace, require one trace per map revision rather than per task, or accept a labeled dry-run walkthrough when no live task exists.

**Counterexample, gotchas, and operational comparison.** A polished trace can hide that its task never completed, and goal statements drift once template vocabulary takes over. Good: a trace for a capability task showing security mode, three files read in order, and a scoped permission decision. Bad: a table whose goal reads apply reference maps. Borderline: a simulated trace marked unexecuted but structurally complete.

**Verification, evidence, and uncertainty.** Check each trace field against the task record and confirm the files named actually exist and were readable in the stated order. The seed's three completion rules map onto the goal, boundary, and gate stages a route trace naturally records. How often the trace must be refreshed as the corpus reorganizes is unmeasured maintenance judgment.

**Second-order consequence.** Routing maps rot invisibly, and only a trace pushed through a real task can certify the junction still connects to its destinations.

**Revision decision.** Extend the three-field skeleton into a route-trace record, task, chosen mode, files read in order, deviations, and the boundary decision produced.

**Retained seed evidence.** The worked-example definition line and the three artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: reference freshness workflow with canonical source policy and update checklist.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Tauri Executable Reference Maps | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which uses of this reference should be taught as exemplary, negligent, or conditionally acceptable. The seed examples restate generic load-canon and thin-evidence verdicts without showing a single mode choice, read order, or routing consequence.

**Recommended default and causal basis.** Anchor the good case in a correct mode call, the bad case in habit-order reading that missed security guidance, and the borderline case in a genuine two-mode straddle. Examples transfer judgment only when they show action, signal, and consequence together, and routing judgment lives in classification calls under ambiguity.

**Operating conditions and assumptions.** Each example names the task shape, the routing action taken, the observable outcome, and the section whose rule the verdict applies. The examples teach routing judgment for this theme and do not certify the Tauri decisions made after arrival at the routed documents.

**Failure boundary and alternatives.** A strawman bad case no agent would produce teaches nothing, and a good case celebrating compliance rather than outcome rewards ritual. Bounded alternatives and recoveries: draw cases from journaled sessions as they accumulate, add a second borderline for no-mode-fits tasks, or pair each verdict with the registry row it illustrates.

**Counterexample, gotchas, and operational comparison.** Examples copied into prompts shed the context that made them borderline, and verdicts can encode taste rather than consequence. Good: classifying an updater task as security-plus-lifecycle and reading both orders. Bad: opening doctrine at random for a spec-mode task and coding from one pattern. Borderline: proceeding without a mode for a task that fits none, with the gap escalated.

**Verification, evidence, and uncertainty.** Trace each example to a reproducible scenario and confirm the bad case would be caught by the registry's detection signals. The reference map's mode table and per-task orders provide ready-made material for grounded routing examples. Which borderline situations occur most often in this team's work is unknown, so the chosen cases reflect judgment about likely ambiguity.

**Second-order consequence.** An example set is the reference's practice field, and agents meet their first ambiguous task untrained if nothing in it could really happen.

**Revision decision.** Rewrite each verdict around a concrete classification event, the order followed or skipped, and what the routing choice then caught or missed.

**Retained seed evidence.** The good, bad, and borderline example lines with their original verdict framing remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Tauri Executable Reference Maps after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Tauri Executable Reference Maps as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Tauri Executable Reference Maps only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which observable signals should trigger revising this reference or the routing discipline it encodes. The seed metrics name a cross-layer testability indicator and a permissions failure signal, both inherited from the content themes, and measure nothing about routing quality, misroute rate, reading efficiency, or route freshness.

**Recommended default and causal basis.** Measure mode-classification corrections, dangling-route discoveries, and reading time saved per journaled session, reviewing at map-revision boundaries. A routing document improves outcomes through fewer misreads, so its health metrics must observe routing events, not the downstream code properties other references own.

**Operating conditions and assumptions.** Each metric states its unit, collection point, healthy range, breach action, and owning reviewer. These metrics evaluate this reference's routing value, not application quality or team velocity, which need their own instruments.

**Failure boundary and alternatives.** Indicators averaged across unlike tasks hide the misroutes, and a signal that fires only after release measures too late. Bounded alternatives and recoveries: sample retrospectives instead of continuous counters, tie review to corpus reorganizations rather than a clock, or pool signals across the Tauri reference family.

**Counterexample, gotchas, and operational comparison.** Reading-time metrics are gamed by skipping necessary reading, and a green verifier says the document is well-formed, not that its routes are right. Good: reopening the mode table after two sessions misclassified security tasks as lifecycle. Bad: declaring routing healthy because the verifier stayed green. Borderline: skipping a scheduled review after a quarter with no Tauri work, recording the skip.

**Verification, evidence, and uncertainty.** Inspect session journals for recorded mode corrections and dangling routes, and confirm each breach maps to a committed revision. The seed's review cadence line already ties re-verification to edits and external releases, which the routing metrics extend. Healthy misroute and correction rates are unmeasured for this team, so initial thresholds are provisional.

**Second-order consequence.** The natural failure signal for a map is a dangling route, and it is also the cheapest to automate.

**Revision decision.** Add misroute-per-session, route-resolution failures, and time-from-task-to-correct-file as primary signals, each bound to a corrective action on a named section.

**Retained seed evidence.** The leading indicator, failure signal, and verifier-driven review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: the feature can be tested across frontend, Rust command, and packaged desktop behavior.
Failure signal: the reference ignores permissions, IPC risk, or platform-specific verification.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide when this reference may be declared ready to hand to an agent. The seed checklist verifies that generic sections exist and never asks the routing-specific questions, do all routed paths resolve, does every mode have an order, and do the orders name sections that exist in the destinations.

**Recommended default and causal basis.** Keep the structural items and add resolution checks, every mapped path opens, every mode row names a reachable next file, and each order's pattern references exist in doctrine. Existence checks catch missing prose cheaply, but a routing document's expensive failures are dangling destinations and orders referencing renamed patterns, which only resolution checks intercept.

**Operating conditions and assumptions.** Each item names the section it inspects, the evidence that satisfies it, and whether a script or human evaluates it. The checklist gates this document's readiness and does not certify outcomes of sessions that later use it.

**Failure boundary and alternatives.** Items ticked from memory without citations measure the checklist, and a structural pass read as content endorsement hides broken routes. Bounded alternatives and recoveries: generate resolution items mechanically from the map's tables, replace some ticks with paired-reviewer sign-off, or deep-check two random routes per review.

**Counterexample, gotchas, and operational comparison.** Reviewers stop reading carefully once early items pass, and a frozen checklist silently stops covering new obligations as sections evolve. Good: ticking the route item after a script confirmed all mapped paths open. Bad: ticking all items in one glance because headings exist. Borderline: carrying forward last revision's resolution tick with a staleness note.

**Verification, evidence, and uncertainty.** Demand a citation per tick, run the structural verifier, and resolve every route the map names. The seed's seven items map onto sections this file genuinely contains, sound scaffolding for the added resolution layer. How much deep review each item deserves per revision depends on edit volume, so depth remains judgment.

**Second-order consequence.** A checklist that can be completed without resolving a single route measures paper, not the map.

**Revision decision.** Append route-resolution, mode-coverage, and destination-section-existence items, each tick citing the line or command output that satisfies it.

**Retained seed evidence.** All seven structural checklist items covering scenario, decision guide, hierarchy, artifact, examples, metrics, and routing remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Tauri Executable Reference Maps.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide when a task should leave this reference and which neighbor should own it. The seed section splits the theme name into tauri, executable, and reference keywords pointing at three unnamed destinations, an ironic failure inside the corpus's own routing theme.

**Recommended default and causal basis.** Route template and packet questions to the Tauri playbook-templates sibling reference, deep content questions to the merged specs reference, and non-Tauri workflow questions out of this family entirely. Routing matters exactly when this file stops answering, and only a condition-plus-named-destination pair converts that moment into a next action.

**Operating conditions and assumptions.** Each route names its trigger, a destination resolving to a real corpus file, the context carried across, and what this file retains. Routing redirects within this corpus and cannot substitute for the user's prioritization or authorize work in a destination's domain.

**Failure boundary and alternatives.** Keyword overlap routes to files that merely share a word, and a plausible route can hide that the corpus lacks the needed reference. Bounded alternatives and recoveries: consult the corpus index for exact titles before routing, keep the task here with a gap note when nothing fits, or escalate genuine ownership conflicts to the user.

**Counterexample, gotchas, and operational comparison.** Adjacent files evolve independently so routes dangle silently, and two references can both claim a task with neither owning integration. Good: sending a work-packet contract question to the playbook-templates reference while keeping the read-order question here. Bad: routing to the executable adjacent reference, which exists only as a keyword. Borderline: duplicating one shared mode definition locally with a sync note.

**Verification, evidence, and uncertainty.** Resolve every named destination to an actual corpus file and walk one sample task through each trigger. The sibling tauri_executable_playbook_templates reference evolved in this same corpus is a verified, resolvable destination for template questions. The best destination for mixed routing-plus-content tasks depends on emphasis, so some calls remain judgment the agent must record.

**Second-order consequence.** Routing quality is a corpus property, and every unresolved route is evidence of a missing or misnamed reference the owner should fix.

**Revision decision.** Replace keyword stubs with condition-keyed routes, playbook-template questions to the playbook-templates reference, doctrine and policy questions to doctrine references, and spec-content questions to the merged specs reference.

**Retained seed evidence.** The adjacency guidance line and the three keyword-derived route stubs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use executable, doctrine, or legacy Tauri references when the task is about specs, policy, or migration.
Adjacent reference 1: consider the tauri adjacent reference when the current task pivots away from tauri executable reference maps.
Adjacent reference 2: consider the executable adjacent reference when the current task pivots away from tauri executable reference maps.
Adjacent reference 3: consider the reference adjacent reference when the current task pivots away from tauri executable reference maps.

## Workload Model

**Decision supported.** This section helps decide how large a routing-and-reading slice one session can safely carry before discipline degrades. The seed model imports a one-crate, hundred-symbol boundary from generic Rust guidance and truncates its own source-pressure cell, without deriving limits from what bounds routing work, modes straddled and files held in context at once.

**Recommended default and causal basis.** Hold one primary mode plus at most one secondary per session, split tasks that demand three modes, and journal the split boundary. The binding constraint on a routing session is comprehension across ordered documents, since each added mode multiplies the sections an agent must hold while synthesizing.

**Operating conditions and assumptions.** The model tracks modes claimed, files read, pattern sections consulted, and deviations logged for the active slice. The model bounds one routing-and-reading slice and says nothing about the implementation workload the routed documents govern.

**Failure boundary and alternatives.** Symbol counts are honored while a session straddles three modes and six documents, exactly the overload the model should catch. Bounded alternatives and recoveries: measure per-session reading time and set caps empirically, use a work-in-progress limit on unresolved routing questions, or split by mode with a handoff journal between slices.

**Counterexample, gotchas, and operational comparison.** Two trivially related modes cost less than one hidden straddle, and numeric caps invite gaming when judgment matters most. Good: splitting a security-plus-lifecycle-plus-spec task into two sessions at the mode boundary. Bad: accepting a hundred-symbol slice that straddles every mode. Borderline: temporarily reading a third mode's order for triage only, with implementation still gated.

**Verification, evidence, and uncertainty.** Count modes and files per journaled session and compare synthesis errors against slice size to test whether the caps predict degradation. The reference map's default read order for large tasks, map then playbook then relevant doctrine sections then conventions, embodies the bounded-slice discipline this model quantifies. The numeric caps are unmeasured heuristics and true limits vary with corpus familiarity, so stated numbers are provisional aids.

**Second-order consequence.** Routing work scales by junctions taken, not lines written, and a model counting symbols will approve exactly the multi-mode sprawl it should split.

**Revision decision.** Rederive boundaries from modes straddled, files opened, and doctrine patterns consulted per session, and repair the truncated heading-signal cell.

**Retained seed evidence.** The four workload dimensions with their boundary values and verification pressure points remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Tauri Executable Reference Maps as a rust system operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | systems implementation, CLI or service hardening, async/runtime review, and safety-sensitive refactoring where compile success is necessary but not sufficient | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one crate or bounded workspace slice, up to 100 changed symbols, and one integration path that exercises error handling | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Reference Map; 1) Choose the Work Mode; 2) Choose by Task Type; Commands and Frontend IPC; Shared State and Rust Core; Capabilities, Permissions, and  | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is reference freshness workflow with canonical source policy and update checklist | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide what measurable reliability this reference must demonstrate before its guidance is trusted. The seed table promises a 100 percent boundary-preservation rate and 18-of-20 routing accuracy without any sampling procedure, population, or owner in existence.

**Recommended default and causal basis.** Keep the four dimensions but ground each, label structure via the verifier, routing accuracy sampled from journaled sessions, unsupported claims caught at review, and recovery paths walked in audits. Targets create reliability only when someone can run the measurement, and unmeasurable precision teaches readers to treat every number as decoration.

**Operating conditions and assumptions.** Each target names its metric, method, sample population, evaluating owner, and the corrective action a miss triggers. The targets judge this document's routing guidance, not the reliability of Tauri or of applications built with it.

**Failure boundary and alternatives.** Percentages quoted as achievements rather than aspirations manufacture confidence exactly where the document claims rigor. Bounded alternatives and recoveries: start with binary pass-fail audits, pool samples across the Tauri reference family, or replace numeric accuracy with defect review of every miss.

**Counterexample, gotchas, and operational comparison.** A 20-use sample may take months for a niche theme, and reviewers disagree on what counts as a routing miss. Good: auditing five journaled sessions and reopening the mode table after two classification misses. Bad: reporting 18-of-20 accuracy no one measured. Borderline: claiming route resolution from a script run while flagging that mode-correctness is unchecked.

**Verification, evidence, and uncertainty.** Run the verifier for structure, script the route-resolution check, and pull journaled sessions for classification outcomes. The four seed dimensions align with failure classes this corpus has met, label loss, misrouting, unsupported claims, and dead-end guidance. No baseline exists for any target, so current compliance is unknown and the first audit defines achievable thresholds.

**Second-order consequence.** For a routing document the most measurable target is route resolution, which can reach a true 100 percent because a script can check it.

**Revision decision.** Attach a sampling procedure, population, owner, and miss-triggered action to each target and mark all thresholds unbaselined.

**Retained seed evidence.** All four reliability rows with their threshold values and evidence-collection methods remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which failures in this reference's routing fabric deserve standing mitigations. The seed table pairs two corpus-hygiene rows with two runtime rows on contention and panics, none of which describe how a routing document actually fails, dangling routes, stale orders, mode drift, and mirror divergence.

**Recommended default and causal basis.** Keep the seed rows and add the four routing failures, each detected by path resolution, order-versus-destination diffing, or mirror hashing before a session relies on the map. Failure tables pay off at triage, and triaging a routing failure needs rows about routes and orders, ranked by how silently each decays.

**Operating conditions and assumptions.** Each row names its trigger, earliest signal, blast radius, containment, corrective action, and the stage where detection belongs. The table covers this document's routing failures, while runtime failures belong to the doctrine and playbook references the map routes to.

**Failure boundary and alternatives.** Rows too abstract to match a live symptom detect nothing, and detection after a misroute has already wasted a session is too late. Bounded alternatives and recoveries: order rows by observed frequency once history accumulates, link each row to the gate that detects it, or merge overlapping rows into families with shared containment.

**Counterexample, gotchas, and operational comparison.** A stale order still resolves path-wise while naming doctrine patterns that were renumbered, the subtlest decay this table must catch. Good: a pre-session script catching a moved conventions file before reading began. Bad: discovering a renamed doctrine pattern mid-implementation. Borderline: proceeding past one dangling route for an unrelated mode, with the gap logged.

**Verification, evidence, and uncertainty.** Seed one representative failure per row in a drill and confirm the named signal fires at the named stage. The reference map's orders cite doctrine patterns by number, so pattern renumbering in the destination is a concrete, checkable drift surface. Real frequency and cost rankings await accumulated session data, so today's ordering encodes expected rather than observed risk.

**Second-order consequence.** A routing document's failures are all silent by nature, which makes scripted detection worth more here than in any content reference.

**Revision decision.** Add dangling-route, stale-read-order, mode-definition-drift, and mirror-divergence rows with earliest observable signals and detection stages.

**Retained seed evidence.** All four seed failure rows with their triggers and mitigation actions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| runtime contention masks failure | async tasks, locks, or shared state degrade under concurrent load | add contention benchmark, timeout budget, and structured cancellation path |
| panic path reaches production | unwrap or unchecked invariant survives compile and unit tests | require panic audit, Result propagation, and integration failure fixture |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide when a failed routing step should be retried, escalated, or halted. The seed bullets classify failure causes and cap retries sensibly but never say what retry means for routing, reclassify the mode once, re-resolve the route once, then escalate rather than looping.

**Recommended default and causal basis.** Classify the failure once in the journal, retry classification or resolution at most once each, halt ordered reading while a route is red, and resume only after the route records green. Routing retries are cheap and bounded, one reclassification and one re-resolution, so unbounded rerouting signals a corpus gap rather than a transient failure.

**Operating conditions and assumptions.** The guidance assumes recorded journals, routes that resolve or fail unambiguously, and an agent empowered to pause its own reading. This governs retry of routing and reading steps, not retries inside the Tauri work the routed documents plan.

**Failure boundary and alternatives.** A task that fits no mode after one honest reclassification will not fit after five, and looping burns the session budget the map exists to protect. Bounded alternatives and recoveries: escalate immediately for corpus-gap failures instead of consuming the retry, amend the map before rerunning, or hand persistent blockers to a fresh session through the journal.

**Counterexample, gotchas, and operational comparison.** Transient and structural routing failures look identical on first occurrence, and rereading the same map unchanged mostly reproduces the confusion. Good: one reclassification from lifecycle to security mode, journaled, then normal reading. Bad: five silent rereads of the mode table for a task that fits none. Borderline: retrying a route once after a suspected typo in the mapped path, with the suspicion logged.

**Verification, evidence, and uncertainty.** Audit journals for classification-before-retry ordering and confirm no session rerouted more than twice before escalating. The seed's four-way failure classification and checkpoint-before-destructive-step rule carry over directly to routing semantics. The right retry budget for ambiguous-mode tasks is judgment, and one bounded retry is a defensible default rather than a derived optimum.

**Second-order consequence.** Backpressure for a junction means refusing to send traffic down unverified routes, halting reading when resolution fails rather than improvising a path.

**Revision decision.** Add routing-specific retry semantics, one mode reclassification, one route re-resolution, then escalation with the journal attached.

**Retained seed evidence.** All five retry and backpressure bullets, including the one-owner-per-file rule, remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide what evidence each routing session must emit so a later reader can reconstruct its decisions. The seed bullets say record sources, commands, and latencies but never name the routing events worth recording, the mode chosen, the order followed, deviations, and dead ends discovered.

**Recommended default and causal basis.** Journal the mode at classification, each file at open time with its order position, every deviation with its reason, and a completion audit naming unresolved routing gaps. Evidence captured at the routing moment stays accurate and cheap, while end-of-session reconstruction forgets the reclassification that actually shaped the outcome.

**Operating conditions and assumptions.** The checklist assumes a persistent journal, stage names shared with the journey scenario, and reviewers who read completion audits. Observability here serves routing reconstruction and review, not runtime telemetry of shipped desktop apps.

**Failure boundary and alternatives.** Records written only after success hide misroutes, and latency percentiles quoted for reading work nobody timed are theater. Bounded alternatives and recoveries: derive open-order records mechanically from tooling where possible, sample deep audits on a fraction of sessions, or attach evidence to queue completion notes when no journal exists.

**Counterexample, gotchas, and operational comparison.** Recording effort competes with task effort so pressured sessions log least when it matters most, and summaries launder bad routes into clean narratives. Good: a completion audit naming one dangling route discovered and one mode straddle. Bad: p95 latency claims for a reading session with no timer. Borderline: retroactively reconstructing one missing open-order record and labeling it reconstructed.

**Verification, evidence, and uncertainty.** Rebuild one session's routing sequence solely from records, then list what could not be recovered and fix the stage that failed to write it. The journal cadence this corpus already uses for evolution work provides the recording surface these signals need. The minimum record set that keeps sessions reconstructable without drowning reviewers is workload-dependent and must be tuned from audits.

**Second-order consequence.** Whatever the workflow does not capture structurally will be missing on exactly the sessions worth investigating.

**Revision decision.** Bind each recorded signal to a routing stage, classification, resolution, ordered reading, and completion audit naming dead ends found.

**Retained seed evidence.** All six observability bullets including the small-audit-evidence preference remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture error count, timeout count, retry count, saturation signal, and rollback trigger.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide how to prove this reference improves routing outcomes enough to justify its maintenance cost. The seed method demands memory, panic, and concurrency evidence with a 10 ms hot-path bound, importing runtime benchmarks into a routing document whose real performance is time-to-correct-file.

**Recommended default and causal basis.** Define the correct destination per test task first, then compare time-to-arrival and classification accuracy for sessions using versus skipping the map. A routing map performs by shortening the path from task to the right section, so its verification must time and score that path, not operations the document never performs.

**Operating conditions and assumptions.** The method assumes representative tasks, comparable runs or reviewers, journaled outcomes, and prior agreement on the correct destination. This method evaluates the map's usefulness, while runtime performance belongs inside the REQ-TAURI contracts the routed playbook produces.

**Failure boundary and alternatives.** Percentile theater crowds out the harder measurement of whether routing decisions improved, and familiarity makes second runs faster regardless of map quality. Bounded alternatives and recoveries: run structured walkthroughs where a reviewer must locate five answers with citations, A-B test two versions of the mode table, or accept expert review as interim evidence.

**Counterexample, gotchas, and operational comparison.** Comprehension tests measure the reader as much as the document, and pass declared because a reviewer liked the prose proves nothing. Good: timing how quickly a reviewer reaches the sidecar read order from a cold task statement. Bad: reporting p99 latency for reading markdown. Borderline: counting reading round-trips saved in one paired task while flagging the sample of one.

**Verification, evidence, and uncertainty.** Define the answer key, run the with-and-without comparison, score arrival correctness and time, and record the delta with its sample-size caveat. The seed's pass condition, a reviewer identifies the correct next action without reading unrelated files, is already the right criterion for a routing map. Effect sizes from single-reviewer comparisons are unstable, so early performance claims must carry low-confidence labels.

**Second-order consequence.** Measuring a junction with runtime metrics is a category error that leaves its one real metric, arrival accuracy, unmeasured.

**Revision decision.** Recenter measurement on routing quality, time-to-correct-file and mode-accuracy with and without the map, moving runtime bounds to the routed references.

**Retained seed evidence.** The performance method line, both indicator lines, the measurement packet, and the pass and fail conditions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require memory, panic, and concurrency evidence; hot-path operations need p95 under 10 ms locally or an explicit benchmark exception.
Leading indicator to measure: the feature can be tested across frontend, Rust command, and packaged desktop behavior.
Failure signal to watch: the reference ignores permissions, IPC risk, or platform-specific verification.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what scale this reference stops being sufficient and what must supplement it. The seed statement names multi-system spans and SLO-bearing traffic as limits but gives no early indicators that the routing scheme itself is outgrowing five modes and eight task types.

**Recommended default and causal basis.** Watch straddle frequency and unclassifiable-task counts per journal review, and split or add modes before either becomes routine. Routing schemes fail by accretion, since each new task type stretches an existing mode until classifications become coin flips, and leading indicators buy response time hard limits cannot.

**Operating conditions and assumptions.** The boundaries assume one corpus family, a stable five-mode vocabulary, and sessions small enough for single-reviewer comprehension. The boundaries bound this routing reference, not the scaling of applications the routed documents plan.

**Failure boundary and alternatives.** Limits read as targets invite corpus sprawl, and a boundary detected at the boundary has already been crossed. Bounded alternatives and recoveries: shard routing by mode family with one owner per shard, hand off through journals at checkpoints, or downgrade to search-only routing when the mode table no longer fits.

**Counterexample, gotchas, and operational comparison.** Boundaries interact, since a small corpus with many mirrored files can exceed maintenance capacity sooner than a large clean one. Good: adding a distinct plugin-development mode when plugin tasks straddle three orders repeatedly. Bad: stretching lifecycle mode to cover store distribution because no row fits. Borderline: continuing past a straddle-rate warning with doubled journal review.

**Verification, evidence, and uncertainty.** Track approach indicators per journal review and test one planned mode split to confirm the handoff preserved routing coverage. The four seed boundary classes match failure surfaces the workload and failure sections independently identify, giving internal corroboration. Exact thresholds where five modes stop sufficing are unmeasured and corpus-dependent, so the boundaries mark judgment lines.

**Second-order consequence.** The natural scale signal for a router is classification confidence, and falling confidence precedes structural failure by many sessions.

**Revision decision.** Add approach signals, rising mode-straddle rates, task types needing new orders, and destination files splitting, each with a pre-crossing action.

**Retained seed evidence.** All four scale boundary statements including the distributed split and context-drift rules remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Tauri Executable Reference Maps stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which future searches would actually refresh this reference's external evidence. The seed query table pastes the corpus-internal theme name into three generic templates whose literal phrasing would return noise, since tauri executable reference maps is this corpus's coinage.

**Recommended default and causal basis.** Search for Tauri 2 release notes and documentation restructures on a release-driven trigger, feeding the mode table and read orders rather than generic best-practice queries. Refresh queries succeed by matching the vocabulary the ecosystem publishes under, and no external author writes about this corpus's internal routing labels.

**Operating conditions and assumptions.** Each query names the section it refreshes, the source type it targets, and the trigger that should fire it. The queries refresh external evidence for this theme and do not govern refreshing the local map, which changes through repository edits.

**Failure boundary and alternatives.** Literal theme-name queries return nothing, and declaring the refresh complete on empty results converts blindness into false freshness. Bounded alternatives and recoveries: subscribe to Tauri release notes instead of polling, pin queries to the official docs domain, or drive refresh from dated retrieval records in the research map.

**Counterexample, gotchas, and operational comparison.** Search engines paraphrase queries so noise looks relevant, and a query that once worked degrades silently as terminology shifts. Good: a release-triggered query for Tauri capability schema changes feeding the security-mode order. Bad: searching the literal theme title and logging zero results as freshness. Borderline: using a community blog result with an inference label pending official confirmation.

**Verification, evidence, and uncertainty.** Run each query once, record whether top results are on-topic for the section it refreshes, and rewrite noise-dominated queries. The seed's three-query structure targets the right source classes, official docs, repository, and release notes, and needs only vocabulary repair. Which phrasings stay effective as Tauri's documentation evolves is unknowable in advance, so queries need their own review cadence.

**Second-order consequence.** For a routing document the highest-value refresh signal is destination change, renames and restructures in the files and frameworks the routes point at.

**Revision decision.** Rephrase queries in ecosystem vocabulary, Tauri 2 documentation structure changes, capability and permission renames, and doctrine-pattern-relevant release notes, each bound to the section it refreshes.

**Retained seed evidence.** All three labeled query rows with their official-docs, repository, and release-notes targets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | tauri executable reference maps official documentation best practices |
| `github_repository_query_phrase` | tauri executable reference maps GitHub repository examples |
| `release_notes_query_phrase` | tauri executable reference maps changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide how statements in this reference must be labeled so readers know what each claim rests on. The seed notes define the three labels correctly but give no application rules for this file's distinctive evidence situation, mirrored local sources counted separately and external rows never retrieved.

**Recommended default and causal basis.** Label at the claim level, count mirrored files as one local source, split mixed statements into separately labeled clauses, and downgrade undated external claims to inference. Evidence boundaries keep confidence calibrated downstream, and a label scheme silent about duplicates lets one document masquerade as two agreeing local facts.

**Operating conditions and assumptions.** The three labels stay stable across the corpus so tooling and reviewers can parse them, and every synthesis clause names a source it combines. The notes govern labeling within this reference and its reuses, and do not rank source quality, which the hierarchy and research map handle.

**Failure boundary and alternatives.** Labels that live only in a closing section are shed the moment a sentence is quoted into a prompt or packet. Bounded alternatives and recoveries: inline labels as parenthetical tags on load-bearing claims, add the dated-retrieval requirement to the external definition, or generate a claim-to-label index during verification.

**Counterexample, gotchas, and operational comparison.** Labels rot fastest at reuse boundaries, and a correct label on a stale claim still misleads. Good: a routing claim labeled local fact with the reference map cited and the mirror noted. Bad: quoting the thesis as external fact because the map lists URLs. Borderline: carrying a label through a paraphrase with a note that wording changed.

**Verification, evidence, and uncertainty.** Sample ten load-bearing claims, check each carries the correct label, and confirm every external-fact label traces to a recorded retrieval. The three seed definitions match the label vocabulary used throughout this corpus and align with the boundary-preservation reliability target. How well labels survive downstream reuse in prompts and packets is unobserved, so durability remains an assumption pending audit.

**Second-order consequence.** A chain of custody is only as strong as its weakest reuse, not the section where the labels were first defined.

**Revision decision.** Add claim-level application rules, a duplicate-source counting rule, and a retrieval-dated requirement for the external label.

**Retained seed evidence.** All three label definitions, local fact, external fact, and combined inference, remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
