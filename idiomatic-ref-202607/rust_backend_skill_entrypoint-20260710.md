# Rust Backend Skill Entrypoint

**Decision supported.** This section helps decide which entry surface a Rust backend task or question should pass through before any bundle file is opened. The seed title names one entrypoint while the two mapped skills are two different front doors, the delivery skill is the working entrypoint that turns backend requests into decision-complete work packets, and the explainer skill is a meta-entrypoint that only explains why the backend skill exists beside the broader Rust stack.

**Recommended default and causal basis.** Enter through the delivery skill for any actual backend task and through the explainer only when the question is about the skill stack itself. An entrypoint's job is triage before content, the delivery skill's own opening line commits it to spec-first, service-aware, production-biased packets, and its first workflow step is classification, choose the active mode set before planning or coding.

**Operating conditions and assumptions.** The task is a Rust HTTP backend service task, the delivery skill's own guardrail exits generic libraries, CLIs, parsers, and FFI to the broader Rust skills. This reference covers the two SKILL.md entry surfaces, the five bundle reference files behind the delivery door belong to their own corpus themes.

**Failure boundary and alternatives.** Conflating the two doors sends explanation requests into work-packet machinery or backend tasks into plain-English narration, each skill's frontmatter description exists precisely to keep the triggers apart. Bounded alternatives and recoveries: entering the bundle through reference-map.md directly, workable for practitioners who already know their mode, it skips the requirement-writing and output-contract steps only the SKILL.md wrapper enforces.

**Counterexample, gotchas, and operational comparison.** The explainer carries stale machine-specific install paths under /Users, a reader treating those as live corpus locations inherits the macOS-path failure this corpus has already catalogued. Good: a vague endpoint request entering Spec Mode plus Delivery Mode via the delivery skill. Bad: answering why does backend work need its own skill by quoting playbook internals. Borderline: a veteran jumping straight to reference-map.md, efficient, the REQ-WEB contract discipline is what gets skipped.

**Verification, evidence, and uncertainty.** For recent backend tasks, check which skill was activated and whether its frontmatter description actually matched the request. Full reads this session of both mapped SKILL.md files and the five-file delivery bundle behind them. How often real dispatch confuses the two doors is unmeasured in this repository.

**Second-order consequence.** The pair encodes a division the corpus rarely makes explicit, skills that do work versus skills that explain why work is organized a certain way, and the explainer's first guardrail, never mention the upstream source material, shows meta-skills need their own evidence discipline.

**Revision decision.** Open with the two-door structure, delivery for doing backend work, explainer for justifying the skill split, and route every use through the right door first.

**Retained seed evidence.** The exact theme title and entrypoint framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide which of the two mapped sources justifies each claim class in this theme. The seed two local rows share one usage role while carrying opposite authority shapes, the delivery skill is executable doctrine whose claims are testable against its own bundle, and the explainer is narrative positioning whose claims are about the skill stack, not about backend engineering.

**Recommended default and causal basis.** Cite the delivery SKILL.md for modes, workflow, contract, and guardrails, the explainer for the scope-split story, and externals as candidates only. Citing them interchangeably blurs claim classes, a mode definition or guardrail must cite the delivery skill, while a scope-split rationale or audience-style rule must cite the explainer, because each is authoritative only over its own genre.

**Operating conditions and assumptions.** Both archive paths remain stable for citation resolution. Provenance for this document's statements only, the four external URLs stayed unretrieved throughout this evolution and confer no current-fact authority.

**Failure boundary and alternatives.** Quoting the explainer for backend technique is the sharper risk, its own guardrails forbid framework detail and upstream mentions, so any technical claim attributed to it is by construction fabricated. Bounded alternatives and recoveries: dropping the explainer row as non-technical, tempting, it would orphan the scope-split rationale that explains why this entrypoint theme exists at all.

**Counterexample, gotchas, and operational comparison.** The explainer's reference-use section cites two absolute paths outside this repository's layout, neither verifiable here, so claims resting on that strategic-research document must be labeled unverifiable rather than local fact. Good: citing the delivery skill for the six-mode list. Bad: citing the explainer for handler design advice its guardrails prohibit it from giving. Borderline: citing the explainer's backend-problem list, legitimate as positioning evidence, not as engineering doctrine.

**Verification, evidence, and uncertainty.** Trace disputed claims to the exact skill file and section that contains them. Full reads this session of both SKILL.md files and their described bundle. Whether the strategic-research document the explainer cites still exists anywhere is unverifiable from this checkout.

**Second-order consequence.** The two skills even sit in different archive families, claude-skills versus codex-skills of the same 202604 month, so this theme is the corpus's clearest evidence that parallel agent ecosystems documented the same backend stack simultaneously.

**Revision decision.** Keep both rows and annotate the genre split, doctrine claims to the delivery row, positioning claims to the explainer row, chained bundle content cited by name.

**Retained seed evidence.** Both local rows and all four external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202604/personal/rust-web-backend-skill-explainer/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202604/rust-web-backend-delivery-01/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| https://github.com/rust-lang/api-guidelines | external_research_source_material | external_research_sourced_fact | Rust library-team API design recommendations |
| https://github.com/tokio-rs/tokio | external_research_source_material | external_research_sourced_fact | async runtime implementation and operational model |
| https://github.com/tokio-rs/axum | external_research_source_material | external_research_sourced_fact | Rust web framework implementation and design claims |
| https://docs.rs/axum/latest/axum/ | external_research_source_material | external_research_sourced_fact | published crate documentation for routing and extractors |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which entrypoint behavior deserves priority when a backend task cannot afford the full ceremony. The seed scored rows rank corpus hygiene while the mapped skills rank their own behaviors, mode selection before any planning, executable requirements before design, boundaries before code, and gates before handoff, an ordering the six-step workflow states as sequence, not preference.

**Recommended default and causal basis.** Walk the six steps in order for any backend task entering through the delivery skill, spending the most care on mode choice and requirement thresholds. Each workflow step consumes the previous step's output, requirements need the mode's risk framing, service design needs requirements to bound it, and the verification mix needs the design's seams, so skipping a step starves the next one.

**Operating conditions and assumptions.** The task is large enough to deserve the ceremony, the mode combinations give small tasks a two-mode shortcut rather than an exemption. This ranking orders entrypoint behaviors, per-topic doctrine rankings live with the playbook, ops, and routing themes.

**Failure boundary and alternatives.** The common inversion is designing before writing REQ-WEB requirements, which produces service boundaries justified by taste rather than by WHEN-THEN-SHALL behavior anyone can test. Bounded alternatives and recoveries: review-first entry through Review Mode for existing services, the skill supports it explicitly and pairs it with the risk modes rather than the delivery sequence.

**Counterexample, gotchas, and operational comparison.** Step three's inline-versus-durable decision is easy to defer as an implementation detail, but the skill demands it early because retrofitting durable background processing under a synchronous contract rewrites the endpoint's semantics. Good: rejecting make login faster until a latency threshold is named. Bad: sketching handlers before deciding whether the risk is security or resilience. Borderline: skipping the traceability table for a one-endpoint fix, workable, the req-to-test link is what quietly disappears.

**Verification, evidence, and uncertainty.** Sample recent backend work packets for the seven contract sections in order and thresholds in every requirement. The delivery skill's numbered workflow and rejection rule read in full. How much ceremony small teams actually sustain per task is unmeasured.

**Second-order consequence.** The workflow's rejection rule is its sharpest tooth, vague claims such as faster, safer, or more reliable are rejected without thresholds, which converts the requirements step from paperwork into a filter that most incoming task descriptions initially fail.

**Revision decision.** Present the workflow sequence as the operative ranking and keep the scored rows as corpus-process emphasis.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `rust_backend_skill_entrypoint` | 95 | default_adoption_pattern_tier | Source Map First: Load local rust backend material before synthesizing skill entrypoint recommendations. |
| `rust_backend_skill_entrypoint` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `rust_backend_skill_entrypoint` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what principle every use of this theme's entry surfaces must satisfy. The seed generic corpus formulas replace the entrypoint thesis, a backend skill entrypoint succeeds when it classifies before it advises, contracts before it designs, and gates before it hands off, with a separate plain-English door for questions about the stack itself.

**Recommended default and causal basis.** Evaluate any proposed entrypoint shortcut against the four clauses and reject shortcuts that remove a clause rather than compress it. The three clauses condense the delivery skill's structure, classification from mode selection, contracts from executable requirements and the output contract, gates from the quality-gate step, while the fourth clause is the explainer's entire reason to exist.

**Operating conditions and assumptions.** Requests arrive with enough context to classify, requests that cannot even be classified go to Spec Mode by the skill's own vague-request rule. The thesis governs entry behavior, what the destination files teach once entered is their own themes' business.

**Failure boundary and alternatives.** Collapsing the thesis to just use the right mode misses the contract clause, a correctly classified task that ships without WHEN-THEN-SHALL requirements still produces the unreviewable work packets the skill exists to prevent. Bounded alternatives and recoveries: a single merged skill handling both work and explanation, simpler to trigger, the explainer's existence is documented evidence that the merged form failed at trigger accuracy.

**Counterexample, gotchas, and operational comparison.** The explain-separately clause cuts both ways, the explainer must not do delivery work, but the delivery skill also should not narrate strategy, mixing genres in either direction dilutes both doors. Good: a request classified, contracted, designed, and gated in contract order. Bad: an implementation plan delivered with open questions silently omitted. Borderline: answering a quick mode question without the full contract, fine, the contract applies to work packets, not to questions.

**Verification, evidence, and uncertainty.** For a recent packet, locate the mode declaration, the thresholded requirements, and the gate results as three artifacts. The delivery skill's workflow and contract plus the explainer's charter read in full. Whether four clauses cover multi-service programs of work is beyond the mapped material.

**Second-order consequence.** The thesis makes the entrypoint a compiler for requests, ill-formed input is rejected at the boundary with a named reason, vague verbs fail the threshold rule and wrong-domain tasks fail the guardrail check, mirroring at the workflow level the parse-at-the-edge doctrine the bundle teaches at the type level.

**Revision decision.** State the four clauses, classify first, contract second, gate last, explain separately, each cited to its skill section.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `rust_backend_skill_entrypoint` contains 2 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Rust Backend Skill Entrypoint comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide how to navigate the two mapped entry files when a task or question arrives. The seed map rows list heading signals without the asymmetry that matters, the delivery skill is a five-file bundle's front matter whose context strategy orders progressive loading, while the explainer is a leaf with no bundle at all, only two external document paths it cannot guarantee.

**Recommended default and causal basis.** Read the delivery skill's mode and strategy sections at dispatch time, defer bundle files to the strategy's order, and read the explainer whole when its trigger fires. Reading strategy differs by shape, the delivery skill is read as a dispatcher, its mode table and context strategy first, its workflow when planning, while the explainer is read whole because its 78 lines are the entire artifact.

**Operating conditions and assumptions.** Questions arrive distinguishable as work versus explanation, mixed requests take the delivery door and hand the explanation part to the explainer afterward. This map describes the two mapped SKILL.md files, the bundle's interior files are mapped by their own corpus themes.

**Failure boundary and alternatives.** Loading the delivery skill's whole bundle eagerly repeats the failure its context strategy exists to prevent, the strategy's first instruction is to read the reference map before the heavier files. Bounded alternatives and recoveries: heading search across both files with the shipped ripgrep pattern, effective for experts, it bypasses the frontmatter descriptions that carry the trigger logic.

**Counterexample, gotchas, and operational comparison.** The explainer's heading signals look richer than its authority, style and output-shape headings govern tone for one narrow question class, they are not backend guidance surfaces despite sitting in the same map table. Good: dispatching from the mode table and loading only the strategy's next file. Bad: reading all five bundle references before classifying the task. Borderline: skimming the explainer for its backend-problem list during delivery work, harmless, the delivery skill's own workflow already covers it.

**Verification, evidence, and uncertainty.** Check that recent consultations followed the progressive-loading order rather than eager whole-bundle reads. Both files' structure and the context strategy's numbered order read in full. Whether the explainer's external document paths resolve anywhere current is unverifiable here.

**Second-order consequence.** The delivery skill even ships its own search tooling, four ripgrep heading commands for the large reference files, meaning the source itself anticipates partial reading and equips it, a property worth preserving in any derived guidance.

**Revision decision.** Annotate the rows with the shapes, dispatcher-plus-bundle versus standalone leaf, and the progressive-loading order the delivery skill prescribes for itself.

**Retained seed evidence.** Both local source rows with title, heading signals, and usage role remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202604/personal/rust-web-backend-skill-explainer/SKILL.md | rust-web-backend-skill-explainer | Rust Web Backend Skill Explainer; Core Principle; What To Explain; Style; Reference Use; Output Shape | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202604/rust-web-backend-delivery-01/SKILL.md | rust-web-backend-delivery-01 | Rust Web Backend Delivery 01; Mode Selection; Workflow; Output Contract; Guardrails; Context Strategy | skill entrypoint or reusable agent prompt |

## External Research Source Map

**Decision supported.** This section helps decide which external targets, if any, deserve retrieval effort when this theme's claims need checking. The seed four inherited URLs anchor framework and runtime code while this theme's subject is skill design, no external row could verify the mode taxonomy, the output contract, or the scope-split rationale, because those are this repository's own constructs.

**Recommended default and causal basis.** Route freshness effort to destination themes' externals and treat this theme's claims as checkable against the two local files alone. The rows fit the bundle's destination content, axum and tokio underpin the playbook's technical claims, but the entrypoint layer above that content has no public analogue to fetch, its correctness is internal consistency, not ecosystem agreement.

**Operating conditions and assumptions.** URL structures remain stable if the rows are ever consumed for destination freshness. External rows serve future freshness verification for destination content, all four remained unretrieved throughout this evolution.

**Failure boundary and alternatives.** Spending retrieval effort on these rows to freshen this theme would verify nothing this theme asserts, the URLs matter one layer down where framework idioms actually appear. Bounded alternatives and recoveries: verifying the entrypoint layer by exercising it, run a real task through the modes and contract, behavioral verification no URL fetch can substitute.

**Counterexample, gotchas, and operational comparison.** The api-guidelines row is the least irrelevant here, requirement-quality language and naming discipline echo its spirit, but citing it for the REQ-WEB convention would dress a local invention in borrowed authority. Good: a dated axum docs fetch to freshen the playbook theme, not this one. Bad: citing the tokio repository to validate the six-mode taxonomy. Borderline: consulting agent-platform skill-format docs, relevant to the frontmatter conventions, absent from the mapped rows.

**Verification, evidence, and uncertainty.** Confirm externally-flavored claims here either cite a dated retrieval or carry the unretrieved-candidate label. Zero fetches this assignment and the local-construct nature of both mapped files. How skill-format conventions have evolved since 202604 is unknowable without platform documentation fetches.

**Second-order consequence.** The nearest true external analogue for this theme is not a crate but the agent-skill format itself, the frontmatter description conventions both files follow, and no mapped URL covers that, a gap future mapping revisions could fill with agent-platform documentation.

**Revision decision.** Keep the rows as inherited candidates, note their one-layer-down relevance, and mark the entrypoint layer as locally verifiable by construction.

**Retained seed evidence.** All four external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://github.com/rust-lang/api-guidelines | Rust API Guidelines | Rust library-team API design recommendations | external_research_sourced_fact |
| https://github.com/tokio-rs/tokio | Tokio repository | async runtime implementation and operational model | external_research_sourced_fact |
| https://github.com/tokio-rs/axum | Axum repository | Rust web framework implementation and design claims | external_research_sourced_fact |
| https://docs.rs/axum/latest/axum/ | Axum docs.rs | published crate documentation for routing and extractors | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which behaviors each entry door must refuse regardless of how plausible the output looks. The seed process rows miss the rejections the two skills name themselves, using the backend skill for generic libraries or CLIs, treating transport validation as the whole story, recommending synchronous handling where durable retries are needed, shipping password flows without storage and failure behavior, deferring migrations to postscript, and the explainer's own four, hype, upstream mentions, framework drowning, and replacement claims.

**Recommended default and causal basis.** Check incoming tasks against the scope guardrails before dispatch and outgoing explanations against the narrative guardrails before delivery. Each rejection is a guardrail transcribed, the delivery skill's five guardrails police scope and technical shortcuts while the explainer's four police narrative honesty, together they define what both doors refuse to emit.

**Operating conditions and assumptions.** Reviewers know which door produced the output, guardrail attribution requires the mode declaration the workflow already mandates. Entrypoint-level rejections only, per-topic technical anti-patterns live with the destination themes.

**Failure boundary and alternatives.** Guardrail violations pass casual review because the output still looks competent, generic-library advice from the backend skill reads plausibly, it is merely wrong-skill advice that a scope check would have caught at dispatch. Bounded alternatives and recoveries: encoding the scope guardrails into the routing theme's dispatch checks, attractive, it centralizes what the skill states locally at the cost of drift between the two statements.

**Counterexample, gotchas, and operational comparison.** The do-not-mention-upstream guardrail is unique in the corpus, it makes the explainer deliberately opaque about provenance, so this reference must carry the provenance the explainer itself is forbidden to state. Good: redirecting a parser-crate question to the broader Rust skills per the first guardrail. Bad: a session-flow recommendation silent on storage and failure behavior. Borderline: brief framework detail in an explainer answer when the user asked for it, the guardrail's own unless-asked clause permits exactly this.

**Verification, evidence, and uncertainty.** Check each registry row quotes a citable guardrail and carries a working detection signal. Both files' guardrail sections read in full. Relative frequency of the nine violations in practice is unmeasured.

**Second-order consequence.** The two guardrail sets police opposite failure directions, the delivery guardrails stop under-engineering, skipped validation layers and postscript migrations, while the explainer guardrails stop over-selling, hype and replacement claims, an entrypoint pair that brackets both ways a skill stack loses trust.

**Revision decision.** Import the nine rejections with detection signals, scope checks at dispatch for the delivery five, genre checks at review for the explainer four.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide what a backend work packet must demonstrate before the entrypoint considers it handed off. The seed document verifier stands alone while the delivery skill defines handoff gates for the work itself, format, lint, tests, and build success, requirement coverage with measurable evidence for non-functional claims, and migration and rollout safety review when schema or deployment behavior changes.

**Recommended default and causal basis.** Require the four technical checks on every packet, a filled traceability row per requirement, and the migration review whenever the schema or deploy surface moves. The gates close the loop the requirements step opens, thresholds demanded at step two are evidenced at step six, so a packet passing the gates has proven its own REQ-WEB claims rather than merely compiling.

**Operating conditions and assumptions.** Requirements were actually written with thresholds, gates cannot evidence claims that were never made measurable. Entrypoint handoff gates, per-topic verification depth belongs to the testing sibling's matrix and the destination themes.

**Failure boundary and alternatives.** Gating only on the four technical checks passes packets whose faster or safer claims were never measured, exactly the vague-claim class the workflow rejected at intake, reintroduced at exit. Bounded alternatives and recoveries: destination-theme gates like the ops file's CI floor, complementary, the entrypoint gates govern the packet while the ops gates govern the pipeline.

**Counterexample, gotchas, and operational comparison.** The migration gate is conditional on behavior change, not on file type, a code-only change that alters rollout behavior still triggers it, reading the condition as touched-a-migration-file misses half its cases. Good: a packet whose traceability table maps every REQ-WEB row to a test id. Bad: a handoff whose reliability claim cites no measurement. Borderline: skipping the table on a two-line fix, tolerable, the requirement-to-evidence link should survive in the commit message.

**Verification, evidence, and uncertainty.** Audit recent packets for the three gate layers and sample one traceability row through to its actual test. The delivery skill's quality-gate step and output contract read in full. Whether teams sustain the traceability table beyond initial adoption is unmeasured.

**Second-order consequence.** The traceability shape, req_id to test_id to assertion to target, makes coverage mechanical, an empty row is a visible hole, so the contract's table converts requirement coverage from a judgment call into a diff-reviewable artifact.

**Revision decision.** Adopt the three gate layers, technical checks, requirement-evidence coverage, and conditional migration review, with the traceability table as the coverage instrument.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide which skill, mode set, and output shape an agent must declare before responding to a backend request. The seed four generic bullets stand where the mapped files define agent behavior precisely, the delivery skill's frontmatter description is a trigger specification, its mode combinations are dispatch defaults, and its output contract is the response format, while the explainer's description enumerates the exact question shapes that should activate it instead.

**Recommended default and causal basis.** Declare the chosen skill and mode set, then produce that door's ordered sections verbatim as the response skeleton. Agents select skills by description matching, so the two descriptions are the actual routing algorithm, the delivery description names technologies and task classes while the explainer description names question patterns, and this reference should preserve that split rather than paraphrase it away.

**Operating conditions and assumptions.** The agent can read both frontmatter descriptions at dispatch time, embedded summaries of them reintroduce the staleness failures the routing theme catalogues. Agent dispatch and response behavior at the entry layer, in-bundle behavior after entry belongs to the destination themes' guides.

**Failure boundary and alternatives.** An agent activating the delivery skill for a why-does-this-skill-exist question produces a work packet for a question, seven contract sections wrapping an answer that needed five plain-English ones. Bounded alternatives and recoveries: restricting agents to the delivery door and reserving explanation for humans, defensible where narrative trust matters, the explainer's style rules exist precisely to make agent explanation safe.

**Counterexample, gotchas, and operational comparison.** Default mode combinations are starting points the workflow refines, an auth task defaults to Spec plus Security, but discovering a migration mid-task adds Operations Mode, agents that freeze the initial combination miss the workflow's re-classification intent. Good: an agent answering a skill-comparison question in the five-part explainer shape. Bad: a work packet missing its Open Questions section because nothing felt open. Borderline: an agent adding Resilience Mode mid-task when an external API appears, correct behavior, worth declaring explicitly.

**Verification, evidence, and uncertainty.** Reject agent responses whose section headings match neither door's ordered output specification. Both frontmatter descriptions, the mode defaults, and both output shapes read in full. How reliably description-based dispatch separates the two doors in practice lacks measurement.

**Second-order consequence.** Both skills specify output format as strictly as input triggers, seven ordered contract sections on one door and five ordered explanation sections on the other, so an agent's compliance is checkable from the response's section headings alone without reading its content.

**Revision decision.** Instruct agents to match against both descriptions first, apply the default mode combinations for work, and emit the matching output shape, contract sections for delivery, the five-part plain-English shape for explanation.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide which entry journey matches the reader's current need before any file is opened. The seed one generic engineer stands in for the pair's three real journeys, a task-holder entering the delivery door with a backend request, a stack-explorer asking the explainer why the skill split exists, and a skill-author studying both files as a template for future scope splits.

**Recommended default and causal basis.** Identify the journey from the request's genre, work, why, or how-to-structure, and enter through the matching door with the matching output expectation. The three journeys want different artifacts, the task-holder leaves with a work packet, the explorer leaves with a five-part plain-English answer, and the author leaves with the structural pattern, trigger description plus guardrails plus output contract, that both files instantiate.

**Operating conditions and assumptions.** The reader can name their journey, mixed requests split into a work leg and an explanation leg served by different doors. Journeys through the entry layer, journeys inside the bundle's content belong to the destination themes.

**Failure boundary and alternatives.** Documentation serving only the task-holder journey leaves the explorer reading workflow mechanics for a why question, precisely the mismatch the explainer skill was created to absorb. Bounded alternatives and recoveries: a single merged journey through one combined document, the historical baseline whose trigger-accuracy cost the explainer's scope-split narrative records.

**Counterexample, gotchas, and operational comparison.** The explorer journey has an audience rule the others lack, the style section demands accessibility to a smart beginner or an outside teammate, so explorer-journey success is measured by a non-expert reader, not by technical completeness. Good: an outside teammate getting the scope-split story in plain English with an analogy first. Bad: a task-holder receiving strategy narration instead of a mode declaration. Borderline: an author copying the guardrail structure without the trigger description, half the template, the halves only work together.

**Verification, evidence, and uncertainty.** Ask recent users which journey they ran and whether the door they used matched it. Both files' trigger descriptions, output shapes, and the explainer's style rules read in full. The real frequency mix of the three journeys is unmeasured.

**Second-order consequence.** The author journey is quietly the highest-leverage one, the explainer documents that a scope split improved trigger accuracy and advice quality, so these two files are the corpus's worked example of when to split a skill, evidence future skill designers can cite.

**Revision decision.** Recast the scenario as three entries, task-holder through modes and contract, explorer through the five-part shape, author through the structural comparison of the two files.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The Rust reliability engineer is starting from a requirement that needs a safe API, explicit error model, and testable boundary and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `rust_backend_skill_entrypoint` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing the idiomatic ownership, async, error, and crate-boundary shape before coding.
Reference opening trigger: Open this file when the task mentions rust backend skill entrypoint, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide how much ceremony, accessibility, and trigger separation each entry-layer use deserves. The seed template rows skip the entry layer's live tensions, ceremony versus speed at the delivery door, plain-English accessibility versus technical precision at the explainer door, and trigger breadth versus trigger accuracy across the pair.

**Recommended default and causal basis.** Keep full contract ceremony for work, analogy-first style for explanation, and separate triggers for the two doors. Each tension has a stance in the sources, the delivery skill spends ceremony to make packets decision-complete, the explainer spends precision to keep explanations accessible, and the pair exists because one skill covering both bought breadth at accuracy's expense.

**Operating conditions and assumptions.** Deviations carry named reasons, a two-line fix may compress the contract if its requirement-to-evidence link survives somewhere reviewable. Tuning entry-layer behavior, technical design tradeoffs belong to the playbook theme and operational ones to the ops theme.

**Failure boundary and alternatives.** Each stance has a seductive inversion, skipping requirements to move fast produces unreviewable packets, technical explainer answers lose the outside teammate, and re-merging the doors for simplicity re-creates the trigger confusion the split fixed. Bounded alternatives and recoveries: environment-differentiated ceremony, full contract for production services and compressed for prototypes, workable when the packet states which regime it used.

**Counterexample, gotchas, and operational comparison.** The accessibility stance does not cap depth, the style rules order the presentation, main idea first, analogies before labels, they never forbid technical content the user asks for, reading them as depth limits produces vacuous explanations. Good: a prototype packet declaring compressed ceremony with requirements intact. Bad: an explainer answer opening with framework internals. Borderline: merging the doors for a team of one, the trigger-accuracy cost is real but its victim, the confused dispatcher, is the same person.

**Verification, evidence, and uncertainty.** Audit recent deviations from the three stances for their named reasons. The delivery skill's ceremony structure and the explainer's style and scope-split sections read in full. The packet size below which ceremony compression is safe is a judgment call.

**Second-order consequence.** All three stances pay a fixed cost per use to avoid a variable cost per failure, contract ceremony against rework loops, analogy-first style against lost audiences, and dual triggers against misdispatch, the same insurance shape at three different layers.

**Revision decision.** Add the three axes with the sources' stances and the narrow conditions justifying deviation on each.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the rust backend skill entrypoint pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong rust backend skill entrypoint guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which of the two files settles a question when both plausibly apply. The seed canonical-versus-legacy roles here invert the working reality, it crowns the explainer canonical and demotes the delivery skill to legacy, yet the delivery skill is the live working entrypoint the whole bundle hangs from, while the explainer is meta-commentary about that skill's existence.

**Recommended default and causal basis.** Resolve work-related questions against the delivery skill and stack-related questions against the explainer, and treat cross-genre citations as red flags. The seed's roles were assigned by template order, not by function, and this theme is the rare case where the assignment misleads, every backend doctrine question resolves against the delivery skill and its bundle, never against the explainer.

**Operating conditions and assumptions.** The genre of a question is decidable, questions about whether the delivery skill should exist are explainer territory even though they mention the delivery skill. Precedence between the two mapped files, precedence inside the bundle belongs to the destination themes' hierarchy sections.

**Failure boundary and alternatives.** Taking the seed's roles literally would route doctrine disputes to a file whose guardrails forbid it from holding doctrine, an authority loop the genre split dissolves immediately. Bounded alternatives and recoveries: treating the delivery bundle's reference-map.md as a third hierarchy layer, correct one level down, this theme's scope stops at the two SKILL.md surfaces.

**Counterexample, gotchas, and operational comparison.** The explainer names a third skill, rust-executable-specs-01 as the broad planner, giving it hierarchy knowledge the delivery skill's guardrails only hint at, so stack-topology questions genuinely need the explainer's map. Good: settling a mode-selection dispute from the delivery skill alone. Bad: citing the explainer to justify a verification-mix choice. Borderline: using the explainer's backend-problem list to argue task scope, acceptable as positioning evidence with the doctrine check done next door.

**Verification, evidence, and uncertainty.** Test hierarchy claims against each file's self-declared description and guardrails. Both files' charters and the seed's role assignments read in full. Whether the seed's role inversion was deliberate template behavior or an artifact is unrecorded.

**Second-order consequence.** Authority here is partitioned by genre rather than ranked by strength, each file is supreme in its genre and silent in the other's, so the usual canonical-supporting ladder flattens into two parallel one-file hierarchies.

**Revision decision.** Record the corrected hierarchy, delivery skill canonical for entry doctrine, explainer canonical only for the scope-split narrative, and neither legacy, both files are the same 202604 vintage.

**Retained seed evidence.** The seed's hierarchy rows and the confidence warning remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202604/personal/rust-web-backend-skill-explainer/SKILL.md | canonical primary source | Rust Web Backend Skill Explainer; Core Principle; What To Explain | What guidance, warning, or example should this source contribute to Rust Backend Skill Entrypoint? |
| agents-used-monthly-archive/codex-skills-202604/rust-web-backend-delivery-01/SKILL.md | legacy historical source | Rust Web Backend Delivery 01; Mode Selection; Workflow | What guidance, warning, or example should this source contribute to Rust Backend Skill Entrypoint? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing record makes entry dispatch and response conformance checkable at a glance. The seed named artifact, a request lifecycle diagram, belongs to the playbook theme, this entry theme's natural object is a dispatch card, one page pairing each door's trigger description with its mode defaults, output shape, and guardrail checklist, consulted before any backend request is answered.

**Recommended default and causal basis.** Create the card from the two live files, keep it beside the routing theme's card, and re-derive it on any bundle revision. The two files' operative content is exactly what dispatch needs and exactly what memory garbles, six modes, five default combinations, seven contract sections, five plain-English sections, nine guardrails, counts a card preserves and recall corrupts.

**Operating conditions and assumptions.** The card quotes the live frontmatter descriptions rather than paraphrasing them, paraphrase is where trigger drift begins. One card for the entry pair, in-bundle routing already has the routing theme's card and lifecycle diagrams belong to the playbook theme.

**Failure boundary and alternatives.** Without the card each dispatch re-derives the trigger split from memory, and remembered triggers drift toward whichever door was used last, the misdispatch pattern both descriptions exist to prevent. Bounded alternatives and recoveries: relying on the agent platform's own description matching without a card, adequate for automated dispatch, it leaves human reviewers with no conformance reference.

**Counterexample, gotchas, and operational comparison.** The card must preserve the mode combinations as defaults, not as rules, freezing them as rules loses the workflow's re-classification step where mid-task discoveries add modes. Good: a review catching a missing Open Questions section against the card's shape block. Bad: dispatch from a six-month-old memory of the mode table. Borderline: one merged card for this pair and the routing theme, compact, the entry-versus-in-bundle distinction is what merging blurs.

**Verification, evidence, and uncertainty.** Diff the card's four blocks against the two live files at each audit cycle. The enumerable trigger, mode, shape, and guardrail content of both files read in full. Card upkeep discipline across bundle revisions is the untested variable.

**Second-order consequence.** The card doubles as a conformance instrument, because both doors specify ordered output sections, a reviewer can hold any response against the card's shape block and validate compliance without opening either source file.

**Revision decision.** Define the artifact as the dispatch card with four blocks, triggers, mode defaults, output shapes, guardrails, updated whenever either SKILL.md changes.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: request lifecycle diagram with persistence boundary, observability hook, and failure table.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Rust Backend Skill Entrypoint | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercise most efficiently builds correct two-door entry habits. The seed abstract usage lines replace the walkthrough this theme wants, one vague backend request taken through the full entry sequence, trigger match, mode selection, REQ-WEB requirements with thresholds, boundary design, verification mix, and the seven-section packet, plus one explainer request answered in the five-part shape.

**Recommended default and causal basis.** Run new backend contributors through both walkthroughs before they answer requests through either door. The entry layer is procedural, its value lives in executed order, and a walkthrough that runs a request like add password reset through Spec plus Security modes teaches the classification judgment that reading the mode table cannot.

**Operating conditions and assumptions.** A realistic vague request is available, sanitized real tickets teach classification far better than invented clean examples. Teaching entry-layer execution, in-bundle technique walkthroughs belong to the playbook, testing, and ops themes.

**Failure boundary and alternatives.** Engineers who have only read the workflow compress it in practice, jumping from request to design because the endpoint shape feels obvious, and the skipped requirements step is exactly where unmeasurable claims re-enter. Bounded alternatives and recoveries: reviewing an existing well-formed packet against the contract, cheaper and useful for the shape, it cannot teach the live classification and thresholding judgment.

**Counterexample, gotchas, and operational comparison.** The explainer walkthrough needs a genuine outsider as audience, its success criterion is the style section's smart-beginner test, and expert-to-expert practice runs systematically overestimate accessibility. Good: a trainee's packet where every requirement row traces to a test id. Bad: certifying entry competence from reading the workflow list. Borderline: practicing only the delivery door, the likelier daily door, the explainer's genre discipline is what stays untrained.

**Verification, evidence, and uncertainty.** Have the trainee run a fresh request unsupervised and audit the packet's sections against the contract order. The delivery skill's workflow and contract plus the explainer's shape and style rules read in full. How long entry discipline persists without periodic vague-request practice is unmeasured.

**Second-order consequence.** The delivery walkthrough's teaching moment is the rejection rule in action, watching make it safer bounce until it becomes a WHEN-THEN-SHALL with a threshold converts the rule from pedantry into the packet's foundation, a conversion only demonstration achieves.

**Revision decision.** Anchor the section on the dual walkthrough, one delivery-door run producing a real packet and one explainer-door run producing a plain-English answer, each checked against its door's ordered shape.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Rust Backend Skill Entrypoint after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Rust Backend Skill Entrypoint as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Rust Backend Skill Entrypoint only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which measurements prove the two-door entry discipline pays for its ceremony. The seed compile-centric indicator ignores entry-layer feedback, nothing counts requests dispatched to the matching door, packets shipping with complete ordered contracts, or requirements surviving to handoff with their thresholds evidenced.

**Recommended default and causal basis.** Label the three event streams as they occur and review the ratios quarterly beside the packet defect count. Each ratio measures one thesis clause in operation, door-match rate proves the trigger split works, contract-completeness rate proves the packet discipline holds, and threshold-evidence rate proves the requirements step feeds the gates as designed.

**Operating conditions and assumptions.** Dispatch declarations exist to count, the mode-declaration habit the agent guide requires is also the measurement instrument. Measuring entry-layer discipline, in-bundle adoption metrics belong to the destination themes' loops.

**Failure boundary and alternatives.** Without the ratios the scope split's claimed benefits, trigger accuracy and advice quality, stay assertions from the explainer's narrative rather than measured properties of this team's practice. Bounded alternatives and recoveries: measuring downstream outcomes only, rework rates and review rounds, closer to business value, unable to attribute improvement to the entry layer specifically.

**Counterexample, gotchas, and operational comparison.** Contract completeness can go hollow, seven sections present with an Open Questions section that always reads none, so the completeness probe should sample section content, not just section headings. Good: a quarter of dispatch records showing explanation requests consistently reaching the explainer door. Bad: defending the skill split with no local data when a merge is proposed. Borderline: counting compressed-ceremony packets as complete, acceptable when the compression was declared, the declaration is what the count keys on.

**Verification, evidence, and uncertainty.** Confirm the three ratios exist, cite real records, and were consumed at the last quarterly review. The alignment between the entry thesis clauses and their observable dispatch and packet signatures. Healthy baseline values for the three ratios await the first measured quarters.

**Second-order consequence.** The ratios test the explainer's historical claim prospectively, the scope split was justified by trigger accuracy, so a sustained high door-match rate is ongoing evidence the split still pays, and a falling one is the earliest signal the skill stack needs redesign.

**Revision decision.** Add the three ratios with their collection points, dispatch records for door match, packet reviews for contract completeness, and gate records for threshold evidence.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: compile attempts and review comments decrease because the API shape is explicit before implementation.
Failure signal: the reference hides ownership or error tradeoffs behind generic guidance.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this entrypoint reference is declared faithful. The seed shape items never audit fidelity to the mapped files' countable content, six modes, five default combinations, six workflow steps, seven contract sections, five explainer output sections, four style rules, five delivery guardrails, and four explainer guardrails.

**Recommended default and causal basis.** Run the count-and-order audit at every reread cycle and after any archive change touching either SKILL.md. This theme's claims transcribe two enumerable sources, so completeness reduces to counting the enumerables against both files and diffing the two ordered output lists, whose power is positional and therefore paraphrase-fragile.

**Operating conditions and assumptions.** Both 202604 files remain at their paths for line-level comparison. Auditing this reference against its two sources, auditing live dispatch practice belongs to the metrics loop.

**Failure boundary and alternatives.** Count-passing paraphrase is the residual risk, a reworded contract list that swaps Implementation Plan ahead of Verification Matrix keeps the count at seven while breaking the order the contract mandates. Bounded alternatives and recoveries: checksumming both files and re-auditing on change only, efficient, blind to drift introduced by this document's own future edits.

**Counterexample, gotchas, and operational comparison.** Auditing against memory of the files rather than the files is the shared corpus failure mode, the check costs minutes only when both sources are actually opened. Good: an audit catching a mode combination dropped from the defaults list. Bad: declaring fidelity because all 26 headings and both tables exist. Borderline: accepting paraphrased guardrail wording, tolerable, the guardrail count and each one's target behavior must survive exactly.

**Verification, evidence, and uncertainty.** Count the enumerables and diff both ordered output lists against the live mapped files. The fully enumerable structure of both SKILL.md files read in full. Whether order-sensitive auditing needs corpus-level tooling remains open.

**Second-order consequence.** The ordered lists are this theme's analogue of the ops theme's migration steps, sequences whose reordering is not stylistic drift but a different and wrong procedure, so the audit must compare position, not just membership.

**Revision decision.** Add the count items and an exact-order check on the two output lists, contract sections and explainer sections, each audit cycle.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Rust Backend Skill Entrypoint.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a request the two entry doors should not answer. The seed token-split rows point nowhere while this theme's neighbors are exact, the routing theme for in-bundle dispatch after entry, the playbook, testing, ops, and security themes for destination content, and the broader Rust skills the delivery guardrail itself names for out-of-scope work, rust-executable-specs-01 and rust-coder-02.

**Recommended default and causal basis.** Test each arriving request against the delivery description first, then the explainer description, and forward anything matching neither with its named destination. This theme sits at the top of a funnel, requests it admits flow down to the routing theme and destinations, requests it rejects flow sideways to the broader Rust skills, so its adjacency map is the funnel's complete edge set.

**Operating conditions and assumptions.** The named broader skills exist under their expected corpus names, pointer probes keep this checkable. Sending away what the entry layer cannot own, destination content summaries are deliberately absent.

**Failure boundary and alternatives.** Keeping rejected work here for convenience violates the delivery skill's first guardrail, generic library or CLI advice from a backend skill is the wrong-skill output the guardrail exists to prevent. Bounded alternatives and recoveries: for platform questions no local skill covers, agent-format documentation externally labeled unretrieved.

**Counterexample, gotchas, and operational comparison.** Requests mentioning Rust and HTTP are not automatically backend service work, the guardrail's examples, parsers and protocol crates, can both involve HTTP without being services, classification is by task shape, not vocabulary. Good: forwarding an HTTP protocol-crate question to the broader Rust skills despite its web vocabulary. Bad: answering a CLI-tool question here because it imports tokio. Borderline: a service-adjacent build-script question, likely sideways, the funnel test settles it case by case.

**Verification, evidence, and uncertainty.** Sample recent forwarded and kept requests against both descriptions and the guardrail's scope line. The delivery guardrails, both descriptions, and the explainer's skill-stack map read in full. Misdispatch frequency into this theme lacks measurement.

**Second-order consequence.** The sideways routes are the only corpus routes named inside the mapped sources themselves, both files name rust-executable-specs-01, making the out-of-scope edge the best-documented edge in this theme's map, better documented than its own downward edges.

**Revision decision.** Route by the funnel, admitted work down through the routing theme, stack questions to the explainer door, out-of-scope work sideways to the named broader skills.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use backend, executable, or quality-gate Rust references when the question shifts to HTTP delivery, specs, or review gates.
Adjacent reference 1: consider the rust adjacent reference when the current task pivots away from rust backend skill entrypoint.
Adjacent reference 2: consider the backend adjacent reference when the current task pivots away from rust backend skill entrypoint.
Adjacent reference 3: consider the skill adjacent reference when the current task pivots away from rust backend skill entrypoint.

## Workload Model

**Decision supported.** This section helps decide how to budget entry-layer effort so ceremony lands on packets and not on questions. The seed endpoint-count budgets fit destination work, not entry work, whose real units are per-request dispatch performed constantly at seconds of cost, per-packet ceremony performed per work item at minutes to hours, and per-revision card upkeep performed rarely when either SKILL.md changes.

**Recommended default and causal basis.** Keep dispatch under a minute via the card, budget packets by their declared mode count, and slot card upkeep into every bundle revision. The three units scale on different axes, dispatch scales with request volume, ceremony scales with work item count and size, and upkeep scales with bundle revision frequency, so one capacity number cannot budget the entry layer.

**Operating conditions and assumptions.** Request volume is high enough that dispatch efficiency matters, low-volume teams can afford slower per-request classification. Budgeting entry-layer effort, implementation-slice budgets belong to the playbook theme and operational budgets to the ops theme.

**Failure boundary and alternatives.** Budgeting ceremony as dispatch, treating the full six-step workflow as a per-request reflex, either starves real packets of their requirement work or inflates trivial questions into work packets, both misallocations of the same flat model. Bounded alternatives and recoveries: letting agents absorb dispatch cost entirely via description matching, near-zero marginal cost, human reviewers still need the card for conformance checks.

**Counterexample, gotchas, and operational comparison.** The upkeep unit is rare enough to be forgotten entirely, and a stale dispatch card silently degrades every subsequent dispatch, so upkeep belongs to the revision event itself, not to a calendar. Good: a roadmap estimating a five-mode review packet at several times a two-mode feature packet. Bad: a sprint plan counting packets without reading their mode declarations. Borderline: batching several small same-mode requests into one packet, efficient, their requirement sets must stay individually traceable.

**Verification, evidence, and uncertainty.** Compare recent packet efforts against their declared mode counts for the predicted scaling. The mode combination structure and workflow depth read from the delivery skill in full. Typical per-mode ceremony cost lacks local measurement.

**Second-order consequence.** Mode count is the natural ceremony multiplier, a Spec-plus-Delivery task and a Review-plus-three-risk-modes task differ predictably in packet effort, so the mode declaration made at dispatch doubles as the packet's effort estimate, one artifact serving two purposes.

**Revision decision.** Restate the model in three units, dispatch cost per request, ceremony cost per packet scaled by mode count, and upkeep cost per bundle revision.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Rust Backend Skill Entrypoint as a backend service operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | service implementation, route review, worker execution, and operational hardening work where a single wrong recommendation can affect live request handling | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one service boundary, up to 25 endpoints or workers, 1000 representative requests, and one deploy rollback path per review batch | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Rust Web Backend Skill Explainer; Core Principle; What To Explain; Style; Reference Use; Output Shape; Rust Web Backend Delivery 01; Mode Selection; W | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is request lifecycle diagram with persistence boundary, observability hook, and failure table | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which entry-layer invariants must demonstrably hold for this theme to count as adopted. The seed document-property thresholds stand where entry-layer invariants should, every response traceable to a declared door and mode set, every packet carrying all seven contract sections in order, every requirement thresholded and evidenced, and zero out-of-scope tasks answered instead of forwarded.

**Recommended default and causal basis.** Audit the four targets quarterly from existing artifacts and treat any scope-target violation as an immediate process review. Each target is a source rule stated as an invariant, the declaration target from the workflow's classification step, the contract target from the output contract, the threshold target from the vague-claim rejection rule, and the scope target from the first delivery guardrail.

**Operating conditions and assumptions.** The artifacts exist, teams that skipped the declaration habit have nothing to audit, which is itself the first finding. Entry-layer invariants, destination-content reliability belongs to the destination themes' targets.

**Failure boundary and alternatives.** Averaging the targets hides the ones that are binary per event, one out-of-scope task answered authoritatively is one wrong-skill artifact in circulation, not a tolerable percentage point. Bounded alternatives and recoveries: sampling-based spot checks instead of full audits, cheaper at volume, they trade the binary targets' zero-tolerance framing for statistical comfort.

**Counterexample, gotchas, and operational comparison.** The contract target's order clause matters as much as its presence clause, sections shuffled to taste defeat the contract's purpose, readers navigate packets by position. Good: a quarter with every sampled packet opening with its Backend Work Mode declaration. Bad: declaring entry discipline from the skill files merely existing in the archive. Borderline: waiving the contract target for compressed-ceremony packets, acceptable when the compression declaration itself is present.

**Verification, evidence, and uncertainty.** Review the four target audits together at each quarterly checkpoint with artifacts attached. The workflow, contract, rejection rule, and first guardrail read in full. Sustainable audit depth per quarter is team-size dependent.

**Second-order consequence.** All four targets are checkable from artifacts the discipline already produces, declarations, packets, gates, and logs, so enforcement adds no new instrumentation, only the habit of actually reading what the process emits.

**Revision decision.** Publish the four targets with their evidence methods, response headers for declarations, section audits for contracts, gate records for thresholds, and dispatch logs for scope.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which decay modes need standing probes and what behavioral signal each probe inspects. The seed generic drift and surge rows miss how entry discipline decays, trigger drift where remembered descriptions replace read ones, ceremony erosion where contract sections quietly drop off packets, mode freeze where initial classifications never update mid-task, and genre bleed where explanation and work vocabulary mix in one response.

**Recommended default and causal basis.** Run the packet-level probes in review and the behavioral probes quarterly, attributing findings to their decay mode. All four decay through familiarity, not through change, the files stay constant while practice drifts away from them, so no diff or revision event exists to trigger a re-read, the decay is purely behavioral.

**Operating conditions and assumptions.** Probes are owned, an unowned probe list is the erosion mode applied to the failure table itself. Decay of entry-layer discipline, destination-content decay belongs to the destination themes' tables.

**Failure boundary and alternatives.** Each mode presents as efficiency, skipping the description check feels fast, dropping Open Questions feels clean, keeping the first mode set feels decisive, and blended responses feel thorough, the discipline's absence reads as fluency until a misdispatch or unreviewable packet surfaces it. Bounded alternatives and recoveries: periodic re-certification through the worked-example walkthroughs, heavier than probes, it renews the habits rather than merely detecting their decay.

**Counterexample, gotchas, and operational comparison.** Genre bleed flows both directions, work packets acquiring strategy narration is as real as explanations acquiring implementation detail, and only the second direction has a guardrail naming it, the first must be policed by the contract's section discipline. Good: a review catching three consecutive packets missing Open Questions. Bad: trusting entry discipline because the skill files have not changed. Borderline: a quarter with zero mid-task mode changes on genuinely simple tasks, plausible, worth one manual sample before accepting.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe, owner, and cadence, and sample one probe's recent findings. The behavioral rather than textual nature of entry-layer decay read against both files' fixed content. Decay onset speed relative to team familiarity is unmeasured.

**Second-order consequence.** Mode freeze is the subtlest row because its probe is an absence, a healthy practice shows occasional mid-task mode additions as discoveries surface, so a long streak of never-updated classifications is itself the anomaly, zero looks like discipline and means its opposite.

**Revision decision.** Add the four rows with behavioral probes, periodic dispatch audits against the live descriptions, section-presence checks per packet, mid-task mode-change counts, and genre checks on sampled responses.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| request surge overloads path | traffic spikes exceed handler, pool, or coroutine limits | apply rate limits, queue bounds, cancellation, and rollback playbook before rollout |
| security boundary silently fails | auth, permission, or input validation assumptions are untested | run abuse-case tests and require explicit deny-by-default behavior |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide which entry-layer failures may retry immediately and which must re-enter at a named step. The seed corpus-process bullets stand where entry-layer retry doctrine should, a failed dispatch and a failed packet retry differently, misdispatch is cheap to redo, re-classify and re-enter through the right door, while a packet failing its gates must not simply regenerate, its failure names which workflow step was starved.

**Recommended default and causal basis.** Re-dispatch freely on classification errors with the card open, and re-enter at the named step on gate failures, escalating to a human after the second same-step failure. Dispatch is stateless and packets are cumulative, a wrong door leaves no artifact worth salvaging, but a gate failure points backward into the packet, unmeasured claims trace to the requirements step and coverage holes to the verification mix, so the retry re-enters the workflow at the failed step, not at the start.

**Operating conditions and assumptions.** Gates report which check failed, a pass-fail-only gate output forfeits the self-locating property and forces whole-packet review. Retry semantics for entry-layer work, request-level retry doctrine belongs to the security sibling's territory one layer down.

**Failure boundary and alternatives.** Regenerating a failed packet from scratch discards the diagnostic the gate failure encoded and usually reproduces the same starved step, the entry-layer version of rerunning a failed migration blindly. Bounded alternatives and recoveries: fully regenerating packets on any failure, simpler to script for agent workflows, it trades the diagnostic for idempotence and fits only fully automated pipelines.

**Counterexample, gotchas, and operational comparison.** Backpressure applies to the request queue, a team gate-failing most of its packets should stop admitting new requests and re-run the walkthrough training, throughput recovered by relaxing gates is the erosion mode wearing a productivity mask. Good: a threshold-gate failure sending the author back to requirements only. Bad: a full packet rewrite after a single missing traceability row. Borderline: re-dispatching a task twice across doors, the third bounce is a signal the request itself needs Spec Mode decomposition.

**Verification, evidence, and uncertainty.** Inject a classification error and a gate failure on a practice task and confirm each takes its regime's path. The sequential gate-to-step coupling read from the delivery skill's workflow in full. The same-step failure count that should trigger escalation is team-calibrated.

**Second-order consequence.** The ordered workflow makes gate failures self-locating, because each gate checks one step's output, the failure message is also the retry address, a property the workflow's sequential design buys and unordered checklists lack.

**Revision decision.** Add the two-regime rule, free re-dispatch for door and mode errors, and step-targeted re-entry for gate failures, with the failed gate naming the re-entry point.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide what evidence entry-layer work must emit for its discipline to be auditable. The seed generic counters replace the record set entry work needs, the declared door and mode set per response, the contract sections present per packet, the requirement-to-test traceability rows, and the forwarded requests with their sideways destinations.

**Recommended default and causal basis.** Wire the four records into the response and review templates rather than into separate logging, the artifacts themselves are the log. Each record serves a distinct consumer, declarations serve the dispatch auditor, section records serve packet reviewers, traceability rows serve the gate, and forwarding records serve whoever tunes the skill stack's scope boundaries.

**Operating conditions and assumptions.** Templates are actually used, free-form responses erase the records by construction. What entry-layer work must record, destination-layer telemetry belongs to the ops theme's checklist.

**Failure boundary and alternatives.** Unrecorded forwarding is the quiet loss, rejected requests vanish without evidence, so the stack's scope gaps, the request classes no skill owns, never accumulate into the signal that would justify a new skill. Bounded alternatives and recoveries: platform-level agent telemetry where available, richer in volume, it rarely captures the semantic fields, mode sets and destinations, that make these records useful.

**Counterexample, gotchas, and operational comparison.** Traceability rows can be present and hollow, a test id that names no real test satisfies the shape while defeating the record, so sampled rows should be followed to their tests occasionally. Good: a quarter's forwarding records revealing a recurring cluster of queue-worker requests no skill owns. Bad: reconstructing dispatch history from memory during a stack redesign. Borderline: recording declarations only for packets and not for explanations, defensible, the explainer door's conformance then goes unmeasured.

**Verification, evidence, and uncertainty.** Pick a recent response and locate all four records from its artifacts alone. The artifact-embedded nature of the contract and workflow read in full. The record volume at which template capture needs tooling support is team-specific.

**Second-order consequence.** The forwarding record is the stack's evolution instrument, the explainer documents that the backend skill itself was born from a recurring cluster of problems, and only forwarding records can reveal the next such cluster while it is still forming.

**Revision decision.** Define the emission floor, declaration per response, section list per packet, filled traceability table per packet, and destination per forwarded request, kept in the same review artifacts the discipline already produces.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture error count, timeout count, retry count, saturation signal, and rollback trigger.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide how to prove the entry ceremony stays cheap enough to keep. The seed request-latency ceilings measure the destination layer, this theme's performance surfaces are dispatch latency from request arrival to door-and-mode declaration, packet lead time from declaration to gate pass, and explanation turnaround for the plain-English door.

**Recommended default and causal basis.** Record the three measurements from existing artifact timestamps and investigate any trend breaking its historical band. The entry layer's cost is process cost, its ceremony competes with delivery speed, so proving the ceremony affordable means measuring the ceremony itself, not the services it eventually produces.

**Operating conditions and assumptions.** Artifacts carry timestamps, declarations and gate records without times cannot yield lead times retroactively. Verifying entry-layer performance, service latency methods belong to the playbook theme's SLO clause and pipeline duration to the ops theme.

**Failure boundary and alternatives.** Leaving entry cost unmeasured lets it be argued anecdotally in both directions, ceremony defenders cite quality and critics cite delay, and without lead-time data the argument resolves by seniority instead of evidence. Bounded alternatives and recoveries: fixed budgets per surface instead of trends, simpler to state, they age poorly as the task mix and team familiarity shift.

**Counterexample, gotchas, and operational comparison.** Dispatch latency has a floor worth respecting, near-zero dispatch times suggest description checks are being skipped, so the alarm should watch both tails, slow dispatch signals friction and instant dispatch signals trigger drift. Good: a trend review catching two-mode packet lead time doubling after a template change. Bad: service p99 dashboards standing in for entry-layer health. Borderline: tolerating slow explanation turnaround, defensible for a rare door, worth one look at whether the five-part shape is the cause.

**Verification, evidence, and uncertainty.** Inspect the three trend series and confirm recent artifacts carry the timestamps that feed them. The ceremony structure whose cost these surfaces measure read in full from both files. Reasonable trend bands for packet lead time vary widely with task mix.

**Second-order consequence.** Splitting packet lead time by declared mode count is what makes the number actionable, a rising two-mode lead time signals process friction while a rising five-mode share signals harder work arriving, the same total hiding two opposite stories.

**Revision decision.** Measure the three surfaces with trend alarms, dispatch latency per request, packet lead time split by mode count, and explanation turnaround per explainer response.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require a service-specific SLO before deployment; absent that, keep local verification to p95 under 200 ms and p99 under 500 ms for representative handlers or document why latency does not apply.
Leading indicator to measure: compile attempts and review comments decrease because the API shape is explicit before implementation.
Failure signal to watch: the reference hides ownership or error tradeoffs behind generic guidance.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what stack size, team structure, or request volume the two-door entry model must be re-derived. The seed document-scale bounds stand where the entry layer's seams should, the two-door model assumes one bundle, one team, and a human-readable request rate, and each assumption has a breaking point, bundle growth beyond the backend skill, team growth splitting dispatch authority, and request volumes demanding automated dispatch.

**Recommended default and causal basis.** Operate the two-door model as written within the assumptions, and treat each trigger as a design event for the entry layer, not a card tweak. The first seam is already visible in the sources, both files name a third skill, so the stack is at least three doors today, and every added skill multiplies the trigger-boundary surface the dispatch card must keep coherent.

**Operating conditions and assumptions.** Stack changes are noticed, skills added quietly to the archive cross the sharpest seam without any dispatch update. Bounding the two-door entry model, destination scale seams belong to the destination themes' statements.

**Failure boundary and alternatives.** Crossing seams unnoticed produces era-mismatched dispatch, a three-door card serving a five-skill stack misroutes the two unmapped skills' work into the nearest familiar door, the misdispatch failure at stack scale. Bounded alternatives and recoveries: adopting a stack-wide routing layer from the start, the routing theme's territory generalized upward, paying coordination cost early to never track the per-skill seam.

**Counterexample, gotchas, and operational comparison.** Automated dispatch at the volume seam changes the failure profile rather than removing it, description matching fails silently on novel request shapes, so the human sampling it must retain is the conformance check, not a formality. Good: a new queue-worker skill landing with its trigger boundary added to the card the same week. Bad: five skills dispatched from a card that names three. Borderline: two teams sharing dispatch informally, workable at two, the written ownership map pays for itself at three.

**Verification, evidence, and uncertainty.** At each stack or team change, re-test the three assumptions and record which seams the change crossed. The three-skill stack named across both files and the explainer's own origin story read in full. The request volume at which automated dispatch becomes necessary is team-specific.

**Second-order consequence.** The explainer is itself evidence of the first seam being crossed once already, it exists because one skill's trigger surface grew confusing enough to need a dedicated explanation door, so stack growth predictably generates meta-skills, a second-order effect stack planners should expect.

**Revision decision.** Name the three seams with their triggers, a new skill in the stack triggers card and trigger-boundary rework, split dispatch authority triggers a written trigger-ownership map, and sustained volume triggers automated description-matching with human conformance sampling.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Rust Backend Skill Entrypoint stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal that this theme's stack topology or format assumptions have gone stale. The seed theme-name queries would surface generic Rust content while this theme's staleness lives in named places, the two SKILL.md files' revision state in the archive, the third skill both files name, and the agent-platform skill-format conventions the frontmatter follows.

**Recommended default and causal basis.** Run the archive watch at every corpus refresh, the naming checks alongside it, and the format probe on a slower cadence with each finding dated. Entry-layer content dates through its stack, a revised delivery skill re-draws the mode table this document transcribes, a renamed broader skill breaks the sideways routes, and a platform format change alters how descriptions trigger, none of which a web search for the theme name would reveal.

**Operating conditions and assumptions.** Probe findings are triaged into transcription-breaking versus topology-breaking before any rewrite. Refreshing this document's stack and format claims, destination-content freshness rides on the destination themes' probes.

**Failure boundary and alternatives.** Name-based searching returns backend tutorials that neither confirm nor deny whether this stack's 202604 trigger topology still holds, the only question a refresh of this theme actually needs answered. Bounded alternatives and recoveries: pinning this document explicitly to the 202604 stack topology and marking all trigger claims archive-dated, zero probe cost, honest, and increasingly stale as the stack evolves.

**Counterexample, gotchas, and operational comparison.** The explainer's external document paths are permanently unverifiable from this checkout, probes should not keep re-attempting them, their status is a recorded boundary, not a pending fetch. Good: a probe noting a delivery-skill revision added a seventh mode and dating the affected sections. Bad: logging that rust backend skill tutorials say nothing new. Borderline: probing other archives for newer skill vintages, adjacent and unbounded, cap it to the two mapped paths and the two named skills.

**Verification, evidence, and uncertainty.** Record each probe with its date and the specific transcription or topology claim it confirmed or flagged. The stack names and archive paths read from both mapped files. Actual revision frequency of the skill archives is unknowable from within one snapshot.

**Second-order consequence.** The probes split by blast radius, a platform format change invalidates dispatch mechanics for every skill at once while an archive revision invalidates one door's transcription, so the format probe is lower frequency but stack-wide in consequence, the inverse of the usual local-versus-global ordering.

**Revision decision.** Replace the name queries with targeted probes, archive watch on both SKILL.md paths, existence and naming checks on rust-executable-specs-01 and rust-coder-02, and a dated look at current agent skill-format documentation.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | rust backend skill entrypoint official documentation best practices |
| `github_repository_query_phrase` | rust backend skill entrypoint GitHub repository examples |
| `release_notes_query_phrase` | rust backend skill entrypoint changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label and with what attached caveats each claim here may be reused. The seed label definitions stand without this assignment's ledger, two mapped SKILL.md files read fully, the delivery bundle's five reference files read fully and cited by name, zero external URLs fetched, and every dispatch-practice construct above marked as inference.

**Recommended default and causal basis.** Before reusing a claim, identify its stratum, cite file and section for facts, flag archive-dating on topology claims, and attach reasoning to inference. The fact stratum is the two files' transcribable content, the six modes, five combinations, six steps, seven contract sections, five explainer sections, nine guardrails, and the named third skill, while the cards, ratios, probes, regimes, and seams are this evolution's constructed machinery.

**Operating conditions and assumptions.** Both archive paths stay stable so fact-class claims remain mechanically checkable. Reuse rules for this document's claims, transcribed structure travels with file-and-section citations, constructed machinery travels only with its reasoning.

**Failure boundary and alternatives.** The costliest mislabel would present the two-door topology as current live fact, both files are 202604 archive snapshots, and the stack they describe may have grown or been renamed since, the topology is archive fact, not present-tense truth. Bounded alternatives and recoveries: per-paragraph inline labels if the stratum-level split ever proves too coarse for a dispute.

**Counterexample, gotchas, and operational comparison.** The mode taxonomy travels especially well and dangerously, six modes read as a universal backend classification, but they are one bundle's design choices, quoting them into another stack's context without the citation presents local design as general truth. Good: reusing the six-mode list with its delivery-skill citation. Bad: citing the dispatch card as bundle doctrine. Borderline: reusing the scope-split rationale for another language's skill stack, the logic carries, the trigger-accuracy evidence behind it does not.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares its stratum cleanly. This assignment's read ledger, seven files read in full, zero retrievals. Readers can check the citations but not the reading behind them.

**Second-order consequence.** This theme adds a stratum its siblings lack, deliberately hidden provenance, the explainer's guardrail forbids mentioning upstream sources, so part of what this document records is information its own source refuses to state, provenance recovered from the outside.

**Revision decision.** Publish the strata, countable skill-file content as local corpus fact, bundle-cited chains as by-name local fact, the explainer's unverifiable external paths as recorded boundaries, and all practice machinery as combined-evidence inference.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
