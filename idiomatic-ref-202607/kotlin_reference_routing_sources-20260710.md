# Kotlin Reference Routing Sources

**Decision supported.** This section helps decide which Kotlin reference, section, or adjacent skill should be loaded next for a named reliability boundary and evidence need. The seed title names source routing but does not yet tell a reader how to minimize context while preserving authority, boundary coverage, and a deliberate handoff for backend-specific decisions.

**Recommended default and causal basis.** Classify the task as API and type design, nullability and Java interop, coroutines and Flow, testing and fixtures, tooling and build gates, or backend delivery; use the Reference Map jump point, load only matching sections, then finish with high-value anti-patterns and the default reliability stack. Boundary-first routing reduces irrelevant context while the final cross-cutting checks catch failures that a narrow section can miss.

**Operating conditions and assumptions.** The task has a concrete Kotlin decision, the repository and dependency versions are inspectable, and the selected source owns the boundary rather than merely sharing a keyword. Apply this reference to source selection and evidence routing for an existing Kotlin task, not as a substitute for the substantive source that owns the implementation decision.

**Failure boundary and alternatives.** A generic Kotlin map is used to decide Spring, Ktor, persistence, authentication, migration, or production runtime policy, or a narrow section is read without the final anti-pattern and reliability checks. Bounded alternatives and recoveries: search the mapped reference headings directly, use the research brief to locate primary authority, inspect repo-local conventions, route backend-specific work to kotlin-backend-delivery-01, or preserve uncertainty when the owning source is unavailable.

**Counterexample, gotchas, and operational comparison.** Keyword-only routing, loading every reference, treating a dated source list as current evidence, confusing generic language reliability with backend architecture, circular generated-reference citations, and skipping local build evidence. Good: route a platform-type question to Nullability And Java Interop and inspect local Java annotations. Bad: use the generic map to invent a Ktor transaction rule. Borderline: coroutine cancellation starts in the generic reliability source but routes to backend delivery when dispatcher placement or request lifecycle is the disputed boundary.

**Verification, evidence, and uncertainty.** Record the task boundary, chosen jump point, loaded section, skipped sources, authority role, repository corroboration, final anti-pattern check, adjacent handoff if any, and the command or review observation that validates the decision. The completely read Reference Map directly defines six jump points, three section-search commands, and a three-step use order, while the research brief explicitly assigns backend framework and runtime decisions to kotlin-backend-delivery-01. The referenced heavy reliability files and public Kotlin sources were not refreshed or exercised in this evolution, so exact current APIs and repository applicability remain conditional.

**Second-order consequence.** Routing is context-budget governance: the best route minimizes loaded material while retaining enough independent checks to prevent a locally correct but systemically unsafe answer.

**Revision decision.** Turn the title into a boundary classifier, minimal-load sequence, final-check rule, backend handoff, and auditable stop condition.

**Retained seed evidence.** The exact title is retained and both mapped local sources continue to govern routing authority and research provenance. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide which source can authorize a routing decision, which can only suggest a destination, and when fresh or repository-local corroboration is required. The seed lists a canonical routing map, a supporting research brief, and four public ecosystem pointers, but it does not assign claim-level authority, retrieval state, or invalidation conditions to those different source types.

**Recommended default and causal basis.** Use the Reference Map for navigation order, the dated research brief for provenance and ownership boundaries, repository files for local applicability, and the closest current primary public source only for a version-sensitive Kotlin, coroutine, Spring, or Ktor fact when retrieval is authorized. Navigation policy, research provenance, local convention, and current framework behavior have different change rates and cannot safely share one undifferentiated authority label.

**Operating conditions and assumptions.** Every routed claim records source role, exact locator, technology boundary, version or date, retrieval state, repository corroboration, and evidence that would reverse the route. Do not mark links current without retrieval, do not let a routing source replace the destination's content, and keep observed repository evidence separate from archived policy.

**Failure boundary and alternatives.** The research brief's links are treated as freshly reviewed, the map's jump points are treated as implementation rules, Spring evidence drives Ktor work, or one generic Kotlin source overrides local build behavior. Bounded alternatives and recoveries: stay with a conditional local route, inspect the target repository and cached dependency source, request authorized primary-source retrieval, route to backend delivery, or leave a version-sensitive claim unresolved.

**Counterexample, gotchas, and operational comparison.** Dated research metadata, stale paths, renamed reliability sections, public tutorial drift, framework halo, source duplication, and counting generated summaries as independent evidence. Good: use Reference Map to choose Coroutines And Flow Reliability, then confirm installed coroutine behavior locally. Bad: cite the Kotlin home page to choose a Spring test slice. Borderline: the research brief can prove intended ownership boundaries but not July 2026 API compatibility.

**Verification, evidence, and uncertainty.** Hash both local files, confirm every mapped path and heading, record public pointers as unrefreshed, inspect repository versions, run a local behavior gate, and attach a claim-specific invalidation trigger. Both local sources were read completely; their direct facts establish navigation, search commands, use order, June 23 preparation date, primary-source categories, repo-local sources, and the explicit backend handoff. None of the four public rows was browsed in this pass, and the referenced heavier reliability files were not yet loaded for a target question.

**Second-order consequence.** The source map should route not only by topic but by evidence operation: navigate, establish provenance, inspect local convention, refresh external semantics, or escalate ownership.

**Revision decision.** Retain all six rows while adding source operation, claim scope, freshness, framework match, local proof, and invalidation metadata.

**Retained seed evidence.** The two local paths and four external URLs remain unchanged with their original evidence-category labels. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202606/kotlin-coder-01/references/reference-map.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202606/kotlin-coder-01/references/sources-and-research-brief.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| https://kotlinlang.org/docs/home.html | external_research_source_material | external_research_sourced_fact | primary language documentation for Kotlin idioms and type system |
| https://github.com/Kotlin/kotlinx.coroutines | external_research_source_material | external_research_sourced_fact | official coroutine library and implementation evidence |
| https://github.com/spring-guides/tut-spring-boot-kotlin | external_research_source_material | external_research_sourced_fact | Spring-maintained Kotlin web application example |
| https://ktor.io/docs/welcome.html | external_research_source_material | external_research_sourced_fact | primary Ktor server and client framework documentation |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which routing safeguards must be applied before a Kotlin source recommendation can guide implementation. The seed preserves 95, 91, and 88 priorities for source-first loading, evidence separation, and verification coupling, but it does not explain their dependency or warn that the numbers are not measured routing-accuracy percentages.

**Recommended default and causal basis.** Treat the three rows as an ordered mandatory chain: load the local routing corpus, separate navigation facts from refreshed external facts and inference, then couple the selected route to a repository or destination-source gate. A route without local context can be irrelevant, a route without evidence labels can inflate authority, and a route without verification can remain plausible while sending work to the wrong boundary.

**Operating conditions and assumptions.** A cold reader can reproduce the route, locate the owning source, explain why alternatives were skipped, and observe a gate that would reject a mismatched destination. Preserve the inherited scores as ordinal metadata and prohibit claims of empirical effectiveness until a documented study exists.

**Failure boundary and alternatives.** 95 is reported as accuracy, high priority excuses omitting final anti-pattern checks, all three rows become checkboxes, or the route is evaluated by source count rather than decision quality. Bounded alternatives and recoveries: use unnumbered mandatory gates, add an explicit context-budget criterion, recalibrate ordinal priority from reviewer samples, or route high-risk contradictions to an owner without changing inherited scores.

**Counterexample, gotchas, and operational comparison.** False precision, citation quantity, generated-reference circularity, local-source loading with no boundary classifier, verification that only checks file existence, and outcome sampling from one topic. Good: load the map, label the research brief as dated provenance, and verify a nullability route against local Java annotations. Bad: claim a 95 percent chance the chosen source is correct. Borderline: a tiny style question still needs a source locator but may use a lightweight review gate.

**Verification, evidence, and uncertainty.** Sample downstream tasks across all six map boundaries, retain expected and actual destination, reviewer verdict, route latency, loaded context, rejected alternatives, and whether destination evidence changed the answer. The three identifiers, scores, tiers, and failure-prevention labels are direct seed facts. The seed contains no scoring rubric, sample frame, calibration history, or statistical interpretation for numeric gaps.

**Second-order consequence.** The scoreboard is an ordering constraint for routing hygiene; actual route quality must be measured separately through reproducibility, context efficiency, and correction rate.

**Revision decision.** Explain the dependency chain, distinguish priority from performance, and add outcome-based calibration plus a context-budget check.

**Retained seed evidence.** The original 95, 91, and 88 rows and default-adoption tiers remain exactly visible. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `kotlin_reference_routing_sources` | 95 | default_adoption_pattern_tier | Source Map First: Load local kotlin reference material before synthesizing routing sources recommendations. |
| `kotlin_reference_routing_sources` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `kotlin_reference_routing_sources` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide how to turn local navigation and provenance evidence into a defensible next-source choice rather than a generic source-reading sequence. The seed states local-first, public-second, verification-backed synthesis, but it does not explain the routing decision that connects a task boundary to one destination, limited context, and a return or escalation condition.

**Recommended default and causal basis.** Use the Reference Map to classify the reliability boundary and reading order, use the research brief to constrain authority and backend ownership, inspect the repository for applicability, and refresh only the public mechanism that remains version-sensitive. Each evidence layer answers a different routing question, so sequencing them prevents broad research from displacing the nearest owning source.

**Operating conditions and assumptions.** The task names one decision, the chosen local jump point owns it, external retrieval is tied to one stale mechanism, and verification can confirm or reject the route. Preserve the distinction among navigation, provenance, repository evidence, refreshed public fact, and inference, and do not imply destination content was loaded when it was not.

**Failure boundary and alternatives.** Local-first means loading every local file, public-second becomes mandatory browsing without a freshness trigger, or combined inference hides that its destination source was never read. Bounded alternatives and recoveries: stay with a provisional map route, search exact headings, inspect repo-local build and code, route backend work to its dedicated skill, or defer a disputed version claim.

**Counterexample, gotchas, and operational comparison.** Topic overlap, context saturation, research loops, authority laundering through synthesis, map paths that drift, and verification performed against the routing file instead of destination behavior. Good: route a sealed-outcome design to API And Type Design, consult current Kotlin semantics only if needed, and compile local usage. Bad: browse all four public surfaces before identifying the question. Borderline: a Flow cancellation issue may require both generic coroutine semantics and backend request-lifecycle guidance.

**Verification, evidence, and uncertainty.** Record boundary classification, map jump point, research-brief ownership note, destination section read, repository observation, external freshness status, final gate, and route invalidation condition. The local map directly specifies jump points, searches, and use order; the brief directly specifies primary categories, repo-local inputs, generic scope, and backend exclusion. The destination reliability sections, current public pages, and target repository were not examined for a concrete implementation claim.

**Second-order consequence.** Routing quality is demonstrated when each added evidence layer either narrows uncertainty, changes destination, or increases confidence; unchanged decorative citations should be omitted.

**Revision decision.** Replace the abstract thesis with a task-to-boundary-to-source-to-local-proof chain plus stop, defer, and backend-handoff branches.

**Retained seed evidence.** The original three evidence labels and local-first ordering remain retained as the synthesis vocabulary. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `kotlin_reference_routing_sources` contains 2 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Kotlin Reference Routing Sources comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide which local routing source should be consulted first and what additional local artifact must be loaded to answer the substantive Kotlin question. The seed maps two short local files and their heading signals, but it does not show their complementary authority, the heavy destinations named by the map, or the limits of using either file without a target decision.

**Recommended default and causal basis.** Use Reference Map as canonical navigation for jump points, exact heading searches, and use order; use Sources And Research Brief as supporting provenance for primary-source scope, repo-local influences, and the generic-versus-backend ownership boundary. The map optimizes where to look while the brief explains why those destinations and exclusions are credible; neither contains the full reliability implementation guidance.

**Operating conditions and assumptions.** A reader can cite an exact heading from one of the two files, open the named destination section, and identify which local or public corroboration remains necessary. Do not promote supporting provenance to canonical navigation, do not call the two files a complete Kotlin corpus, and do not infer repository applicability without inspection.

**Failure boundary and alternatives.** The two files are treated as duplicate tutorials, the research brief outranks the map for navigation, the map substitutes for destination content, or generic Kotlin scope absorbs backend-specific responsibilities. Bounded alternatives and recoveries: search the named reliability references with the provided rg commands, inspect kotlin-backend-delivery-01 for backend work, consult repository conventions, or request a new supporting source when the map omits a boundary.

**Counterexample, gotchas, and operational comparison.** Stale relative paths, missing heavy reference files, title-only loading, repo-local sources from other languages generalized without evidence, and the dated brief mistaken for current external verification. Good: use the map's Testing And Fixtures jump point, then open the actual testing section and inspect local test conventions. Bad: cite the jump-point bullet as complete fixture guidance. Borderline: generic build-gate routing can use the map, but framework plugin tasks may belong to backend delivery.

**Verification, evidence, and uncertainty.** Hash and reread both local files, check every heading and command, confirm each named destination exists before routing, compare generic versus backend scope, and record uncovered or conflicting boundaries. The 26-line map and 25-line brief were read completely and directly establish their titles, headings, commands, sources, date, synthesis notes, and backend handoff. The heavy destination references and the three listed repo-local skill trees were not completely read for this routing-only evolution.

**Second-order consequence.** A good local map is intentionally incomplete: its quality comes from fast, accurate indirection and explicit ownership boundaries, not from duplicating destination guidance.

**Revision decision.** Expand both rows into role, direct coverage, named destinations, omissions, conflict handling, and proof-of-existence requirements.

**Retained seed evidence.** Both original paths, titles, heading signals, and skill-entrypoint usage roles remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202606/kotlin-coder-01/references/reference-map.md | Reference Map | Reference Map; Jump Points; Section Search; Use Order | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202606/kotlin-coder-01/references/sources-and-research-brief.md | Sources And Research Brief | Sources And Research Brief; Primary Sources; Repo-Local Sources; Synthesis Notes | skill entrypoint or reusable agent prompt |

## External Research Source Map

**Decision supported.** This section helps decide which primary external surface should be consulted after local routing identifies a version-sensitive language, runtime, or framework mechanism. The seed retains Kotlin, coroutine, Spring, and Ktor public locations but does not say that none was refreshed here or specify which routing uncertainty each can resolve.

**Recommended default and causal basis.** Route Kotlin syntax, type, convention, nullability, interop, and sealed-type questions to official Kotlin documentation; coroutine scheduling, cancellation, and exceptions to the official coroutine project; Spring and Ktor behavior only to the framework matching the repository. Primary-source specificity reduces version and framework transfer errors while local routing prevents broad external research from preceding the actual question.

**Operating conditions and assumptions.** Retrieval is authorized, installed versions are known, the exact claim and primary domain are recorded, and a local compile or behavior check corroborates the result. Never claim freshness from URL presence, never transfer framework authority, and scope public facts to the exact version and mechanism reproduced.

**Failure boundary and alternatives.** Public sources are loaded before local classification, a home page substitutes for a claim locator, the Spring guide governs Ktor, or an unbrowsed URL is labeled current evidence. Bounded alternatives and recoveries: use locally cached docs or dependency source, preserve a provisional route, inspect repository behavior, request targeted retrieval, or route to backend delivery when architecture rather than API semantics is disputed.

**Counterexample, gotchas, and operational comparison.** Version drift, tutorial simplification, evolving coroutine APIs, framework sample bias, release documentation gaps, and confusion between library repository state and released dependency behavior. Good: after a cancellation route, identify the installed coroutine version, retrieve primary semantics, and reproduce locally. Bad: use a Spring tutorial to select a generic Kotlin nullability pattern. Borderline: official Kotlin docs establish value-class semantics but serializer and ORM compatibility need framework and repository evidence.

**Verification, evidence, and uncertainty.** Retain the freshness trigger, exact source and locator, version, retrieval date, rejected results, paraphrased fact, local reproducer, routing change, and next invalidation condition. The seed directly assigns distinct external roles to the four retained locations, and the research brief names narrower official Kotlin pages for several generic reliability topics. Current July 2026 contents and compatibility were not retrieved, so external rows remain routing pointers rather than verified claims.

**Second-order consequence.** External research should resolve one residual uncertainty after local routing; if it does not change destination or confidence, it is likely unnecessary context.

**Revision decision.** Retain all rows while adding claim domain, local prerequisite, freshness state, version match, reproducer, and reject conditions.

**Retained seed evidence.** The four original public URLs, names, roles, and external evidence labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://kotlinlang.org/docs/home.html | Kotlin documentation | primary language documentation for Kotlin idioms and type system | external_research_sourced_fact |
| https://github.com/Kotlin/kotlinx.coroutines | Kotlin coroutines repository | official coroutine library and implementation evidence | external_research_sourced_fact |
| https://github.com/spring-guides/tut-spring-boot-kotlin | Spring Boot Kotlin guide | Spring-maintained Kotlin web application example | external_research_sourced_fact |
| https://ktor.io/docs/welcome.html | Ktor documentation | primary Ktor server and client framework documentation | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which routing smells should block a Kotlin source recommendation before a reader invests context or changes code. The seed records context-free summaries, unsourced claims, and unverified instructions, but source routing also fails through overloading, keyword matches, circular authority, stale paths, and ownership leakage.

**Recommended default and causal basis.** Retain the three provenance failures and add checks for load-everything behavior, keyword-only destination choice, map-as-content substitution, dated-link freshness claims, generic-to-backend scope leakage, and generated-reference circularity. A wrong route can produce polished, well-cited guidance that never reaches the source owning the actual reliability boundary.

**Operating conditions and assumptions.** Each smell has an observable routing trace, causal risk, bounded exception, safer replacement, and a test task that demonstrates the corrected route. Apply smell detection to the route and claim under review, allowing justified multi-source plans with explicit subdecision ownership.

**Failure boundary and alternatives.** The registry becomes a preference against broad reading, rejects legitimate multi-boundary tasks, forbids secondary sources entirely, or flags a stale path without checking whether an equivalent destination exists. Bounded alternatives and recoveries: load two sources in primary-supporting roles, accept a broad orientation pass for an unfamiliar task, use a repository-local equivalent, preserve a broken-link warning, or escalate an ambiguous ownership boundary.

**Counterexample, gotchas, and operational comparison.** Silent fallback to memory, repeated generated summaries, map paths copied without existence checks, public documentation loaded for authority theater, backend handoff ignored, and final anti-pattern checks omitted. Good: reject a map-only answer and open the named nullability section plus local annotations. Bad: load all Kotlin references and choose by term frequency. Borderline: a cross-cutting migration may require several sections, but each must own a named subdecision.

**Verification, evidence, and uncertainty.** Review source-opening order, loaded bytes or sections, route rationale, path existence, evidence labels, destination ownership, rejected alternatives, final anti-pattern pass, and seeded misrouting scenarios. The local map directly warns through its selective use order, while the brief directly limits generic scope and routes backend concerns elsewhere. Acceptable context size, multi-source depth, and legacy-path tolerance depend on task risk and repository scale.

**Second-order consequence.** Routing anti-patterns should be ranked by decision distortion, not context volume alone; extra context is costly, but missing the owning source is the more serious failure.

**Revision decision.** Keep the three seed rows and add routing-specific signals, exceptions, correction paths, and misroute tests.

**Retained seed evidence.** Context-free summary, unsourced recommendation, and unverified instruction remain the inherited registry foundation. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide which structural, discovery, destination, and local-behavior gates must pass before a routing recommendation is accepted. The seed includes the archived generated-reference verifier, while the canonical map supplies three rg searches, but neither command class alone proves that a routed destination owns or answers a concrete Kotlin decision.

**Recommended default and causal basis.** Run the focused reference verifier for artifact invariants, use the map's exact heading searches to locate candidate destinations, confirm the destination path and ownership, then run the target repository's compile, test, or review gate for the claimed behavior. Artifact structure, source discoverability, semantic ownership, and local applicability are independent failure surfaces.

**Operating conditions and assumptions.** Every command is run from its required directory, returns the expected locator or behavior, and has an explicit pass, fail, unavailable, or not-applicable outcome. Never claim an unrun gate passed, never substitute structural success for semantic correctness, and preserve unavailable outcomes.

**Failure boundary and alternatives.** The archive generator proves route correctness, rg matches are treated as semantic evidence, a missing path silently falls back to memory, or a destination is cited without running the repository gate. Bounded alternatives and recoveries: report a missing command, use the repository's documented equivalent, inspect a local source manually, preserve provisional status, or stop when no substantive destination can be verified.

**Counterexample, gotchas, and operational comparison.** Relative-path drift, shell glob expansion, headings inside code fences, duplicate section names, global Gradle or Maven replacing wrappers, stale generated indexes, and filtered tests used as final evidence. Good: rg locates Coroutines And Flow Reliability, the destination is read, and a local cancellation test runs. Bad: the document verifier passes and the agent declares a routing conclusion correct. Borderline: a path-existence gate can validate navigation but not the implementation guidance within the file.

**Verification, evidence, and uncertainty.** Record exact command, working directory, exit code, matched heading or test, expected failure where relevant, exclusions, unavailable dependencies, and the claim each gate permits. The local map directly provides three heading searches, and the seed directly retains the final-stage generated-reference command. The mapped destination files' current paths and any target repository wrapper commands must be inspected in context.

**Second-order consequence.** Verification should follow the same indirection as routing: prove the map, then destination ownership, then local behavior; skipping a hop narrows the warranted claim.

**Revision decision.** Retain the inherited command while separating artifact, discovery, ownership, destination-content, and local-behavior gates.

**Retained seed evidence.** The final-stage archive verifier remains preserved for the generated-reference pipeline, with the map's search commands retained in local evidence. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide how an agent should move from a Kotlin request to the smallest authoritative context packet and know when routing is complete. The seed tells agents to start local, prefer gates, use external sources for freshness, and preserve labels, but it does not specify a reproducible route-selection workflow or stopping rule.

**Recommended default and causal basis.** State the decision and boundary, inspect repository technology, read Reference Map scoreboard, load only matching destination sections, consult the research brief for authority and scope, verify locally, then finish with anti-pattern and default-stack checks. A fixed sequence makes omissions and unnecessary context visible while leaving room for task-specific evidence.

**Operating conditions and assumptions.** The agent can name the destination's ownership, the evidence packet stays bounded, and a concrete gate confirms that the selected guidance applies. Use this guide to select and validate context, not to implement substantive guidance before its owning destination is read.

**Failure boundary and alternatives.** The task is still ambiguous, no destination owns it, the map path is missing, backend architecture is being decided by generic sources, or external freshness cannot be established for a critical mechanism. Bounded alternatives and recoveries: ask for a sharper decision, inspect adjacent sources, use repository characterization, route to backend delivery, preserve a provisional recommendation, or stop for an owner.

**Counterexample, gotchas, and operational comparison.** Opening search results before local maps, loading full corpora, forgetting skipped-source reasons, changing frameworks while routing, citing source lists without reading destinations, and omitting return criteria after escalation. Good: route a Java platform-type bug through nullability guidance and local annotations, then run compilation. Bad: produce a Kotlin style essay from the research brief alone. Borderline: an endpoint coroutine bug may require generic cancellation semantics plus backend lifecycle guidance under explicit ownership.

**Verification, evidence, and uncertainty.** Require a routing record with question, boundary, repository signals, local map heading, destination, supporting source, skipped alternatives, evidence labels, command, stop condition, and unresolved uncertainty. The canonical map directly defines selective use order, and the supporting brief directly defines generic scope, primary sources, repo-local synthesis, and backend ownership. The best context budget and whether adjacent guidance is stronger remain task-specific judgments.

**Second-order consequence.** Agent autonomy improves when routing has negative decisions: recording why a plausible source was not loaded prevents both context creep and later repetition.

**Revision decision.** Convert the seed bullets into classify-inspect-route-load-verify-finish-or-escalate steps with negative-source decisions.

**Retained seed evidence.** All four original local-first, gate, freshness, and evidence-label bullets remain retained. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide how a Kotlin maintainer or agent should find the authoritative next source quickly enough to act without losing evidence boundaries. The seed frames a generic backend builder journey even though this theme's actual user is choosing sources and context before substantive Kotlin work begins.

**Recommended default and causal basis.** Capture the user's concrete question and repository surface, classify it against the map's six reliability boundaries, open the smallest matching destination, consult provenance only where authority is uncertain, and return a route record plus verification gate. The user's immediate problem is not choosing a backend pattern in the abstract but reducing source uncertainty into an executable next reading or inspection action.

**Operating conditions and assumptions.** The user has one primary decision, can inspect repository technology, and can access at least one mapped or adjacent substantive source. Assume one audience, one primary Kotlin decision, and authorized access only to local sources unless external retrieval is explicitly permitted.

**Failure boundary and alternatives.** The request spans unrelated systems, asks for current facts without retrieval permission, lacks repository context, or needs backend architecture beyond the generic Kotlin skill. Bounded alternatives and recoveries: narrow the question, perform an orientation pass, characterize repository behavior, route to backend delivery, ask an owner, or produce a provisional source shortlist with explicit uncertainty.

**Counterexample, gotchas, and operational comparison.** Starting from favorite docs, confusing an audience request with a technical boundary, measuring success by links collected, loading stale duplicates, and never returning from source research to the implementation decision. Good: a maintainer debugging Java interop opens the nullability section, inspects annotations, and compiles. Bad: read every Kotlin source and summarize them. Borderline: a request mixing Flow and Ktor lifecycle is split into generic cancellation and backend request-scope subdecisions.

**Verification, evidence, and uncertainty.** Review the journey for actor, starting evidence, boundary classifier, selected and skipped sources, destination ownership, local gate, stop or escalation condition, elapsed reviewer time, and final next action. The map's jump points and use order directly support this route journey, and the brief's synthesis notes directly define generic versus backend scope. Actual repository structure, destination availability, dependency versions, and user risk tolerance are absent from the seed.

**Second-order consequence.** A successful routing journey ends when the user can stop reading and act; a longer bibliography without an implementation-relevant decision is a failure.

**Revision decision.** Replace the generic backend opening with a source-uncertainty journey from question capture through minimal context, local proof, and handoff.

**Retained seed evidence.** The inherited role, starting state, decision, and opening trigger remain preserved beneath the routing-specific journey. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The Kotlin backend builder is starting from a service, route, or worker that must become production-safe and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `kotlin_reference_routing_sources` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing the smallest reliable backend pattern that preserves coroutine, framework, and operational discipline.
Reference opening trigger: Open this file when the task mentions kotlin reference routing sources, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide whether to follow the canonical route unchanged, adapt it to repository evidence, avoid the map for this question, or escalate because a misroute has high consequence. The seed provides Adopt, Adapt, Avoid, and Cost of being wrong rows centered on evidence agreement, but it does not compare route precision, context cost, freshness work, destination ownership, and reversibility.

**Recommended default and causal basis.** Adopt when a jump point owns the decision and paths are valid, adapt when repository structure or versions differ, avoid when the task lies outside generic Kotlin reliability, and escalate when a wrong source could authorize security, data, concurrency, or migration behavior. Routing trades search time and context volume against the much larger cost of implementing advice from the wrong authority.

**Operating conditions and assumptions.** Each option is compared against one named question and produces a selected destination, rejected alternatives, local gate, and rollback or re-route path. Evaluate viable sources within the target repository and technology, and keep escalation proportional to real downstream harm.

**Failure boundary and alternatives.** Adopt means blind map compliance, Adapt quietly changes ownership, Avoid ends without a route, or high-cost language is used to justify unlimited research. Bounded alternatives and recoveries: use a two-source primary-supporting packet, inspect repository code first, route directly to backend delivery, request fresh primary evidence, or defer only the disputed claim.

**Counterexample, gotchas, and operational comparison.** Assuming canonical means current, adapting paths without documenting drift, ignoring map exclusions, external research loops, and irreversible implementation begun before destination verification. Good: adapt a missing relative path to the repository's equivalent section and record the mapping. Bad: adopt the map's generic coroutine route for a database transaction question. Borderline: two destinations are justified when one owns language semantics and one owns framework lifecycle.

**Verification, evidence, and uncertainty.** Compare options on ownership fit, freshness, context cost, route reversibility, local evidence, consequence of error, and whether a seeded contrary task is correctly rejected. The seed directly preserves the four decision options and their evidence questions, while the local sources support selective routing and backend exclusion. Route cost, acceptable context, and consequence thresholds require local measurement and domain ownership.

**Second-order consequence.** The safest route is not always the shortest; high-consequence boundaries justify one corroborating source, but every added source needs a distinct decision role.

**Revision decision.** Make each seed option state route-fit, context, freshness, ownership, reversibility, and evidence consequences.

**Retained seed evidence.** All four original option rows and reviewer questions remain retained for provenance. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the kotlin reference routing sources pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong kotlin reference routing sources guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which local routing statement controls when the two sources agree, diverge, age, duplicate another file, or point to a missing destination. The seed correctly assigns canonical status to Reference Map and supporting status to Sources And Research Brief, but it does not define conflict handling, freshness, duplicate detection, or promotion rules.

**Recommended default and causal basis.** Let Reference Map control navigation and use order, let the research brief explain provenance and scope, treat repository observations as applicability evidence, and escalate contradictions instead of silently changing either source's role. Canonical navigation and supporting rationale can disagree for legitimate reasons such as path drift or a later repository convention, and authority must remain explicit during resolution.

**Operating conditions and assumptions.** Each source has a role, hash, date or freshness signal, direct heading, downstream destination, conflict note, and reviewer question. Do not change hierarchy from this generated reference alone and scope repository observations to the repository observed.

**Failure boundary and alternatives.** The supporting brief overrides navigation, a generated reference becomes a third authority, a passing repository pattern is generalized beyond its project, or stale and duplicate files are counted as corroboration. Bounded alternatives and recoveries: preserve both interpretations, select a current repository-local path while retaining policy conflict, consult destination owners, characterize behavior, or propose a reviewed hierarchy update.

**Counterexample, gotchas, and operational comparison.** Circular references, identical copies with new filenames, path drift without semantic change, old dates mistaken for invalidity, local exceptions mistaken for policy, and unresolved ownership of heavy references. Good: keep the map canonical while adapting a renamed destination path with repository evidence and owner review. Bad: call this generated file independent confirmation. Borderline: the dated brief remains valid for ownership boundaries even if one external URL requires refresh.

**Verification, evidence, and uncertainty.** Hash both sources, compare role-specific claims, validate destination paths and headings, identify duplicates, record conflicts, run representative routes, and require owner approval for role changes. The seed hierarchy and complete local files directly establish one canonical navigation source and one supporting provenance source. No legacy, duplicate, or conflicting additional local prose sources were mapped, and destination ownership has not been audited across a target repository.

**Second-order consequence.** A two-source hierarchy is strongest when it preserves asymmetry: the map answers where and in what order, while the brief answers why, from what evidence, and where scope stops.

**Revision decision.** Define canonical, supporting, legacy, duplicate, conflicting, and repository-observation roles with conflict, promotion, and deprecation procedures.

**Retained seed evidence.** The original classification vocabulary and both hierarchy rows remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202606/kotlin-coder-01/references/reference-map.md | canonical primary source | Reference Map; Jump Points; Section Search | What guidance, warning, or example should this source contribute to Kotlin Reference Routing Sources? |
| agents-used-monthly-archive/codex-skills-202606/kotlin-coder-01/references/sources-and-research-brief.md | supporting detail source | Sources And Research Brief; Primary Sources; Repo-Local Sources | What guidance, warning, or example should this source contribute to Kotlin Reference Routing Sources? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what artifact must be produced so another reader can reproduce, challenge, and update a Kotlin source route. The seed names canonical source routing rules for stale, duplicate, and conflicting sources, but its three generic fields do not make route selection, evidence movement, or conflict resolution reviewable.

**Recommended default and causal basis.** Create a routing ledger with user decision, repository boundary, candidate source, hierarchy role, exact locator, freshness state, duplicate fingerprint, conflict summary, selected destination, skipped alternatives, context budget, verification gate, owner, invalidation trigger, and return action. Routing decisions decay as paths, versions, ownership, and questions change; a compact ledger preserves the causal basis without reloading the entire research process.

**Operating conditions and assumptions.** One row resolves one subdecision, links to substantive destination evidence, distinguishes direct fact from inference, and provides a reversible re-route when assumptions fail. Scale the artifact to decision risk and source count; do not require a large matrix for one obvious local locator.

**Failure boundary and alternatives.** The artifact is a bibliography, labels stale sources without a criterion, merges conflicts into consensus prose, counts duplicate copies as support, or omits skipped-source reasoning. Bounded alternatives and recoveries: use an inline route note for a trivial decision, a source matrix for several candidates, a conflict log for policy disagreement, or a handoff packet for an adjacent owner.

**Counterexample, gotchas, and operational comparison.** URL-only identifiers, mutable branch links, hashes without semantic locators, missing retrieval dates, unbounded candidate lists, personal preference presented as ranking, and stale routes with no owner. Good: a coroutine row records map locator, installed version, one official semantic source, local reproducer, rejected backend route, and refresh trigger. Bad: list ten links and choose none. Borderline: duplicate files can share one evidence role if a canonical copy and deprecation path are explicit.

**Verification, evidence, and uncertainty.** Have a cold reader reproduce the route from the ledger, test path and locator existence, hash duplicates, exercise a conflicting task, run the destination gate, and confirm invalidation causes a controlled re-route. The seed directly requires stale, duplicate, and conflicting source rules, and the two local files supply navigation, provenance, and scope inputs. Ownership metadata, source lifecycle policy, and acceptable context budget vary by repository and organization.

**Second-order consequence.** The routing ledger is a cache with invalidation semantics: explicit freshness and conflict triggers turn context reuse from convenience into controlled evidence management.

**Revision decision.** Expand the three seed fields into a complete routing ledger schema with hierarchy, freshness, duplication, conflict, budget, gate, owner, and re-route controls.

**Retained seed evidence.** The original user-goal, decision-boundary, and verification-gate rows remain the ledger's top-level contract. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: canonical source routing rules for stale, duplicate, and conflicting sources.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Kotlin Reference Routing Sources | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide how to recognize a source route that is minimal and authoritative, misleading despite citations, or valid only with a supporting destination. The seed's good, bad, and borderline examples describe generic source discipline but do not demonstrate different routing outcomes for API design, interop, coroutines, testing, build gates, and backend work.

**Recommended default and causal basis.** Make each example name the request, boundary classification, repository signals, selected and rejected sources, loaded locator, evidence roles, local gate, and residual uncertainty. Routing examples teach transferable judgment only when the difference between topic similarity and decision ownership is visible.

**Operating conditions and assumptions.** The good case reaches an owning source with bounded context, the bad case reveals a specific misroute and consequence, and the borderline case shows why two sources or an escalation are needed. Use examples as decision templates, never as permission to skip reading the owning destination or inspecting repository versions.

**Failure boundary and alternatives.** Examples merely label local good and external bad, omit destination content, assume one framework, or praise minimal context even when a high-risk corroboration is necessary. Bounded alternatives and recoveries: use paired route traces, one example per jump point, a stale-versus-current comparison, a duplicate-source conflict, or a generic-to-backend handoff.

**Counterexample, gotchas, and operational comparison.** Examples tailored to filenames rather than decisions, hidden repository assumptions, URL prestige, map bullets copied as substantive advice, and success defined by fast retrieval instead of correct action. Good: route a Java platform type to Nullability And Java Interop, inspect annotations, and compile. Bad: use the research brief's source list as the answer. Borderline: a Ktor Flow cancellation bug needs generic coroutine semantics plus backend request-lifecycle ownership, with separate claims.

**Verification, evidence, and uncertainty.** Seed an alternative destination or stale path, ask a cold reader to reproduce each route, run the stated local gate, and confirm the bad route misses or distorts the intended decision. The local map directly supplies six example boundaries and explicit backend handoff evidence comes from the research brief. Exact destination file contents and repository APIs must be adapted rather than inferred from example labels.

**Second-order consequence.** A worked routing example is a negative-space map: it should teach which plausible source cannot decide the question and why.

**Revision decision.** Replace generic labels with boundary-specific route traces, explicit misroute consequences, and complementary-source handoffs.

**Retained seed evidence.** The original good, bad, and borderline source-discipline sentences remain retained after the evolved route examples. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Kotlin Reference Routing Sources after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Kotlin Reference Routing Sources as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Kotlin Reference Routing Sources only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which signals reveal whether source routing improves the next decision without becoming a fast path to the wrong authority. The seed gives one-session lead time, guessed framework or coroutine behavior as a failure signal, and a refresh cadence, but it does not measure route correctness, context efficiency, or correction quality.

**Recommended default and causal basis.** Baseline time to correct next action, first-route acceptance, reviewer correction rate, destination ownership accuracy, context loaded, stale-path detection, duplicate suppression, unresolved conflict age, and downstream rework caused by misrouting. Speed and minimal context can improve while correctness declines, so outcome measures must balance efficiency, authority fit, and reversibility.

**Operating conditions and assumptions.** Metrics use defined tasks and reviewers, distinguish first route from corrected route, compare against a retained baseline, and trigger an owned map or brief update. Do not compare unrelated repositories or infer causal improvement from one sample, and separate navigation quality from destination-content quality.

**Failure boundary and alternatives.** Link count or page views become success, the same boundary dominates samples, research time excludes correction, fast misroutes are rewarded, or arbitrary targets are presented as universal evidence. Bounded alternatives and recoveries: use qualitative cold-reader reviews in a small corpus, sample route traces, run seeded stale-path scenarios, measure only correction reasons, or establish thresholds after baseline collection.

**Counterexample, gotchas, and operational comparison.** Selection bias, ambiguous gold destinations, correlated reviewers, hidden repository familiarity, context bytes confused with useful evidence, and route changes caused by source quality rather than navigation. Good: reviewers find interop guidance quickly and rarely re-route after local compilation. Bad: average retrieval time falls because agents stop at the map. Borderline: a higher context load is justified for a cross-boundary migration if correction and rework decrease.

**Verification, evidence, and uncertainty.** Retain task set, expected owner, route trace, loaded context, elapsed decision time, reviewer verdict, correction cause, downstream action, and refresh triggered by each miss. The seed directly names a leading indicator, failure signal, and refresh cadence, while local sources support selective loading and backend scope separation. Acceptable route latency, context size, sample count, and correction rate require local calibration.

**Second-order consequence.** The strongest feedback loop updates the classifier or ownership boundary after each misroute, rather than merely adding another source to the index.

**Revision decision.** Retain inherited signals while adding balanced accuracy, efficiency, staleness, duplication, conflict, correction, and rework measures.

**Retained seed evidence.** The original one-session indicator, guessed-behavior failure signal, and edit-time refresh cadence remain visible for calibration. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: lead time from requirement to verified endpoint stays under one focused implementation session.
Failure signal: framework choice or coroutine behavior is guessed instead of verified with tests and docs.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide whether this routing reference and a downstream route ledger are complete enough for another reader to act without reconstructing the search. The seed checks scenario, tradeoffs, hierarchy, artifact, examples, metrics, and adjacency, but it does not prove that a route reaches an owning destination with bounded context and an executable local gate.

**Recommended default and causal basis.** Require locatable evidence for question and boundary, repository signals, canonical map heading, substantive destination, supporting provenance, source hierarchy, freshness, skipped alternatives, conflict handling, context budget, local verification, stop or escalation, and refresh trigger. A checklist prevents routing omissions only when every item points to a concrete ledger field, source locator, or command result.

**Operating conditions and assumptions.** A cold reader selects the same destination, explains rejected candidates, runs the same gate, and knows when to stop or re-route. Scale depth to risk while preserving source roles, command honesty, and explicit not-applicable reasoning.

**Failure boundary and alternatives.** Items are self-attested, a map heading counts as destination content, not-applicable fields lack reasons, or document structure is confused with correct source ownership. Bounded alternatives and recoveries: use a compact route note for one obvious source, a matrix for multiple candidates, a conflict log for hierarchy disagreement, or stop when no owning destination exists.

**Counterexample, gotchas, and operational comparison.** Missing skipped-source rationale, external freshness assumed, destination path unverified, backend handoff absent, final anti-pattern check omitted, and unresolved conflict without owner. Good: every checklist item links to a route ledger row, exact destination heading, local compile gate, and invalidation trigger. Bad: mark complete because all mapped files exist. Borderline: external freshness is not applicable to a stable local naming rule only when no current API claim is made.

**Verification, evidence, and uncertainty.** Automate counts and path checks, manually review ownership and inference, perform a cold-reader replay, run exact commands, and reject evidence that cannot be located. The seed directly supplies seven reference-level checks, and the local map plus brief supply route-order and scope obligations. Some fields may be unnecessary for a low-risk local question, requiring justified exclusion rather than forced research.

**Second-order consequence.** Completeness is evidence locality: a route that is correct but cannot be reproduced is fragile organizational memory.

**Revision decision.** Retain all seven checks and add destination ownership, context bound, skipped alternatives, local proof, conflict owner, and cold-reader replay.

**Retained seed evidence.** Every original checklist sentence remains retained beneath the operational routing gates. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Kotlin Reference Routing Sources.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide when this source-routing reference stays primary and when the substantive question should move to the Kotlin playbook, testing fixtures, Spring/Ktor idioms, or another specialist. The seed names generic Kotlin, reference, and routing neighbors plus runtime, security, and testing guidance, but it does not define decision triggers, handoff payloads, or a return path.

**Recommended default and causal basis.** Stay here while classifying sources, hierarchy, freshness, duplicates, and conflicts; route service architecture and operations to the Kotlin backend playbook, fixture design to Kotlin backend testing fixtures, and framework-specific mechanics to Spring/Ktor idioms. This file owns context selection, not the full decisions made by its destinations, so explicit handoffs prevent routing metadata from becoming accidental implementation policy.

**Operating conditions and assumptions.** The unresolved decision is named, the destination owns it, the route ledger and evidence travel with the handoff, and a return artifact updates or confirms the route. Describe and validate routes without opening later assignment content before the current assignment is Refactor-complete.

**Failure boundary and alternatives.** Routing is keyword-driven, creates loops, sends generic language questions to backend references, drops prior uncertainty, or uses adjacency to avoid opening a substantive source. Bounded alternatives and recoveries: use primary-supporting references, split a cross-boundary question, request an owner, continue nonblocking route rows, or hold a provisional route until the destination is available.

**Counterexample, gotchas, and operational comparison.** Coroutine topics that cross generic and backend scope, testing questions without known contracts, framework docs changing architecture, duplicate destinations, and handoffs without completion criteria. Good: route Ktor plugin configuration to Spring/Ktor idioms and return with a versioned behavior decision. Bad: use this routing file to invent plugin configuration. Borderline: cancellation semantics remain generic until request lifecycle or dispatcher ownership makes backend guidance necessary.

**Verification, evidence, and uncertainty.** Record origin, trigger, destination, owning question, evidence carried, expected return artifact, owner, stop condition, route update, and loop detection. The research brief directly identifies backend delivery as the owner of runtime, persistence, auth, migrations, Spring Boot, and Ktor, while the authorized batch names adjacent Kotlin references. Specialist inventory outside the authorized Kotlin set and repository-specific ownership may differ.

**Second-order consequence.** Adjacent routing is successful only when it returns a smaller uncertainty and a concrete decision; one-way handoffs merely move context loss elsewhere.

**Revision decision.** Replace generic adjacent suggestions with decision triggers, named Kotlin destinations, payloads, loop guards, and return criteria.

**Retained seed evidence.** The original runtime, security, testing, Kotlin, reference, and routing adjacency language remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use runtime, security, or testing Kotlin references when the question is about day-two operation, abuse cases, or fixtures.
Adjacent reference 1: consider the kotlin adjacent reference when the current task pivots away from kotlin reference routing sources.
Adjacent reference 2: consider the reference adjacent reference when the current task pivots away from kotlin reference routing sources.
Adjacent reference 3: consider the routing adjacent reference when the current task pivots away from kotlin reference routing sources.

## Workload Model

**Decision supported.** This section helps decide how to bound and partition a source-routing task before candidate growth turns synthesis into an unranked list. The seed bounds one audience, one decision, up to 12 source documents, and one verification checklist, but it does not explain how boundary count, source overlap, freshness work, and context size affect routing capacity.

**Recommended default and causal basis.** Model audience, decision count, Kotlin boundaries, repositories, candidate sources, versions, freshness checks, conflicts, context budget, reviewer ownership, and required gates before loading sources. Routing cost grows with independent decisions and conflicting authority, not merely document count, and a dozen short sources may be easier than three overlapping frameworks.

**Operating conditions and assumptions.** One reviewer can rank candidates for one primary decision, every source has a role, context remains within a declared budget, and conflicts have owners. Retain the 12-document figure as a batch-splitting prompt only and require narrower work when multiple ownership boundaries emerge.

**Failure boundary and alternatives.** 12 is treated as a universal capacity guarantee, several decisions are hidden in one request, source count substitutes for semantic coverage, or unbounded discovery delays action. Bounded alternatives and recoveries: split by reliability boundary, repository, framework, or ownership; cap discovery; use a map-reduce source inventory; or create a narrower specialist route.

**Counterexample, gotchas, and operational comparison.** Duplicate documents inflating count, huge destination files hidden behind one path, version fan-out, external retrieval latency, cross-repository conventions, and context spent on sources already rejected. Good: route ten candidate documents for one coroutine decision by role and load two substantive sections. Bad: call 12 links a synthesized corpus. Borderline: more than 12 sources can remain tractable when duplicates are collapsed and one owner decision dominates.

**Verification, evidence, and uncertainty.** Record dimensions, estimate source sizes, deduplicate hashes, track loaded versus skipped context, measure reviewer route time, sample decision accuracy, and split when ownership or conflict boundaries multiply. The seed directly supplies the one-audience, one-decision, 12-source, and one-checklist planning values plus source and artifact pressure rows. Those values are inherited planning heuristics without demonstrated universal capacity or quality guarantees.

**Second-order consequence.** Source count is a weak capacity proxy; independent authority conflicts and context bytes are better predictors of routing difficulty.

**Revision decision.** Reinterpret all four workload rows as measurable routing dimensions with context, conflict, ownership, deduplication, and split criteria.

**Retained seed evidence.** The original workload rows and up-to-12 planning value remain visible unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Kotlin Reference Routing Sources as a documentation reference operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | reference selection, writing, roadmap, or explanation work where the output should improve the next human or agent decision | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one audience, one decision, up to 12 source documents, and one verification checklist per reference use | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Reference Map; Jump Points; Section Search; Use Order; Sources And Research Brief; Primary Sources; Repo-Local Sources; Synthesis Notes | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is canonical source routing rules for stale, duplicate, and conflicting sources | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide what measurable conditions make repeated Kotlin source routing trustworthy rather than merely well documented. The seed sets 100 percent boundary visibility, at least 18 of 20 correct decisions, zero unsupported claims, and recovery for every avoid case, but it does not define route samples, verdicts, or context-efficiency evidence.

**Recommended default and causal basis.** Retain all four inherited acceptance targets, define recommendation and task denominators, sample across map boundaries, and add first-route reproducibility, stale-path detection, duplicate suppression, bounded context, and correction ownership. Visible evidence labels can coexist with wrong destinations, while accurate routes can remain unreproducible or expensive without route-trace and context measures.

**Operating conditions and assumptions.** Each target has a sample frame, reviewer rubric, collection period, owner, failure action, and separate interpretation for source hygiene, destination ownership, and routing efficiency. Preserve inherited values as artifact acceptance targets without generalizing them to model accuracy or ecosystem-wide performance.

**Failure boundary and alternatives.** 100 percent is asserted without enumeration, 18 of 20 is cherry-picked, zero unsupported claims is confused with certainty, or recovery clarity is counted without testing a re-route. Bounded alternatives and recoveries: use a smaller fully enumerated sample, cold-reader route replay, seeded stale and conflicting cases, provisional status, or qualitative correction logs before setting budgets.

**Counterexample, gotchas, and operational comparison.** Ambiguous gold destinations, correlated tasks, one reviewer, repeated questions, external freshness dominating samples, and context reduction rewarded despite more rework. Good: 20 varied tasks retain expected owners and correction reasons, with every recommendation labeled. Bad: call all routes reliable because links resolve. Borderline: 18 of 20 is an inherited acceptance rule, not a statistical confidence guarantee.

**Verification, evidence, and uncertainty.** Publish sample selection, expected owner, first route, loaded context, reviewer verdict, correction, evidence labels, unsupported-claim audit, and recovery outcome. All four threshold values and methods are direct seed facts. The seed supplies no historical route study, statistical rationale, context baseline, or repository-specific calibration.

**Second-order consequence.** Routing reliability combines epistemic traceability, destination accuracy, and operational efficiency; failure in any one axis should narrow downstream claims.

**Revision decision.** Define denominators and failure actions for the seed targets and add route reproducibility, staleness, deduplication, context, and correction measures.

**Retained seed evidence.** The four original reliability rows and values remain unchanged beneath the expanded measurement protocol. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which routing failures must be modeled explicitly and what evidence shows that mitigation restores a correct next-source decision. The seed identifies source drift, generic advice, implicit decisions, and list-like large corpora, but it does not specify how to detect, reproduce, or recover from each routing failure.

**Recommended default and causal basis.** Map every failure to trigger, observable route trace, affected boundary, source role, misleading outcome, controlled test case, mitigation, recovery destination, owner, and refresh condition. A failure label is operational only when a reviewer can distinguish a bad map, stale source, ambiguous classifier, destination gap, or unranked corpus.

**Operating conditions and assumptions.** The trigger can be reproduced safely, the route trace exposes where authority moved incorrectly, and mitigation changes the next action without erasing uncertainty. Test failure scenarios in local or synthetic contexts and do not mutate canonical source roles without owner evidence.

**Failure boundary and alternatives.** Source drift is inferred from age alone, generic advice is detected only by wording, implicit decisions gain a table but no owner, or a large corpus is merely split into smaller unranked lists. Bounded alternatives and recoveries: repair a path, update a classifier, add destination ownership, collapse duplicates, introduce ranked canonical roles, route to a specialist, or preserve a conflict for owner resolution.

**Counterexample, gotchas, and operational comparison.** Broken links confused with obsolete guidance, duplicated sources with different dates, public results ranked by search position, local exceptions generalized, routing loops, and mitigations untested against contrary cases. Good: a missing map path triggers repository search, owner-confirmed replacement, ledger update, and route replay. Bad: delete every old source by date. Borderline: a large corpus can remain usable when sources are role-ranked and only relevant sections load.

**Verification, evidence, and uncertainty.** Seed one stale path, generic answer, ambiguous task, and duplicate corpus; capture first route, observed failure, mitigation, second route, destination gate, and retained uncertainty. The four failure names, triggers, and required mitigations are direct seed facts. Actual drift thresholds, ownership, and corpus scale depend on repository lifecycle and decision risk.

**Second-order consequence.** Routing failures are often failures of classification or ownership before they are failures of retrieval; adding search capacity cannot repair an undefined destination.

**Revision decision.** Retain each seed row while adding fault injection, route trace, discrimination, recovery proof, owner, and invalidation controls.

**Retained seed evidence.** Source drift, generic advice, implicit decisions, and list-like corpora remain the four inherited failure modes. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| decision path remains implicit | reader cannot tell when to adopt, adapt, or avoid the pattern | add decision table, cost of being wrong, and adjacent-reference route |
| large corpus becomes list | many sources are indexed but not synthesized into ranked guidance | classify canonical, supporting, legacy, duplicate, and conflicting sources |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide when to retry source discovery or route evaluation, when to stop loading context, and what evidence must survive the retry. The seed requires classified bounded retries, one stale-evidence refresh, backpressure, checkpoints, and exclusive ownership, but it does not distinguish a source retry from an uncontrolled research or routing loop.

**Recommended default and causal basis.** Classify failure as transient access, stale path, missing destination, ambiguous boundary, conflicting authority, duplicate source, or wrong route; retry only a named hypothesis once unless policy states otherwise, retaining the first route and source state. Unclassified retries erase why the route failed and can accumulate context until the original decision disappears.

**Operating conditions and assumptions.** The retry changes one controlled variable, has an expected discriminating result, bounded context and time, checkpointed evidence, and an escalation owner. Never retry to achieve a preferred answer, never discard the first failure, and keep one writer per routing artifact.

**Failure boundary and alternatives.** Agents search repeatedly until finding agreeable guidance, source refresh silently changes the question, broken destinations trigger broad browsing, or work continues while heading, uniqueness, or ownership gates are red. Bounded alternatives and recoveries: search repository paths, inspect cached source, narrow the boundary, route to an owner, preserve competing sources, quarantine a stale pointer, or stop with a provisional route.

**Counterexample, gotchas, and operational comparison.** Automatic web retries, duplicate search results, mutable branch content, context compaction hiding first-route rationale, parallel agents rewriting one ledger, and retry success treated as proof of source authority. Good: retain a missing-path failure, run one repository search, confirm the renamed destination, and replay the route. Bad: browse until a source supports the preferred answer. Borderline: one transient fetch retry may be valid but still requires version and local corroboration.

**Verification, evidence, and uncertainty.** Capture first route, classification, hypothesis, controlled change, retry count, loaded context, source hash or retrieval state, final route, escalation, and evidence that new work paused on red gates. All five inherited retry and ownership bullets are direct seed facts. Tool retry behavior, retrieval availability, context limits, and source-owner response time vary by environment.

**Second-order consequence.** Backpressure protects decision identity: stopping context growth preserves the question and evidence chain needed to decide whether re-routing is justified.

**Revision decision.** Extend the inherited workflow with a routing-failure taxonomy, controlled source retries, context budgets, first-route preservation, and loop detection.

**Retained seed evidence.** All five original retry, refresh, backpressure, checkpoint, and ownership bullets remain retained. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide what routing telemetry must be retained so a reviewer can reproduce source selection without receiving a raw research transcript. The seed records loaded sources, proof, timing, reviewer decision, uncertainty, refresh trigger, and compact evidence, but it does not expose the route transitions needed to diagnose a wrong destination.

**Recommended default and causal basis.** Record task and route IDs, repository signals, boundary classifier, candidate roles, source hashes or retrieval dates, loaded and skipped context, destination locator, conflicts, reviewer verdict, correction, command result, stop condition, and refresh trigger. A final link cannot show whether the classifier, map, path, hierarchy, freshness check, or destination content caused success or failure.

**Operating conditions and assumptions.** Signals correlate from question through destination and local gate, sensitive repository data is redacted, and the packet remains small enough for cold review. Capture the minimum reproducible trace, never secrets or private content, and avoid high-cardinality monitoring labels for raw questions.

**Failure boundary and alternatives.** Raw tool output replaces decisions, source and route errors share one label, retries overwrite the first route, context size is unmeasured, or public retrieval metadata is omitted. Bounded alternatives and recoveries: use a compact manual ledger for one decision, structured logs for automated routers, diff-based source snapshots, or a reviewer checklist when telemetry tooling is absent.

**Counterexample, gotchas, and operational comparison.** Unstable URLs, mutable branches, high-cardinality query text, timestamps without timezone, hashes without locators, skipped sources omitted, and route corrections not linked to downstream rework. Good: a route trace shows map heading, two candidates, one rejected backend source, loaded lines, local compile result, and refresh trigger. Bad: attach search output and state 'source found.' Borderline: p50/p95 reviewer time helps at scale but one low-risk route needs only wall time and decision evidence.

**Verification, evidence, and uncertainty.** Seed map, path, hierarchy, and freshness failures separately; confirm telemetry distinguishes them, replay the route, inspect redaction, and verify concise evidence survives a correction. The six seed bullets directly define source, proof, timing, decision, uncertainty, refresh, and compact-audit obligations. Available telemetry hooks, retention, source-size measurement, and privacy policy depend on the agent harness and repository.

**Second-order consequence.** Routing observability is most valuable at decision boundaries: knowing why a source was rejected often saves more future context than recording every source that was opened.

**Revision decision.** Split inherited observations into classifier, candidate, source-state, context, destination, gate, correction, privacy, and handoff signals.

**Retained seed evidence.** All six original observability bullets remain retained under the expanded routing trace. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture reviewer decision, unresolved uncertainty, and next refresh trigger.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide how to test claims that this routing reference reduces decision time without rewarding fast but incorrect source selection. The seed asks readers to find the correct next action within 10 minutes and mixes routing time, implementation lead time, runtime percentiles, and reviewer decision time without a routing experiment design.

**Recommended default and causal basis.** Define a representative task set and expected owning destinations, separate map lookup, destination reading, local verification, correction, and downstream action time, then report first-route accuracy and context loaded alongside elapsed percentiles. A route can be fast because it stops at shallow metadata, while a slower route may prevent much larger implementation rework.

**Operating conditions and assumptions.** Tasks cover distinct boundaries, reviewers have comparable repository access, warmup and familiarity are recorded, timers have clear start and stop events, and corrections remain in the result. Preserve 10 minutes as an inherited local sampling target only and never infer runtime or universal productivity from it.

**Failure boundary and alternatives.** The 10-minute statement becomes a universal SLO, one expert reviewer defines performance, runtime p99 is mixed with documentation lookup, or wrong routes are excluded from timing. Bounded alternatives and recoveries: use qualitative usability review for a tiny corpus, report median and individual traces, measure correction reasons, compare two route procedures, or defer quantitative claims until a baseline exists.

**Counterexample, gotchas, and operational comparison.** Learning effects, cache warmth, source path locality, task difficulty imbalance, reviewer expertise, network retrieval latency, and context-loading time omitted from totals. Good: reviewers route varied tasks, read owning sections, run local gates, and report first-route verdict plus total time. Bad: time only rg and claim action found. Borderline: 10 minutes is a useful inherited sampling target but needs local calibration and accuracy evidence.

**Verification, evidence, and uncertainty.** Retain task definitions, expected owner, reviewer context, source sizes, start and stop events, first route, correction, local gate, wall time, percentile method, and confidence limits or qualitative caveats. The 10-minute review statement, one-session lead indicator, failure signal, measurement fields, and pass/fail sentences are direct seed facts. No benchmark, hardware, reviewer sample, task distribution, or statistical rationale accompanies the inherited timing target.

**Second-order consequence.** Routing performance is end-to-decision evidence, not lookup latency; the cost of correction belongs inside the measurement because it is the primary harm of a misroute.

**Revision decision.** Separate lookup, reading, proof, correction, and action time while coupling all timing to route accuracy and context volume.

**Retained seed evidence.** All original performance sentences and the 10-minute sampling figure remain visible and explicitly qualified. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require the reader to find the correct next action in 10 minutes or less during review sampling.
Leading indicator to measure: lead time from requirement to verified endpoint stays under one focused implementation session.
Failure signal to watch: framework choice or coroutine behavior is guessed instead of verified with tests and docs.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide when one Kotlin routing ledger and source hierarchy cease to be reviewable and how to partition larger source ecosystems. The seed stops this reference at multiple systems, ownership boundaries, unbounded discovery, or production traffic without an SLO, but it does not explain how to federate source maps and preserve cross-boundary evidence.

**Recommended default and causal basis.** Keep one route ledger per owned decision domain and split by repository, framework, deployment boundary, source authority, or maintainer ownership when classifiers, refresh cycles, or gates diverge. A global map without local owners becomes stale, while isolated maps without contracts create duplicate and conflicting routes.

**Operating conditions and assumptions.** Each split has an owner, canonical map, supporting provenance, source lifecycle, verification gate, evidence handoff, and one integration-level route for cross-domain questions. Do not use this single reference as a global source registry for independent systems and preserve one verification owner per split plus a named integration owner.

**Failure boundary and alternatives.** Parallel agents edit one hierarchy, each repository invents incompatible labels, a central list has no destination owners, or splitting leaves generic-to-backend handoffs untested. Bounded alternatives and recoveries: use federated maps with a common schema, dependency-graph narrowing, contract-style handoffs, a central index of local owners, or staged source migration.

**Counterexample, gotchas, and operational comparison.** Duplicate canonical claims, version skew, shared mutable source indexes, private-source leakage, broken relative paths across repos, and merge-time loss of conflict notes. Good: generic Kotlin and backend teams own separate maps joined by an explicit coroutine-lifecycle handoff. Bad: one global file ranks every ecosystem source. Borderline: a shared schema is useful when each domain retains independent freshness and verification ownership.

**Verification, evidence, and uncertainty.** Audit path and writer exclusivity, validate federated links, replay cross-domain routes, compare duplicate hashes, exercise independent refresh, and trace one question through every handoff. The seed directly names system, ownership, discovery, distributed execution, context drift, and graph-narrowing scale boundaries. The right federation and shared schema depend on organization, repositories, privacy, and release cadence.

**Second-order consequence.** Source routing architecture mirrors organizational ownership; stale maps frequently reveal missing maintainers or unclear decision boundaries before they reveal search defects.

**Revision decision.** Add federation criteria, map ownership, shared-schema limits, cross-map handoffs, privacy, refresh, and merge verification.

**Retained seed evidence.** All four original scale-boundary paragraphs remain preserved as the hard outer limit. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Kotlin Reference Routing Sources stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which future query should be run to repair or confirm one source route and what evidence must return before the hierarchy changes. The seed offers broad official-docs, repository, and release-note phrases, but they do not encode a stale routing claim, installed version, owning mechanism, or primary-domain filter.

**Recommended default and causal basis.** Derive each query from the stale claim, Kotlin boundary, technology, installed version, desired primary-source type, and known local locator; use broad seed phrases only to discover a narrower authoritative target. Claim-triggered searches reduce tutorial noise and make it possible to connect retrieval to a route change, local reproducer, and next refresh condition.

**Operating conditions and assumptions.** A freshness trigger exists, browsing is authorized, the route question is narrow, primary domains are prioritized, and retrieved facts are checked against repository behavior. Do not browse during this assignment, infer current facts from query text, or promote public results above local hierarchy without conflict review.

**Failure boundary and alternatives.** Queries are scheduled without stale claims, result ranking determines authority, a repository example becomes policy, or search output changes canonical roles without owner review. Bounded alternatives and recoveries: inspect local dependency source or cached docs, use repository search, preserve uncertainty, ask the map owner, or defer a version-sensitive route.

**Counterexample, gotchas, and operational comparison.** SEO tutorials, old major versions, generic 'Kotlin' results, Spring/Ktor overlap, mutable branch examples, release notes without migration context, and query wording treated as evidence. Good: query official Kotlin documentation for the installed version's platform-type rule after a route conflict and compile a reproducer. Bad: search 'best Kotlin references' and add every result. Borderline: GitHub examples can reveal a missing destination but cannot promote it without maintenance and version checks.

**Verification, evidence, and uncertainty.** Record trigger, exact query, domain filters, retrieval date, selected and rejected results, version match, paraphrased fact, local proof, hierarchy or route change, owner, and invalidation trigger. The three query categories and phrases are direct seed facts, and the research brief names several narrower official Kotlin topics. No query was run in this no-browse pass, so current result quality and APIs remain unknown.

**Second-order consequence.** A refresh query is maintenance control, not evidence; success means one uncertainty was resolved and the routing ledger changed or was explicitly reaffirmed.

**Revision decision.** Retain broad discovery phrases while adding versioned claim templates and a retrieval-to-route-change evidence record.

**Retained seed evidence.** The official-docs, GitHub-repository, and release-notes query rows remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | kotlin reference routing sources official documentation best practices |
| `github_repository_query_phrase` | kotlin reference routing sources GitHub repository examples |
| `release_notes_query_phrase` | kotlin reference routing sources changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide how every source route should expose what is known, what was merely pointed to, what was observed locally, and what remains an inference. The seed defines local facts, external facts, and combined inference, but routing work also needs to distinguish navigation rules, supporting provenance, unrefreshed pointers, repository observations, executable results, and unresolved conflicts.

**Recommended default and causal basis.** Label canonical navigation facts, supporting provenance facts, refreshed external facts, unrefreshed external pointers, repository observations, executable results, and combined routing inference separately with locator, date or version, scope, and invalidation condition. Source routes blend where-to-look policy with substantive evidence, and collapsing them lets a map bullet or URL masquerade as implementation proof.

**Operating conditions and assumptions.** A reviewer can trace each route step, see which destination was actually read, reproduce local evidence, identify conflict, and know what new fact would reverse the selection. Do not upgrade confidence through repetition, preserve conflicts until discriminating evidence exists, and scope executable results to observed versions and environments.

**Failure boundary and alternatives.** A URL becomes an external fact without retrieval, a map heading is quoted as destination guidance, repository behavior is generalized, inference is written as canonical policy, or conflict disappears in synthesis. Bounded alternatives and recoveries: downgrade to provisional routing, present competing destinations, run a discriminating local check, request current primary evidence, or assign an owner without forcing consensus.

**Counterexample, gotchas, and operational comparison.** Generated-reference circularity, stale source hashes, path aliases, framework examples mistaken for Kotlin rules, hidden environment state, and confidence words without criteria. Good: label a jump point as canonical navigation, the dated brief as supporting provenance, a compiled annotation behavior as repository evidence, and the final route as inference. Bad: call an unbrowsed Ktor URL a current fact. Borderline: one local success strongly supports that repository but weakly supports portability.

**Verification, evidence, and uncertainty.** Sample every recommendation, confirm labels and locators, hash local sources, record retrieval state, reproduce executable claims, inspect conflicts, and keep uncertainty adjacent to conclusions. Both local sources and frozen seed were completely read and hashed, and the three inherited labels are direct facts. Public sources, destination reliability content, and target repository behavior were not refreshed or exercised, so current API routes remain conditional.

**Second-order consequence.** Evidence labels are routing instructions for future maintenance: they tell the next reader whether to reread a map, open a destination, refresh a public fact, rerun a local gate, or revisit inference.

**Revision decision.** Extend the three retained labels with navigation, provenance, retrieval state, repository observation, executable result, conflict, confidence, and invalidation metadata.

**Retained seed evidence.** The original local-corpus, external-research, and combined-inference definitions remain the closing vocabulary. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
