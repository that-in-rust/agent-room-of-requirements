# Rust Legacy Coder Prompts

**Decision supported.** This section helps decide how an agent should use three generations of Rust coder prompts without copying stale, duplicated, unsupported, or repository-local rules into a new task. The seed title names a theme but does not explain whether these prompts are executable instructions, historical evidence, a migration aid, or current Rust doctrine.

**Recommended default and causal basis.** Treat this reference as a provenance-aware migration and critique guide: recover intent from rust-coder-01, use rust-coder-02 to identify reliability gaps and general-versus-local distinctions, and use Rust-Coder-1000IQ-01 as the more rigorous operator doctrine subject to repository and task verification. Legacy prompts bundle durable first principles with local rituals, dated crate examples, unsupported numbers, duplicated catalogs, and broad commands, so safe reuse requires classification before instruction.

**Operating conditions and assumptions.** The agent names task mode, target repository, source generation, retained rule, rejected or adapted rule, evidence grade, affected boundary, verification, waiver, and adjacent handoff. Use this reference for prompt interpretation and migration; it does not replace current compiler, crate, framework, repository, security, or product evidence.

**Failure boundary and alternatives.** A whole prompt is pasted into context as authority, four-word naming becomes a language law, cargo clean becomes routine, editorial scores become measured confidence, or current API behavior is inferred from unbrowsed links. Bounded alternatives and recoveries: use the current Rust operator doctrine directly for new work, route to a backend or security reference for specialized delivery, extract one bounded pattern card, or preserve a historical rule only as an explicit repository convention.

**Counterexample, gotchas, and operational comparison.** Duplicated rust-coder-01 sections, its 184-item catalog of uneven depth, rust-coder-02 source paths not present in the mapped corpus, 1000IQ normative language that still needs local adaptation, and mutable external documentation. Good: retain parse-at-the-edge and cancellation rules with task-specific tests while rejecting universal naming and latency claims. Bad: command an agent to implement every listed pattern. Borderline: keep a four-word name only when an established repository convention and API clarity both support it.

**Verification, evidence, and uncertainty.** Identify the exact source and heading, compare later-source critique, classify local versus general guidance, inspect repository constraints and callers, select the smallest applicable rule, run claim-specific tests and gates, and record exclusions or waivers. All three local files were read completely and provide direct evidence of their differing scope, duplication, critique, and operational maturity. The external Rust, Tokio, and Axum links were preserved but not browsed, and the provenance of the internal Notes2026 inputs named by rust-coder-02 was not independently reconstructed.

**Second-order consequence.** The value of a legacy prompt is not obedience to its wording but the recoverable decision history showing which rules survived critique and which became explicit boundaries.

**Revision decision.** Add a source-generation model, reuse protocol, rejection criteria, verification ladder, and no-browse boundary before preserving the seed mappings.

**Retained seed evidence.** The exact title and its focus on Rust legacy coder prompts remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide which source can support a legacy-prompt claim, what authority it has, and what additional verification is required before reuse. The seed lists three local files and four public links but assigns every local file the same artifact role, omits byte identity and generation order, and marks public sources as facts without recording whether they were actually opened.

**Recommended default and causal basis.** Map rust-coder-01 as the broad February legacy prompt, rust-coder-02 as its reliability-focused successor and critique, Rust-Coder-1000IQ-01 as the detailed working operator doctrine, and each public link as an unrefreshed candidate source rather than current evidence. Generation, purpose, and retrieval status determine whether a sentence is historical intent, comparative critique, operational doctrine, or an unverified ecosystem pointer.

**Operating conditions and assumptions.** Each row records exact path, SHA-256, size, document status, direct-use scope, known limitations, upstream dependencies, conflict relationships, retrieval status, and the gate needed to promote a claim. The map establishes provenance and reuse pressure, not universal technical truth or present ecosystem compatibility.

**Failure boundary and alternatives.** File count substitutes for corroboration, a later document is automatically treated as correct, a declared normative status overrides repository rules, or a URL row implies current framework behavior without a complete read. Bounded alternatives and recoveries: use a claim-level ledger for complex conflicts, group repeated sections within one source, route current framework questions to specialized references, or downgrade unresolved advice to provisional inference.

**Counterexample, gotchas, and operational comparison.** Rust-coder-01 repeats its opening doctrine and mixes 184 catalog entries with templates, rust-coder-02 depends on Notes2026 paths outside this mapping, 1000IQ is explicitly a working draft, and external pages can change independently. Good: cite rust-coder-02 for the local-versus-general distinction and 1000IQ for waiver fields, while citing rust-coder-01 only for historical four-word naming intent. Bad: call all three independent proof. Borderline: prefer the later doctrine when it directly addresses an earlier gap but still test repository fit.

**Verification, evidence, and uncertainty.** Recompute local hashes, open the complete source, locate the exact heading, compare overlapping claims across generations, record contradictions and dependencies, preserve external retrieval status, and attach a repository-specific verification path. The local artifacts were completely read at hashes aaec85ca39103591e54cb3bc9a8535916853dd25256ac0f8eb4539b60f0bbd13, 71bc70834548a7304b35a94b0de8a31135f06230446fd0d02929da2690b3af9f, and 9006daebdade32af945c70e82eaff6487fa1ac440677c80f4739bc5f6eee2322. Authorship, original creation context, referenced Notes2026 inputs, and present public-link contents were not independently verified.

**Second-order consequence.** Three files are not three votes; they form an evolution chain whose disagreements reveal where prompt rules needed boundaries, evidence, or replacement.

**Revision decision.** Enrich the mapping with generation, hash, authority basis, limitation, dependency, retrieval status, conflict, and promotion-gate columns while retaining every seed row.

**Retained seed evidence.** All three local paths and all four Rust, Tokio, and Axum URLs remain preserved exactly in the original table. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/rust-coder-01.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/claude-skills-202602/rust-coder-02.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| idiomatic reference file/Rust-Coder-1000IQ-01.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| https://github.com/rust-lang/api-guidelines | external_research_source_material | external_research_sourced_fact | Rust library-team API design recommendations |
| https://github.com/tokio-rs/tokio | external_research_source_material | external_research_sourced_fact | async runtime implementation and operational model |
| https://github.com/tokio-rs/axum | external_research_source_material | external_research_sourced_fact | Rust web framework implementation and design claims |
| https://docs.rs/axum/latest/axum/ | external_research_source_material | external_research_sourced_fact | published crate documentation for routing and extractors |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which legacy-prompt handling patterns deserve priority when an agent has limited context and must avoid propagating obsolete or overbroad instructions. The seed gives three high numeric scores to one repeated identifier without a scoring rubric, denominator evidence, source-generation check, local-versus-general distinction, or cost of applying the pattern.

**Recommended default and causal basis.** Rank source-generation classification, local-versus-general separation, claim-level evidence boundaries, smallest-sufficient rule selection, verification-gate coupling, completion honesty, and explicit waiver or routing discipline using a disclosed qualitative rubric. The highest-value pattern is the one that prevents the most consequential misuse at acceptable ceremony, not the row with the most authoritative-looking number.

**Operating conditions and assumptions.** Each rank explains bug class prevented, misuse resistance, diagnosability, compatibility effect, verification strength, context cost, failure boundary, and source basis, with ties or uncertainty left visible. Rankings guide attention in this reference and must not be read as empirical effectiveness, security assurance, or repository policy.

**Failure boundary and alternatives.** Editorial scores are treated as probabilities, three rows reuse one identifier, numeric precision hides disagreement, broad ceremony outranks task risk, or a locally effective ritual is promoted as general Rust. Bounded alternatives and recoveries: use tiers without numbers, maintain task-mode-specific rankings, compare only patterns relevant to the current boundary, or leave incomparable controls unranked.

**Counterexample, gotchas, and operational comparison.** Later sources may be more rigorous but broader, a low-cost source check can outrank deep technical advice, verification commands can be wrong for a workspace, and prevention value changes across library, CLI, async, FFI, and review modes. Good: rank evidence-boundary splitting above naming ritual because it blocks unsupported migration across every mode. Bad: infer a 95 percent success chance. Borderline: rank four-word naming highly only inside a repository that deliberately enforces and validates it.

**Verification, evidence, and uncertainty.** Define the rubric before scoring, trace every criterion to a source and failure case, test rankings against representative task modes, solicit disagreement, record application cost, and revise when downstream errors contradict the ordering. All three local sources support evidence, verification, and explicit boundary selection, while rust-coder-02 and 1000IQ directly challenge undifferentiated pattern adoption. The corpus contains no outcome dataset linking any score to defect reduction, development speed, or reviewer agreement.

**Second-order consequence.** A useful scoreboard ranks decision safeguards, not Rust cleverness; it should make the cheapest high-leverage mistake prevention visible first.

**Revision decision.** Replace repeated identifiers and unsupported precision with stable pattern IDs, disclosed criteria, qualitative tiers, task-mode caveats, evidence, and re-ranking triggers.

**Retained seed evidence.** Source Map First, Evidence Boundary Split, and Verification Gate Coupling remain the three retained top-ranked safeguards. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `rust_legacy_coder_prompts` | 95 | default_adoption_pattern_tier | Source Map First: Load local rust legacy material before synthesizing coder prompts recommendations. |
| `rust_legacy_coder_prompts` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `rust_legacy_coder_prompts` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what governing principle should control every extraction, adaptation, or rejection from the legacy coder prompts. The seed says to load local sources first, check public guidance second, and create verification-backed decisions, but it does not explain how to resolve conflicts among legacy generations or separate repository ritual from general Rust doctrine.

**Recommended default and causal basis.** Reuse a legacy instruction only when its intended failure prevention survives later-source critique, matches the current task mode and repository boundary, states normative strength and exception conditions, and maps to fresh verification. Prompt age alone neither invalidates nor validates a rule; durable advice persists because its causal mechanism, scope, and proof still fit, while brittle advice fails at one of those boundaries.

**Operating conditions and assumptions.** The synthesis identifies source generation, claim class, task mode, governing contract, affected caller or lifecycle, later critique, current evidence status, repository override, test or gate, waiver, and stop condition. The thesis governs instruction selection; it does not make the chosen technical pattern correct without code, caller, runtime, and repository evidence.

**Failure boundary and alternatives.** Local-first becomes local-always, public recency erases intentional repo conventions, later prose wins without evidence, or a useful principle is converted into a universal command. Bounded alternatives and recoveries: retain a rule as historical context only, adapt it into a narrower pattern card, replace it with current local doctrine, route to a domain reference, or mark the decision unresolved pending evidence.

**Counterexample, gotchas, and operational comparison.** The same phrase can shift from convention to doctrine across files, MUST language in a working draft may be provisional, examples can encode obsolete APIs, and broad principles such as testability can be over-applied as traits everywhere. Good: preserve executable requirements and completion honesty, adapt trait injection to real side-effect seams, and reject mandatory four-word naming as general Rust. Bad: select rust-coder-01 because the seed calls it canonical. Borderline: keep Mermaid-only diagrams as a documentation convention, not a Rust correctness rule.

**Verification, evidence, and uncertainty.** Trace the rule through all available generations, state the causal failure it prevents, inspect repository policy and task mode, test counterexamples, run the narrow verification path, and record override or waiver evidence. Rust-coder-02 explicitly distinguishes repo-local from general guidance, and 1000IQ supplies modes, evidence grades, normative strength, waivers, and completion-evidence rules. External ecosystem freshness and undocumented original motivations remain outside the completed local read.

**Second-order consequence.** Legacy prompt evolution is most useful as a falsification trail: later documents reveal which earlier absolutes needed scope, counterexamples, and proof.

**Revision decision.** Replace the retrieval-order slogan with a conflict-resolution thesis based on causal survival, mode fit, normative scope, fresh proof, and explicit exceptions.

**Retained seed evidence.** Local retrieval, external freshness checking, and verification-backed agent decisions remain retained as subordinate steps. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `rust_legacy_coder_prompts` contains 3 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Rust Legacy Coder Prompts comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide which local source and exact region an agent should retrieve for a naming-history, reliability-pattern, current doctrine, verification, waiver, or domain-routing question. The seed records three titles and a few opening headings but does not reveal that rust-coder-01 is 3,681 lines with repeated doctrine and a broad catalog, rust-coder-02 is a 1,541-line selective critique, and 1000IQ is a 1,795-line mode-and-rule operator manual.

**Recommended default and causal basis.** Use rust-coder-01 for historical intent and examples, rust-coder-02 for the curated reliability delta and local-versus-general split, and 1000IQ for mode selection, output contracts, normative rules, anti-patterns, architecture, test matrices, gates, context packs, waivers, and traceability. Targeted retrieval preserves useful context while preventing the largest and oldest prompt from dominating simply because it contains the most material.

**Operating conditions and assumptions.** The map gives source hash, size, generation role, strongest headings, known duplication, unsupported or provisional claims, named dependencies, ideal questions, questions it cannot answer, and escalation path. The map optimizes local evidence retrieval and does not validate current crate APIs, compiler behavior, or domain-specific production policy.

**Failure boundary and alternatives.** Opening headings substitute for a complete read, rust-coder-01's catalog is treated as uniformly vetted, rust-coder-02's scores become measurements, or 1000IQ's working-draft status and domain limits disappear. Bounded alternatives and recoveries: retrieve a single pattern or anti-pattern card, compare all generations for contested guidance, use repository-local instructions as higher authority, or route framework and operational details to narrower references.

**Counterexample, gotchas, and operational comparison.** Rust-coder-01 has repeated introductions and malformed or questionable examples, rust-coder-02 points to uncollected Notes2026 sources, 1000IQ contains broad default cargo gates that may not fit every workspace, and all sources mix principles with examples. Good: read 1000IQ Sections 1-4 and 12-13 for a review output contract, then compare rust-coder-02's LLM decision ladder. Bad: load 136 KB of rust-coder-01 for one conversion question. Borderline: consult its advanced catalog only when the exact niche pattern is independently verified.

**Verification, evidence, and uncertainty.** Choose the question first, open the complete relevant section, check source hash, inspect preceding assumptions and following exceptions, compare successor treatment, identify repository overrides, and attach the selected passage to an executable gate. The three files were fully read and their line counts, hashes, structure, and cross-generation relationships were directly observed. The map cannot establish whether any source is officially maintained or whether its uncollected upstream notes would alter the interpretation.

**Second-order consequence.** Progressive disclosure should follow decision pressure: retrieve the smallest source region that can expose both the rule and its failure boundary.

**Revision decision.** Expand each row with size, hash, generation role, high-value regions, limitations, dependencies, ideal query, prohibited inference, and handoff.

**Retained seed evidence.** The three exact local paths, titles, opening heading signals, and reusable-prompt roles remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/rust-coder-01.md | rust-coder-01 | Codebase Wisdom 101; Technical Design101: TDD-First Architecture Principles; CRITICAL: FOUR-WORD NAMING CONVENTION (LLM Optimization); Product thinking for us; The Essence: Executable Specifications Drive Everything; The Non-Negotiables: 8 Architectural Principles | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/claude-skills-202602/rust-coder-02.md | rust-coder-02 | Rust Coder 02; Premise Check; What rust-coder-01 Gets Right; Where rust-coder-01 Is Thin; Source Pool; Selection Thesis | skill entrypoint or reusable agent prompt |
| idiomatic reference file/Rust-Coder-1000IQ-01.md | Rust Coder 1000IQ 01 | Rust Coder 1000IQ 01; 0. Document Status; 1. Fast Router; 2. Mode Selection; 3. Output Contract; 4. Normative Conventions | skill entrypoint or reusable agent prompt |

## External Research Source Map

**Decision supported.** This section helps decide how future research should use each external source to refresh a specific legacy claim without replacing repository evidence or confusing implementation with normative guidance. The seed names Rust API Guidelines, Tokio, Axum's repository, and Axum docs but presents their usage roles as sourced facts even though this evolution did not retrieve or inspect those public pages.

**Recommended default and causal basis.** Treat the API Guidelines as a candidate for library-surface checks, Tokio sources and documentation as candidates for runtime and cancellation semantics, the Axum repository as versioned implementation evidence, and docs.rs as published API evidence tied to an exact crate version. Public sources answer different questions and change at different rates; conflating repository code, release state, and rendered documentation creates false freshness.

**Operating conditions and assumptions.** A future evidence row records URL, source type, exact commit or version, retrieval date, complete section read, claim supported, claim not supported, repository applicability, contradiction, reproduction, and expiry trigger. External research can update ecosystem facts but cannot establish product requirements, repository conventions, threat models, or production behavior by itself.

**Failure boundary and alternatives.** A retained link is called verified, main-branch code is mixed with a released crate, Tokio behavior is generalized to every runtime, Axum convenience becomes core Rust doctrine, or external guidance silently overrides explicit local policy. Bounded alternatives and recoveries: use local dependency source and lockfile metadata, official language or crate documentation, release notes and migration guides, repository issue history for ambiguity, or defer the claim with an uncertainty marker.

**Counterexample, gotchas, and operational comparison.** Docs.rs version selectors, default and optional features, examples compiled against different MSRVs, GitHub main moving after retrieval, framework APIs changing while general principles remain stable, and blog summaries omitting caveats. Good: verify an Axum extractor claim against the exact locked version and test it in the repository. Bad: infer cancel safety from a Tokio repository link. Borderline: use API Guidelines as supporting rationale where the repository intentionally documents a different naming convention.

**Verification, evidence, and uncertainty.** During a future refresh, open the complete authoritative material, pin version or commit, read surrounding caveats, reproduce examples under repository features and toolchain, compare local rules, record conflict resolution, and rerun affected tests. The seed accurately preserves four plausible evidence destinations and identifies their intended API, runtime, implementation, and documentation roles. No external page was browsed, so availability, current version, wording, and continued authority are unknown.

**Second-order consequence.** External freshness is claim-relative: a stable first principle and a framework method signature require different refresh frequencies and evidence forms.

**Revision decision.** Retain the four URLs while changing their status to unrefreshed candidates and adding version, source-type, applicability, contradiction, reproduction, and expiry controls.

**Retained seed evidence.** The exact Rust API Guidelines, Tokio repository, Axum repository, and Axum docs.rs rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://github.com/rust-lang/api-guidelines | Rust API Guidelines | Rust library-team API design recommendations | external_research_sourced_fact |
| https://github.com/tokio-rs/tokio | Tokio repository | async runtime implementation and operational model | external_research_sourced_fact |
| https://github.com/tokio-rs/axum | Axum repository | Rust web framework implementation and design claims | external_research_sourced_fact |
| https://docs.rs/axum/latest/axum/ | Axum docs.rs | published crate documentation for routing and extractors | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which observable smells should block reuse of a legacy coder prompt and what narrower recovery preserves useful intent. The seed lists context-free summaries, unsourced claims, and unverified instructions, but it omits the legacy-specific hazards exposed by duplicated prompts, local rituals, editorial scores, stale APIs, catalog overload, and universal command or performance claims.

**Recommended default and causal basis.** Register whole-prompt transplant, local-rule universalization, later-document supremacy, score-as-evidence, stale-example copying, abstraction buffet selection, compiler-acceptance proof, generic gate substitution, hidden waiver, and unsupported performance precision. Legacy prompt failures usually occur when context or normative strength is silently widened, so detection must identify the exact transfer that made a once-useful instruction unsafe.

**Operating conditions and assumptions.** Each anti-pattern names source generation, copied claim, widened assumption, affected caller or lifecycle, symptom, causal failure, detection evidence, containment, replacement, exception, verification, and owner. The registry blocks unsafe prompt reuse but does not replace code review, threat analysis, semver evaluation, or runtime diagnosis.

**Failure boundary and alternatives.** The registry mocks obviously bad code, safer replacement is another slogan, a smell is prohibited without an exception boundary, or detection depends on the same source that introduced the claim. Bounded alternatives and recoveries: downgrade to historical context, extract a bounded pattern card, ask for repository evidence, use a successor's explicit waiver model, route specialist work, or omit the instruction.

**Counterexample, gotchas, and operational comparison.** A useful local convention can resemble universalization, newer examples can still be wrong, strict gates may be intentional release policy, dynamic errors can be valid at application boundaries, and cloning can simplify ownership at acceptable measured cost. Good: flag exact-four-word naming as a local convention requiring repository proof. Bad: call every trait abstraction overengineering. Borderline: retain all-features Clippy only when the workspace supports that matrix and suppressions are reviewed.

**Verification, evidence, and uncertainty.** Trace the instruction to its source, state the widened assumption, construct a realistic counterexample, inspect successor treatment and repository policy, apply the replacement, and run a test that exposes the original failure mode. Rust-coder-02 explicitly catalogs where the first prompt is thin and separates local conventions, while 1000IQ supplies anti-pattern cards with exceptions and verification. Frequency and severity of these failure modes are not measured across agent sessions.

**Second-order consequence.** A legacy anti-pattern is often a scope mutation rather than a technically false sentence; preserving the original boundary can recover the value.

**Revision decision.** Expand three generic rows into a source-aware causal registry with widening mechanism, exception, containment, replacement, proof, and ownership.

**Retained seed evidence.** Source-map-first synthesis, evidence-boundary splitting, and concrete verification-gate requirements remain preserved as the primary mitigations. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide which layered gates must pass before the reference artifact or a migrated legacy instruction can be called complete. The seed supplies one archive-wide final-stage generator command, which exists locally, but it does not prove this individual evolved file, its question packet, normalized uniqueness, frozen spans, source hashes, prompt-selection reasoning, or task-specific Rust claims.

**Recommended default and causal basis.** Run the focused file verifier for structure and packet uniqueness, audit exact heading order and per-section expansion, validate frozen source rows and hashes, scan placeholders and whitespace, reread packet and reference, then run repository-adapted Rust tests and conditional gates for any instruction applied to code. Artifact correctness, provenance correctness, and target-code correctness are different claims; one global generation command cannot substitute for all three.

**Operating conditions and assumptions.** The gate record names claim, command or review method, working directory, toolchain and features, expected failure, actual exit status, output summary, exclusions, timestamp, owner, and next action. These commands verify the reference artifact and guide downstream gate selection; they do not certify security, performance, or runtime reliability absent relevant fixtures.

**Failure boundary and alternatives.** The archive verifier passes while this packet is malformed, a generic Cargo command is run from the wrong root, an unsupported tool is declared mandatory, a prior result is reused, or compilation is reported as proof of cancellation or semver safety. Bounded alternatives and recoveries: use an equivalent repository command with documented coverage, perform a structured manual review when automation is unavailable, add a targeted harness, or mark completion blocked rather than inventing evidence.

**Counterexample, gotchas, and operational comparison.** All-features combinations may be invalid, docs and examples can use separate crates, Miri and fuzzing require support, focused artifact checks do not validate technical advice, and a zero exit code can accompany skipped tests. Good: run `python3 tests/verify_idiomatic_reference_file.py --path idiomatic-ref-202607/rust_legacy_coder_prompts-20260710.md`, audit frozen rows, then run only Rust gates justified by the migrated rule. Bad: cite the archived final-stage command alone. Borderline: replace unavailable Miri with a reviewed safety harness and explicit residual risk.

**Verification, evidence, and uncertainty.** Force or observe each gate's relevant failure before trusting it where feasible, execute fresh from the declared root, inspect collected and skipped cases, compare output to the claimed scope, preserve evidence, and rerun after reconciliation. The focused verifier and frozen-row tests are present in this repository, the archived generator command exists, and 1000IQ directly requires fresh claim-specific verification plus conditional gates. The target repository, toolchain, features, supported optional tools, and exact code claim are unknown until this reference is applied.

**Second-order consequence.** Verification must be compositional: every layer proves only its own contract, and completion is the intersection of those bounded proofs.

**Revision decision.** Retain the archive command while adding focused artifact, packet, frozen-span, source, reread, and task-specific claim gates with substitution and skipped-test rules.

**Retained seed evidence.** The existing `verify_reference_generation.py --stage final` command remains preserved as the archive-wide generation gate. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide how an agent should choose between historical analysis, prompt modernization, technical planning, implementation guidance, and code review before retrieving any legacy rule. The seed says to open this reference on a theme or path match and then load local sources, prefer gated patterns, check public freshness, and preserve labels, but it does not classify user intent, task mode, risk surface, or legacy migration depth.

**Recommended default and causal basis.** Identify the requested outcome, select Spec, Delivery, Reliability, Architecture, or Review modes, load repository instructions, name the affected Rust boundary, retrieve the smallest relevant source generation, and produce an evidence-labeled decision with tests, exceptions, and routing. The same legacy sentence can be useful evidence in a history audit, harmful as an implementation command, or appropriate as a local convention, depending on mode and authority.

**Operating conditions and assumptions.** The guide records user goal, starting code and repository state, source trigger, chosen modes, requirements, callers, failure and compatibility risks, selected and rejected rules, evidence status, output contract, verification, and stop condition. Use the guide to decide how to reason and retrieve; it does not authorize edits, destructive actions, external research, or completion claims beyond the user's scope.

**Failure boundary and alternatives.** A keyword selects the whole prompt, mode remains implicit, review output turns into implementation, historical facts become recommendations, or the agent loads broad catalogs before inspecting the actual code and requirements. Bounded alternatives and recoveries: answer with a bounded historical digest, use a narrower current Rust reference, ask for missing product constraints, retrieve one rule card, or stop at a risk finding without prescribing code.

**Counterexample, gotchas, and operational comparison.** Tasks can need several modes with precedence, repo-local conventions can override generic defaults, code review should lead with findings, tiny changes may not justify full ceremony, and unsafe or framework work needs specialized context. Good: for an async worker bug choose Reliability plus Review, retrieve cancellation and structured-concurrency cards, inspect current code, and write a failing shutdown test. Bad: invoke all 60 rust-coder-02 patterns. Borderline: use Delivery alone for a private pure helper after proving no public or lifecycle boundary.

**Verification, evidence, and uncertainty.** State the selected modes and why, inspect whether the output matches their contracts, trace each recommendation to source and code evidence, test the highest-risk counterexample, run fresh gates, and confirm adjacent routes were considered. 1000IQ directly defines five modes, precedence, output order, and review behavior, while rust-coder-02 provides a seven-step LLM decision ladder. Task-specific mode choice and acceptable ceremony depend on user intent, repository rules, blast radius, and available evidence.

**Second-order consequence.** Prompt routing is a control-flow decision: choosing mode first prevents context retrieval from predetermining the answer.

**Revision decision.** Replace keyword-only triggering with intent and mode classification, risk and boundary inventory, progressive retrieval, output contract, verification, and stop rules.

**Retained seed evidence.** Local-source-first retrieval, gated-pattern preference, external freshness checks, and evidence-label preservation remain retained actions. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide how a practitioner moves from a legacy coder-prompt trigger to one bounded, verified Rust decision without letting the prompt substitute for repository understanding. The seed identifies a Rust reliability engineer, a safe API and error-model goal, local paths, and a pre-coding ownership decision, but it stops before showing source comparison, rejected legacy rules, code inspection, tests, feedback, rollback, or completion evidence.

**Recommended default and causal basis.** Follow a journey from intent and mode selection through repository and caller inspection, targeted source retrieval, rule comparison, requirements, counterexamples, test selection, minimal implementation or review finding, fresh gates, and an evidence-rich handoff. A journey reveals where prompt advice enters control flow and where code, product, compatibility, and runtime evidence must be allowed to override it.

**Operating conditions and assumptions.** The scenario names actor, goal, starting artifacts, constraints, trigger, decisions, retrieved sections, retained and rejected rules, code boundary, red test, implementation, failure injection, full gates, rollback, unresolved risk, and final next action. Adapt the scenario to task risk and user authorization; it does not mandate code edits, a particular runtime, or every available gate.

**Failure boundary and alternatives.** The actor starts coding after reading the prompt, the scenario assumes a greenfield repository, no downstream callers appear, tests validate only syntax, or completion is declared without the user's actual outcome. Bounded alternatives and recoveries: perform a review-only journey, create a historical migration memo, stop after a requirements packet, route unsafe or backend delivery, or retain the legacy rule without code changes when it is already verified local policy.

**Counterexample, gotchas, and operational comparison.** The prompt may prescribe a pattern before the code is inspected, the cheapest change may be documentation or test repair, public API compatibility can dominate elegance, and a failing test must fail for the intended reason. A maintainer modernizing an async library ignores rust-coder-01's universal trait rule, uses 1000IQ Reliability and Architecture modes, finds an unbounded detached worker, writes a cancel-during-work test, introduces bounded ownership, runs feature and shutdown gates, and records the remaining external-caller risk.

**Verification, evidence, and uncertainty.** Replay the journey from the stated starting revision, confirm each decision consumes named evidence, observe the regression fail before the change, inspect caller and error behavior, run fresh complete gates, and check that rollback restores the prior contract. The local sources provide every journey component: executable requirements and TDD history, reliability-focused rule selection, and a current mode-output-verification contract. No single repository, user interaction, or production workload is supplied, so the scenario remains a reusable operational model rather than a factual incident.

**Second-order consequence.** The prompt's proper role is a decision checkpoint inside the journey, not the journey's starting implementation plan.

**Revision decision.** Extend the opening role statement into a full evidence-to-decision-to-test-to-handoff journey with alternate exits and rollback.

**Retained seed evidence.** The Rust reliability engineer, safe API, explicit errors, testable boundary, local paths, ownership and async decision, and opening triggers remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The Rust reliability engineer is starting from a requirement that needs a safe API, explicit error model, and testable boundary and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `rust_legacy_coder_prompts` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing the idiomatic ownership, async, error, and crate-boundary shape before coding.
Reference opening trigger: Open this file when the task mentions rust legacy coder prompts, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide whether a specific legacy instruction should be adopted unchanged, adapted, replaced, routed, retained only as history, or removed from active guidance. The seed offers Adopt, Adapt, Avoid, and Cost of being wrong, but it compares only local-versus-external agreement and does not distinguish verbatim preservation, causal adaptation, replacement, specialist routing, or retirement.

**Recommended default and causal basis.** Choose by governing requirement, source generation, repo-local status, causal failure prevented, caller and compatibility effect, runtime lifecycle, current evidence, implementation cost, verification strength, reversibility, and consequence of error. Two instructions can both sound idiomatic while imposing different obligations; explicit tradeoffs reveal when compatibility, ownership simplicity, diagnosability, or bounded shutdown outweighs stylistic consistency.

**Operating conditions and assumptions.** Each option names use conditions, assumptions, benefits, direct and second-order costs, counterexample, evidence grade, verification, rollback, review trigger, and owner. This guide structures a technical decision and cannot decide product value, legal policy, threat acceptance, or organizational ownership without stakeholders.

**Failure boundary and alternatives.** Adopt means sources agree without code evidence, Adapt changes wording but not scope, Avoid discards a useful local rule, or Cost of wrong mentions only wasted time while ignoring public API and runtime damage. Bounded alternatives and recoveries: time-box a provisional rule, run two designs against caller fixtures, use a compatibility adapter, narrow the rule to one module, collect missing evidence, or escalate a security and unsafe decision.

**Counterexample, gotchas, and operational comparison.** Later doctrine can overfit a different domain, local naming may be externally visible, replacing error types can be semver-breaking, trait removal can hurt real substitution, and simpler code can still weaken observability. Good: replace universal trait injection with seams only at external boundaries, proven by fake and real-adapter tests. Bad: adapt four-word naming by allowing three words without questioning the premise. Borderline: preserve a public legacy name for compatibility while applying current naming to new private items.

**Verification, evidence, and uncertainty.** Write the decision record before implementation, enumerate callers and failure paths, compare at least one viable alternative, run misuse and compatibility tests, measure claimed costs, rehearse rollback, and set a re-evaluation trigger. Rust-coder-02's selection thesis and 1000IQ's normative, evidence, waiver, architecture, and review sections directly support a richer option model. The relative cost of compatibility, ceremony, performance, and migration depends on repository and user priorities not present in the corpus.

**Second-order consequence.** The key tradeoff is not old versus new; it is which rule places the unavoidable complexity at the boundary best equipped to own and verify it.

**Revision decision.** Expand the four rows into six disposition choices with causal criteria, caller and lifecycle costs, evidence, rollback, and revisit conditions.

**Retained seed evidence.** Adopt, Adapt, Avoid, and Cost of being wrong remain preserved as core dispositions within the expanded matrix. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the rust legacy coder prompts pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong rust legacy coder prompts guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide how to assign authority by claim type without erasing historical origin, successor critique, current operational detail, or repository-local override. The seed calls rust-coder-01 canonical, rust-coder-02 legacy historical, and 1000IQ supporting, but complete reads show a chronological and functional hierarchy in which rust-coder-02 explicitly succeeds and critiques the first file while 1000IQ is the most operationally rigorous working draft.

**Recommended default and causal basis.** Use a faceted hierarchy: rust-coder-01 is the historical baseline and origin for local rituals, rust-coder-02 is the reliability curation and critique bridge, 1000IQ is the operational working doctrine, and explicit repository rules outrank all three for local behavior. One global canonical label cannot describe a corpus where different documents own provenance, correction, and execution semantics.

**Operating conditions and assumptions.** The hierarchy records claim family, source role, chronological relation, document status, direct evidence, successor treatment, conflict rule, repository precedence, confidence, verification, and refresh trigger. The hierarchy orders these local prompt artifacts only and cannot supersede compiler guarantees, current dependency contracts, repository tests, or user requirements.

**Failure boundary and alternatives.** Newest automatically wins, oldest remains canonical by seed label, document length becomes authority, a working draft's MUST overrides codebase policy, or multiple sources repeat one unsupported claim and inflate confidence. Bounded alternatives and recoveries: select per-heading authority, preserve a conflict set, treat a repository ADR or tests as canonical, require human adjudication, or route current framework behavior to versioned external evidence.

**Counterexample, gotchas, and operational comparison.** Rust-coder-02 calls itself a successor but cites uncollected inputs, 1000IQ declares normative authority while also declaring working-draft status, rust-coder-01 contains both local conventions and broad general material, and all can contain stale examples. Good: use rust-coder-01 to explain why four-word naming appeared, rust-coder-02 to classify it as repo-local, and 1000IQ to prefer domain-intent names under repository rules. Bad: call the first file technically canonical. Borderline: use 1000IQ as operational primary when no stronger local doctrine exists, with explicit verification.

**Verification, evidence, and uncertainty.** For each contested claim locate all versions, compare wording and rationale, identify correction or expansion, inspect repository authority, test the chosen rule, document discarded alternatives, and record the claim-specific hierarchy. The documents' own status, premise, source, local-versus-general, and output-contract sections directly establish the observed functional relationships. The corpus lacks a signed governance record naming an officially maintained successor or approval process.

**Second-order consequence.** Authority is multidimensional: historical authenticity, explanatory critique, and operational completeness can belong to different files simultaneously.

**Revision decision.** Replace the single canonical/supporting/legacy ranking with a claim-specific provenance, critique, operational, and local-override hierarchy while retaining the seed classification as historical metadata.

**Retained seed evidence.** The seed's canonical, supporting, legacy, duplicate, and conflicting vocabulary and all three source rows remain preserved for audit. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/rust-coder-01.md | canonical primary source | Codebase Wisdom 101; Technical Design101: TDD-First Architecture Principles; CRITICAL: FOUR-WORD NAMING CONVENTION (LLM Optimization) | What guidance, warning, or example should this source contribute to Rust Legacy Coder Prompts? |
| agents-used-monthly-archive/claude-skills-202602/rust-coder-02.md | legacy historical source | Rust Coder 02; Premise Check; What rust-coder-01 Gets Right | What guidance, warning, or example should this source contribute to Rust Legacy Coder Prompts? |
| idiomatic reference file/Rust-Coder-1000IQ-01.md | supporting context source | Rust Coder 1000IQ 01; 0. Document Status; 1. Fast Router | What guidance, warning, or example should this source contribute to Rust Legacy Coder Prompts? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what reviewable artifact should accompany every consequential adoption, adaptation, replacement, or retirement of a legacy coder-prompt instruction. The seed requests a worked example with user goal, decision boundary, and verification gate, but those three fields cannot show the exact legacy claim, successor correction, local-versus-general status, normative strength, affected Rust contract, alternatives, waiver, or rollback.

**Recommended default and causal basis.** Create a Legacy Prompt Migration Record containing source path and hash, exact claim locator, original intent, successor delta, repository authority, task mode, requirement, caller and boundary impact, disposition, normative level, evidence grade, alternatives, counterexample, tests, gates, waiver, rollback, owner, and refresh trigger. The record turns prompt modernization into a reproducible technical decision and prevents polished prose from hiding which obligation changed.

**Operating conditions and assumptions.** Every field has direct source or code evidence, the selected rule is narrower than its proof, rejected alternatives have consequences, fresh gates match risk, and rollback can restore compatibility and lifecycle behavior. Require the full record for public, concurrent, unsafe, architectural, or performance-affecting changes; use a justified reduced form for private low-risk conventions.

**Failure boundary and alternatives.** The artifact summarizes whole files, quotes a rule without context, omits successor disagreement, uses editorial score as evidence, lists generic tests, or records a waiver with no compensating control and expiry. Bounded alternatives and recoveries: use a small local-convention record for naming or docs, a semver appendix for public APIs, a lifecycle appendix for async work, a safety case for unsafe code, or a historical-only note when no active instruction remains.

**Counterexample, gotchas, and operational comparison.** Source snippets can omit nearby exceptions, code may already depend on the legacy behavior, removing a prompt rule can change generated output broadly, feature matrices can hide callers, and rollback may restore syntax but not persisted side effects. A complete record migrates 'every component depends on traits' into 'add seams at material external boundaries,' cites rust-coder-02 and 1000IQ, inventories real adapters and callers, compares concrete generics, adds fake plus real-adapter tests, and sets an expiry for any temporary wrapper.

**Verification, evidence, and uncertainty.** Reopen the exact source span, recompute hash, compare successor sections, inspect repository and caller evidence, challenge the disposition with a counterexample, execute all linked tests and gates, rehearse rollback, and obtain the named review. The source corpus directly supplies mode, output, rule-card, evidence-grade, waiver, test, gate, and traceability structures needed for this artifact. Specialist domains may require fields for threat models, deployment, data migration, ABI, or legal review beyond this generic record.

**Second-order consequence.** A migration record preserves why a rule changed, allowing future agents to distinguish intentional evolution from accidental prompt drift.

**Revision decision.** Expand the three seed rows into a provenance, contract, disposition, alternatives, verification, waiver, rollback, ownership, and refresh artifact.

**Retained seed evidence.** The user's concrete goal, use-or-avoid decision boundary, and command or review verification gate remain mandatory fields. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: worked rust legacy coder prompts example with user goal, decision point, failure mode, and verification gate.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Rust Legacy Coder Prompts | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide what concrete examples teach an agent to preserve durable intent while rejecting literal or overbroad legacy instructions. The seed's good, bad, and borderline cases differ only in whether sources and confidence labels were consulted; they do not show an actual legacy rule, code boundary, competing design, failure consequence, test, compatibility concern, or waiver.

**Recommended default and causal basis.** Compare examples on source context, successor critique, repository fit, caller effect, ownership and error semantics, lifecycle behavior, compatibility, disposition, counterexample, fresh verification, and reversibility. Agents learn scope from contrasts; examples must show how a plausible instruction becomes safe or unsafe under real constraints rather than merely labeling process compliance.

**Operating conditions and assumptions.** The good case demonstrates a bounded causal migration, the bad case is tempting and shows concrete damage, and the borderline case states the evidence that makes a narrower choice acceptable plus a revisit trigger. Use examples to calibrate reasoning, not as copy-ready code or a substitute for current source and repository inspection.

**Failure boundary and alternatives.** The bad case is absurd, the good case assumes a greenfield codebase, examples vary only in wording, tests are omitted, or the borderline case becomes an unreviewed permanent exception. Bounded alternatives and recoveries: use naming, trait seam, typed error, cancellation, unsafe containment, feature gate, performance, or documentation examples depending on the current task mode.

**Counterexample, gotchas, and operational comparison.** A repository may intentionally enforce naming, trait abstraction may protect multiple real adapters, dynamic errors can be correct at binary top level, cargo clean can diagnose stale artifacts, and exact API examples may age. Good: narrow 'traits for every component' to a storage boundary and prove fake plus real-adapter behavior. Bad: rename public APIs to exactly four words and add unconditional cargo clean, breaking callers and build cache without evidence. Borderline: retain an existing four-word public name for compatibility while documenting the convention as local and reviewing new names for domain clarity.

**Verification, evidence, and uncertainty.** For each example locate the source claim, state later critique, enumerate callers and failure paths, compare an alternative, run compile, behavior, misuse, feature, shutdown, or semver checks as applicable, and confirm rollback. The examples are grounded in directly observed tensions among rust-coder-01's absolutes, rust-coder-02's local-versus-general critique, and 1000IQ's contextual rules and waivers. The examples are schematic and cannot predict a repository's API consumers, build economics, adapter count, or naming policy.

**Second-order consequence.** The borderline case is often the most educational because compatibility can justify preserving legacy shape while rejecting the original universal rationale.

**Revision decision.** Replace source-loading contrasts with concrete trait, naming, cleaning, error, and compatibility migrations that include consequences, tests, alternatives, and limits.

**Retained seed evidence.** The original good use, generic misuse, and confidence-warning borderline categories remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Rust Legacy Coder Prompts after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Rust Legacy Coder Prompts as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Rust Legacy Coder Prompts only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which signals reveal whether legacy-prompt migration improves decisions and Rust outcomes without gaming speed, lint, abstraction, or documentation counts. The seed tracks compile attempts and review comments and watches hidden ownership or error tradeoffs, but these workflow counts neither isolate prompt quality nor detect stale rules, over-abstraction, compatibility damage, cancellation failures, unsupported claims, or expired waivers.

**Recommended default and causal basis.** Track unsupported instructions blocked, rules narrowed after successor comparison, migrated-rule reversals, prompt-to-requirement-to-test trace completeness, caller-visible API rework, failure-path regressions, unowned tasks, unbounded work, abstraction removals, stale evidence age, waiver expiry, and reviewer disagreement. A fast compile loop can coexist with the wrong contract, while a slower migration that prevents public or lifecycle damage may be the better outcome.

**Operating conditions and assumptions.** Each metric defines event, population, numerator and denominator, task mode, source generation, repository, observation period, exclusions, baseline, adverse threshold, owner, and feedback action. Measure prompt migration and technical decision quality, not individual productivity, causal business impact, or production reliability without separate evidence.

**Failure boundary and alternatives.** Fewer comments mean quality, more traits mean testability, all Clippy warnings mean correctness, one migration supplies percentages, agents self-score their own routing, or teams suppress disagreement to improve a dashboard. Bounded alternatives and recoveries: use case reviews for small samples, maintain a decision ledger, audit high-risk migrations, compare pre/post caller fixtures, or track qualitative surprise and rollback narratives until counts are meaningful.

**Counterexample, gotchas, and operational comparison.** One root cause can generate many findings, public compatibility defects may appear later, feature-gated failures can be missed, expected prompt retirement can look like reversal, and repository-policy exceptions are not necessarily defects. Good: review every high-risk migrated rule for unsupported scope, caller regression, and waiver expiry, then trace findings to prompt changes. Bad: claim 67 percent faster development from compile counts. Borderline: use review-time reduction only beside stable defect and disagreement outcomes.

**Verification, evidence, and uncertainty.** Publish metric definitions and raw cases, sample from a declared population, independently review ambiguous classifications, connect every adverse signal to a source or code change, test corrective action, and reassess after enough comparable observations. The seed provides workflow and hidden-tradeoff signals, while the local successors emphasize failure prevention, completion honesty, verification mix, waivers, and compatibility review. No migration baseline, controlled comparison, defect dataset, or organizational observation period accompanies the sources.

**Second-order consequence.** The best feedback signal is fewer boundary surprises: generated guidance should predict what callers, tasks, errors, and gates actually do.

**Revision decision.** Demote compile and comment counts to secondary context and add source, routing, contract, failure, compatibility, verification, waiver, and disagreement feedback loops.

**Retained seed evidence.** Compile-attempt and review-comment efficiency, hidden ownership or error failure signals, per-edit verification, and public-source refresh triggers remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: compile attempts and review comments decrease because the API shape is explicit before implementation.
Failure signal: the reference hides ownership or error tradeoffs behind generic guidance.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be demonstrated before this legacy-prompt reference or a migration decision is complete. The seed checks seven document features but does not prove complete source reads, generation-aware hierarchy, local-versus-general classification, legacy-rule disposition, mode fit, caller and lifecycle analysis, normalized packet uniqueness, frozen-span preservation, or fresh task-specific evidence.

**Recommended default and causal basis.** Require exact source and heading preservation, verified local hashes, explicit external retrieval status, all ten question decisions per section, unique rationale fields, source-generation comparison, task mode, rule disposition, affected contracts, alternatives, counterexamples, tests, waivers, rollback, routing, complete reread, and focused plus frozen-span gates. A document can look complete while still smuggling one unsupported legacy instruction into active guidance or placing correct packet answers under the wrong section.

**Operating conditions and assumptions.** Every checklist item points to a file, span, hash, packet field, decision record, code site, test output, review verdict, or explicit out-of-scope note. Apply the artifact gate to this file and the migration gate to consequential rule reuse; scale ceremony down explicitly only when risk is genuinely local and reversible.

**Failure boundary and alternatives.** Heading presence proves coverage, source paths prove full reads, unique wrapper prefixes prove substantive uniqueness, compilation proves behavior, or no open question is mistaken for no uncertainty. Bounded alternatives and recoveries: use a reduced low-risk convention checklist, add semver, async-lifecycle, unsafe, or performance appendices, or stop with a documented block when required evidence is unavailable.

**Counterexample, gotchas, and operational comparison.** Duplicated source material, section-order errors, normalized duplicate prose, preserved tables with malformed widths, code fences broken by inserted prose, hidden skipped tests, and waivers lacking expiry. A complete public-API migration preserves all frozen seed spans, records source conflicts and caller compatibility, contains 1,560 unique packet fields, runs focused verification and downstream tests, and documents rollback; a polished source summary alone is incomplete.

**Verification, evidence, and uncertainty.** Map every criterion to evidence, recompute counts and hashes, compare exact heading order, validate per-section question ownership, inspect tables and fences, rerun fresh gates, reread both artifacts, and reconcile any packet conclusion with prose. The accepted evolution contract and local 1000IQ completion-honesty, waiver, test, gate, and traceability structures directly support this checklist. Downstream code-specific completion remains impossible until a concrete repository and migrated instruction are supplied.

**Second-order consequence.** Completeness is closure across provenance, decision, implementation effect, and verification, not saturation of document structure.

**Revision decision.** Replace seven structural bullets with an evidence-indexed artifact and migration completion matrix including normalized uniqueness and full reread.

**Retained seed evidence.** Role scenario, decision options, hierarchy, artifact, examples, metrics, and adjacent-routing checks remain preserved as the structural subset. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Rust Legacy Coder Prompts.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide when this legacy-prompt migration reference should hand control to a current idiom, executable-spec, backend, security, quality-gate, or skill-design reference. The seed correctly mentions backend, executable, and quality-gate pivots but its three adjacent rows are tautological rust, legacy, and coder labels with no destination path, trigger, handoff payload, precedence, or return condition.

**Recommended default and causal basis.** Stay here for provenance and rule migration; route general idiom review to rust_idioms_checklist_patterns, executable requirements to rust_executable_reference_maps, service delivery to rust_backend_reference_routing or rust_backend_playbook_reference, resilience threats to rust_backend_security_resilience, and release checks to rust_conventions_quality_gates or rust_quality_gate_patterns. Specialized references own current operational evidence that a historical prompt atlas cannot safely reconstruct, while this file preserves the source and disposition record they need.

**Operating conditions and assumptions.** The handoff states unresolved question, destination path, trigger, current evidence, inherited constraints, expected artifact, owner, precedence, completion condition, and return check. Do not route away merely because another file mentions Rust; hand off only when a narrower current obligation governs the next decision.

**Failure boundary and alternatives.** Routing follows a keyword, several destinations are listed without selection, the target is assumed current from its filename, historical rationale is discarded, or two agents edit the same artifact. Bounded alternatives and recoveries: use repository-local documentation as primary, combine two references under one integration owner, route unsafe or FFI work to specialist review, or remain here with a provisional decision when no verified destination exists.

**Counterexample, gotchas, and operational comparison.** Idiom and executable concerns overlap, backend references still need general Rust invariants, quality gates cannot choose architecture, security can override ergonomics, and a destination file may be stale or incomplete. Good: migrate a legacy retry instruction here, then route the HTTP idempotency and timeout policy to backend resilience while returning to record the final disposition. Bad: use rust-coder-01 as an Axum runbook. Borderline: pair executable and idiom references for a public async API with separate proofs and one owner.

**Verification, evidence, and uncertainty.** Confirm the destination exists, inspect its scope and evidence status, pass the exact unresolved obligation and source record, run its gates, preserve decisions in one owner-controlled artifact, and return to recheck migration, compatibility, and waiver fields. The seed explicitly identifies backend, executable, and quality-gate handoffs, and the current reference inventory contains named destinations for those concerns. File names alone do not establish destination freshness, authority, or fitness for a particular repository.

**Second-order consequence.** Routing should preserve decision lineage: the specialist resolves the current technical question while this reference records why the legacy instruction changed.

**Revision decision.** Replace three generic adjacent labels with trigger-to-path routes, precedence, handoff payload, co-ownership prohibition, completion, and return checks.

**Retained seed evidence.** The original instruction to route HTTP delivery, executable specifications, and review gates remains the routing backbone. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use backend, executable, or quality-gate Rust references when the question shifts to HTTP delivery, specs, or review gates.
Adjacent reference 1: consider the rust adjacent reference when the current task pivots away from rust legacy coder prompts.
Adjacent reference 2: consider the legacy adjacent reference when the current task pivots away from rust legacy coder prompts.
Adjacent reference 3: consider the coder adjacent reference when the current task pivots away from rust legacy coder prompts.

## Workload Model

**Decision supported.** This section helps decide how to bound a legacy-prompt migration so source comparison, affected contracts, code evidence, verification, and rollback remain coherent. The seed frames systems, CLI, service, async, and safety-sensitive use, then imposes one crate, 100 changed symbols, and one integration path without evidence; it also truncates the rust-coder-01 heading list and ignores source-generation and rule-migration complexity.

**Recommended default and causal basis.** Scope by migrated rule set, source generations, task modes, repositories and crates, public callers, ownership and async lifecycles, error and compatibility surfaces, feature and toolchain matrices, unsafe or FFI proofs, tests, waivers, and rollback units. Migration effort grows with independent obligations and consumers, not with a fixed symbol count; one sentence can alter a public API or shutdown protocol across many call sites.

**Operating conditions and assumptions.** The workload packet states source hashes, rules in scope, exclusions, code revision, boundaries, callers, risk multipliers, feature and target matrix, evidence budget, owners, verification paths, split triggers, and merge or rollback order. This workload model governs prompt evolution and associated code decisions; production capacity and organizational planning require separate evidence.

**Failure boundary and alternatives.** A small text edit changes broad generated behavior, a large private wording cleanup is split by symbol count, one record mixes unrelated modes, or source discovery expands without a stop rule. Bounded alternatives and recoveries: split by rule family, task mode, public contract, runtime lifecycle, unsafe boundary, feature family, repository, or rollback unit; stage compatibility adapters; or perform a historical inventory before active migration.

**Counterexample, gotchas, and operational comparison.** One prompt rule can affect many generated files, duplicated wording can conceal distinct contexts, downstream callers may be outside the repo, codegen and macros amplify changes, and feature-unified CI can hide invalid combinations. Good: migrate naming, trait seams, and completion gates as separate records with shared provenance and one integration review. Bad: approve all prompt changes because fewer than 100 symbols changed. Borderline: batch purely editorial duplicate removal after proving no instruction semantics or generated output changes.

**Verification, evidence, and uncertainty.** Inventory rules and affected outputs before work, map each to callers and failure paths, estimate independent verification and rollback units, compare actual scope after each batch, trigger splits at evidence-closure failure, and run cross-slice integration checks. The sources themselves span modes, rule families, context packs, and risk-specific gates, directly supporting obligation-based rather than symbol-based scoping. The corpus provides no universal capacity threshold for claims, files, crates, context tokens, reviewers, or migration duration.

**Second-order consequence.** The unit of prompt migration is a coherent instruction contract whose provenance, downstream effect, and rollback can be verified together.

**Revision decision.** Replace fixed symbol capacity and truncated heading pressure with rule, source, mode, contract, risk, verification, split, ownership, and rollback dimensions.

**Retained seed evidence.** Systems implementation, CLI and service hardening, async and safety-sensitive review, bounded workspace scope, one integration path, source comparison, and a worked artifact remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Rust Legacy Coder Prompts as a rust system operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | systems implementation, CLI or service hardening, async/runtime review, and safety-sensitive refactoring where compile success is necessary but not sufficient | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one crate or bounded workspace slice, up to 100 changed symbols, and one integration path that exercises error handling | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Codebase Wisdom 101; Technical Design101: TDD-First Architecture Principles; CRITICAL: FOUR-WORD NAMING CONVENTION (LLM Optimization); Product thinkin | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is worked rust legacy coder prompts example with user goal, decision point, failure mode, and verification gate | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which measurable properties make repeated legacy-prompt migration trustworthy rather than merely uniform. The seed targets complete evidence labels, 18-of-20 decision accuracy, zero unsupported operational claims, and recovery clarity, but the sampled threshold lacks a baseline and the targets omit source-generation accuracy, rule disposition, repository override, waiver expiry, packet grouping, and downstream contract outcomes.

**Recommended default and causal basis.** Treat exact provenance, no false external-refresh claims, complete field population, per-section packet grouping, normalized uniqueness, frozen-span preservation, and zero unsupported production claims as deterministic invariants; establish baseline-relative targets for mode choice, routing, disposition, caller compatibility, lifecycle behavior, and reviewer agreement. Structural consistency cannot prevent an agent from faithfully applying the wrong generation or scope, while a fixed small-sample percentage creates certainty without statistical support.

**Operating conditions and assumptions.** Each target defines population, source and task strata, numerator and denominator, adjudication rubric, independent reviewer, baseline, uncertainty, threshold rationale, breach response, owner, and recalibration date. These targets assess prompt migration and reference use; service availability, security, and product correctness require specialized objectives.

**Failure boundary and alternatives.** Selected easy cases produce accuracy, deterministic checks are averaged with judgments, agents grade their own migrations, one root error counts as many independent failures, or no corrective action follows a breach. Bounded alternatives and recoveries: enumerate all high-risk decisions, use double review for ambiguous dispositions, audit regressions and waivers, maintain qualitative case studies at low volume, or defer numeric targets until a stable baseline exists.

**Counterexample, gotchas, and operational comparison.** Historical-only retention is not a failed migration, deliberate repository overrides can disagree with general doctrine, downstream defects can surface after release, and correct routing may still find an incomplete destination. Good: require every migrated claim to have provenance and verification, then baseline blinded reviewer agreement across real task modes. Bad: call 18 selected agreements 90 percent accuracy. Borderline: use a provisional threshold only with declared sampling, uncertainty, and a scheduled recalibration.

**Verification, evidence, and uncertainty.** Run deterministic artifact and claim scans over the full population, sample decisions from a declared set, capture independent verdicts and disagreements, replay caller and shutdown cases, audit waivers and routes, and trace each breach to remediation. The accepted artifact contract supports deterministic structure gates, while 1000IQ directly supports evidence grades, modes, waivers, recovery, fresh verification, and completion honesty. No historical routing baseline, reviewer-agreement distribution, migration defect rate, or production outcome sample accompanies the corpus.

**Second-order consequence.** Reliability is the chance that a migrated instruction preserves the intended contract under a real repository and failure path, not the consistency of its prose.

**Revision decision.** Retain auditable evidence and unsupported-claim invariants, remove the fixed 18-of-20 threshold, and add source, mode, disposition, compatibility, lifecycle, waiver, and grouping targets.

**Retained seed evidence.** Source-boundary preservation, decision sampling, zero unsupported operational claims, and explicit recovery paths remain the four retained target families. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide how to detect, contain, and recover from distinct failures in both the prompt-migration process and the Rust behavior it influences. The seed includes source drift, generic advice, runtime contention, and production panic, but it omits authority inversion, local-rule universalization, stale example copying, rule-buffet overdesign, public compatibility breakage, unowned task migration, waiver rot, false completion, and malformed packet ownership.

**Recommended default and causal basis.** Maintain causal rows for provenance loss, hierarchy inversion, scope widening, obsolete API transfer, unsupported precision, abstraction inflation, caller breakage, error-contract loss, cancellation or backpressure regression, unsafe proof expansion, expired waiver, verification mismatch, and artifact structure corruption. Similar symptoms such as extra compile errors or longer reviews can arise from different mechanisms, and each mechanism requires a different repair rather than another generic best practice.

**Operating conditions and assumptions.** Each row names triggering migration, affected rule and contract, causal mechanism, observable symptom, earliest detection, immediate containment, corrective disposition, rollback, owner, residual risk, and specialist route. This table covers migration and generic Rust failure classes; security incidents, distributed consistency, and domain recovery need dedicated analyses.

**Failure boundary and alternatives.** Failure names are slogans, mitigation repeats the preferred pattern, prompt defects and code defects are conflated, rollback cannot restore public or persisted behavior, or verification never reproduces the symptom. Bounded alternatives and recoveries: quarantine one rule, revert generated guidance, preserve a compatibility facade, narrow source authority, add a regression harness, route a specialist boundary, or mark the mechanism unknown pending instrumentation.

**Counterexample, gotchas, and operational comparison.** Multiple failures can share one legacy sentence, compatibility breaks can compile internally, async shutdown defects may appear only under load, source hashes prove identity not truth, and packet misordering can leave total counts unchanged. Good: classify a detached-task regression as scope-widened legacy async guidance, stop rollout, restore supervisor ownership, add cancel-during-work and teardown tests, and update the migration record. Bad: blame Tokio and add retries. Borderline: retain a compatibility wrapper while the new typed API is staged and measured.

**Verification, evidence, and uncertainty.** Reproduce the smallest failing migration or runtime case, trace source-to-disposition-to-code flow, observe the claimed mechanism, apply one containment, run targeted plus broader gates, test rollback, and record residual uncertainty. The three sources directly expose broad legacy absolutes, successor reliability gaps, and detailed anti-pattern, async, compatibility, unsafe, waiver, and completion failure classes. Their real-world frequency, severity, and interaction across repositories are not measured.

**Second-order consequence.** The most consequential prompt failure is a hidden obligation transfer: an instruction simplifies agent output by pushing compatibility, lifecycle, or proof burden onto callers and operators.

**Revision decision.** Expand four rows into a dual prompt-and-code causal registry with detection, containment, corrective disposition, rollback, owner, residual risk, and routing.

**Retained seed evidence.** Source drift, generic unverified advice, concurrent contention, and unchecked panic remain preserved as high-priority failures. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| runtime contention masks failure | async tasks, locks, or shared state degrade under concurrent load | add contention benchmark, timeout budget, and structured cancellation path |
| panic path reaches production | unwrap or unchecked invariant survives compile and unit tests | require panic audit, Result propagation, and integration failure fixture |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide when another prompt, source read, test run, or runtime attempt can add evidence and what backpressure prevents repeated work from laundering a contradiction or overloading the system. The seed limits stale-evidence refresh, stops work on red gates, checkpoints long workflows, and assigns one owner, but it does not distinguish prompt regeneration, source retrieval, flaky verification, deterministic code failure, or production retry semantics.

**Recommended default and causal basis.** Classify first: reread an incomplete local span deterministically, refresh stale public evidence once when authorized, replay a suspected flaky observation under a bounded diagnostic plan, fix deterministic failures, and route production retries to a domain reference that can prove idempotency, deadlines, capacity, and cancellation. Retry consumes context, time, compute, and often external capacity; repeating without a new information path can reinforce prompt bias, hide tests that fail consistently, duplicate effects, or delay shutdown.

**Operating conditions and assumptions.** The policy records operation, failure class, new evidence expected, maximum attempts, total budget, idempotency basis, queue or context bound, cancellation behavior, terminal result, telemetry, owner, and escalation. This section governs reference work and generic lifecycle hygiene; application network, storage, queue, and payment retries require protocol-specific evidence.

**Failure boundary and alternatives.** The agent re-prompts until a preferred answer appears, reruns Cargo until green, reloads every large source after each question, retries a public write without deduplication, or treats contradictory sources as transient. Bounded alternatives and recoveries: narrow the query, inspect the exact span, stop generation, quarantine a flaky test, fix the deterministic cause, pause producers, shed optional work, preserve a dead-letter record, roll back, or request human adjudication.

**Counterexample, gotchas, and operational comparison.** Tool output truncation can resemble missing evidence, caches can make retries differ, timeout can occur after a remote effect, nested prompt and code retries multiply attempts, cancellation can lose a partial migration record, and queue capacity can be bounded while task fan-out is not. Good: reread one truncated source range, then stop because the complete bytes are now available. Bad: regenerate a migration decision until it agrees with rust-coder-01. Borderline: rerun one scheduler-sensitive shutdown test with fixed seed and diagnostics, then quarantine and investigate any divergence.

**Verification, evidence, and uncertainty.** Inject or reproduce every failure class, count attempts and total budget, prove each retry gains named evidence, preserve first and terminal errors, observe context or queue pressure and cancellation, and confirm deterministic failures stop immediately. The seed supports bounded evidence refresh and workflow backpressure, while the local successors explicitly address retries, bounded queues, cancellation, completion honesty, and checkpoint discipline. The corpus defines no universal prompt, time, token, attempt, queue, or service budget.

**Second-order consequence.** Backpressure protects epistemic quality as well as resources: stopping prevents repeated outputs from becoming false corroboration.

**Revision decision.** Separate source reread, public refresh, prompt regeneration, test replay, deterministic repair, and runtime retry, with budgets, new-evidence criteria, idempotency, stop conditions, and routes.

**Retained seed evidence.** Failure classification, one bounded external refresh, stopping new work on red gates, frequent checkpoints, sole file ownership, and merge-time invariants remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide which compact observations let reviewers reconstruct why a legacy instruction was selected and whether the resulting code behaved as predicted. The seed records sources, proof commands, latency percentiles, errors, timeouts, retries, saturation, rollback, and compact audit evidence, but it does not expose source generation, mode, rule disposition, repository override, waiver, generated-output effect, or downstream Rust lifecycle correlation.

**Recommended default and causal basis.** Record artifact revision, source path and hash, claim locator, task mode, rule ID or text, disposition, evidence grade, affected contract, repository override, verification and outcome, waiver state, rollback trigger, owner, and redacted runtime fields for task ownership, cancellation, queue pressure, errors, and latency only when relevant. Observability should connect a migrated instruction to an observable decision and behavior; raw transcripts and aggregate metrics cannot show which scope assumption failed.

**Operating conditions and assumptions.** Events use stable fields and units, bound cardinality, preserve source and terminal error context, correlate prompt decision with code and gate results, redact sensitive inputs, and lead to a named response. The checklist governs decision and technical evidence records, not legal discovery, privacy compliance, production SLO design, or chain-of-thought capture.

**Failure boundary and alternatives.** Hidden reasoning or full prompts are logged, secrets or code payloads escape, free-form summaries cannot be queried, editorial scores become telemetry, average latency lacks workload, or runtime events cannot trace back to a migrated rule. Bounded alternatives and recoveries: use a structured migration ledger for offline work, terse command summaries for libraries, tracing spans for services, sampled high-cardinality diagnostics with expiry, or manual review for sensitive repositories.

**Counterexample, gotchas, and operational comparison.** Source paths can reveal private structure, hashes can be mistaken for trust, user input can explode labels, retry attempts can obscure the first failure, feature and toolchain context may be omitted, and telemetry schema can outlive the migrated rule. Good: record that a cancellation rule from 1000IQ changed one worker supervisor, link its shutdown tests and bounded-queue observations, and omit payload data. Bad: store the entire agent transcript. Borderline: retain detailed diagnostics temporarily for a migration incident with access control, expiry, and redaction review.

**Verification, evidence, and uncertainty.** Exercise adopt, adapt, reject, route, waiver, test-failure, rollback, cancellation, and saturation paths; inspect field presence, cardinality, correlation, redaction, access, retention, owner, and response action. The seed and 1000IQ directly support source logging, structured observability, correlation context, secret discipline, gate evidence, waivers, and completion honesty. The corpus does not prescribe a telemetry backend, schema registry, access policy, retention period, or privacy classification.

**Second-order consequence.** Migration observability should capture explicit decision rationale and evidence, not private thought process; this keeps audits useful without turning context into a liability.

**Revision decision.** Extend the generic signals into a redacted source-to-disposition-to-code observation schema with stable fields, lifecycle correlation, access, retention, and response ownership.

**Retained seed evidence.** Loaded and skipped sources, exact proof, latency or reviewer time, errors and saturation, rollback, leading and failure signals, and compact summaries remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture error count, timeout count, retry count, saturation signal, and rollback trigger.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide how to verify performance claims made by a legacy prompt, the migration workflow, or resulting Rust code without treating one timing or reduced context as proof of quality. The seed demands memory, panic, concurrency evidence and a universal local p95 below 10 ms, then mixes runtime latency with compile attempts, tool calls, source count, and reviewer decision time without a causal model.

**Recommended default and causal basis.** Separate workflow efficiency from runtime performance; for either claim define operation, requirement, workload, baseline, environment, toolchain and features, sample protocol, metric and unit, correctness guard, variance, regression threshold, decision rule, and rollback. Legacy sources contain precise timing and productivity assertions without supporting datasets, while build cache, context size, allocator, hardware, runtime topology, and input distribution can dominate observed results.

**Operating conditions and assumptions.** Measurements use representative and adversarial inputs, compare like-for-like revisions, verify outputs and failure behavior, disclose warmup and noise, retain raw summaries, and connect the result to an explicit migration decision. Measurements support only the declared operation and environment; production capacity, cost, and team productivity need broader evidence.

**Failure boundary and alternatives.** The 10 ms threshold is reused universally, compile attempts imply development speed, fewer tokens imply better decisions, debug builds support release claims, averages hide tails, or optimization weakens cancellation, API clarity, or proof without requirement value. Bounded alternatives and recoveries: use complexity analysis before timing, profile the end-to-end system, measure allocation or binary size when those are the constraint, evaluate reviewer disagreement alongside time, or retain simpler code under an explicit performance waiver.

**Counterexample, gotchas, and operational comparison.** Incremental compilation, prompt caching, filesystem cache, thermal scaling, benchmark dead-code elimination, runtime startup, feature unification, reviewer learning effects, and generated output size changing token counts. Good: profile a known parser hotspot, benchmark two implementations with correctness and worst-case fixtures, and reject unsafe optimization if the requirement is already met. Bad: repeat rust-coder-01's 67 percent faster claim. Borderline: use local reviewer time as exploratory workflow evidence with no causal or universal conclusion.

**Verification, evidence, and uncertainty.** Freeze artifact and code revisions, capture environment, run correctness gates first, collect repeated baseline and candidate observations, inspect distributions and profiles, reproduce, test adverse paths, and record uncertainty, decision, and rollback threshold. 1000IQ explicitly requires profiling, representative thresholds, named metrics, and regression guards, while rust-coder-02 rejects advanced patterns without reliability value. The corpus supplies no valid universal runtime, memory, compile, context, reviewer-time, or productivity threshold.

**Second-order consequence.** Performance evidence is valuable when it can veto a legacy optimization or workflow ritual that adds complexity without moving the actual constraint.

**Revision decision.** Remove the universal p95 gate and unsupported speed implications, split workflow and runtime methods, and add reproducibility, correctness, variance, adverse-case, decision, and rollback controls.

**Retained seed evidence.** Memory, panic, concurrency, explicit exceptions, compile and review indicators, ownership and error failure signals, measurement fields, actionability, and stop conditions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require memory, panic, and concurrency evidence; hot-path operations need p95 under 10 ms locally or an explicit benchmark exception.
Leading indicator to measure: compile attempts and review comments decrease because the API shape is explicit before implementation.
Failure signal to watch: the reference hides ownership or error tradeoffs behind generic guidance.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide when one migration record and this reference can safely coordinate legacy-prompt evolution and when sources, repositories, agents, generated outputs, or risk domains require decomposition. The seed stops at multiple systems, more than one ownership boundary, unbounded discovery, production traffic without an SLO, distributed file edits, context drift, and an unnarrowed large codebase, but one ownership transfer is too low a cutoff and prompt-scale risks are absent.

**Recommended default and causal basis.** Continue while one owner can connect every migrated instruction to provenance, affected outputs, contracts, tests, and rollback; split or route when independent repositories, prompt families, public APIs, runtime domains, unsafe proofs, security boundaries, live migrations, unbounded source discovery, or parallel writers defeat that closure. Scale becomes unsafe when no reviewer can reconstruct how a rule propagates or reverse it coherently, not merely when code crosses more than one ordinary ownership boundary.

**Operating conditions and assumptions.** The boundary review inventories sources, prompts and generated outputs, repositories, crates, consumers, teams, task modes, risk domains, feature and target matrices, owners, merge order, integration gates, rollback units, and evidence freshness. This reference may remain provenance input after routing but cannot own fleet rollout, organization-wide governance, live service migration, or threat acceptance.

**Failure boundary and alternatives.** One packet hides independent migrations, agents edit the same file, a split loses shared source decisions, downstream outputs are sampled without rationale, production changes proceed without specialized objectives, or line count alone triggers decomposition. Bounded alternatives and recoveries: split by source generation, rule family, task mode, repository, public contract, runtime lifecycle, unsafe boundary, output fleet, or rollback unit; appoint one integration verifier and shared decision ledger.

**Counterexample, gotchas, and operational comparison.** One shared system prompt can fan out to many agents, generated outputs can drift by model or tool version, common types couple separate crates, external consumers are invisible, feature matrices multiply, and rollback order can differ from deployment order. Good: keep three related naming-rule migrations together when one owner can inspect every affected prompt and API. Bad: use this file alone to rewrite a multi-repository agent fleet and live Rust services. Borderline: split by repository while one integration owner preserves shared rule IDs and cross-repo compatibility tests.

**Verification, evidence, and uncertainty.** Map propagation from source to every prompt and output class, prove each slice has complete inputs and owners, define merge and rollback order, run cross-slice tests, inspect specialist security or SLO evidence, and reevaluate after scope changes. The seed directly supports distributed ownership, context drift, traffic, and graph narrowing, while the local doctrine supports modes, context packs, workspace boundaries, public APIs, and explicit ownership. There is no universal count of sources, agents, repositories, outputs, crates, or risk domains that mandates decomposition.

**Second-order consequence.** The scale limit is traceability closure: a migration is too large when rule lineage, consequence, and recovery cannot be reviewed as one coherent unit.

**Revision decision.** Replace the one-ownership cutoff with prompt-propagation and evidence-closure criteria, explicit decomposition axes, one-writer ownership, integration, rollback, and re-entry checks.

**Retained seed evidence.** Multiple systems, unbounded discovery, missing production SLOs, one owner per split, merge checkpoints, context-drift control, and graph narrowing remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Rust Legacy Coder Prompts stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which future searches can detect drift in a migrated rule and return evidence precise enough to alter its disposition. The seed supplies three title-only searches for official docs, GitHub examples, and release notes, but 'rust legacy coder prompts' is an internal theme unlikely to retrieve authoritative evidence about specific language, runtime, framework, or prompt-migration claims.

**Recommended default and causal basis.** Query by exact obligation and authoritative source: Rust API and edition guidance for public surfaces, Cargo documentation for features and MSRV, Tokio documentation and releases for tasks and cancellation, Axum versioned docs and migrations for framework APIs, and repository history for the local prompt's origin and successors. Source-plus-obligation queries map results to a decision and avoid ranking generic advice pages as authority for an internal prompt lineage.

**Operating conditions and assumptions.** Each query records claim at risk, preferred domain or repository, version or date, expected evidence type, affected migration record, inclusion and exclusion criteria, contradiction handling, reproduction, and refresh decision. Search discovers candidate evidence and never overrides user scope, repository contracts, or stronger local tests by itself.

**Failure boundary and alternatives.** The internal title is searched verbatim, snippets replace full reads, blog recency outranks official material, main-branch examples are mixed with releases, or search results are copied into active prompts without a repository test. Bounded alternatives and recoveries: inspect local lockfile and dependency source offline, compare archived prompt history, consult release and migration notes, use issue or pull-request discussion for ambiguous behavior, or leave the claim provisional.

**Counterexample, gotchas, and operational comparison.** Edition and MSRV differences, Tokio runtime variants, Axum examples from unreleased code, feature resolver and optional dependency behavior, renamed lints, docs.rs default version selection, and repository forks. Good: search official Tokio material for cancellation behavior of the exact primitive named in a migration, pin version, and reproduce shutdown. Bad: search best Rust prompts 2026. Borderline: use a maintained repository example as support only after matching its commit, features, toolchain, and contract.

**Verification, evidence, and uncertainty.** During future authorized research, open the complete primary source, capture version and date, read caveats and migration notes, reproduce under repository conditions, compare local and successor claims, document conflicts, and rerun the affected gate before editing. The seed preserves official, repository, and release-note search families, and the local prompts identify the concrete Rust, Tokio, Axum, Cargo, API, async, feature, and diagnostic obligations worth refreshing. No web search was performed, so query quality, current source availability, release versions, and terminology remain untested.

**Second-order consequence.** The most useful refresh query is precommitted to a claim that could change and a verification capable of rejecting the new result.

**Revision decision.** Replace title-only phrases with an obligation-indexed query matrix covering local lineage and authoritative language, Cargo, Tokio, and Axum sources plus a full-read reproduction protocol.

**Retained seed evidence.** Official documentation, GitHub repository examples, and release or migration notes remain the three retained query families. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | rust legacy coder prompts official documentation best practices |
| `github_repository_query_phrase` | rust legacy coder prompts GitHub repository examples |
| `release_notes_query_phrase` | rust legacy coder prompts changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide how a reader can reproduce the provenance of a legacy-prompt claim and distinguish observed source content from migration judgment or unverified ecosystem guidance. The seed defines local, external, and combined-inference labels but omits claim locators, source hashes, generation relationships, missing upstream dependencies, external retrieval status, editorial-score treatment, conflict rules, confidence updates, and refresh triggers.

**Recommended default and causal basis.** Maintain a claim ledger with boundary label, exact path or URL, heading locator, SHA-256 or version, document generation and status, complete-read state, faithful paraphrase, successor conflict, inference step, evidence grade, verification, owner, and refresh trigger. A local file can be directly observed yet still be historically stale or normatively weak, while a preserved public URL supplies no present factual support until read.

**Operating conditions and assumptions.** Source facts are traceable to bytes and headings, external claims state retrieval status, inferences name premises and counterconditions, editorial scores remain judgments, conflicts stay visible, and operational claims have tests or explicit unknowns. The ledger provides artifact traceability, not authorship proof, legal provenance, upstream endorsement, security assurance, or current ecosystem certification.

**Failure boundary and alternatives.** Three distinct files are counted as agreement without comparing lineage, a URL is called current, missing Notes2026 inputs are silently treated as read, the seed's canonical label becomes technical truth, or hashes are mistaken for correctness. Bounded alternatives and recoveries: downgrade to provisional guidance, retain contradictory rows, omit unsupported advice, retrieve missing upstream material in an authorized refresh, route a specialist claim, or preserve a rule as history only.

**Counterexample, gotchas, and operational comparison.** Working-draft MUST language, duplicated content inside one file, mutable URLs, versionless examples, archive paths implying authority, exact hashes changing under line-ending normalization, and local repository rules superseding general doctrine. Good: identify rust-coder-01 at aaec85ca39103591e54cb3bc9a8535916853dd25256ac0f8eb4539b60f0bbd13 as historical baseline, rust-coder-02 at 71bc70834548a7304b35a94b0de8a31135f06230446fd0d02929da2690b3af9f as critique, and 1000IQ at 9006daebdade32af945c70e82eaff6487fa1ac440677c80f4739bc5f6eee2322 as operational working doctrine without calling any universally correct.

**Verification, evidence, and uncertainty.** Recompute local and seed hashes, confirm complete reads and exact headings, search repository inventory for named dependencies, audit every external statement for no-browse status, inspect inferences for widened scope, scan numeric claims for methods, and trace each recommendation to proof or uncertainty. All three mapped local files and the byte-identical working seed were completely read; their distinct hashes, internal statuses, disagreements, and missing Notes2026 inventory matches were directly checked. The four public links were intentionally not browsed, the original Notes2026 sources named by rust-coder-02 are absent from the repository inventory, and no governance record establishes official succession.

**Second-order consequence.** Evidence boundaries actively change migration outcomes by preventing source age, document status, score precision, or link presence from becoming counterfeit authority.

**Revision decision.** Expand three labels into claim-level hash, generation, status, read state, dependency, conflict, inference, evidence grade, verification, and refresh rules with explicit no-browse and missing-source notes.

**Retained seed evidence.** Local-corpus fact, external-research fact, and combined-evidence inference remain the required top-level labels. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
