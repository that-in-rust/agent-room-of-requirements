# Tdd Cycle Skill Patterns

**Decision supported.** This section helps decide how to frame an enforcement-heavy skill that is simultaneously archived and live. The seed no seed line conveys that this theme holds a 371-line enforcement-grade TDD skill from the superpowers collection, present as byte-identical twins in the 202603 sweep and the live claude-skills tree, built around the Iron Law, no production code without a failing test first, a dot-graph cycle with verification gates, and an unusual mass of counter-rationalization material.

**Recommended default and causal basis.** Treat the live claude-skills copy as operative doctrine and the sweep copy as its dated witness. The skill is written for an agent that will be tempted, most of its bulk is not procedure but pre-emptive rebuttal, rationalization tables, red-flag lists, argument-by-argument replies, because the authors evidently expected the cycle to be argued with more than misunderstood.

**Operating conditions and assumptions.** Both copies remain byte-identical, divergence would promote the live copy and date the claim. This reference covers the TDD cycle skill, the referenced testing-anti-patterns file and the retainer family belong to their own themes.

**Failure boundary and alternatives.** Summarizing it as write tests first misses its genre, this is compliance engineering, the procedure is three boxes and the fortifications around them are ten times the size. Bounded alternatives and recoveries: the tdd context retainer themes for the memory layer this cycle assumes.

**Counterexample, gotchas, and operational comparison.** The skill's severity is calibrated for production code, its own when-to-use section carves out prototypes, generated code, and configuration, quoting the Iron Law without the exception list overstates even this maximalist document. Good: citing the Iron Law with its delete-means-delete enforcement and its human-partner exception valve. Bad: presenting TDD mechanics as the skill's main content. Borderline: applying the skill's severity to exploratory scripts, its own exceptions cover that, route through the human partner.

**Verification, evidence, and uncertainty.** Confirm the section inventory against either twin before structural claims. Both files read in full with checksums this session. The superpowers collection's authorship and upstream origin are unrecorded locally.

**Second-order consequence.** The live-tree copy makes this one of the few themes whose source is currently deployed doctrine rather than archive history, the twins differ not in content but in status, one is a record, the other is an instruction an agent may be operating under right now, including agents working in this very repository.

**Revision decision.** Open with the Iron Law, the twin placement across archive and live tree, and the procedure-versus-fortification proportions.

**Retained seed evidence.** The exact theme title and framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide what the effective evidence set is and what the archive-live match proves. The seed two local rows list paths without recording that they are byte-identical twins with one checksum, but with a topology new to the corpus, not two archive sweeps but archive-and-live, the 202603 sweep copy dating the content and the claude-skills copy deploying it, one document witnessed as both history and current instruction.

**Recommended default and causal basis.** Cite the live copy for operative claims and the sweep copy to date content stability. The live tree is the repository's operative skill set while the monthly sweeps are its snapshots, so equality here means the skill has not changed since at least the 202603 sweep, a stability reading rather than a redundancy reading.

**Operating conditions and assumptions.** The live tree remains the deployment surface, a restructure would demote the inference. Provenance for this theme's claims, the superpowers collection's other skills have their own themes where mapped.

**Failure boundary and alternatives.** Treating the pair as archive redundancy misses the status difference, and counting them as two sources inflates one text into false corroboration. Bounded alternatives and recoveries: treating the sweep copy as canonical, backwards, live status outranks archive date for operative questions.

**Counterexample, gotchas, and operational comparison.** Future audits must expect divergence as the normal evolution path, the live copy changing while the sweep stays frozen, equality is the current finding, not a permanent property. Good: one-document synthesis with the match read as stability-to-present. Bad: two-source corroboration claims. Borderline: assuming the live copy is loaded by agents in this repo, plausible from placement, verify before asserting operation.

**Verification, evidence, and uncertainty.** Re-run both checksums and re-test equality at each audit. Checksum runs and full reads of both paths this session. Whether upstream superpowers versions exist beyond this repository is unknown locally.

**Second-order consequence.** Archive-live equality is a freshness measurement the twin themes before this could not provide, twin sweeps prove only that two snapshots agree, an archive-live match proves the doctrine survived to the present unchanged, making this the corpus's first source pair that dates its own currency.

**Revision decision.** Annotate the rows with the shared checksum, the archive-versus-live status split, and the stability inference.

**Retained seed evidence.** The two local rows and three external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/superpowers/test-driven-development/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| claude-skills/superpowers/test-driven-development/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| https://developers.openai.com/codex/guides/agents-md | external_research_source_material | external_research_sourced_fact | primary agent instruction context model |
| https://docs.github.com/actions | external_research_source_material | external_research_sourced_fact | verification and automation model |
| https://agents.md/ | external_research_source_material | external_research_sourced_fact | general agent instruction format |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which of the skill's patterns deserve default-tier ranking on structural evidence. The seed three corpus-process rows stand where the skill's own priorities rank, the Iron Law leads as the single rule everything else defends, then the two mandatory verify gates, watch it fail and watch it pass, the only steps marked mandatory never skip, then delete-means-delete, the enforcement that makes the law binding rather than advisory.

**Recommended default and causal basis.** Hold the law absolute inside its scope, run both gates every cycle, and enforce violations by deletion rather than adaptation. The law ranks first because the skill says so structurally, every argument section, flag list, and table exists to hold that one line against erosion, the document is a fortress with a single treasure.

**Operating conditions and assumptions.** The ranking reflects the document's structure and emphasis, not usage data. Ranking this skill's patterns, corpus-process patterns rank in the seed rows.

**Failure boundary and alternatives.** Ranking the red-green-refactor mechanics first mistakes the diagram for the doctrine, the cycle is standard TDD, what this skill adds is the law and its enforcement apparatus. Bounded alternatives and recoveries: the seed's process-tier scoreboard for corpus workflow ranking.

**Counterexample, gotchas, and operational comparison.** Verify-red has a failure taxonomy of its own, a test that errors rather than fails, or fails from a typo rather than the missing feature, passes a careless gate while proving nothing, the gate checks the reason, not just the redness. Good: a cycle with both gate runs logged and a wrong-failure loop honored. Bad: trusting a red test that errored on an import. Borderline: batching several red tests before any green, the diagram shows one test per loop, batch discipline needs explicit judgment.

**Verification, evidence, and uncertainty.** Spot-check ranking claims against the document's structure at audit. The law, gates, and enforcement sections read verbatim this session. How often the deletion enforcement is actually exercised is unrecorded.

**Second-order consequence.** The verify-red gate is the skill's epistemological core, its core principle says an unfailed test proves nothing about what it tests, so watching the failure is not procedural hygiene but the only evidence the test measures the intended behavior, the gate manufactures the proof the law then protects.

**Revision decision.** Present the three-pattern core, law, gates, enforcement, with the fortress reading as their relation.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `tdd_cycle_skill_patterns` | 95 | default_adoption_pattern_tier | Source Map First: Load local tdd cycle material before synthesizing skill patterns recommendations. |
| `tdd_cycle_skill_patterns` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `tdd_cycle_skill_patterns` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what the skill fundamentally claims about where TDD's value lives. The seed generic corpus formulas stand where the skill states its thesis twice with escalating force, the core principle, if you didn't watch the test fail, you don't know if it tests the right thing, and the sharper genre claim, violating the letter of the rules is violating the spirit of the rules, together asserting that TDD's value lives in its ritual and cannot be recovered by good intentions after the fact.

**Recommended default and causal basis.** Treat unfailed tests as unverified claims and test ordering as evidence-bearing, not stylistic. The letter-is-spirit line exists because the most persuasive violations arrive dressed as spirit, tests-after achieve the same goals, it's about spirit not ritual, so the skill closes the loophole by defining the ritual as the spirit.

**Operating conditions and assumptions.** The code is production code, the skill's own exception list bounds the thesis. The thesis governs the cycle's epistemology, memory and cadence around the cycle belong to the retainer themes.

**Failure boundary and alternatives.** The spirit-first reading fails on the skill's own analysis, tests written after pass immediately, and immediate passage is evidence of nothing, the ritual's ordering is what manufactures the evidence. Bounded alternatives and recoveries: coverage-metric thinking, which the thesis dismantles as measuring presence rather than proof.

**Counterexample, gotchas, and operational comparison.** The thesis makes passing-immediately a warning sign, inverted from ordinary intuition where passing is good news, a green first run means fix the test, the skill's most counterintuitive instruction. Good: rejecting a test that passed on first run and rewriting until it fails for the right reason. Bad: backfilling tests to reach coverage and calling it TDD. Borderline: writing several assertions then watching them fail together, ordering preserved at coarser grain, note the grain.

**Verification, evidence, and uncertainty.** Audit cycles for witnessed failures before every implementation. The overview and why-order-matters sections read verbatim this session. The thesis's empirical performance claims, TDD faster than debugging, are asserted, not cited.

**Second-order consequence.** The skill's deepest claim is temporal, tests-first ask what should this do while tests-after ask what does this do, the same assertions in a different order measure different things, requirement versus recollection, which is why no quantity of after-the-fact coverage converts into TDD, the ordering is the information.

**Revision decision.** State the double thesis, failure-witnessing as evidence manufacture, and letter-as-spirit as the anti-loophole.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `tdd_cycle_skill_patterns` contains 2 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Tdd Cycle Skill Patterns comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide how the skill's sections divide between teaching and defending. The seed heading signals name six sections while the working map is a three-ring fortress, the procedure at the center, cycle diagram, phase sections, worked example, checklist, a middle ring of judgment aids, good-tests table, when-stuck table, debugging integration, and an outer ring of defenses, why-order-matters arguments, rationalization table, red flags, final rule.

**Recommended default and causal basis.** Learn the center once, internalize the outer ring, and consult the middle ring per incident. The rings exist because the skill fights on two fronts, competence, does the agent know the procedure, and compliance, will the agent follow it under temptation, and the outer ring exists entirely for the second front.

**Operating conditions and assumptions.** The document remains one file, ring claims are cheap to verify. Navigation of this theme's one document, the referenced anti-patterns file navigates through its own theme.

**Failure boundary and alternatives.** Reading only the center produces an agent that knows TDD and abandons it at the first schedule pressure, the defenses are load-bearing for exactly the moments the procedure is most likely to be dropped. Bounded alternatives and recoveries: heading order navigation, workable, the rings explain why the document is shaped this way.

**Counterexample, gotchas, and operational comparison.** The dot diagram carries unique content, the wrong-failure back-edge from verify-red and the stay-green edge on refactor appear nowhere in the prose as explicitly, skipping the graph loses two edges of the machine. Good: consulting the when-stuck table for a hard-to-test design and the flag list for a tempting shortcut. Bad: excerpting the procedure without the defenses for agent use. Borderline: teaching humans from the center only, humans face the same temptations, the outer ring transfers.

**Verification, evidence, and uncertainty.** Trace section claims to ring positions at review. The whole document read and mapped this session. None about structure, all 371 lines were read wholesale.

**Second-order consequence.** The rationalization table and red-flag list are the same content compiled twice for different moments, the table rebuts excuses analytically for when the agent is reasoning, the flag list pattern-matches phrases for when the agent is already rationalizing, one doctrine in a slow form and a fast form, recognition beating deliberation at the moment of temptation.

**Revision decision.** Annotate the map with the three rings and the two-front reading, procedure for competence, defenses for compliance.

**Retained seed evidence.** The two local source rows with titles, heading signals, and usage roles remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/superpowers/test-driven-development/SKILL.md | test-driven-development | Test-Driven Development (TDD); Overview; When to Use; The Iron Law; Red-Green-Refactor; RED - Write Failing Test | skill entrypoint or reusable agent prompt |
| claude-skills/superpowers/test-driven-development/SKILL.md | test-driven-development | Test-Driven Development (TDD); Overview; When to Use; The Iron Law; Red-Green-Refactor; RED - Write Failing Test | skill entrypoint or reusable agent prompt |

## External Research Source Map

**Decision supported.** This section helps decide which external claims this theme owns and how each decays. The seed three inherited agent-format anchors stand while this skill's external surface is a typescript-and-jest dialect, npm test invocations, jest mock APIs in the bad examples, typescript generics in the good ones, plus a relative reference to @testing-anti-patterns.md and the dot notation of its diagram, none of it load-bearing for the doctrine.

**Recommended default and causal basis.** Reuse the doctrine across languages freely and treat the typescript examples as one dialect's illustration. The examples need some concrete language and the authors chose the typescript ecosystem, but every rule, gate, and defense is language-independent, the dialect is packaging.

**Operating conditions and assumptions.** The consuming stack has runnable tests with observable failures, the gates need that substrate. External dependencies for freshness tracking, all URLs in inherited rows stayed unretrieved.

**Failure boundary and alternatives.** Reading the skill as typescript-specific would wall off doctrine that transfers to any test-capable stack, the corpus's own python-based process is a working counterexample. Bounded alternatives and recoveries: the seed's inherited AGENTS.md and Actions anchors, retained for ecosystem context.

**Counterexample, gotchas, and operational comparison.** The @testing-anti-patterns.md reference is relative and collection-internal, consumers excerpting this skill elsewhere break the link silently, the anti-pattern content must travel or be re-pointed. Good: applying the verify gates in a pytest workflow unchanged. Bad: dismissing the skill for a rust project because its examples are typescript. Borderline: porting the examples to another language for local teaching, faithful if the good-bad contrasts survive translation.

**Verification, evidence, and uncertainty.** Check the relative reference's resolution wherever the skill is deployed. The example blocks and reference line read verbatim this session. Whether the anti-patterns file exists at the expected relative path was not resolved this session.

**Second-order consequence.** The skill's decay surface is almost entirely in its examples, jest APIs and npm commands will date while the Iron Law cannot, so future refreshes should expect example rot with doctrine stability, the inverse decay profile of API-documenting references where the rules rot and the examples survive as fossils.

**Revision decision.** Keep the inherited rows and note the dialect inventory, the relative file reference, and the doctrine's language independence.

**Retained seed evidence.** All three external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://developers.openai.com/codex/guides/agents-md | OpenAI Codex AGENTS.md guide | primary agent instruction context model | external_research_sourced_fact |
| https://docs.github.com/actions | GitHub Actions documentation | verification and automation model | external_research_sourced_fact |
| https://agents.md/ | AGENTS.md open format | general agent instruction format | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which abandonment arguments the skill pre-refutes and with what uniform response. The seed three corpus-process rows stand where the skill ships the corpus's largest native registry, eleven rationalization rows each pairing an excuse with its reality, too simple to test, I'll test after, keep as reference, deleting hours is wasteful, TDD is dogmatic, and thirteen red flags including the meta-flag, this is different because, the excuse-shaped excuse for all excuses.

**Recommended default and causal basis.** Match temptations against the flag list verbatim and treat a match as a stop condition, not a discussion prompt. The registry is large because it was evidently harvested from real arguments, each row reads like a transcript fragment, the skill is a fossil record of every way its authors watched TDD get abandoned.

**Operating conditions and assumptions.** The human-partner exception valve remains available for the genuinely exceptional case. Cycle-abandonment rejections, mock-and-test-design anti-patterns route to the referenced anti-patterns file's theme.

**Failure boundary and alternatives.** The registry's own analysis says these failures compound through plausibility, each excuse is locally reasonable, sunk cost, pragmatism, spirit over ritual, and the registry exists because reasonableness is exactly how the law erodes. Bounded alternatives and recoveries: entry templates cannot help here, this registry polices reasoning rather than artifacts, review and self-scan are the only enforcement.

**Counterexample, gotchas, and operational comparison.** The flags are phrase-shaped on purpose, hearing yourself say the words is the detection event, agents should scan their own reasoning for the listed phrasings, self-application is the intended use. Good: catching already spent three hours mid-thought and deleting the untested code. Bad: granting this is different because it's just a small utility. Borderline: a genuine throwaway prototype, the exception list covers it, the valve is asking, not deciding alone.

**Verification, evidence, and uncertainty.** Confirm registry rows quote the skill's tables faithfully at audit. The rationalization table and red-flag list read verbatim this session. Which excuses fire most often in practice is unrecorded.

**Second-order consequence.** Every row resolves to the same reality, the excuse optimizes for felt speed while the law optimizes for evidence, and the skill's uniform answer, delete and start over, is deliberately non-negotiable because case-by-case adjudication is itself the vulnerability, a registry that negotiates is a registry that loses.

**Revision decision.** Surface the native registry as the section's core with the meta-flag as its closing insight.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide what qualifies a red and a green under the skill's gates. The seed document verifier text stands where the skill is itself a verification doctrine, two mandatory gates with npm test invocations, verify-red demanding the test fail for the expected reason with the expected message, verify-green demanding passage, full-suite health, and pristine output with no errors or warnings, plus the eight-box completion checklist whose failure verdict is you skipped TDD, start over.

**Recommended default and causal basis.** Run both gates with reason checks every cycle and hold green runs to pristine output. The gates exist because both colors can lie, a red test may error rather than fail and a green run may hide breakage in other tests or warnings in output, so each gate specifies what qualifies, not just what color.

**Operating conditions and assumptions.** The test runner reports failures distinguishably from errors, the reason check needs that visibility. The skill's verification doctrine, this document's own structural verification keeps the seed's retained block.

**Failure boundary and alternatives.** Gate theater is the characteristic failure, running the command without checking the reason, a typo-red or a warning-polluted green waved through proves nothing while feeling rigorous. Bounded alternatives and recoveries: the corpus verifier command in the seed's retained block for this document's own checks.

**Counterexample, gotchas, and operational comparison.** Verify-green includes other tests still pass, a locally green change that breaks the suite elsewhere fails the gate, fix now is the skill's phrasing, deferral is not offered. Good: a red gate log noting fails because feature missing, message as expected. Bad: accepting FAIL output that was actually a module resolution error. Borderline: a deprecation warning from a dependency at green, outside the change's control, document it rather than silently tolerating.

**Verification, evidence, and uncertainty.** Check cycle records for reason-qualified gate entries at sampled sessions. Both gate sections and the checklist read verbatim this session. How strictly pristine output is held in real deployments is unrecorded.

**Second-order consequence.** Pristine output is the gate's most easily dismissed clause and its most forward-looking, warnings tolerated at green become the noise floor that hides tomorrow's real signal, the skill treats output cleanliness as part of correctness because degraded signal quality is a future verification failure already in progress.

**Revision decision.** State the qualified-gate reading, each gate checks color, reason, and cleanliness, with the checklist as the cycle-level gate.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide how agents should hold a law they cannot waive. The seed four generic bullets stand where the skill is explicitly agent-directed doctrine with a human in its loop, the description fires it on any feature or bugfix before implementation code, the exception list routes through ask your human partner, and the final rule forbids exceptions without that partner's permission, an authority structure, agent bound by law, human holding the waiver.

**Recommended default and causal basis.** Load the skill on any implementation task, hold the law without self-granted exceptions, and escalate genuine exception candidates to the human partner. The skill assumes agents rationalize fluently, its entire outer ring targets machine-generated excuses, and it solves the authority problem by denying the agent waiver power entirely rather than trusting agent judgment about exceptions.

**Operating conditions and assumptions.** A human partner is reachable, the waiver valve assumes one exists. Agent consumption of the cycle skill, journaling the cycle's state belongs to the retainer themes.

**Failure boundary and alternatives.** An agent that grants itself exceptions has, by the skill's own meta-flag, already failed, this is different because is listed as a red flag precisely because self-waivers always arrive reasoned. Bounded alternatives and recoveries: softer TDD guidance for teams that reject the deletion enforcement, with the skill's own warning that softening is the failure mode.

**Counterexample, gotchas, and operational comparison.** The description's trigger, before writing implementation code, means the skill loads at the moment of maximum temptation, an agent already holding drafted code is already in violation, the load timing is the enforcement. Good: an agent pausing a bugfix to write the reproducing test first and watching it fail. Bad: an agent keeping generated code as reference while writing tests. Borderline: an agent asked to prototype quickly, the exception list applies, confirm the throwaway status with the human first.

**Verification, evidence, and uncertainty.** Sample agent implementation sessions for law adherence and waiver routing. The description, exception list, and final rule read verbatim this session. How often human partners grant exceptions in practice is unrecorded.

**Second-order consequence.** The skill and the retainer family compose into a complete agent TDD stack documented across this corpus's themes, this skill governs what the cycle demands, the retainer governs how its state survives interruption, and the cadence playbook governs when state gets captured, three families the archive keeps separate that an operating agent runs as one system.

**Revision decision.** Recast the section around the law-and-waiver authority structure with the agent as bound party.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide what a complete bug-fix cycle looks like under the law. The seed one generic engineer stands where the skill's own bug-fix example traces the natural journey, an empty email accepted in production, the fixer writes the rejecting test first, watches it fail with expected Email required got undefined, implements the two-line guard, watches it pass, and considers extraction only after green, the whole cycle in four artifacts and two gate runs.

**Recommended default and causal basis.** Open every bug fix by encoding the reproduction as a failing test and let the fix be whatever turns it green. The journey works because the bug itself becomes the test specification, a reproduced failure is a requirement statement with proof built in, which is why the skill's debugging integration says never fix bugs without a test.

**Operating conditions and assumptions.** The bug is reproducible in the test harness, irreproducible bugs need diagnosis first. Journeys through this skill's cycle, session interruption journeys belong to the retainer themes.

**Failure boundary and alternatives.** The journey's failure branch is the direct patch, guard added first, test after or never, leaving no proof the guard addresses the reported case and no tripwire when a refactor later drops it. Bounded alternatives and recoveries: the feature journey, same cycle with the specification written from requirements instead of from a defect.

**Counterexample, gotchas, and operational comparison.** The verify-red step must show the bug's actual signature, expected Email required got undefined, a test failing some other way reproduces a different bug, the gate's reason check catches the mismatch. Good: the empty-email trace, red with the bug's signature, minimal guard, green, then refactor. Bad: patching the guard in and confirming manually in the browser. Borderline: fixing an adjacent bug noticed mid-cycle, the diagram says one loop at a time, journal it and take it next.

**Verification, evidence, and uncertainty.** Audit bug-fix commits for reproducing tests preceding fixes. The example-bug-fix and debugging-integration sections read verbatim this session. The example's simplicity is deliberate, journey shape at gnarly-bug scale is asserted by rule, not shown.

**Second-order consequence.** The bug-fix case is TDD's cleanest sell because the failing test costs nothing extra, the bug must be reproduced to be fixed anyway, and encoding the reproduction as a test converts a one-time diagnostic into a permanent regression guard, the skill's never-fix-without-a-test rule is thus its easiest law to justify economically.

**Revision decision.** Recast the scenario as the bug-to-test round trip using the skill's own empty-email example as the trace.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The technical lead or implementation agent is starting from an ambiguous request that must become acceptance criteria and tests and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `tdd_cycle_skill_patterns` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing the requirement IDs, negative cases, verification gates, and handoff evidence.
Reference opening trigger: Open this file when the task mentions tdd cycle skill patterns, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide which side wins each recurring cycle trade-off and by whose authority. The seed template rows skip the trade-offs the skill adjudicates and its arguments section litigates, felt speed versus evidence, resolved by the claim that TDD is faster than debugging in production, sunk work versus trustworthy code, resolved by deletion since kept unverified code is technical debt, flexibility versus enforceability, resolved by letter-is-spirit, and strictness versus practicality, resolved by the human-partner exception valve rather than by agent discretion.

**Recommended default and causal basis.** Resolve cycle trade-offs toward evidence and route genuine exceptions through the human valve. Every ruling trades a present comfort for a future guarantee, and the skill can hold the harsh line because it pairs each ruling with the valve, absolutism inside the scope, escalation at the boundary.

**Operating conditions and assumptions.** The valve is honored as an asking mechanism, a rubber-stamp valve is discretion wearing a process costume. Trade-off structure the skill teaches, corpus-process trade-offs stay in the seed rows.

**Failure boundary and alternatives.** Resolving any of these by agent discretion reopens the loophole the letter-is-spirit line closed, discretion is where every listed rationalization lives. Bounded alternatives and recoveries: risk-tiered TDD policies that scale strictness by code criticality, unsupported by this skill's text, which tiers by code genre instead.

**Counterexample, gotchas, and operational comparison.** The practicality objection inverts under the skill's analysis, pragmatic shortcuts equal debugging in production equals slower, the skill claims the pragmatist's own metric, speed, as its justification, not despite it. Good: deleting an afternoon's untested implementation and rebuilding it test-first. Bad: keeping it as reference during the rewrite. Borderline: a config-file change, the exception list covers configuration, confirm rather than assume.

**Verification, evidence, and uncertainty.** Review cycle decisions for valve routing at retrospectives. The why-order-matters arguments read as rulings this session. The faster-than-debugging claim's magnitude is asserted without local measurement.

**Second-order consequence.** The deletion ruling is the skill's boldest economics, it prices untested working code as negative value, debt rather than asset, because code you cannot trust costs verification eventually anyway plus interest in shipped bugs, deletion just pays the bill at today's rate, the sunk-cost framing makes the harsh ruling arithmetically ordinary.

**Revision decision.** Add the four trade-off rows with the future-over-present resolver and the valve as the pressure release.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the tdd cycle skill patterns pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong tdd cycle skill patterns guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which file governs which question class within this small family. The seed role labels rank two paths while the operative hierarchy places this skill inside the superpowers collection twice, live tree and sweep snapshot, as a peer of sibling skills with at least one declared dependency edge, its pointer to testing-anti-patterns for mock pitfalls, and above nothing, it is a leaf skill that governs behavior rather than routing to depth files.

**Recommended default and causal basis.** Cite this skill directly for cycle doctrine and follow its single outbound link only for mock-related questions. The superpowers collection packages independent behavioral skills rather than entrypoint-plus-references families, so this skill's hierarchy is collection membership plus explicit cross-links, flat by design.

**Operating conditions and assumptions.** The collection keeps its flat structure, a future refactor into references would change the reading. Precedence within this theme's family, the anti-patterns file and other superpowers skills rank in their own themes.

**Failure boundary and alternatives.** Expecting a retainer-style tree here misreads the genre, there is no depth layer to route to, the 371 lines are the whole doctrine, and the one outbound link is a peer reference, not a child. Bounded alternatives and recoveries: treating the sweep copy as a hierarchy layer, wrong, it is the same node witnessed twice.

**Counterexample, gotchas, and operational comparison.** Flat does not mean standalone, the anti-patterns dependency means excerpting this skill without that file leaves its mock guidance dangling, noted already at the external surface, structural here. Good: answering cycle questions from this skill and mock-testing questions via its link. Bad: hunting for a references directory this genre does not have. Borderline: citing sibling superpowers skills as context, legitimate collection reading, outside this theme's mapped sources.

**Verification, evidence, and uncertainty.** Test citation targets against the flat topology at audit. Both copies' placement and the outbound reference verified this session. The full sibling inventory of the superpowers collection was not enumerated this session.

**Second-order consequence.** The corpus has now documented three family shapes, the retainer's entrypoint-with-references tree, the tauri doctrine's scored-pattern monolith, and this collection's flat peers with cross-links, and the shapes track enforcement style, trees for progressive loading, monoliths for reference depth, flat peers for always-on behavioral law that must load whole or not at all.

**Revision decision.** State the flat topology, collection peer with one outbound edge, present in both live and archived copies.

**Retained seed evidence.** The hierarchy rows and the classification vocabulary remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/superpowers/test-driven-development/SKILL.md | canonical primary source | Test-Driven Development (TDD); Overview; When to Use | What guidance, warning, or example should this source contribute to Tdd Cycle Skill Patterns? |
| claude-skills/superpowers/test-driven-development/SKILL.md | supporting context source | Test-Driven Development (TDD); Overview; When to Use | What guidance, warning, or example should this source contribute to Tdd Cycle Skill Patterns? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing structure makes gate witnessing durable and auditable. The seed generic worked-example artifact misses this theme's natural object, a cycle compliance log, one row per red-green-refactor loop recording the test name, the verify-red evidence, failure message and reason class, the verify-green evidence, suite status and output cleanliness, and any flag-list matches encountered and their resolutions, deletion, valve escalation, or clean pass.

**Recommended default and causal basis.** Log every loop at gate time and answer the completion checklist from the log, not from recall. The skill demands witnessed gates but provides no witnessing medium, and its checklist asks at completion whether each test was watched failing, a question only answerable from records made at gate time.

**Operating conditions and assumptions.** Gate output is capturable, test runners make this trivial. One log format for any workstream under this skill, composable with the retainer journal as its cycle-grain layer.

**Failure boundary and alternatives.** Without the log, the checklist is answered from memory, and the skill's own doctrine holds that unwitnessed claims prove nothing, the completion gate inherits exactly the weakness the cycle gates were built to remove. Bounded alternatives and recoveries: the retainer journal alone, which captures session grain but not per-loop gate evidence.

**Counterexample, gotchas, and operational comparison.** The log must record the failure reason class, feature-missing versus error versus typo, since that distinction is what the red gate actually checks, a log of bare pass-fail colors misses the gate's substance. Good: a loop row with the red message verbatim and a flag column reading none. Bad: a completion checklist ticked from memory a day later. Borderline: logging only the red gate since green is in CI history, half the evidence, the pristine-output check has no CI witness.

**Verification, evidence, and uncertainty.** Re-derive sampled checklist answers from log rows at audit. The gate requirements and checklist read together this session. The log's overhead at high loop frequency is untested locally.

**Second-order consequence.** The log makes the outer ring auditable for the first time, flag matches and their resolutions become data, revealing which rationalizations actually fire in a team or agent and how often the valve gets invoked, turning the skill's fossil record of past arguments into a live measurement of present ones.

**Revision decision.** Define the artifact as the per-loop log with gate evidence and flag-resolution columns.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: RED/GREEN transcript with refactor rule and resume checkpoint.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Tdd Cycle Skill Patterns | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercises transmit gate discipline and example discrimination fastest. The seed abstract usage lines stand where the skill teaches by paired contrast, the retryOperation good-bad pair at red showing a real-behavior test against a mock-measuring one, the same function's good-bad pair at green showing just-enough code against a YAGNI-commented option bag, and the empty-email fix as the full-cycle trace, contrasts carrying the doctrine that tables only state.

**Recommended default and causal basis.** Certify understanding by having learners diagnose both bad examples before writing their own cycle. The pairs work because each bad example is plausible, the mock test looks rigorous and the option bag looks thorough, the lesson is precisely that plausible and correct diverge, which prose assertion cannot teach as fast as adjacent code.

**Operating conditions and assumptions.** Learners read typescript enough to follow the pairs, translation preserves the contrasts otherwise. Teaching this theme's material, mock anti-pattern depth routes to the linked file's theme.

**Failure boundary and alternatives.** Teaching from the good examples alone loses the discrimination training, learners must see why the bad mock test passes review while proving nothing about the code. Bounded alternatives and recoveries: this corpus's own generator-and-verifier discipline as a non-typescript worked instance of test-first thinking.

**Counterexample, gotchas, and operational comparison.** The bad red example's flaw is subtle on first read, it calls the mock and asserts on the mock, the real retryOperation's behavior is never exercised, learners who miss this will write mock-measuring tests believing them real. Good: a learner explaining why attempts counting in a closure beats mock call counting. Bad: memorizing the good examples as templates. Borderline: practicing on the empty-email trace verbatim, faithful start, real certification needs a novel behavior.

**Verification, evidence, and uncertainty.** Grade diagnoses of the bad examples at review. All four example blocks read verbatim this session. Transfer from diagnosis to sustained practice is unmeasured.

**Second-order consequence.** The bad green example refutes itself in one comment, the YAGNI marker sits inside the speculative options object, the code confessing its own violation, a compressed teaching device where the anti-pattern and its diagnosis occupy the same line, the skill's densest single teaching moment.

**Revision decision.** Anchor the section on the two contrast pairs and the full trace, with plausibility-versus-proof as the common lesson.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Tdd Cycle Skill Patterns after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Tdd Cycle Skill Patterns as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Tdd Cycle Skill Patterns only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which numbers show the law working versus being performed. The seed compile-centric indicator misses the cycle's measurable surface, witnessed-failure rate, loops whose red gate evidence exists, first-run-pass incidents, tests that never failed and were fixed or investigated, deletion events, the enforcement actually exercised, valve invocations with outcomes, and regression catches, bugs stopped by tests written in earlier cycles, the law's long-term payoff appearing as prevented incidents.

**Recommended default and causal basis.** Log the five measures per workstream and defend the practice's cost with the regression-catch series. The skill's promises are distributed in time, gates pay immediately in test validity while the law pays over months in regression prevention, so the metric set must span both horizons or the slow payoff goes unmeasured and the practice gets priced on cost alone.

**Operating conditions and assumptions.** Cycles are logged at gate grain, the compliance log from the artifact section is the substrate. Measuring cycle practice, session-continuity metrics belong to the retainer themes.

**Failure boundary and alternatives.** Measuring only velocity invites exactly the skill's listed rationalizations, TDD will slow me down wins any argument where prevented bugs are invisible in the data. Bounded alternatives and recoveries: coverage percentages, which the skill's own arguments dismantle as presence without proof.

**Counterexample, gotchas, and operational comparison.** Deletion events at zero is ambiguous between perfect discipline and unenforced law, cross-check against flag matches, discipline with temptations resisted looks different from discipline never tested. Good: a quarter's report pairing gate compliance with three caught regressions. Bad: reporting coverage growth as TDD health. Borderline: counting valve invocations as failures, they are the system working, count outcomes instead.

**Verification, evidence, and uncertainty.** Confirm workstreams log the five measures at each cycle review. The skill's promise structure and gate requirements read this session. Baseline regression rates without the practice are unmeasured locally.

**Second-order consequence.** First-run-pass incidents are the practice's most diagnostic number because both explanations are actionable, either the test measures existing behavior, a test bug the gate caught, or the feature already existed, a knowledge gap worth closing, the metric cannot be high in a healthy practice and every elevation has a name.

**Revision decision.** Adopt the five measures with regression catches as the law's visible long-horizon dividend.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: every shipped claim has a requirement ID and fresh verification evidence.
Failure signal: the reference describes TDD or specs without showing failing and passing evidence.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this synthesis is declared faithful. The seed shape items never audit this theme's distinctive claims, the archive-live twin equality and its stability inference, the three-ring structural reading, the eleven-row and thirteen-item registry counts, the dot diagram's two back-edges, the outbound anti-patterns link, and the authority structure of law plus human valve.

**Recommended default and causal basis.** Run the equality test first each cycle and branch the audit on its result. The synthesis rests on a live-tree relationship and precise structural counts, each verifiable cheaply, and the live half can change independently of any sweep, making this checklist's freshness clock faster than archive-only themes.

**Operating conditions and assumptions.** Both paths remain in the repository, live-tree restructures would relocate the moving part. Auditing this reference against its two mapped paths with the live copy as the moving part.

**Failure boundary and alternatives.** A shape-only audit passes while the live copy diverges from the sweep, silently converting every operative-doctrine claim here into a claim about a superseded version. Bounded alternatives and recoveries: sampled auditing, workable for the structural counts, wrong for the equality and existence checks which need exact results.

**Counterexample, gotchas, and operational comparison.** Equality checking is cheap but insufficient alone, if the live copy moved rather than changed, the checksum comparison silently tests a stale path, existence precedes equality in the audit order. Good: an audit logging existence, equality, both checksums, and spot-checked registry counts. Bad: declaring fidelity from seed-shape checks alone. Borderline: auditing only the sweep copy in a cycle, half the claims covered, the operative half is the one skipped.

**Verification, evidence, and uncertainty.** Execute the distinctive items in existence-equality-content order and date each result. The claim structure of every distinctive statement in this document. The live tree's change frequency is unknown, it has not changed since 202603.

**Second-order consequence.** This checklist's critical item is the equality test itself, uniquely, divergence would not invalidate the synthesis but re-scope it, the sweep copy claims would stand as history while the live copy would need re-reading, so the audit's first question is not is this right but which document is this still about.

**Revision decision.** Add the distinctive items, both checksums with equality status, the registry counts, the diagram edges, the link target's existence, and the valve language.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Tdd Cycle Skill Patterns.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a question arriving at the cycle's law layer. The seed token-split rows point nowhere while this theme's real neighbors are the agent TDD stack and the testing depth files, session memory and journaling go to the retainer themes, checkpoint timing to the cadence theme, mock pitfalls to the testing-anti-patterns file the skill itself links, debugging method to the systematic debugging theme, and cycle law, gates, and rationalization defense stay here.

**Recommended default and causal basis.** Answer law, gate, and rationalization questions here and forward memory, timing, and mock questions to their owners explicitly. The skill governs the cycle's law layer while its neighbors govern memory, timing, and specialized testing judgment, a separation the skill itself encodes by linking out for mock guidance rather than absorbing it.

**Operating conditions and assumptions.** The sibling themes remain complete, the retainer and cadence themes were finished as assignments 64 and 65. Sending away what this theme holds only incidentally, destination doctrine is deliberately not restated.

**Failure boundary and alternatives.** Answering mock-design questions here from the skill's two-line summary shortchanges a topic it explicitly delegates, while routing away Iron Law questions abandons the theme built to hold them. Bounded alternatives and recoveries: for TDD tooling beyond the archive, nothing local, say so rather than stretching.

**Counterexample, gotchas, and operational comparison.** Debugging questions split by layer, never fix bugs without a test is this theme's law, how to find the cause is the debugging theme's method, the same bug crosses both in one session. Good: answering must I watch it fail here and forwarding how do I mock this API to the anti-patterns theme. Bad: adjudicating mock design from this skill's summary lines. Borderline: a question about journaling gate evidence, this theme's artifact meets the retainer's journal, cite both.

**Verification, evidence, and uncertainty.** Sample recent answers for layer-consistent routing. The skill's own outbound link and the sibling theme map read across assignments. Whether the anti-patterns file has its own queue entry is determined by the queue, not this analysis.

**Second-order consequence.** The corpus's TDD-adjacent themes now form a documented stack with this skill as its law layer, and the routing map doubles as the stack's wiring diagram, an agent assembling a full TDD practice can read this section's destinations as its load list, the routing table graduating from navigation aid to composition recipe.

**Revision decision.** Route by stack layer, law and gates here, memory to the retainer, timing to the cadence, mock depth to the linked file, diagnosis method to debugging.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use TDD, traceability, or completion verification references when the work is already scoped.
Adjacent reference 1: consider the tdd adjacent reference when the current task pivots away from tdd cycle skill patterns.
Adjacent reference 2: consider the cycle adjacent reference when the current task pivots away from tdd cycle skill patterns.
Adjacent reference 3: consider the skill adjacent reference when the current task pivots away from tdd cycle skill patterns.

## Workload Model

**Decision supported.** This section helps decide how to budget a practice whose enforcement cost is designed to be avoidable. The seed symbol-count boundary misprices this node's work, whose units are the sunk 371-line read in under an hour, per-loop cycle overhead, a witnessed red, a minimal green, both gates, minutes each at the skill's intended grain, per-violation deletion cost, hours occasionally, the enforcement's real price, and per-audit checks led by the cheap equality test.

**Recommended default and causal basis.** Budget loops at minutes, keep them small enough that deletion never exceeds a loop's cost, and let violations price themselves. The skill's economics concentrate cost at violations rather than at compliance, clean loops are cheap and deletions are expensive, an incentive design where the costly path is the one the law forbids.

**Operating conditions and assumptions.** Loop grain stays fine, coarse loops inflate both compliance and violation costs together. Budgeting this node's upkeep and the cycle's operation, corpus assignment budgets stay in the seed rows.

**Failure boundary and alternatives.** Pricing TDD by its violation cost, the feared deletion, rather than its compliance cost, minutes per loop, is exactly the sunk-cost framing the skill's arguments dismantle, the deletion is the price of having violated, not of practicing. Bounded alternatives and recoveries: the seed's crate-slice and symbol boundaries, retained as outer limits on any single consultation's scope.

**Counterexample, gotchas, and operational comparison.** The first loops on an untested legacy surface cost more, harness setup and seam finding, the skill's existing-code row acknowledges the case, budget the surcharge once rather than reading it as the practice's steady state. Good: a team tracking minutes per loop and finding deletions rare and small. Bad: citing one large deletion as proof TDD is expensive. Borderline: batching trivial loops for related one-line behaviors, cheaper and coarser, watch the deletion exposure grow with batch size.

**Verification, evidence, and uncertainty.** Check workstream records for loop-grain budgeting at review. The minimal-code doctrine and enforcement rules read this session. Real loop-cost distributions across stacks are unmeasured locally.

**Second-order consequence.** The skill's minimal-code doctrine is itself a workload rule, the green phase's just-enough discipline caps each loop's implementation cost by construction, YAGNI enforced at the loop level keeps loops small, keeps failures local, and makes the deletion penalty survivable when it fires, the law's harshness is funded by the cycle's smallness.

**Revision decision.** Restate the model in its four units, sunk read, per-loop minutes, violation-priced deletions, and equality-led audits.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Tdd Cycle Skill Patterns as a agent workflow operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | agent planning, tool use, context loading, delegation, or skill/plugin execution where bad guidance compounds across long-running sessions | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one primary task, up to 10 source files, up to 3 delegated subtasks, and a written completion audit for every run | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Test-Driven Development (TDD); Overview; When to Use; The Iron Law; Red-Green-Refactor; RED - Write Failing Test; Test-Driven Development (TDD); Overv | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is RED/GREEN transcript with refactor rule and resume checkpoint | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which invariants must demonstrably hold for this node's claims to stay citable. The seed document-property thresholds stand where this node's invariants should, archive-live equality status currency, quotation fidelity for the law, the gates, and the registries, scope fidelity keeping the exception list attached to every severity claim, authority fidelity keeping the human valve visible wherever the law is cited, and dialect labeling keeping typescript examples marked as illustration rather than requirement.

**Recommended default and causal basis.** Hold all five invariants at audit cadence with scope and authority checked together as the calibration pair. Each invariant guards a distinct corruption, a stale equality status misdates the doctrine, misquotation softens or sharpens the law, scope loss turns calibrated severity into indiscriminate dogma, authority loss turns the law into agent self-governance, and dialect confusion walls the doctrine into one stack.

**Operating conditions and assumptions.** The live copy remains the operative reference, equality currency depends on it. Invariants for this node's claims, cycle reliability in practice belongs to the metrics loop.

**Failure boundary and alternatives.** The costliest breach is scope loss because it weaponizes the skill against itself, the Iron Law quoted without the exception list and valve reads as the dogmatism the skill's own arguments explicitly deny being. Bounded alternatives and recoveries: fact-only invariants, blind to the calibration this document's value rides on.

**Counterexample, gotchas, and operational comparison.** Quotation fidelity includes the law's typography, the skill renders it as a fenced block in capitals, reproductions that paraphrase it into a sentence demote a designed artifact into ordinary advice. Good: an audit confirming the law quoted verbatim with valve and exceptions in reach. Bad: a derived style guide commanding TDD always with no exception list. Borderline: quoting the law alone in a discussion of enforcement styles, acceptable meta-use if the calibration is noted.

**Verification, evidence, and uncertainty.** Review the five invariant series at each corpus checkpoint with dated evidence. The claim structure established across this document. None about current truth, all five invariants were verified during this evolution.

**Second-order consequence.** This theme's invariants defend calibration more than facts, the skill is engineered tension between absolutism and its valve, and nearly every corruption path runs through losing one side of that tension, reproductions that keep the law and drop the valve, or keep the valve and soften the law, both betray a document whose design is the pairing.

**Revision decision.** Publish the five invariants with evidence methods, equality runs, quote audits, scope spot-checks, valve-presence checks, and dialect-label reviews.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which decay modes need standing probes for a live behavioral law. The seed generic rows miss how this node specifically dies, silent live-copy divergence stranding the synthesis on a superseded version, calibration stripping, the law circulating without its valve and exceptions, gate theater, commands run without reason checks until witnessing hollows, and registry staleness, new rationalizations evolving past the eleven rows the fossil record captured.

**Recommended default and causal basis.** Run the four probes at audit cadence with new-excuse harvesting feeding the registry review. The first attacks evidence currency, the second attacks the document's engineered balance, the third attacks practice from inside compliant-looking behavior, and the fourth reflects that excuse evolution continues while the registry is frozen at its authorship date.

**Operating conditions and assumptions.** Gate logs exist for the theater probe, the compliance-log artifact is its substrate. Decay of this node's claims and the skill's deployed fidelity, practice-level metrics belong to their loop.

**Failure boundary and alternatives.** They compound in agent deployments, an agent on a diverged copy, holding a stripped law, running theatrical gates, and rationalizing in phrasings the flag list never anticipated, four failures each invisible to the check that catches the others. Bounded alternatives and recoveries: quotation-only reuse with calibration intact, resistant to stripping and helpless against theater and staleness, which live in behavior and time.

**Counterexample, gotchas, and operational comparison.** The meta-flag's breadth is also its weakness, everything matches this is different because at sufficient abstraction, the probe should watch for the valve being bypassed, not for the phrase alone. Good: a probe catching a team wiki quoting the law valve-free and restoring the pairing. Bad: pristine checksums cited as proof the practice is healthy. Borderline: an excuse that maps to an existing row in new words, registry coverage working as designed, log the phrasing anyway.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe and trigger point. The decay structure consolidated from this document's preceding sections. The rate at which novel rationalizations emerge is unknowable in advance.

**Second-order consequence.** Registry staleness is the mode the skill cannot self-detect, its defenses pattern-match known phrasings, so novel rationalizations pass the flag list precisely because they are novel, and the meta-flag, this is different because, is the authors' own hedge against this, a catch-all built for the excuses they knew they had not met yet.

**Revision decision.** Add the four rows with probes, equality runs for divergence, calibration checks on derived materials, gate-log reason sampling for theater, and periodic registry review against newly observed excuses.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| context window loses plan | long-running session forgets early constraints or overwrites user intent | write checkpoint summaries and re-read plan before each destructive step |
| tool fanout outruns budget | parallel actions create conflicts, duplicate work, or unbounded external calls | cap fanout, assign ownership, and require merge/audit checkpoints |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide what each cycle failure is allowed to change. The seed corpus-process bullets stand beside what the skill rules for cycle failures, a wrong-failure red loops back to red until the test fails for the right reason, a failing green fixes code, never the test, other tests breaking at green are fixed now, not deferred, a violation discovered mid-stream triggers deletion and restart rather than salvage, and repeated stuck states route through the when-stuck table toward design simplification, not toward gate relaxation.

**Recommended default and causal basis.** Under pressure, adjust the design or the test's grain, never the gates, and treat persistent stuckness as architectural information. Every response channels pressure away from the law and into the work, the gates absorb retries, the design absorbs complexity, and the law itself is never the adjustable part, backpressure by architecture.

**Operating conditions and assumptions.** Design change is within the practitioner's authority, coupled legacy surfaces may need staged seams first. Failure-response doctrine for the cycle, journal-repair doctrine belongs to the retainer themes.

**Failure boundary and alternatives.** The erosion path is retrying against the gate instead of the work, loosening what counts as a proper failure or a pristine pass after repeated frustration, the gate quietly recalibrated by fatigue. Bounded alternatives and recoveries: the retainer's journaling for capturing stuck-state context across the pause a redesign may need.

**Counterexample, gotchas, and operational comparison.** Fix code not test has one honest exception the skill implies at verify-red, a test that passed immediately was itself wrong and gets fixed, the rule governs green-phase failures, not red-phase test bugs. Good: a hard-to-test module getting dependency injection instead of a mock forest. Bad: widening the pristine-output tolerance after three noisy greens. Borderline: simplifying a test's scope after repeated wrong failures, legitimate grain adjustment if the behavior stays covered.

**Verification, evidence, and uncertainty.** Audit stuck-state resolutions for design-side changes at review. The gate loops and when-stuck table read verbatim this session. How often stuckness resolves through design versus persistence is unrecorded.

**Second-order consequence.** The when-stuck table's diagnosis is the skill's most constructive move, every stuck state is read as a design signal, hard to test means hard to use, must mock everything means too coupled, huge setup means simplify, converting the practitioner's frustration into architecture feedback, the cycle as a design linter that happens to produce tests.

**Revision decision.** State the response rules, red loops to red, green fixes code, breakage fixes now, violations delete, stuckness simplifies design.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide which properties of the system and process the cycle makes visible. The seed generic records stand beside what the cycle makes observable, every behavior change leaves a paired trace, a test that names the behavior and an implementation that satisfies it, the gates leave witnessable events, failure messages and passing runs, the checklist leaves a completion attestation, and the flag list makes even the practitioner's reasoning partially observable through its phrase-shaped detectors.

**Recommended default and causal basis.** Read the test suite as the system's intention record and keep gate evidence in the compliance log. TDD's ordering forces intentions into artifacts before code exists, the test is the recorded intention, so a TDD codebase carries its own requirements trace as executable documentation, observability as a side effect of the law.

**Operating conditions and assumptions.** Tests keep behavioral names, the clear-name requirement is what makes the suite readable as specification. Observability through the cycle's artifacts, session-state observability belongs to the retainer themes.

**Failure boundary and alternatives.** Tests-after codebases record what the code does, not what it was meant to do, the intention layer is unrecoverable after the fact, which is the observability restatement of the skill's ordering thesis. Bounded alternatives and recoveries: prose requirement documents, richer in rationale and unenforced in accuracy.

**Counterexample, gotchas, and operational comparison.** Mock-heavy tests break the specification reading, they record interactions with test doubles rather than system behavior, the skill's real-code requirement protects the observability property, not just test validity. Good: onboarding by reading the test suite as the behavior catalog. Bad: a suite of test1 through test40 opaque to any reader. Borderline: generated test names from a property-based framework, observable differently, document the property instead.

**Verification, evidence, and uncertainty.** Sample test suites for behavioral readability at audit. The good-tests table and documentation argument read this session. How often suites get read as specifications in practice is unmeasured.

**Second-order consequence.** The skill's tests-document-behavior argument makes the test suite a living specification whose accuracy is machine-enforced, unlike prose specs it cannot drift silently because drift fails the build, the only documentation genre with an automated fidelity gate, and the cycle is the only process that guarantees the spec preceded the system.

**Revision decision.** State the paired-trace framing, tests as recorded intentions, gates as witnessed events, checklist as attestation.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture tool-call count, context files loaded, delegated tasks, retry count, and completion-audit outcome.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide where performance verification lives relative to the cycle. The seed latency packet stands beside the skill's performance posture, which is deliberate silence, no benchmark gates, no performance tests in the cycle, and one adjacent clause, pristine output at green, the skill's only quality bar beyond passage, performance belongs to other layers and the cycle's gates measure correctness and cleanliness only.

**Recommended default and causal basis.** Keep cycle gates performance-free, record benchmarks in the journal layer, and treat regressions as checkpoint and investigation triggers. The cycle's tests assert behavior, and performance assertions in unit cycles produce flaky gates, timing variance breaking red-green determinism, so the skill's silence is a scoping decision consistent with its one-behavior test doctrine.

**Operating conditions and assumptions.** Performance-critical behaviors get their own measurement harness outside the unit cycle. The skill's performance posture, benchmark methodology belongs to the retainer's metrics section and the system design themes.

**Failure boundary and alternatives.** Stretching the cycle to carry performance gates imports nondeterminism into a machine built on deterministic color changes, a flaky red is indistinguishable from a wrong failure and corrodes gate trust. Bounded alternatives and recoveries: benchmark-driven development for hot paths, a separate discipline with its own harness, compatible beside the cycle rather than inside it.

**Counterexample, gotchas, and operational comparison.** Behavioral tests with implicit timeouts smuggle performance assertions into the cycle accidentally, a slow-but-correct implementation failing on runner timeout is a flaky gate in disguise, keep timeouts generous in cycle tests. Good: a retry loop tested for behavior in the cycle and profiled in a separate benchmark suite. Bad: a unit test asserting the operation completes in fifty milliseconds. Borderline: asserting an algorithmic property like call count, deterministic and performance-adjacent, legitimate cycle content.

**Verification, evidence, and uncertainty.** Sample cycle tests for smuggled timing assertions at audit. The skill's gate definitions and its silence on benchmarks, verified by full read. The silence is interpreted as scoping, the authors' intent is inferred from the document's consistency.

**Second-order consequence.** The corpus's TDD stack divides performance labor cleanly without any file coordinating it, this skill keeps performance out of the cycle's gates, the retainer gives performance numbers a standing journal field, and the cadence playbook makes regressions a checkpoint trigger, three non-overlapping assignments that compose into a complete posture, measure elsewhere, record always, react on regression.

**Revision decision.** State the silence as scoping, correctness cycles here, performance measurement in adjacent layers, with pristine output as the boundary clause.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require tool-call budgets, timeout budgets, and a resumable journal when the workflow exceeds one focused session.
Leading indicator to measure: every shipped claim has a requirement ID and fresh verification evidence.
Failure signal to watch: the reference describes TDD or specs without showing failing and passing evidence.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what grain the cycle operates and where its authority stops. The seed task-splitting bounds stand beside the cycle's own scale limits, one behavior per test and one test per loop by the requirements and the diagram, features built as chains of loops, the skill scaling by repetition rather than by loop growth, and its authority ending at implementation ordering, it governs how code comes into being, not what architecture it forms or which features are worth building.

**Recommended default and causal basis.** Hold the behavior grain, scale by more loops rather than bigger ones, and keep the cycle out of architecture and prioritization decisions. The one-behavior grain keeps failures diagnostic, a multi-behavior test that reds gives no single cause, so the grain limit is what keeps the gate's evidence readable, scale and observability sharing one constraint.

**Operating conditions and assumptions.** Behaviors decompose cleanly, some integration behaviors resist unit grain and need their own harness layer. Scale limits of the cycle, this evolution's own task splitting keeps the seed paragraphs.

**Failure boundary and alternatives.** The characteristic scale failure is the growing loop, tests accreting assertions and implementations accreting features per pass until red tells nothing and green means everything, the cycle's evidence property dissolving with size. Bounded alternatives and recoveries: coarser acceptance-test-driven loops for feature-level behavior, a complementary outer loop, not a replacement grain.

**Counterexample, gotchas, and operational comparison.** The ordering authority still shapes architecture indirectly through the when-stuck table, testability pressure pushes toward injection and decoupling, the skill disclaims architectural authority while its constraints exert architectural force, an influence worth naming when tracing design decisions. Good: a feature landing as nine small loops with nine diagnostic tests. Bad: one loop with a twelve-assertion test and a hundred-line green. Borderline: a parameterized test covering ten input cases of one behavior, one behavior at wider input grain, legitimate.

**Verification, evidence, and uncertainty.** At each audit, sample test names for the and-rule and loops for grain. The requirements list, diagram, and good-tests table read this session. The practical ceiling of loop chains on large features is asserted by design, not measured here.

**Second-order consequence.** The and-in-name rule is the grain limit made lexically enforceable, a test name needing and names two behaviors and splits, giving the skill a scale detector that operates on language rather than on code, the cheapest boundary check in the corpus, readable at review speed without running anything.

**Revision decision.** State the three boundaries, behavior-grain tests, repetition-based scaling, and ordering-only authority.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Tdd Cycle Skill Patterns stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal decay in this node's claims and on what clocks. The seed theme-name queries would surface generic TDD chatter while this node's staleness has exact homes, the archive-live equality status, the live copy's continued existence at its claude-skills path, the linked anti-patterns file's resolution, the sweep inventory that could add later snapshots of the superpowers collection, and the example dialect's continued readability as typescript idioms age.

**Recommended default and causal basis.** Run existence and equality first each cycle and let a flip escalate to full re-synthesis. A live-tree source decays by deployment change rather than by external drift, so the probes watch the repository's own moving parts, the live path, the equality, the link, rather than any outside subject.

**Operating conditions and assumptions.** The repository remains the deployment surface for the live skill. Refreshing this node's evidence and labels, cycle doctrine itself needs no external refresh.

**Failure boundary and alternatives.** Undirected refreshing would miss the one event that matters most, a live-copy edit, which changes operative doctrine while leaving every archive fact intact and every name query unenlightening. Bounded alternatives and recoveries: declaring the theme stable and unwatched, wrong for the corpus's first live-tree source, the moving part is the point.

**Counterexample, gotchas, and operational comparison.** Sweep scans should also watch for the skill appearing in later monthly archives, a 202605 or later snapshot would add a dating point and could reveal an intermediate edit history invisible to the two-point comparison. Good: a refresh logging live-path present, equality holds, link resolves, no newer snapshots. Bad: logging that TDD cycle searches returned nothing new. Borderline: skipping the dialect review most cycles, acceptable on a slow clock, the doctrine outlives the idioms.

**Verification, evidence, and uncertainty.** Record each probe with its date and the claim it confirmed or updated. The archive-live topology established this session. Future live-tree governance, who edits it and when, is unrecorded.

**Second-order consequence.** The equality probe doubles as a change-detection service for the whole theme, any live edit flips it and triggers a full re-read, so this theme gets an automatic freshness alarm no archive-only theme can have, the live tree's volatility converted from risk into instrumentation.

**Revision decision.** Replace name queries with targeted probes, live-path existence, archive-live equality, link resolution, sweep scans for newer superpowers snapshots, and example-dialect review on a slow clock.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | tdd cycle skill patterns official documentation best practices |
| `github_repository_query_phrase` | tdd cycle skill patterns GitHub repository examples |
| `release_notes_query_phrase` | tdd cycle skill patterns changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label, chain, and state each claim class here may be reused. The seed label definitions stand without this assignment's distinctive ledger, two mapped paths read in full and resolving to one byte-identical document spanning archive and live tree, both checksums recorded, the linked anti-patterns file noted but not read, zero external URLs fetched, the skill's empirical claims, TDD faster than debugging, quoted as doctrine rather than verified, and the corpus's own non-typescript process observed as a partial analogue, not a compliance instance.

**Recommended default and causal basis.** Before reusing a claim, declare it structural fact, anchored doctrine, authorial assertion, or synthesis, and treat unlabeled claims as inference. An enforcement document's evidence splits three ways, what it says, verifiable locally and fully done, whether its claims hold, asserted by the authors without citation, and whether it is followed, observable only in deployments this session did not inspect.

**Operating conditions and assumptions.** Both copies stay unedited, the equality finding dates the ledger. Reuse rules for this document's claims, each claim travels with class and verification state.

**Failure boundary and alternatives.** The costliest mislabel would present the skill's performance claims as established findings, the document argues them forcefully and cites nothing, rhetorical strength and evidential strength travel separately. Bounded alternatives and recoveries: instrumenting a real deployment against the skill's claims, the natural validation upgrade, outside this corpus's scope.

**Counterexample, gotchas, and operational comparison.** Quoted doctrine includes the skill's empirical-sounding lines, tests catch breaks immediately reads factual and is doctrine, the anchor label must not upgrade rhetoric into measurement. Good: reusing the Iron Law as anchored doctrine with its valve and the speed claim as authorial assertion. Bad: citing TDD faster than debugging as a research finding. Borderline: recommending the skill for agent deployments on this evidence, reasonable guidance if the assertion labels travel with it.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares its class cleanly. This assignment's ledger, full reads, dual checksums, unread link noted, zero fetches. The skill's empirical claims remain unvalidated anywhere in the local evidence.

**Second-order consequence.** This theme closes the corpus's TDD triptych with its epistemically loudest and least externally evidenced document, the retainer family carried a live usage record and the cadence playbook carried structural modesty, while this skill carries maximal confidence on authorial authority alone, the three sources spanning the full range of claim-to-evidence ratios the archive contains.

**Revision decision.** Publish the strata explicitly, texts and checksums as same-session local facts, the law, gates, and registries as quoted doctrine with section anchors, the speed and debt claims as authorial assertions, and the stack-composition readings as this evolution's synthesis.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
