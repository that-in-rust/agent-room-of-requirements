# Tauri Executable Skill Patterns

**Decision supported.** This section helps decide how to frame one skill shipped in two platform-shaped forms. The seed no seed line conveys that this theme holds one skill in two platform dialects, a 126-line Claude agent with persona voice, sonnet model, and teal color frontmatter, twin-copied byte-identically into the unclassified directory, and an 84-line Codex skill that compresses the same five modes, seven-step workflow, and seven-part output contract into imperative one-liners with a progressive-loading context strategy.

**Recommended default and causal basis.** Treat the shared core as the skill's identity and choose the dialect by the consuming platform. The two dialects exist because the platforms consume skills differently, the Claude form embeds everything an agent persona needs inline while the Codex form externalizes depth into four referenced files it loads on demand.

**Operating conditions and assumptions.** All three copies remain at their archive paths, checksums verified this session. This reference covers the entrypoint variants, the four referenced depth files belong to their own completed themes.

**Failure boundary and alternatives.** Reading either variant as the canonical skill misses the design, the invariant core is what both share, modes, workflow order, output contract, guardrail substance, and the differences are packaging. Bounded alternatives and recoveries: the doctrine and gates themes for the depth content this entrypoint routes toward.

**Counterexample, gotchas, and operational comparison.** The Claude variant's Reference Use section points at a personal live-install path on the author's machine, dead by construction here, while its repo-mirror path resolves locally under the idiomatic-references-202604 directory. Good: citing mode definitions as the shared core with either variant as witness. Bad: reporting the persona framing as part of the Codex skill. Borderline: quoting the sonnet model pin as skill doctrine, it is deployment configuration, platform-specific by nature.

**Verification, evidence, and uncertainty.** Confirm the shared-core inventory against both variants before structural claims. All three files read in full with checksums this session. Which variant the author authored first is unrecorded.

**Second-order consequence.** The pair is a controlled experiment in skill portability, same doctrine, two runtimes, and the diff shows exactly what porting costs, the Claude form spends 42 extra lines re-inlining what Codex delegates to references, so the marginal price of a self-contained skill is measurable here line by line.

**Revision decision.** Open with the two-dialect structure, the shared invariant core, and the twin-copy status of the Claude form.

**Retained seed evidence.** The exact theme title and framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide what the effective evidence set is once the twin collapses. The seed three local rows list paths without recording the copy topology, the Claude variant and the unclassified single-MD file are byte-identical twins sharing one checksum, so the three rows hold two distinct documents, and the Codex row's SKILL.md anchors the directory whose four references three prior assignments already evolved.

**Recommended default and causal basis.** Cite the Claude variant at its classified path and mention the twin only for copy-integrity checks. The twin copy exists because the unclassified directory holds pre-classification drops, the same file awaiting curation, which doubles apparent evidence without adding any.

**Operating conditions and assumptions.** No future sweep edits one twin without the other, which would fork the document. Provenance for this theme's claims, the referenced depth files carry their own provenance in their themes.

**Failure boundary and alternatives.** Counting three independent sources would inflate corroboration, the effective evidence is one Claude-dialect text, one Codex-dialect text, and their diff. Bounded alternatives and recoveries: treating the unclassified copy as canonical, backwards given the classified copy's curated location.

**Counterexample, gotchas, and operational comparison.** The Claude variant's two internal reference paths are author-machine absolute paths, one dead and one resolvable only by translating to the repo-relative mirror location. Good: two-document synthesis with the twin noted as a copy check. Bad: three-source corroboration claims. Borderline: citing the twin's directory as evidence of pending re-classification work, plausible inference, label it.

**Verification, evidence, and uncertainty.** Re-run both checksums and the twin comparison at each audit. Checksum runs and full reads of all three paths this session. Whether the unclassified twin will be pruned or reclassified in future sweeps.

**Second-order consequence.** The twin's location is itself evidence about the archive's lifecycle, files land in unclassified-yet, then get classified into the skills directories, so the identical pair captures one document at two pipeline stages and dates the classification process the corpus otherwise never sees.

**Revision decision.** Annotate the rows with both checksums, the twin relationship, and the two-document effective count.

**Retained seed evidence.** The three local rows and three external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202604/tauri-executable-specs-01.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| agents-used-monthly-archive/codex-skills-202604/tauri-executable-specs-01/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| unclassified-yet/Tauri Executable Specifications - single MD file.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| https://v2.tauri.app/ | external_research_source_material | external_research_sourced_fact | primary Tauri 2 application and security documentation |
| https://github.com/tauri-apps/tauri | external_research_source_material | external_research_sourced_fact | desktop runtime implementation and release surface |
| https://github.com/rust-lang/api-guidelines | external_research_source_material | external_research_sourced_fact | Rust-facing API and error design reference |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which entrypoint patterns deserve default-tier ranking on this evidence. The seed three corpus-process rows stand where the skill's own priorities rank, the mode-selection discipline leads, five named modes with five default combinations chosen before any planning or coding, then the executable-requirement rule, REQ-TAURI contracts in WHEN-THEN-SHALL form that reject vague claims like safer without thresholds, then the Tauri-over-generic precedence guardrail both variants state first among their guardrails.

**Recommended default and causal basis.** Gate any use of this skill on explicit mode choice and threshold-bearing requirements before design talk. Mode selection ranks first because everything downstream keys on it, the workflow's first step is classification, the context strategy loads references by mode, and the output contract opens by naming the mode chosen.

**Operating conditions and assumptions.** The ranking reflects both variants' shared structure, not usage frequency data. Ranking the entrypoint's own patterns, the depth files' pattern boards live in their themes.

**Failure boundary and alternatives.** Ranking by line count would promote the workflow's middle steps over the mode gate that determines which steps matter for a given task. Bounded alternatives and recoveries: the seed's process-tier scoreboard for corpus workflow ranking.

**Counterexample, gotchas, and operational comparison.** The default combinations are two-mode pairs, single-mode use is allowed but never listed as a default, a subtle push toward combining perspectives. Good: opening a Tauri request by naming modes and writing thresholded REQ rows. Bad: starting design from generic Rust taste with modes unnamed. Borderline: skipping Spec Mode for a task with crisp existing contracts, the mode's own use-when clause licenses it.

**Verification, evidence, and uncertainty.** Spot-check first-position claims against both variants at audit. Both variants' section orders read this session. Real usage frequency of each mode is unrecorded.

**Second-order consequence.** The vague-claim rejection rule is the skill's sharpest teeth, it converts adjectives into obligations, safer must become a threshold or a named boundary effect, which is the same move this corpus's own spec makes with its measurable-field contract, spec-first cultures converge on banning unmeasurable praise.

**Revision decision.** Present the three-pattern core, mode gate, executable requirements, precedence guardrail, with their structural first-position evidence.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `tauri_executable_skill_patterns` | 95 | default_adoption_pattern_tier | Source Map First: Load local tauri executable material before synthesizing skill patterns recommendations. |
| `tauri_executable_skill_patterns` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `tauri_executable_skill_patterns` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what mental model turns a Tauri request into trustworthy work. The seed generic corpus formulas stand where the skill states its thesis in its core principle, do not treat Tauri work as only frontend work or only Rust work, always decide what must be specified, what must be designed across frontend, command, Rust core, and desktop boundaries, and what must be verified to trust the result.

**Recommended default and causal basis.** Run every Tauri request through the three questions and let the answers populate the output contract. Tauri's architecture spans a web runtime and a Rust process joined by IPC, so single-discipline habits, frontend-only or Rust-only, each miss half the failure surface, and the skill's whole apparatus exists to force the cross-boundary view.

**Operating conditions and assumptions.** The work is Tauri-shaped, a Rust backend and web frontend joined by IPC, per both descriptions. The thesis governs how Tauri requests become work packets, the depth doctrine behind each decision belongs to its themes.

**Failure boundary and alternatives.** The single-lens failure is concrete, frontend thinking ships permission creep it never sees, Rust thinking ships UI stalls it never feels, each locally competent. Bounded alternatives and recoveries: single-discipline workflows, the documented failure mode the core principle names.

**Counterexample, gotchas, and operational comparison.** Decision-complete is the quiet standard both variants set, a packet that defers key choices to the implementer fails the thesis even with all sections present. Good: a feature packet with thresholded requirements, a four-layer design split, and a matched verification mix. Bad: a plan that is a Rust module sketch with UI and permissions to be decided later. Borderline: a tiny fix skipping the full packet, the thesis's questions still apply at proportional depth.

**Verification, evidence, and uncertainty.** Audit packets for all three artifact families before handoff. The core principle and output contract read in both variants this session. How the thesis performs on non-Tauri desktop stacks is beyond both variants' claims.

**Second-order consequence.** The thesis is a triage loop rather than a doctrine, its three questions produce the skill's three artifact families, requirements, design splits, and verification matrices, so the output contract is the thesis made inspectable, any packet missing one family skipped one question.

**Revision decision.** State the three-question thesis, specify, design across boundaries, verify, as the skill's irreducible core.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `tauri_executable_skill_patterns` contains 3 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Tauri Executable Skill Patterns comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide which variant to open for which question about this skill. The seed heading signals name sections while the working map is the variants' divergence, both share modes, workflow, and output contract, but only the Claude form carries Core Principle and Reference Use, only the Codex form carries Context Strategy and a References list, and the guardrails differ, six imperative merged rules in Codex against seven skeptical stance rules in Claude.

**Recommended default and causal basis.** Consult the shared spine for doctrine and the dialect-specific sections only for platform-fit questions. Each platform's loading model shaped its map, Claude gets one self-contained prompt so principles and reference pointers ride inline, Codex loads files on demand so its map externalizes depth and keeps the entrypoint lean.

**Operating conditions and assumptions.** Both variants remain at their archive paths unedited. Navigation across this theme's two documents, the four referenced files navigate through their own themes.

**Failure boundary and alternatives.** Navigating the Codex form for the core principle or the Claude form for the progressive-loading order finds nothing, section-level content is dialect-specific even where doctrine is shared. Bounded alternatives and recoveries: reading only the richer Claude form, losing the context strategy that makes the Codex directory navigable.

**Counterexample, gotchas, and operational comparison.** The Claude workflow carries sub-bullets the Codex workflow compresses away, the seven steps match but step-level detail lives only in the Claude text. Good: quoting workflow detail from the Claude sub-bullets and loading order from the Codex strategy. Bad: citing a References list from the Claude variant. Borderline: quoting guardrails without naming the dialect, the substance overlaps but the wording and count differ.

**Verification, evidence, and uncertainty.** Trace section claims to the correct variant at review. The section-by-section diff of both variants this session. None about structure, both documents were read wholesale.

**Second-order consequence.** The two maps answer different questions, the Claude form answers who am I and what do I believe, persona, principle, stance guardrails, while the Codex form answers what do I load next, strategy, references, so the dialect diff is really a prompt-versus-procedure split in how each platform constructs agent competence.

**Revision decision.** Annotate the map with the shared-section spine and the four dialect-only sections, two per variant.

**Retained seed evidence.** The three local source rows with titles, heading signals, and usage roles remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202604/tauri-executable-specs-01.md | tauri-executable-specs-01 | Tauri Executable Specs 01; Core Principle; Mode Selection; Workflow; Output Contract; Reference Use | local agent-usable source material |
| agents-used-monthly-archive/codex-skills-202604/tauri-executable-specs-01/SKILL.md | tauri-executable-specs-01 | Tauri Executable Specs 01; Mode Selection; Workflow; Output Contract; Guardrails; Context Strategy | skill entrypoint or reusable agent prompt |
| unclassified-yet/Tauri Executable Specifications - single MD file.md | tauri-executable-specs-01 | Tauri Executable Specs 01; Core Principle; Mode Selection; Workflow; Output Contract; Reference Use | local agent-usable source material |

## External Research Source Map

**Decision supported.** This section helps decide which external claims this theme owns and how each decays. The seed three inherited agent-format anchors stand while this theme's real external surface is small and specific, the Claude variant's dead live-install path on the author's machine, its repo-mirror path that translates to a resolvable local file, the sonnet model pin that dates the deployment era, and the Tauri 2 platform vocabulary both descriptions use, all archive-dated to 202604.

**Recommended default and causal basis.** Treat paths as archive artifacts to translate, never to dereference literally, and the model pin as dated configuration. Entrypoint files carry less external surface than doctrine files because they route rather than justify, their dependencies are paths and platforms, not research pedigrees.

**Operating conditions and assumptions.** The repo-mirror target file stays at its idiomatic-references-202604 location. External dependencies for freshness tracking, all URLs stayed unretrieved this evolution.

**Failure boundary and alternatives.** The volatile item is the model pin, sonnet as a deployment choice decays with model generations, quoting it as a current recommendation rather than an archived configuration would mislead. Bounded alternatives and recoveries: the seed's inherited AGENTS.md and Actions anchors, retained for ecosystem context.

**Counterexample, gotchas, and operational comparison.** The mirror path in the file names a Desktop checkout that differs from this Linux checkout root, resolution requires the repo-relative suffix, not the absolute prefix. Good: resolving the mirror reference by its repo-relative path with the translation noted. Bad: reporting the live-install path as an available resource. Borderline: citing sonnet as evidence of the skill's intended model class, reasonable historically, label the date.

**Verification, evidence, and uncertainty.** Re-check the mirror target's existence and the twin checksums at audit. Path resolution attempts and frontmatter reads this session. Current Tauri platform state and model-generation fit are unverified locally.

**Second-order consequence.** The dead live-install path is quietly informative, it reveals the author's deployment layout, skills installed under a personal agent-memory directory with the repo as mirror, documenting a two-tier skill lifecycle, live install plus versioned archive, that the corpus itself now continues.

**Revision decision.** Keep the inherited rows and add the theme's own inventory, two author-machine paths with their resolution status, the model pin, and the platform vocabulary, each labeled.

**Retained seed evidence.** All three external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://v2.tauri.app/ | Tauri documentation | primary Tauri 2 application and security documentation | external_research_sourced_fact |
| https://github.com/tauri-apps/tauri | Tauri repository | desktop runtime implementation and release surface | external_research_sourced_fact |
| https://github.com/rust-lang/api-guidelines | Rust API Guidelines | Rust-facing API and error design reference | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which process failures this skill rejects at packet-making time. The seed three corpus-process rows stand where the skill names its own rejected behaviors, vague claims such as safer or more reliable without thresholds, generic Rust rules overriding Tauri-specific security guidance, forcing every repo-local convention onto every Tauri task, treating tray, deep links, and startup behavior as polish rather than correctness, and the closing skeptic's list, malformed input, permission creep, event spam, detached sidecars, updater trust, lifecycle drift.

**Recommended default and causal basis.** Screen work packets against the entrypoint registry before any code exists to review. An entrypoint's anti-patterns guard the packet-making process itself, bad requirements, wrong precedence, convention overreach, misfiled correctness concerns, upstream of the code-level anti-patterns the depth files police.

**Operating conditions and assumptions.** Reviewers distinguish specification failures from implementation failures when filing findings. Process-level rejections from the entrypoint, code-level rejections belong to the doctrine and gates themes.

**Failure boundary and alternatives.** Merging this registry with the doctrine's seven code anti-patterns confuses altitudes, a vague requirement is a specification failure no code review can catch later. Bounded alternatives and recoveries: post-hoc code review alone, structurally blind to requirement vagueness.

**Counterexample, gotchas, and operational comparison.** The polish anti-pattern is easy to commit silently, window-state and deep-link behavior default to afterthought in web-trained habits, both variants explicitly reclassify them as correctness. Good: bouncing a requirement that says improves reliability back for a threshold. Bad: approving a packet whose design split omits permissions because the code looks clean. Borderline: applying four-word naming to a legacy surface, the registry's overreach entry and the compatibility guardrail both bear.

**Verification, evidence, and uncertainty.** Confirm registry rows cite their variant and altitude at audit. Guardrails and workflow rejection clauses read in both variants this session. Relative frequency of these process failures is not recorded.

**Second-order consequence.** The convention-overreach entry makes the skill self-limiting the same way the gates file's section 7 does, both variants ship rules and simultaneously forbid universalizing them, suggesting the author treats every convention set as a scoped tool, a meta-pattern now seen three times across this skill family.

**Revision decision.** Add the entrypoint registry with its altitude label and the six-hazard skeptic's list as standing suspicion targets.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide how verification is selected and evidenced before implementation. The seed document verifier text stands where the skill specifies verification as a chosen mix, Rust unit, Rust integration, frontend tests, typed invoke contract checks, mocked Tauri boundary tests, desktop boundary tests, and performance only when explicitly stated, matched to the real risk surface instead of defaulting to one layer, then recorded in a traceability table pairing req_id, test_id, test_type, assertion, and metric.

**Recommended default and causal basis.** Choose the mix per risk surface, record it in the traceability table, and admit performance tests only against stated thresholds. The entrypoint owns verification selection while the gates file owns verification commands, choosing what proves what is a design decision this skill makes before any command runs.

**Operating conditions and assumptions.** The risk surface is honestly assessed, the mix rule assumes someone names where the danger lives. The source's verification-selection doctrine, this document's own structural verification keeps the seed's retained block.

**Failure boundary and alternatives.** Defaulting to one test layer, unit tests for everything or end-to-end for everything, is the exact failure the deliberate-mix rule names, coverage theater at the wrong boundary. Bounded alternatives and recoveries: the corpus verifier command in the seed's retained block for this document's own checks.

**Counterexample, gotchas, and operational comparison.** Typed invoke contract checks are their own row in the mix, frontend tests passing does not certify the IPC contract, drift between the two is the desktop-specific gap the option exists to close. Good: a matrix pairing each REQ row with a test at its risk boundary. Bad: a packet whose matrix lists unit tests for a permission-scoping requirement. Borderline: adding a performance row without a stated threshold, the qualifier says wait for the requirement.

**Verification, evidence, and uncertainty.** Check packets for populated traceability tables at review. Workflow step four and the table shape read in both variants this session. How teams weight the seven options in practice is unrecorded.

**Second-order consequence.** The performance qualifier is a scope discipline in miniature, performance tests enter the mix only when a requirement states a threshold, which back-propagates pressure into the requirements, if you want it fast you must say how fast, closing the loop with the vague-claim rejection rule.

**Revision decision.** State the seven-option mix, the risk-surface matching rule, the performance-only-when-stated qualifier, and the traceability row shape.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide which variant an agent adopts and in what consumption mode. The seed four generic bullets stand where the two variants define exact consumption per platform, a Claude agent adopts the persona wholesale, principle, modes, guardrails as standing identity, while a Codex agent executes the workflow and loads references progressively, map first, playbook for vague requests, doctrine sections by relevance, gates before final verification.

**Recommended default and causal basis.** Match consumption mode to variant, wear the Claude form, execute the Codex form, and never mix a variant with the other's consumption style. The consumption split follows the packaging split, identity-shaped skills are worn continuously while procedure-shaped skills are stepped through, and each variant is built for its consumption style.

**Operating conditions and assumptions.** The consuming agent's platform matches a variant, novel platforms must choose the nearer dialect deliberately. How agents consume this theme's entrypoints, depth-file consumption belongs to their themes.

**Failure boundary and alternatives.** Cross-consuming fails quietly, an agent stepping through the Claude form misses the loading order it lacks, one wearing the Codex form as identity carries no principle or stance to reason from at edge cases. Bounded alternatives and recoveries: the doctrine theme's pattern-number consumption for agents already past the entrypoint.

**Counterexample, gotchas, and operational comparison.** The heading-search tips in the Codex context strategy assume execution from the skill root, an agent running them from the repo root gets silent empty results. Good: a Codex agent reading the map then only mode-relevant doctrine sections. Bad: an agent skipping the map and loading all four references wholesale for a tiny fix. Borderline: a third-platform agent adapting the Codex form with inline principle added, a labeled hybrid, reasonable if recorded.

**Verification, evidence, and uncertainty.** Sample agent sessions for consumption-mode fit to the variant used. The reference-use and context-strategy sections read this session. How gracefully each form degrades on platforms neither targets.

**Second-order consequence.** The guardrails converge on the same behavior from different directions, Claude's stay skeptical stance list and Codex's imperative rule list produce matching outputs, evidence that stance plus judgment and rules plus compliance are interchangeable implementations of one doctrine when the doctrine is well factored.

**Revision decision.** Recast the section around the two consumption modes, worn identity versus executed procedure, with the progressive-loading sequence as the Codex path's spine.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide what a complete packet-making journey looks like on a vague request. The seed one generic engineer stands where the natural journey is a vague request's transformation, a stakeholder asks for a safer file importer, the agent names Spec plus App Architecture modes per the default table, rejects safer pending thresholds, writes REQ-TAURI rows in WHEN-THEN-SHALL, splits design across frontend contract, command layer, Rust core, and permission boundaries, picks a verification mix, and emits the seven-part packet ending with open questions.

**Recommended default and causal basis.** Walk requests through the seven sections in order and let open questions carry everything genuinely undecidable. The journey follows the output contract's fixed order because the contract is the workflow made visible, each section the residue of one workflow step.

**Operating conditions and assumptions.** The requester is available for threshold negotiation, vague-claim rejection needs someone to answer the rewrite. Journeys through this theme's packet-making, implementation journeys belong to the depth themes.

**Failure boundary and alternatives.** The journey's failure branch is skipping the rewrite, accepting safer as a requirement and proceeding, which poisons every downstream artifact with unmeasurable success criteria. Bounded alternatives and recoveries: the gates theme's closing-review journey for work arriving finished.

**Counterexample, gotchas, and operational comparison.** Mode selection happens before requirement writing, a journey that writes requirements first then retrofits modes inverts the workflow's first two steps and usually mismatches the design split. Good: safer becoming three thresholded REQ rows with a permission-boundary design and matched tests. Bad: a packet whose requirements still contain reliability adjectives. Borderline: a journey ending with many open questions, honest if they are genuinely blocking, a smell if they were decidable.

**Verification, evidence, and uncertainty.** Audit packets for section order and empty-adjective absence at sampled requests. The workflow, output contract, and rejection rule read this session. Typical journey duration against request vagueness is unrecorded.

**Second-order consequence.** The open-questions section is the journey's honesty valve, the contract requires it last, so every packet ends by declaring what remains undecided, converting the decision-complete standard from a claim into a checkable boundary, complete except for exactly these listed items.

**Revision decision.** Recast the scenario as the vague-to-executable transformation with the packet's seven sections as journey milestones.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The Tauri desktop app team is starting from a frontend action that must cross into Rust commands and platform permissions and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `tauri_executable_skill_patterns` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing the safest command, permission, IPC, packaging, and verification boundary.
Reference opening trigger: Open this file when the task mentions tauri executable skill patterns, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide which side wins each recurring entrypoint trade-off and what condition flips it. The seed template rows skip the trade-offs the entrypoint adjudicates, self-containment versus progressive loading, resolved by platform loading model, single mode versus mode combination, resolved toward pairing by the default table, inline depth versus referenced depth, resolved oppositely by each variant, persona stance versus imperative rules, resolved by how each platform builds agent behavior, and thin command boundaries versus rich handlers, resolved for thinness when deeper Rust logic should stay testable.

**Recommended default and causal basis.** Combine modes by the default table, thin the command layer when core logic needs tests, and package depth per the consuming platform's loading model. Each ruling balances context cost against decision quality, modes combine because single lenses miss cross-boundary risk, references externalize because most tasks need a fraction of the depth, boundaries thin because testability outlives convenience.

**Operating conditions and assumptions.** Platform constraints are known before packaging choices, the dialect diff is the worked example. Trade-off structure the entrypoint teaches, code-level trade-offs belong to the depth themes.

**Failure boundary and alternatives.** Flattening the packaging trade-off into one right form misreads the evidence, both variants are the author's own answer for different constraints, the trade-off is real and platform-shaped. Bounded alternatives and recoveries: uniform maximal packets for every task, safe-looking and context-wasteful.

**Counterexample, gotchas, and operational comparison.** Thin boundaries are conditional, when deeper Rust logic should stay testable, a trivial command with no core logic gains nothing from ceremonial layering. Good: a heavy business-logic command built as a thin adapter over a tested core module. Bad: one skill form forced onto both platforms for consistency. Borderline: a medium command splitting logic mid-handler, the testability condition gives shape without a threshold, record the call.

**Verification, evidence, and uncertainty.** Review packaging and boundary decisions for named conditions. The dialect diff and workflow step three read this session. Quantitative thresholds for boundary thinness are unstated everywhere.

**Second-order consequence.** The thin-boundary ruling quietly imports hexagonal architecture into Tauri terms, keep the IPC layer a thin adapter so the core stays testable without a desktop runtime, connecting the entrypoint's one architecture opinion to the mocked-boundary testing preference the doctrine states independently.

**Revision decision.** Add the five trade-off rows each phrased as condition-then-choice with the platform or testability condition explicit.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the tauri executable skill patterns pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong tauri executable skill patterns guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which family file governs which question class now that all are evolved. The seed role labels rank three paths while the operative hierarchy crowns this entrypoint pair atop the skill family, the variants orchestrate, choosing modes and emitting packets, the map routes beneath them, the playbook, doctrine, and gates serve as the depth layer, and the twin copy sits outside the hierarchy entirely as a pipeline artifact.

**Recommended default and causal basis.** Cite the entrypoint for what happens when, the depth files for what is true and why, and never the twin. Orchestration outranks depth for sequencing questions because the entrypoint decides what gets consulted at all, yet depth outranks orchestration for content questions, the entrypoint states rules the doctrine argues.

**Operating conditions and assumptions.** All family files remain co-archived at their 202604 versions. Precedence within the skill family as it bears on this theme, the family's internal precedence below the entrypoint was mapped in the doctrine theme.

**Failure boundary and alternatives.** Flattening the two rank orders produces miscitations both ways, sequencing from the doctrine or rationale from the entrypoint. Bounded alternatives and recoveries: flat authority across the family, which the anti-duplication rule was written to prevent.

**Counterexample, gotchas, and operational comparison.** The Claude variant predates directory-style loading and points at a monolithic reference file, its internal hierarchy is two-level while the Codex family is three-level, hierarchy claims must name which dialect's world they describe. Good: sequencing from the entrypoint and citing the doctrine's Pattern 4 for capability rationale. Bad: quoting entrypoint guardrails as the full security doctrine. Borderline: citing the monolithic reference the Claude form names, legitimate for that dialect's lineage, note it bypasses the Codex family structure.

**Verification, evidence, and uncertainty.** Test citation targets against the dual ranking at audit. The family's six evolved files read across three assignments. None material, the family is fully enumerated and evolved.

**Second-order consequence.** With this assignment the corpus completes the entire tauri-executable-specs-01 family, spine, map, playbook context, doctrine, gates, and now both entrypoint dialects, making it the first fully-evolved multi-file skill in the corpus and a template for how the remaining families' hierarchies should be documented.

**Revision decision.** State the dual ranking, entrypoint governs sequence and mode, depth governs content and rationale, twin excluded as a copy.

**Retained seed evidence.** The hierarchy rows and the classification vocabulary remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202604/tauri-executable-specs-01.md | canonical primary source | Tauri Executable Specs 01; Core Principle; Mode Selection | What guidance, warning, or example should this source contribute to Tauri Executable Skill Patterns? |
| agents-used-monthly-archive/codex-skills-202604/tauri-executable-specs-01/SKILL.md | legacy historical source | Tauri Executable Specs 01; Mode Selection; Workflow | What guidance, warning, or example should this source contribute to Tauri Executable Skill Patterns? |
| unclassified-yet/Tauri Executable Specifications - single MD file.md | supporting context source | Tauri Executable Specs 01; Core Principle; Mode Selection | What guidance, warning, or example should this source contribute to Tauri Executable Skill Patterns? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing structure makes the two-dialect skill safely quotable. The seed generic worked-example artifact misses this theme's natural object, a dialect diff ledger, a section-by-section table with one row per skill section holding the Claude wording, the Codex wording, a shared-or-divergent verdict, and for divergent rows the platform reason, maintained as the canonical answer to which variant says what.

**Recommended default and causal basis.** Build the ledger once from the current texts, store it beside the audit records, and re-derive it after every sweep. Every consumer of this two-dialect skill eventually asks whether some rule is doctrine or dialect, and the ledger answers in one lookup what otherwise costs a two-file comparison each time.

**Operating conditions and assumptions.** Section alignment stays feasible, a restructured variant would need the ledger rebuilt rather than patched. One ledger for this skill's variant pair, the pattern generalizes to any future multi-dialect skill in the archive.

**Failure boundary and alternatives.** Without the ledger, dialect drift goes unnoticed, a future edit to one variant silently forks the doctrine and every consumer inherits whichever fork they happened to read. Bounded alternatives and recoveries: on-demand file comparison per question, workable and quadratically wasteful across consumers.

**Counterexample, gotchas, and operational comparison.** Byte-level diffing is the wrong tool, the variants share almost no identical sentences while sharing nearly all substance, the ledger must compare at the claim level, not the string level. Good: a ledger row marking mode definitions shared with trivially different phrasing. Bad: a ledger built by string diff reporting everything divergent. Borderline: including the twin as a third column, redundant while checksums match, the checksum check covers it.

**Verification, evidence, and uncertainty.** Re-derive the ledger and diff against the stored version at each audit. The section-by-section comparison performed this session. How often sweeps will touch these files is unknown.

**Second-order consequence.** The ledger doubles as a fork detector, re-deriving it after any sweep and diffing against the stored version catches single-variant edits mechanically, turning a curation hazard the twin-copy row already demonstrates into a checked invariant.

**Revision decision.** Define the artifact as the section-aligned diff table with verdict and platform-reason columns.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: skill activation contract with progressive disclosure, misuse case, and verification gate.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Tauri Executable Skill Patterns | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercises transmit the entrypoint's transformations fastest. The seed abstract usage lines stand where the skill supplies teachable objects, the five-row default-combination table mapping request shapes to mode pairs, the traceability row shape pairing req_id through metric, and the vague-to-executable rewrite implied by the rejection rule, safer becoming WHEN a malformed archive is imported THEN the importer SHALL reject it with a serializable error.

**Recommended default and causal basis.** Certify understanding by rewriting three vague claims into thresholded WHEN-THEN-SHALL rows with matching test entries. An entrypoint teaches request handling, so its worked examples are transformations, request to modes, requirement to test row, adjective to contract, each a mapping a learner can drill.

**Operating conditions and assumptions.** Drill rewrites are checked against the rejection rule's standard, thresholds or explicit boundary effects, not just formal shape. Teaching this theme's material, depth-content drills belong to the doctrine and gates themes.

**Failure boundary and alternatives.** Teaching the skill as section summaries misses that its value is transformational, knowing the mode names without the request-to-mode mapping produces classification theater. Bounded alternatives and recoveries: worked examples from live Tauri projects, richer and outside local evidence.

**Counterexample, gotchas, and operational comparison.** contract grammar without measurability is the drill's trap, a row can wear the WHEN-THEN-SHALL shape while still saying nothing checkable, the metric column exposes it. Good: a rewrite with a threshold, a boundary effect, and a filled metric cell. Bad: SHALL be more reliable in contract costume. Borderline: a qualitative boundary effect with no number, the rule accepts explicit boundary effects, judge the explicitness.

**Verification, evidence, and uncertainty.** Grade drill outputs against the rejection rule and metric column at review. The mode table, row shape, and rejection clause read this session. Transfer from drills to live packet quality is unmeasured.

**Second-order consequence.** The adjective-to-contract drill is the highest-leverage of the three because it is platform-independent and Tauri-independent, the same rewrite muscle serves any spec-first workflow, which explains why both this skill and the corpus spec independently enforce it, it is the transferable core of executable specification.

**Revision decision.** Anchor the section on the three transformation drills, request-to-modes, adjective-to-contract, requirement-to-test-row, each with pass and fail forms.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Tauri Executable Skill Patterns after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Tauri Executable Skill Patterns as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Tauri Executable Skill Patterns only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which numbers show the entrypoint producing decision-complete packets versus theater. The seed compile-centric indicator misses the entrypoint's measurable surface, packet completeness against the seven-section contract, adjective escape rate, vague claims surviving into accepted requirements, mode-choice accuracy against the default table, traceability coverage, REQ rows with matching test rows, and open-question volume as an honesty signal per packet.

**Recommended default and causal basis.** Track the five measures per packet batch and read open-question trends against request vagueness before reacting. Packet quality is measurable at packet time while its consequences surface months later in delivery, so leading indicators at the contract layer are the only timely feedback this skill can get.

**Operating conditions and assumptions.** Packets are archived inspectably, the output contract's fixed order makes the measures cheap to extract. Measuring this entrypoint's operation plus this node's own evidence series, depth-file metrics live in their themes.

**Failure boundary and alternatives.** Without the loop, packet decay is invisible until implementation, vague requirements and unmatched tests pass silently and surface as rework whose cause nobody traces back to the packet. Bounded alternatives and recoveries: delivery-outcome metrics alone, accurate and months too late to steer packet practice.

**Counterexample, gotchas, and operational comparison.** Traceability coverage can be gamed with placeholder test ids, weight rows whose test_id resolves to an actual test artifact. Good: a batch review finding two adjective escapes and tightening the rewrite habit. Bad: celebrating zero open questions as rigor. Borderline: rising open-question volume after a new stakeholder joins, a request-quality signal read through the vagueness lens.

**Verification, evidence, and uncertainty.** Confirm packet archives feed the five series at each review cycle. The output contract structure and rejection rule read this session. No packet corpus exists locally, the loop starts unprimed.

**Second-order consequence.** Open-question volume is a two-sided signal like the gates theme's waiver rate, zero open questions across many packets means the honesty valve is disused, packets claiming completeness they cannot have, while high volume means requests arrive too vague or the agent under-decides, the healthy band is in the middle and only trend data locates it.

**Revision decision.** Adopt the five packet-derived measures with adjective escape rate as the sharpest quality alarm.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: the feature can be tested across frontend, Rust command, and packaged desktop behavior.
Failure signal: the reference ignores permissions, IPC risk, or platform-specific verification.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this synthesis is declared faithful. The seed shape items never audit this theme's distinctive claims, the twin checksum equality, the two-document effective count, the shared-spine and dialect-diff inventory, the dead and resolvable path statuses, the model pin's dated-configuration label, and the family-complete hierarchy claim now that all six files are evolved.

**Recommended default and causal basis.** Run all distinctive checks wholesale each cycle, including the sideways family check, and date the results. The synthesis rests on a copy topology and a cross-file diff, both cheap to re-verify and both silently breakable by sweeps that touch any of the three paths.

**Operating conditions and assumptions.** The three paths and the mirror target remain in the archive. Auditing this reference against its three mapped paths and the mirror target, all read or resolved this session.

**Failure boundary and alternatives.** A shape-only audit passes while a sweep edits one twin, forking the document this review reports as identical, or moves the mirror target the resolvable-path claim depends on. Bounded alternatives and recoveries: sampled auditing, ill-suited to claims about exact copy equality.

**Counterexample, gotchas, and operational comparison.** Twin equality must compare full bytes, not sizes or heads, a single-line curation edit to the unclassified copy is exactly the drift the check exists to catch. Good: an audit logging both checksums, twin equality, path statuses, and sibling-theme spot checks. Bad: declaring fidelity from seed-shape checks alone. Borderline: skipping the sideways check when no sibling changed, defensible if sibling commits are verified unchanged.

**Verification, evidence, and uncertainty.** Execute the distinctive items and date each result in the audit record. The claim structure of every distinctive statement in this document. Future sweep behavior toward the unclassified directory is the main unknown.

**Second-order consequence.** This theme's audit is the first in the corpus with a cross-theme dependency, its family-complete claim is falsified if any sibling tauri theme regresses, so the checklist must look sideways at three other references, not just down at its own sources, a shape future multi-file families will repeat.

**Revision decision.** Add the distinctive items, both checksums with twin equality, the diff-ledger re-derivation, both path resolutions, and the family-completeness cross-check against the other tauri themes.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Tauri Executable Skill Patterns.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a question arriving at the family's front door. The seed token-split rows point nowhere while this theme completes a four-theme family whose routing is now closed, packet-making and mode questions stay here, delivery gating goes to the gates theme, pattern rationale to the doctrine theme, and the WHEN-THEN-SHALL requirement machinery shared with non-Tauri work goes to the executable-specs and TDD themes.

**Recommended default and causal basis.** Answer intake and orchestration questions here and forward each depth question to its stage owner explicitly. The entrypoint is the family's front door, so most arriving questions are really destined inward, and the routing map's own logic, load depth by task type, is the template this theme's routing follows at corpus scale.

**Operating conditions and assumptions.** All three sibling themes remain complete, verified this assignment. Sending away what this theme holds only incidentally, destination doctrine is deliberately not restated.

**Failure boundary and alternatives.** Answering depth questions at the door recreates the duplication the family forbids, while routing away mode and packet questions abandons the one theme that owns orchestration. Bounded alternatives and recoveries: for platform questions neither dialect answers, nothing local, say so rather than stretching.

**Counterexample, gotchas, and operational comparison.** Verification questions split by stage, choosing the mix is intake territory here, running the commands is gates territory, the same word verification crosses the boundary. Good: answering which modes for a sidecar request here, then forwarding supervision rationale to the doctrine theme. Bad: explaining capability pattern scores at the entrypoint. Borderline: a traceability-table question, the shape is here, the test-first sequencing belongs to the TDD themes, split it.

**Verification, evidence, and uncertainty.** Sample recent answers for stage-consistent routing. The family structure and corpus theme map read across three assignments. None material, the family and its themes are fully enumerated.

**Second-order consequence.** The family's four corpus themes now mirror the skill's own context strategy, intake here, playbook grammar in spec themes, doctrine and gates in their themes, so an agent can navigate the corpus's tauri coverage using the skill's own load-progressively instructions, the archive's structure and the corpus's structure have converged.

**Revision decision.** Route by lifecycle stage, request intake and mode choice here, design rationale to doctrine, delivery checks to gates, requirement grammar shared beyond Tauri to the spec themes.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use executable, doctrine, or legacy Tauri references when the task is about specs, policy, or migration.
Adjacent reference 1: consider the tauri adjacent reference when the current task pivots away from tauri executable skill patterns.
Adjacent reference 2: consider the executable adjacent reference when the current task pivots away from tauri executable skill patterns.
Adjacent reference 3: consider the skill adjacent reference when the current task pivots away from tauri executable skill patterns.

## Workload Model

**Decision supported.** This section helps decide how to budget a theme whose recurring cost is vagueness negotiation. The seed symbol-count boundary misprices this node's work, whose units are the sunk two-variant read, 210 effective lines in minutes, per-request packet-making that scales with request vagueness rather than code size, per-audit twin and diff checks in minutes, and the one-time diff-ledger construction.

**Recommended default and causal basis.** Budget packets by adjective count and stakeholder availability, not code size, and keep audits at the checksum-and-diff pace. Packet-making cost tracks vagueness because every unmeasurable adjective demands a negotiation round trip, while crisp requests flow through the seven sections at reading speed.

**Operating conditions and assumptions.** Stakeholders answer threshold questions promptly, negotiation latency dominates vague-request cost. Budgeting this node's upkeep and the entrypoint's operation, implementation time belongs to the depth themes' models.

**Failure boundary and alternatives.** Budgeting packets by expected code size inverts the real driver, a one-line fix behind a vague safety demand costs more packet time than a large feature with clean acceptance criteria. Bounded alternatives and recoveries: the seed's crate-slice and symbol boundaries, retained as outer limits on any single consultation's scope.

**Counterexample, gotchas, and operational comparison.** The twin check is constant-time but the diff-ledger re-derivation is not, a restructured variant turns minutes of audit into a rebuild, budget the rare case separately. Good: a sprint reserving packet time proportional to open threshold questions. Bad: skipping the packet for a small fix hiding an unmeasured safety claim. Borderline: batching several crisp micro-requests into one packet, economical if their boundaries truly overlap.

**Verification, evidence, and uncertainty.** Check packet records for duration against adjective counts at review. The line counts and contract structure from this session. Real negotiation latencies vary by organization and are unrecorded.

**Second-order consequence.** The entrypoint's own design minimizes its recurring cost, the mode table and output contract are decision caches, precomputed answers to which lenses and which sections, so per-request thinking spends only on the request's novel content, the same cache economy the doctrine's non-negotiables provide one layer down.

**Revision decision.** Restate the model in its four units, sunk read, vagueness-scaled packet-making, audit checks, and ledger setup.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Tauri Executable Skill Patterns as a rust system operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | systems implementation, CLI or service hardening, async/runtime review, and safety-sensitive refactoring where compile success is necessary but not sufficient | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one crate or bounded workspace slice, up to 100 changed symbols, and one integration path that exercises error handling | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Tauri Executable Specs 01; Core Principle; Mode Selection; Workflow; Output Contract; Reference Use; Tauri Executable Specs 01; Mode Selection; Workfl | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is skill activation contract with progressive disclosure, misuse case, and verification gate | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which invariants must demonstrably hold for this node's claims to stay citable. The seed document-property thresholds stand where this node's invariants should, twin checksum equality, dialect-attribution fidelity keeping every quote tagged to its variant, shared-core fidelity keeping doctrine claims backed by both texts, path-status accuracy for the dead and resolvable references, and family-completeness consistency with the three sibling themes.

**Recommended default and causal basis.** Hold all five invariants at wholesale-audit cadence with the dual-witness check weighted heaviest. Each invariant guards a distinct corruption, twin forks split the document, misattribution manufactures dialect content, single-variant doctrine claims overstate the shared core, stale path statuses mislead resolvers, and sibling regressions orphan the family claim.

**Operating conditions and assumptions.** Both variants stay readable at their paths and sibling themes stay complete. Invariants for this node's claims, reliability of packets made with the skill belongs to their makers.

**Failure boundary and alternatives.** The costliest breach is shared-core inflation, a rule present in one variant quoted as the skill's doctrine, because every downstream consumer of the ledger inherits the overstatement. Bounded alternatives and recoveries: fact-only invariants, blind to the attribution drift that matters most here.

**Counterexample, gotchas, and operational comparison.** The dual-witness rule has legitimate exceptions, the core principle genuinely exists only in the Claude text, such single-witness claims are valid with their tag, the invariant polices missing tags, not single witnesses. Good: an audit confirming every shared-core row has quotes from both files. Bad: a summary presenting the context strategy as the skill's universal loading rule. Borderline: paraphrasing convergent guardrails as one rule, faithful in substance, note the merged wording.

**Verification, evidence, and uncertainty.** Review the five invariant series at each corpus checkpoint with dated evidence. The claim structure established across this document. None about current truth, all five invariants were verified during this evolution.

**Second-order consequence.** Dual-witness checking is the invariant unique to multi-dialect themes, every doctrine-level claim needs a witness in both variants before it enters the shared core, a two-source rule applied inside one skill, and claims with a single witness must carry their dialect tag as a visible limitation.

**Revision decision.** Publish the five invariants with evidence methods, checksum runs, attribution spot checks, dual-witness checks for core claims, path re-resolutions, and sibling-theme cross-checks.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which decay modes need standing probes for a twin-copied two-dialect theme. The seed generic rows miss how this node specifically dies, twin fork, one copy edited without the other, dialect blur, quotes circulating without variant tags until the two forms merge in folklore, core inflation, single-variant rules promoted to shared doctrine, and contract rot, the seven-section output order drifting in reuse until packets lose comparability.

**Recommended default and causal basis.** Run the four probes at each audit, extending the contract-rot probe to any local packet templates derived from the skill. All four attack the theme's load-bearing structure, its copy topology, its attribution discipline, its dual-witness core, and the fixed packet shape that makes outputs auditable.

**Operating conditions and assumptions.** Derivatives are discoverable, unobserved templates default the probe to fixed-cadence checks on known consumers. Decay of this node's claims and reuse, packet failures in live use belong to the metrics loop.

**Failure boundary and alternatives.** They compound, a forked, blurred, inflated citation of the skill produces packets in a format nobody can check against either original, authority intact and content unmoored. Bounded alternatives and recoveries: quotation-only reuse with variant tags and section anchors, resistant to all four modes.

**Counterexample, gotchas, and operational comparison.** Dialect blur reads as harmless simplification, the skill says, until a blurred rule contradicts one variant, reconstructing which text actually said what then costs the full diff the ledger would have cached. Good: a probe catching an edited unclassified twin one sweep after curation touched it. Bad: a team template emitting packets with quality gates before the verification matrix. Borderline: a derivative merging open questions into the plan section, materially rot, flag it even though the words survive.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe and trigger point. The decay structure consolidated from this document's preceding sections. Derivative visibility determines probe coverage.

**Second-order consequence.** Contract rot is the mode with the widest blast radius because the output contract is the skill's public interface, downstream tooling and reviewers key on the seven-section order, so a reordered template breaks every consumer silently while both source texts remain pristine, decay can live entirely in the derivatives.

**Revision decision.** Add the four rows with probes, byte comparison for forks, tag presence checks for blur, dual-witness sampling for inflation, and section-order checks on reused packet templates.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| runtime contention masks failure | async tasks, locks, or shared state degrade under concurrent load | add contention benchmark, timeout budget, and structured cancellation path |
| panic path reaches production | unwrap or unchecked invariant survives compile and unit tests | require panic audit, Result propagation, and integration failure fixture |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide what a contested or bounced packet is allowed to trigger. The seed corpus-process bullets stand beside what the entrypoint implies for contested packets, a rejected requirement is renegotiated with the stakeholder, not silently reworded by the agent, a packet bounced at review returns to the failing workflow step rather than being patched at the surface, and repeated rejections of the same request shape signal a missing default combination or a stakeholder education gap, not grounds for loosening the rejection rule.

**Recommended default and causal basis.** Route every packet failure to its owning workflow step and every recurring failure shape to process review, never to quiet standard erosion. Spec-first workflows concentrate their friction at intake by design, every threshold negotiation is friction pulled forward from implementation where it would cost more, so intake pressure is the system working, not failing.

**Operating conditions and assumptions.** Failure records name the step at fault, the packet's section order makes the mapping mechanical. Failure-response doctrine for packet-making, delivery-gate failure response belongs to the gates theme.

**Failure boundary and alternatives.** The erosion path is accommodation, agents quietly accepting adjectives to keep stakeholders comfortable, which defers each negotiation to the moment a test cannot be written, the most expensive possible venue. Bounded alternatives and recoveries: the doctrine theme's rubric-valve for contested rules, same principle at the doctrine layer.

**Counterexample, gotchas, and operational comparison.** Stakeholder fatigue is the pressure that breaks the rule, the third threshold question in one meeting tempts the agent to guess, guessed thresholds wearing WHEN-THEN-SHALL grammar are the hardest vagueness to detect later. Good: a bounced design section reworked from step three with the requirement rows untouched. Bad: an agent softening a rejected safer into robust to avoid a second negotiation. Borderline: a provisional threshold marked as agent-estimated pending confirmation, acceptable exactly because the tag preserves the negotiation debt.

**Verification, evidence, and uncertainty.** Audit rework records for step addresses and renegotiation notes. The workflow numbering and rejection clause read this session. Escalation thresholds for recurring shapes are judgment, neither variant sets them.

**Second-order consequence.** The workflow's numbered steps give bounce-backs a natural address, a packet failing on design coherence returns to step three, one failing on vague requirements returns to step two, so the seven-step structure doubles as a triage system for rework, each failure type has a home step and skipping back too few steps is the subtle form of surface patching.

**Revision decision.** State the response rules, renegotiate rather than reword, return to the failing step, and escalate repeated same-shape rejections to process review rather than rule loosening.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide which documentary properties every packet must carry. The seed generic records stand beside the entrypoint's own visibility demands, the output contract makes every packet's reasoning inspectable in fixed order, the traceability table makes requirement-to-test linkage a visible artifact, open questions make residual uncertainty explicit, and the quality-gate-results section forces verification outcomes into the packet rather than leaving them in terminal scrollback.

**Recommended default and causal basis.** Emit every packet in contract order with populated traceability and gate sections, treating structure as the observability layer. An orchestration skill's observability is documentary, its work product is decisions, and the contract's seven sections are precisely the decision log that makes packet-making auditable after the fact.

**Operating conditions and assumptions.** Packets are retained in an archive, observability of discarded documents is nothing. Observability of packet-making this theme owns, runtime observability of Tauri apps belongs to the doctrine theme's supervision rules.

**Failure boundary and alternatives.** Free-form packet writing loses the audit trail, the same decisions scattered across prose cost a reconstruction every time someone asks why this design or where is the test for this requirement. Bounded alternatives and recoveries: the doctrine theme's supervision-shaped runtime observability, the other half of this skill family's visibility story.

**Counterexample, gotchas, and operational comparison.** The gate-results section tempts summarization, all green, which destroys the signal, the section wants the per-gate outcomes the gates theme's record form captures. Good: a packet archive answering a quarter's design-omission query with one search. Bad: a packet whose matrix says tests exist without ids. Borderline: an abbreviated packet for a trivial change, acceptable if the omitted sections are declared empty rather than absent.

**Verification, evidence, and uncertainty.** Check archived packets for the four surfaces at sampled batches. The output contract and table shape read in both variants this session. Packet retention practices vary by team and are unrecorded here.

**Second-order consequence.** The contract turns packets into longitudinal data, because every packet shares the section order, questions like how often do designs omit permissions become greppable across a packet archive, the fixed shape is what upgrades individual documents into the metrics substrate the feedback loop needs.

**Revision decision.** State the four documentary surfaces, ordered sections, traceability rows, open questions, and embedded gate results, each with what it makes visible.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture error count, timeout count, retry count, saturation signal, and rollback trigger.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide when performance enters a packet's verification mix. The seed latency packet stands beside the entrypoint's performance stance, performance is admitted to the verification mix only when explicitly stated in a requirement, which means stated as a threshold in a REQ-TAURI row, and until then the packet's performance duty is structural, flagging UI-stall risks into the design split rather than benchmarking them.

**Recommended default and causal basis.** Ask the performance question explicitly during requirement writing and record either a threshold or a conscious waiver. An intake skill cannot measure anything, its performance lever is whether performance becomes a requirement at all, so the qualifier concentrates the entire performance question into the requirement-writing step.

**Operating conditions and assumptions.** Stakeholders understand the waiver's meaning, an unstated threshold is a decision, not an oversight, only if it was asked. The entrypoint's performance-admission rule, structural performance doctrine belongs to the doctrine theme, measurement method to the system design theme.

**Failure boundary and alternatives.** The silent failure is performance by assumption, nobody states a threshold, the mix omits performance tests, and the first measurement happens in a user's complaint. Bounded alternatives and recoveries: default performance budgets for every packet, safer and contrary to the explicit-statement rule both variants carry.

**Counterexample, gotchas, and operational comparison.** Structural performance rules still apply without any stated threshold, async commands and payload routing are correctness under the doctrine, the qualifier governs only measurement-based verification. Good: an import feature whose REQ row states a ten-second bound and whose matrix carries the matching perf test. Bad: a packet inferring speed matters and adding unanchored benchmarks. Borderline: a stakeholder saying fast enough, record the negotiation and the chosen proxy threshold.

**Verification, evidence, and uncertainty.** Sample packets for asked-and-answered performance dispositions. Workflow step four's qualifier read in both variants this session. How often unstated performance needs bite in practice is unrecorded.

**Second-order consequence.** The qualifier is a forcing function aimed at stakeholders, not testers, by refusing unstated performance work it makes the cost of vague speed wishes visible at negotiation time, either name a number or accept that no test will guard it, the same adjective-to-threshold pressure applied to the performance domain.

**Revision decision.** State the admission rule and its consequence, performance intent must surface at step two as a threshold or it legitimately vanishes from verification.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require memory, panic, and concurrency evidence; hot-path operations need p95 under 10 ms locally or an explicit benchmark exception.
Leading indicator to measure: the feature can be tested across frontend, Rust command, and packaged desktop behavior.
Failure signal to watch: the reference ignores permissions, IPC risk, or platform-specific verification.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what scale this skill applies and where its authority hands off. The seed task-splitting bounds stand beside the entrypoint's own scale limits, the skill governs one Tauri request at a time through one packet, its modes bound the lenses applied rather than the request size, its authority ends at packet handoff where implementation and gating take over, and its dialect pair bounds platform reach, Claude-shaped and Codex-shaped consumption with anything else requiring labeled adaptation.

**Recommended default and causal basis.** Split multi-request intakes into per-request packets and hand off at the contract's last section every time. An intake skill scales by packet repetition, each request gets the same bounded treatment, and its self-limits, selective conventions, explicit exclusions of forced universality, keep packet cost flat as request volume grows.

**Operating conditions and assumptions.** Requests are separable, genuinely coupled changes may share a packet with the coupling named. Scale limits of this skill's application, this evolution's own task splitting keeps the seed paragraphs.

**Failure boundary and alternatives.** Scope creep here means the packet swallowing implementation, design sections growing into code, or one packet spanning unrelated requests until traceability rows cross-tangle. Bounded alternatives and recoveries: program-level planning instruments for multi-feature roadmaps, upstream of what this per-request skill owns.

**Counterexample, gotchas, and operational comparison.** The handoff boundary is porous in practice, implementers ask the packet author to just decide emerging questions, each answered question outside a packet revision erodes the decision-complete record. Good: three related features arriving together and leaving as three packets with named dependencies. Bad: a packet amended verbally for a week after handoff. Borderline: a follow-up question answered in a dated packet revision, the record survives if the revision is real.

**Verification, evidence, and uncertainty.** At each audit, check packet archives for scope-shape consistency. The contract structure and selective-convention rules read this session. How teams handle the porous handoff in practice is unrecorded.

**Second-order consequence.** The one-packet-per-request bound is what makes the metrics loop possible, packets are comparable units only while they share scope shape, so the boundary is not bureaucracy but the sampling design that lets adjective escape rates and omission queries mean anything across a packet archive.

**Revision decision.** State the three boundaries, one request per packet, handoff at packet completion, and dialect-bounded platform reach.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Tauri Executable Skill Patterns stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal decay in this node's claims and on what clocks. The seed theme-name queries would surface generic Tauri chatter while this node's staleness has exact homes, the two checksums and their twin equality, the unclassified directory's curation state, the mirror target's continued existence, the sweep inventory that could gain newer skill revisions or a third dialect, and the model pin whose generation-currency only external windows can judge.

**Recommended default and causal basis.** Run the structural probes each cycle and note platform-pin currency at external windows. An entrypoint's decay is structural rather than factual, its doctrine barely ages but its copies, paths, and platform pins all track external processes, curation, sweeps, and model generations.

**Operating conditions and assumptions.** The archive remains locally accessible, all probes here are offline. Refreshing this node's evidence and labels, live packet practice needs no refresh from here.

**Failure boundary and alternatives.** Undirected refreshing would miss the quiet fork, one twin edited in a curation pass, which no topical search would ever surface. Bounded alternatives and recoveries: declaring the theme historical wholesale, wasteful for a skill family the corpus just finished documenting end to end.

**Counterexample, gotchas, and operational comparison.** A vanished unclassified twin is not necessarily loss, curation may have pruned a duplicate deliberately, the probe's finding needs a curation-intent check before being filed as damage. Good: a refresh logging twin equality, mirror existence, and no new dialects in the sweep. Bad: logging that Tauri skill patterns searches returned nothing new. Borderline: filing a pruned twin as an open question pending curation intent, the right hesitation.

**Verification, evidence, and uncertainty.** Record each probe with its date and the claim it confirmed or updated. The copy topology and path inventory established this session. Future curation of the unclassified directory is what the probes exist to learn.

**Second-order consequence.** The twin makes this theme's refresh unusually mechanical, byte equality is a one-command probe with a binary answer, so this node can detect its most dangerous drift, the fork, more cheaply than any prose theme detects paraphrase decay, copy topology is a refresh asset, not just a hazard.

**Revision decision.** Replace name queries with targeted probes, dual checksum runs each cycle, unclassified-directory status checks, mirror-target existence checks, sweep scans for new dialects or revisions, and a model-pin currency note at external windows.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | tauri executable skill patterns official documentation best practices |
| `github_repository_query_phrase` | tauri executable skill patterns GitHub repository examples |
| `release_notes_query_phrase` | tauri executable skill patterns changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label, chain, and state each claim class here may be reused. The seed label definitions stand without this assignment's distinctive ledger, three mapped paths read in full, two resolving to one byte-identical document, both checksums recorded, the mirror target's existence confirmed by path resolution, the four family references already read in prior assignments, zero external URLs fetched, and no packet produced or Tauri request processed with the skill this session.

**Recommended default and causal basis.** Before reusing a claim, declare it structural fact, dual-witnessed doctrine, single-variant doctrine, or designed inference, and treat unlabeled claims as inference. An entrypoint review's evidence splits between what the texts say, fully verifiable locally, and what the skill does, demonstrable only through packets this corpus has never run through it.

**Operating conditions and assumptions.** All three paths stay unedited and labels persist through reuse, both watched by the established probes. Reuse rules for this document's claims, each claim travels with class, variant tag, and verification state.

**Failure boundary and alternatives.** The costliest mislabel would present the workflow as validated practice, both variants are one author's archived design, internally coherent and cross-consistent, but no packet outcome exists locally to witness their effectiveness. Bounded alternatives and recoveries: running a real Tauri request through the skill to earn a demonstrated stratum, declined for scope, queued as the natural verification upgrade.

**Counterexample, gotchas, and operational comparison.** Cross-variant agreement corroborates intent, not correctness, both texts could faithfully preserve a shared mistake, the dual witness upgrades authorship confidence only. Good: reusing the mode set as dual-witnessed doctrine and the core principle as Claude-only with its tag. Bad: citing the workflow as field-proven method. Borderline: reusing the traceability row shape as a general spec tool, sound design divorced from its evidence, label it method reuse.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares its class cleanly. This assignment's ledger, full reads, dual checksums, zero fetches, zero packets. The skill's practical effectiveness awaits packets that may never be run locally.

**Second-order consequence.** The dialect pair gives quoted doctrine an internal consistency check no single-source theme has, where both variants state a rule independently in different words, the agreement is evidence of deliberate doctrine rather than drafting accident, a weak but real corroboration layer between local fact and unsupported quotation.

**Revision decision.** Publish the strata explicitly, texts, checksums, and copy topology as same-session local facts, modes, workflow, contract, and guardrails as quoted doctrine with variant tags, the author-machine paths as archive artifacts, and all extensions here, the diff ledger, the dual-witness rule, the vagueness-scaled budget, as labeled inference.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
