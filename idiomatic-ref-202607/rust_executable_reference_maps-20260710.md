# Rust Executable Reference Maps

**Decision supported.** This section helps decide which routing artifact to hand an agent and when inlined doctrine should replace routed doctrine. The seed no seed line states what this theme's maps actually are, two routing artifacts from one bundle, a 91-line router that dispatches requests across three sibling references by work mode and task type, and a 246-line standalone reference that inlines the merged doctrine so a reader can skip the routing entirely.

**Recommended default and causal basis.** Use the router as the entry artifact when working inside the bundle and hand the standalone merge to any context that cannot follow file references. The bundle shipped both shapes deliberately, the router serves agents working inside the skill with siblings on disk, while the standalone merge serves contexts where only one file can travel, so the theme is really about when routing beats inlining.

**Operating conditions and assumptions.** The bundle's sibling files remain at their referenced paths, broken routing silently degrades to guesswork. This reference covers the routing and packaging patterns of the executable-specs bundle, the routed content's own doctrine lives with the gates, reliability, and spec themes.

**Failure boundary and alternatives.** Conflating the two shapes wastes their designs, reading the standalone file section by section through the router's dispatch logic doubles the navigation, and carrying the router alone into a context without its siblings routes to files that are not there. Bounded alternatives and recoveries: single monolithic references with internal navigation, the rust-coder bundles' shape, heavier per load and immune to broken sibling paths.

**Counterexample, gotchas, and operational comparison.** The third mapped source is a byte-identical copy of the second sitting in an unclassified folder, so the theme's three local rows carry only two distinct documents, an audit that counts rows as sources overstates the corpus. Good: an agent loading the router, matching Reliability Mode, and reading only the routed sections. Bad: pasting the router into a chat with no sibling files attached. Borderline: reading the standalone merge inside the bundle, redundant but harmless, the siblings hold the deeper prose.

**Verification, evidence, and uncertainty.** Check that consumers of this theme can name which shape they hold and that routed paths resolve. Both distinct documents read in full and the duplicate verified by diff this session. Which shape agents actually receive more often in practice is unrecorded.

**Second-order consequence.** The standalone file is itself evidence about compression, it reduces three references and a playbook to 246 lines by keeping tables, templates, and lists while dropping prose justification, a worked answer to how much doctrine survives when the byte budget shrinks tenfold.

**Revision decision.** Open with the two-shape framing, router plus standalone merge, and the selection rule, route when siblings are reachable, inline when the reference must stand alone.

**Retained seed evidence.** The exact theme title and map framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide which source justifies each claim class reused from this theme. The seed three local rows present as three sources when a diff run this session proves rows two and three byte-identical, the unclassified-yet copy duplicates the idiomatic-references file exactly, so the theme's evidence base is one router plus one standalone merge, not three documents.

**Recommended default and causal basis.** Cite the router and the canonical standalone copy for local claims and name the duplicate only when discussing corpus filing. The corpus inherited files from multiple archive sweeps and the unclassified folder holds material awaiting classification, duplication across sweeps is a filing artifact, not independent corroboration.

**Operating conditions and assumptions.** The diff-verified identity holds only as of this session's read, future edits to either copy break the equivalence. Provenance for this document's statements, the four external URLs stayed unretrieved throughout this evolution and confer no current-fact authority.

**Failure boundary and alternatives.** Counting the duplicate as corroboration inflates confidence, two identical files agreeing is one author speaking twice, and any future divergence between the copies would signal an edit event, not a disagreement between sources. Bounded alternatives and recoveries: a future mapping revision that collapses the duplicate rows and adds the routed sibling files the router points at, which are the theme's real unread dependencies.

**Counterexample, gotchas, and operational comparison.** The four external rows are the rust-cluster template set again, api-guidelines, tokio, axum twice, none of which the routing patterns cite or need, the theme's content is bundle-internal. Good: citing the router's line for a work-mode dispatch claim. Bad: citing both identical copies as agreeing sources. Borderline: citing the unclassified path directly, resolvable today, the canonical path is the stabler citation.

**Verification, evidence, and uncertainty.** Re-run the diff between the two copies before relying on their equivalence. Full reads of both distinct documents and the byte-identical diff result this session. Why the copy landed in unclassified-yet and whether classification will move or edit it is unknown.

**Second-order consequence.** The duplicate is itself corpus signal, a file important enough to be swept twice and staged for classification suggests the standalone merge circulated as a working artifact beyond its bundle, which is exactly the portability the two-shape framing predicts.

**Revision decision.** Annotate the mapping with the deduplication finding, cite the idiomatic-references path as the canonical copy, and treat the unclassified path as a filing duplicate pending its classification.

**Retained seed evidence.** All three local rows and four external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202604/rust-executable-specs-01/references/reference-map.md | local_corpus_source_material | local_corpus_sourced_fact | reference detail file for deeper pattern evidence |
| agents-used-monthly-archive/idiomatic-references-202604/rust-executable-specs-01/rust-executable-specs-reference.md | local_corpus_source_material | local_corpus_sourced_fact | historical idiomatic pattern corpus |
| unclassified-yet/Rust Executable Specifications Reference.md | local_corpus_source_material | local_corpus_sourced_fact | local agent-usable source material |
| https://github.com/rust-lang/api-guidelines | external_research_source_material | external_research_sourced_fact | Rust library-team API design recommendations |
| https://github.com/tokio-rs/tokio | external_research_source_material | external_research_sourced_fact | async runtime implementation and operational model |
| https://github.com/tokio-rs/axum | external_research_source_material | external_research_sourced_fact | Rust web framework implementation and design claims |
| https://docs.rs/axum/latest/axum/ | external_research_source_material | external_research_sourced_fact | published crate documentation for routing and extractors |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide in what order classification signals determine which reference material loads first. The seed scored corpus rows stand where the theme's own ranking is the router's dispatch hierarchy, work mode outranks task type, the router asks which of four modes fits before offering six task-type reading orders, and both outrank the default read order, which is the fallback when neither classification bites.

**Recommended default and causal basis.** Classify mode first, refine by task type second, and fall back to the four-step default order for large or unclassifiable tasks. Mode captures the dominant risk, vague requirements, structural delivery, correctness under stress, or review, while task type captures the artifact shape, and risk determines which sibling reference leads more reliably than shape does.

**Operating conditions and assumptions.** The request carries enough signal to classify, genuinely ambiguous requests take the default order by design. This ranking orders the routing decision, pattern-level scores for the routed content live with the reliability theme's scoreboard.

**Failure boundary and alternatives.** Dispatching by task type first sends a vague library request into API-design sections when its real blocker is that success is not yet measurable, the router's Spec Mode row exists precisely to intercept that case. Bounded alternatives and recoveries: the standalone merge's default combinations table, which pairs modes rather than sequencing files, the right ranking when working from the single-file shape.

**Counterexample, gotchas, and operational comparison.** Two modes share a reading list, Reliability Mode and Review Mode route identically, the distinction is posture, building versus critiquing, not materials, so mode classification changes what the reader does with the same sections. Good: routing a cancellation bug straight to Reliability Mode's list. Bad: reading all three siblings end to end for a one-file CLI fix. Borderline: a vague async feature, both Spec and Delivery match, the merge's default combinations say run both.

**Verification, evidence, and uncertainty.** Sample recent routed tasks and check the mode call preceded the section selection. The router's structure and the merge's mode-combination table read in full. Misclassification rates for mode-first dispatch are unmeasured.

**Second-order consequence.** Every task-type reading order ends at the same file, the conventions and gates reference is the terminal read in all six orders, so the router encodes a fixed invariant, whatever path a task takes, it exits through the closing gate, routing variety converges on gate uniformity.

**Revision decision.** Present the mode-first hierarchy as the operative ranking and keep the scored seed rows as corpus-process emphasis.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `rust_executable_reference_maps` | 95 | default_adoption_pattern_tier | Source Map First: Load local rust executable material before synthesizing reference maps recommendations. |
| `rust_executable_reference_maps` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `rust_executable_reference_maps` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide whether a given consumption of bundle doctrine should route, search, or take the standalone merge. The seed generic corpus formulas stand where the bundle's implicit thesis is route-then-load economy, the router's first line says use this file first to route the request before loading heavier references, spending a 91-line read to avoid multi-hundred-line reads, navigation as a budgeting discipline.

**Recommended default and causal basis.** Enter through the router in-bundle, carry the merge cross-context, and never consume the heavy siblings without a routing decision first. Reference doctrine grew past what any single consultation can absorb, so the bundle split navigation from content and made the cheap artifact mandatory reading, the router pays for itself the first time it prevents one full read of an unneeded sibling.

**Operating conditions and assumptions.** The consumer honors reading orders, a router only budgets attention for readers who follow its dispatch. The thesis governs how this bundle's material gets consumed, what the material says is the business of the routed themes.

**Failure boundary and alternatives.** Skipping the router restores the problem it solved, agents load whole references, attention dilutes across irrelevant sections, and the relevant boundary guidance competes with hundreds of unrelated lines. Bounded alternatives and recoveries: search-driven consultation with ripgrep over all siblings, which the router itself endorses in its heading-search section as the complement for known-item lookups.

**Counterexample, gotchas, and operational comparison.** The merge silently drops the playbook's process depth, its requirement template and TDD loop are compressions, so route-then-load and the merge answer different fidelity needs, the thesis is economy, not equivalence. Good: a five-minute task answered from the merge's verification matrix alone. Bad: pasting three full siblings into a context window for a naming question. Borderline: ripgrep straight to a heading, endorsed for known targets, blind to the reading-order context around the hit.

**Verification, evidence, and uncertainty.** Check consultations for a visible routing step before any heavy reference load. The router's charter line and the merge's standalone charter read in full. The actual attention savings of routed versus unrouted consultation lack measurement.

**Second-order consequence.** The bundle solved portability twice from opposite directions, the router assumes reachable siblings and minimizes what loads, the merge assumes nothing reachable and maximizes what one file carries, most reference systems pick one answer, this bundle shipped the pair.

**Revision decision.** State the route-then-load thesis explicitly and pair it with the packaging corollary, when routing cannot work, ship the standalone merge instead of shipping nothing.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `rust_executable_reference_maps` contains 3 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Rust Executable Reference Maps comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide which of the two distinct documents answers the current question and at which internal entry point. The seed map rows list heading signals but not the two documents' division of labor, the router holds dispatch tables and reading orders with zero doctrine, while the merge holds doctrine in its most compressed usable forms, templates, scaffolds, matrices, and lists, each useless for the other's job.

**Recommended default and causal basis.** Enter the router by mode or task type and enter the merge at the workflow stage the current question occupies. The bundle separated concerns cleanly, so a reader who wants which file do I read consults only the router, and a reader who wants what does the doctrine say with no bundle access consults only the merge, cross-consultation buys nothing.

**Operating conditions and assumptions.** The reader knows which document they hold, the identical titles of the merge and its duplicate invite confusion with the bundle's own reference files. Navigation of the two distinct mapped documents, the routed siblings' internals belong to their own themes.

**Failure boundary and alternatives.** Seeking doctrine in the router finds file names, seeking navigation in the merge finds section numbers for a bundle the reader may not hold, both misreads waste the visit. Bounded alternatives and recoveries: the router's ripgrep commands for direct heading search across all bundle files, the fastest path for known-item lookups.

**Counterexample, gotchas, and operational comparison.** The seed's heading signals for both merge copies stop at task-type headings, omitting the requirement template, scaffold, matrix, loop, stack, gates, anti-patterns, and output contract, the eight sections that carry the file's actual doctrine. Good: pulling the requirement template from merge section 3 for a spec task. Bad: searching the router for error-design guidance it never contains. Borderline: using the merge's task-type priorities as a routing surrogate, workable solo, the router's read orders carry more sequencing detail.

**Verification, evidence, and uncertainty.** Check that recent consultations entered the document matching their question class. The full structure of both distinct documents read this session. How often readers confuse the merge with the bundle's sibling references is unrecorded.

**Second-order consequence.** The merge's section order retells the bundle's workflow, it runs from classification through requirements, design, verification, and gates to the output contract, so reading it linearly walks the same path the router dispatches across files, the single file preserves the process shape, not just the content.

**Revision decision.** Annotate the rows with the division of labor and the merge's internal order, modes first, task priorities second, then templates, scaffold, matrix, loop, stack, gates, anti-patterns, and the output contract last.

**Retained seed evidence.** All three local source rows with titles, heading signals, and usage roles remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202604/rust-executable-specs-01/references/reference-map.md | Reference Map | Reference Map; 1) Choose the Work Mode; 2) Choose by Task Type; Library or Public API; CLI or Binary; Async Service or Backend | reference detail file for deeper pattern evidence |
| agents-used-monthly-archive/idiomatic-references-202604/rust-executable-specs-01/rust-executable-specs-reference.md | Rust Executable Specs Reference | Rust Executable Specs Reference; 1) Work Modes; 2) Choose by Task Type; Library or Public API; CLI or Binary; Async Service or Backend | historical idiomatic pattern corpus |
| unclassified-yet/Rust Executable Specifications Reference.md | Rust Executable Specs Reference | Rust Executable Specs Reference; 1) Work Modes; 2) Choose by Task Type; Library or Public API; CLI or Binary; Async Service or Backend | local agent-usable source material |

## External Research Source Map

**Decision supported.** This section helps decide which external targets deserve retrieval effort when this theme's claims need freshness. The seed four inherited framework anchors stand where this theme's real external surface is nearly empty, routing patterns and packaging decisions cite no external authority, and the only externally-sensitive content the merge carries is its embedded command gates and named verification tools, which age with the toolchain, not with any listed URL.

**Recommended default and causal basis.** Defer command-currency questions to the gates theme's dry-run probe and treat this theme's own content as toolchain-insensitive. The theme's subject is corpus-internal architecture, how one bundle organizes its own consumption, so external freshness pressure lands on the embedded compressions, the cargo command block and the tool names in the verification matrix, loom, Miri, llvm-cov, rather than on framework documentation.

**Operating conditions and assumptions.** The gates theme's refresh actually propagates to embedded copies, nothing currently enforces that. External rows serve future freshness verification only, all four remained unretrieved throughout this evolution.

**Failure boundary and alternatives.** A refresh effort spent fetching the axum rows would leave the merge's command block, the one externally-datable content it embeds, exactly as unverified as before. Bounded alternatives and recoveries: reading a current workspace's CI configuration to see which named tools remain in active use, faster signal than release-note surveys.

**Counterexample, gotchas, and operational comparison.** The verification matrix names third-party tools without versions, loom, Miri, cargo-llvm-cov, so its rows age at those tools' release tempos even though the matrix's selection logic, which risk deserves which verification, is nearly timeless. Good: noting the command block's currency rides on the gates theme's dated dry run. Bad: fetching tokio's repository to freshen a routing table. Borderline: an api-guidelines fetch for the merge's API-design priorities, adjacent to the routed content, not to the routing.

**Verification, evidence, and uncertainty.** Confirm externally-flavored claims name a dated retrieval or carry the unretrieved-candidate label. Zero fetches this assignment, the embedded command block and tool names read in full. Drift between the merge's embedded copies and their source themes since 202604 is unchecked.

**Second-order consequence.** The merge duplicating the gates theme's command block creates a cross-theme consistency obligation, if a toolchain probe ever updates one copy, the other goes stale silently, so the freshness dependency here is really on the sibling theme's refresh discipline, an internal edge wearing external clothing.

**Revision decision.** Keep the rows as inherited candidates, flag their weak fit, and route real freshness needs to the same toolchain probes the gates theme owns, since the merge transcribes that theme's command block verbatim.

**Retained seed evidence.** All four external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://github.com/rust-lang/api-guidelines | Rust API Guidelines | Rust library-team API design recommendations | external_research_sourced_fact |
| https://github.com/tokio-rs/tokio | Tokio repository | async runtime implementation and operational model | external_research_sourced_fact |
| https://github.com/tokio-rs/axum | Axum repository | Rust web framework implementation and design claims | external_research_sourced_fact |
| https://docs.rs/axum/latest/axum/ | Axum docs.rs | published crate documentation for routing and extractors | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which rejection vocabulary a reviewer armed only with this theme's material can enforce. The seed three corpus-process rows stand where the merge ships an eleven-item rejection list spanning the whole delivery surface, vague requirements, container-reference APIs, application-grade errors from libraries, unjustified unwraps, executor blocking, locks across await, unbounded channels, UTF-8 assumptions, detached tasks, unverified concurrency, and layer leakage.

**Recommended default and causal basis.** Sweep the eleven rejections at review when working from the merge and route to the owning theme when a rejection needs its repair doctrine. The merge compressed the reliability theme's twelve rejections and the bundle's process rejections into one list, making it the bundle's densest single artifact, eleven shippable failure classes in eleven lines.

**Operating conditions and assumptions.** Reviewers hold either the merge or routed access, the vocabulary works from both shapes. Work-product rejections carried by this theme's merge, the full clause detail behind each rejection lives with the reliability and gates themes.

**Failure boundary and alternatives.** Each rejection names something that passes compilation and often passes tests, the list exists because no command gate sees any of them, only review armed with the vocabulary does. Bounded alternatives and recoveries: encoding the mechanically-detectable subset as lints, container references and unwraps grep cleanly, most of the rest stays judgment.

**Counterexample, gotchas, and operational comparison.** The merge's list drops the reliability theme's diagnostics-location rejection while adding the layering one, the two lists overlap at ten items but neither contains the other, quoting either as the complete registry misses a row. Good: blocking a detached-task PR by name from the merge's list. Bad: approving an unbounded channel because the command gates passed. Borderline: a justified unwrap with its invariant documented, the list's own wording admits it, the invariant story is the gate.

**Verification, evidence, and uncertainty.** Check review records cite rejections by name and route repair questions to owning themes. The merge's eleven-item list read in full against the reliability theme's twelve. Whether the two lists' divergence is editorial drift or deliberate re-scoping is unrecorded.

**Second-order consequence.** The list's compression has a cost worth naming, the reliability theme pairs each rejection with its repair clauses while the merge names offenses bare, so the merge suffices to block a merge but not to guide the fix, which is the exact fidelity boundary between the standalone shape and the routed shape.

**Revision decision.** Import the eleven rejections as the merge's review vocabulary and add this theme's own routing anti-patterns, sibling-blind routing, carrying the router without its bundle, and duplicate-counting filing copies as corroboration.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide which verification classes a task owes and what command floor closes it. The seed document verifier stands where the merge carries two verification layers of its own, the transcribed four-plus-two cargo command battery, and above it the ten-row verification selection matrix that decides which verification classes a task needs before any command runs.

**Recommended default and causal basis.** Walk the matrix at task start, record which rows trigger, and require the triggered evidence plus the four-command floor at close. The matrix is the merge's real contribution, commands verify what was built, but the matrix chooses what evidence the task owes, unit for local determinism, doctest for contract examples, compile-fail for misuse, property for invariants, fuzz for input frontiers, loom for interleavings, Miri for unsafe, performance for stated thresholds, coverage for risk.

**Operating conditions and assumptions.** Risk classification is honest, a matrix walked after implementation rationalizes whatever evidence already exists. Verification selection and gating as this theme's merge carries them, command currency belongs to the gates theme, suite construction to the testing themes.

**Failure boundary and alternatives.** Running the command battery without the matrix decision produces green badges over unowed evidence, a parser fuzzed never, a concurrency change explored never, all four commands passing throughout. Bounded alternatives and recoveries: the reliability theme's richer gate stacks for boundary-heavy work, the matrix is the selection layer, not the ceiling.

**Counterexample, gotchas, and operational comparison.** The matrix's performance row triggers only when thresholds were explicitly stated, which quietly imports the gates theme's claim rule, no stated threshold means no performance verification owed and also no performance claim allowed. Good: a protocol change with matrix-triggered fuzz and round-trip evidence plus four green commands. Bad: a concurrency merge whose only evidence is the command battery. Borderline: skipping loom on shared state behind a proven-single-threaded boundary, the matrix's own trigger wording allows it when the model genuinely has no interleavings.

**Verification, evidence, and uncertainty.** Audit closes for a recorded matrix walk with evidence per triggered row. The merge's matrix and command block read in full. How teams record matrix walks in practice is undesigned here.

**Second-order consequence.** The matrix rows map risks to tools one-to-one, which makes verification selection auditable, a reviewer can read a change's risk profile and check each triggered row has its evidence, turning what-testing-is-enough from taste into a table lookup.

**Revision decision.** Pair the two layers explicitly, choose verification classes from the matrix at planning, then run the command battery as the floor at closing, and route document verification to the seed's own verifier.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide what response structure an agent producing substantial Rust work under this bundle must return. The seed four generic bullets stand where the merge ends with an explicit agent contract, a seven-part output order for substantial Rust responses, work mode first, executable requirements, design with layers and traits, verification matrix, implementation plan, gate results, open questions, plus a traceability table schema linking requirements to tests.

**Recommended default and causal basis.** Require the seven-part order on substantial Rust responses and accept trimmed forms only for genuinely small tasks. The output contract converts the bundle's whole process into a response format, an agent that fills the seven parts in order has necessarily classified, specified, designed, selected verification, planned, and gated, the format is the process check.

**Operating conditions and assumptions.** The task is substantial, forcing the full contract onto a typo fix produces ceremony, the merge's own wording scopes it to substantial responses. Agent behavior over this theme's material, the routed themes' own agent guidance governs their depth.

**Failure boundary and alternatives.** Agents without the contract produce code-first answers where requirements and verification appear retroactively if at all, the exact inversion the bundle's spec-first doctrine exists to prevent. Bounded alternatives and recoveries: post-hoc human review against the review questions without the response format, workable, it moves the process check from generation time to review time.

**Counterexample, gotchas, and operational comparison.** The traceability table is the contract's most skipped part because it demands real test identifiers, agents fabricating placeholder test names to fill the schema convert the strongest part into decoration, empty-but-honest beats filled-but-fake. Good: a feature response opening with mode and requirements before any code. Bad: a code dump with a retrofitted requirements section. Borderline: omitting open questions on a fully specified task, acceptable when stated as none rather than silently dropped.

**Verification, evidence, and uncertainty.** Sample substantial agent responses for the seven parts in order with resolvable test identifiers. The merge's output contract and review questions read in full. How strictly the seven-part order improves outcomes versus its ceremony cost is unmeasured.

**Second-order consequence.** The merge's closing review questions are the output contract inverted into audit form, which modes, which minimal requirement set, which invariants in L1, which risks deserve which verification, which names must stay stable, an agent can self-review by answering five questions against its own response.

**Revision decision.** Inject the router as the agent's dispatch step, the output contract as its response skeleton for substantial work, and the review questions as its self-check before returning.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide which entry artifact and reading pattern fits the current consumer's context. The seed one generic engineer stands where the two shapes serve distinct travelers, an in-bundle agent who reads the router, classifies mode, and loads routed sections, a portable-context consumer who receives only the merge and works its sections linearly, and a corpus maintainer who must know the third copy is a filing duplicate.

**Recommended default and causal basis.** Identify which traveler the current reader is before recommending an entry artifact. Each traveler holds different files and different questions, so journey design here is shape-matching, giving the router to the portable consumer or the bare merge to the in-bundle agent misfits both.

**Operating conditions and assumptions.** The reader's context is known, ambiguous cases default to the merge, which degrades more gracefully than a router without siblings. Journeys through this theme's routing artifacts, journeys through the routed doctrine belong to the owning themes.

**Failure boundary and alternatives.** The portable consumer given the router hits dead file references, the in-bundle agent given only the merge loses the playbook depth the router would have surfaced, both failures look like bad doctrine when they are bad packaging. Bounded alternatives and recoveries: a single all-travelers reading of the merge, simplest to teach, it forfeits the routing economy for the one traveler who had it available.

**Counterexample, gotchas, and operational comparison.** The in-bundle agent's journey has a hidden first step, verifying the sibling paths still resolve, a bundle partially copied into a new repository keeps the router's confidence while breaking its dispatch. Good: a portable agent working the merge linearly from modes to output contract. Bad: a chat context receiving the router and hallucinating the siblings it references. Borderline: an in-bundle reader starting from the merge for speed, workable, the router's task-type orders carry sequencing the merge compresses away.

**Verification, evidence, and uncertainty.** Ask recent consumers which artifact they held and whether its references resolved. The two shapes' charters and the duplicate finding read in full. The real population split across the three traveler types is unrecorded.

**Second-order consequence.** The maintainer journey is the one the seed could not anticipate, whoever classifies the unclassified-yet folder will face the duplicate and needs this theme's diff finding to avoid either double-filing one document or deleting what looks like a stray but is the canonical merge's twin.

**Revision decision.** Recast the scenario as the three travelers with their entry artifacts, router for in-bundle work, merge for portable contexts, and the deduplication note for corpus maintenance.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The Rust reliability engineer is starting from a requirement that needs a safe API, explicit error model, and testable boundary and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `rust_executable_reference_maps` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing the idiomatic ownership, async, error, and crate-boundary shape before coding.
Reference opening trigger: Open this file when the task mentions rust executable reference maps, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide how recurring packaging arguments between routing, inlining, and copying get settled. The seed template rows skip the theme's live tensions, routing economy versus standalone fidelity, where the router saves attention but the merge survives detachment, compression versus repair-depth, where the merge's lists block bad work but cannot guide fixes, and duplication versus canonicalization, where the filing copy aids circulation but corrodes source counting.

**Recommended default and causal basis.** Resolve each axis by testing the consumer's resolution capability rather than by fixed preference. Each tension is a packaging decision with a measurable cost on each side, attention spent, fidelity lost, or provenance confused, so resolutions are context rules rather than universal picks.

**Operating conditions and assumptions.** The consumer's context is assessable, when unknown, assume minimal reach and ship the self-sufficient shape. Tuning this theme's packaging choices, doctrinal tradeoffs inside the routed content belong to the owning themes.

**Failure boundary and alternatives.** Each tension has a seductive wrong pole, always-route breaks in portable contexts, always-inline bloats every consultation, and tolerated duplicates eventually diverge into two silently different doctrines. Bounded alternatives and recoveries: generated packaging, building portable merges from routed sources on demand, which keeps one canonical doctrine and derives shapes, at tooling cost no evidence shows the bundle paid.

**Counterexample, gotchas, and operational comparison.** The compression axis hides a ratchet, content compressed out of the merge is invisible to merge-only consumers, who cannot know what depth they are missing, so the merge should advertise what it dropped, which it currently does not. Good: shipping the merge to a sibling-less code review context. Bad: mandating router-first reading for consumers who cannot open the routed files. Borderline: keeping the unclassified duplicate during a migration window, tolerable exactly as long as the canonical citation rule holds.

**Verification, evidence, and uncertainty.** Audit recent packaging choices for a recorded context-resolution test. The two shapes' complementary designs and the duplicate finding read in full. How often the copies have already diverged elsewhere in circulation is unknowable from this corpus.

**Second-order consequence.** All three axes reduce to one question, what can the consumer's context resolve, siblings resolvable means route, repair doctrine reachable means compress, canonical path known means copies are harmless, packaging decisions are really assumptions about the reader's reach.

**Revision decision.** Add the three axes with context rules, route in-bundle and inline cross-context, compress for review and route for repair, circulate copies freely but count and cite only the canonical.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the rust executable reference maps pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong rust executable reference maps guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which document settles a question given what the asker can currently reach. The seed file-position roles miss the actual precedence structure, the router is canonical for dispatch and reading order, the merge is canonical for nothing, every doctrine section it carries is a compression of some sibling or bundle theme, and the duplicate copy carries no authority at all beyond confirming the merge circulated.

**Recommended default and causal basis.** Resolve navigation questions from the router, doctrine questions from the deepest reachable source, and cite the merge as authority only alongside a statement that nothing deeper was reachable. The merge declares itself a merger of the bundle's strongest parts, so by its own charter it is derivative, authoritative only in contexts where its sources are unreachable, a situational authority rather than a structural one.

**Operating conditions and assumptions.** Reachability is honestly assessed, claiming detachment to avoid a longer read abuses the situational rule. Precedence among this theme's three mapped files and their relation to the bundle, cross-corpus precedence belongs to adjacent routing.

**Failure boundary and alternatives.** Settling doctrine disputes from the merge when routed sources are reachable resolves them from compressions, the requirement template stripped of the playbook's process, the anti-pattern list stripped of repair clauses. Bounded alternatives and recoveries: treating the merge as canonical everywhere for simplicity, defensible for teams that only ever hold the merge, and exactly wrong inside this corpus where the sources sit evolved.

**Counterexample, gotchas, and operational comparison.** The merge's compressions have already drifted from their sources in at least one place, its anti-pattern list and the reliability theme's differ by a row in each direction, so merge-based rulings can contradict source-based rulings today, not just hypothetically. Good: settling a reading-order dispute from the router directly. Bad: quoting the merge's gate block over the gates theme's evolved doctrine in-corpus. Borderline: a detached consumer ruling from the merge's reliability stack, correct for their context, flagged for re-check on reattachment.

**Verification, evidence, and uncertainty.** Test hierarchy claims against each document's self-declared charter and the reachability of its sources. The merge's own merger charter and the router's routing charter read in full. Unread routed siblings may claim authority structures this analysis cannot see.

**Second-order consequence.** Situational authority is this hierarchy's exportable idea, a document can be the right authority in one context and the wrong one in another without any edit, the merge's authority is a function of what else the reader can reach, not of its content.

**Revision decision.** Record the working hierarchy, router canonical for navigation, routed siblings and their evolved themes canonical for doctrine, merge authoritative only when detached, duplicate authoritative never.

**Retained seed evidence.** The hierarchy rows and the confidence warning remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/codex-skills-202604/rust-executable-specs-01/references/reference-map.md | canonical primary source | Reference Map; 1) Choose the Work Mode; 2) Choose by Task Type | What guidance, warning, or example should this source contribute to Rust Executable Reference Maps? |
| agents-used-monthly-archive/idiomatic-references-202604/rust-executable-specs-01/rust-executable-specs-reference.md | legacy historical source | Rust Executable Specs Reference; 1) Work Modes; 2) Choose by Task Type | What guidance, warning, or example should this source contribute to Rust Executable Reference Maps? |
| unclassified-yet/Rust Executable Specifications Reference.md | supporting context source | Rust Executable Specs Reference; 1) Work Modes; 2) Choose by Task Type | What guidance, warning, or example should this source contribute to Rust Executable Reference Maps? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing evidence lets a receiving context trust and trace the bundle material it holds. The seed generic worked-example artifact misses this theme's natural object, a routing manifest for any context that receives bundle material, one block recording which shape was shipped, router or merge, which sibling paths were verified resolvable at shipping time, which canonical copy the merge was taken from, and the date of the reachability check.

**Recommended default and causal basis.** Generate the manifest when packaging bundle material for any new context and refresh it when the bundle revises. Every failure this theme names, dead sibling references, duplicate-counting, silent compression loss, is a packaging-time fact that becomes invisible after shipping, the manifest preserves exactly the facts consumers cannot reconstruct.

**Operating conditions and assumptions.** Shipping events are discrete enough to attach records to, ambient copying between repositories defeats any manifest discipline. One manifest per shipped bundle context, gate records belong to the gates theme and boundary ledgers to the reliability theme.

**Failure boundary and alternatives.** Without the manifest, a consumer holding a router with dead paths cannot tell a broken copy from a broken bundle, and a consumer holding a divergent merge copy cannot tell edits from corruption. Bounded alternatives and recoveries: trusting file headers and titles, which this theme's own evidence defeats, the merge and its duplicate share both title and bytes while differing in the only fact that matters, location.

**Counterexample, gotchas, and operational comparison.** The manifest must record the canonical path, not just a checksum, because the duplicate problem is precisely that identical bytes exist at two paths, a checksum matches both and identifies neither. Good: a shipped merge carrying its canonical path and check date. Bad: a router copied into a new repo with no path verification note. Borderline: a manifest without a check date, better than nothing, staleness becomes unjudgeable.

**Verification, evidence, and uncertainty.** Sample shipped bundle contexts for manifests whose recorded paths still resolve. The duplicate finding and dead-path failure mode reasoned from documents read in full. Manifest upkeep across informal copying practices is the untested variable.

**Second-order consequence.** The manifest generalizes the deduplication finding into standing practice, this assignment's diff run was a one-off manifest entry performed late, at consumption time, the artifact moves that verification to shipping time where it is cheap and certain.

**Revision decision.** Define the artifact as the routing manifest with four fields, shape shipped, paths verified, canonical source, check date, attached wherever bundle material travels.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: reference freshness workflow with canonical source policy and update checklist.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Rust Executable Reference Maps | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercise most efficiently builds correct shape-selection and path-verification habits. The seed abstract usage lines stand where this theme wants a routing rehearsal, take one concrete request, an async service with a vague success criterion, walk the router, mode classification catches Spec plus Delivery, the task-type order sequences playbook then reliability sections then gates, and contrast the same request answered from the merge alone.

**Recommended default and causal basis.** Run new corpus users through the contrast rehearsal before they package or consume bundle material unsupervised. The contrast run teaches the shapes' tradeoff concretely, the routed path surfaces the playbook's full requirement process while the merge path reaches its compressed template immediately, speed against depth made visible on one request.

**Operating conditions and assumptions.** A realistic practice request exists, trivial requests collapse the mode classification the rehearsal exists to exercise. Teaching this theme's routing decisions, rehearsals of the routed doctrine belong to the owning themes.

**Failure boundary and alternatives.** Engineers who never run the contrast default to whichever shape they met first, router loyalists over-read in portable contexts and merge loyalists under-read in-bundle, both from habit rather than fit. Bounded alternatives and recoveries: learning by shadowing real routed tasks, authentic and slow, the seeded drill compresses the rare failure modes into one session.

**Counterexample, gotchas, and operational comparison.** The rehearsal should include the duplicate encounter, learners finding two identical merge copies tend to invent explanations, versioning, forking, backup, and the filing-artifact truth needs stating before the inventions harden. Good: a trainee catching the seeded dead path at the manifest step. Bad: certifying routing competence from merge-only reading. Borderline: running the rehearsal on a pure review task, gentle, Review Mode's routing is the least differentiated case.

**Verification, evidence, and uncertainty.** Have the trainee package bundle material for a new context and audit the shipped manifest. The router's dispatch structure and the merge's compression read in full. Habit persistence without recurring packaging events is unmeasured.

**Second-order consequence.** The broken-path drill teaches the theme's least intuitive lesson, navigation artifacts fail silently, a router with a dead reference reads as confidently as a healthy one, and only the resolution check distinguishes them, confidence in a map says nothing about the territory.

**Revision decision.** Anchor the section on the contrast rehearsal plus one packaging drill, hand the learner a router with one deliberately broken sibling path and require the manifest check to catch it before any reading starts.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Rust Executable Reference Maps after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Rust Executable Reference Maps as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Rust Executable Reference Maps only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which measurements prove this theme's routing and packaging guidance pays for itself. The seed compile-centric indicator misses what routing discipline should move, routed-read efficiency, sections loaded per task against sections the router prescribed, dead-reference incidents caught at packaging versus discovered at consumption, and duplicate-citation errors, filings cited as corroborating sources in downstream documents.

**Recommended default and causal basis.** Tag the three event streams as they occur and review the metrics at each corpus refresh beside the refresh probes. Each metric tests one theme mechanism in operation, read efficiency tests whether routing actually budgets attention, incident location tests whether manifests run at shipping time, and citation errors test whether canonicalization holds under real use.

**Operating conditions and assumptions.** Consultation logging exists in some form, without it read efficiency stays anecdotal. Measuring this theme's routing and packaging discipline, doctrine-quality outcomes belong to the routed themes.

**Failure boundary and alternatives.** Unmeasured routing decays invisibly, agents drift to loading whole siblings, packaged copies ship unverified, and the first symptom is a downstream document confidently citing two identical files as agreeing evidence. Bounded alternatives and recoveries: counting corpus consultations without routing detail, already available and information-free about the mechanism this theme owns.

**Counterexample, gotchas, and operational comparison.** Read efficiency can be gamed by under-reading, an agent loading less than the router prescribes scores well while missing prescribed material, the metric needs the prescription as its denominator, not just the load count. Good: a quarter showing dead references caught only at packaging, never at consumption. Bad: defending the router with no load-efficiency data when its ceremony is challenged. Borderline: counting a deliberate full-bundle read for corpus evolution as inefficiency, evolution work legitimately reads everything, the metric should exclude it.

**Verification, evidence, and uncertainty.** Confirm the three metrics exist, cite real event streams, and were consumed at the last refresh review. The alignment between this theme's mechanisms and their observable failure signatures. Healthy baselines for all three metrics await first measurement.

**Second-order consequence.** Read efficiency is the metric that can invert the thesis, if routed tasks consistently load nearly everything anyway, the router's economy claim fails empirically and the bundle should consider the monolithic shape, the metric does not just monitor the design, it retests it.

**Revision decision.** Add the three metrics with collection points, consultation logs for read efficiency, manifest audits for incident location, and citation sampling in downstream references for duplicate errors.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: compile attempts and review comments decrease because the API shape is explicit before implementation.
Failure signal: the reference hides ownership or error tradeoffs behind generic guidance.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this routing reference is declared faithful. The seed shape items never audit fidelity to the mapped documents' countable content, the router's four modes, six task-type orders, four-step default order, and three search commands, the merge's twelve sections, four modes with four default combinations, six task types, ten matrix rows, five loop phases, seven stack steps, six command lines, eleven anti-patterns, seven output parts, and five review questions.

**Recommended default and causal basis.** Run the count-and-diff audit at every reread cycle and after any archive or unclassified-folder change. This theme's claims transcribe two enumerable documents plus one diff finding, so completeness reduces to counting the enumerables and re-verifying the duplicate identity, the one claim that can silently expire.

**Operating conditions and assumptions.** Both copies remain at their recorded paths, a moved or classified copy converts the diff item into a provenance investigation. Auditing this reference document against its sources, auditing routing practice belongs to the metrics loop.

**Failure boundary and alternatives.** The diff claim is the audit's perishable item, both copies remain editable, and every restatement of their identity inherits this session's date, an audit that skips the re-diff certifies a possibly stale equivalence. Bounded alternatives and recoveries: checksumming both copies and comparing digests, equivalent to the diff and cheaper, provided the digests are recomputed rather than recorded once.

**Counterexample, gotchas, and operational comparison.** The merge's counts are easy to misread from its section numbering, the anti-pattern list numbers eleven items under one heading while the matrix's ten rows sit in one table, counting headings instead of items undercounts both. Good: an audit re-running the diff and finding continued identity. Bad: declaring fidelity because all 26 headings exist. Borderline: accepting reworded mode descriptions, tolerable, the four-mode count and each mode's dispatch target must survive.

**Verification, evidence, and uncertainty.** Count the enumerables in both documents and re-diff the two merge copies, dating the result. The fully enumerable structure of both distinct documents read this session. Whether the unclassified copy will move before the next audit is unknown.

**Second-order consequence.** This checklist is the only one in the rust cluster with a volatile local claim, other themes' local facts sit in one archive file each, this theme asserts a relation between two files, and relations decay when either side moves, the audit must treat the diff as a measurement, not a fact.

**Revision decision.** Add the count items for both documents and a mandatory re-diff of the two merge copies each audit cycle, with the result dated.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Rust Executable Reference Maps.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a routing-flavored question this theme does not own. The seed token-split rows point nowhere while this theme's neighbors are the routed content's own evolved themes, the gates theme for the command block the merge transcribes, the reliability theme for the anti-patterns and stack it compresses, the backend themes for the async-service priorities it lists, and the spec-oriented themes for the requirement template and TDD loop.

**Recommended default and causal basis.** Answer packaging questions here and forward content questions with the owning theme named. A routing theme's questions are almost all about routed content wearing routing vocabulary, so this section's job is unusually pure, nearly everything substantive forwards, and only shape-selection, packaging, and provenance questions stay.

**Operating conditions and assumptions.** Destination themes exist under their expected corpus names. Sending away what this theme cannot own, destination summaries are deliberately absent.

**Failure boundary and alternatives.** Answering doctrine questions from this theme's compressed carriers gives the merge's bare lists where the owning themes hold clauses, repairs, and boundaries, shallow authority again, one level removed. Bounded alternatives and recoveries: for work-mode methodology questions no local theme covers, the bundle's unread playbook remains the likely source, flagged as unread rather than cited.

**Counterexample, gotchas, and operational comparison.** Questions about the merge's fidelity, does its gate block match the gates theme, are genuinely this theme's to answer, they concern the compression relation, not the doctrine, forwarding them loses the one comparison this theme is positioned to make. Good: forwarding an error-design question to the reliability theme while keeping the merge-fidelity comparison here. Bad: answering a cancellation question from the merge's compressed stack. Borderline: a verification-matrix interpretation question, the matrix lives in the merge, its testing depth lives with the testing themes, split the answer accordingly.

**Verification, evidence, and uncertainty.** Sample recent kept and forwarded questions against the packaging-versus-content test. The compression relations mapped from full reads of both documents. Misroute frequency into this theme lacks measurement.

**Second-order consequence.** This theme is itself a router about routers, so its routing rule is self-similar, just as the mapped router sends readers to siblings for depth, this reference sends corpus users to evolved themes for depth, the theme practices the pattern it documents.

**Revision decision.** Route by content class, keep shape, packaging, manifest, and duplicate questions here, forward every doctrine question to the theme that owns its depth.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use backend, executable, or quality-gate Rust references when the question shifts to HTTP delivery, specs, or review gates.
Adjacent reference 1: consider the rust adjacent reference when the current task pivots away from rust executable reference maps.
Adjacent reference 2: consider the executable adjacent reference when the current task pivots away from rust executable reference maps.
Adjacent reference 3: consider the reference adjacent reference when the current task pivots away from rust executable reference maps.

## Workload Model

**Decision supported.** This section helps decide how to budget routing effort so the cheap fixed read keeps preventing expensive unrouted loads. The seed symbol-count boundary misprices this theme's work, whose units are per-consultation routing cost, the fixed small read of the router before any task, per-packaging cost when bundle material ships to a new context with its manifest, and per-audit cost for the count-and-diff cycle including the duplicate re-check.

**Recommended default and causal basis.** Treat the routing read as non-negotiable per consultation, batch packaging events where possible, and fold the audit into the corpus reread cadence. Routing cost is tiny and constant while its savings scale with the size of the references not loaded, packaging cost is rare and bursty, and audit cost is periodic, so the theme's economics are dominated by how often heavy references would otherwise load unrouted.

**Operating conditions and assumptions.** Consultations target the bundle, tasks that never touch bundle doctrine owe no routing read. Budgeting routing, packaging, and audit work for this theme, the routed content's implementation workload belongs to its own themes.

**Failure boundary and alternatives.** Pricing this work per symbol makes the router look like pure overhead, ninety-one lines read before every task, when its value is precisely the hundreds of lines per task it prevents, symbol accounting inverts the theme's value. Bounded alternatives and recoveries: the seed's crate-slice and symbol boundaries, retained as outer limits on any single consultation's scope.

**Counterexample, gotchas, and operational comparison.** Packaging cost hides a long tail, the manifest is cheap to write and expensive to honor, refreshing shipped manifests when the bundle revises is the recurring cost teams forget to budget. Good: a task log showing the router read followed by two targeted section loads. Bad: a consultation skipping the router to save five minutes and reading three full siblings. Borderline: memorized routing for a developer who knows the dispatch table cold, legitimate until the router revises under them.

**Verification, evidence, and uncertainty.** Compare loaded-section counts between routed and unrouted consultations of similar tasks. The size asymmetry between the router and its routed targets read in full. Typical consultation frequency against the bundle is unrecorded.

**Second-order consequence.** The routing read amortizes within a single task, unlike most fixed costs that amortize across tasks, one avoided sibling load repays the router read severalfold immediately, which is why route-first survives even under deadline pressure that strips other disciplines.

**Revision decision.** Restate the model in three units, fixed routing read per consultation, manifest cost per shipping event, and count-and-diff cost per audit cycle.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Rust Executable Reference Maps as a rust system operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | systems implementation, CLI or service hardening, async/runtime review, and safety-sensitive refactoring where compile success is necessary but not sufficient | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one crate or bounded workspace slice, up to 100 changed symbols, and one integration path that exercises error handling | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Reference Map; 1) Choose the Work Mode; 2) Choose by Task Type; Library or Public API; CLI or Binary; Async Service or Backend; Rust Executable Specs  | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is reference freshness workflow with canonical source policy and update checklist | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which routing invariants must demonstrably hold for this theme's guidance to count as adopted. The seed document-property thresholds stand where routing invariants should, every bundle consultation preceded by a routing decision, every shipped bundle context carrying a current manifest, every corpus citation of the merge naming the canonical path, and every audit cycle re-verifying the duplicate identity with a dated result.

**Recommended default and causal basis.** Hold all four targets through the corpus audit cadence with the re-diff result dated and age-bounded. Each target operationalizes one theme mechanism, the consultation target holds the route-first thesis, the manifest target holds packaging discipline, the citation target holds canonicalization, and the re-diff target keeps the theme's one volatile claim measured.

**Operating conditions and assumptions.** Audit cycles actually run on cadence, a skipped cycle silently doubles the re-diff window. Routing and packaging invariants for this theme, doctrine-quality targets belong to the routed themes.

**Failure boundary and alternatives.** Informally held, these targets decay in the usual order, routing lapses first under deadline, manifests stop refreshing on bundle revisions, and the citation rule erodes as downstream documents copy each other's references. Bounded alternatives and recoveries: trust-based routing with retrospective-only enforcement, cheaper and blind until a dead reference or divergent copy surfaces in someone else's failure.

**Counterexample, gotchas, and operational comparison.** The citation target is enforced downstream, in documents this theme does not control, so its verification is sampling other references for how they cite this theme's sources, an outward-facing audit unusual for reliability targets. Good: an audit record with a dated identical re-diff result. Bad: declaring routing adopted from the absence of complaints. Borderline: a manifest one bundle-revision old, failing the currency letter, acceptable exactly when the revision touched no shipped paths.

**Verification, evidence, and uncertainty.** Review the four target audits at each corpus checkpoint with dated evidence attached. The mechanism structure of this theme's routing and packaging content. Sampling rates that catch citation drift early are uncalibrated.

**Second-order consequence.** The re-diff target is the only self-expiring one, the others decay through neglect while this one decays through time alone, every day since the last diff widens the window in which either copy could have changed, so its evidence method needs a maximum age, not just a presence check.

**Revision decision.** Publish the four targets with evidence methods, consultation sampling for routing, shipped-context audit for manifests, downstream citation review for canonical paths, and the dated re-diff in each audit record.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which routing-decay modes need standing probes and what each probe checks. The seed generic rows miss how routing artifacts specifically decay, dispatch rot where the router's tables drift from revised sibling structures until routed sections no longer exist under their listed names, compression drift where the merge's transcribed blocks diverge from their evolving sources, copy divergence where the duplicate stops being identical without anyone re-checking, and route abandonment where consumers quietly stop reading the router at all.

**Recommended default and causal basis.** Run the four probes on the corpus audit cadence, with the rot grep automated if any probe is. All four decay modes share one driver, the routing layer and its targets evolve on independent schedules with no mechanism binding them, every sibling revision, theme evolution, or copy edit moves one side of a relation the router or merge froze.

**Operating conditions and assumptions.** Probes are owned, an unowned probe list is this table's abandonment row applied to itself. Decay of this theme's routing and packaging artifacts, doctrine decay belongs to the routed themes' own failure tables.

**Failure boundary and alternatives.** The modes compound quietly, a rotted dispatch table pushes readers toward the merge, whose drifted compressions then serve stale doctrine confidently, abandonment following rot following drift, each stage hiding the previous. Bounded alternatives and recoveries: periodic full re-derivation of the router and merge from current sources, the reset option that sustained rot and drift eventually justify.

**Counterexample, gotchas, and operational comparison.** Compression drift is already real, not hypothetical, the merge's anti-pattern list and the reliability theme's diverge by a row in each direction today, so the drift probe's baseline must record known divergences or every audit rediscovers them as new findings. Good: a rot grep catching a renamed reliability section the router still lists. Bad: trusting the duplicate's identity from this session's diff two audit cycles later. Borderline: a consumer skipping the router on a memorized dispatch, abandonment by the letter, harmless until the next router revision.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe, owner, and cadence, and sample one probe's recent findings. The relation-freezing structure of both documents and the observed list divergence. Decay onset speed for each mode is unmeasured in this corpus.

**Second-order consequence.** Dispatch rot has a cheap mechanical probe the other modes lack, every routed section name in the router can be grepped against its target file, a script-sized check that converts the most consequential decay mode into a CI-style verification, the router is testable in a way prose never is.

**Revision decision.** Add the four rows with probes, heading-resolution checks of routed section names for rot, block-level comparison against source themes for drift, the dated re-diff for divergence, and consultation sampling for abandonment.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| runtime contention masks failure | async tasks, locks, or shared state degrade under concurrent load | add contention benchmark, timeout budget, and structured cancellation path |
| panic path reaches production | unwrap or unchecked invariant survives compile and unit tests | require panic audit, Result propagation, and integration failure fixture |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide where retry and backpressure behavior gets specified and which artifacts verify it. The seed corpus-process bullets stand beside the merge's own retry-adjacent doctrine, its async priorities demand bounded concurrency, cancellation, and shutdown behavior, its anti-patterns reject unbounded channels and detached tasks, and its requirement rules demand explicit units for retries and concurrency limits, retry policy as a specified contract rather than an improvised loop.

**Recommended default and causal basis.** Specify retry and backpressure behavior in requirement clauses with units, verify per the matrix, and sweep the async anti-patterns at review. The bundle's spec-first stance moves retry decisions upstream, a requirement that states retry counts, backoff, and concurrency ceilings in measurable units converts retry behavior from runtime folklore into testable contract, which is the merge's whole method applied to backpressure.

**Operating conditions and assumptions.** The work passes through the bundle's spec process, retrofitting contracts onto shipped retry loops is repair work owned by the reliability theme. How this theme's carriers frame retry and backpressure contracts, the deep cancel-safety and channel doctrine lives with the reliability theme.

**Failure boundary and alternatives.** Unspecified retry behavior fails the bundle's own vague-requirements rejection, ships without matrix-selected verification, and surfaces as the unbounded-channel and detached-task anti-patterns under load, three named defenses bypassed by one omission. Bounded alternatives and recoveries: runtime-tuned retry configuration outside the requirement contract, flexible in operations and invisible to the traceability table, the bundle's method argues against it.

**Counterexample, gotchas, and operational comparison.** The merge compresses away the reliability theme's cancel-safe select discipline, so a merge-only consumer can specify perfect retry contracts and still implement them on cancel-unsafe primitives, contract correctness does not confer implementation safety. Good: a requirement clause stating maximum three retries with exponential backoff in stated units, linked to a test. Bad: a retry loop added in review with no contract change. Borderline: infrastructure-level retries below the application contract, legitimate layering, the contract should state its assumption about them.

**Verification, evidence, and uncertainty.** Audit retry-bearing requirements for units and linked tests, and their implementations for the swept anti-patterns. The merge's requirement rules, async priorities, and anti-pattern rows read in full. How often teams actually specify retry contracts upstream is unmeasured.

**Second-order consequence.** The requirement template's explicit-units rule quietly makes retry storms a specification defect rather than an operational surprise, when the contract states three retries with stated backoff, a metric showing thirty is a violated requirement with a traceable test, accountability the improvised loop never offers.

**Revision decision.** Add the contract framing, retry counts, backoff shapes, and concurrency ceilings belong in the requirement template's measurable clauses, get verified per the matrix, and get reviewed against the async anti-patterns.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide what the routing layer must record about itself and where emission questions forward. The seed generic records stand beside the merge's own observability stance, its reliability stack makes observability and secret discipline part of the design, its output contract requires gate results and traceability tables as response evidence, and this theme adds its own layer, manifests and audit records as the observability of the routing system itself.

**Recommended default and causal basis.** Emit the routing layer's three record types at their trigger events and route code-emission questions to the owning themes. The theme operates at two levels, the routed doctrine tells code what to emit, while the routing layer's own health is visible only through the records this theme prescribes, manifests, dated diffs, and consultation logs, systems and meta-systems each needing their own telemetry.

**Operating conditions and assumptions.** Record-keeping has a home, records scattered across chat logs and commit messages observably exist but cannot be audited. What this theme's routing layer must record about itself, code-level telemetry design belongs to the reliability and ops themes.

**Failure boundary and alternatives.** The routing layer's failures are silent by nature, a dead reference emits nothing, a divergent copy logs nothing, an abandoned router raises no alert, without prescribed records the layer's first observable symptom is a downstream doctrine error. Bounded alternatives and recoveries: treating corpus commit history as implicit routing records, partially workable for audits, mute about consultations and shipments that never touch the repository.

**Counterexample, gotchas, and operational comparison.** Consultation logging is the record type most likely to be judged not worth it, and its absence is self-concealing, without it route abandonment is invisible until rot is discovered by failure, the cheapest record guards the most silent decay. Good: an audit reconstructing the duplicate's status from dated re-diff records alone. Bad: discovering route abandonment from a stale-doctrine incident. Borderline: manifests kept only for cross-repository shipments, defensible scoping, in-repo copies then rely on the audit cadence alone.

**Verification, evidence, and uncertainty.** Pick a recent audit and confirm both levels' records were locatable from their prescribed homes. The merge's stack and output contract read in full, plus this theme's record reasoning. The sustainable granularity of consultation logging is untested.

**Second-order consequence.** The traceability table in the merge's output contract is observability for reasoning, it records which requirement each test observes just as spans record which request each operation serves, the bundle applies telemetry thinking to its own process artifacts, and this theme extends the same move to routing.

**Revision decision.** Pair the two levels explicitly, forward code-observability questions to the routed doctrine, and hold the routing layer to its own record set, manifest per shipment, dated re-diff per audit, routing decision per consultation where logging exists.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture error count, timeout count, retry count, saturation signal, and rollback trigger.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide which performance statements may exist and when performance verification is owed. The seed latency packet stands beside the merge's two performance rules, the requirement template demands explicit units for latency, memory, and throughput claims, and the verification matrix triggers performance testing only when thresholds were explicitly stated, specification and verification bound into one conditional pair.

**Recommended default and causal basis.** Sweep requirement sets for unitless performance language at specification review and check matrix walks trigger the performance row exactly when thresholds exist. The pairing closes the loop from both ends, no performance requirement without units means every stated threshold is testable, and no performance testing without stated thresholds means every test verifies a real contract, neither vague claims nor aimless benchmarks survive.

**Operating conditions and assumptions.** Thresholds are testable in the project's environment, aspirational numbers with no runnable method fail the pair's spirit. How performance claims get specified and triggered for verification under this theme's carriers, benchmark construction lives with the reliability theme's correctness-preserving doctrine.

**Failure boundary and alternatives.** Breaking either half re-opens the classic failures, unitless requirements produce unfalsifiable fast-enough claims, and threshold-less benchmarking produces numbers with no contract to violate, measurement theater in both directions. Bounded alternatives and recoveries: ambient benchmark suites tracking everything continuously, richer data, unmoored from contracts, the pair's discipline argues for adding thresholds rather than adding dashboards.

**Counterexample, gotchas, and operational comparison.** The conditional trigger can be gamed by omission, teams avoiding performance requirements to avoid performance testing, the specification review must ask whether performance silence is honest indifference or testing avoidance. Good: a throughput clause in stated units whose matrix walk funds a matching benchmark. Bad: a fast-parsing claim in release notes with no requirement behind it. Borderline: a latency regression on an unspecified path, no contract violated, the incident argues for adding the missing clause, not for blaming the code.

**Verification, evidence, and uncertainty.** Sample requirements for units on performance language and matrix walks for correctly conditional performance rows. The template's units rule and the matrix's conditional row read in full. How often threshold omission is strategic rather than honest is unmeasurable from documents.

**Second-order consequence.** The matrix's conditional trigger is a quiet workload policy, it spends benchmark effort only where contracts demand it, freeing teams from ambient benchmarking while making every funded benchmark contractually meaningful, verification budget allocated by specification rather than by anxiety.

**Revision decision.** Enforce the conditional pair, performance clauses carry units at specification, the matrix's performance row triggers on their presence, and absence of thresholds means absence of both testing obligation and claiming rights.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require memory, panic, and concurrency evidence; hot-path operations need p95 under 10 ms locally or an explicit benchmark exception.
Leading indicator to measure: compile attempts and review comments decrease because the API shape is explicit before implementation.
Failure signal to watch: the reference hides ownership or error tradeoffs behind generic guidance.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what bundle count or doctrine size this theme's routing artifacts must be re-architected rather than stretched. The seed task-splitting bounds stand beside this theme's real scale seams, the router's dispatch assumes one bundle with four siblings, the merge assumes one file can still carry the doctrine, and both assumptions break as the corpus grows, more bundles need cross-bundle routing the router never models, and doctrine growth eventually overflows any single-file compression.

**Recommended default and causal basis.** Use per-bundle routers within their bundles, the corpus's routing sections across themes, and regenerate merges rather than patching them when sources outgrow the compression. Routing artifacts are sized to their corpus generation, the router's four modes and six task types partition the 202604 bundle's space, and each new theme, bundle, or doctrine area added since strains a partition that has no extension mechanism.

**Operating conditions and assumptions.** Growth is visible, silent doctrine additions that bypass corpus process cross seams without triggering anything. Bounding this theme's routing and packaging patterns, the routed doctrine's own scale limits belong to its themes.

**Failure boundary and alternatives.** Stretched past its seams, the router silently misroutes, new risk categories fall into the nearest old mode, and the merge's compression drops proportionally more as its sources grow, both artifacts degrading without any visible failure event. Bounded alternatives and recoveries: one corpus-wide master router, the centralized answer, a single artifact whose dispatch table would itself need routing at this corpus's scale.

**Counterexample, gotchas, and operational comparison.** The merge's fixed section count is the stealth seam, new doctrine areas have no section to land in, so a regenerated merge needs a structural decision, extend the twelve sections or split by audience, patching content into old sections hides the growth. Good: a new bundle shipping its own router while corpus routing sections handle cross-bundle questions. Bad: extending the 202604 router's tables to dispatch across bundles it predates. Borderline: patching one new task type into the router, absorbable once, a pattern of patches is the seam announcing itself.

**Verification, evidence, and uncertainty.** At each corpus growth event, re-test both assumptions and record which seams the event approached. The router's fixed partition and the merge's fixed structure read in full against corpus growth. The doctrine volume at which single-file merges stop being useful is unquantified.

**Second-order consequence.** This evolved corpus has already crossed the first seam, ninety-nine themes with adjacent-reference routing sections form exactly the cross-bundle layer the 202604 router lacks, the corpus's answer to routing scale is distributed routing sections rather than one master router, a design this theme documents from inside.

**Revision decision.** Name the seams with triggers, a second bundle triggers a cross-bundle routing layer above per-bundle routers, doctrine growth past what the merge's shape absorbs triggers regeneration or splitting, and corpus-wide routing belongs to the corpus's own maps, not to any bundle's router.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Rust Executable Reference Maps stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal that this theme's routing relations have gone stale. The seed theme-name queries would surface generic navigation content while this theme's staleness lives in named places, the bundle's own revision state in the archive, the unclassified copy's classification status, the routed siblings' structural drift against the router's tables, and the source themes' drift against the merge's transcribed blocks.

**Recommended default and causal basis.** Run the relational probe set at each corpus refresh and date each result against this assignment's baseline. Routing content ages relationally, nothing inside the router or merge decays alone, staleness arrives when some counterpart moves, so probes must check relations, path resolution, heading existence, block equivalence, rather than searching for newer routing prose.

**Operating conditions and assumptions.** Probe findings distinguish moved from changed, a relocated file fails path resolution while an edited one fails the diff, the repairs differ. Refreshing this document's routing and packaging claims, doctrine refresh rides with the routed themes' own probes.

**Failure boundary and alternatives.** Name-based searching returns reference-organization essays that neither confirm nor deny whether this router's six reading orders still name real sections, the one operational question a refresh must answer. Bounded alternatives and recoveries: pinning this document explicitly to the 202604 bundle state and marking all relations archive-dated, zero probe cost, and decreasingly honest as the corpus evolves around a frozen claim set.

**Counterexample, gotchas, and operational comparison.** The heading grep must use the router's exact section strings, paraphrase-tolerant matching would mask precisely the rename-drift the probe exists to catch. Good: a dated probe run showing all routed headings still resolve. Bad: logging that reference-map tutorials say nothing new. Borderline: probing whether newer bundles ship better router designs, genuine research, it refreshes the pattern, not this document's claims.

**Verification, evidence, and uncertainty.** Record each probe with its date and the specific relation it confirmed or flagged. The relational structure of every claim this document makes about its sources. Actual drift since 202604 is unknowable until the probes run.

**Second-order consequence.** Every probe this theme needs is mechanical, greps, diffs, and path checks, no external retrieval and no judgment reading, making this the cheapest full refresh in the rust cluster and the only one whose probes could run entirely in CI, relational staleness is automatable staleness.

**Revision decision.** Replace the name queries with relational probes, a path-resolution and heading-grep run over the router's targets, a re-diff of the two merge copies, a block comparison of the merge's gate and anti-pattern sections against their source themes, and an archive watch for bundle revisions.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | rust executable reference maps official documentation best practices |
| `github_repository_query_phrase` | rust executable reference maps GitHub repository examples |
| `release_notes_query_phrase` | rust executable reference maps changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label and with what attached caveats each claim here may be reused. The seed label definitions stand without this assignment's ledger, two distinct documents read fully, one byte-identical duplicate verified by diff, zero external URLs fetched, the bundle's playbook sibling unread and known only through the router's descriptions of it, and every routing-practice construct above marked as inference.

**Recommended default and causal basis.** Before reusing a claim, identify its stratum, cite documents for facts, date the diff relation, mark playbook claims as unread-source descriptions, and attach reasoning to inference. The fact stratum is the two documents' transcribable content, the router's tables and orders, the merge's templates, matrix, lists, and contract, plus one measured relation, the duplicate identity, while manifests, probes, targets, and traveler journeys are this evolution's constructed machinery.

**Operating conditions and assumptions.** The archive and unclassified paths stay stable so fact-class claims remain mechanically checkable. Reuse rules for this document's claims, transcribed content travels with document citations, the diff finding travels with its date, and constructed machinery travels only with its reasoning.

**Failure boundary and alternatives.** The costliest mislabel would present the duplicate identity as a standing fact rather than a dated measurement, the claim expires with either file's next edit, and downstream reuse without the date converts a session observation into a false invariant. Bounded alternatives and recoveries: per-claim inline labels if the stratum-level split ever proves too coarse for a dispute.

**Counterexample, gotchas, and operational comparison.** The router describes its playbook sibling's purpose in one line per mention, and this document repeats those descriptions, they are facts about what the router says, not about what the playbook contains, a two-hop provenance easily flattened in reuse. Good: reusing the verification matrix with its merge citation. Bad: citing the routing manifest as bundle doctrine. Borderline: reusing the duplicate finding elsewhere, valid with its date attached, misleading as an undated invariant.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares its stratum cleanly. This assignment's read ledger, two documents read fully, one diff run, zero retrievals. Readers can check the citations but not the reading behind them.

**Second-order consequence.** This theme contains the corpus's clearest example of a fact with a shelf life, most local facts here are stable archive content, but a relation between two live files is evidence of a different kind, and the label system's fact category quietly assumes stability it should sometimes date.

**Revision decision.** Publish the strata, countable document content as local corpus fact by section, the duplicate identity as a dated measurement, the playbook's nature as router-described hearsay, and all practice machinery as combined-evidence inference.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
