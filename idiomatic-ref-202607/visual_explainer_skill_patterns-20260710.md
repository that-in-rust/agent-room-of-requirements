# Visual Explainer Skill Patterns

**Decision supported.** This section helps decide how to frame an experience-producing skill inside a code-centric archive. The seed no seed line conveys that this theme holds a single 168-line personal 202603 skill that turns codebase explanation into a produced artifact, an interactive scrollable HTML page built from a five-part process, agent-driven code exploration, act-structured narrative, mandatory pedagogy, template-based generation, and a fixed output location with browser preview.

**Recommended default and causal basis.** Invoke for explain-how-X-works requests wanting visual or ELI5 output, and run all five process stages in order. The skill exists because documentation defaults to boring and unread, its when-to-use list literally includes documentation that is not boring, so it replaces prose docs with a designed reading experience, metaphors, progressive disclosure, and visual variety doing what walls of text do not.

**Operating conditions and assumptions.** The skill file remains at its archive path, checksum recorded this session. This reference covers the explainer-production practice, the code-exploration machinery it launches belongs to the exploration themes.

**Failure boundary and alternatives.** Reading the theme as a styling guide misses its ordering, the process spends its first stage entirely on code understanding, the bold rule forbidding a single word of explanation before deep exploration, presentation being the last mile of a comprehension pipeline. Bounded alternatives and recoveries: plain markdown explanation, the null format the not-boring trigger exists to escape.

**Counterexample, gotchas, and operational comparison.** The personal directory in its path marks it as an individual's skill rather than a team standard, its conventions, .prd/ELI5 output, home-directory template, reflect one workflow, portable in method, local in wiring. Good: an eight-act explainer of a queue subsystem opened in a browser. Bad: a styled page written before the code was explored. Borderline: using the act structure for a markdown doc, the pedagogy transfers, the interactivity is lost.

**Verification, evidence, and uncertainty.** Confirm the single-file inventory against the archive path before structural claims. The file read in full with checksum this session. Whether the author's template and output conventions evolved after 202603 is unrecorded.

**Second-order consequence.** The skill is the archive's only member whose output is an experience rather than code or prose, and it still inherits the archive's engineering habits, a process contract, component skeletons, and a closing checklist, the house method applied to storytelling, rigor and whimsy in one file.

**Revision decision.** Open with the single-source inventory, the artifact-not-answer framing, and the understanding-before-writing rule.

**Retained seed evidence.** The exact theme title and framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide what the effective evidence set is for a one-file theme with an off-archive asset. The seed one row lists the path without recording the measured topology, a single-source single-generation theme of 168 lines, no twin in any sweep, and a hard external dependency, the template at a home-directory path outside the archive, the skill's CSS and JS foundation being unverifiable from this repository.

**Recommended default and causal basis.** Cite the archive path for all doctrine and flag any template-content claim as unverifiable from here. Personal skills live outside the sweep-duplication pattern, one author, one copy, and their assets often stay on the author's machine, this theme inheriting both properties at once.

**Operating conditions and assumptions.** No future sweep vendors the template or a revised skill, the standing supersession caveat. Provenance for this theme's claims, the How LLMs Work inspiration link is an unretrieved external candidate.

**Failure boundary and alternatives.** Treating the skill as self-contained misses that its generation stage cannot run from the archive alone, the adapt-don't-start-from-scratch instruction pointing at a file only the author's home directory holds. Bounded alternatives and recoveries: reconstructing a template from the embedded skeletons, a fallback this mapping notes without performing.

**Counterexample, gotchas, and operational comparison.** The checklist item scroll-reveal animations work asserts behavior only the template implements, one quality gate being unverifiable exactly because its mechanism is the missing file. Good: doctrine synthesis from the 168 verified lines with the template gap declared. Bad: describing the template's dark theme as verified content. Borderline: quoting the template's feature list from the skill's own description, quoted claim, label the indirection.

**Verification, evidence, and uncertainty.** Re-run the checksum and re-check the archive for a vendored template at each audit. The checksum run and full read this session. The template's actual contents are permanently unverifiable from this repository.

**Second-order consequence.** The component reference partially rescues the missing template, seven HTML skeletons with their class vocabularies are embedded in the skill itself, so the document carries a degraded but real fallback, enough structure to rebuild a template, not enough to reproduce the original's polish.

**Revision decision.** Annotate the row with the line count, checksum, no-twin topology, and the missing-template dependency.

**Retained seed evidence.** The single local row and external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/personal/visual-explainer/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| https://react.dev/learn | external_research_source_material | external_research_sourced_fact | component-driven frontend behavior reference |
| https://threejs.org/docs/ | external_research_source_material | external_research_sourced_fact | visual scene API documentation |
| https://docs.github.com/actions | external_research_source_material | external_research_sourced_fact | delivery verification and automation reference |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which skill patterns deserve default-tier ranking on this evidence. The seed three corpus-process rows stand where the skill's own priorities rank, understanding-before-writing leads, the bold rule that deep exploration precedes a single word of explanation, then metaphors-first-code-second as the signature pedagogy, then one-concept-per-act as the disclosure discipline the checklist enforces twice.

**Recommended default and causal basis.** Hold every explainer to the three-pattern core before any component work begins. Understanding ranks first because every downstream technique amplifies whatever the author believes, metaphors for a misunderstood mechanism teach the misunderstanding vividly, the pipeline's order being its safety mechanism.

**Operating conditions and assumptions.** The audience needs teaching, reference documentation for experts answers to different rules. Ranking the skill's internal patterns, corpus-process patterns rank in the seed rows.

**Failure boundary and alternatives.** Ranking the visual components above the pedagogy produces beautiful wrong explainers, the component skeletons are the cheapest layer to imitate and the least valuable to get right, polish without comprehension being the skill's own failure mode inverted. Bounded alternatives and recoveries: the seed's process-tier scoreboard for corpus workflow ranking.

**Counterexample, gotchas, and operational comparison.** Apply ALL of these introduces the techniques as mandatory, not a menu, the skill's pedagogy being conjunctive where most style guides are elective, skipping one technique is non-compliance, not adaptation. Good: an explainer whose polling act opens with the mailbox metaphor. Bad: a page of styled code blocks with no metaphor layer. Borderline: two tightly-coupled concepts in one act with explicit bridging, still a violation, split the act.

**Verification, evidence, and uncertainty.** Spot-check ranking claims against the process text at audit. The process stages and techniques read verbatim this session. The cognitive claims behind metaphor priority are asserted, not cited.

**Second-order consequence.** Metaphors-first is a claim about cognition smuggled into a process step, the skill betting that a relatable anchor placed before code changes what the code reads as, the four worked examples, postal worker, microphone, USB, bank transaction, all mapping mechanism to social experience, the pedagogy having a consistent theory it never states.

**Revision decision.** Present the three-pattern core, understanding first, metaphor priority, single-concept pacing.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `visual_explainer_skill_patterns` | 95 | default_adoption_pattern_tier | Source Map First: Load local visual explainer material before synthesizing skill patterns recommendations. |
| `visual_explainer_skill_patterns` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `visual_explainer_skill_patterns` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what problem the skill actually solves and by what mechanism. The seed generic corpus formulas stand where the skill's thesis is implicit and sharp, technical understanding transfers through designed experience, not through accurate prose, an explainer being an argument sequenced as acts, anchored in metaphors, and paced one concept at a time, with the code itself demoted to evidence for the narrative.

**Recommended default and causal basis.** Sequence explainers as the five-role arc and demote code to evidence within it. Prose documentation fails at transfer because it optimizes for completeness over sequence, readers meet every fact and retain no arc, the act structure imposing the dramatic ordering, mystery, mechanics, comparisons, resilience, architecture, that memory actually holds.

**Operating conditions and assumptions.** The subject has a genuine problem-solution shape, purely arbitrary conventions resist the mystery framing. The thesis governs explanatory artifacts, API reference and specification writing serve different contracts.

**Failure boundary and alternatives.** The thesis's negation is the definition-first document, opening with what X is instead of the problem X exists to solve, the checklist's first item, opens with a mystery not a definition, existing precisely because definitions are how experts start and how novices stop reading. Bounded alternatives and recoveries: reference-manual completeness, correct for lookup, hopeless for onboarding.

**Counterexample, gotchas, and operational comparison.** Concrete-before-abstract inverts expert instinct, the SELECT-before-offset example ordering the specific query before the general pattern, authors who know the abstraction fight the ordering hardest. Good: act one asking how data at rest becomes data in motion. Bad: act one defining change data capture in vendor vocabulary. Borderline: a mystery opening for a config file explainer, forced but workable, find the real tension.

**Verification, evidence, and uncertainty.** Audit explainers for arc order and mystery openings. The act table and checklist read verbatim this session. Retention claims for act-structured explainers are unmeasured anywhere.

**Second-order consequence.** The act table's arc is a genre convention borrowed from drama and applied to systems, the mystery is a cold open, comparisons are the second-act complication, the architecture finale is the ensemble reveal, the skill treating a codebase walkthrough as a story with the code as its cast.

**Revision decision.** State the designed-experience thesis with the act arc as its structural core.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `visual_explainer_skill_patterns` contains 1 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Visual Explainer Skill Patterns comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide how one document's three zones divide production, reference, and review. The seed heading signals name sections while the working map is a pipeline document, five process stages in execution order, then a component reference as the build-time catalog, then the quality checklist as the exit gate, front-to-back mirroring the work itself rather than topic clusters.

**Recommended default and causal basis.** Read in pipeline order when producing, checklist-first when reviewing or learning. A generation skill reads best in execution order because its consumer is mid-task, the map is the process, stage one's reader needs exploration guidance, stage four's needs the skeletons, the document's structure being its own routing.

**Operating conditions and assumptions.** The single file remains the theme's whole local surface. Navigation of this theme's single document, the launched Explore agents belong to other themes.

**Failure boundary and alternatives.** Consulting only the component reference produces markup without method, the skeletons are the least instructive third of the file, the pedagogy and checklist being where the skill's actual judgment lives. Bounded alternatives and recoveries: heading search within one 168-line file, trivial, the zone split is the real navigation.

**Counterexample, gotchas, and operational comparison.** Stage five's output convention, .prd/ELI5 with index.html or topic names, is easy to dismiss as trivia and is the difference between artifacts a team can find and orphaned HTML, location being part of the contract. Good: a producer at stage four copying the vs-grid skeleton directly. Bad: a reviewer grading an explainer without the checklist open. Borderline: reading only the pedagogy section to improve prose docs, partial reuse, legitimate and incomplete.

**Verification, evidence, and uncertainty.** Trace consultations against the zone split at review. The full document read several times this session. None about structure, 168 lines were read wholesale.

**Second-order consequence.** The checklist is the document's summary in disguise, its ten items compress every process stage and technique into verifiable form, a reader with five minutes should read the checklist first and the process second, the exit gate doubling as the fastest faithful abstract.

**Revision decision.** Annotate the map with the pipeline reading and the three-zone split, process, catalog, gate.

**Retained seed evidence.** The single local source row with title, heading signals, and usage role remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/personal/visual-explainer/SKILL.md | visual-explainer | Visual Explainer; When to Use; Process; 1. Deep Exploration; 2. Narrative Architecture; 3. Pedagogical Techniques | skill entrypoint or reusable agent prompt |

## External Research Source Map

**Decision supported.** This section helps decide which external claims this theme owns and how each decays. The seed inherited doc-site rows stand while the skill's real external surface is small and specific, the How LLMs Work inspiration page whose approach the skill generalizes, the home-directory template with its CSS and JS, and the implicit browser platform, scroll-reveal, CSS animations, and responsive layout all assuming a modern rendering engine.

**Recommended default and causal basis.** Reuse the pedagogy freely, verify template availability before generation, and treat the component CSS classes as conventions, not standards. A presentation skill decays at its rendering assumptions and its assets, the pedagogy is timeless while CSS-only pulsing arrows and scroll-reveal implementations age with browser fashion, and the template ages invisibly because it lives outside the archive.

**Operating conditions and assumptions.** Browsers keep rendering the component patterns, a safe bet at the decade scale. External dependencies for freshness tracking, all inherited URLs stayed unretrieved candidates.

**Failure boundary and alternatives.** The aging risk with no probe is the template, the archive cannot detect its drift, deletion, or improvement, every generation-stage claim resting on a file whose state is permanently unknown from here. Bounded alternatives and recoveries: the seed's inherited ecosystem anchors, retained for context.

**Counterexample, gotchas, and operational comparison.** The emoji placeholder in the metaphor-card skeleton marks where the archive's no-emoji instinct meets a design system that wants icons, consumers matching repo conventions may need substitutes. Good: rebuilding the template from the embedded skeletons when the original is absent. Bad: promising scroll-reveal behavior sight-unseen from the missing file. Borderline: fetching the inspiration page to enrich the theme, valuable, a future-refresh action, not this evolution's.

**Verification, evidence, and uncertainty.** Check template presence and inspiration-link liveness at each audit. The skill's external references enumerated from the full read, none fetched. The template's contents and the inspiration page's current state are both unretrieved.

**Second-order consequence.** The inspiration credit is load-bearing provenance, the skill names the page whose style it generalizes, so the theme's lineage runs through a public artifact rather than an archive sweep, the one external link whose retrieval would genuinely upgrade this theme's evidence.

**Revision decision.** Keep the inherited rows and add the three-item surface, inspiration link, off-archive template, browser platform.

**Retained seed evidence.** All external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://react.dev/learn | React documentation | component-driven frontend behavior reference | external_research_sourced_fact |
| https://threejs.org/docs/ | Three.js documentation | visual scene API documentation | external_research_sourced_fact |
| https://docs.github.com/actions | GitHub Actions documentation | delivery verification and automation reference | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which explainer behaviors the skill rejects. The seed three corpus-process rows stand where the skill implies a native registry through its rules and checklist, explanation before understanding, definitions before mysteries, code before metaphors, two concepts in one act, single-component monotony, comparisons omitted when alternatives exist, unlabeled code blocks, and endings without the summary table.

**Recommended default and causal basis.** Review every explainer against the implied rows with the understanding check first. Each rejection inverts a stated rule, the registry is the checklist read as prohibitions, and the checklist exists because these failures are the natural defaults of expert authors, who define, abstract, and bundle concepts by habit.

**Operating conditions and assumptions.** The artifact claims to teach, expert-to-expert notes answer to different rules. Explainer-production rejections, corpus-process rejections stay in the seed rows.

**Failure boundary and alternatives.** The compounding failure is explanation before understanding, every other row degrades presentation while this one falsifies content, a fluent act structure wrapped around a misread mechanism teaching the misreading with production values. Bounded alternatives and recoveries: the quality checklist itself, the same content as affirmative gates.

**Counterexample, gotchas, and operational comparison.** Visual variety has a failure mode in both directions, monotony bores and carnival distracts, the checklist's floor of three component types setting a minimum without licensing all eight everywhere. Good: a draft rejected because its retry act carried no metaphor. Bad: an explainer opening with the subsystem's class hierarchy. Borderline: an act with one concept and two metaphors, redundant but harmless, prune to the stronger.

**Verification, evidence, and uncertainty.** Confirm registry rows map to rule or checklist text at audit. The rules and checklist read verbatim this session. Relative frequency of these failures in real use is unrecorded.

**Second-order consequence.** The registry's presentation rows share one root, author-shaped output, definitions, abstractions, dense acts, and bare code are all how the material looks from inside expert knowledge, the skill's rules being systematic inversions of expertise's defaults, a checklist against the curse of knowledge.

**Revision decision.** Add the implied registry with premature explanation as the gateway row.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide how experience artifacts get verified when their content is narrative and their behavior is visual. The seed document verifier text stands where the skill ships a ten-item exit gate, mystery opening, metaphor-before-code everywhere, one concept per act, three-plus component types, comparisons where alternatives exist, labeled highlighted code, summary-table ending, nav dots matching acts, working scroll-reveal, and browser opened after creation.

**Recommended default and causal basis.** Walk all ten gates before delivery, splitting reading checks from interaction checks. An experience artifact resists automated testing, so the gate is a human inspection protocol, each item checkable by reading or clicking, the checklist making done a property with a definition rather than a feeling.

**Operating conditions and assumptions.** A browser is available, headless environments defer the interaction gates honestly. The skill's verification doctrine, this document's own structural verification keeps the seed's retained block.

**Failure boundary and alternatives.** The unverifiable item from the archive is the animation check, its mechanism lives in the missing template, a consumer without the template inheriting nine checkable gates and one that requires rebuilding the foundation first. Bounded alternatives and recoveries: the corpus verifier command in the seed's retained block for this document's own checks.

**Counterexample, gotchas, and operational comparison.** The gates check structure, not truth, an explainer can pass all ten while misexplaining the code, the understanding rule from stage one being the truth gate the checklist assumes rather than repeats. Good: a review walking the ten items with the page open beside the code. Bad: approval on visual polish with the concept-pacing items unchecked. Borderline: deferring animation checks in CI, honest deferral, gate them at first human view.

**Verification, evidence, and uncertainty.** Check sampled explainers for gate-walk records at review. The ten checklist items read verbatim this session. How often structurally-passing explainers still misteach is unmeasured.

**Second-order consequence.** The final gate, open in browser after creation, verifies the author rather than the artifact, forcing the producer to experience their own explainer as a reader immediately, the cheapest usability test in existence built into the process as a mandatory last step.

**Revision decision.** State the ten-gate inspection with the checkable-by-reading versus checkable-by-clicking split.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide when and how agents should deploy the explainer machinery. The seed four generic bullets stand where the skill defines agent consumption precisely, trigger on explain-how-X-works, ELI5, onboarding, and not-boring-documentation requests, launch Explore agents for the comprehension stage, apply all techniques conjunctively, adapt the template rather than starting fresh, and write to the fixed output location with a browser preview.

**Recommended default and causal basis.** Trigger on the four request shapes, subcontract exploration, apply all techniques, and constrain generation to template adaptation. The skill is itself an agent prompt whose first stage delegates to other agents, exploration is subcontracted while narrative, pedagogy, and generation stay with the invoking agent, a two-tier agent architecture inside one skill.

**Operating conditions and assumptions.** The template resolves at its path, its absence forces the rebuild fallback the evidence section notes. Agent consumption of this skill, the Explore agents' own doctrine belongs to exploration themes.

**Failure boundary and alternatives.** The characteristic agent failure is skipping the exploration stage because the agent believes it already knows the code, the bold rule existing precisely because generation-capable agents write fluently from shallow context, fluency masking the missing comprehension. Bounded alternatives and recoveries: freeform explanation in chat, the default the skill upgrades when the request shape matches.

**Counterexample, gotchas, and operational comparison.** The trigger phrases are conversational, not technical, help me understand Y qualifying as readily as an explicit explainer request, agents routing only on the word visual will miss most of the skill's intended traffic. Good: an onboarding request answered with an eight-act page in .prd/ELI5. Bad: a from-scratch HTML page ignoring the template foundation. Borderline: a chat-length ELI5 for a tiny question, proportionate, offer the full artifact.

**Verification, evidence, and uncertainty.** Sample agent responses to explain-shaped requests for stage compliance. The description field and process stages read verbatim this session. Real trigger-recognition rates among agents are unrecorded.

**Second-order consequence.** Adapt-the-template-don't-start-from-scratch is an agent-specific instruction, generation models default to producing fresh HTML, and the rule pins them to the proven foundation, variance constrained to color variables, titles, and content, the skill managing its agent's creativity budget explicitly.

**Revision decision.** Recast the section around the two-tier architecture with the conjunctive-techniques mandate.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide what a complete explainer journey looks like from request to gate. The seed one generic engineer stands where the natural journey is the subsystem onboarding, an engineer must onboard a teammate on the ingestion pipeline, launches exploration to map structs, flow, and error paths with file-and-line citations, structures seven acts from the data-at-rest mystery through polling versus CDC comparisons to the commit-discard protocol and plugin finale, assigns each concept its metaphor, adapts the template's colors and dots, writes to .prd/ELI5, and walks the checklist with the page open.

**Recommended default and causal basis.** Plan acts around the concept arc, not the file tree, and keep the artifact as standing documentation. The journey works because each stage's output is the next stage's input, exploration's citations become the acts' evidence, the act list becomes the navigation dots, the techniques become the checklist's pass conditions, nothing generated at the end that was not gathered at the start.

**Operating conditions and assumptions.** The subsystem is stable enough to document, fast-moving code outdates acts like any docs. Journeys through the skill's practice, corpus-process journeys stay in the seed lines.

**Failure boundary and alternatives.** The journey derails at act planning most often, experts drafting acts around the code's file structure instead of the concept's arc, module-by-module tours being the org chart of the code, not the story of the mechanism. Bounded alternatives and recoveries: a live walkthrough meeting, richer once, unavailable to the next new hire.

**Counterexample, gotchas, and operational comparison.** The exploration stage's file-and-line citations age fastest, an explainer whose narrative survives refactors may still point at moved lines, the artifact needing the same refresh discipline as this corpus. Good: a new hire onboarded from the page alone, questions arriving act-numbered. Bad: seven acts mirroring seven source directories. Borderline: reusing an old explainer after a refactor, narrative holds, re-verify the citations.

**Verification, evidence, and uncertainty.** Audit sampled journeys for arc-planned acts and post-delivery reuse. The process stages and act table read this session. How often explainers survive their subsystems' evolution is unrecorded.

**Second-order consequence.** The finished artifact outlives its occasion, the onboarding page doubles as durable subsystem documentation precisely because it was built for a reader who knew nothing, the skill's not-boring trigger quietly producing the only docs new hires reliably finish.

**Revision decision.** Recast the scenario as the onboarding build with act planning as the load-bearing stage.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The reader or documentation author is starting cold and deciding whether the reference answers the next practical question and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `visual_explainer_skill_patterns` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing the reading path, example, diagram, or rewrite pattern that makes action obvious.
Reference opening trigger: Open this file when the task mentions visual explainer skill patterns, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide which explainer trade-offs the skill fixes and by what tests. The seed template rows skip the trade-offs the skill adjudicates, richness versus effort, resolved toward a full designed artifact for onboarding-grade requests, freshness versus foundation, resolved by template adaptation over from-scratch generation, completeness versus pacing, resolved by one concept per act even when the material resists splitting, and accuracy versus accessibility, resolved by metaphor-first with the code kept as the authority beneath.

**Recommended default and causal basis.** Resolve each trade-off as the skill does and test every metaphor for preserved distinctions. Each resolution spends author effort to buy reader throughput, the skill consistently pricing the author's time below the audience's comprehension, the opposite exchange rate from expert-convenience documentation.

**Operating conditions and assumptions.** The audience is genuinely novice, expert audiences renegotiate the pacing rate. Trade-off structure the skill teaches, corpus adopt-adapt-avoid rows stay in the seed.

**Failure boundary and alternatives.** The accuracy-accessibility line is the most misjudged, metaphors that flatter the mechanism mislead, the microphone-versus-mailbox pair works because it preserves the actual distinction, push versus poll, a metaphor that loses the distinction is decoration, not pedagogy. Bounded alternatives and recoveries: the seed's adopt, adapt, avoid framework for corpus-level decisions.

**Counterexample, gotchas, and operational comparison.** The richness resolution scales poorly downward, a full act-structured page for a two-line question is ceremony, the skill's triggers all describe subsystem-scale requests, scope the machinery to the question. Good: the bank-transaction metaphor for two-phase commit, both sides must agree preserved. Bad: a car metaphor for a queue because cars are relatable. Borderline: splitting one hard concept across two acts, pacing honored, continuity owed.

**Verification, evidence, and uncertainty.** Review explainers for metaphor fidelity and act scoping at audit. The four worked metaphors and technique rules read this session. The trade-offs' reception across audience levels is unmeasured.

**Second-order consequence.** One concept per act is a pacing rule that doubles as a scoping tool, material that cannot be split into single-concept acts is material the author does not yet understand at act grain, the constraint diagnosing comprehension gaps while it paces disclosure.

**Revision decision.** Add the four trade-off rows with metaphor fidelity as the governing test.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the visual explainer skill patterns pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong visual explainer skill patterns guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which zone governs which question class inside one document. The seed role labels rank one file while the operative hierarchy is internal, the process stages govern the component reference, and the checklist governs both, rules outranking skeletons because the skeletons are replaceable while the rules are the skill, with the off-archive template below all three as an asset, not an authority.

**Recommended default and causal basis.** Resolve conflicts toward the checklist, treat skeletons as defaults, and the template as convenience. A single-file theme's hierarchy is its zones' precedence, and here the gate zone wins conflicts, an artifact meeting every skeleton but failing a checklist item fails, the checklist being the contract the rest of the file exists to satisfy.

**Operating conditions and assumptions.** The file's zones remain internally consistent, no contradiction was found this session. Precedence within this theme's single document, cross-theme precedence belongs to the routing section.

**Failure boundary and alternatives.** Inverting the hierarchy treats markup conformance as compliance, pages built from the exact skeletons that open with definitions failing the actual contract while matching every div. Bounded alternatives and recoveries: flat authority across zones, which turns skeleton choices into contested doctrine.

**Counterexample, gotchas, and operational comparison.** The output-location rule sits in the process zone but behaves like a gate, a checklist-passing page in the wrong directory still breaks the convention consumers rely on, location compliance being an unlisted eleventh item. Good: a nonstandard component accepted because all ten gates pass. Bad: a gate waived because the markup matched the reference. Borderline: a new component type beyond the eight, the variety floor permits it, the fidelity tests still apply.

**Verification, evidence, and uncertainty.** Test conflict resolutions against the zone precedence at audit. The document's zone structure read this session. None about current structure, the single file was read wholesale.

**Second-order consequence.** The hierarchy explains what survives loss, if the template vanishes the skill still works degraded, if the skeletons vanished the classes could be reinvented, if the checklist vanished the skill would lose its definition of done, resilience ordering matching authority ordering exactly.

**Revision decision.** State the three-zone precedence, checklist over process over catalog, with the template as subordinate asset.

**Retained seed evidence.** The hierarchy rows and the classification vocabulary remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.
Confidence warning: only one local corpus source is mapped, so this reference should invite adjacent-source discovery before becoming canonical policy.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202603/personal/visual-explainer/SKILL.md | canonical primary source | Visual Explainer; When to Use; Process | What guidance, warning, or example should this source contribute to Visual Explainer Skill Patterns? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing structure keeps produced explainers trustworthy over time. The seed generic artifact fields stand where this theme's natural object is the explainer inventory, one row per produced page, its topic, act count, component types used, source-code citations with their verification date, checklist walk date, and staleness state against the code it explains, the .prd/ELI5 directory turned from a dumping ground into a maintained collection.

**Recommended default and causal basis.** Add an inventory row at each page's creation and re-verify citations when the cited files change. The skill fixes where artifacts land but nothing tracks what lands there, and explainers decay against their subjects silently, the inventory making the collection's health visible the way the corpus queue makes evolution visible.

**Operating conditions and assumptions.** The team produces more than one explainer, single-artifact users can hold state in the page itself. One artifact format for any team using the skill, composable with the seed's completion rules.

**Failure boundary and alternatives.** Without the inventory, stale explainers teach outdated mechanisms with full production polish, the format's persuasiveness making decayed content more dangerous than decayed prose, confidence without currency. Bounded alternatives and recoveries: organizational doc registries, coarser and blind to the citation-level staleness this format enables.

**Counterexample, gotchas, and operational comparison.** Staleness is per-act, not per-page, a refactored retry path outdates one act while six others hold, the inventory row wanting act-level notes before a full regeneration is ordered. Good: a refactor PR flagging two explainer acts via the inventory's citation index. Bad: a three-year-old page onboarding hires onto a rewritten subsystem. Borderline: marking a page historical instead of refreshing it, honest, label it in the page too.

**Verification, evidence, and uncertainty.** Diff inventory citations against current code in sampled audits. The output convention and citation requirement read this session. Inventory upkeep costs at real collection sizes are unmeasured.

**Second-order consequence.** The inventory converts the skill from a one-shot generator into a documentation system, pages become maintained assets with refresh triggers tied to their cited code paths, the file-and-line citations from stage one becoming the staleness probes of the collection's lifecycle.

**Revision decision.** Define the artifact as the inventory with staleness state as its operative column.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: visual grammar with before/after diagram and comprehension test.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Visual Explainer Skill Patterns | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercises transmit the mapping discipline fastest. The seed abstract usage lines stand where the skill teaches by worked example already, four metaphor mappings, postal worker for polling, microphone versus mailbox for CDC, USB for plugins, bank transaction for retry protocol, one act-arc example threading a data-pipeline subject, and the SELECT-before-offset ordering demonstration, the document modeling its own pedagogy throughout.

**Recommended default and causal basis.** Certify understanding by mapping three unfamiliar mechanisms to distinction-preserving metaphors. A pedagogy skill must exhibit its techniques or forfeit credibility, and this one does, every abstract rule in the file arriving with its concrete instance first, the document obeying concrete-before-abstract while teaching it.

**Operating conditions and assumptions.** Learners know the mechanisms they are mapping, the metaphor test presumes the truth is known. Teaching this theme's material, general writing pedagogy belongs elsewhere.

**Failure boundary and alternatives.** Teaching from the four metaphors as a reusable library misses their point, they are worked examples of the mapping discipline, not stock answers, the postal worker reused for every polling system regardless of fit being decoration in the fidelity section's sense. Bounded alternatives and recoveries: studying the inspiration page's technique directly, the upstream examples this skill generalized.

**Counterexample, gotchas, and operational comparison.** Metaphors leak, every mapping breaks somewhere, the postal worker does not batch, the discipline includes knowing where the metaphor ends and saying so before the reader extrapolates past its validity. Good: a learner's metaphor for backpressure that preserves the feedback distinction. Bad: the USB metaphor pasted onto a non-discovery plugin system. Borderline: a metaphor with a stated breaking point, mature usage, exactly the discipline.

**Verification, evidence, and uncertainty.** Grade mapping exercises on distinction preservation at review. The four mappings and ordering example read verbatim this session. Transfer from mapping exercises to real explainer quality is unmeasured.

**Second-order consequence.** The CDC pair is the set's masterclass, two metaphors in deliberate contrast, checking the mailbox versus a microphone in the room, teaching by opposition what each approach costs, comparison built into the metaphor layer itself, the technique and the mid-act comparison rule fusing in one example.

**Revision decision.** Anchor the section on the mapping exercise, new mechanism, candidate metaphor, distinction-preservation test.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Visual Explainer Skill Patterns after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Visual Explainer Skill Patterns as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Visual Explainer Skill Patterns only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which numbers show the practice teaching versus merely producing. The seed generic indicator lines stand where the practice's gauges are audience-shaped, completion, whether readers reach the final act, comprehension, whether onboarded readers can answer act-level questions, reuse, whether the pages get shared and reopened after their occasion, staleness, the inventory's citation-drift rate, and production health, checklist pass rates at first review.

**Recommended default and causal basis.** Gather at least informal completion and comprehension signals per page and review the inventory's staleness quarterly. An explainer exists to transfer understanding, so its metrics live in readers rather than in the artifact, the page's polish being an input while the onboarded teammate is the output.

**Operating conditions and assumptions.** Readers are reachable, published-and-forgotten pages yield no loop at all. Measuring the practice's operation, corpus-document metrics stay in the seed lines.

**Failure boundary and alternatives.** Measuring production volume breeds gallery decay, many beautiful pages, no evidence anyone finished one, the not-boring promise unverified against the only audience that can verify it. Bounded alternatives and recoveries: author self-assessment via the checklist alone, necessary and audience-blind.

**Counterexample, gotchas, and operational comparison.** Comprehension checks must target the mechanism, not the metaphor, a reader who remembers the postal worker but cannot say what the offset does has retained the story and missed the system, the gauge testing transfer, not recall. Good: an onboarding retro asking act-three questions a week later. Bad: celebrating page count with no reader signal. Borderline: dot-based scroll analytics on an internal page, informative, weigh the observation cost.

**Verification, evidence, and uncertainty.** Confirm gauge collection in sampled teams at each cycle. The practice's reader-facing purpose enumerated from the triggers this session. Baseline completion rates for conventional docs are unmeasured here.

**Second-order consequence.** The act structure makes reader telemetry natural, navigation dots segment the page into measurable stages, drop-off by act locating exactly where an explainer loses its audience, the dramatic structure doubling as an analytics schema no prose document offers.

**Revision decision.** Adopt the five gauges with reader completion as the anchor.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: a new reader can apply the reference without asking the author for hidden context.
Failure signal: the reference is a literature dump instead of a guided explanation.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this synthesis is declared faithful. The seed shape items never audit this theme's distinctive claims, the checksum, the single-source no-twin topology, the five-stage process order, the 5-8 act range, the conjunctive techniques mandate, the eight component types and the ten checklist items by count, the off-archive template dependency, and the .prd/ELI5 output convention.

**Recommended default and causal basis.** Run the mechanical checks each cycle with the absence check explicitly included. The synthesis quotes at count and clause grain, stages, acts, components, gates, so its audit must check at that grain, heading checks passing while a count or the template caveat drifted.

**Operating conditions and assumptions.** The single file remains the claims' sole local ground. Auditing this reference against its one mapped path at clause grain.

**Failure boundary and alternatives.** A shape-only audit misses the theme's most important negative claim, that the template is absent from the archive, a future vendoring of it would silently falsify the dependency caveat across four sections while improving the evidence. Bounded alternatives and recoveries: sampled auditing, pointless at 168 source lines.

**Counterexample, gotchas, and operational comparison.** The act range and component count live in prose and lists rather than tables, count checks here need reading rather than parsing, slightly more expensive and still minutes. Good: an audit logging the checksum, matched counts, and template still absent. Bad: declaring fidelity from seed-shape checks alone. Borderline: skipping the absence check after stable cycles, keep it, its failure is the update trigger.

**Verification, evidence, and uncertainty.** Execute the distinctive items and date each result in the audit record. The claim structure of every distinctive statement in this document. Future vendoring or supersession are the moving parts.

**Second-order consequence.** The template-absence item is the corpus's first standing check for something not existing, most audit items confirm presence while this one confirms a gap, and its failure mode is the good direction, the check existing so the caveat gets retired the moment it stops being true.

**Revision decision.** Add the distinctive items, checksum, counts of stages, acts, components, and gates, the conjunctive-mandate quote, and the template-absence check.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Visual Explainer Skill Patterns.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a question arriving at the explainer format's boundary. The seed token-split rows point at generic visual and skill neighbors while the operative routes follow the skill's own composition, the exploration stage's machinery routes to the deep-exploration and codebase-analysis themes, the subject matter of any explainer routes to that subject's technical theme, documentation-process questions route to the writing and journaling themes, and the explainer format itself, acts, metaphors, components, gates, stays here.

**Recommended default and causal basis.** Answer format and pedagogy questions here and pair the subject theme explicitly when an explainer carries technical content. The skill composes like the timeline theme does, a format over content other themes own, comprehension subcontracted to exploration, truth owned by the subject theme, experience design owned here.

**Operating conditions and assumptions.** The adjacent themes exist in the corpus, their coverage is the queue's fact. Sending away what this theme holds only incidentally, destination doctrine is not restated.

**Failure boundary and alternatives.** Answering exploration-methodology questions from this theme's one-stage summary restates a sibling secondhand, the skill launches Explore agents and says nothing about how they work. Bounded alternatives and recoveries: for print or slide formats, nothing local, say so rather than stretching the HTML doctrine.

**Counterexample, gotchas, and operational comparison.** Explainer requests about this corpus itself would compose the format with the corpus-process themes, a self-referential pairing that works exactly like any other subject pairing. Good: an ingestion explainer citing this theme for structure and the data-pipeline theme for facts. Bad: exploration-agent tactics answered from the one-line stage description. Borderline: a metaphor-quality question, format here, subject fidelity from the subject theme, cite both.

**Verification, evidence, and uncertainty.** Sample recent answers for composition-consistent routing. The exploration delegation and format ownership read this session. The corpus's exploration-theme coverage is the queue's fact, not this analysis's.

**Second-order consequence.** This theme is the corpus's second meta-reference and its expressive complement, the timeline theme structures decisions and this one structures explanations, both consuming other themes' content through a designed format, the corpus quietly holding a pair of reasoning-and-teaching formats above its technical strata.

**Revision decision.** Route by composition, format here, comprehension machinery and subject truth to their owners.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use visual, ASCII, HTML, or writing references when the output medium is fixed.
Adjacent reference 1: consider the visual adjacent reference when the current task pivots away from visual explainer skill patterns.
Adjacent reference 2: consider the explainer adjacent reference when the current task pivots away from visual explainer skill patterns.
Adjacent reference 3: consider the skill adjacent reference when the current task pivots away from visual explainer skill patterns.

## Workload Model

**Decision supported.** This section helps decide how to budget a practice whose cost is comprehension and design rather than reading. The seed generic boundary rows stand beside this node's measured units, a 168-line source read in minutes, per-artifact cost dominated by exploration and act design rather than markup, hours for a subsystem-grade page, and per-cycle upkeep of one checksum, count checks, and the absence probe, the corpus's second-lightest maintenance surface around an hours-scale practice.

**Recommended default and causal basis.** Invoke at subsystem scale, spend the effort on stages one through three, and let the template amortize stage four. Like the timeline theme, this skill inverts document weight and practice weight, the reference is trivial to keep fresh while each invocation spends real effort, the workload living in the producer's comprehension and design, not in the reading.

**Operating conditions and assumptions.** The template or its rebuilt equivalent exists, from-scratch generation multiplies stage four's cost. Budgeting this node's upkeep and the practice's invocation, artifact scope is set by the request.

**Failure boundary and alternatives.** Budgeting by the file's size invites casual invocation of an hours-scale format, the machinery deployed for questions a paragraph would serve, the triggers' subsystem-scale shape being the built-in scope guard. Bounded alternatives and recoveries: the seed's bounded-capacity row, retained as the outer limit on any single consultation's scope.

**Counterexample, gotchas, and operational comparison.** Act design resists time-boxing the way the timeline theme's lived-experience step does, rushed explainers reliably ship file-tree acts, the diagnostic being acts named after directories instead of questions. Good: a day spent on exploration and acts, an hour on template adaptation. Bad: a week of custom CSS for a page whose acts took an afternoon. Borderline: a half-scale three-act page for a mid-size question, proportionate if the arc survives the compression.

**Verification, evidence, and uncertainty.** Check produced artifacts for effort distribution across stages at review. The file measured and the practice's stages walked this session. Real production times per explainer are unmeasured.

**Second-order consequence.** The template is the practice's own workload valve, adapt-don't-start-from-scratch converting the most expensive stage, visual design, into variable substitution, the hours saved there being redirected by the process into exploration and pedagogy where they buy comprehension.

**Revision decision.** Restate the model as thin-reference thick-practice with trigger-shaped invocation.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Visual Explainer Skill Patterns as a agent workflow operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | agent planning, tool use, context loading, delegation, or skill/plugin execution where bad guidance compounds across long-running sessions | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one primary task, up to 10 source files, up to 3 delegated subtasks, and a written completion audit for every run | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Visual Explainer; When to Use; Process; 1. Deep Exploration; 2. Narrative Architecture; 3. Pedagogical Techniques | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is visual grammar with before/after diagram and comprehension test | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which invariants must demonstrably hold for this node's claims to stay citable. The seed generic thresholds stand where this node's invariants should, source integrity, the checksum holding, process fidelity, the five stages in order with the understanding rule bold and first, pedagogy fidelity, the conjunctive mandate and the three core techniques quoted intact, gate fidelity, all ten checklist items traveling together, and dependency honesty, the template's off-archive status declared wherever the generation stage is taught.

**Recommended default and causal basis.** Hold all five invariants at audit cadence with the stage-order check as the derivative tripwire. Each invariant guards a distinct corruption, file rot breaks evidence, a reordered process licenses explanation before understanding, an elective reading of the techniques dissolves the pedagogy into suggestions, a truncated checklist redefines done, and a hidden dependency caveat sells unverifiable behavior as verified.

**Operating conditions and assumptions.** Derivatives are discoverable, the format absorbed into private prompt libraries evades inspection. Invariants for this node's claims, artifact quality in deployment belongs to the metrics loop.

**Failure boundary and alternatives.** The costliest breach is process reordering, derivatives that open with the component catalog invert the skill's safety mechanism, producing the beautiful-wrong artifacts the understanding rule exists to prevent. Bounded alternatives and recoveries: byte-exact reuse only, safe and hostile to the format's natural portability.

**Counterexample, gotchas, and operational comparison.** Faithful translation into other stacks is permitted, slide decks or terminal UIs replacing HTML while keeping the arc, the mandate, and the gates, the invariants following the pedagogy, not the markup. Good: an audit finding a team variant with stages ordered and the template caveat intact. Bad: a derivative teaching component markup with no exploration stage at all. Borderline: a variant with nine gates merging two originals, content preserved, count drifted, note the mapping.

**Verification, evidence, and uncertainty.** Review the five invariant series at each corpus checkpoint with dated evidence. The claim structure established across this document. None about current truth, all five invariants were verified during this evolution.

**Second-order consequence.** The invariants are all checkable from one small file plus derivative inspection, but the dependency-honesty invariant is unique, it guards a claim about what is missing, fidelity here including faithful transmission of a gap, the first invariant in the corpus protecting an absence.

**Revision decision.** Publish the five invariants with evidence methods, checksum runs, stage-order checks, mandate quotation diffs, gate counts, and dependency-caveat presence in derivatives.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which decay modes need standing probes for a personal experience-format skill. The seed the seed's generic rows stand while this node's own decay modes go unnamed, template loss becoming permanent as the author's environment changes, supersession, a later personal or team sweep revising the skill unnoticed, pedagogy dilution, the conjunctive mandate softening into style suggestions in retellings, and format fetishism, the components adopted without the process until the house style decorates unexplored code.

**Recommended default and causal basis.** Run the four probes at audit cadence with citation presence as the fetishism signal. The first decays on the author's environment clock and is already unverifiable, the second on sweep cadence, the third through friendly summarization, and the fourth through the same structure-laundering channel the timeline theme documented, polish worn without the work.

**Operating conditions and assumptions.** Produced pages are discoverable for sampling, private galleries evade the probe. Decay of this node's claims and the practice's deployed fidelity, artifact staleness belongs to the inventory.

**Failure boundary and alternatives.** Format fetishism compounds worst here because the format is genuinely attractive, teams copy the dark theme and act layout for their wikis, the visual signature spreading detached from the understanding rule that made it trustworthy, the brand outrunning the discipline. Bounded alternatives and recoveries: quotation-only reuse, resistant to dilution, helpless against fetishism, which lives in use.

**Counterexample, gotchas, and operational comparison.** Pedagogy dilution reads as reasonable flexibility, use metaphors where helpful being true and toothless, the probe checking for the load-bearing specifics, the mandate, the ordering rules, the gate count, not topical resemblance. Good: a probe flagging a styled onboarding page with zero code citations. Bad: dark-themed wikis counted as skill adoption. Borderline: a diluted retelling that links the original, recoverable, route readers to the source.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe and clock. The decay structure consolidated from this document's preceding sections. Real fetishism rates in deployed use are unmeasured.

**Second-order consequence.** Fetishism is detectable through citations, the skill's exploration stage demands file paths and line numbers, so a styled page without code citations wears the format and skipped the pipeline, the same assumptions-thinness forensic the timeline theme found, comprehension leaving traces that decoration cannot fake.

**Revision decision.** Add the four rows with probes, the absence check, sweep scans, mandate-quotation checks in derivatives, and fetishism sampling, pages bearing the style checked for exploration evidence.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| context window loses plan | long-running session forgets early constraints or overwrites user intent | write checkpoint summaries and re-read plan before each destructive step |
| tool fanout outruns budget | parallel actions create conflicts, duplicate work, or unbounded external calls | cap fanout, assign ownership, and require merge/audit checkpoints |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide what a production failure is allowed to trigger and in which direction. The seed corpus-process bullets stand beside what the skill implies for production failures, exploration that cannot ground an act sends the author back to the code, not forward into prose, an act that will not reduce to one concept splits or returns to planning, a metaphor failing the fidelity test is replaced rather than qualified into mush, and a checklist failure at review reopens the failing stage, not just the failing markup.

**Recommended default and causal basis.** Treat gate failures as stage failures, split dense acts along the mystery-mechanics seam, and never qualify a broken metaphor. Every response routes failure backward through the pipeline because each stage's output is the next one's input, patching downstream decorates an upstream defect, the same dependency-chain logic the timeline theme's workflow enforces.

**Operating conditions and assumptions.** The producer can distinguish stage defects from surface defects, rushed reviews cannot. Failure-response doctrine for the practice, corpus-process retry rules stay in the seed bullets.

**Failure boundary and alternatives.** The erosion path is cosmetic repair, a failed metaphor gate patched by rewording the caption instead of remapping the concept, gate compliance restored while the pedagogical defect ships intact. Bounded alternatives and recoveries: the seed's bounded-retry bullets for corpus-workflow failures, the same shape at document grain.

**Counterexample, gotchas, and operational comparison.** Backward routing is cheap early and expensive after generation, template adaptation being the stage after which fixes get sticky, the checklist walked on a draft outline catching most failures before any markup exists. Good: a comparison-gate failure resolved by returning to exploration for the second approach's real behavior. Bad: a reworded caption over an unmapped concept. Borderline: salvaging a page's markup while replanning its acts, economical when the components genuinely survive.

**Verification, evidence, and uncertainty.** Audit failed reviews for backward routing and stage-level repairs. The pipeline's dependency order and gate structure read this session. How often gate failures trace to which stages is unrecorded.

**Second-order consequence.** An act that resists single-concept reduction is the practice's convergence signal, it usually means the author has bundled a mechanism with its motivation, the split almost always yielding a mystery act and a mechanics act, the failure containing its own repair instruction.

**Revision decision.** State the response rules, backward routing per gate, split-or-replan for dense acts, replace-not-qualify for failed metaphors.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide which understanding properties become visible and where the records live. The seed the seed's evidence bullets stand while the practice's observability is built into its artifacts, every explainer exposes its own comprehension trail, the acts declare the concept sequence, the citations pin each claim to file and line, the metaphors reveal what the author believed each mechanism was, and the navigation dots segment reader behavior, the page being simultaneously the lesson and the record of its author's understanding.

**Recommended default and causal basis.** Retain exploration notes beside the page and review misconceptions at the metaphor layer. The process forces the trail, a compliant page cannot hide its concept model because the act structure is the concept model, published, the same trace-by-construction property the timeline theme found in its output contract.

**Operating conditions and assumptions.** Pages are reviewed by someone who knows the code, unreviewed pages observe nothing. Observability of the practice, reader analytics belong to the metrics loop.

**Failure boundary and alternatives.** The observability hole is the exploration stage, its findings live in the final page only as citations, the intermediate understanding, what was explored and rejected, evaporating unless the author keeps notes, the page showing conclusions without the comprehension path. Bounded alternatives and recoveries: outcome-only review, judging pages by reader results and learning nothing about where authors go wrong.

**Counterexample, gotchas, and operational comparison.** The trail ages with the page, citations drift and metaphors outlive refactors, observability claims holding at creation time and decaying on the inventory's staleness clock. Good: a review correcting one metaphor's mapping and tracing it to a misread function. Bad: a rejected page with no statement of which act misteaches. Borderline: exploration notes kept privately, the path exists, accountability does not.

**Verification, evidence, and uncertainty.** Check that sampled pages are diagnosable from their act and metaphor structure alone. The citation requirement and structural transparency read this session. Retention practices for exploration notes are unrecorded anywhere.

**Second-order consequence.** A wrong explainer is more diagnosable than wrong prose, the metaphor layer externalizes the author's mental model so precisely that a reviewer can locate the exact misconception, the postal worker who batches, the USB device that polls, errors in the mapping pointing straight at errors in the understanding.

**Revision decision.** State the artifact-as-trace framing with the exploration notes as the deliberate extension.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture tool-call count, context files loaded, delegated tasks, retry count, and completion-audit outcome.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide how both performances are kept honest without a numeric gate. The seed the seed's latency packet stands beside the practice's real performance surface, page weight and rendering cost, scroll-reveal and CSS animations over long pages taxing low-end machines, and the reading performance the format exists for, comprehension per reader minute, the two performances trading against each other as visual richness grows.

**Recommended default and causal basis.** Stay within the format's implicit constraints and spot-check long pages on modest hardware. An explainer's product is reader understanding per unit attention, visual components buy engagement and cost load time, the format's CSS-only animation choice already encoding the trade, motion without JavaScript weight.

**Operating conditions and assumptions.** Pages stay single-file and framework-free, imported libraries void the implicit budget. The practice's performance doctrine, corpus-document measurement keeps the seed's packet.

**Failure boundary and alternatives.** The failure with no gate is the heavy page, checklist-compliant pages accumulating components until they stutter on the machines new hires actually have, the ten gates checking presence and behavior but never cost. Bounded alternatives and recoveries: formal performance budgets with measurement, heavier machinery the single-file constraint mostly obviates.

**Counterexample, gotchas, and operational comparison.** Syntax-highlighted code blocks are the sneaky weight, span-per-token markup multiplying DOM size on code-heavy pages, the concrete-before-abstract technique needing code excerpts, not code dumps. Good: an eight-act page scrolling smoothly on an old laptop. Bad: a component carnival stuttering through act two. Borderline: one animated diagram worth its cost, judgment call, test it where readers live.

**Verification, evidence, and uncertainty.** Review page smoothness and excerpt discipline in sampled artifacts. The CSS-only and template constraints read this session. No numeric weight guidance exists in the source.

**Second-order consequence.** The format's constraints are its performance budget in disguise, CSS-only animation, a single HTML file, no framework, the skill implicitly capping page weight by construction the way its act range caps narrative length, budget-by-constraint replacing budget-by-measurement.

**Revision decision.** State the dual-performance reading, rendering cost bounded, comprehension rate maximized, with the CSS-only choice as the model.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require tool-call budgets, timeout budgets, and a resumable journal when the workflow exceeds one focused session.
Leading indicator to measure: a new reader can apply the reference without asking the author for hidden context.
Failure signal to watch: the reference is a literature dump instead of a guided explanation.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what scale the format applies and how coverage grows past one page. The seed the seed's distributed-execution paragraphs stand while the format's own scale limits go unnamed, the act structure holds 5-8 concepts and one subsystem, whole-system explanations exceed a single page's arc, the format serves novice comprehension, not expert reference or lookup, and the single-file constraint caps how much interactivity the experience can carry before it becomes an application.

**Recommended default and causal basis.** Scope each page to one subsystem's arc and grow coverage as a federated collection. The boundaries follow the pedagogy, progressive disclosure works over one narrative arc, arcs do not concatenate into libraries, and lookup readers need indexes, not mysteries, the format optimizing one reading mode at the deliberate expense of others.

**Operating conditions and assumptions.** Subsystem boundaries are real, tangled architectures resist per-subsystem arcs until untangled. Scale limits of the format, corpus-execution splitting stays in the seed paragraphs.

**Failure boundary and alternatives.** The characteristic overreach is the epic, a fifteen-act page attempting the whole platform, the arc collapsing into a tour and the one-concept pacing into a syllabus, the format's strengths inverting past its stated range. Bounded alternatives and recoveries: documentation sites and books past the single-page frontier, heavier forms for genuinely larger arcs.

**Counterexample, gotchas, and operational comparison.** The novice-mode boundary cuts both ways, experts consulting an explainer for a forgotten detail will find it slow, pairing each page with conventional reference docs serving the lookup mode the format deliberately declines. Good: five bounded explainers and an index page covering a platform. Bad: one heroic page attempting the monolith. Borderline: a two-page arc with an explicit to-be-continued seam, workable federation, keep each arc whole.

**Verification, evidence, and uncertainty.** At reviews, check page scope against the act range and federation pattern. The act range and output convention read this session. Where exactly arcs stop compressing is judgment, not measurement.

**Second-order consequence.** The format scales by federation, not extension, the output convention already anticipating it, topic-name.html for multiple topics, a collection of bounded arcs with an index outperforming any single epic, the .prd/ELI5 directory being the intended macro-structure the way inflection points were the timeline theme's.

**Revision decision.** State the three boundaries, one-subsystem arcs, novice-mode reading, single-file interactivity.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Visual Explainer Skill Patterns stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal decay in this node's claims and on what clocks. The seed theme-name queries would surface visualization-library noise while this node's staleness has exact homes, the checksum, the sweep folders where a revised or team-adopted variant would land, the personal directory whose conventions may migrate, the template's continued absence, and the inspiration page's liveness, the one external fetch that would enrich the theme.

**Recommended default and causal basis.** Run the mechanical probes each cycle batched with the other personal-skill themes. A single-source personal skill decays by supersession and asset drift, not by ecosystem motion, its probes being archive operations plus one link check, the lightest probe set in the corpus alongside the timeline theme's.

**Operating conditions and assumptions.** The archive stays locally searchable, only the link check needs the network. Refreshing this node's evidence and labels, presentation-technology tracking is deliberately out of scope.

**Failure boundary and alternatives.** Undirected refreshing would miss a team-ified successor under a new path, the fingerprint that survives renames is the doctrine's distinctive vocabulary, metaphors first code second, one concept per act, the mystery opening, phrases any faithful revision carries. Bounded alternatives and recoveries: declaring the theme stable and unwatched, nearly defensible, the absence check alone justifies the cycle.

**Counterexample, gotchas, and operational comparison.** The vocabulary fingerprint's phrases are quotable enough to spread into unrelated documents, a sweep hit needing the process structure present, not just the slogans, to count as a successor. Good: a refresh logging checksum, no successor, template still absent, inspiration link alive. Bad: logging that visual documentation searches returned nothing new. Borderline: fetching the inspiration page for enrichment, valuable, propose it as a theme upgrade, not a staleness probe.

**Verification, evidence, and uncertainty.** Record each probe with its date and the claim it confirmed or updated. The theme topology and dependency set established this session. Future sweep organization is the standing hedge.

**Second-order consequence.** This theme closes the corpus's coverage, the ninety-ninth reference, and its refresh doctrine arrives already standardized, the probe template the recent themes converged on applying without modification, the corpus ending with its maintenance practices proven general enough to fit even its most unusual member.

**Revision decision.** Replace name queries with targeted probes, the checksum, a sweep scan on the vocabulary fingerprint, the absence check, and the inspiration-link check.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | visual explainer skill patterns official documentation best practices |
| `github_repository_query_phrase` | visual explainer skill patterns GitHub repository examples |
| `release_notes_query_phrase` | visual explainer skill patterns changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label, chain, and state each claim class here may be reused. The seed label definitions stand without this assignment's distinctive ledger, one mapped path read in full, 168 lines, checksum recorded, every stage, technique, component, and gate quoted from verified text, the template dependency verified absent from the archive, the inspiration link and all external URLs left unretrieved, and every effectiveness claim, retention, comprehension, completion, resting on design analysis alone.

**Recommended default and causal basis.** Before reusing a claim, declare it text fact, quoted doctrine, negative finding, synthesis, or design inference, and treat unlabeled claims as inference. A one-file theme permits total verification of the doctrine while its practical promise, that designed experiences teach better than prose, is precisely the kind of claim a text audit cannot touch, the cleanest fact-inference split since the timeline theme, plus one verified absence.

**Operating conditions and assumptions.** The file stays unedited and the template stays unvendored. Reuse rules for this document's claims, each claim travels with class and verification state.

**Failure boundary and alternatives.** The costliest mislabel would present the pedagogy's benefits as established results, no reader data exists anywhere in this evidence, the skill is a coherent design with an unmeasured effect, recommendable on coherence, not on outcomes. Bounded alternatives and recoveries: reader studies on produced explainers, the natural validation upgrade, beyond this corpus's scope.

**Counterexample, gotchas, and operational comparison.** The absence finding is time-stamped, not permanent, it means the template was not in the archive this session, reusing it as the template never existed overreads a snapshot into a history. Good: reusing the ten gates as quoted doctrine with the file cited. Bad: citing improved onboarding speed as an established result. Borderline: recommending the format on its design coherence, reasonable guidance with the inference label attached.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares its class cleanly. This assignment's ledger, one full read, one checksum, one verified absence, zero fetches. The format's real effect on comprehension remains unmeasured anywhere in this evidence.

**Second-order consequence.** This final theme's ledger adds the corpus's first verified absence to its evidence vocabulary, a claim checked and found missing rather than found present, the label set completing on the last assignment with presence, quotation, absence, synthesis, and inference all exercised, the boundary discipline finishing exactly as broad as the corpus needed.

**Revision decision.** Publish the strata explicitly, the text and checksum as same-session local facts, quoted rules and gates as archive doctrine, the template absence as a same-session negative finding, the arc and fetishism readings as this evolution's synthesis, and all teaching-effectiveness claims as labeled design inference.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
