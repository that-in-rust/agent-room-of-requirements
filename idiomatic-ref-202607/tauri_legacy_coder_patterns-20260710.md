# Tauri Legacy Coder Patterns

**Decision supported.** This section helps decide how to frame an ancestor doctrine that outlives its successor in detail. The seed no seed line conveys that this theme holds the ancestor generation of the corpus's Tauri coverage, a 1406-line doctrine authored as a Claude agent in the 202602 sweep, twin-copied byte-identically into a 202603 Codex directory as references material behind a 42-line lean entrypoint and a 26-line jump-point map, one month of skill-architecture evolution captured in the copy itself.

**Recommended default and causal basis.** Treat this theme as the historical deep layer, consult it when the successor's compression drops detail a task needs. The family exists in two sweeps because the author migrated packaging styles, the monolithic Claude agent became a lean-entrypoint-plus-references Codex skill, and the doctrine text crossed unchanged while everything around it was rebuilt.

**Operating conditions and assumptions.** All four copies remain at their archive paths, checksums verified this session. This reference covers the legacy generation, the successor family's four themes are already evolved and own the current doctrine.

**Failure boundary and alternatives.** Reading this theme as merely an older duplicate of the tauri-doctrine theme misses that this generation is fuller, its Pattern 22 carries working vitest mocking code the 470-line successor dropped to a table row. Bounded alternatives and recoveries: the successor doctrine theme for current-generation rules, this theme owns depth and lineage.

**Counterexample, gotchas, and operational comparison.** The Claude frontmatter's description field is literally the typo tauri-code-01, a one-character fossil that also distinguishes the generations at a glance. Good: citing Pattern 22's mocking code from here with its generation labeled. Bad: quoting this generation's rules as the family's latest word. Borderline: using the lean SKILL.md's default rules as current guidance, they match the successor's gates closely, verify against the newer file first.

**Verification, evidence, and uncertainty.** Confirm the lineage claims against both generations' texts before framing. All four files read with checksums this session plus the successor from assignment 61. Whether intermediate revisions existed between the two sweeps is unrecorded.

**Second-order consequence.** The archive captures a rare complete succession chain, rust-coder-01 critiqued in the premise check, this full doctrine built from it, then the successor compressed from this, three generations legible locally, and the losses at each hop, detail, code, a whole pattern heading, are measurable rather than remembered.

**Revision decision.** Open with the two-sweep lineage, the byte-identical migration, and the fuller-ancestor relationship to the successor.

**Retained seed evidence.** The exact theme title and framing remain unchanged. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

## Source Evidence Mapping Table

**Decision supported.** This section helps decide what the effective evidence set is once the cross-sweep twin collapses. The seed four local rows list paths without recording the topology, the 1406-line Claude file and the Codex references doctrine are byte-identical twins sharing one checksum, so four rows hold three distinct documents, the doctrine, the 42-line entrypoint, and the 26-line map, and the twins straddle two monthly sweeps.

**Recommended default and causal basis.** Cite the doctrine at its Codex references path as the family-structural location and note the Claude twin for lineage dating. The cross-sweep twin exists because the 202603 migration imported the 202602 doctrine unmodified, making the pair a dated marker of when the references-directory architecture was adopted.

**Operating conditions and assumptions.** No future sweep edits one twin without the other. Provenance for this theme's claims, the successor generation's provenance lives in its own themes.

**Failure boundary and alternatives.** Counting four independent sources would inflate corroboration, the effective evidence is one doctrine text, one entrypoint, one map, and their inter-generational relationships. Bounded alternatives and recoveries: treating the Claude copy as canonical since it is older, defensible for lineage questions, misleading for family-structure questions.

**Counterexample, gotchas, and operational comparison.** The seed's usage-role column labels all four rows as skill entrypoints, but only SKILL.md plays that role, the doctrine and map are depth and routing layers behind it. Good: three-document synthesis with the twin as a migration-fidelity check. Bad: four-source corroboration claims. Borderline: citing the sweep dates as authorship dates, sweeps capture state, not creation, label the inference.

**Verification, evidence, and uncertainty.** Re-run all four checksums and the twin comparison at each audit. Checksum runs and reads across both sweep directories this session. Whether the 202602 Claude copy was itself the original authoring venue.

**Second-order consequence.** Unlike assignment 62's within-directory twin, this twin spans sweeps, so its equality is evidence about migration fidelity, the author moved doctrine without editing it, and any future divergence between the copies would mean retroactive editing of a frozen sweep, a much stronger integrity signal than duplicate-file drift.

**Revision decision.** Annotate the rows with the shared checksum, the three-document effective count, and the cross-sweep twin relationship.

**Retained seed evidence.** The four local rows and three external rows with their column values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| source_location_path_key | source_category_artifact_type | source_authority_confidence_level | source_usage_synthesis_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/Tauri-coder-01.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202603/tauri-coder-01/SKILL.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202603/tauri-coder-01/references/doctrine.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202603/tauri-coder-01/references/reference-map.md | local_corpus_source_material | local_corpus_sourced_fact | skill entrypoint or reusable agent prompt |
| https://v2.tauri.app/ | external_research_source_material | external_research_sourced_fact | primary Tauri 2 application and security documentation |
| https://github.com/tauri-apps/tauri | external_research_source_material | external_research_sourced_fact | desktop runtime implementation and release surface |
| https://github.com/rust-lang/api-guidelines | external_research_source_material | external_research_sourced_fact | Rust-facing API and error design reference |

## Pattern Scoreboard Ranking Table

**Decision supported.** This section helps decide which generation to cite for scored-pattern questions at what depth. The seed three corpus-process rows stand where this generation carries the original 29-row scoreboard the successor inherited verbatim, capability minimalism at 98, serializable errors at 96, mutex state at 95, async handlers at 94, sidecar supervision at 93, with identical scores and justifications across both generations, and this generation backing each row with far fuller pattern sections.

**Recommended default and causal basis.** Cite scores from either generation as one event, and use this generation when the question needs the full section behind a row. The scores were computed here first under the same four-part rubric, correctness 35, security 25, operability 20, leverage 20, and the successor copied the table while compressing the sections behind it.

**Operating conditions and assumptions.** The ranking reflects one author's rubric at 202602, not community consensus. Ranking this generation's patterns and their inheritance, corpus-process patterns rank in the seed rows.

**Failure boundary and alternatives.** Treating the two generations' boards as independent corroboration double-counts, the successor's board is a copy, not a re-derivation, one scoring event wearing two files. Bounded alternatives and recoveries: the seed's process-tier scoreboard for corpus workflow ranking.

**Counterexample, gotchas, and operational comparison.** This generation numbers Pattern 22 properly with a heading and code, so table-to-heading reconciliation succeeds here, the mismatch documented in assignment 61 is a successor-compression artifact, not an authoring flaw. Good: citing the 98-scored capability pattern from the successor and drilling into this generation's fuller section when arguing an exception. Bad: presenting matching scores across generations as replication. Borderline: citing this generation's Pattern 22 score for mocked testing, correct here and heading-less in the successor, name the generation.

**Verification, evidence, and uncertainty.** Spot-check score identity across both generations' tables at audit. Both tables compared row by row across assignments 61 and 63. Whether any score was quietly reconsidered during migration is undocumented.

**Second-order consequence.** This generation's rubric section states no cut line while the successor added Cut line 81+, and the lowest admitted score here is 81, so the successor's line was reverse-engineered from this table's floor, documentation of an admission rule written only after the admissions were made.

**Revision decision.** Present the top tier with its scores and note the verbatim inheritance, one scoring event propagated across the family.

**Retained seed evidence.** The three scored seed rows with tier labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| pattern_identifier_stable_key | pattern_score_numeric_value | pattern_tier_adoption_level | pattern_failure_prevention_target |
| --- | ---: | --- | --- |
| `tauri_legacy_coder_patterns` | 95 | default_adoption_pattern_tier | Source Map First: Load local tauri legacy material before synthesizing coder patterns recommendations. |
| `tauri_legacy_coder_patterns` | 91 | default_adoption_pattern_tier | Evidence Boundary Split: Separate local facts, external facts, and inference before giving agent instructions. |
| `tauri_legacy_coder_patterns` | 88 | default_adoption_pattern_tier | Verification Gate Coupling: Attach each recommendation to at least one command, checklist, or review gate. |

## Idiomatic Thesis Synthesis Statement

**Decision supported.** This section helps decide what founding claim organizes this generation's twenty-nine patterns. The seed generic corpus formulas stand where this generation states the family's original thesis, for Tauri, reliability mostly comes from getting the boundaries right, naming five, the Rust command boundary, window capability boundary, filesystem scope boundary, sidecar process boundary, and persistence size boundary, closing that when those are explicit, typed, and least-privilege, the rest of the app gets dramatically calmer.

**Recommended default and causal basis.** Use the five boundaries as the diagnostic frame for any Tauri reliability question, then apply the successor's precedence policy within the located boundary. The five-boundary framing works because each names a place where a trust or resource domain changes hands, and desktop failures cluster at handoffs rather than inside computation.

**Operating conditions and assumptions.** The work is Tauri 2 app-layer development, both generations' scope statements agree on this. This generation's thesis and its succession, the precedence-rule thesis belongs to the successor's theme.

**Failure boundary and alternatives.** The successor replaced this thesis with a precedence rule, Tauri-specific rules first when they sharpen safety, losing the five-boundary enumeration, so consumers of only the successor never learn the map of where reliability actually lives. Bounded alternatives and recoveries: single-boundary obsession, hardening commands while permissions stay broad, the distributed version of the single-lens failure.

**Counterexample, gotchas, and operational comparison.** Dramatically calmer is a qualitative promise, the thesis predicts reduced chaos, not measured reliability figures, quoting it as a quantified claim overreaches the text. Good: triaging a desktop bug by asking which of the five boundaries it crosses. Bad: citing the boundary map as the successor's current thesis. Borderline: adding an update trust boundary as a sixth, the updater pattern supports it, label it an extension.

**Verification, evidence, and uncertainty.** Audit reliability discussions for boundary-located framing. The final thesis read verbatim this session and the successor's thesis from assignment 61. Why the succession dropped the enumeration is undocumented.

**Second-order consequence.** The two theses answer different questions, this one answers where reliability comes from, a diagnostic map, the successor's answers which rules win conflicts, a precedence policy, and a practitioner is better served holding both, the map to locate a problem and the policy to resolve doctrine collisions over it.

**Revision decision.** State the five-boundary thesis verbatim as the family's founding claim and note what the succession kept and dropped.

**Retained seed evidence.** The three labeled thesis statements remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`local_corpus_sourced_fact`: The local row for `tauri_legacy_coder_patterns` contains 4 source path(s), which should be treated as the first retrieval surface for this theme.
`external_research_sourced_fact`: The external source map provides public documentation, repository, or ecosystem analogues where available.
`combined_evidence_inference_note`: Reliable use of Tauri Legacy Coder Patterns comes from loading the local corpus first, checking public ecosystem guidance second, and converting both into verification-backed agent decisions.

## Local Corpus Source Map

**Decision supported.** This section helps decide how to navigate a three-layer legacy family efficiently. The seed heading signals name doctrine sections while the working map is the three-layer family, the 42-line entrypoint answers what to do, ten default rules and a five-step workflow, the 26-line map answers where to look, jump points clustering patterns by boundary, and the 1406-line doctrine answers why and how in full, with the entrypoint's workflow hardwiring the layer order, classify, skim the map, open only relevant doctrine sections.

**Recommended default and causal basis.** Enter through SKILL.md's workflow, route via the map's clusters, and open doctrine sections by pattern number. The three layers exist because a 1406-line doctrine is unloadable per task, the 202603 migration built the map and entrypoint precisely to make the big file navigable on demand.

**Operating conditions and assumptions.** The three files remain co-located so the relative links resolve. Navigation of this theme's three documents, successor-family navigation lives in its themes.

**Failure boundary and alternatives.** Entering the doctrine cold ignores the family's own use order, the map's three steps, chosen patterns first, then matching sections, then anti-patterns and TDD checks, exist because front-to-back reading of 1406 lines serves nobody. Bounded alternatives and recoveries: heading search directly against the doctrine, workable for readers who already know pattern numbers.

**Counterexample, gotchas, and operational comparison.** The map's rg search commands use paths relative to the skill root, run from elsewhere they silently return nothing, the same trap the successor family carries. Good: a sidecar task jumping via the map to Patterns 11, 19, and 25. Bad: pasting all 1406 lines into an agent context for a one-command fix. Borderline: skipping the map when the task names its pattern already, harmless, the map's job is done.

**Verification, evidence, and uncertainty.** Trace navigation claims to the map's clusters and use order at review. All three layers read in full this session. None about structure, the family was read wholesale.

**Second-order consequence.** The jump-point clusters group patterns exactly along the final thesis's five boundaries, commands and state, capabilities and filesystem, IPC transport, sidecars, lifecycle, so the little map operationalizes the thesis, and a reader who only ever sees the map still absorbs the boundary framing implicitly.

**Revision decision.** Annotate the map with the three-layer anatomy, the hardwired layer order, and the jump-point clusters as the doctrine's table of contents by boundary.

**Retained seed evidence.** The four local source rows with titles, heading signals, and usage roles remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| local_source_filepath_value | local_source_title_text | local_source_heading_signals | local_source_usage_role |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/Tauri-coder-01.md | tauri-coder-01 | Tauri Coder 01; Premise Check; Source Base; Selection Rubric; Coverage Verdict; Chosen Patterns | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202603/tauri-coder-01/SKILL.md | tauri-coder-01 | Tauri Coder 01; Overview; Workflow; Default Rules; Reference Use; Boundary Notes | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202603/tauri-coder-01/references/doctrine.md | tauri-coder-01 | Tauri Coder 01; Premise Check; Source Base; Selection Rubric; Coverage Verdict; Chosen Patterns | skill entrypoint or reusable agent prompt |
| agents-used-monthly-archive/codex-skills-202603/tauri-coder-01/references/reference-map.md | Reference Map | Reference Map; Jump Points; Section Search; Use Order | skill entrypoint or reusable agent prompt |

## External Research Source Map

**Decision supported.** This section helps decide which external claims this theme owns and how each decays. The seed three inherited agent-format anchors stand while this generation's external surface matches its successor's declared pedigree exactly, the same five Notes2026 documents, the same fourteen official-doc cross-check topics, plus generation-specific items, the sonnet model pin, the vitest and tauri-apps mocking API the Pattern 22 code imports, and the WebDriver macOS exclusion stated as a platform fact.

**Recommended default and causal basis.** Reuse the pattern judgments as archive doctrine and the API-level specifics as archive-dated code pending a compile check. The pedigree identity across generations confirms the succession copied the source base wholesale, one research effort feeding both files, while the code-bearing patterns here import real APIs the compressed successor no longer names.

**Operating conditions and assumptions.** An external window with a JavaScript toolchain eventually opens. External dependencies for freshness tracking, all URLs stayed unretrieved this evolution.

**Failure boundary and alternatives.** The volatile item unique to this generation is the mocking API surface, mockIPC, mockWindows, clearMocks from tauri-apps api mocks, and the macOS WebDriver exclusion, both platform facts frozen at 202602 that later Tauri releases may have changed. Bounded alternatives and recoveries: the seed's inherited AGENTS.md and Actions anchors, retained for ecosystem context.

**Counterexample, gotchas, and operational comparison.** The WebDriver macOS exclusion is stated inside a rules list where it reads as permanent, platform support lists are among the fastest-decaying claim types in the archive. Good: re-dating the mocking imports after a successful compile at an external window. Bad: citing the macOS exclusion as current platform truth. Borderline: reusing the vitest test shape as a generic pattern, the structure travels even if the imports drift, label the split.

**Verification, evidence, and uncertainty.** Record currency labels on any externally-facing reuse of API specifics. Zero fetches this assignment and the pedigree comparison across generations. Current Tauri mocking and WebDriver support state is unverified locally.

**Second-order consequence.** The code samples make this generation's external claims more falsifiable than the successor's, prose rules degrade gracefully but import statements either compile or do not, so this theme owns the family's most testable currency claims, one npm install away from verification at any external window.

**Revision decision.** Keep the inherited rows and add the generation-specific inventory, the mocking API imports, the WebDriver exclusion, and the model pin, each archive-dated.

**Retained seed evidence.** All three external rows with their boundary labels remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| external_source_url_value | external_source_name_text | external_source_usage_role | evidence_boundary_label_value |
| --- | --- | --- | --- |
| https://v2.tauri.app/ | Tauri documentation | primary Tauri 2 application and security documentation | external_research_sourced_fact |
| https://github.com/tauri-apps/tauri | Tauri repository | desktop runtime implementation and release surface | external_research_sourced_fact |
| https://github.com/rust-lang/api-guidelines | Rust API Guidelines | Rust-facing API and error design reference | external_research_sourced_fact |

## Anti Pattern Registry Table

**Decision supported.** This section helps decide which generation's registry text serves which dispute depth. The seed three corpus-process rows stand where this generation carries the original seven-entry registry the successor inherited, blocking async commands, borrowed async inputs, broad filesystem permissions, panicking command paths, Arc double-wrapping managed state, plugin-store as blob sink, and unchecked sidecar spawning, here each backed by fuller explanatory sections than the successor's one-liners.

**Recommended default and causal basis.** Screen Tauri code against the seven and pull this generation's fuller sections when a finding is contested. The registry originated in this generation as the premise check's answer to its predecessor's too-few anti-patterns complaint, so the list is a deliberate correction, sized and scored against the pattern board from the start.

**Operating conditions and assumptions.** Reviewers track which generation they quote, the entries match but the surrounding text differs. Tauri code-level rejections and their lineage, process-level rejections belong to the entrypoint themes.

**Failure boundary and alternatives.** Citing the successor's compressed entries when a dispute needs the fuller reasoning wastes this generation's surviving detail, the depth exists one hop back in the lineage. Bounded alternatives and recoveries: the gates theme's delivery-time rejections, the same hazards at checklist altitude.

**Counterexample, gotchas, and operational comparison.** The entries' stability does not extend to their examples, code snippets around the registry differ between generations, quote-matching against the wrong generation fails string checks while matching substance. Good: rejecting an event-spam stream with the successor's line and unpacking this generation's fuller section when challenged. Bad: treating the two registries as independent confirmations. Borderline: citing registry stability itself as evidence of importance, sound inference, label it as such.

**Verification, evidence, and uncertainty.** Confirm registry rows cite generation and pattern mirror at audit. Both generations' registries compared entry by entry this session. Real-world hit frequency of each entry is not recorded anywhere in the family.

**Second-order consequence.** The registry's stability across generations is its strongest credential, seven entries survived a compression that dropped a whole pattern heading and rewrote the thesis, meaning the author considered these seven the least negotiable content in fourteen hundred lines, survival through revision is revealed importance.

**Revision decision.** Add the seven entries with their pattern mirrors and note this generation as the registry's origin and fuller text.

**Retained seed evidence.** The three seed process rows with their detection signals remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| anti_pattern_failure_name | failure_cause_risk_reason | safer_default_replacement_pattern | detection_signal_review_method |
| --- | --- | --- | --- |
| context_free_summary_output | agent skips local corpus and produces generic advice | source_map_first_synthesis | verify every listed local path appears in the generated file |
| unsourced_recommendation_claims | guidance appears authoritative without source boundary | evidence_boundary_split_pattern | check for local, external, and inference labels |
| unverified_agent_instruction | recommendation cannot be checked by command or review gate | verification_gate_coupling | require concrete gate in generated reference |

## Verification Gate Command Set

**Decision supported.** This section helps decide what verification layers this generation demands and where its example lives. The seed document verifier text stands where this generation specifies two concrete verification layers, mocked frontend IPC tests using mockIPC, mockWindows, and clearMocks with a working vitest example, and runtime-backed Rust tests, plus the entrypoint's workflow step four, verify with the repo's normal Rust, frontend, and Tauri test or build commands, deferring exact invocations to the repo.

**Recommended default and causal basis.** Require both layers for changed Tauri boundaries and seed new mocked suites from the Pattern 22 example. The two-layer split exists because desktop IPC has two ends, the mocked layer catches frontend contract regressions without a desktop runtime while Rust tests exercise the backend against real state.

**Operating conditions and assumptions.** The frontend stack supports the tauri-apps mocking utilities, archive-dated APIs pending verification. The source's test-layer doctrine, this document's own structural verification keeps the seed's retained block.

**Failure boundary and alternatives.** Verifying only one end ships the classic desktop gap, green frontend mocks over a broken backend or green Rust tests under a drifted invoke contract. Bounded alternatives and recoveries: the corpus verifier command in the seed's retained block for this document's own checks.

**Counterexample, gotchas, and operational comparison.** The example's clearMocks-after-each rule is load-bearing, leaked mocks make later tests pass against stale handlers, the desktop cousin of the test-pollution hazard the debugging theme's find-polluter script hunts. Good: a new command landing with a mockIPC test and a Rust test in one change. Bad: WebDriver end-to-end runs standing in for both layers. Borderline: skipping the mocked layer for a backend-only refactor with an unchanged contract, defensible under the changed-boundary logic, record it.

**Verification, evidence, and uncertainty.** Check changed boundaries for both test layers at review. Pattern 22's code and the entrypoint workflow read verbatim this session. Current compatibility of the mocking API is unverified locally.

**Second-order consequence.** This generation is where the family's mocked-testing preference carries its only executable form, the successor's TDD checks and the gates file's command families both assume mocked boundary tests exist without showing one, so this theme owns the family's single runnable teaching example for its most repeated testing advice.

**Revision decision.** State the two-layer requirement with the vitest example's four rules, clear mocks after each test, mock IPC for command validation, mock events when testing event flows, mock windows for label-oriented logic.

**Retained seed evidence.** The seed's document verifier command block remains preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`verification_gate_command_set`: Run the repository verifier after editing this file.

```bash
python3 agents-used-monthly-archive/idiomatic-references-202606/tools/verify_reference_generation.py --stage final
```

## Agent Usage Decision Guide

**Decision supported.** This section helps decide when and how an agent consumes each layer of the legacy family. The seed four generic bullets stand where the lean entrypoint defines consumption in ten default rules and five workflow steps, an agent classifies the task into one of eight named buckets, skims the map, opens only matching doctrine sections, applies the defaults while matching the repo's runtime and plugin choices, and pauses explicitly when a requested change would weaken permissions or lifecycle safety.

**Recommended default and causal basis.** Run the five-step workflow per task, honor the interrupt, and open doctrine whenever a default rule meets resistance. The pause rule exists because agents comply by default, a user asking to widen a permission gets compliance unless the skill installs a stop, so step five converts least-privilege from a preference into an interrupt.

**Operating conditions and assumptions.** The consuming agent can resolve the relative reference links, the family assumes its directory layout. How agents consume this legacy family, successor consumption lives in its themes.

**Failure boundary and alternatives.** The characteristic misuse is defaults-only operation, applying the ten rules without ever opening doctrine, which works until the first edge case where the rule's boundary conditions live in the unopened pattern section. Bounded alternatives and recoveries: the successor entrypoint's mode system for spec-first packet work, a later and heavier consumption model.

**Counterexample, gotchas, and operational comparison.** The boundary notes scope the whole skill as partial guidance for plugin internals, mobile APIs, and non-Tauri shells, an agent consuming only the default rules never sees this disclaimer. Good: an agent pausing a widen-the-filesystem-scope request and proposing the scoped alternative. Bad: an agent applying default rules to a mobile-only API question the boundary notes exclude. Borderline: defaults-only operation for a trivial rename, acceptable, the buckets exist for changes that touch boundaries.

**Verification, evidence, and uncertainty.** Sample agent sessions for bucket classification and interrupt honoring. The entrypoint's workflow and boundary notes read verbatim this session. How often the interrupt fires in real use is unrecorded.

**Second-order consequence.** The step-five pause is the family's only rule that instructs an agent to resist its user, choose the least-privilege version explicitly even when the request points elsewhere, an early instance of the escalate-over-comply doctrine that later skills in this archive formalize, and evidence the author had already met compliant permission creep in practice.

**Revision decision.** Recast the section around the bucket-classify-route loop and the step-five interrupt as the family's agent-safety mechanism.

**Retained seed evidence.** The four seed usage bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`agent_usage_decision_guide`: Use this reference when a task mentions this theme, one of the listed local source paths, or a nearby technology/workflow surface.

- Start with the local corpus source map.
- Prefer patterns with explicit verification gates.
- Treat external sources as freshness and ecosystem checks, not replacements for local repo conventions.
- Preserve the evidence boundary labels when reusing recommendations.

## User Journey Scenario

**Decision supported.** This section helps decide what a well-run generational inheritance walk concludes. The seed one generic engineer stands where the natural journey is an inheritance decision, a maintainer holding a Tauri app built under this legacy family discovers the successor exists, walks both generations, finds the scores and registries identical, the thesis reframed, Pattern 22's code surviving only here, and decides what to migrate, adopting the successor's gates for delivery while keeping this generation bookmarked for depth.

**Recommended default and causal basis.** Adopt the newest generation's checking surfaces and keep the ancestor accessible for depth the compression dropped. The journey arises because archives accumulate generations, and every consumer of an old skill eventually faces the migrate-or-stay question with real switching costs on both sides.

**Operating conditions and assumptions.** Both generations stay archived and their relationship stays documented, this reference is that documentation. Journeys through this theme's generational boundary, single-generation usage journeys live in the successor's themes.

**Failure boundary and alternatives.** The journey's failure branch is wholesale replacement, discarding the legacy family entirely and losing the mocking example and fuller sections the successor never carried forward. Bounded alternatives and recoveries: the successor themes' design-time and delivery-time journeys for within-generation work.

**Counterexample, gotchas, and operational comparison.** The generations' near-identity tempts consumers to deduplicate the archive, deleting the ancestor as redundant, which would destroy the only copy of the mocking example and the five-boundary thesis. Good: a team gating on the successor's commands while training new hires on this generation's worked patterns. Bad: deleting the legacy directory after adopting the successor. Borderline: freezing on the legacy generation entirely, coherent but forfeits the successor's gate file and routing improvements.

**Verification, evidence, and uncertainty.** Audit adoption decisions for layered rather than wholesale outcomes. The cross-generation comparison performed across assignments 61 and 63. Real switching costs vary by team and are unrecorded.

**Second-order consequence.** The journey's rational endpoint is layered adoption rather than migration, the generations disagree on almost nothing, so the choice is not which is right but which layer serves which moment, current gates from the successor, deep rationale and test scaffolding from here, the succession added altitude without invalidating the base.

**Revision decision.** Recast the scenario as the inheritance walk with its comparison milestones, scores, registries, theses, and the Pattern 22 delta.

**Retained seed evidence.** The role, starting state, decision, and trigger lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Role based opening scenario: The Tauri desktop app team is starting from a frontend action that must cross into Rust commands and platform permissions and needs a reference that turns source evidence into an executable next step.
Primary user starting state: The user has a `tauri_legacy_coder_patterns` task, one or more local source paths, and uncertainty about which pattern should drive implementation.
Decision being made: choosing the safest command, permission, IPC, packaging, and verification boundary.
Reference opening trigger: Open this file when the task mentions tauri legacy coder patterns, any mapped local source path, or an adjacent workflow with the same failure mode.

## Decision Tradeoff Guide

**Decision supported.** This section helps decide which packaging side wins under which constraint across the lineage. The seed template rows skip the trade-offs this generation adjudicates, detail versus loadability, resolved by the 202603 migration splitting entrypoint from doctrine, code examples versus compression, resolved here for code and in the successor against it, monolith versus references directory, resolved by one month of real usage between sweeps, and preservation versus pruning in succession, resolved by keeping scores and registries while dropping detail.

**Recommended default and causal basis.** When packaging doctrine, keep the incompressible layers, code, worked examples, enumerations, reachable even if the lean form omits them. Each trade-off is visible because the archive holds both sides, the same author chose differently at different times under different constraints, making this theme a record of trade-off resolution rather than trade-off theory.

**Operating conditions and assumptions.** The consumer's context budget is known, the whole trade-off pivots on what fits per task. Trade-off structure the lineage teaches, code-level Tauri trade-offs belong to the doctrine content itself.

**Failure boundary and alternatives.** Reading the later choices as corrections misreads succession, the successor did not fix this generation, it repackaged it for a workflow where context budget mattered more than completeness. Bounded alternatives and recoveries: single-generation archives that overwrite in place, cheaper and history-blind.

**Counterexample, gotchas, and operational comparison.** The twin doctrine inside the 202603 directory shows the migration hedged, the references layer is the monolith unedited, splitting was additive, not destructive, the pruning happened only in the next generation. Good: a new skill shipping lean with its full source retained as references. Bad: compressing away the only executable example of a core recommendation. Borderline: dropping the five-boundary thesis for a precedence rule, defensible repackaging with real loss, exactly why both generations stay cited.

**Verification, evidence, and uncertainty.** Review packaging decisions for incompressible-layer reachability. The three-generation packaging sequence read across sweeps this session. The author's actual constraints between sweeps are inferred from artifacts.

**Second-order consequence.** The lineage shows compression's real cost structure, scores and lists survived intact because they compress losslessly, worked code and enumerended theses vanished because they do not, so what a compression drops is predictable from content shape, and archives should expect to keep ancestors alive precisely for the incompressible parts.

**Revision decision.** Add the four trade-off rows each phrased as the choice, the constraint that drove it, and the sweep where it flipped.

**Retained seed evidence.** The adopt, adapt, avoid, and cost-of-being-wrong rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| decision_option_name | when_to_choose_condition | tradeoff_cost_description | verification_question_prompt |
| --- | --- | --- | --- |
| Adopt when | local corpus and external evidence agree on the tauri legacy coder patterns pattern | fastest path, but can copy stale local assumptions | Does the selected pattern appear in the canonical source and current external evidence? |
| Adapt when | local sources are strong but public ecosystem guidance has changed | preserves repo fit, but requires explicit inference notes | Did the reference label the local fact, external fact, and combined inference separately? |
| Avoid when | source evidence is thin, conflicting, or unrelated to the user journey | prevents false confidence, but may require deeper research | Is there a confidence warning or adjacent reference route? |
| Cost of being wrong | wrong tauri legacy coder patterns guidance can send an agent to the wrong files, tests, or abstraction | wasted implementation loop and weaker verification | Would a reviewer know what to undo and what to inspect next? |

## Local Corpus Hierarchy

**Decision supported.** This section helps decide which generation's wording governs which question class. The seed role labels rank four paths while the operative hierarchy is generational, within this family the entrypoint orchestrates, the map routes, and the doctrine grounds, and across the archive this whole family sits one generation beneath the tauri-executable-specs-01 family whose doctrine explicitly claims to preserve this one's content.

**Recommended default and causal basis.** Govern current practice by the successor, resolve depth and lineage questions here, and cite the preservation line when bridging them. Succession creates a precedence default, newer generations govern current practice because they encode later judgment, while ancestors retain authority over their own fuller text and over lineage questions the successor cannot answer about itself.

**Operating conditions and assumptions.** Both families remain co-archived at their sweep versions. Precedence within and across the two tauri families, the successor's internal hierarchy was mapped in its themes.

**Failure boundary and alternatives.** Flat ranking across generations produces contradictions that are not real, the theses differ by reframing, not disagreement, and treating them as rivals invents a conflict the lineage does not contain. Bounded alternatives and recoveries: treating each sweep as an independent authority, which fragments one evolving doctrine into false rivals.

**Counterexample, gotchas, and operational comparison.** The preservation claim is slightly generous, the successor preserved the table and registries but not Pattern 22's heading or the thesis, preserved means curated, not copied. Good: citing the successor for current gate rules and this family for why the mutex rule was scored 95. Bad: quoting this generation's thesis as the family's current position. Borderline: citing the preservation line as proof of full content equality, it claims continuity, verify the specific content survived.

**Verification, evidence, and uncertainty.** Test citation targets against the dual structure at audit. The preservation line and both families' texts read across assignments. None material, both families are fully enumerated and evolved.

**Second-order consequence.** The successor's opening line, preserving the scored patterns from tauri-coder-01, is the archive's only explicit inter-family citation, making this pair the one place where precedence between families is declared by the files themselves rather than inferred by this corpus, a self-documenting lineage.

**Revision decision.** State the dual structure, internal three-layer ranking and cross-family generational ranking with the preservation claim as the link.

**Retained seed evidence.** The hierarchy rows and the classification vocabulary remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Classification vocabulary includes canonical, supporting, legacy, duplicate, and conflicting source roles.

| local_source_filepath_value | corpus_hierarchy_role | heading_signal_to_convert | reviewer_question_to_answer |
| --- | --- | --- | --- |
| agents-used-monthly-archive/claude-skills-202602/Tauri-coder-01.md | canonical primary source | Tauri Coder 01; Premise Check; Source Base | What guidance, warning, or example should this source contribute to Tauri Legacy Coder Patterns? |
| agents-used-monthly-archive/codex-skills-202603/tauri-coder-01/SKILL.md | legacy historical source | Tauri Coder 01; Overview; Workflow | What guidance, warning, or example should this source contribute to Tauri Legacy Coder Patterns? |
| agents-used-monthly-archive/codex-skills-202603/tauri-coder-01/references/doctrine.md | supporting detail source | Tauri Coder 01; Premise Check; Source Base | What guidance, warning, or example should this source contribute to Tauri Legacy Coder Patterns? |
| agents-used-monthly-archive/codex-skills-202603/tauri-coder-01/references/reference-map.md | supporting detail source | Reference Map; Jump Points; Section Search | What guidance, warning, or example should this source contribute to Tauri Legacy Coder Patterns? |

## Theme Specific Artifact

**Decision supported.** This section helps decide what standing structure makes the succession safely quotable. The seed generic worked-example artifact misses this theme's natural object, a succession delta ledger, a table with one row per doctrine component, table, rubric, verdict, non-negotiables, each pattern, registry, TDD checks, thesis, holding its state in this generation, its state in the successor, and a survived, compressed, or dropped verdict with the notable deltas quoted.

**Recommended default and causal basis.** Build the ledger once from both texts, store it with the audit records, and re-derive it if either family's checksums move. Every cross-generation question, did the scores change, where did the mocking code go, which thesis is current, resolves to a row lookup once the ledger exists, and reconstructing it per question costs a 1406-against-470-line comparison each time.

**Operating conditions and assumptions.** Component alignment stays feasible, both generations share section vocabulary almost entirely. One ledger for this succession, the pattern generalizes to any future generation pair in the archive.

**Failure boundary and alternatives.** Without the ledger, succession knowledge lives in whoever last compared the files, and casual consumers assume either full preservation or full replacement, both wrong in documented ways. Bounded alternatives and recoveries: on-demand comparison per question, quadratically wasteful across the family's consumers.

**Counterexample, gotchas, and operational comparison.** Row granularity matters, a pattern-level ledger shows survived for Pattern 2 while its rules text was compressed fivefold, pair each verdict with a size delta to keep survived honest. Good: a ledger row reading Pattern 22, heading dropped, table row survived, code lost, cite legacy. Bad: a ledger marking the thesis survived because both files end with one. Borderline: including the entrypoint generations in the same ledger, different component sets, better as a second table.

**Verification, evidence, and uncertainty.** Re-derive sampled ledger rows against both texts at each audit. The component comparison performed across assignments 61 and 63. Future revisions of either family would obsolete rows unpredictably.

**Second-order consequence.** The ledger's verdict distribution is itself a finding, mostly survived, a few compressed, exactly two dropped, the pattern heading and the thesis enumeration, quantifying that this succession was conservative curation rather than rewrite, a number that settles the migrate-or-stay journey's central anxiety.

**Revision decision.** Define the artifact as the component-aligned succession table with per-row verdicts and quoted deltas.

**Retained seed evidence.** The artifact field rows with completion rules and evidence hints remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Theme specific artifact: worked tauri legacy coder patterns example with user goal, decision point, failure mode, and verification gate.

| artifact_field_name | artifact_completion_rule | evidence_source_hint |
| --- | --- | --- |
| user_goal_statement | state the user's concrete goal before applying Tauri Legacy Coder Patterns | local corpus hierarchy plus critique findings |
| decision_boundary_rule | define the point where this reference should be used or avoided | decision tradeoff guide |
| verification_gate_rule | define the command, checklist, or review question that proves the artifact worked | verification gate command set |

## Worked Example Set

**Decision supported.** This section helps decide which exercises transmit the family's incompressible knowledge fastest. The seed abstract usage lines stand where this generation is the family's example vault, Pattern 22's complete vitest mocking test with its four usage rules, Pattern 1's async command in fuller form than the successor kept, and the map's jump-point clusters as a worked example of routing a 1406-line file through 26 lines.

**Recommended default and causal basis.** Certify family understanding by adapting the mocking test to a new command, preserving all four usage rules. This generation predates the compression, so it holds the family's teaching material in original density, and its examples are the ones the successor's terse forms silently assume the reader has seen.

**Operating conditions and assumptions.** Adaptations are checked against the four rules, a mock without clearMocks passes review only as a spotted deliberate error. Teaching this theme's material, the successor themes' drills reference these examples as their missing originals.

**Failure boundary and alternatives.** Teaching the family from the successor alone produces rule-followers who have never seen the rules applied, the mocking preference without the mock, the async mandate without the handler. Bounded alternatives and recoveries: live-project examples, richer and outside local evidence.

**Counterexample, gotchas, and operational comparison.** The example returns a JSON string, not an object, matching how the settings command serializes, adapters who return objects change the contract under test without noticing. Good: an adapted test mocking a new command with cleared mocks and label-scoped windows. Bad: a mocked suite sharing handlers across tests without cleanup. Borderline: extending the example with event mocking via shouldMockEvents, supported by the rules, label the extension.

**Verification, evidence, and uncertainty.** Grade adaptations against the four usage rules at review. Pattern 22's code read verbatim this session. Transfer from adaptation drills to real suite quality is unmeasured.

**Second-order consequence.** The mocking example teaches a subtlety the prose never states, the mock handler validates the command name and returns typed data, meaning the test pins the invoke contract itself, so Pattern 22 quietly implements Pattern 7's typed-contract rule in test form, patterns interlocking in code that no compressed one-liner can show.

**Revision decision.** Anchor the section on the mocking test as the flagship example, with the clearMocks rule and label-oriented mockWindows usage as its teachable details.

**Retained seed evidence.** The good, bad, and borderline seed lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Good example: Use Tauri Legacy Coder Patterns after loading the canonical source, confirming the external evidence boundary, and writing a verification gate before implementation.
Bad example: Use Tauri Legacy Coder Patterns as a generic tutorial while ignoring the mapped local paths, source hierarchy, and cost of being wrong.
Borderline case: Use Tauri Legacy Coder Patterns only after adding a confidence warning when local evidence is thin or conflicts with current ecosystem guidance.

## Outcome Metrics and Feedback Loops

**Decision supported.** This section helps decide which numbers show the ancestor earning its archive space. The seed compile-centric indicator misses this theme's measurable surface, legacy-consultation frequency, how often depth questions route back to this generation, delta-ledger hit rate, which succession rows actually get asked about, mocking-example adoption, suites seeded from Pattern 22's test, and deduplication pressure, attempts to prune the ancestor as redundant caught before deletion.

**Recommended default and causal basis.** Track the four measures per cycle and treat sustained zero consultation as evidence to re-examine, not just archive noise. A legacy theme earns its keep by being consulted, and only usage data distinguishes a load-bearing ancestor from sentimental clutter the archive should honestly prune.

**Operating conditions and assumptions.** Consultations are attributable to generations, citation tags make this cheap. Measuring this legacy theme's operation, the successor's usage metrics live in its themes.

**Failure boundary and alternatives.** Without the loop, the theme's fate is decided by vibes, either kept forever unread or deleted in a cleanup sweep, both endpoints uninformed. Bounded alternatives and recoveries: the successor themes' citation-index metrics, measuring the living generation.

**Counterexample, gotchas, and operational comparison.** Consultation via the successor's compressed content is invisible to this metric, the ancestor's ideas circulate without its name, undercounting is structural. Good: a quarter showing three depth consultations and one blocked deletion attempt. Bad: pruning the family because no one cited it while its patterns circulate renamed. Borderline: high consultation driven by one confused reader, volume without breadth, read the distribution.

**Verification, evidence, and uncertainty.** Confirm citation records carry generation tags at each cycle. The succession structure and citation conventions established this session. No usage baseline exists, the loop starts unprimed.

**Second-order consequence.** Legacy-consultation frequency should decay slowly and never to zero if the succession analysis is right, the incompressible parts guarantee residual demand, so a true zero over many cycles would falsify this theme's central claim and justify pruning, the metric doubles as an ongoing test of the analysis.

**Revision decision.** Adopt the four usage-derived measures with deduplication pressure as the existential alarm.

**Retained seed evidence.** The leading indicator, failure signal, and review cadence lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Leading indicator: the feature can be tested across frontend, Rust command, and packaged desktop behavior.
Failure signal: the reference ignores permissions, IPC risk, or platform-specific verification.
Review cadence: Re-run the verifier after every generated-reference edit and refresh external sources when public APIs, docs, or tooling releases change.

## Completeness Checklist

**Decision supported.** This section helps decide what must be confirmed before this synthesis is declared faithful. The seed shape items never audit this theme's distinctive claims, the cross-sweep twin equality, the three-document effective count, the Pattern 22 presence with code, the no-cut-line rubric difference, the five-boundary thesis, the frontmatter typo, and the succession deltas against the assignment 61 findings.

**Recommended default and causal basis.** Run all distinctive checks wholesale each cycle, paired with the successor theme's audit, and date the results. The synthesis rests on inter-generational comparisons that a change to either family silently invalidates, so the audit must span both families every cycle.

**Operating conditions and assumptions.** Both families' files remain at their sweep paths. Auditing this reference against its four mapped paths and the successor doctrine, all read this or the prior assignments.

**Failure boundary and alternatives.** A shape-only audit passes while a sweep touches either generation, breaking delta claims this reference states as facts about frozen texts. Bounded alternatives and recoveries: sampled auditing, ill-suited to claims about exact cross-file equality and specific line-level survivals.

**Counterexample, gotchas, and operational comparison.** Twin equality here spans directories from different months, a sweep-restructuring that moves either copy breaks the check's paths before it breaks the content, distinguish path failure from content drift. Good: an audit logging four checksums, twin equality, Pattern 22 intact, and three re-verified deltas. Bad: declaring fidelity from seed-shape checks alone. Borderline: skipping delta re-verification when both families' checksums are unchanged, defensible, note the shortcut.

**Verification, evidence, and uncertainty.** Execute the distinctive items and date each result in the audit record. The claim structure of every distinctive statement in this document. Future sweep restructuring is the main structural unknown.

**Second-order consequence.** This audit and the successor theme's audit are mirror-dependent, each cites deltas against the other's source, so the corpus now contains audit pairs that should run together, a coupling the checklist must name or the two audits will drift apart on shared facts.

**Revision decision.** Add the distinctive items, all four checksums, twin equality, the Pattern 22 heading-and-code check, the rubric difference, the thesis wording, and sampled succession-delta re-verification.

**Retained seed evidence.** All seven seed checklist bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- The role scenario names the user, starting state, decision, and trigger for Tauri Legacy Coder Patterns.
- The decision guide includes Adopt when, Adapt when, Avoid when, and Cost of being wrong.
- The local corpus hierarchy identifies canonical and supporting sources or gives a confidence warning.
- The theme specific artifact is concrete enough to review without reading every mapped source.
- The examples cover good, bad, and borderline usage.
- The metrics section names one leading indicator and one failure signal.
- The adjacent routing section points to a better reference when this one is not the right fit.

## Adjacent Reference Routing

**Decision supported.** This section helps decide where to send a question arriving at the legacy theme. The seed token-split rows point nowhere while this theme's routing is generational, current-practice questions go forward to the successor family's four themes, entrypoint, doctrine, gates, and their shared map, depth and lineage questions stay here, mocked-testing scaffolding questions stay here as the only code-bearing source, and generic Rust questions route sideways to the Rust themes as always.

**Recommended default and causal basis.** Answer lineage and depth questions here and forward current-practice questions to the successor themes explicitly. Succession converts most incoming traffic into forwarding, the majority of questions want current doctrine and belong one generation up, while the residual depth demand is exactly what justifies the ancestor's retention.

**Operating conditions and assumptions.** The successor themes remain complete, verified through this assignment. Sending away what this theme holds only incidentally, destination doctrine is deliberately not restated.

**Failure boundary and alternatives.** Answering current-practice questions from the legacy text risks serving superseded packaging, while forwarding depth questions upward loses the fuller text the successor no longer has. Bounded alternatives and recoveries: for platform questions neither generation answers, nothing local, say so rather than stretching.

**Counterexample, gotchas, and operational comparison.** Questions about the missing Pattern 22 arrive at the successor theme naturally, its documented irregularity, but resolve here where the pattern exists in full, the forwarding runs backward for exactly this one address. Good: forwarding a current gate question to the gates theme while answering the thesis-history question here. Bad: teaching the legacy default rules as the family's current entrypoint. Borderline: a mutex question, the rules match across generations, answer from either with the generation named.

**Verification, evidence, and uncertainty.** Sample recent answers for temporally-consistent routing. The five-theme Tauri map completed across three assignments. None material, both generations and all themes are enumerated.

**Second-order consequence.** With this assignment the corpus's Tauri coverage closes completely, five themes spanning two generations, and the routing between them reproduces the archive's own structure, forward edges for succession, downward edges for depth, so a question can enter at any Tauri theme and reach its answer by following declared edges rather than searching.

**Revision decision.** Route by temporal intent, what does the family currently say goes forward, what did it originally say and why stays here, and show me working test code stays here.

**Retained seed evidence.** The three seed adjacent-reference lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Adjacent reference guidance: Use executable, doctrine, or legacy Tauri references when the task is about specs, policy, or migration.
Adjacent reference 1: consider the tauri adjacent reference when the current task pivots away from tauri legacy coder patterns.
Adjacent reference 2: consider the legacy adjacent reference when the current task pivots away from tauri legacy coder patterns.
Adjacent reference 3: consider the coder adjacent reference when the current task pivots away from tauri legacy coder patterns.

## Workload Model

**Decision supported.** This section helps decide how to budget a theme whose reads are rare and whose audits are coupled. The seed symbol-count boundary misprices this node's work, whose units are the sunk 1474-line family read in minutes, rare per-question depth consultations of one pattern section each, the one-time succession-ledger construction requiring the full cross-generation comparison, and paired audits that must walk both families, the corpus's first structurally-coupled audit budget.

**Recommended default and causal basis.** Budget consultations as negligible, the ledger as one-time, and audits at paired-walk rates alongside the successor theme. A legacy theme's recurring cost is nearly zero by design, consultations are rare and narrow, but its audit cost is doubled by the mirror-dependency with the successor theme, an ongoing tax the succession analysis imposed.

**Operating conditions and assumptions.** Audit pairing is honored, the coupled budget assumes both walks happen together. Budgeting this node's upkeep, the successor family's own costs live in its themes' models.

**Failure boundary and alternatives.** Budgeting this theme's audit as a single-family walk misses the pairing, and an unpaired audit can certify delta claims the other family's drift has already falsified. Bounded alternatives and recoveries: the seed's crate-slice and symbol boundaries, retained as outer limits on any single consultation's scope.

**Counterexample, gotchas, and operational comparison.** The 1406-line doctrine reads slower than its line count suggests, twenty-two of its patterns carry code or contracts that reward careful reading, budget it as dense. Good: an audit slot sized for both tauri doctrines walked together. Bad: a consultation budget assuming frequent full rereads of 1406 lines. Borderline: skipping the paired walk when only this family's checksum is being verified, acceptable for pure integrity checks, not for delta claims.

**Verification, evidence, and uncertainty.** Check audit records for pairing and duration proportionality. The line counts and coupling structure from this session. Real consultation frequency depends on Tauri work volume, unrecorded here.

**Second-order consequence.** Succession shifts cost from consultation to audit, the ancestor gets read less but checked more elaborately because every claim about it is now relational, and archives planning multi-generation retention should expect audit cost to scale with lineage depth even as read cost falls.

**Revision decision.** Restate the model in its four units, sunk read, narrow consultations, ledger construction, and paired audits.

**Retained seed evidence.** The workload dimension rows with boundary values remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

`combined_evidence_inference_note`: Treat Tauri Legacy Coder Patterns as a rust system operating reference, not as a prose summary.

| workload_dimension_name | workload_boundary_value | verification_pressure_point |
| --- | --- | --- |
| primary_usage_surface | systems implementation, CLI or service hardening, async/runtime review, and safety-sensitive refactoring where compile success is necessary but not sufficient | verify that the reference changes the next implementation or review action |
| bounded_capacity_model | one crate or bounded workspace slice, up to 100 changed symbols, and one integration path that exercises error handling | split the task or create a narrower reference when this boundary is exceeded |
| source_pressure_model | local heading signals include Tauri Coder 01; Premise Check; Source Base; Selection Rubric; Coverage Verdict; Chosen Patterns; Tauri Coder 01; Overview; Workflow; Default Rules; Re | compare guidance against canonical local sources before following external examples |
| artifact_pressure_model | required artifact is worked tauri legacy coder patterns example with user goal, decision point, failure mode, and verification gate | require the artifact before claiming the reference is operationally usable |

## Reliability Target

**Decision supported.** This section helps decide which invariants must demonstrably hold for this node's claims to stay citable. The seed document-property thresholds stand where this node's invariants should, cross-sweep twin equality, generation-attribution fidelity keeping every quote tagged to its sweep, succession-delta accuracy against both frozen texts, Pattern 22 integrity as the family's only code-bearing copy, and lineage-claim consistency with the successor theme's documented irregularities.

**Recommended default and causal basis.** Hold all five invariants at paired-audit cadence with delta re-verification weighted heaviest. Each invariant guards a distinct corruption, twin forks break migration-fidelity evidence, missing tags blur generations into folklore, stale deltas misreport what succession preserved, code loss destroys the incompressible layer, and lineage inconsistency splits the paired audits.

**Operating conditions and assumptions.** Both families stay readable at their paths and the successor themes stay complete. Invariants for this node's claims, reliability of Tauri applications belongs to their teams.

**Failure boundary and alternatives.** The costliest breach is succession-delta drift, a delta claim like the successor dropped the thesis enumeration surviving after a hypothetical successor revision restores it, leaving this reference confidently wrong about the living generation. Bounded alternatives and recoveries: fact-only invariants, blind to the anachronism drift that matters most here.

**Counterexample, gotchas, and operational comparison.** The twin equality invariant spans sweeps, so a legitimate future sweep of the 202602 directory could touch the Claude copy's metadata without malice, distinguish sweep mechanics from content edits before filing a fork. Good: an audit re-verifying three sampled deltas against both texts with dates. Bad: a summary quoting the successor's cut line as this generation's rubric. Borderline: paraphrasing shared non-negotiables without a generation tag, faithful since they match, tag anyway for the audit trail.

**Verification, evidence, and uncertainty.** Review the five invariant series at each corpus checkpoint with dated evidence. The claim structure established across this document. None about current truth, all five invariants were verified during this evolution.

**Second-order consequence.** Generation attribution is the invariant unique to lineage themes, single-source themes decay by paraphrase and multi-dialect themes by blur, but lineage themes decay by anachronism, a later rule quoted as original or an original quoted as current, so the probe must check dates and directions, not just words.

**Revision decision.** Publish the five invariants with evidence methods, checksum runs, tag audits, delta re-verification, code-presence checks, and cross-theme consistency reviews.

**Retained seed evidence.** All four seed reliability rows with thresholds remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| reliability_target_name | measurable_threshold_value | evidence_collection_method |
| --- | --- | --- |
| source_boundary_preservation | 100 percent of recommendations keep local, external, and inference boundaries visible | review generated text for the three evidence labels before reuse |
| decision_accuracy_sample | at least 18 of 20 sampled uses route to the correct adopt, adapt, avoid, or adjacent-reference decision | sample downstream tasks and record reviewer verdicts |
| unsupported_claim_rate | zero unsupported production, security, latency, or reliability claims in final guidance | reject claims without source row, explicit inference note, or verification method |
| recovery_path_clarity | every avoid or failure case names the rollback, escalation, or next-reference route | inspect failure-mode and adjacent-routing sections together |

## Failure Mode Table

**Decision supported.** This section helps decide which decay modes need standing probes for a retained ancestor theme. The seed generic rows miss how this node specifically dies, ancestor pruning, the legacy directory deleted as redundant with its incompressible content lost, generation blur, quotes circulating without sweep tags until the two doctrines merge in memory, delta rot, succession claims surviving revisions to either family, and example decay, the mocking code aging into uncompilable folklore nobody re-tests.

**Recommended default and causal basis.** Run the four probes at each paired audit and keep the retention rationale discoverable from the archive's cleanup paths. All four attack what retention was for, the unique content, the lineage clarity, the comparative claims, and the executable example that justify keeping fourteen hundred lines the successor mostly duplicates.

**Operating conditions and assumptions.** Cleanup processes consult documentation before deletion, an assumption worth verifying itself. Decay of this node's claims and reuse, Tauri application failures belong to the doctrine's own registry.

**Failure boundary and alternatives.** They compound, a pruned, blurred, rotten lineage leaves the corpus citing succession facts about files nobody can check, history claims with the history deleted. Bounded alternatives and recoveries: quotation-only reuse with sweep tags and line anchors, resistant to blur and rot, helpless against pruning.

**Counterexample, gotchas, and operational comparison.** Delta rot is asymmetric, revisions to the living successor are likelier than edits to the frozen ancestor, so the probe should watch the successor's checksum as this theme's early warning, drift arrives from the file this theme does not own. Good: a probe catching a dedup proposal against the twin before deletion. Bad: a wiki merging both generations' rules under one undated heading. Borderline: a reuse quoting the shared registry without tags, low harm since the entries match, still blur in the audit's terms.

**Verification, evidence, and uncertainty.** Confirm each failure row names its probe and trigger point. The decay structure consolidated from this document's preceding sections. Cleanup-process behavior toward documented retention is untested.

**Second-order consequence.** Ancestor pruning is the mode with an institutional accomplice, storage hygiene, deduplication tooling flags the twin as waste by design, so the probe must outrun automation, a documented retention rationale filed where cleanup decisions happen, this reference being exactly that filing.

**Revision decision.** Add the four rows with probes, deletion-attempt monitoring for pruning, tag checks for blur, paired delta re-verification for rot, and periodic compile-or-inspect checks for the example.

**Retained seed evidence.** All four seed failure rows with mitigations remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| failure_mode_name | likely_trigger_condition | required_mitigation_action |
| --- | --- | --- |
| source drift hides truth | external or local guidance changes after the reference was written | refresh public evidence, rerun local corpus gate, and mark stale claims before reuse |
| generic advice escapes review | agent copies plausible best practices without theme-specific verification | block completion until the verification gate names concrete command, reviewer question, or metric |
| runtime contention masks failure | async tasks, locks, or shared state degrade under concurrent load | add contention benchmark, timeout budget, and structured cancellation path |
| panic path reaches production | unwrap or unchecked invariant survives compile and unit tests | require panic audit, Result propagation, and integration failure fixture |

## Retry Backpressure Guidance

**Decision supported.** This section helps decide what a cross-generation doctrine tension is allowed to trigger. The seed corpus-process bullets stand beside what the lineage implies for doctrine pressure, a rule contested in the living generation is relitigated there, not by retreating to the ancestor's fuller text as trump, ancestor content re-enters current practice only through recorded succession, a new revision citing what it restores, and repeated depth consultations on one topic signal the successor compressed too far there, feedback for its next revision rather than license to fork practice across generations.

**Recommended default and causal basis.** Route every cross-generation tension through the living generation's dispute process with the ancestor as evidence, never as verdict. Two live generations invite doctrine shopping, choosing whichever text supports a position, so lineage integrity depends on a single direction of authority for practice and a recorded channel for restoration.

**Operating conditions and assumptions.** Dispute records name which generation supplied which evidence, tags make the appellate role auditable. Dispute-handling across the generational boundary, within-generation dispute routing belongs to the successor's rubric-valve.

**Failure boundary and alternatives.** The erosion path is silent forking, teams practicing from different generations without knowing it, each citing the family, until the shared vocabulary hides divergent rules. Bounded alternatives and recoveries: the successor theme's rubric-valve for scored-rule disputes, the deciding venue this guidance feeds.

**Counterexample, gotchas, and operational comparison.** The mocking example is the tempting exception, teams may adopt it directly since the successor offers no equivalent, that adoption is legitimate precisely because it fills a gap rather than contradicting a successor rule. Good: a dispute over mutex guidance argued with this generation's fuller section as evidence in the successor's rescoring. Bad: a team adopting the legacy thesis as policy because they prefer enumeration. Borderline: seeding a test suite from Pattern 22 directly, gap-filling rather than forum-shopping, record the source.

**Verification, evidence, and uncertainty.** Audit dispute records for evidence-not-verdict use of ancestor text. The succession structure and rubric-valve design read across assignments. No cross-generation dispute has yet been run locally, the arrangement is designed but unexercised.

**Second-order consequence.** The ancestor functions as the family's appellate record rather than a parallel court, its fuller reasoning informs disputes without deciding them, and the successor's rubric remains the deciding venue, an arrangement that preserves depth's value while preventing its weaponization as an authority of convenience.

**Revision decision.** State the response rules, relitigate in the living generation, restore through recorded revision, and read consultation hotspots as compression feedback.

**Retained seed evidence.** All five seed retry and backpressure bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Retry only after the failed verification signal is classified as transient, stale evidence, missing context, or true contradiction.
- Use one bounded retry for stale external evidence refresh, then escalate to a human or a narrower source-specific reference.
- Apply backpressure by stopping new generation or implementation work when source coverage, critique coverage, or verification gates are red.
- For long-running agent workflows, checkpoint after each batch and re-read the current spec before any broad rewrite, commit, push, or destructive operation.
- For distributed execution, assign one owner per generated file or theme batch and require merge-time verification of exact path, heading, and evidence-boundary invariants.

## Observability Checklist

**Decision supported.** This section helps decide which observability surfaces the legacy family requires and where each lives. The seed generic records stand beside this generation's observability content, the same supervision doctrine its successor kept, sidecar health checks, bounded startup waits, restart rules, and log caps, plus this generation's testing-side visibility, mocked IPC tests that make the invoke contract observable in CI without a desktop runtime, and the WebDriver note bounding where end-to-end observation was possible at archive time.

**Recommended default and causal basis.** Maintain both surfaces, supervised runtime children and a mocked contract suite, before calling a Tauri boundary observable. The family's observability story spans runtime and test time, supervised children observable in production and mocked boundaries observable in CI, and this generation is where the test-time half carries its working example.

**Operating conditions and assumptions.** The mocking utilities remain available in the project's stack, archive-dated pending verification. Observability content this generation carries plus this node's own audit records, the successor's supervision framing was covered in its theme.

**Failure boundary and alternatives.** Adopting only the runtime half leaves contract drift invisible until users find it, the mocked layer exists precisely to observe the IPC contract continuously and cheaply. Bounded alternatives and recoveries: the gates theme's subtractive log hygiene for the shipping-time observability checks.

**Counterexample, gotchas, and operational comparison.** The mocked suite observes beliefs, not behavior, a contract change implemented on both ends but not in the mocks passes runtime and fails the suite, treat such failures as documentation drift, not code regression. Good: CI running the mocked contract suite while production sidecars carry health probes and capped logs. Bad: a desktop app whose only observability is end-to-end WebDriver runs. Borderline: mocks maintained only for the riskiest commands, partial coverage honestly labeled beats false completeness.

**Verification, evidence, and uncertainty.** Check projects for both surfaces at sampled boundaries. Patterns 11 and 22 read verbatim this session. Current mocking-API availability is unverified locally.

**Second-order consequence.** Mocked boundary tests are observability infrastructure wearing test clothing, each mockIPC handler is a checkpoint recording what the frontend believes the contract is, so a full mocked suite is a machine-checked contract inventory, documentation that fails when it lies, the strongest observability property a desktop IPC surface can have.

**Revision decision.** State the two-sided model, runtime supervision surfaces and test-time contract surfaces, with the mocking example as the test-time anchor.

**Retained seed evidence.** All six seed observability bullets remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- Record which local sources were loaded and which were intentionally skipped.
- Record the exact verification command, reviewer question, or rendered artifact used as proof.
- Record p50/p95/p99 latency or reviewer-time measurements when the reference affects runtime or workflow speed.
- Capture error count, timeout count, retry count, saturation signal, and rollback trigger.
- Record leading indicator and failure signal from this file in the coverage manifest or journal when the reference drives real work.
- Keep audit evidence small enough to review: command output summary, changed-file list, and unresolved-risk notes are preferred over raw transcript dumps.

## Performance Verification Method

**Decision supported.** This section helps decide what performance evidence the legacy doctrine demands and how it aged. The seed latency packet stands beside this generation's performance stance, structurally identical to what the successor kept, async commands guarding the UI thread, channels for sustained streams, bounded store payloads with blobs out of band, and bounded setup work, with this generation adding the fuller sections where each rule's stall scenario is spelled out rather than implied.

**Recommended default and causal basis.** Apply the structural rules as correctness during design and route quantitative questions to stated thresholds per the family's constant. The performance doctrine survived succession intact because it was already structural, rules about coupling shape rather than numbers compress losslessly, exactly the survival pattern the trade-off section predicts.

**Operating conditions and assumptions.** Payload and workload estimates are honest, structural routing needs scale intent as ever. This generation's structural performance rules and their succession, measurement method belongs to the system design theme.

**Failure boundary and alternatives.** Reading this generation for benchmarks finds none anywhere in the lineage, both generations gate performance structurally and defer measurement to stated requirements, a family-wide constant. Bounded alternatives and recoveries: profiling-first review, correct after structure passes and wrong as a substitute, unchanged across generations.

**Counterexample, gotchas, and operational comparison.** This generation's fuller sections include specific stall anecdotes that read as measurements but are illustrations, quoting their numbers as benchmarks manufactures data the family never claimed. Good: citing the structural rules from either generation with the constant noted. Bad: extracting stall-anecdote numbers as performance baselines. Borderline: using this generation's fuller scenarios to teach the successor's terse rules, the intended layering, name the generations.

**Verification, evidence, and uncertainty.** Sample performance discussions for structural-first framing with generation tags. The performance-relevant patterns compared across both generations. Quantitative thresholds remain unquantified everywhere in the family.

**Second-order consequence.** The doctrine's performance rules are all phrased as protections for a single user-perceived property, responsiveness, never throughput, and this framing survived both generations untouched, suggesting the author regarded desktop performance as a UX correctness matter from the start, which is why it lives in a reliability doctrine at all.

**Revision decision.** State the structural rules with their inheritance status and this generation's fuller stall scenarios as the teaching layer.

**Retained seed evidence.** The method, indicator, measurement packet, and pass and fail lines remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Performance method: require memory, panic, and concurrency evidence; hot-path operations need p95 under 10 ms locally or an explicit benchmark exception.
Leading indicator to measure: the feature can be tested across frontend, Rust command, and packaged desktop behavior.
Failure signal to watch: the reference ignores permissions, IPC risk, or platform-specific verification.
Required measurement packet: capture input size, source count, tool-call count, wall-clock time, p50/p95/p99 latency where runtime applies, and reviewer decision time where documentation applies.
Pass condition: the reference must let a reviewer identify the correct next action, verification gate, and stop condition without reading unrelated files.
Fail condition: the reference encourages implementation before the workload, reliability target, and failure-mode table are explicit.

## Scale Boundary Statement

**Decision supported.** This section helps decide at what scope this generation speaks and how its boundary behaved under succession. The seed task-splitting bounds stand beside this generation's own scale limits, the boundary notes scope the skill as partial guidance for plugin-author internals, mobile-only APIs, and non-Tauri desktop shells, the same three exclusions the successor's final thesis restates, plus this generation's respect-the-repo rule, preserve an established permission model or plugin stack unless it is broader than needed.

**Recommended default and causal basis.** Apply the doctrine within its three exclusions, respect established repo models, and tighten permissions only in the loosening-forbidden direction. The exclusions held constant across generations because they mark where the author's evidence ended, not where fashion pointed, scope drawn by research coverage survives repackaging.

**Operating conditions and assumptions.** The repo's existing model is actually established, a half-adopted permission scheme is not a model to respect. Scale limits of this generation's authority, this evolution's own task splitting keeps the seed paragraphs.

**Failure boundary and alternatives.** The respect-the-repo rule's exception clause is the subtle boundary, unless broader than needed licenses tightening but never loosening, misreading it as symmetric turns a scale bound into a refactoring license. Bounded alternatives and recoveries: greenfield-only doctrine adoption, simpler and irrelevant to most real desktop codebases.

**Counterexample, gotchas, and operational comparison.** The exclusions are absences of evidence, not warnings of danger, the same reading discipline the successor theme documented, silence beats improvisation in excluded territory. Good: adopting the doctrine in a brownfield app while preserving its narrower-than-doctrine permission model. Bad: widening an established scheme to match a doctrine example. Borderline: replacing a repo's ad hoc plugin bridging with official plugins, the doctrine prefers it, the repo-respect rule demands the change be argued, not assumed.

**Verification, evidence, and uncertainty.** At each audit, check adoption decisions against the exclusions and the asymmetric exception. The boundary notes and successor thesis compared this session. How the exclusions map to post-archive Tauri surface growth is unknowable locally.

**Second-order consequence.** The repo-respect rule scales the skill socially rather than technically, it bounds how much of an existing codebase the doctrine may claim, making the skill safe to adopt incrementally in brownfield projects, and its survival into the successor's gates confirms brownfield adoption was the expected deployment mode all along.

**Revision decision.** State the three constant exclusions, the respect-the-repo rule with its asymmetric exception, and the boundary's stability across succession.

**Retained seed evidence.** All four seed scale-boundary paragraphs remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

Tauri Legacy Coder Patterns stops being sufficient when the task spans multiple independent systems, more than one ownership boundary, unbounded source discovery, or production traffic without an explicit SLO.
Under distributed execution, split work by theme file and preserve one verification owner per split; do not let parallel agents rewrite the same reference without a merge checkpoint.
Under long-running agent workflows, treat context drift as a reliability failure: checkpoint state, summarize open risks, and verify against the spec before continuing.
Under large-codebase scale, require dependency or source-graph narrowing before applying this reference; a source list without ranked canonical guidance is not enough.

## Future Refresh Search Queries

**Decision supported.** This section helps decide which probes reveal decay in this node's claims and on what clocks. The seed theme-name queries would surface generic Tauri chatter while this node's staleness has exact homes, the four checksums and the cross-sweep twin equality, the successor's checksum as the delta-rot early warning, the sweep inventory that could gain a third generation, the mocking API's compile-currency at external windows, and the WebDriver platform-support claim's currency.

**Recommended default and causal basis.** Run the relational probes each cycle and the external checks at windows, watching the successor first. A lineage theme decays relationally, its facts are mostly about the gap between two frozen files, so its refresh probes watch both files and the space between them rather than any topic in the world.

**Operating conditions and assumptions.** External verification windows occur, without them API and platform claims stay archive-dated. Refreshing this node's evidence and labels, live project practice needs no refresh from here.

**Failure boundary and alternatives.** Undirected refreshing would miss a third generation landing in a future sweep, the single event that would most reshape this theme's routing and delta claims. Bounded alternatives and recoveries: declaring the theme historical wholesale, self-defeating for a theme whose function is precisely curated history.

**Counterexample, gotchas, and operational comparison.** A new generation appearing would not update this reference's text automatically, the probe's finding triggers a re-evolution of routing and deltas, never an in-place patch of frozen claims. Good: a refresh logging five checksums, no new generations, and a dated mocking-import inspection. Bad: logging that Tauri legacy patterns searches returned nothing new. Borderline: probing only the successor's checksum in a quick cycle, the highest-value single check, note the narrowed scope.

**Verification, evidence, and uncertainty.** Record each probe with its date and the claim it confirmed or updated. The relational fact structure established across this document. Future sweep contents and Tauri platform evolution are what the probes exist to learn.

**Second-order consequence.** The successor-checksum probe inverts normal ownership, this theme's most important refresh signal lives in a file another theme owns, formalizing that lineage themes must subscribe to their living relative's changes, a subscription the paired-audit design already implements.

**Revision decision.** Replace name queries with targeted probes, the four-checksum run plus successor checksum each cycle, sweep scans for tauri-coder or tauri-executable successors, a compile-or-inspect check on the mocking imports at external windows, and a WebDriver support currency note when docs are reachable.

**Retained seed evidence.** The three seed query rows remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

| search_query_label_name | search_query_text_value |
| --- | --- |
| `official_docs_query_phrase` | tauri legacy coder patterns official documentation best practices |
| `github_repository_query_phrase` | tauri legacy coder patterns GitHub repository examples |
| `release_notes_query_phrase` | tauri legacy coder patterns changelog release notes migration |

## Evidence Boundary Notes

**Decision supported.** This section helps decide under what label, chain, and state each claim class here may be reused. The seed label definitions stand without this assignment's distinctive ledger, four mapped paths resolving to three documents, all read in full, cross-sweep twin equality checksum-verified, the successor doctrine held from assignment 61 for every delta claim, zero external URLs fetched, the mocking code inspected but never executed, and no Tauri application built or tested this session.

**Recommended default and causal basis.** Before reusing a claim, declare it per-file fact, tagged quotation, shared pedigree, or comparative finding, and treat unlabeled claims as inference. A lineage review's evidence adds a relational stratum to the usual three, same-session local facts about each file, quoted doctrine from each generation, declared pedigree shared across them, and now comparative facts, deltas verified by holding both texts simultaneously, the stratum unique to succession themes.

**Operating conditions and assumptions.** Both generations stay unedited at their paths and labels persist through reuse, watched by the relational probes. Reuse rules for this document's claims, each claim travels with class, generation tag, and verification state.

**Failure boundary and alternatives.** The costliest mislabel would present a delta as a quote, the successor dropped the thesis enumeration is this corpus's comparison, not either file's statement, and attributing it to the texts fabricates a self-awareness the files lack. Bounded alternatives and recoveries: executing the mocking example against a sample project to earn a demonstrated stratum, declined for scope, queued as the natural verification upgrade.

**Counterexample, gotchas, and operational comparison.** The mocking code's correctness is undemonstrated despite its concreteness, inspected code reads as verified code, but no vitest run occurred, the example is quoted doctrine in executable costume. Good: reusing the twin equality as per-file fact and the Pattern 22 drop as a comparative finding. Bad: citing the succession deltas as statements the files make about themselves. Borderline: reusing the five-boundary thesis as current family doctrine, it is tagged legacy quotation, the successor reframed it.

**Verification, evidence, and uncertainty.** Sample claims from earlier sections and confirm each declares its class cleanly. This assignment's ledger, full reads, five checksums counting the successor, zero fetches, zero executions. The comparative stratum's lifetime depends on both files' future stability.

**Second-order consequence.** Comparative findings are simultaneously the theme's most valuable and most fragile stratum, verified against two frozen texts they are hard local facts today, but they alone decay when either file changes, so the ledger's strata differ not in current confidence but in decay coupling, a distinction flat labeling schemes cannot express.

**Revision decision.** Publish the four strata explicitly, per-file structural facts, generation-tagged quoted doctrine, shared declared pedigree, and comparative findings labeled as this evolution's analysis.

**Retained seed evidence.** The three labeled boundary definitions remain preserved. The original source facts, examples, and tables follow so that the evolved guidance remains auditable.

- `local_corpus_sourced_fact`: statements tied directly to the local source paths above.
- `external_research_sourced_fact`: statements tied to the public URLs above.
- `combined_evidence_inference_note`: synthesis that combines local and public evidence into agent guidance.
