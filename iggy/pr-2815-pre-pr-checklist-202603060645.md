# PR-2815 MongoDB Sink Pre-PR Checklist

Generated: 2026-03-06 06:45 (Asia/Kolkata)  
Branch: `codex/2739-sink-sync`  
Spec baseline: `/Users/amuldotexe/Desktop/agent-room-of-requirements/iggy/pr-2815-mongodb-sink-executable-spec-202603060705.md`

## Scope Checked
- Working tree diff against branch tip.
- Executable spec requirements `REQ-MDB-001.1` to `REQ-MDB-009.1`.
- Rust quality gates and MongoDB sink integration tests.

## Command Evidence
- `cargo fmt --all -- --check` -> pass
- `cargo clippy -p iggy_connector_mongodb_sink --all-targets --all-features -- -D warnings` -> pass
- `cargo test -p iggy_connector_mongodb_sink` -> pass (13 passed, 0 failed)
- `cargo test -p integration --test mod connectors::mongodb::mongodb_sink::` -> pass (10 passed, 0 failed)
- `cargo machete --with-metadata` -> fail for sink crate (`dashmap`, `once_cell` reported unused)
- `cargo sort --check --workspace` -> pass
- `cargo test --locked --doc` -> pass
- `cargo doc --no-deps --all-features --quiet` -> pass (non-blocking rustdoc warnings in unrelated crates)
- `./scripts/ci/trailing-whitespace.sh --check --ci` -> pass
- `./scripts/ci/trailing-newline.sh --check --ci` -> pass
- `./scripts/ci/taplo.sh --check --ci` -> pass
- `./scripts/ci/markdownlint.sh --check` -> pass
- `./scripts/ci/sync-rust-version.sh --check` -> pass
- `./scripts/ci/python-version-sync.sh --check` -> pass
- `./scripts/ci/licenses-list.sh --check` -> pass
- `./scripts/ci/license-headers.sh --check` -> fail in dirty workspace due generated `test_logs/**/current_config.toml` files lacking headers (non-source artifacts)

## Pickups from Existing Sync Docs

Source docs reviewed:
- `/Users/amuldotexe/Desktop/agent-room-of-requirements/iggy/docs-2739/issue-2739-pr-preparation.md`
- `/Users/amuldotexe/Desktop/agent-room-of-requirements/iggy/docs-2739/issue-2739-e2e-testing-learnings.md`
- `/Users/amuldotexe/Desktop/agent-room-of-requirements/iggy/docs-2739/tdd-state-mongodb-e2e-tests.md`

Reusable checklist items adopted for PR-2815:
1. Run repo-level CI scripts in addition to crate-level Rust checks.
2. Include `cargo sort`, docs build, and doctest gates in pre-PR validation.
3. Keep explicit PR body quality gates (`Which issue`, rationale, local execution evidence).
4. Verify connector runtime binary path assumptions in integration fixtures.
5. Ensure MongoDB sink integration is executed against a real Docker-backed MongoDB (not compile-only).

## Requirement Compliance Matrix

| Requirement | Status | Implementation Evidence | Test Evidence | Notes |
| --- | --- | --- | --- | --- |
| REQ-MDB-001.0 Idempotent duplicate handling | PASS | Duplicate-only insert errors are treated as success via `count_duplicate_write_errors` and success path in unordered insert flow (`core/connectors/sinks/mongodb_sink/src/lib.rs`) | `duplicate_key_is_explicit_failure_and_not_silent_success` now expects idempotent success and passes (`core/integration/tests/connectors/mongodb/mongodb_sink.rs`) | Docs remain single per `_id` asserted in integration test |
| REQ-MDB-002.0 Unordered bulk progress | PASS | `insert_many(...).ordered(false)` is used; inserted count computed even with duplicate write errors (`core/connectors/sinks/mongodb_sink/src/lib.rs`) | Duplicate mixed-batch integration tests pass | Error handling splits duplicate vs transient/non-transient paths |
| REQ-MDB-003.0 Composite `_id` uniqueness | PARTIAL | `_id` built as `"{stream}:{topic}:{partition}:{message_id}"` via `build_composite_document_id` (`core/connectors/sinks/mongodb_sink/src/lib.rs`) | Existing Mongo integration tests pass with new `_id` format | No explicit cross-topic collision test found yet |
| REQ-MDB-004.0 Offset progress safety with no runtime change | PASS | Duplicate batches return `Ok`; runtime file is not in diff (`git diff --name-only` excludes `core/connectors/runtime/src/sink.rs`) | Duplicate/idempotency integration tests pass with sink staying running | Meets “No runtime change” constraint |
| REQ-MDB-005.0 Numeric range safety | PARTIAL | Offset conversion uses `i64::try_from(...)` else `iggy_offset_str`; partition stored as checked `i32` else `i64` via `build_partition_metadata_value` (`core/connectors/sinks/mongodb_sink/src/lib.rs`) | No dedicated unit test asserting `iggy_offset_str` fallback and partition range behavior | Implementation present; explicit tests missing vs spec matrix |
| REQ-MDB-006.0 Config normalization and warnings | PARTIAL | Config values cached at `new()` (`batch_size`, include flags, payload format, retry settings). Unknown payload format triggers `warn!` and defaults to binary (`core/connectors/sinks/mongodb_sink/src/lib.rs`) | Payload format and retry config unit tests pass | No assertion yet for warning emission or full include-flag normalization matrix |
| REQ-MDB-007.0 Dependency hygiene | FAIL | `cargo-machete` ignore block removed from sink `Cargo.toml` | `cargo machete --with-metadata` reports `dashmap` and `once_cell` unused in sink crate | Blocker before PR under this spec |
| REQ-MDB-008.0 Accurate counters without locks | PASS | `Mutex<State>` replaced with `AtomicU64` counters; processed count increments by successful inserts only (`core/connectors/sinks/mongodb_sink/src/lib.rs`) | Sink unit tests pass including zero/initial counter checks | Duplicate-excluded processed accounting implemented in sink internals |
| REQ-MDB-009.0 Retry with backoff | PASS | Transient classification + linear backoff (`sleep(retry_delay * attempts)`) with max retries and final error propagation (`core/connectors/sinks/mongodb_sink/src/lib.rs`) | Retry/failover integration tests pass (`retryable_write_failover...`, `no_writes_performed...`, timeout path) | Behavior aligns with requirement |

## Current Diff (Pre-PR)
- `Cargo.lock`
- `core/connectors/sinks/mongodb_sink/Cargo.toml`
- `core/connectors/sinks/mongodb_sink/src/lib.rs`
- `core/integration/tests/connectors/mongodb/mongodb_sink.rs`

## Pre-PR Decision
- PR readiness: **NOT READY**
- Blocking items:
  - `REQ-MDB-007.0` unresolved (`cargo machete` reports unused dependencies).
  - `license-headers` script currently fails in this workspace because of generated `test_logs/**` artifacts; run from a clean tree or exclude generated logs before final PR gate.
  - Spec test matrix gaps for `REQ-MDB-003.0`, `REQ-MDB-005.0`, `REQ-MDB-006.0` remain partially covered.

## Suggested Next Actions Before Raising PR
1. Remove/justify unused sink dependencies (`dashmap`, `once_cell`) and re-run `cargo machete --with-metadata`.
2. Add explicit tests for:
   - Cross-topic composite `_id` collision avoidance.
   - Offset range fallback to `iggy_offset_str` and partition range conversion.
   - Unknown payload format warning + include-flag normalization behavior.
3. Re-run the same checklist commands after the above updates and regenerate this pre-PR checklist.
